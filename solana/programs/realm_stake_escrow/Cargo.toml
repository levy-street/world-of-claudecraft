[package]
name = "realm_stake_escrow"
version = "0.1.0"
description = "Non-custodial per-realm $WOC stake escrow (#475)"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "realm_stake_escrow"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build", "anchor-spl/idl-build"]

[dependencies]
anchor-lang = "0.30.1"
anchor-spl = "0.30.1"
