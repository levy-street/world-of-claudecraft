# Masterwrought phase 10 QA captures

Captured at the phase 10 QA (2026-08-16) with `scripts/pr_screenshots.mjs` through the
`masterwrought-phase10-consumables` target (`scripts/pr_shot_targets.mjs`), offline
client, lowest graphics preset, every state raised through the real bound events.

- `after-flask-tooltip-desktop.png`: the Ironhusk Flask tooltip: the shared Use line
  plus the three flask rules, including the ranked clean-slate clause the QA added.
- `after-role-food-tooltip-desktop.png`: the Stonepot Stew tooltip with the Well Fed
  line and its one-effect rule.
- `after-enchant-picker-skill-gated-desktop.png` / `-mobile.png`: the Apply Enchant
  picker at Enchanting 99: every Lucent row inert behind its floor line ("Requires
  Enchanting 100", the Infusion 125), the mobile gate line at the touch size.
- `after-enchant-picker-perfected-only-desktop.png` / `-mobile.png`: the same picker
  at Enchanting 125: the three apex rows actionable, the Lucent Infusion inert behind
  "Only a Perfected item can bear that enchant." (nothing is Perfected before phase 12).
- `after-consumable-tray-six-kinds-mobile.png`: the mobile consumable tray with all six
  kinds competing for six slots; the starter jerky already holds a food slot, so the
  drink is the one shed, the recorded phase 14 residual seen live.

No before shots: the phase shipped without captures, so these are the first evidence
of the surfaces.
