# Phase 19A: Content

Opened 2026-09-01 under ruling qr-19-best-for-project. New and changed content records: item ids, quests, deeds, Reliquary pages, recipes, mob and loot tables. Every new id parks its art for the phase 21 wave and carries its full same-change obligation set.

**18 units, about 48.00 hours estimated.** Every row here was answered in the Phase 19 rulings gate; this phase executes, it does not re-decide. A row whose ruling turns out to rest on a wrong premise is an ESCALATION back to the ledger, never a quiet re-ruling.

## Units

### D002

Already settled: the landed qr-19-crop-accent-cap-refusal ruling (state.md:15961, the block running 15961 to 15968; AMENDED 2026-09-01, the 15902 address was wrong) rules the standing reading and says in terms that "the contingent apex-feast crop-count row stays at 1", so D002 executes as one dated RULED line recording that it is determined, and no code moves.

- **Files:** `docs/prd/masterwrought/state.md`, `src/sim/content/recipes.ts`, `tests/provisioning_supply_line_apex.test.ts`, `tests/apex_feast_craft.test.ts`, `tests/farm_recipes.test.ts`, `src/guide/content.generated.ts`
- **Same-change obligations:** ANCHOR: put the RULED (qr-19-apex-feast-crop-count, 2026-09-01) line AFTER the unique line "expensive as it found it." in state.md (grep -cF = 1; it closes the apex-feast crop paragraph at state.md:15502-15508, under the "## Phase 11k BUILT ledger (2026-08-24, the provisioning capstone)" heading at state.md:15359, which opens THE CROP IS AT ONE RATHER THAN THE ACCENT CAP OF TWO, DELIBERATELY (AMENDED 2026-09-01: the label "Phase 15c" occurs nowhere in state.md outside the Phase 19 decision-table rows, and that opening sentence is NOT unique, grep -cF = 2 counting the table row, so it must never be used as an insertion anchor)). Cross-cite the already landed D004 ruling by its unique tail "open item (10) retires with it, and the contingent apex-feast crop-count row stays at 1." (grep -cF = 1). No handoff-table row exists for this one, so no status cell to flip. SAME-CHANGE OBLIGATIONS ON THE DETERMINED ANSWER: none at all. No wiki regen, no M16 non-Latin fills, no deed row, no Reliquary page, no art park, no census row, no parity golden, no monolith ceiling, R5 untouched. FOR REFERENCE ONLY, the price if the maintainer instead reverses D004 and raises the crop to 2: evergarden_greens count 1 to 2 on all three byte-identical apex feast rows (src/sim/content/recipes.ts, recipe_stonepot_feast at :3373, recipe_warspice_feast at :3395, recipe_sageleaf_feast at :3418), each row's "Input 510 vs output 300" authoring comment re-derived rather than deleted, the block-header derivation comment at src/sim/content/recipes.ts:3320-3330, which states the 80-against-56 arithmetic and names the census, and the refused-under-count-reading census at tests/provisioning_supply_line.test.ts:801-813 (the six-entry literal at :801-808, the equality at :809, the four-row checksum at :810-813). AMENDED IN PLACE 2026-09-01, the earlier price was wrong three ways and was never measured: tests/apex_feast_craft.test.ts carries NO input-value pin and NO longest-bill pin at all (it drives a real Sim craft and reads reagent counts generically at :68), tests/farm_recipes.test.ts holds no 510 anywhere and its input-value pin is the PARTY feast recipe_harvest_feast (:58, :865, :937) with its greens pin at :924 also the party feast, while its apex-feast references at :386-391, :417-419 and :450-455 are id membership lists a reagent COUNT cannot move, and the accent census is NOT in tests/provisioning_supply_line_apex.test.ts (whose APEX_ROWS table pins the three apex feasts OUT at :402-411 as the rows 11h did not touch; its recipe_laden_hearth produce block at :359-361 is real and is the correct SHAPE reference, but it is the hearth's row, not a feast row). The two longest-bill pins, tests/guide.test.ts:2070 and tests/provisioning_supply_line_apex.test.ts:465 and :477, both derive r.reagents.length and so cannot move on a count change either, and npm run wiki:content because src/guide/content.generated.ts:10149 carries the bill, freshness-gated by tests/guide.test.ts. That arm would also contradict a landed qr-19 ruling, so it is an escalation and not an execution.
- **Reviewers:** qa-checklist
- **Depends on:** nothing
- **Estimate:** 0.5h

### D008+D089

> MERGED UNIT: D008 rules the obtainability basis names the wield rung and D089 amends the acceptance wording to the composed floor. They are one edit and were cyclic when split.

Rule that obtainability rulings name the hoe wield rung beside farmCropSkillThreshold, amend the 11h-GATE-B basis text and its 11i/11j/11k heir records, and audit every player-facing surface that states what a crop or a catch asks of a player. PLUS Amend the 11h-GATE-B crop obtainability acceptance wording (and its 11i, 11j and 11k heirs) to name the composed floor max(farmCropSkillThreshold(tier), wieldRequirementForTier(tier)) instead of the profession threshold alone, a wording change with no behavior change and no pins moving.

- **Files:** `docs/prd/masterwrought/state.md`, `docs/prd/masterwrought/farming/state.md`, `src/ui/gather_tool_tooltip.ts`, `src/ui/hud/professions/farming_plant_sheet_view.ts`, `src/ui/hud/professions/cooking_catch_hint_view.ts`, `src/sim/professions/wield_gate.ts`, `src/sim/content/farm_crops.ts`, `tests/masterwrought_budget.test.ts`, `src/sim/content/recipes.ts`, `tests/provisioning_supply_line_apex.test.ts`
- **Same-change obligations:** ANCHORS, both verified unique. (1) The handoff statement: put the RULED (qr-19-tool-gate-obtainability-ruling, 2026-09-01) line AFTER the wrapped continuation line "  wieldRequirementForTier as well as the profession threshold." in state.md (grep -cF with the two leading spaces = 1; the bare sentence without them also matches the D008 table row, so the leading spaces are load-bearing). (2) The open-item-6 statement to amend in the same edit: state.md line "rung is the maintainer's call; the arm now measures it either way." (grep -cF = 1), which closes the (6) THE TOOL GATE IS NOT IN ANY OBTAINABILITY RULING block. (3) The farming twin: the Phase 11h QA note heading "## Phase 11h QA note (2026-08-22): the tool gate is the third plant gate, and no ruling names it" in farming/state.md. No handoff-table row carries this one, so there is no status cell to flip. AUDIT RESULT, pre-run here so the price is honest: every live player-facing obtainability surface ALREADY composes both gates. src/ui/gather_tool_tooltip.ts:168 calls wieldRequirementForTier directly; src/ui/hud/professions/farming_plant_sheet_view.ts:136 resolves through bestWieldableGatherToolTierOrNone and its header states the three-gate contract; the composition is already pinned in tests at tests/farming_plant_sheet_view.test.ts:32 and tests/farming_plant_sheet_window.test.ts:38 as Math.max(farmCropSkillThreshold(t), wieldRequirementForTier(t)); the catch hint (cooking_catch_hint_view.ts) states purpose only and no requirement; and neither src/guide nor scripts references either gate, so the wiki states no floor. So the expected execution is DOCS ONLY. SAME-CHANGE OBLIGATIONS: none. No wiki regen, no new t() key and so no M16 fills, no deed row, no Reliquary page, no committed WebP, no census row, no parity golden, no world_api parity member, no monolith ceiling, R5 untouched. LAND IT WITH D089, which is the same decision seen from the crop-wording end (it prices the identical amendment against the farming three-gate block at farming/state.md:4590 onward); ruling them apart would put the packet's two records on two authorities again, which is exactly what this row exists to stop.   THE ONE THING THE PARENT MUST NOT MISS: D089 AND D008 ARE THE SAME QUESTION, PRICED TWICE, on two sides of one record. D008 (tool-gate-obtainability-ruling) asks it from state.md open item 6 and prices the state.md heir records plus a copy audit; D089 asks it from the farming/state.md 11h audit and prices the farming ledger wording plus test comments. Both recommend naming the wield rung. Answer them together and execute them as ONE commit, or the two ledgers end up ruled by two ids on one question. Recommend the merged commit carry qr-19-obtainability-ruling-wield-rung and the D008 row be amended in place to point at it. RULING RECORD, two anchors, both verified unique with grep -c = 1. Farming side: "rung as well is farming's call, not a QA session's; the arm measures" in farming/state.md, RULED line AFTER it at column 0, closing the FOR THE MAINTAINER paragraph of the "THE PLANT PATH RUNS THREE GATES, NOT TWO" block. Masterwrought side: "rung is the maintainer's call; the arm now measures it either way." in state.md, RULED line AFTER it, closing open item 6. No handoff table row on either side, so no status cell to flip. THE HEIR WORDINGS the amendment must reach, since 11i, 11j and 11k all inherit GATE-B's text: two live restatements in shipped source at tests/masterwrought_budget.test.ts:574 and src/sim/content/recipes.ts:2979, both citing 11h-GATE-B by name, plus the obtainability sweep arm in tests/provisioning_supply_line_apex.test.ts, which the 11h audit records as already measuring BOTH gates (so nothing red follows from the amendment). BEHAVIOR RE-VERIFIED THIS SESSION: src/sim/professions/farming.ts:675 calls bestWieldableGatherToolTierOrNone(meta.inventory, 'farming', skill, ITEMS), and src/sim/professions/wield_gate.ts drops any hoe whose wield requirement exceeds the player's own counter, so the real plant floor is the composition, tier 3 at max(50, 70) = 70 and tier 4 at max(75, 85) = 85, the hoe binding on both tiers. CENSUS ROWS, wiki regen, M16 fills, deed rows, Reliquary pages, art parks, parity goldens and monolith ceilings: none, this is wording only. Reviewer: content-obligations-reviewer, because the wording is a content acceptance basis and the two restatements sit in src/sim/content and tests. Merged D008 plus D089 execution 2.5h; if the maintainer instead keeps the wording as-is it collapses to two RULED lines at 1h, and the derivation stays true-but-partial on the record.
- **Reviewers:** qa-checklist, frontend-seam-reviewer
- **Depends on:** nothing
- **Estimate:** 5h

### D009+D170

> MERGED UNIT: D009 authors the starter-zone gray drop and D170 is col_junk_drawer margin, which that drop is the source for. Cyclic when split; the item must exist before the margin is re-derived.

Restore a starter-zone gray drop so the Sell Junk loop the guide teaches works before zone 2, on both the zone-1 and the Proving Shore kill tables. PLUS Source the three routeless Brightwood Glade wildlife items (amber_hide, soft_down, stag_antler) onto zone-1 kill tables so col_junk_drawer's reachable poor pool rises from thirteen to sixteen, lifting the margin without retro-editing a shipped trigger, 18 locale overlays, or the append-only deed catalog proof.

- **Files:** `src/sim/content/items.ts`, `src/sim/content/zone1.ts`, `src/sim/content/proving_shore.ts`, `src/ui/icons.ts`, `src/ui/i18n.catalog/items.ts`, `tests/deeds_content.test.ts`, `tests/shipped_item_ids.golden.json`, `scripts/item_art_audit.mjs`, `src/guide/content.generated.ts`, `docs/prd/masterwrought/state.md`, `docs/prd/masterwrought/farming/state.md`, `tests/material_taxonomy.test.ts`, `docs/prd/masterwrought/progress.md`
- **Same-change obligations:** ANCHORS. State.md: put the RULED (qr-19-zone1-no-poor-drop, 2026-09-01) line AFTER the unique line "the zone-1 no-poor-drop row now spans both starter zones since the island's" (grep -cF = 1), inside the thirteen-carried-in paragraph; the originating paragraph is anchored by "This QA ADDS ONE:" (grep -cF = 1) if the ruling is preferred there. HANDOFF ROW to flip, farming/state.md line 625, match on the unique prefix "  Zone 1 (Eastbrook Vale, levels 1 to 7) carries NO poor mob drop since 11l" (grep -cF = 1); its status cell reads "open ruling-owed (opened 2026-08-24)" and the replacement cell is "CLOSED 2026-09-01 by qr-19-zone1-no-poor-drop (see the ledger line)". THE ROW'S PRICE IS WRONG AND OWES AN AMENDMENT IN PLACE, dated 2026-09-01: the row prices only "a new item id plus committed WebP art", but three poor ids ALREADY EXIST for exactly this place and are pinned as unreachable. src/sim/content/items.ts:2748 opens a block commented "--- Brightwood Glade wildlife pack ---" holding soft_down, amber_hide and stag_antler, all quality poor, all already art-backed (public/ui/items/soft_down.webp and siblings, ITEM_IMAGE_IDS at src/ui/icons.ts:5432), all already named in every locale, and all pinned as having NO acquisition route at tests/deeds_content.test.ts:2216. Brightwood Glade IS a zone-1 point (src/sim/content/zone1.ts:178, inside eastbrook_vale). So the cheapest and safest shape is to give one or more of those three a faucet on a zone-1 mob table rather than mint a fourth id: it costs zero new ids, zero art, zero i18n, zero golden movement, zero art-audit re-mint. OBLIGATIONS OF THE MINIMAL SHAPE: loot rows in src/sim/content/zone1.ts (the shape is { itemId, chance } beside wolf_fang at :253) and in src/sim/content/proving_shore.ts (whose only killable mobs today are training_effigy and shore_scuttler at copper-only loot and mister_crabs at quest loot, so the island genuinely has no gray); tests/deeds_content.test.ts:2190-2211 re-derived (the arm titled 'the reachable poor set is exactly the thirteen survivors' plus its literal list) and :2212-2216 re-derived (the arm asserting the unreachable remainder is exactly the Brightwood pack), with the margin comment at :2223 corrected rather than deleted; npm run wiki:content because the item pages gain a drop source, freshness-gated by tests/guide.test.ts; a new reachability arm proving the drop is lootable in zone 1 and on the island and that sellAllJunk sweeps it. NOT owed on the minimal shape: no committed WebP, no ITEM_ART_PENDING move, no M16 non-Latin fills, no shipped_item_ids golden row, no scripts/item_art_audit.mjs literal re-mint, no deed record and no Reliquary page (poor junk is not conquerable content), no world_api parity member, no parity golden, no monolith ceiling. OBLIGATIONS IF THE MAINTAINER INSTEAD MINTS A NEW ID: everything above PLUS a junk def in items.ts (the six-line soft_down shape at :2751), a procedural recipe plus an ITEM_ART_PENDING park row in src/ui/icons.ts (81 to 82; committed art needs the maintainer's master SHA per the profession_icons E2 rule, so the park is the only legal in-branch shape), an English name in src/ui/i18n.catalog/items.ts then npm run i18n:gen, M16 non-Latin fills if the name is wordy, a tests/shipped_item_ids.golden.json row, and pendingArtCount 81 re-minted at scripts/item_art_audit.mjs:181 (liveItemCount 1140 at :147 holds, since the def is minted and parked in the same change). IF A NEW MOB IS AUTHORED for the glade, add a src/ui/world_entity_i18n.ts row and a parked mob portrait, which is what makes that shape the expensive one. INTERACTION: this raises the reachable poor pool, so it must be ruled consistently with D170 (col_junk_drawer's three of margin); the live assertion at tests/deeds_content.test.ts:2240 is toBeLessThanOrEqual so it stays green, but the two exact-set arms above do not.   MAINTAINER INPUT: the drop chances for the three items are a design call the maintainer may name or delegate, and the shape choice (loot rows on the existing forest_wolf and wild_boar templates, or a new wildlife carrier if the antler and the down want a stag or a bird of their own) rides with it. SAME-CHANGE OWED: flip the reachable-set pin in tests/deeds_content.test.ts from thirteen to sixteen and either empty or retire the unreachable-pack arm; re-run npm run wiki:content as run-and-verify (the generated bundle carries no mob drop table today, so the freshness gate in tests/guide.test.ts settles it); confirm the quality pin in tests/material_taxonomy.test.ts stays at sixteen (it is a quality pin, not a route pin, so it should not move). NOT OWED: no new item id, so NO committed WebP art (all three .webp files already ship), no world_entity name additions, no i18n or M16 fills, no deed record (the trigger never moves, so the frozen catalog digest does not budge), no Reliquary page, no census row, no parity golden, no world_api member, no monolith ceiling. INTERACTION: Brightwood Glade is a zone-1 POI, so these same rows close D009's affordance gap outright, which is why D009 must be answered first or in the same sitting. REVIEWER NOTE: architecture-reviewer is skipped by the matrix for a pure content-table edit, but spawn it if a NEW carrier mob template is authored. ANCHOR (grep -c = 1) in /Users/fernando/Documents/wocc-masterwrought/docs/prd/masterwrought/state.md: 'still three of margin: the live poor set is sixteen, thirteen reachable;'; the dated RULED line goes AFTER that paragraph's last line. HANDOFF ROW in farming/state.md: match the row opening '  `col_junk_drawer` (src/sim/content/deeds.ts, "Discover 10 different poor-quality items", renown 5) has THREE of margin'; its status cell reads 'open ruling-owed (opened 2026-08-24)'. STATUS CELL REPLACEMENT: 'open ruling-owed (opened 2026-08-24)' becomes 'CLOSED 2026-09-01 by ruling qr-19-col-junk-drawer-margin (Phase 19, under qr-19-best-for-project): the trigger stays at 10 and the Brightwood Glade wildlife pack is SOURCED onto the zone-1 kill tables, taking reachable from 13 to 16 and closing D009 with the same rows.'
- **Reviewers:** content-obligations-reviewer, test-coverage-auditor, frontend-seam-reviewer, qa-checklist
- **Depends on:** D010
- **Estimate:** 12h

### D010

> MOVED to the front of 19A: it is a one-hour ledger record that D009, D013, D027 and D077 all wait on, and leaving it in ROUTE-OUT made four rows depend on a wave that never runs.

Schedule the parked art batch as the maintainer's own post-merge wave on his master SHA, which is the only place committed art can be minted, so nothing but the ledger and the handoff row moves in this branch.

- **Files:** `docs/prd/masterwrought/farming/state.md`, `docs/prd/masterwrought/state.md`, `src/ui/icons.ts`, `docs/achievements/icon-brief.md`
- **Same-change obligations:** ANCHOR: put the RULED (qr-19-art-batch-ledger, 2026-09-01) line AFTER the unique line "3. THE ART BATCH HANDOFF. The deliverable here is THIS LEDGER, never" in farming/state.md (grep -cF = 1). HANDOFF ROW to flip, farming/state.md line 505, match on the unique prefix "  The art batch ledger (gates block item 3: ITEM_ART_PENDING 44" (grep -cF = 1); its status cell reads "handed-to-maintainer" and the replacement cell is "SCHEDULED 2026-09-01 by qr-19-art-batch-ledger: the wave runs on the maintainer's master SHA after the merge; stays handed-to-maintainer as his queue item". TWO FIGURES IN THE ROW AND THE LEDGER ARE STALE AND OWE AN AMENDMENT IN PLACE, dated 2026-09-01, never a rewrite: the row's "ITEM_ART_PENDING 44" and "DEED_ART_PENDING 8 plus crest replacement" read 81 and 20 in the live tree (counted here: src/ui/icons.ts:5825 holds 81 ids, src/ui/icons.ts:5963 holds 20 rows), and the D010 table row's own "DEED_ART_PENDING 15 rows" is also stale at 20. The extra rows are not all the packet's: the release merges brought exp_the_last_keep and exp_dawnhold_castle, the bank-storage pair soc_strongbox_outfitter and soc_four_bags_deep, prog_ready_for_an_adventure, prog_legendmaker, and the five Crucible deeds dgn_ignivar, dgn_ignivar_heroic, dgn_varkhul, dgn_varkhul_heroic and dgn_varkhul_flawless, so the amendment should say the wave's scope is now wider than this packet. SAME-CHANGE OBLIGATIONS: none in code. No wiki regen, no M16 fills, no deed record, no Reliquary page, no new art park (the parks ARE the deliverable and stay exactly as they are), no census row, no parity golden, no monolith ceiling, R5 untouched. The judgment sub-items ride the same scheduling line so they are not lost: the cross-family icon reads (highwatch_barley_porridge reading alchemy, fenbridge_rice_pudding reading currency), the four hoe icons and the glazed-carrots-vs-pottage 32px legibility eyeballs, and the map-pin colour unification, which per D025 is now only about the painted pin.
- **Reviewers:** qa-checklist
- **Depends on:** nothing
- **Estimate:** 1h

### D038

Convert the sunder lock-exemption from a merge fiat into a ruling: a player-locked raid epic can still be deliberately sundered, consistent with disenchant, or the two mutually-naming twin pins flip together and a lock deny lands in sunderAdmitted plus the id-only victim ladder.

- **Files:** `docs/prd/masterwrought/state.md`, `tests/bag_item_context_menu.test.ts`, `tests/masterwrought_materials.test.ts`, `src/sim/professions/sundering.ts`, `src/sim/professions/enchanting.ts`, `src/ui/bag_item_context_menu.ts`
- **Same-change obligations:** RECOMMENDED ARM (ratify lock-exempt): no behavior change; the only code-adjacent edits are two test-comment amendments. ANCHOR (state.md, grep -c = 1): 'ratified deny must cover victim selection too.', the last line of the RULING WANTED bullet; the dated RULED line goes AFTER it, house form 'RULED (qr-19-sunder-lock-exemption-ratification, 2026-09-01, under qr-19-best-for-project): ...'. TWIN PIN AMENDMENTS, both owed in the same commit because each comment currently says the classification was 'recorded for maintainer ratification' and that sentence is now false: tests/bag_item_context_menu.test.ts (the 'keeps Sunder on a locked raid epic' case, the comment naming the v0.38.0 sync merge fa51741408) and tests/masterwrought_materials.test.ts (the 'sunders a player-locked copy' case). Each must cite qr-19-sunder-lock-exemption-ratification and each must KEEP its 'if the sim ever gains a lock deny, flip both pins together' sentence, which is the mechanism that makes the pair inseparable. VERIFY BEFORE RULING, live and not from the record: sunderAdmitted in src/sim/professions/sundering.ts has no lock arm, consumePreferredDisenchantVictim in src/sim/professions/enchanting.ts has no lock arm either (so a ratified deny really would have to cover victim selection), and the context menu still offers sunder on a locked copy. No wiki regen, no M16 fills, no deed or Reliquary rows, no art park, no census row, no parity golden, no monolith ceiling. Run tests/bag_item_context_menu.test.ts and tests/masterwrought_materials.test.ts. SCOPE FENCE: this row is NOT seeded row 16 (the Crucible gear-tier sundering admission); the ruling line must say so, or the two will be read as one. FLIP ARM PRICE: roughly 10 hours, and it is a shipped-behavior change touching sim and ui together (lock deny in sunderAdmitted, a lock arm in the id-only victim ladder, context-menu gating, both twin pins flipped, sim tests, both-hosts check), dispatching architecture-reviewer, cross-platform-sync, frontend-seam-reviewer and test-coverage-auditor.
- **Reviewers:** test-coverage-auditor, qa-checklist, architecture-reviewer, frontend-seam-reviewer, cross-platform-sync
- **Depends on:** nothing
- **Estimate:** 2.5h

### D044

Rule whether crafted land tools get Reliquary pages matching the three crafted rods already on the shelf, or whether the land-tool no-row precedent (pick, axe, sickle and hoe all rowless) stands as the recorded cosmetic residual.

- **Files:** `docs/prd/masterwrought/state.md`, `src/sim/content/reliquary.ts`, `tests/reliquary_content.test.ts`, `tests/delve_shop.test.ts`, `src/world_api/reliquary.ts`, `tests/reliquary_cell_art.test.ts`
- **Same-change obligations:** RECOMMENDED ARM (keep the precedent): record only. ANCHOR (state.md, grep -c = 1): 'ruling touches the deed only; the Reliquary decline recorded above keeps its own LAND-tool', the last line of the ALREADY-LANDED qr-19-apex-hoe-deed-decline paragraph in the 11j QA 'RIGHT FOR THE WRONG REASON' block; the dated RULED line goes AFTER that paragraph, house form 'RULED (qr-19-apex-hoe-reliquary-decline, 2026-09-01, under qr-19-best-for-project): ...'. The sibling deed row D043 is already ruled with exactly the symmetry argument, so this line must land after it and read as its Reliquary twin, not restate it. SECOND EDIT owed in the same commit: the NO HOE PAGE comment block in src/sim/content/reliquary.ts (inside the delve-spoils shelf's relics list) currently records the decline as an unruled residual and names the DOC_RELICS growth an author would owe; amend it to cite the ruling id. No wiki regen, no M16 fills, no new deed rows, no art park move, no census row, no parity golden, no monolith ceiling under this arm. FLIP ARM PRICE, and the obligation chain is longer than the table's own estimate: authoring one to four pages owes the relic rows plus their acquisition hints in src/sim/content/reliquary.ts, the prose keys and their census rows, tests/reliquary_content.test.ts pins, wiki regen via npm run wiki:content (freshness-gated by tests/guide.test.ts), AND two things the table does not name. First, osmium_hoe sits on a delve counter (src/sim/content/delves/shop.ts, marks 24, gate clears:3), so cataloguing it makes isCataloguedRelicItem true for a stocked id and reds the exact-set DOC_RELICS pin in tests/delve_shop.test.ts, which in turn defends the 'the delve Marks shops sell six catalogued relics' sentence in src/world_api/reliquary.ts:79; both must move together. Second, the hoes carry procedural icons only, so tests/reliquary_cell_art.test.ts requires every procedural relic to be an enumerated ITEM_ART_PENDING member; the hoe ids already are, so the arm passes, but the art park row must be checked, not assumed. That arm is 12 hours and dispatches content-obligations-reviewer plus architecture-reviewer and test-coverage-auditor.
- **Reviewers:** content-obligations-reviewer, qa-checklist, architecture-reviewer, test-coverage-auditor
- **Depends on:** nothing
- **Estimate:** 2h

### D048

Ratify or overturn the silent auto-equip decline at the Masterwrought family cap, where a lootable in-slot upgrade is skipped without a toast because the gate passes empty ignoreSlots on both worn-family rules.

- **Files:** `docs/prd/masterwrought/state.md`, `docs/prd/masterwrought/phase-19-rulings-gate.md`, `src/sim/auto_equip_gate.ts`, `src/sim/sim.ts`, `tests/auto_equip_gate.test.ts`
- **Same-change obligations:** THE ROW'S OWN LIVE-CODE CITE IS STALE AND MUST BE AMENDED IN PLACE, not rewritten. D048 cites 'live code src/sim/sim.ts (the masterwroughtConflictSlot call, empty ignoreSlots, comment at lines 9183-9186)'. That logic was extracted: Sim.maybeAutoEquip now calls autoEquipFamilyConflict, and both the empty-ignoreSlots decision and its stated reason live in src/sim/auto_equip_gate.ts (its docblock says ignoreSlots is empty on BOTH rules by design, because auto-equip has chosen no target yet and so declines rather than reasoning about which worn piece a swap would free). Append a dated line to the D048 row in phase-19-rulings-gate.md, and to its copy in state.md, recording the moved anchor; the behavior is unchanged, only the address. RECOMMENDED ARM (ratify the silent decline): record only. ANCHOR (state.md, grep -c = 1): 'decaying to the cap, which is the worse outcome for the feature.', the last line of the ALREADY-LANDED qr-19-overcap-legacy-swap-freeze paragraph inside the same Phase 01 QA design-notes block; the dated RULED line goes AFTER it. CRITICAL SCOPE POINT: the row's THIRD option (ratify the decline but unfreeze over-cap legacy swaps) is already foreclosed, because D093 ruled the v1 freeze stands and ignoreSlots is not widened for same-family swaps. D048 can therefore only answer the first half, the in-slot upgrade decline, and its line must say so or the two rulings will read as contradicting each other. OPTIONAL SECOND EDIT: cite the ruling id in the auto_equip_gate.ts docblock; that makes the diff a src/sim change and pulls in architecture-reviewer, so decide deliberately rather than by accident. No wiki regen, no M16 fills, no deed or Reliquary rows, no art park, no census row, no parity golden, no monolith ceiling. Run tests/auto_equip_gate.test.ts and tests/masterwrought_cap.test.ts. FLIP ARM PRICE: roughly 10 hours; teaching auto-equip the swap means populating ignoreSlots like the explicit equip path, cap and legacy over-cap test arms, both-worlds parity, and it abandons the unique-equipped-precedent symmetry the docblock pins, so it dispatches architecture-reviewer, cross-platform-sync and test-coverage-auditor.
- **Reviewers:** architecture-reviewer, test-coverage-auditor, qa-checklist, cross-platform-sync
- **Depends on:** nothing
- **Estimate:** 2.5h

### D053

Ratify the shipped harvest-spread substitutes (Sundered Horror carries tusk, horn's floor holds on six) and keep the at-most-one-specimen-less-family-per-corpse capacity premise, or relax the premise to admit dune_troll and frostmane_yeti.

- **Files:** `docs/prd/masterwrought/state.md`, `tests/corpse_harvest_sim.test.ts`, `tests/harvest_geography.test.ts`
- **Same-change obligations:** RECOMMENDED ARM (ratify the substitutes): record only. THREE ANCHORS, all grep -c = 1, and all three want the same dated ruling id because the record states the deviation in three places: (1) the summary bullet, anchor "carriers): the Sundered Horror carries tusk, horn's floor holds on six.", the tail of DEVIATION (c); (2) the tusk family bullet, anchor 'so the capacity pre-gate premise'; (3) the horn family bullet, anchor "specimen-less, so the premise refused it; horn's floor holds on the six". Put the full dated RULED paragraph after (1), house form 'RULED (qr-19-capacity-pregate-candidate-refusals, 2026-09-01, under qr-19-best-for-project): ...', and a one-line back-reference to it after (2) and (3) so a reader arriving at either family bullet does not read the refusal as still open. FOURTH EDIT owed in the same commit: the premise comment in tests/corpse_harvest_sim.test.ts ('no corpse tags two specimen-less harvest families together', the capacity pre-gate premise case) should cite the ruling id, since the comment is what explains WHY the guard exists and it currently reads as an unratified content shape. VERIFY BEFORE RULING, live: that sundered_horror still carries tusk and that horn still has six reachable carriers (moor_ram, veiled_stag, gilded_stag, moonfleece_grazer, aurelhorn, sethrael_palecoil), because the ratification's whole warrant is that both refusals already have working replacements. No wiki regen, no M16 fills, no deed or Reliquary rows, no art park, no census row, no parity golden, no monolith ceiling under this arm. RELAX ARM PRICE, roughly 14 hours and it is the riskiest of the ten: amending the fitsAll pre-gate weakens the reserve-room guarantee for EVERY multi-family corpse, not just these two, so it owes a new safety argument as well as the code; then the pinned premise test flips, the two mob templates are re-tagged (content), the three harvest censuses and the R22 spread corpus arms in tests/harvest_geography.test.ts re-run, and it dispatches architecture-reviewer, content-obligations-reviewer, cross-platform-sync and test-coverage-auditor.
- **Reviewers:** content-obligations-reviewer, test-coverage-auditor, qa-checklist, architecture-reviewer, cross-platform-sync
- **Depends on:** nothing
- **Estimate:** 2.5h

### D055

Decide whether the consumable ladder gets a docs/design home, or whether the PRD derivation plus the def comments plus the enforced pins are ratified as its permanent record.

- **Files:** `docs/prd/masterwrought/state.md`, `docs/design/`
- **Same-change obligations:** RECOMMENDED ARM (ratify the status quo): record only, one dated line, no new file. ANCHOR (state.md, grep -c = 1): 'and the pins (maintainer call); the one unscoped commit subject'. Note that the shorter substring 'has no docs/design home, its derivation lives in this PRD, the def comments' matches 2, because the D055 table row quotes it, so use the longer anchor. The dated RULED line goes AFTER that line, house form 'RULED (qr-19-consumable-ladder-design-doc, 2026-09-01, under qr-19-best-for-project): ...', and it should name the two things that carry the authority today (the def comments and the pins) so a future reader knows where to look instead of hunting for a doc that does not exist. CONFIRM BEFORE RULING that no docs/design consumable doc has appeared since the read was taken (ls docs/design/). No wiki regen, no M16 fills, no deed or Reliquary rows, no art park, no census row, no parity golden, no monolith ceiling. AUTHOR-DOC ARM PRICE: roughly 8 hours for a docs/design/consumable-ladder.md that is genuinely worth having, because it must be written under the anchor rule from docs/qa-gate.md (stable paths, exported symbols and pinned tests, never literal counts or line numbers that rot), and docs/CLAUDE.md's own code-over-doc warning means a doc that restates what the pins already enforce is a liability rather than an asset. If it is authored it needs a fresh reader for drift risk, not a domain reviewer, since no code moves.
- **Reviewers:** qa-checklist
- **Depends on:** nothing
- **Estimate:** 1h

### D069

Rule whether apex flask timers keep ticking through death (classic fidelity) or the shipped pause-while-dead stands, where the recommendation is to ratify the pause with a dated line beside the nuance the ledger already recorded.

- **Files:** `docs/prd/masterwrought/state.md`, `src/sim/resurrection.ts`, `src/sim/combat/auras.ts`, `src/sim/spirit.ts`, `tests/flask_consumables.test.ts`
- **Same-change obligations:** WAVE CAVEAT, stated plainly: this taxonomy has no sim wave, and a flask is an apex consumable (Phase 10 content), so 19A-content is the least wrong home for the RATIFY arm. If the maintainer takes the tick-through arm it is NOT content work and must be re-routed to its own sim-systems unit; do not execute a load-bearing guard split inside a content lane. RULING RECORD anchor, verified unique with grep -c = 1 in state.md: "is what the sicknesses rely on). Both now in the resurrection.ts comment." and the RULED line goes AFTER it at column 0. No handoff table row, so no status cell to flip. Second, cheaper home worth taking in the same commit: the src/sim/resurrection.ts comment block already carries the nuance in prose ("While dead the timer pauses with every other aura (updateAuras early-returns for a dead entity), so a death effectively extends a flask by the time spent dead"), and adding the ruling id there is where the next reader lands. NO content obligations under the ratify arm: no wiki regen, no M16 fills, no deed rows, no Reliquary pages, no art parks, no census rows, no parity goldens, no monolith ceilings. LIVE CODE RE-VERIFIED: the top guard is src/sim/combat/auras.ts updateAuras, "if (e.dead) { e.stealthed = ...; return; }", and the resurrection.ts header block names both sicknesses, the Cauterize lockout, the Cheater mark, encounter-owned unbreakable control and Aura.flask as the death-survivors, so the guard really is shared and really is load-bearing. THE TICK-THROUGH ARM, honestly sized: splitting that guard so flask auras age while sickness handling still halts is a determinism-visible change to the 20 Hz tick, so it owes architecture-reviewer on rng draw order and tick phases, cross-platform-sync on all three hosts, new arms in tests/flask_consumables.test.ts, a parity golden re-record wherever a death occurs with a flask up, and a tooltip and wiki wording check, because the flask duration claim in player copy becomes a different claim. Ratify arm 1.5h; tick-through arm 8h and its own unit.
- **Reviewers:** qa-checklist, qa-checklist LAST
- **Depends on:** nothing
- **Estimate:** 1.5h

### D123

Re-word the 11l jewelcrafting rung-exclusion record at its live figures (34 uncrafted neck and ring items, 9 at zero, 24 above 600, mother_of_pearl at 50 sitting inside the band the record calls empty), keeping all four exclusions on the narrower ground that the one in-band row is a Proving Shore starter ring.

- **Files:** `docs/prd/masterwrought/state.md`, `tests/trophy_domination_claims.test.ts`
- **Same-change obligations:** NO content id moves, which is what keeps this cheap: no new recipe, no new item id, so NO wiki regen, no M16 fills, no deed row, no Reliquary page, no art park, no census row, no committed WebP, no i18n key, no parity golden, no facet member, no monolith ceiling. THE ONE REAL OBLIGATION is the packet's own rule that this record states: figures are RE-DERIVED at execution, never pasted (phase-11l-trophy-economy.md says so of its own exclusion list), so re-run the census arm and take the four numbers from the run, not from this row. SECOND FILE IS NOT OPTIONAL: tests/trophy_domination_claims.test.ts carries the contradiction in its describe name (`the jewelcrafting exclusion, recomputed: the header is now STALE`) and in an `OPEN FINDING for the maintainer` comment; both stop being true the moment the record is re-worded, so they are re-worded in the SAME commit while the four numeric pins stay exactly as they are. Confirm at execution whether docs/prd/masterwrought/phase-11l-trophy-economy.md carries the same clause (as read it carries the exclusion list at line 76 and 97 but not the census sentence, so it likely needs no edit). RECORDING: dated RULED line, id qr-19-jewelcrafting-exclusion-stale. ANCHOR, state.md 11l OUTPUT-EXCLUDED derivation: `endgame epics at 1800 to 50000; nothing in (25, 460]. A sweep of every` (grep -c == 1); the RULED line goes AFTER the bullet it sits in, and the stale clause itself is AMENDED IN PLACE with the dated line rather than silently rewritten. Test-side anchor: `OPEN FINDING for the maintainer: the` in tests/trophy_domination_claims.test.ts (grep -c == 1), line AFTER. No handoff-table row (Seed row column reads `new`).
- **Reviewers:** content-obligations-reviewer, test-coverage-auditor, qa-checklist
- **Depends on:** nothing
- **Estimate:** 2h

### D134

Rule whether a player-named legendary instance earns a Reliquary rung: under the recommended ratification it is three dated doc lines and zero code, and under option B it is a new instance-class mark carrying the entire Reliquary same-change chain.

- **Files:** `OPTION A (recommended): docs/prd/masterwrought/farming/state.md, ruling 135 p13-OPEN-RELIQUARY at :1845 to :1859`, `docs/design/reliquary.md, the 'Locked decisions' block (heading at :474), one new bullet in the shape the block's own bullets use (AMENDED 2026-09-01: the permanent recipe-pattern exclusion is a SENTENCE inside the 'Source-hint vocabulary' bullet, not a bullet of its own; the gate row states it correctly)`, `docs/prd/masterwrought/state.md (Phase 19 ledger) and /Users/fernando/Documents/wocc-masterwrought/docs/prd/masterwrought/progress.md`, `OPTION B ONLY, if ruled: src/sim/content/reliquary.ts (RELIQUARY_PROFESSION_MARKS at :459, the professions_masterwork page at :1300, the derived mark-id allowlist), /Users/fernando/Documents/wocc-masterwrought/src/sim/professions/perfecting.ts (the promotePerfectedCopy stamp site at :645 onward), /Users/fernando/Documents/wocc-masterwrought/src/sim/reliquary.ts noteReliquaryMark, /Users/fernando/Documents/wocc-masterwrought/server/character_sheet.ts RELIQUARY_MARK_ENGLISH, /Users/fernando/Documents/wocc-masterwrought/scripts/wiki/build_content.mjs RELIQUARY_MARK_GUIDE_NAMES, /Users/fernando/Documents/wocc-masterwrought/src/ui/i18n.catalog/hud_chrome.ts, /Users/fernando/Documents/wocc-masterwrought/src/ui/reliquary_cell_art.ts, /Users/fernando/Documents/wocc-masterwrought/src/ui/reliquary_window.ts cellQuality, plus tests/reliquary_content.test.ts, tests/character_sheet.test.ts, tests/reliquary_window.test.ts, tests/guide.test.ts, tests/profile_page.test.ts`
- **Same-change obligations:** OPTION A: none. No wiki regen, no M16 fills, no deed rows (both credits already ship, col_first_legendary and prog_legendmaker), no Reliquary pages, no art parks, no new item id so no census row and no committed WebP, no parity goldens, no monolith ceilings, R5 surface untouched. The wording IS the work: widen the ruling to name BOTH deed credits (row 135's text names only prog_legendmaker) and rest the refusal on the def-keyed model plus reliquary.md rules 3 and 5, NEVER on a conquerability claim, because 11j already found a Reliquary decline right for the wrong reason on exactly that ground. ANCHORS: in farming/state.md the string 'no phase may author an instance-keyed reliquary page' (grep -c 1); the dated RULED line goes AFTER it, closing ruling 135's block (the row was RECORDED-undecided, not wrong, so this is a RULED append and not an amendment). In docs/design/reliquary.md the heading '## Locked decisions (maintainer rulings; do not re-litigate in passing)' (grep -c 1); the new bullet goes AFTER that heading, among its existing bullets. NO handoff-table row exists for ruling 135 (verified: the farming handoff table 465 to 627 carries no Reliquary row) and decisions-index.md holds no 135 and no Reliquary symbol, so no status cell to flip and no index row. OPTION B, if ruled, carries the full chain in ONE change: reliquary.ts mark plus page seat plus source hint; a noteReliquaryMark call at the stamp site with a sim test carrying a negative pre-promotion arm; tests/reliquary_content.test.ts slot total, mark total, overview pair, character completion pair, the page-count pins if it takes its own page, the own-hint rule for multi-source pages and the per-page EXPECTED_DISTINCT_SOURCES equality (:3921); server/character_sheet.ts RELIQUARY_MARK_ENGLISH byte-identical to the client leaf (the sheet suite sweeps every id, and an unknown id silently drops the /c/ SSR entry); scripts/wiki/build_content.mjs RELIQUARY_MARK_GUIDE_NAMES or the wiki emit THROWS, then npm run wiki:content with tests/guide.test.ts as the freshness gate; one English hudChrome.reliquary.markFind leaf in src/ui/i18n.catalog/hud_chrome.ts forced by the derived pin in tests/reliquary_window.test.ts, with the five non-Latin fills an M16 obligation only if the English is wordy (the golden-harvest precedent shipped all five in-change); committed cell art or an authored glyph, since a new family falls through to null in reliquary_cell_art.ts AND cellQuality paints unknown prefixes 'common', so either reuse the masterwork: or gather_event: namespace or give both resolvers an arm; page-id APPEND only, never an edit. Still no new item id under B, so no census row and no item WebP, and R5 stays untouched. OPTION C (an instance-keyed rung) contradicts reliquary.md rules 3 and 5 and needs per-instance persisted state: multi-session, out of this packet, and it re-opens the moderation-strip and takedown story.
- **Reviewers:** qa-checklist, content-obligations-reviewer, frontend-seam-reviewer, database-performance-reviewer, privacy-security-review
- **Depends on:** nothing
- **Estimate:** 2h

### D143

Keep REF_ARMOR pinned at 2861, record the widened calibration gap as the model's stated conservatism with a dated appended line, and pay the provenance debt Phase 18 scoped but never landed by adding the raw-kit-versus-mastery-folded basis sentence to the fifteen sites that quote the constant as live fact.

- **Files:** `docs/design/power-verification.md, the section 14 REF_ARMOR bullet at :1241 to :1248 (its 'several hundred points above it' is now stale against the merged catalog)`, `docs/prd/masterwrought/state.md, the Phase 15 recorded read at :21876 and the eighth-sync read at :22246 to :22247`, `the four floor suites that hold the literal: tests/heroic_difficulty_floors.test.ts (:33, the re-pin note at :323 to :336 with the live literals armor 4085 at :337 and maxHp 1582 at :342), /Users/fernando/Documents/wocc-masterwrought/tests/rift_difficulty_floors.test.ts (:48), /Users/fernando/Documents/wocc-masterwrought/tests/gravewyrm_normal_tuning.test.ts (:37), /Users/fernando/Documents/wocc-masterwrought/tests/wildheart_normal_tuning.test.ts (:38)`, `the eleven sites quoting it as live fact: src/sim/content/dungeon_difficulty.ts (three comments), /Users/fernando/Documents/wocc-masterwrought/src/sim/rift/ranks.ts (which also states the derived ~39.8 percent), /Users/fernando/Documents/wocc-masterwrought/src/sim/entity.ts (the bear-parity rationale), /Users/fernando/Documents/wocc-masterwrought/scripts/healing_montecarlo.ts, /Users/fernando/Documents/wocc-masterwrought/docs/healing-monte-carlo-analysis.md (three), /Users/fernando/Documents/wocc-masterwrought/docs/rift-rank-monte-carlo-analysis.md, /Users/fernando/Documents/wocc-masterwrought/docs/prd/wildheart-basin-open-field-dungeon.md`
- **Same-change obligations:** OPTION A: comment and doc only. No suite re-records, no content record moves, no wiki regen (verified: the wiki builder emits no difficulty or mob-damage surface and the guide's rift entries are qualitative), no M16 fills, no deed rows, no Reliquary pages, no art parks, no census rows, no parity goldens, no server or headless surface, no monolith ceiling (none of the touched files is pinned). Validation is npx tsc --noEmit, tests/architecture.test.ts, and npm run ci:changed on the touched files. THE BASIS QUESTION IS UNSETTLED AND THE PROSE MUST NOT PAPER OVER IT: four suite headers call 2861 the max-armour kit with prot mastery folded in, while the re-pin comment says the 4085 and 1582 literals are raw kit numbers without prot mastery, and prot mastery RAISES armour, so 2861 cannot be a mastery-folded reading of a catalog whose raw max-armour kit measured 2969. Say so rather than restating the gap. ARITHMETIC that belongs on the record, derived from the committed formula rather than an R5 re-measure: the armour step passes 44.24 percent at 2861 and 35.72 percent at 4085 against the level-22 heroic pin, so post-armour melee falls about 19 percent and holding a melee floor would need about 24 percent more mob melee; the comments' '~39.8%' would read about 32.1 percent. RECORDING: dated AMENDED and RULED lines carrying qr-19-ref-armor-calibration-constant, APPENDED in section 14's own house style, never a rewrite of the sealed 'several hundred points' sentence. ANCHORS, each grep -c 1: power-verification.md "packet's defs leaves the max-mitigation kit unchanged" (the bullet's tail; the dated line goes AFTER it) and state.md 'thousand points, which sharpens the section 14 REF_ARMOR read). Three more' (the dated line goes AFTER that sentence). No handoff-table row exists, so no status cell to flip. OPTIONS B, C AND D ARE BLOCKED HERE: both ladders were solved AT their floors, heroic Nythraxis and S rift are R5's two protected assets with difficulty AS the protected asset, and the frozen harness itself imports both tuning tables, so any raise is an R5 escalation under the packet's stopping rule and must ride D140's re-measure. If ruled anyway, they become a 19A-content re-tune: the multiplier ladders in src/sim/content/dungeon_difficulty.ts and src/sim/rift/ranks.ts, mechanic overrides decided per mob (mechanicDamageMult DEFAULTS to the melee factor, so avoidable mechanics ride up unless held), all four floor suites re-recorded, 22 suites importing dungeon_difficulty and 10 importing rift/ranks in the blast radius, the parity coverage scenarios whose recorded runs kill heroic and raid bosses, and both Monte Carlo analysis docs either re-run or dated stale.
- **Reviewers:** qa-checklist, architecture-reviewer, content-obligations-reviewer, test-coverage-auditor
- **Depends on:** nothing
- **Estimate:** 4h

### D157

Ratify the tier 3 and tier 4 seed-back rates (0.08/0.40 and 0.06/0.35) now that Phase 11e's vendor faucet falsified their original premise and the comment block was already re-argued and found defensible, and retire the provisional flagging in the same change so a later reader does not re-open a settled number.

- **Files:** `docs/prd/masterwrought/farming/state.md`, `src/sim/professions/farming.ts`, `tests/professions_farming.test.ts`
- **Same-change obligations:** HANDOFF ROW to flip, exact text to match (grep -c = 1 in docs/prd/masterwrought/farming/state.md): '  Seed-back rates 0.08/0.40 tier 3, 0.06/0.35 tier 4 (economy-sensitive)   P5   maintainer   open ruling-owed  '. REPLACEMENT STATUS CELL: 'CLOSED 2026-09-01 by ruling qr-19-seed-back-rates (Phase 19, under qr-19-best-for-project): all four rates and FARM_SEED_BACK_MIN_TIER 3 are RATIFIED as shipped, no retune. The constants were deliberately RE-ARGUED at the Phase 11e QA after GATE 1 stocked all eight upper-tier seeds and falsified the no-vendor-faucet premise they were authored on, and were still found defensible (the seed-back is now a discount on a counter route that exists, not the only route to a tier 3/4 seed); the derived expectation stands at 0.48 seeds per tier-3 harvest and 0.41 per tier-4, and no phase or QA sweep since has filed a finding against it. A re-tune would need the maintainer own numbers, which the packet does not hold: it records no replant-cost model and no market price series.' SECOND EDIT SITE, same ruling id: the reads-owed line reading '   seed-back rates 0.08/0.40 tier 3 and 0.06/0.35 tier 4; compost 2 sell /' (state.md:423 as read). THE OPTION-2 COMMENT RETIRE, which is the only executable half and is recommended: the 'live tuning question for the maintainer' paragraph at src/sim/professions/farming.ts:266-270 and the 'TUNING, PROVISIONAL, FLAGGED FOR THE MAINTAINER (economy-sensitive)' banner at tests/professions_farming.test.ts:2589-2591 become dated RULED citations, following the D039 teardown-citations family. Zero behavior change, zero pin-value change. VERIFIED LIVE: the constants at src/sim/professions/farming.ts:271-273, the one-draw two-threshold resolve at :898-903, the grant at :933-934, the literal map pin at tests/professions_farming.test.ts:2588-2601, and the cross-pin at tests/parity/coverage_c.test.ts:954 proving the recorded golden shift was a re-seat and not a retune. NOT OWED under ratify: wiki regen and the tests/guide.test.ts freshness gate (no guide or generated surface emits the rate), i18n keys or M16 fills (hudChrome.farming.seedBackLine and seedBackLineQty are number-free and already filled in all five non-Latin locales), deed rows, Reliquary pages, art parks, census rows, parity goldens, IWorld parity members, monolith ceilings (src/sim/professions/farming.ts carries no row). IF THE MAINTAINER RETUNES INSTEAD (about +6h): the two maps move, the derived-expectation sentence at :265-268 is restated, the literal pin and every band probe (:2616, :2638-2639, :2660, :2680-2682, :2706-2707, :2760) re-derive, tests/parity/coverage_c.test.ts:954 re-crosses, and tests/parity/golden/farming_session.json may re-record under UPDATE_PARITY as its own reviewed commit; moving FARM_SEED_BACK_MIN_TIER is strictly worse because it changes the harvest DRAW COUNT and re-records the WHOLE parity directory. Under a retune the roll must stay exactly one ctx.rng.next inside the fixed draw block or determinism and every golden re-seat, which is why architecture-reviewer is dispatched there and skipped for a comment-only change.
- **Reviewers:** qa-checklist, content-obligations-reviewer, architecture-reviewer
- **Depends on:** nothing
- **Estimate:** 2h

### D158

Ratify the growth tonic's sell-only economy (sellValue 6, no buyValue) and accept the (ai) cheapest-rung-0-alchemy-skill-up finding with the intent recorded, taking the one-line sellValue pin as cheap insurance, because the only option that closes the faucet moves a frozen R5 input and is an escalation rather than an execution.

- **Files:** `docs/prd/masterwrought/farming/state.md`, `tests/farm_recipes.test.ts`, `src/sim/content/items.ts`, `src/sim/content/recipes.ts`
- **Same-change obligations:** HANDOFF ROW to flip, exact text to match (grep -c = 1 in docs/prd/masterwrought/farming/state.md): '  Growth tonic 6 sell-only plus (ai) cheapest rung-0 alchemy skill-up ruling   P4 / P6 QA   maintainer   open ruling-owed  '. REPLACEMENT STATUS CELL: 'CLOSED 2026-09-01 by ruling qr-19-growth-tonic-price-and-skillup-faucet (Phase 19, under qr-19-best-for-project), both halves. sellValue 6 with NO buyValue is RATIFIED: the sell-only doctrine is written as design twice (src/sim/content/items.ts:1105-1109 and farming/state.md:2611-2613) and a buyValue would price a faucet the record says must not exist. The (ai) cheap-skill-up faucet is ACCEPTED with the intent recorded, which is exactly what the QA addendum asked for, and the hazard is measurably NARROWER than that addendum recorded: on the basis a player actually pays (buyValue where it exists, the vial at 12) the tonic reads 20 against recipe_minor_healing_potion at 15, so the cheapest rung-0 alchemy skill-up is a station-free, trainer-free row that shipped long before farming. The addendum reached its finding on a sell-value basis (docs/design/power-verification.md:1104-1107) that its own parenthetical then skipped that row on.' SECOND AND THIRD EDIT SITES, same ruling id: the (ai) QA addendum block at state.md:2871-2882 (which asks in words for the intent to be recorded there), and the two separate reads-owed lines at state.md:424 ('8 buy with FARM_HUSKS_PER_COMPOST 2; growth tonic 6 sell-only; watch') and :426 ('rare; (ai) the tonic as the cheapest rung-0 alchemy skill-up; (aj) the'). THE ONE EXECUTABLE LINE, recommended: tests/farm_recipes.test.ts:836 asserts only sellValue toBeGreaterThan(0), so the 6 being ratified is the only number in the row nothing pins and any value from 1 to 19 passes silently; tighten it to toBe(6) beside the existing TONIC_EXPECTED_INPUT_VALUE literal at :715. R5 FENCE, load-bearing: this row is r5Surface=true. Option 3 (herbs 2 to 3) moves docs/design/power-verification.md:1085, where recipe_growth_tonic occupies alchemy's cheap-path column at '(0, 11c)', and 12.3's 0.66-to-0.88 band at :1143-1148 is measured off exactly that per-skill-point cost, so BOTH the number and which row owns the column move and the 2026-08-29 Verdict re-opens on its own conditions; under the packet's stopping rules that is an ESCALATION, not an execution, and R5 stays frozen. NOT OWED under ratify: wiki regen (src/guide/content.generated.ts:9188-9192 carries the output name, count and quality, never its value), i18n keys or M16 fills, deed rows, Reliquary pages, art parks, census rows, parity goldens (tests/parity/scenarios.ts:5989 grants the tonic with addItem rather than crafting it), IWorld members, monolith ceilings. OPTIONAL comment restamps in the same change: src/sim/content/recipes.ts:4128-4136 (the FLAGGED FOR THE MAINTAINER block) and the (ai) deviation comment already ledgered at tests/professions_zone_rollout.test.ts:1320-1323.
- **Reviewers:** qa-checklist, content-obligations-reviewer
- **Depends on:** nothing
- **Estimate:** 2.5h

### D159

Ratify the eight plain farm dishes' foodHp/sellValue pairs and their live reagent bills as authored and retire the two pending-sign-off headers in the same change, but it must land after D071 because that row owns the crafted food points these dishes reuse and the reuse arm requires each allowed point to be carried by a live non-dish food.

- **Files:** `docs/prd/masterwrought/farming/state.md`, `src/sim/content/profession_items.ts`, `src/sim/content/recipes.ts`, `tests/farm_recipes.test.ts`, `tests/vendor_floor.test.ts`
- **Same-change obligations:** HANDOFF ROW to flip, exact text to match (grep -c = 1 in docs/prd/masterwrought/farming/state.md): '  Dish foodHp/sellValue assignments and reagent counts (all curve-point reuses)   P6   maintainer   open ruling-owed  '. REPLACEMENT STATUS CELL: 'CLOSED 2026-09-01 by ruling qr-19-dish-curve-point-assignments (Phase 19, under qr-19-best-for-project): the eight pairs (90/6, 117/12, 243/25, 432/40, 552/60, 552/75, 980/150, 980/150) and the live bills are RATIFIED as authored. Every pair still reuses a point a live NON-dish food carries, every bill is gold-negative at the exact literal pinned with the beet braise deliberately at break-even so cooking converts produce without minting copper, and four of the eight bills were already revised once deliberately under 11e add-never-substitute and re-derived at the time, so these numbers have survived a real revision plus the packet economy sweeps with no finding against them. A re-set needs the maintainer own numbers, which the packet records do not hold: they carry one set, the shipped one.' ALSO in the same edit, the Phase 6 sign-off list at state.md:4444-4448 loses its two dish lines to the same ruling id. THE OPTION-2 EXECUTABLE HALF, recommended: comment-only rewrite of the 'VALUES ARE PROPOSED AND FLAGGED FOR THE MAINTAINER' paragraphs at src/sim/content/profession_items.ts:900 and src/sim/content/recipes.ts:3738 so they cite the ruling instead of an open sign-off; only the copy scan (no em or en dashes, no emoji) rides it. MUST LAND AFTER D071: contingentOn is stated unconditionally in the row, because D071 owns the crafted food points these dishes reuse and tests/farm_recipes.test.ts:1126-1141 requires every allowed point to be carried by a live NON-dish food, so a smoothing ruled there decides which points survive for this row to ratify. NOT OWED under ratify: wiki regen, M16 fills, deed rows, Reliquary pages, art parks, census rows, parity goldens, IWorld members, monolith ceilings (neither content file sits under a tests/monolith_budget.test.ts row; content is exempt at :36). NOT an R5 input: R5's food term is the apex plate wellFed value (docs/design/power-verification.md:164). AMENDED 2026-09-01 at execution, verified against the tree: the clause "these dishes' wellFed fields are 11c-D-2's and stay untouched" is FALSE, because the eight plain dishes carry NO wellFed field at all; every wellFed payload in that file belongs to the SIBLING buff-dish block, which is 11c-D-2's. The not-owed conclusion is unchanged and in fact strengthened, since a def with no wellFed field cannot be an R5 input by construction. Two further corrections at execution: the handoff-row anchor above is the RENDERED table form and greps 0 (the byte-exact anchor is the pipe form, a defect shared by every handoff-row anchor in this file), and "the Phase 6 sign-off list at state.md:4444-4448" is stale by 12 lines and is farming/state.md:4456-4464. The D071 contingency is DISCHARGED rather than pending: qr-19-food-per-point-inversions landed ratifying both reads with nothing moving, so this row's "MUST LAND AFTER D071" and its own "Depends on: nothing" now agree. IF THE MAINTAINER RE-SETS INSTEAD (about +8 to 12h): EXPECTED_INPUT_VALUE re-derives per row (tests/farm_recipes.test.ts:171-194) AND each touched recipe's own 'Input N vs output M' authoring comment must be CORRECTED rather than deleted (the 11e QA caught exactly that rot); the exact-input arm :558-571, the raw-sell no-minting arm :572-601 whose non-vacuity rests on the beet braise sitting exactly at break-even (so touching that bill means nominating a replacement break-even row), the reuse arm's spread and 980/150 ceiling non-vacuity :538-555, the crafted-only id-to-value map tests/vendor_floor.test.ts:322-357 plus the tercile-label arm :293-310 and the tier list :281-283; and npm run wiki:content because src/guide/content.generated.ts:10386-10424 carries both the counts and effect.food.amount, freshness-gated by tests/guide.test.ts. Minting a NEW curve point is worse still and is fenced: the backing arm refuses a dish-only point by construction, and a farm dish at or above 1392 sits on the apex plates' own band, which is 11c/11k surface to escalate.
- **Reviewers:** content-obligations-reviewer, test-coverage-auditor, qa-checklist
- **Depends on:** nothing
- **Estimate:** 2h

### D160

Ratify hoe-reagent-only as shipped design while correcting the ledger row's falsified 'these two' to the live three (fine_vale_wheat, fine_marsh_rice, fine_highland_barley), and restamp the three shipped source comments that still advertise the question as open.

- **Files:** `docs/prd/masterwrought/farming/state.md`, `src/sim/content/items.ts`, `src/sim/content/recipes.ts`, `tests/farm_recipes.test.ts`, `tests/professions_zone_rollout.test.ts`
- **Same-change obligations:** HANDOFF ROW to flip, exact text to match (grep -c = 1 in docs/prd/masterwrought/farming/state.md; the cell is long, match on the head): '  fine_marsh_rice / fine_highland_barley dish consumers (hoe-reagent-only today)   P6   maintainer   open ruling-owed'. REPLACEMENT STATUS CELL: 'CLOSED 2026-09-01 by ruling qr-19-fine-twin-dish-consumers (Phase 19, under qr-19-best-for-project): hoe-reagent-only is RATIFIED as shipped design, and the row set is AMENDED in place rather than rewritten. The closing clause These two remain the only twins whose sole consumer is a hoe rung is FALSIFIED by live code: the set is THREE, because fine_vale_wheat has exactly one reagent site in all of src/sim (recipe_bronze_hoe at src/sim/content/recipes.ts:3530) and the ledger row never named it. The record calls the state deliberate twice in shipped source (items.ts:1217-1221 and :1294-1297) and once calls a change to it a content decision, not a gap in the closure pin (state.md:4449-4452); the load-bearing invariant, that every twin has a consumer by SOME route, is already green over the whole twin column at tests/farm_recipes.test.ts:659-663; and the loaf and the rice bowl carry no twin for exactly the coherent reason that their crop twin went to the hoe ladder. The counterweight, recorded so the ratification is informed: Phase 11j retired the exclusivity rule that made hoe-reagent-only structural, since fine_evergarden_greens is now deliberately double-booked (recipes.ts:3594 and :3946).' COMMENT RESTAMPS in the same or a later commit (comment-class, no assertion behind them): src/sim/content/recipes.ts:3962-3963 ('an OPEN maintainer question'), and the two 'A hoe reagent, and ONLY that' blocks at src/sim/content/items.ts:1217-1221 and :1294-1297. VERIFIED LIVE: fine_evergarden_greens carries THREE consumers (recipes.ts:3594 count 2, :3950 (AMENDED 2026-09-01, the cite read :3946 and had drifted four lines), :3203), so the candidate framing that grouped it with the hoe-only set is wrong against the tree. NOT OWED under ratify: wiki regen, M16 fills, deed rows, Reliquary pages, art parks, census rows (symbol_census.mjs carries no reagent or itemId term), parity goldens, IWorld members, monolith ceilings (content is exempt at tests/monolith_budget.test.ts:36). IF THE MAINTAINER TAKES OPTION 2 INSTEAD (about +5 to 7h, still 19A-content): two or three one-line reagent additions at recipes.ts:3812-3830 and :3847-3873 following the shipped count-1 base-plus-fine pattern; EXPECTED_INPUT_VALUE re-pins at tests/farm_recipes.test.ts:174 and :186 AND its derivation comment at :175-186 gains the new arithmetic rather than going stale; the arm that actually reds is 'exactly the apex twin is double-booked' at :674-677; the raw-sell comment figure at :583 re-derives; four prose sites become lies unless moved in the same change (items.ts:1217-1221, :1294-1297, recipes.ts:3727-3730, :3962-3963, and tests/professions_zone_rollout.test.ts:1042-1046); and npm run wiki:content is owed because src/guide/content.generated.ts:10469 and :10547 gain a reagent and the twins' material pages gain a used-in entry, freshness-gated by tests/guide.test.ts. FOUR MAINTAINER VALUES ARE OWED FIRST and none may be invented: the scope (three or two, and if two, why wheat is excluded), the reagent count per twin, whether the bannock may carry a SECOND fine twin, and under option 3 the new dish ids, names, rungs and curve points. A reagent count of 2 rather than 1 would additionally want D004's value-half answered first; at the shipped count of 1 there is no dependency.
- **Reviewers:** content-obligations-reviewer, qa-checklist
- **Depends on:** nothing
- **Estimate:** 2h

### D163

Ratify the quest-template fallback grant that force-adds First Furrow's hoe and seed into a full 16/16 backpack and name that path in the src/sim/bags.ts capacity doctrine header so its absence stops reading as an oversight.

- **Files:** `src/sim/bags.ts`, `src/sim/quests/quest_commands.ts`, `docs/prd/masterwrought/farming/state.md`, `docs/prd/masterwrought/farming/progress.md`, `tests/quest_fallback.test.ts`, `tests/professions_starter_tools.test.ts`
- **Same-change obligations:** SCOPE NOTE: under the recommended arm the diff is COMMENTS ONLY (extend the enumerated ungated list in the bags.ts doctrine header to include the accept and giver-talk fallback grant, carrying the soft-lock reason fishing.ts already states, plus an optional pointer line at the finalizeQuestAccept grant site). The last two test files are the bill ONLY if the maintainer picks the capacity-gate arm, which is a QUEST-TEMPLATE rule change reaching every requiredItems quest in the game (the Proving Shore tutorial's ps_briny_lure and ps_passing_stone, the two zone-1 gathering intros, firebottle, crypt_keystone), and whose refusal SHAPE (refuse the whole accept, or accept and withhold with the giver re-talk as the recovery route) is part of the ruling. NOT OWED either way: no new t() key (error.bagsFull is already in the client matcher), no M16 fills, no wiki regen, no deed row, no Reliquary page, no art, no census row (no new export), no parity golden, no world_api member, no monolith ceiling (neither file is ratchet-pinned). Under the gate arm expect NO parity golden movement either, since both shipped accept scenarios use quests without requiredItems. ANCHOR (grep -c = 1) in /Users/fernando/Documents/wocc-masterwrought/docs/prd/masterwrought/farming/state.md: 'requiredItems grant on accept and re-talk bypasses bag capacity (a full'; the dated RULED line goes AFTER that residual paragraph's last line. HANDOFF ROW, match exactly: '  q_prof_intro requiredItems overflow grant on a full bag (17/16 visible)   P9 QA   maintainer   open ruling-owed  '. STATUS CELL REPLACEMENT: 'open ruling-owed' becomes 'CLOSED 2026-09-01 by ruling qr-19-qprofintro-overflow-grant (Phase 19, under qr-19-best-for-project): the template bypass is RATIFIED as shipped and the fallback grant is named in the src/sim/bags.ts capacity doctrine header beside the other deliberately ungated paths.'
- **Reviewers:** qa-checklist, architecture-reviewer, cross-platform-sync, test-coverage-auditor
- **Depends on:** nothing
- **Estimate:** 1h

## Exit criteria

1. Every unit above executed, or escalated with a dated reason.
2. Each unit's domain reviewers dispatched FRESH (never the implementer), all findings applied: blocking, should-fix and nits alike.
3. The fix round re-read by a fresh reader, because fixes are unreviewed code.
4. Every new pin mutated and watched go red. A pin that survives its own mutation is not a pin.
5. `npx tsc --noEmit` clean, the architecture, monolith and parity guards green, the census RESULT PASS, `npm run ci:changed` after the LAST commit.
6. `node scripts/gate_select.mjs` on the COMMITTED tree, pg-armed, judged by exit code.
7. `docs/prd/masterwrought/phase-19a-qa.md` written and its verdict recorded before the next wave opens.

## Standing rules

NO push, NO PR, NO teardown. Local only. A green gate is not consent.
