# Phase 19E: Measurement

Opened 2026-09-01 under ruling qr-19-best-for-project. Rows whose shape must be chosen on a number rather than an argument. The bench lands first, the shape is ruled on the figure it returns.

**4 units, about 23.00 hours estimated.** Every row here was answered in the Phase 19 rulings gate; this phase executes, it does not re-decide. A row whose ruling turns out to rest on a wrong premise is an ESCALATION back to the ledger, never a quiet re-ruling.

## Units

### D062

The arcane_shard disenchant ratio outlier (12 consumers and 21 units against essence's 40 and 84): take the supply and demand measurement the packet twice deferred, then decide, rather than ruling a shape on an admittedly unmeasured ratio.

- **Files:** `docs/prd/masterwrought/state.md`, `src/sim/content/recipes.ts`, `src/sim/content/enchants.ts`, `src/sim/professions/enchanting.ts`, `src/sim/professions/sundering.ts`
- **Same-change obligations:** WHY 19E AND NOT A STRAIGHT RATIFY: D062 does not stand alone. D047 (arcane-shard-greater-rung-sink) asks the same question from the supply side and its own recommendation is to COMMISSION the measurement, and D034 (lucent-weapon-rung-r21-demand-risk) prices the same demand risk from the enchant-rung side. One measurement answers all three, and every one of them is currently blocked on the same missing number, which is exactly the case 19E exists for: the measurement must land before any shape is chosen. Ruling D062 'keep the outlier' on its own while D047 rules 'measure' would leave the packet holding two answers to one question. THE MEASUREMENT, concretely: re-derive the consumer side live off src/sim/content/recipes.ts and src/sim/content/enchants.ts (the ledger's 12 consumers and 21 units, being the two rung-25 charms at 5 each, nine enchant rows at 1, and Lucent Infusion at 2, a split the 11m QA already corrected once from a wrong 'four Lucent rows at 1' summing to 20, so re-derive rather than copy), then the supply side off the disenchant yield in src/sim/professions/enchanting.ts and the sundering path (one shard per epic or legendary disenchant regardless of item level, against dust's quantity-scaled yield), and state the essence comparison on the same basis. Record it as a READ with its derivation, no code. ANCHOR (state.md, grep -c = 1): 'band would spend a scarce supply to fix a ratio nobody measured. NO shard', inside THE SINKS section; the dated RULED line goes AFTER the sentence it ends, house form 'RULED (qr-19-disenchant-ratio-outlier, 2026-09-01, under qr-19-best-for-project): ...', and it must state which of the four 'NEVER arcane_shard' reservations in recipes.ts the answer binds or leaves standing. No wiki regen, no M16 fills, no deed or Reliquary rows, no art park, no census row, no parity golden, no monolith ceiling for the measurement itself. IF THE MEASUREMENT SAYS THE SINK IS STARVED: adding a shard-rung consumer is a further 10 hours and a separate ruling, owing new consumer defs in src/sim/content, the masterwrought_materials and sundering suites, R21 presence arms in tests/gathering_supply_coverage.test.ts, census rows for any new content id, and wiki regen, dispatching content-obligations-reviewer and architecture-reviewer.
- **AMENDED IN PLACE 2026-09-02 at execution, eight corrections; two change the measurement's shape and the recommended arm survives all of them.** (1) SUNDERING IS NOT A SHARD SUPPLY. `src/sim/professions/sundering.ts` mints `sundered_essence` only (SUNDERED_ESSENCE_YIELD 1; `grep -c arcane_shard` on the file is 0) and admits raid gear epics, so it is a competing spend of the shard's INPUT and belongs on the demand side of the read, never on "the sundering path" of the supply. The sole shard supply is resolveDisenchant in `src/sim/professions/enchanting.ts` through the rung map DISENCHANT_MATERIAL_BY_QUALITY, which lives in `src/sim/professions/disenchant_reagents.ts` (:32 to :38, imported at enchanting.ts:95), a file the Files cell omits and the read names. (2) THE DEMAND SIDE HAS A SINK OUTSIDE BOTH REAGENT FILES that the reagent-row census cannot see: the tool-effect recharge (`src/sim/professions/tools.ts`, R39) prices arcane_shard at the epic and legendary rungs (R47: at the higher of the owned gather tool's rung and the slot's latched ceiling rung), six epic gather-tool defs reach that rung on the merged catalog (a first draft of the read said 35, which counted every epic def of kind 'tool', sigils and stations included; corrected at the review round), and enchants.ts's own header already counts it among "the four arcane_shard sinks"; the read states it beside the 12 / 21 fixed rows instead of walking reagent rows alone. (3) "essence's 40 and 84" reads 41 / 86 on the merged tree (recipe_deed_of_making's 2-essence row joined 2026-08-27); the shard's 12 / 21 re-derives exactly. (4) recipes.ts carries THREE literal "NEVER arcane_shard" reservations (:1815, :2014, :2205) plus a differently worded fourth, "arcane_shard is reserved for the apex band" (:4302), which is what the ledger's own "all four" means; the RULED line names all four. (5) There is no sundering suite file: the sundering arms are the "sundered essence: the extraction" describe inside tests/masterwrought_materials.test.ts (:804 to :1117). (6) The source row's "Phase 15 premise-measurement RECORDED/NOT-CHANGED block (lines 17902-17910)" is the Phase 11m ledger's player-report bullet at state.md:18970 to :18977 (under "THE PLAYER REPORTS, EACH MAPPED TO WHAT WAS MEASURED, CHANGED, AND NOT" at :18909, addresses at the merge tip 2ebe95e731), not a Phase 15 record; :17902 to :17910 now holds the 11m ledger's opening paragraph. (7) The anchor straddles a line: "NO shard consumer is added." ends on state.md:18410, so the RULED block goes after that line and the paragraph continues below it. (8) The eleventh sync (2ebe95e731) moved the supply INFLOW under this row: the Crucible re-cut to one gear item per five raiders per kill (docs/prd/ignivar-raid-loot.md, "The cadence rule", 2026-09-02) cuts the raid's gear-epic inflow by half on Normal and by about two thirds on Heroic, so the read is derived from the merged tables and says so, and states the raid arm as a lower bound beside the 179 non-raid disenchantable epic-plus defs no record prices. (9) The row's house-form example carried the sitting's date, 2026-09-01; the RULED line carries the execution date, 2026-09-02, as every 19E unit does. Refuted and kept as written: "reopened under qr-18-REOPEN" is the blanket reopen; no per-record marker was ever written for this record.
- **Reviewers:** content-obligations-reviewer, qa-checklist, architecture-reviewer
- **Depends on:** nothing
- **Estimate:** 5h

### D128

Measure the two shapes of the single-slot elision cache against the committed HUD perf baseline first, then land Option A (a real slot key on SingleSlotEntry so two writer kinds on one element cannot collide) only if the numbers say the removed writes beat the per-element memory.

- **Files:** `tests/hud_perf_budget.baseline.md`, `tests/hud_perf_budget.test.ts`, `src/ui/painter_host.ts`, `src/ui/hud.ts`, `tests/painter_host.test.ts`, `tests/painter_slot_collision.test.ts`, `tests/painter_single_slot_collision_guard.test.ts`, `scripts/perf_tour.mjs`
- **Same-change obligations:** READ THE FORK FIRST: the row's own recommendation says Option A is a MAINTAINER-OWNED change with a tour capture attached and NOT folded into this packet. If the maintainer takes that reading, this row is ROUTE-OUT and the deliverable is the measurement plus a filed follow-up. It is placed in 19E because the measurement is the thing that decides the shape and it is runnable here: hudHotDomWrites is the run-length-independent anchor the baseline says TRAVELS ACROSS HARDWARE, so a before/after capture is comparable, while frameLong50 and tourMinFrames are same-machine values that must NOT be re-minted from this machine. MEASUREMENT HALF (about 3h): npm run dev on 5173, scripts/perf_tour.mjs headless with --use-angle=swiftshader, both viewports, plus ARM 2's deterministic fake-DOM skip-rate and per-frame allocation budgets as the allocation probe. IMPLEMENTATION HALF, gated on those numbers (about 7h): SingleSlotEntry stops being {kind, value} and shouldWriteSingleSlot compares the slot for the requested kind; the helper is shared VERBATIM by Hud's two private writers (src/ui/hud.ts:3142 and :3151, whose call form is source-pinned at tests/painter_host.test.ts:506-507) and by makeWriterFacet over the SAME cache, so both move together or the invariant goes. PINS ARE REWRITTEN, NOT ADJUSTED: painter_host.test.ts's decision table and its allocation-free-skip arm, painter_slot_collision.test.ts, and the guard's recommendation block, which becomes the record of the ruling. The perf baseline is a GOLDEN: a deliberate budget change updates its row in the same commit. Two riders the row names and that must ride whichever way it goes: the AST guard stays keyed on the SHAPE and stays an AST walk (the mutation proved a name or regex detector walks past the two loop-variable receivers, and the original list named four sites where the scan found seven), and ARM 2's exemption rationale correction (elision is guaranteed per (element, KIND), never per element) lands in the same change. src/ui/hud.ts ceiling 18728 with the file at 18708. No content, no i18n, no wire, no facet, no parity golden. RECORDING: dated RULED line, id qr-19-single-slot-writer-slot-key. ANCHOR: `THE RECOMMENDATION the head comment defers to` in tests/painter_single_slot_collision_guard.test.ts (grep -c == 1), the RULED line goes AFTER that comment block's opening so the whole priced-options section reads as answered. No handoff-table row (Seed row column reads `new`).
- **AMENDED IN PLACE 2026-09-02 at execution, five corrections; one changes the implementation half's price.** (1) Hud's two private writers are the `setText` and `setDisplay` methods of `Hud` in `src/ui/hud.ts` (at :3151 and :3160 when read; the row's :3142 and :3151 were stale, and per the anchor rule the durable citation is the method names). (2) Their call form is source-pinned by the test titled 'Hud private single-slot writers route through the shared decision helper' in `tests/painter_host.test.ts` (the row's :506 to :507 was correct when read; a first draft of this correction wrote :505 to :506, which is wrong, caught at the review round). (3) THE CEILING HAS ZERO SLACK: `src/ui/hud.ts` is 18716 lines against a ceiling of 18716 (tests/monolith_budget.test.ts, lowered by 19D's D129 extraction and its review round), not "18728 with the file at 18708", so any hud.ts growth the implementation half needs is paid for by extraction, never a raise. (4) ARM 2's exemption rationale correction is NOT a rider: it landed on 2026-08-31 (d8feee8fa8, an ancestor of this document) and `tests/hud_perf_budget.test.ts` already states the per-(element, KIND) guarantee exactly; the one rider that rides either way is the shape-keyed AST guard. (5) The golden hudHotDomWrites row (1062) and every re-derivation in its history are HEADED `PERF_GPU=1` two-run captures, and ARM 3 consumes a PERF_GPU=1 artifact; the prescribed headless swiftshader before/after is comparable to itself (its run-1 desktop figure agreed with the headed anchor's 1054 to within two writes) but showed a 20-write run-to-run band on desktop under software rasterization, so it can say whether a reduction EXISTS and cannot re-mint the golden; a golden update, if the shape ever lands and the anchor moves, is a headed two-run capture. Taken as prescribed and recorded with that caveat.
- **Reviewers:** frontend-seam-reviewer, render-performance-reviewer, test-coverage-auditor, gate-integrity-reviewer, qa-checklist
- **Depends on:** D111, D129
- **Estimate:** 10h

### D138

Amend qr-GRAY's row 128 in place against the measurement that falsifies it (floor spam is longer on all ten crafts and dearer on eight, and the real arbitrage is the lowest reagent cost per skill point one or two tiers under the band), carry the amendment to the brainstorm mirror that aims the future-tier revisit, and rename section 12.2's own 'cheap path' label that section 12.3 measures as dearer.

- **Files:** `docs/prd/masterwrought/farming/state.md, ruling row 128 qr-GRAY at :1768 to :1777`, `docs/prd/masterwrought/brainstorm.md, the future-tier gray-grind bullet at :75 to :78 (the line a future-tier session actually loads)`, `docs/design/power-verification.md, section 12.2's column header at :1082 ('cheap path') and its two prose uses at :1080 and :1101, against section 12.3's measured 8-of-10`, `docs/prd/masterwrought/state.md, the Phase 15 RECORDED-NOT-ACTED read at :21899 to :21902`, `OPTIONAL RIDER F: a new scripts/ three-path walk module on the scripts/r5_envelope_probe.ts precedent plus a new tests/ suite on the tests/r5_envelope_probe.test.ts precedent`, `NOT in scope unless the maintainer picks option B: docs/prd/masterwrought/implementation-plan.md:879 and /Users/fernando/Documents/wocc-masterwrought/docs/prd/masterwrought/phase-15-power-verification.md:139, the two historical phase specs repeating 'the cheapest below-band spam path'`
- **Same-change obligations:** Docs only on the recommended arm: no code, no test, no wiki regen, no M16 fills, no deed rows, no Reliquary pages, no art parks, no census rows, no parity goldens, no monolith ceilings, R5 surface untouched. Verified NO non-doc mirror exists (a grep over src/, server/, tests/, scripts/ and public/ for qr-GRAY, gray-grind, 'cheapest path', 'bulk spam' and 'below-band' finds nothing load-bearing), so no pin, no wiki row and no player string carries the false wording. The DECIDED half of qr-GRAY never rested on the false premise (its stated WHY is the shipped-multiplier derivation of the 11e, 11f and 11i pacing models), so nothing re-opens and src/sim/professions/wheel.ts tierProgressMultiplier is NOT touched. WORDING TRAP the amendment must not fall into: the two eights are DIFFERENT sets. The measurement supports LONGER on all ten crafts (12.2's table) and DEARER on eight (12.3, excluding leatherworking and inscription, including alchemy and enchanting); enchanting has no tier-0 row at all. Do not conflate them into one claim. DOCTRINE: amend in place with a dated line, never rewrite (farming/state.md's own rule at :677 to :678 and decisions-index.md's 'The rule'), which is why option D is refused. ANCHORS, each grep -c 1: farming/state.md 'low recipes rather than crafting the band's real content' (the false sentence) with the dated AMENDED line going AFTER the row's last line, whose tail anchor is 'against the shipped multiplier.'; brainstorm.md 'bulk spam of low recipes; Phase 15 records the measurement, and any change to' with the dated line AFTER that bullet; state.md 'The record states both; the row itself is left for the maintainer.' with the dated RULED line qr-19-qr-gray-row-wording-false AFTER it. No handoff-table row exists, so no status cell to flip. RIDER F price, stated separately: one new scripts/ module plus one new test file, tsc and changed-file biome, and it FORCES the re-read against the merged catalog that 12.2 has never had (section 12.4's wiki bullet was already amended for the seventh sync's four tailoring bag recipes while 12.2's table and 12.3's split were not), so the amended numbers would be re-derived rather than copied.
- **AMENDED IN PLACE 2026-09-02 at execution, ten corrections; the recommended arm survives all of them, and one changes the size of option C.** Every address below is at the merge tip 2ebe95e731, before this wave's own insertions. (1) Row 128 sits at `farming/state.md:1834` to `:1843` (the falsified sentence :1836 to :1837, the DECIDED half :1837 to :1843, the adoption-pass heading :1672), a uniform +66 on the cited :1768 to :1777. (2) The doctrine rule ("an existing row that was wrong is AMENDED IN PLACE with a dated line rather than rewritten") is at :740 to :741, not :677 to :678, which now reads the status-vocabulary paragraph. (3) Every section-12 address in `docs/design/power-verification.md` is +17: 12.2 at :1093, the header row :1099, the definition prose :1097 and the second prose use :1118, the table :1101 to :1110 (enchanting :1107), the hand-worked note :1121 to :1126, 12.3 at :1138 with the qr-GRAY bullet :1149 to :1165 (the enchanting sentence :1156 to :1158, the cheapest-per-point read :1159 to :1165), 12.4 at :1176. (4) OPTION C IS LARGER THAN "a header plus two prose uses": section 12.2 carries the "cheap" label at FOUR sites and FIVE tokens (the definition line :1097, the header row :1099 twice, "cheap path" and "the cheap recipe", and the prose at :1118 and :1134), the fourth site having been present when the row was read; the rename covers all five. (5) The Phase 15 RECORDED-NOT-ACTED read is at `state.md:22671` to `:22674`, not :21899 to :21902 (the Phase 14 screenshots block) nor the source row's :21347 to :21350; its neighbours moved non-uniformly (+1287 to +1324) because two Phase 19 blocks were inserted into the same RECORDED block: the heading :22599, the Phase 15 ledger :22416, the RE-OPENED neighbours :22601, :22604, :22628 and :22666, the r5 probe move :22729 to :22735, the nine corrections :22755 to :22761. (6) The falsified sentence wraps farming/state.md :1836 and :1837 and cannot be grepped whole; the Files cell's single-line fragment is the anchor, at :1837. (7) The source row's DEARER sentence wraps power-verification.md :1151 and :1152, inside the qr-GRAY bullet at :1149 to :1165 (the source row's :1132 to :1148 is stale); "DEARER than the intended path for 8" greps 1 in that document (the two phase records that quote it are further hits). (8) The source row's "five test citations of farming/state.md and brainstorm.md" reads TEN citing lines across EIGHT test files today (nine across seven when read; tests/farm_recipes.test.ts:733 joined 2026-09-01), all of them comments, so the no-non-doc-mirror premise stands on the corrected count. (9) The source row's option B precedent quote from phase-15-power-verification.md wraps :131 and :132 and uses double quotes around "three", so its single-quoted one-line form greps 0 in its cited home (the source row itself is the one hit elsewhere). (10) The `git grep` sweep the source row prescribes returns TWO hits today, the second being tests/masterwrought_budget.test.ts:1817, a 'below-band crafted output' item-level arm added by 19D, unrelated to the gray grind; refuted as a defect of the row (it was correct when read) and noted so the sweep is not re-run against a stale expectation. (11) The Reviewers cell names test-coverage-auditor for rider F; rider F was declined, and the auditor was dispatched anyway, which is how the no-pin residue above was named.
- **Reviewers:** test-coverage-auditor, qa-checklist
- **Depends on:** nothing
- **Estimate:** 3h

### D139

Ratify Phase 18's measured refusal of the shared-bytes splice, then MEASURE the half the refusal never touched (emitToZonePlayers walking every player in the realm with a zoneAt call per celebration) on the fixture the fanout suite already builds, and rule the shape on the number rather than blind.

- **Files:** `src/sim/professions/gather_events.ts (emitToZonePlayers at :71 to :85, the ctx.players.values() walk with the DUNGEON_X_THRESHOLD and zoneAt filter; announceGatherRareEvent at :91 to :107, the FOURTH tenant and second direct caller; announceZoneCelebration at :129 to :138)`, `src/sim/data.ts (zoneAt, which walks the ACTIVE content's zone list per call; ZONES holds 15, not the record's assumed 20; zoneContaining, the strict rect helper option 4 would use)`, `tests/zone_celebration_fanout_shape.test.ts (304 lines today, not the 225 the row read; its fixture at :71 to :107 already builds N in-zone players plus far-zone and instanced non-recipients, so the wasted walk is already expressible there; the suite pins premises and holds NO timing assertion today)`, `OPTION 3 ONLY: src/sim/sim_context.ts, /Users/fernando/Documents/wocc-masterwrought/src/sim/spatial.ts, /Users/fernando/Documents/wocc-masterwrought/scripts/merge_audit/symbol_census.mjs (SIM_EVENT_FANOUT_HELPERS, hand-maintained), tests/merge_audit_symbol_census.test.ts, tests/professions_attunement_events.test.ts, tests/gather_rare_events.test.ts, tests/masterwork_zone_broadcast.test.ts`
- **Same-change obligations:** OPTION 2 (recommended): no production code, so no wiki regen, no M16 fills, no deed rows, no Reliquary pages, no art parks, no census rows, no parity goldens, no monolith exposure, no IWorld facet or tests/world_api_parity.test.ts duty (the fanout is sim-internal). Whether the bench lives as a Vitest arm or a scratch harness with only the premises pinned is MAINTAINER-NAMED, since the suite pins premises and never timings today. Two facts the bench must carry that neither record does: the fanout has a FOURTH tenant (announceGatherRareEvent), so the shared scan runs at roughly double the rate the record's line assumes, and that line assumes 20 zones where ZONES holds 15 (conservative direction). RECORDING: two dated RULED lines carrying qr-19-zone-celebration-fanout-shape, one for the ratified splice half and one for the measured scan half. ANCHORS, each grep -c 1 in state.md: 'RE-OPENED (qr-18-REOPEN, 2026-08-31): actioned by Phase 18 as zone-celebration-fanout-tenants.' (the reopened Hot-path F4 note; the dated line goes AFTER it) and 'this scan was left standing' (the RECORDED-NOT-ACTED bullet; the dated line goes AFTER that bullet's closing sentence). The Phase 18 ledger's own table row for the slug already reads JUDGED, not open, so there is NO 'open ruling-owed' or 'handed-to-maintainer' status cell to flip. IF OPTION 3 IS RULED, four obligations beyond the code, all verified live: the SimEvent emit census names its fanout helpers BY HAND in scripts/merge_audit/symbol_census.mjs, so a rename or second helper must join that set plus its two pin arms or those event kinds silently leave the emits class; three suites hand-build a minimal SimContext slice and each fake must build the new field exactly; roster maintenance added on Sim reds the src/sim/sim.ts ceiling in tests/monolith_budget.test.ts, where the answer is extraction and never a raise; and ORDER is the trap, since the fan-out emits in ctx.players roster order and tests/masterwork_zone_broadcast.test.ts pins 'in roster order' verbatim, so a bucket either re-sorts (handing back the saving) or moves that pin. Note the cheap form of option 3 is SLOWER than the walk on a small realm (a rect query would visit roughly 12 by 12 cells for the 360 by 360 zone rect at cellSize 32).
- **AMENDED IN PLACE 2026-09-02 at execution, twelve corrections; one changes what the bench must carry, and the recommended arm survives all of them.** Every address below is at the merge tip 2ebe95e731, before this wave's own insertions. (1) "TWO FACTS ... THAT NEITHER RECORD DOES" IS HALF FALSE: the suite record has carried the fourth tenant since f14f015a7b (its header names "The four tenants (gatherRareEvent and masterworkZone ...)", its :168 to :183 arm is titled "the fourth tenant", its :22 to :38 realm-rate derivation is taken from gatherRareEvent's OWN cadence as the widest tenant, and :250 pins GATHER_RARE_EVENT_CHANCE at 1/90); only the two state.md records lack it. What NO record carried was the rate the other three tenants and the farming crop stream add on top of that node-only line, and "roughly double" was never a number either record states; the bench states the terms and the ledger line says the realm rate is every tenant's cadence summed. The 20-zones-versus-15 half holds (:30 "Twenty zones"). (2) The state.md homes: the reopened Hot-path F4 note at :24046 to :24049 (its RE-OPENED slug line :24047), the MEASURED REFUSAL record at :24968 to :24975 (slug :24969), the hot-path amendment at :25033 to :25041 ("RECORDED: emitToZonePlayers" at :25039); Option 1's homes are :24046 and :24968. (3) The suite addresses (304 lines): CELEBRATION_COPY_MAX_BYTES at :78, shapes.size at :148 to :149, the widths arm at :276 to :284, the instanced zero arm at :219 to :226, the ceiling read at :287 to :303, the routing amendment header at :40 to :52, the fixture fakeWorld at :92 to :131 (not :71 to :107), its fake SimContext slice at :119 to :129. (4) The source row's "suite header :13-14 as read, '1 per zone per 20 min, 20 zones, 200 in-zone'" sentence no longer exists: the realm-rate bullet is :13 and the derivation :22 to :38, whose single-line anchor is "event per zone per 20 minutes. Twenty zones is 1 celebration per minute" (:30). (5) tests/masterwork_zone_broadcast.test.ts does NOT pin "in roster order" verbatim: that phrase wraps a comment at :140 to :141 and greps 0; the roster order is pinned by the ordered two-element toEqual at :142 to :160 (crafter, then nearby). (6) All four "as read" state.md quotes wrap lines (:24048 to :24049, :24969 to :24975, :25039 to :25041) and grep 0 whole; the text is intact. (7) The census resolver branch `SIM_EVENT_FANOUT_HELPERS.has(tok.v)` is at scripts/merge_audit/symbol_census.mjs:1522 to :1530 (the set itself at :130 still holds exactly emitToZonePlayers and announceZoneCelebration), not :1505 to :1513. (8) The src/sim/sim.ts entry in tests/monolith_budget.test.ts spans :790 to :977 (file: at :820, ceiling 12028 at :975), not ":797 onward". (9) docs/qa-gate.md routes simulation architecture to architecture-reviewer at :630, not :612. (10) announceGatherRareEvent spans gather_events.ts:91 to :118 (the emitToZonePlayers call :98 to :107, the deed and Reliquary hook :108 to :117), not :91 to :107. (11) The sim_context.ts quote "one integer read per tick instead of an O(players) walk" wraps :130 to :131 and greps 0 whole. (12) The Phase 18 ledger row this unit ratifies (state.md:24498) cites "Ledger, state.md:23291-23296" inside its own text, itself stale; amended in place beside the ratification.
- **Reviewers:** server-hot-path-reviewer, architecture-reviewer, test-coverage-auditor, qa-checklist
- **Depends on:** nothing
- **Estimate:** 5h

## Exit criteria

1. Every unit above executed, or escalated with a dated reason.
2. Each unit's domain reviewers dispatched FRESH (never the implementer), all findings applied: blocking, should-fix and nits alike.
3. The fix round re-read by a fresh reader, because fixes are unreviewed code.
4. Every new pin mutated and watched go red. A pin that survives its own mutation is not a pin.
5. `npx tsc --noEmit` clean, the architecture, monolith and parity guards green, the census RESULT PASS, `npm run ci:changed` after the LAST commit.
6. `node scripts/gate_select.mjs` on the COMMITTED tree, pg-armed, judged by exit code.
7. `docs/prd/masterwrought/phase-19e-qa.md` written and its verdict recorded before the next wave opens.

## Standing rules

NO push, NO PR, NO teardown. Local only. A green gate is not consent.
