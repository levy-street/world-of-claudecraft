# Phase 19G: Closeout

Opened 2026-09-01 under ruling qr-19-best-for-project. The two rows that cannot run inside their own wave because they consume 19F output.

**2 units, about 13.00 hours estimated.** Every row here was answered in the Phase 19 rulings gate; this phase executes, it does not re-decide. A row whose ruling turns out to rest on a wrong premise is an ESCALATION back to the ledger, never a quiet re-ruling.

## Units

### D168

> MOVED to 19G: depends on D161 and D169, which are 19F, so it cannot run inside its original wave.

Run the wholesale CI shard-weight re-harvest at go-public riding the delivery PR, re-valuing all 3651 rows off the branch's own green full-mode run and dropping the 712 carried rows, with the non-vacuity pin that the harvest would red repaired honestly in the same change.

- **Files:** `scripts/ci_shard_weights.generated.json`, `tests/ci_shard_partition.test.ts`, `docs/qa-gate.md`, `docs/prd/masterwrought/farming/state.md`, `docs/prd/masterwrought/farming/progress.md` (scripts/ci_shard_weights_harvest.mjs is the tool that writes the table, not a deliverable; dropped from this list at the D168 review round)
- **Same-change obligations:** HARD PRECONDITION: a completed, all-green, FULL-MODE CI run of a PUSHED branch. This worktree is deliberately local, so the row cannot be executed before the push ruling, and the harvest must be the LAST thing that lands, after every code-carrying row in the wave plan, or the table is taken off a tree that no longer exists. SAME-CHANGE OWED: regenerate scripts/ci_shard_weights.generated.json through scripts/ci_shard_weights_harvest.mjs (never hand-edited); relax or reword the carried-set non-vacuity arm in tests/ci_shard_partition.test.ts, because a wholesale harvest writes an empty carried map and zero is not greater than zero, while KEEPING the fabrication check meaningful (the harvestedFiles plus carried identity and the carried-defects modal check must still bite), since that arm is the machine half of the gate reviewer's finding; re-read the live bars off the NEW weights (worst over median at or under 1.15, coverage), where the heal for a breach is a re-partition and NEVER a bar raise; retire the CARRIED to Phase 20 block in docs/qa-gate.md and fix the coverage-literal wording drift there (the live literal is 0.94 while the doc says 95 percent); biome check --write on the JSON only. NOT OWED: no content, i18n, wiki regen, deed, Reliquary, art, parity golden or monolith ceiling. HONEST COST: it re-baselines a repo-wide shared artifact inside a game-content PR, and the harvest commit necessarily lands after the run it was taken from. ANCHOR (grep -c = 1) in /Users/fernando/Documents/wocc-masterwrought/docs/prd/masterwrought/farming/progress.md: 'ride the neutral fallback until scripts/ci_shard_weights_harvest.mjs re-runs;'; the dated RULED line goes AFTER that sixteenth-absorb entry's last line. HANDOFF ROW in farming/state.md, match exactly: '  CI shard-weights harvest re-run at go-public (scripts/ci_shard_weights_harvest.mjs)   16th absorb   go-public session   handed-to-maintainer  '. STATUS CELL REPLACEMENT: 'handed-to-maintainer' becomes 'CLOSED 2026-09-01 by ruling qr-19-ci-shard-weights-go-public-harvest (Phase 19, under qr-19-best-for-project): the wholesale harvest rides the delivery PR after its first green full-mode run, with the carried-set non-vacuity arm repaired in the same change.'
- **Reviewers:** gate-integrity-reviewer, qa-checklist
- **Depends on:** D161, D169, D009+D170, D171
- **Estimate:** 6h
- **AMENDED IN PLACE 2026-09-02 at execution, seven corrections at the pre-execution verification pass (the session starter's Step 1) and twenty-six in all after the review rounds below; the row is ESCALATED by the maintainer in session and NOT executed.** (1) ESCALATED 2026-09-02 (the maintainer's answer: 'Escalate whole'): the HARD PRECONDITION, a green FULL-MODE CI run of a PUSHED branch, is unreachable under the standing NO-push rule, and the dependency D009+D170 has been ESCALATED and unexecuted since 19A (the carrier assignment is the maintainer's), so the harvest could not be the LAST code-carrying change even if the run existed. No file in the harvest set moves; the docs-drift rider (95 percent in docs/qa-gate.md against the live 0.94 literal) stays bundled with the harvest change by the maintainer's choice. The ready-to-run recipe stays on the row for the push ruling (D022, ip-17-push-consent). (2) COUNTS: the committed table at 6377bed8b0 is 3650 rows, harvestedFiles 2938, 712 carried (302 local-median with the reason 'phase 18 local carry pending the post-push harvest' plus 410 prose-backfill), not 3651 and 2939 (one row was pruned after the row was priced). (3) The HANDOFF ROW anchor is pipe-flattened (grep -cF = 0); the live row is '| CI shard-weights harvest re-run at go-public (scripts/ci_shard_weights_harvest.mjs) | 16th absorb | go-public session | handed-to-maintainer |' in farming/state.md. Under the escalation its status cell is NOT flipped (the 19A precedent: an escalated row stays open); the STATUS CELL REPLACEMENT text is the execution shape and waits for the push ruling. (4) PINS BY NAME, all in tests/ci_shard_partition.test.ts: the arm a wholesale harvest reds is 'the identity is non-vacuous: real rows on both sides, and every carried row is a row' (its carried-count floor of one); the bars are 'balances total weight closer than contiguous equal-size slices on a skewed set' and 'partitions the real tests/ tree into N complete packs (suite completeness)' (worst over median at or under 1.15; coverage at or over 0.94); the fabrication check is 'an undeclared row, a mis-stated ms, and a fabricated fallback block each RED it' plus 'passes carriedDefects with the map REQUIRED, not merely consistent when present'. (5) The farming/progress.md anchor holds (grep -cF = 1); a dated ESCALATED line goes after that sixteenth-absorb entry's last line in place of the RULED line. (6) RECORDING: D168 is a phase-19-new-rows.md row (D152 to D176 live there, not in the gate table), so its ESCALATED clause is appended to its FLAGS line there (the D169 shape), never in phase-19-rulings-gate.md. (7) Dependencies: D171 is executed first in this phase (the order this phase demands); D161 and D169 landed at 19F. CORRECTED 2026-09-02 by the D168 review round (gate-integrity-reviewer), eight more, all about the recorded recipe rather than the escalation: (8) a wholesale harvest reds TWO arms of tests/ci_shard_partition.test.ts, not one: the identity arm named in (4) AND 'an undeclared row, a mis-stated ms, and a fabricated fallback block each RED it', whose mis-stated-ms and modal mutations read the live carried map (the first throws on an undefined key, the second has nothing to fabricate); the repair moves those two mutations onto a synthetic table that carries rows, never behind a truthiness guard on the live map, and tests/ci_shard_weight_carry.test.ts already pins all three defect classes on fixtures. (9) THE COVERAGE DRIFT RAN THE OTHER WAY: the release commit b38f38ba16 lowered the test literal from 0.95 to 0.94 and rewrote its comment, while docs/qa-gate.md (five sites) and scripts/merge_audit/shard_weight_union.mjs (which refuses under 0.95) kept 95 percent; the rider is therefore 'reconcile the three surfaces', with the recommendation to RESTORE 0.95 in the test (live coverage 0.9989; MEASURED_FALLBACK_MS is 28 ms, so every uncovered file is planned as nearly free), never to write 0.94 into the prose; the choice is the maintainer's at the harvest. (10) The 'CARRIED to Phase 20' block in docs/qa-gate.md is the only complete acceptance list; it is retired only in the commit that lands a replacement carrying BOTH the block's own four acceptance items (the pre-run row-count floor cleared; harvestedFiles equal to the row count with carried empty; the partition suite fully green, coverage clearing the literal AND the stale-row arm finding nothing; worst over median at or under 1.15) AND the three procedural items the row's recipe would otherwise lose (the floor command itself, read BEFORE the run because a smaller result afterwards means the run was not full mode; tests/ci_shard_weight_carry.test.ts in the vitest command; the --carry-local-missing fallback when no green full-mode run exists); the D168 row's recipe now carries all of them (the round-one wording listed four items, two of the block's own acceptance items and two procedural ones, and called them the block's criteria; the fresh read corrected the label). (11) Of the two bars in (4), only 'partitions the real tests/ tree into N complete packs (suite completeness)' reads the table; 'balances total weight closer than contiguous equal-size slices on a skewed set' is synthetic and cannot move; the literal arms a re-harvest CAN move are the named-file weight arms (druid_balance_probe above 120000, mail_expiry below 80000, at least five files above 60000) [this list is NOT exhaustive: superseded by (22)]. (12) 'Heal a breach by a re-partition' names no remedy (the LPT packer is deterministic with no tunable); the heals are a re-harvest, --carry-local-missing for uncovered rows, or a deliberate shard-count change, never a bar raise. (13) Correction (4) had swapped the contract's 'carried-defects modal check must still bite' for the requireMap arm without saying so; the modal check is unreachable on an empty map (gated on carriedKeys.length > 0 in scripts/lib/ci_shard_weight_carry.mjs), so the achievable requirement is the requireMap arm plus the three defect classes on fixtures; recorded as the correction it is. (14) 'biome check --write on the JSON only' is a RED step, not a no-op (biome.json excludes the table, so biome exits 1 with 'No files were processed in the specified paths'; serializeWeightTable is the table's formatter and the exclusion is load-bearing), dropped from the recipe with the warning never to 'fix' it by un-ignoring the file; docs/qa-gate.md carries the same inherited step, to drop at the harvest. (15) scripts/ci_shard_weights_harvest.mjs is the tool, not a deliverable (no edit to it is owed), dropped from the Files list. Carried, not part of the row: the harvester accepts a log with no mode line at all (only mode=(unset) and non-full modes red), backstopped by the row-count floors; the union tool's 0.95 floor is asserted by no test. CORRECTED 2026-09-02 by the D168 qa-checklist read, three more: (16) the STATUS CELL REPLACEMENT text above carries a frozen date (2026-09-01) and the label 'Phase 19'; copied verbatim after the push ruling it would stamp a past date, so at execution the cell takes the harvest commit's own date and reads 'escalated at Phase 19G, executed at the push ruling'. (17) The identity arm's in-code comment ('2939 harvested + 410 carried = 3349 at Phase 18') is already stale (2938, 712) and the harvest makes it wrong a third way; the recipe on the row tells the executor to re-cut it. (18) 'No file in the harvest set moved' is imprecise: of the Files list, farming/progress.md moved at the escalation commit dda61d91be and at each record round since, and farming/state.md at the round-one commit 50620a909a (both by design, they are the record); the code and the artifact did not. CORRECTED 2026-09-02 by the fresh read of the D168 round (round two), three more: (19) DISPOSITION of the D168 qa-checklist read's fourteen items, so none is left unjudged: S1 (two arms red) applied as (8); S2 (the heal) applied as (12); S3 (the frozen status-cell date and label) applied as (16); S4 (the five qa-gate sites and the union tool's own floor) applied as (9); S5 (four records point at phase-19g-qa.md, not yet in the tree) is exit criterion 7, written at the close; N1 as (18); N2 (the farming line names its commit) applied in round one; N3 (the escalation commit's body says the dated line 'closes' the sixteenth-absorb entry where the handoff row stays open) is history the commit keeps, recorded here as an imprecision; N4 (the Phase 20 section's intro sentence) applied in round one; N5 as (17); N6 (item 21 of farming/state.md now points at the row) applied in round one; N7 (the packet progress.md phase table lacks a 19G row) is written at the close; N8 (stale line addresses on the row's historical pricing text) recorded on the row's AMENDED clause with the live addresses at dda61d91be, the pricing text itself never rewritten; N9 (the D022 row now lists D168) applied in round one. (20) Correction (6) said nothing is written in phase-19-rulings-gate.md; round one wrote a dated dependency NOTE on the D022 row there, a recorded exception (a pointer, not a record of the ruling), and the three record-placement edits of round one (that note, the item-21 pointer, the Phase 20 section's intro) belong to this list. (21) Numbers this bullet quotes that are not literals in the tree carry their tip: MEASURED_FALLBACK_MS 28 ms and live coverage 0.9989 are as measured at dda61d91be. The bold header above states both counts. CORRECTED 2026-09-02 by the fresh read of the D168 round two (round three), three more, all record consistency: (22) correction (11) was stated as exhaustive and is not: the literal assertions a re-harvest can move are the row's step-4 list, BY ARM: 'prefers a measured duration over every heuristic and falls back cleanly' (druid_balance_probe above 120000); 'a table member returns exactly its measured ms, anchored independently' (mail_expiry below 80000, and the derived heaviest-row bound, the table's argmax above 120000); 'carries a plausibly complete measured table with provenance' (the variance floor of at least five files above 60000, the per-row 20-minute ceiling, the 2400-row vacuity floor); 'the identity is non-vacuous: real rows on both sides, and every carried row is a row' (its 3000-row and 2000-harvested floors); two more table-reading floors exist that no real harvest can breach (every row's ms above zero; the fallback median above zero) and are listed only so this list is complete. The row's list is the authoritative one. (23) The three of round two's recipe edits not already numbered (its two step-6 edits are recorded by amending (10) and (14)) belong on this list too: step 2 names the harvester's fewer-than-ten-green-jobs refusal (the eight shards plus the two lanes); step 4 carries the expanded literal-arms list; step 4 states the coverage heal (--carry-local-missing for the uncovered rows, or a fresh harvest) apart from the ratio heal (a deliberate shard-count change or measuring where the distribution moved), never a bar raise. (24) The row's AMENDED clause cited the harvester's harvestedFiles-plus-carried write as :306-316, the whole out literal (:306 opens the object; the provenance literal is :307-316); the write itself is :314-315 at dda61d91be, and the clause now says so. CORRECTED 2026-09-02 by the fresh read of the D168 round three (round four), two more, both this list's own hygiene: (25) round three's step-3 edit, the carried-count relaxation stated as the REPAIR ('is RELAXED to admit an empty map (today it refuses one)') rather than as current behavior; (26) round three's step-6 edit, the block's procedural items given their true subject (they are lost by RETIRING the block, and the recipe carries them). The bullet carries twenty-six corrections in five dated batches (the Step 1 amendment and four CORRECTED batches). Corrections are appended, never rewritten; an earlier correction a later one supersedes carries only a forward pointer, as (11) does to (22).

### D171

> MOVED to 19G: depends on D161 and D169, which are 19F, so it cannot run inside its original wave.

Restore the Phase 06 rung-50 scroll and elixir input parity by adding frost_gourd count 1 to recipe_sunpetal_scroll (the only reagent whose unit value lands the repair exactly), and pay the row's second half whichever way it is ruled: the pin the ledger asked for and never got, plus the two false comment clauses and the false player-facing wiki claim.

- **Files:** `src/sim/content/recipes.ts`, `tests/inscription_catalog.test.ts`, `tests/recipe_economy.test.ts`, `tests/provisioning_supply_line.test.ts`, `src/ui/i18n.catalog/guide.ts`, `src/ui/i18n.locales/ja_JP.ts`, `src/ui/i18n.locales/ko_KR.ts`, `src/ui/i18n.locales/ru_RU.ts`, `src/ui/i18n.locales/zh_CN.ts`, `src/ui/i18n.locales/zh_TW.ts`, `src/guide/content.generated.ts`, `src/ui/i18n.status.json`, `docs/prd/masterwrought/state.md`, `docs/prd/masterwrought/progress.md`
- **Same-change obligations:** MAINTAINER VALUES NEEDED: (a) the REAGENT that carries the repair, since frost_gourd at unit value 15 is the only exact-parity item in the catalog and admitting it puts the first crop on an inscription row and a farming-50 gate on a scribe's own rung, while the ink register cannot reach 15 and lands 3 copper off either way; (b) the BAND SCOPE (rung 50 alone, rung 50 plus the unrecorded 16 copper rung-25 break, or all three bands); and (c) whether the pin records the NUMBERS so a future produce insertion reds, or only the doctrine so bills may float. SAME-CHANGE OWED WHICHEVER ARM IS PICKED, because the shipped record and the shipped player copy both assert the parity as present fact: author the parity pin the ledger asked for and never got, computing both bills through the shipped reagent unit-value rule (it cannot simply live in the inscription-only suite, so either that file's scope widens by one stated exception or the arm lands beside the output-under-input sweep in tests/recipe_economy.test.ts); rewrite the 22-line standing note in tests/inscription_catalog.test.ts from an open question into the ruling and re-cut the bill literal; correct the two now-false clauses in the recipes.ts comment (the rung-25 parity claim, live 90 against 106, and the only-crafting-sink claim, now three apex flask sinks); reword the false wiki prose that tells players the batch is priced even with the elixir, which makes the five non-Latin overlay rows stale, so a wordy replacement pulls its M16 fills in-change (the 15 Latin rows are already pending, so no new pending row is minted); npm run i18n:gen. WIKI REGEN IS OWED on the repair arms only (the bill is carried verbatim in src/guide/content.generated.ts), freshness-gated by tests/guide.test.ts. ACCENT-RULE INTERACTION: the repair puts a crop on a non-cooking, non-alchemy row, which the RULE 2 accent sweep's filter does not govern; the row clears both readings on the numbers, but leaving the filter unwidened silently exempts a shipped crop placement from the rule D004 settles, and the touched-rows profession assertion moves if the row joins. NOT OWED: no new item id, so no art, no ITEM_ART_PENDING or art-audit re-mint, no deed, no Reliquary page, no shipped-id golden, no census row, no world_api parity or golden, no monolith ceiling. R5: r5Surface is true but the recommended arm does NOT move the frozen record; only the all-bands arm edits an input to power-verification section 12.2 and that is the R5 escalation, which stays refused while R5 is frozen. REGEN ORDERING: batch the i18n regen with D161 and D169 or land after them. ANCHOR (grep -c = 1) in /Users/fernando/Documents/wocc-masterwrought/docs/prd/masterwrought/state.md: 'THE SUNPETAL SCROLL / SERPENT ELIXIR PARITY IS NOW BROKEN'; the dated RULED line goes AFTER that finding block's last line. NO handoff table row: this is a seeded row 3 member and its open text lives in state.md prose plus a progress.md open line, both of which take the ruling id.
- **Reviewers:** content-obligations-reviewer, frontend-seam-reviewer, test-coverage-auditor, qa-checklist
- **Depends on:** D161, D169
- **Estimate:** 7h
- **AMENDED IN PLACE 2026-09-02 at execution, twelve corrections; the ruled arm survives all of them, one premise was corrected before the maintainer's values were taken, and the values were taken in session, never guessed.** (1) THE PREMISE 'the only reagent whose unit value lands the repair exactly' IS FALSE on the live catalog at 6377bed8b0: under the shipped unit-value rule (buyValue when finite and positive, else sellValue) every tier-3 crop lands 15 (frost_gourd, highland_barley, thornpeak_cabbage, frost_lentils, all farming-50 gated) and so does chipped_tusk (a zone-4 poor drop, output-excluded); the ink register still cannot reach 15 (226 or 232). The corrected menu was put to the maintainer before any value was taken. Bills re-derived live through that rule: recipe_sunpetal_scroll 214, recipe_elixir_of_the_serpent 229, the rung-25 pair 90 against 106, the rung-0 pair 26 against 36. (2) MAINTAINER VALUES, taken in session 2026-09-02: (a) frost_gourd count 1, because it mirrors the elixir's own produce line so the twin bills differ only in gland-against-ink; (b) rung 50 ALONE (the rung-25 and rung-0 gaps stay recorded as facts, not repaired; the all-bands arm is the R5 escalation and was not taken); (c) the pin records the NUMBERS and the equality (both bills computed through the shipped rule, each exactly 229 and equal to each other). (3) The player-copy correction is a RETIRE-AND-RE-KEY, never an in-place reword (the reword-is-a-new-key convention, the D169, D144 and D085 precedents): guide.profPages.craftProse.inscription.materialsBody is retired (a RETIRED_KEYS row in scripts/i18n_retired_keys.mjs; its five reviewed non-Latin rows are kept) and its successor guide.profPages.craftProse.inscription.materialsBodyFrostGourd carries the corrected English, wired through the bodyKey override of proseSection in src/guide/pages/professions_craft.ts (the engineering precedent). The successor MINTS fifteen new pending Latin rows (the 'no new pending row is minted' clause was true only of an in-place reword, which the convention forbids; the retired key's fifteen pending rows leave the worklist through RETIRED_KEYS), and the five non-Latin fills are fresh full translations, fact-checked against the live sim before filling and FLAGGED for the maintainer's read in phase-19g-qa.md; a census row in docs/prd/masterwrought/merge-deletion-list.md and a hand-carry bullet in phase-20-release-fill-and-reclose.md ride the same change. (4) FILES: src/ui/i18n.status.json is gitignored (untracked; the D148 record) and is not a deliverable, dropped. ADDED: tests/material_profession_affinity.test.ts (its arm pins craftIdsForMaterialItem('frost_gourd') to exactly ['alchemy', 'cooking'], which moves to ['alchemy', 'cooking', 'inscription'] in CRAFT_RING order; the Frost Gourd tooltip's Used-by line gains Inscription through formatList, no new i18n row; the Fine Frost Gourd is farming's own twin record, not a material_grades grade, so it inherits nothing and keeps 'Used by Cooking.', pinned in the same arm), scripts/i18n_retired_keys.mjs, src/guide/pages/professions_craft.ts, tests/guide.test.ts (the English per-clause pin and the five-locale pin over the successor prose; no arm covered the predecessor), src/ui/i18n.catalog/translation_keys.generated.ts and src/ui/i18n.resolved.generated/* (the regen), docs/prd/masterwrought/merge-deletion-list.md, docs/prd/masterwrought/phase-19-new-rows.md (the ANSWERED clause), docs/prd/masterwrought/phase-20-release-fill-and-reclose.md (the hand-carry). (5) ANCHORS drifted: the guide prose sits at src/ui/i18n.catalog/guide.ts:2896-2897 at 6377bed8b0 (not 2874-2875) and the five overlay rows at ja_JP.ts:12734, ko_KR.ts:12719, ru_RU.ts:12928, zh_CN.ts:12172, zh_TW.ts:12174 (each four lower than cited); the durable anchor is the key name. The state.md anchor holds (grep -cF = 1) and the finding block's last line is 'option should pin it in the same change.'; the RULED line goes after it at column 0 (the block is column-0 prose under a '### ' heading). The progress.md open line is the 11g QA row ('THREE open items stay the maintainer's: the scroll/elixir parity this phase broke by 15 copper'), which takes the ruling id in place. (6) RECORDING: D171 is a phase-19-new-rows.md row (D152 to D176 live there, not in the gate table), so its ANSWERED clause is appended to its FLAGS line there (the D169 shape), never in phase-19-rulings-gate.md and never in the state.md draft table. (7) THE ACCENT-RULE INTERACTION, corrected: TOUCHED_ROWS in tests/provisioning_supply_line.test.ts is the literal of the nine rows Phase 11g touched (its length is pinned to 9 by 'the touched-row table'), so the scroll row must NOT join it and 'every touched row belongs to a CONSUMABLE profession' does not move; the widening lands on the DERIVED predicate accentGovernedRows instead (every non-farm consumable row that consumes produce, whatever its profession), with a membership pin that the scroll row is governed and clears both readings (crop 1 below essence 2; 15 at or under the herb's 160), and, on RULE 2's open value half, both competing value references as well (15 under the dominant-contribution reference of 160 and under the count-reference of 36, essence 2 at 18; only the contribution reading is pinned, the other being the D004 question); the refused-under-count list does not change. (8) THE PIN lands in tests/recipe_economy.test.ts beside 'every non-legacy recipe vendors strictly below its input value' (that file owns reagentUnitValue); tests/inscription_catalog.test.ts keeps its byte literal (frost_gourd: 1 added) and its standing note becomes the ruling with a pointer to the pin by name. (9) THE COMMENT CORRECTIONS in src/sim/content/recipes.ts, verified live: pristine_venom_gland has FOUR sinks (the serpent elixir plus the three apex flasks), the rung-25 pair is 90 against 106 since 11g's bog_beet 2 on recipe_venomfire_elixir (recipe_goldleaf_scroll's own 'Input 90 vs output 15' stays true), and rung 0 is 26 against 36 since 11g's vale_wheat on recipe_elixir_of_the_boar. (10) WIKI REGEN IS OWED (the bill moves): npm run wiki:content, freshness-gated by 'matches the sim (regenerating leaves the committed file unchanged)' in tests/guide.test.ts; tests/inscription_flow.test.ts and tests/professions_crafting.test.ts grant reagents off the live recipe and need no edit. (11) Dependencies D161 and D169 landed at 19F (32e6e8fcf6 then 31084b67ef; b1d693efed), so the REGEN ORDERING clause is discharged. (12) Reagent placement: the crop goes second, after the rung herb (every LADDER row of inscription leads with its rung herb; the writ and the voidbound grimoire, outside the ladder, lead with vellum) and before the ink register; the crafting UI and the generated wiki carry the array verbatim. CORRECTED 2026-09-02 by the D171 review rounds (content-obligations, frontend-seam, test-coverage, qa-checklist and the fresh reads), the items that changed a record rather than a pin: (13) the accent-governed floor in tests/provisioning_supply_line.test.ts stood at 17 against a derived set of 21 (twenty by the syncs since 11h, twenty-one with the scroll), the slack its own docblock warns about; raised to 21 in the round. (14) R5: section 12.2 of docs/design/power-verification.md is untouched as (2) says, and section 12.3, which takes this recipe's reagent value as a live input, was RE-DERIVED by the content review through the live gain curve on 12.3's own sellValue basis: the inscription intended path's reagent value moves 15525 to 15900, the cheapest path stays 12650, the ratio moves 0.8148 to 0.7956 inside the printed 0.66 to 0.88 band, the craft multiple stays 2.00 and the printed 125 intended crafts reproduce, so every printed claim of the frozen record survives and no doc edit is owed. (15) The new prose's 'only the vial is bought for coin' is true of the reagents (the gourd's SEED is bought from Farmer Hollis at 32 copper, the same convention the cooking and farming pages use). (16) The player surfaces: the farm page's beds prose named two produce-buying crafts and nowhere the third, so a NEW sibling paragraph guide.profPages.farm.bedsBodyThirdCraft renders after the shipped bedsBody (untouched, its reviewed fills kept), with five fresh non-Latin fills, a census row and a Phase 20 hand-carry; the inscription page's lead sentence and materials heading were asked for too and REFUSED with the reason in phase-19g-qa.md. (17) The pins: every count in the five inscription fills is derived from the live bills and bound per clause in a per-locale forms table; the sweep's exclusion set is pinned to the hoe ladder; the Frost Gourd's three-craft Used-by line is pinned as a string with its fine twin; the unit-value rule lives once in tests/helpers/reagent_unit_value.ts; the English arm's narrowness is exact (undo the two edits and the predecessor returns). CORRECTED 2026-09-02 by the fresh reads of round one (coverage and frontend-seam), round two: (18) the five-locale inscription arm windowed only the grimoire and scroll clauses, so a false count at the head of paragraph two or a sentence slipped into the closing region survived (two green mutants); each locale now carries an exact narrowness transform (its successor-only runs mapped back to the predecessor's text, each present exactly once) and a per-count numeral weld, so the fills' paragraph two is the predecessor's plus the two repairs and nothing else. (19) 'A third craft buys from the beds too' was a count the page never defines (the hoe ladder takes fine produce as a tool reagent, and the five reviewed bedsBody fills name the kitchens only, so the ordinal had no antecedent for those readers): the sentence now names the scribe's desk and no ordinal, the five fills were re-filled (each kept byte-identical after its opening), the English arm derives BOTH the consumable buyers (alchemy, cooking, inscription) and the any-produce buyers (those plus engineering, whose every such row is a gathering tool), so the carve-out is pinned positively, and a five-locale arm pins the farm fills the way the inscription fills are pinned. (20) The new paragraph had no render anchor in the page arm; it has one, apostrophe-free, so deleting its paras() call reds. (21) The helper claimed the rule lived once; six restatements existed. Three more now import it (tests/provisioning_supply_line.test.ts, its apex twin, tests/professions_tool_effect_recharge.test.ts) and the two that restate it on purpose are named in the helper (tests/farm_recipes.test.ts by its own header; the inline variant in tests/masterwrought_budget.test.ts). (22) The five reviewed bedsBody fills are stale in ALL FIVE non-Latin locales (a pre-11g English, the kitchens as the only buyer) and keep 'Vale Wheat' and 'Marsh Rice' in English: carried for Phase 20, not minted here; the inscription materials heading's under-count is flagged for the same pass, the refusal re-stated on cost.

## Exit criteria

1. Every unit above executed, or escalated with a dated reason.
2. Each unit's domain reviewers dispatched FRESH (never the implementer), all findings applied: blocking, should-fix and nits alike.
3. The fix round re-read by a fresh reader, because fixes are unreviewed code.
4. Every new pin mutated and watched go red. A pin that survives its own mutation is not a pin.
5. `npx tsc --noEmit` clean, the architecture, monolith and parity guards green, the census RESULT PASS, `npm run ci:changed` after the LAST commit.
6. `node scripts/gate_select.mjs` on the COMMITTED tree, pg-armed, judged by exit code.
7. `docs/prd/masterwrought/phase-19g-qa.md` written and its verdict recorded before the next wave opens.

## Standing rules

NO push, NO PR, NO teardown. Local only. A green gate is not consent.
