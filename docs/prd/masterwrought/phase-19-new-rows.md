# Phase 19: the 25 newly priced decision rows (D152 to D176)

Appended after the Phase 19 completeness pass. Same contract as D001 to D151.

### D152  [structural-affinity-exemption]
SEED: row 4 (the standing farming deviations '(a), (w), (al), (ap), (bb), (be), (bg), (bh)', to be priced one row each; phase-19-rulings-gate.md:195 as read). Siblings already expanded: (a) as D027, (bb) as D023, (be) as D030, (bg) as D021; (w) was left unexpanded.
QUESTION: Ratify the structural farming exemption in the affinity census as it stands, a blanket skip over every id FARM_MATERIAL_ITEM_IDS derives (39 today, not the 27 the ledger still records), or narrow it to the fifteen ids that can never have a recipe consumer, now that every produce and fine twin has one? A third disposition rides the same ruling: whether the exemption's one player-facing residue, the material stacks whose tooltip can never render a Used-by line, is closed with purpose lines or left as recorded.
ANCHORS: LEDGER: farming/state.md:472 as read, the handoff row '| (w) structural affinity exemption, 27 materials (standing since Phase 4) | P4 / P5 | maintainer | open ruling-owed |', still open where its neighbours at :469 and :477 carry dated CLOSED text; the maintainer-reads-owed gate at farming/state.md:410 as read, '(w) the structural affinity exemption (standing since Phase 4, now 27 materials)'; the deviation block itself at farming/state.md:2741-2757 as read, recording that the enumerated CONSUMER_DEFERRED_MATERIALS list was REPLACED rather than shrunk because 'the list's self-clearing arm keyed on RECIPE consumers (craftIdsForMaterialItem scans recipes and enchants only)' and because 'compost/growth_tonic will NEVER gain a recipe consumer at all'; the Phase 5 QA uphold at farming/state.md:3665 as read ('the deviation (w) auto-exemption with the exact-set pin as its gate'); the superseded predecessor at :2630 and :2717 as read. SEED: phase-19-rulings-gate.md:195 as read (seeded row 4, 'to be priced, one row each'). LIVE CODE, all re-read in this worktree: the exemption is a TEST-suite construct only, tests/material_profession_affinity.test.ts:16-41 (the banner, whose HISTORY half at :28-41 records the replacement and then anticipates the dish consumers arriving with 'the id starts passing the census on its own terms and the exemption stays true (the watch fee still consumes produce)') and :42 (const COMMAND_CONSUMED_FARM_MATERIALS: ReadonlySet<string> = new Set(FARM_MATERIAL_ITEM_IDS)); the census arm it skips at :277-306, with the non-vacuity pair at :304 (expect(exempted).toBe(COMMAND_CONSUMED_FARM_MATERIALS.size)) and :305 (MATERIAL_ITEM_IDS.size - exempted - pending.size above 40); the honesty arm at :308-320, which checks only that every exempted id is still a material and never that it still NEEDS the exemption. The set's content source is src/sim/content/farm_crops.ts:355-359, derived from FARM_CROPS plus FARM_WITHERED_HUSK_ITEM_ID (:332) plus FARM_SUPPLY_ITEM_IDS (:339-342). The smuggle guard the record leans on is the exact-set pin at tests/material_taxonomy.test.ts:430-484, whose sorted literal at :444 lists all 39 ids by name. THE LEDGER COUNT IS STALE: the live figure is 39, pinned at tests/professions_zone_rollout.test.ts:1008 (expect(FARM_MATERIAL_ITEM_IDS.length).toBe(39)), with that arm's own comment at :1003-1005 explaining '27 before Phase 11e, plus that phase's four crop families at three ids each'. THE GROUND HAS MOVED FOR MOST OF THE SET: the affinity leaf reads recipes and enchants only (src/sim/material_profession_affinity.ts:22-25 imports, deriveDirectCraftConsumers at :28-48, craftIdsForMaterialItem at :67; fine grades inherit only through baseMaterialFor, and farming twins deliberately carry no MATERIAL_GRADES row per deviation (o), farming/state.md:2660-2666 as read), and a tally over src/sim/content/recipes.ts re-derived here shows all 12 produce ids and all 12 fine twins now appear as live reagents (vale_wheat 7, highland_barley 7, evergarden_greens 7, bog_beet 4, frost_gourd 4, marsh_rice 4, brook_carrot 3, gilded_sunmelon 3, thornpeak_cabbage 2, evergarden_pumpkin 1, frost_lentils 1, gilded_yam 1, and at least one row for every fine_ twin), while NO seed id appears as a reagent anywhere in that file and compost, growth_tonic and withered_husks cannot by construction. So 24 of the 39 would pass the census unaided today and 15 would not. THE PLAYER-FACING RESIDUE: the shipped tooltip takes the same zero-consumer branch, src/ui/hud/professions/material_profession_hint_view.ts:86-99 returning '' when craftIdsForMaterialItem is empty, with src/ui/hud.ts:6611 its one call site (no guide, wiki or script consumer), so compost renders as a Materials-classified stack with nothing in game saying what spends it (src/sim/content/items.ts:1111-1117, a name-only def; src/ui/i18n.catalog/items.ts:3209 name only), as do the twelve seeds (catalog :3202 name only) and withered_husks; growth_tonic is the one id already covered, through the purpose-line route at src/ui/hud/professions/material_hint_view.ts:77 with its copy at src/ui/i18n.catalog/hud_chrome.ts:4151-4153. PRICING SCOPE: scripts/merge_audit/symbol_census.mjs:110, EXPORT_ROOTS = ['src', 'server', 'headless', 'bot', 'scripts'], so a test-only change owes no merge-deletion-list row and a new export under src/ does; docs/prd/masterwrought/implementation-plan.md:152, the Review Dispatch Matrix, skips architecture-reviewer for pure data and test changes.
OPTIONS:
  1. [no-op] Ratify the structural exemption as it stands, blanket over the whole derived set, with the stale count corrected in the same line
     PRICE: Genuinely no-op in code. One dated RULED line (qr-19-structural-affinity-exemption) where the row stands, flipping the status cell at farming/state.md:472 and the maintainer-reads-owed entry at :410, plus the free editorial rider correcting '27 materials' to the live 39 in both places (the derivation already exists at tests/professions_zone_rollout.test.ts:1003-1005, so the correction cites rather than invents). No source edit, no test edit, no pin re-mint, no wiki regen and so no tests/guide.test.ts freshness exposure, no i18n key and so no M16 non-Latin fills, no census row (the exemption lives under tests/, outside EXPORT_ROOTS at symbol_census.mjs:110), no deed record, no Reliquary page, no committed WebP and no item_art_consistency re-mint, no parity golden, no tests/world_api_parity.test.ts member, no movement against any ceiling in tests/monolith_budget.test.ts. Reviewer: qa-checklist only. What it concedes, stated so the ruling is informed: nothing in the tree can then notice a farming material that quietly loses its only real consumer, because both arms that could bite are blanket-skipped or command-covered (the census arm skips all 39; the closure arm at tests/professions_zone_rollout.test.ts:976-1012 counts every seedItemId and produceItemId as live by construction), so a future crop shipping produce with no dish passes both.
  2. [WORK] Narrow the exemption to the fifteen structurally uncoverable ids (the twelve seeds, withered_husks, compost, growth_tonic), leaving the twelve produce and twelve fine twins subject to the census on their own terms
     PRICE: Verified GREEN before proposing: the tally above shows all 24 produce and twin ids carry at least one live reagent row in src/sim/content/recipes.ts, so the narrowed census bites nothing on the current tree. Two shapes with different bills. TEST-LOCAL: derive the narrow set inside tests/material_profession_affinity.test.ts (seeds from FARM_CROPS plus the three named ids), rewriting the banner at :16-41, the set at :42, the exempted non-vacuity at :304 and the honesty arm at :308-320; no census row, no new export, but the anti-smuggle anchoring the Phase 4 record prizes ('smuggling an orphan into the exemption requires editing content') weakens, because the narrow set would then be derived in the test rather than in content. CONTENT-SIDE: add a derived sibling export beside FARM_MATERIAL_ITEM_IDS at src/sim/content/farm_crops.ts:339-359, which keeps the content anchoring and owes an 'Explained extras' row in docs/prd/masterwrought/merge-deletion-list.md for the new name (src/ is a census root, symbol_census.mjs:110), plus the same four test edits. Either way: no wiki regen, no guide reword, no i18n key, no M16 fills, no deed, no Reliquary page, no art, no parity golden, no world_api member, no monolith ceiling. Re-run tests/material_profession_affinity.test.ts, tests/material_taxonomy.test.ts and tests/professions_zone_rollout.test.ts together, the three being deliberately coupled as each other's smuggle guard (professions_zone_rollout.test.ts:987-990). Reviewers: qa-checklist, plus content-obligations-reviewer on the content-side shape; architecture-reviewer skipped under implementation-plan.md:152 for a pure data and test change. What it buys and what it costs are one fact: a future crop whose produce ships before its dish reds the census instead of passing, coverage nothing in the tree has today, and which would have fired during Phase 11e's four-family widening before Phases 11h and 11j authored the dishes.
  3. [WORK] Keep the exemption and close its player-facing residue: author tooltip purpose lines for the ids whose Used-by line can never render
     PRICE: Orthogonal to the census question and rulable alongside either option above. New hudChrome.materialHint keys in src/ui/i18n.catalog/hud_chrome.ts beside growthTonic at :4151-4153, written from the live mechanic per docs/design/tooltip-writing.md and the write-game-tooltips skill (plant_crop spends a seed, convert_husks eats husks at FARM_HUSKS_PER_COMPOST, compost raises the survival roll), English only per the contributor rule with the non-Latin fills routed to the Phase 20 release fill unless the maintainer wants them in-change under M16 (a wordy English value pulls its own); rows in the MATERIAL_HINT_KEYS map at src/ui/hud/professions/material_hint_view.ts:77 onward; the exact-set arm at tests/material_hint_view.test.ts:89 ('no more and no less') flips and the render arm at :160 gains the new lines; a per-seed key would be twelve near-identical values, so the cheaper authoring is one shared seed lead, the way the nine intermediates share one (:95). Each new i18n key owes a merge-deletion-list 'Explained extras' row (src/ui is a census root). No wiki regen (src/ui/hud.ts:6611 is the only call site; no guide or wiki surface reads it), no deed, no Reliquary page, no art, no parity golden, no ceiling movement (the copy lands in the catalog and the view module, never in hud.ts). Reviewers: frontend-seam-reviewer plus qa-checklist.
  4. [WORK] Restore an enumerated deferral list in place of the structural exemption
     PRICE: The disposition the Phase 4 record already argued against, priced so the ruling is complete rather than because it is attractive. Reintroducing CONSUMER_DEFERRED_MATERIALS means re-authoring the self-clearing arm the record says could never fire for command consumers (farming/state.md:2741-2751 as read), and it puts compost and growth_tonic on a list whose contract promises they will one day leave it, which the same record calls a permanent lie because neither will ever gain a recipe consumer; a hand list of fifteen or thirty-nine ids also re-opens the maintenance burden the derivation removed (Phase 11e added four crop families and FARM_MATERIAL_ITEM_IDS needed no edit, farm_crops.ts:349-352 as read). Cost is a full rewrite of tests/material_profession_affinity.test.ts:16-320 plus a list every future crop phase must edit, for coverage option 2 delivers derived.
RECOMMENDATION: Ratify as standing, with the 27-to-39 correction riding the same line: the packet's own records lean that way twice, the Phase 5 QA having re-judged and upheld the auto-exemption with the exact-set pin as its gate (farming/state.md:3665 as read) and the suite's own banner having anticipated exactly the dish consumers that later arrived, declaring no edit owed (tests/material_profession_affinity.test.ts:38-41), so the blanket is stale in its stated count but not false in its claim, since the watch fee still consumes produce; the narrowing is a real and cheap tripwire nothing in the tree has today, but the records never asked for it, which is why it is offered rather than recommended.
FLAGS: needsMaintainerValue=false; r5Surface=false; contingentOn=none

### D153  [art-suite-guard-extensions]
SEED: row 4
QUESTION: Ratify the branch's two extensions to the release-owned art-program guards, together with the standing obligations they impose: (1) the ART-SUBJECT rule plus the pendingArtCount debt literal in the item art audit, which lets a live def ship artless while it sits on ITEM_ART_PENDING, and (2) the fingerprint-only receipt-free refresh in the portrait manifest guard, gated on the drift classifier's bookkeepingOnly verdict. Or refuse them and restore the release guards as written? (The bundle-hash renderer/content split, the other half of deviation (al), is D029's; this row is the extensions themselves.)
ANCHORS: LEDGER: farming/state.md:473, the handoff row as read ('(al) art-suite guard extensions plus the bundle-hash renderer/content split | P6 / P6 QA | maintainer | open ruling-owed'); the maintainer-reads list at farming/state.md:409-411 as read ('(al) the art-suite guard extensions plus the sharpened bundle-hash renderer/content split'); the (al) paragraph opening at farming/state.md:2908 as read ('TWO release art-program suites (new in the tenth absorb) collide structurally with any feature branch that adds items or sim content, and were healed in-branch with the guards extended, never gutted; maintainer read owed on both'); the ABSORB CHECKLIST ADDITION at farming/state.md:2925-2931 as read (run build_mob_portrait_source_manifest.mjs --write and item_art_audit.mjs --verify-only after every absorb, resolve a manifest conflict by re-running the CLI on the merged tree, never by hand-picking a side); the Phase 6 QA GUARD EXTENSIONS block (commit 719d701d66) at farming/state.md:2990-3000 as read; the SEVENTEENTH ABSORB 'NEW (al)-CLASS MEMBER' rule at farming/state.md:3022-3036 as read (any future release seal over a live inventory farming's pending ids can join gets the same ART-SUBJECT split, never a raised release literal and never a gutted arm); the packet-wide inheritance at phase-11d-derived-artifacts.md:227-233 as read (the base carried liveItemCount 838 with NO pendingArtCount field; farming introduced it) and the release_v039_icon_art settlement at :243-245 as read. LIVE CODE, re-read in this worktree: scripts/lib/item_art_audit.mjs:485-496 (both directions policed, a pending id must be a live def with NO shipping webp), :497-501 (pending ids excused from the missing-art list), :561 and :639-642 (liveItemCount is live defs MINUS the pending set), :572-573 (the pendingArtCount expected-literal assert); scripts/item_art_audit.mjs:126 catalogCount 1125, :147 liveItemCount 1140, :181 pendingArtCount 81, with the ART-SUBJECT comment at :127-128; scripts/item_art_audit.mjs:59-82, the audit reads ITEM_ART_PENDING through the UI seam as the one union ledger; src/ui/icons.ts:5817-5826, where ITEM_ART_PENDING spreads the RELEASE's own content-side arm IGNIVAR_ART_PENDING_ITEM_IDS (src/sim/content/ignivar_loot.ts:3299, currently empty after the crucible-set-icons-2026-08-29 wave drained its 192 ids); scripts/lib/mob_portrait_manifest_guard.mjs:47-64, the receipt-free path gated on describeManifestDrift().bookkeepingOnly AND an unchanged row count, with the residual named in the comment at :56-60 and ledgered there under deviation (al); scripts/lib/mob_portrait_manifest_diff.mjs and scripts/build_mob_portrait_source_manifest.mjs, the release-added diagnostic and the --write CLI the checklist names. PINS: tests/item_art_audit_builder.test.ts:774-800 (the ART-SUBJECT fixture arm, then the pendingArtCount mismatch arm one off in each direction) and :925-934 (the merged-tree literals: catalogSha256 b5601f86..., rendererFingerprint 41f5404c..., liveItemCount 1140); tests/release_v039_icon_art.test.ts:453-462 (the ART-SUBJECT split of the release's own hotbar seal), :495 and :522-526 (pending hotbar items pinned at 20, each of which must ship no webp); tests/mob_portrait_source_manifest.test.ts:212-219 (the 'authorizes a fingerprint-only refresh without a receipt, and only that' arm, citing (al)); tests/item_art_consistency.test.ts:344-347 and :666-670 (the re-minted verdict evidence, citing (al) twice).
OPTIONS:
  1. [docs] Ratify the guard extensions and the standing obligations that ride them (status quo)
     PRICE: Zero code movement: scripts/lib/item_art_audit.mjs:485-501/561/572-573/639-642, scripts/item_art_audit.mjs:126-181, scripts/lib/mob_portrait_manifest_guard.mjs:47-64 and the four pin suites all stand exactly as written. The whole cost is one dated RULED line in the (al) paragraph at farming/state.md:2908 and the status cell at farming/state.md:473 flipped, made in the SAME edit as D029's answer so the row flips once. No wiki regen (npm run wiki:content and the tests/guide.test.ts freshness gate are untouched, no guide.* prose key moves), no M16 non-Latin fills, no deed record, no Reliquary page, no committed WebP, no census row (scripts/merge_audit/symbol_census.mjs:216-226 makes scripts/lib/*.mjs a census path, but nothing is added or removed here), no parity golden, no tests/world_api_parity.test.ts member, no tests/monolith_budget.test.ts ceiling (none of these files carries one). What ratification BINDS, and what the maintainer is actually signing, is the standing behaviour: every future absorb and every phase touching sim content re-runs the two CLIs and resolves manifest conflicts on the merged tree rather than by picking a side; any future release seal over a live inventory the packet's pending ids can join gets the ART-SUBJECT split rather than a raised release literal; and the three literals (catalogCount 1125, liveItemCount 1140, pendingArtCount 81) are re-minted at every content move, which is the point of the split. One residual ratifies with it: the committed final-item-art-audit-verdict.json carries THIS environment's per-sheet digests and sheetSetSha256, which the ledger says the maintainer's canonical environment should re-mint at feature review (farming/state.md:2932-2944 as read). Reviewers: none beyond the standing qa-checklist; the Review Dispatch Matrix (implementation-plan.md:144-154) spawns nothing for a docs-only edit.
  2. [WORK] Refuse the extensions: restore the release-owned guards as written and clear the debt before merge
     PRICE: Not executable in-packet, and it collapses into another parked decision. Reverting the ART-SUBJECT rule means every one of the 81 ids on ITEM_ART_PENDING must ship committed WebP plus a mapping.json owner before scripts/item_art_audit.mjs can pass, which IS the art wave D095 and D010 already park on the maintainer's own master SHA under the profession_icons E2 rule, so this option cannot be taken before that wave runs. Reverting the receipt-free path means every sim-content commit that moves the stills bundle digest demands a full rendered portrait receipt (a render_finder_portraits run per content commit), which the ledger records as the exact collision the extension was written for. Files: scripts/lib/item_art_audit.mjs, scripts/item_art_audit.mjs, scripts/lib/mob_portrait_manifest_guard.mjs, src/ui/icons.ts (ITEM_ART_PENDING emptied, and its spread of the release's own IGNIVAR_ART_PENDING_ITEM_IDS arm removed with it), tests/item_art_audit_builder.test.ts (the fixture arm and both merged-tree literal blocks), tests/release_v039_icon_art.test.ts (the hotbar seal returns to a flat count that must then be RAISED past the release literal 72, the exact move the seventeenth-absorb rule forbids), tests/item_art_consistency.test.ts, tests/mob_portrait_source_manifest.test.ts. Census: removing branch-added exports under scripts/lib/ moves the merged scan (symbol_census.mjs isCensusPath at :1936), so the extras allowlist is re-derived in the same change. Every one of the 81 ids also needs its art before merge, which drags the M16 fills, the item-art re-pins in tests/item_icons.test.ts and the mapping.json ownership rule along with it. Reviewers: qa-checklist plus frontend-seam-reviewer (src/ui/icons.ts).
  3. [WORK] Keep the extensions but re-home them upstream: land the ART-SUBJECT rule and the bookkeepingOnly refresh on the release as its own change first
     PRICE: A standalone PR against the newest release/** carrying scripts/lib/item_art_audit.mjs, scripts/item_art_audit.mjs and scripts/lib/mob_portrait_manifest_guard.mjs with their pins, minted against an EMPTY pending set, after which the packet merge only ADDS its 81 ids to a mechanism the release already owns. Retires the per-absorb collision the checklist has fired on at (at least) the eleventh, twelfth, sixteenth, seventeenth and nineteenth absorbs plus both v0.41.0 syncs. Costs a full extra review cycle plus a re-mint of the release's own three literals and its portrait manifest, and it does NOT retire the per-sync re-mint work: the packet still re-mints at every subsequent sync. Note the ground has already shifted under this option: the release has adopted the mechanism on its own content side (src/sim/content/ignivar_loot.ts:3299 IGNIVAR_ART_PENDING_ITEM_IDS, spread into ITEM_ART_PENDING at src/ui/icons.ts:5817-5826, which the release used to park 192 Crucible ids until its own painted wave drained them), so most of what this option would upstream is upstream already. Reviewers: qa-checklist plus gate-integrity-reviewer (the change moves guard/CI-adjacent tooling on a release branch).
RECOMMENDATION: Ratify (option 1): the packet's own records show the extensions were built as extensions rather than gutting, each policed both directions and each self-clearing when the art wave lands (a pending id must be a live def with NO shipping webp, scripts/lib/item_art_audit.mjs:485-496; the debt literal reds when the set grows even though liveItemCount is blind to it, tests/item_art_audit_builder.test.ts:780-800), refusal is blocked behind the very art wave D095 and D010 already park on the maintainer's master SHA, and the release itself has since parked 192 of its own Crucible ids through this same ledger, which is the strongest evidence the mechanism is the repo's art-program doctrine and not a branch-local escape hatch. This expands seeded row 4's standing deviation (al).
FLAGS: needsMaintainerValue=false; r5Surface=false; contingentOn=D029, the other half of deviation (al). Ask this row beside D029 and record ONE combined RULED line: both rows' no-op options claim the same status flip at farming/state.md:473, and that row can only be flipped once both halves are answered.

### D154  [farmnowms-facet-member]
SEED: row 4 (standing deviation (ap), named explicitly in that seeded row's deviation list and left unexpanded; D072 rules only the fplot wire field and says so in its own text)
QUESTION: Ratify Phase 7's widening of IWorldFarming with the clock read farmNowMs(), the packet's one surface-before-widening case, together with the two acceptances the Phase 7 QA recorded as riding it (an online client wall clock running fast can paint the stage4 'looks ready' mesh before the authority flips status, and withered surfaces online only when the heavy-gated fplot re-diffs); or refuse the member and unwind the widening?
ANCHORS: LEDGER: farming/state.md:474 handoff row, as read ('(ap) farmNowMs facet member (re-argued KEEP at P7 QA) | P7 | maintainer | open ruling-owed'); the deviation itself at farming/state.md:3168-3182, as read, ending 'The packet's stopping rule said surface-before-widening: MAINTAINER READ OWED on this member'; the owed re-argument and its verdict at farming/state.md:3255-3277, as read ('(ap) ADDENDUM, the owed re-argument, verdict KEEP as the design's own anticipated seam ... MAINTAINER READ STILL OWED at feature review per D22'), which is also where the two RECORDED ACCEPTANCES are stated and where capping the fraction path at stage3 is recorded as rejected; the maintainer-reads roster at farming/state.md:405-413; the count-pin baseline at farming/state.md:363 and the per-phase parity movement at farming/state.md:2469-2474 ('parity pins moved 327 to 328 members, 239 to 240 methods ... the parity exhaustion is TYPE-LEVEL, so run tsc as well as the suite'); phase-07-render-and-juice.md:45-49; phase-07-qa.md:12-13 (the QA told to re-argue it); progress.md:1333-1336 and 1432-1437; phase-09b-bed-verbs.md:301 ('the facet is FROZEN this phase'); and D072's own exclusion at phase-19-rulings-gate.md:107. LIVE CODE, re-read in this worktree: the declaration and its contract paragraph at src/world_api/farming.ts:57 with the CLOCK-BASE CONTRACT it discharges at src/world_api/farming.ts:40-56; the Sim arm at src/sim/sim.ts:11933-11935 (returns this.lockoutNowMs()); the ClientWorld arm at src/net/online.ts:1768-1770 (returns Date.now(), with the riftEventMsRemaining reasoning in the comment above it); the render consumer at src/render/farm_patches.ts:477 inside the 0.5 s throttled sync (FARM_SYNC_INTERVAL_S at src/render/farm_patches.ts:95) and its structural read type FarmPlotSource at src/render/farm_patches.ts:960-965; the pure core's clock-base banner at src/render/farm_patches_core.ts:13-19; the harvest journal's two reads at src/ui/hud/professions/harvest_journal_window.ts:243 and :272 with its banner at :31 and the view-core note at src/ui/hud/professions/harvest_journal_view.ts:13. PINS: tests/world_api_parity.test.ts:504 (the member row), :2101-2103 (FACET_FARMING, type-level exhaustion), :922 and :1340 (the two sorted-name snapshots), :781-783 (the counts, currently 354 / 97 data / 257 method) and the narrative at :688-690; tests/command_facets.test.ts:518-524 (farmNowMs must stay untagged, no wire command); tests/professions_farming.test.ts:4396-4434 (the behavioral clock-base arms); tests/farming_command_chain_online.test.ts:614-625 (the online authority-base arm). Stub worlds that declare the member: tests/farm_patches_adapter.test.ts:74, tests/harvest_journal_window.test.ts:63 and :554, tests/entity_gate_stand_in.test.ts:423, tests/renderer_compile_gate.test.ts:836, tests/farming_windows_body_class.test.ts:69, tests/browser/farm_prewarm_programs.browser.test.ts:83, tests/browser/a11y.browser.test.ts:1358; capture rigs at scripts/farm_gpu_tour.mjs:90, :214, :237 and scripts/pr_shot_targets.mjs:11319, :12910.
OPTIONS:
  1. [no-op] Ratify KEEP, the member and both recorded acceptances
     PRICE: Genuinely no-op: a dated RULED line flipping farming/state.md:474 out of the open-family class and stamping the (ap) block at farming/state.md:3168 and the Phase 7 QA addendum at :3255. No code moves, no pin moves, no golden moves (the member never rode the wire and mints no wire token, which tests/command_facets.test.ts:518-524 already pins). No wiki regen, no M16 fills, no deed, no Reliquary page, no WebP art, no census row, no monolith ceiling. The ruling must say out loud that it also ratifies the two acceptances the QA recorded under it, because they are inseparable from the member: online, a fast client clock can render the stage4 mesh before the authority flips status (NTP-scale, the harvest then refuses not_ready, and the authoritative facts stay status plus events), and withered surfaces only when the heavy-gated fplot re-diffs, so a doomed crop can read stage4 up to the staggered-refresh backstop.
  2. [WORK] Refuse the member and unwind Phase 7's widening
     PRICE: WORK, and the largest of the three. Delete the declaration and its contract paragraph at src/world_api/farming.ts:49-57, the Sim arm at src/sim/sim.ts:11933, and the ClientWorld arm at src/net/online.ts:1768, then re-source the clock for the two live consumers: the render adapter (src/render/farm_patches.ts:477, whose FarmPlotSource structural type at :960-965 declares the member) and the harvest journal (src/ui/hud/professions/harvest_journal_window.ts:243 and :272). With no facet clock the only two routes left are letting those consumers read Date.now, which the CLOCK-BASE CONTRACT at src/world_api/farming.ts:40-46 forbids because the offline and headless hosts write farm timestamps in the sim clock, or plumbing a nowMs argument down a non-facet seam from the renderer and the Hud, which is a new seam the packet never scoped and which the render pure core's banner (src/render/farm_patches_core.ts:13-19) says is the caller's job not to get wrong. Parity: the member row at tests/world_api_parity.test.ts:504, FACET_FARMING at :2101 (type-level exhaustion, so tsc runs as well as the suite, per farming/state.md:2472-2474), both sorted-name snapshots at :922 and :1340, and the three count pins at :781-783 (354 / 97 / 257, down one member and one method), plus the narrative at :688-690. Behavioral pins die outright: tests/professions_farming.test.ts:4396-4434, tests/farming_command_chain_online.test.ts:614, and the untagged-read arm at tests/command_facets.test.ts:518-524. Eight stub worlds re-shape (tests/farm_patches_adapter.test.ts:74, tests/harvest_journal_window.test.ts:63 and :554, tests/entity_gate_stand_in.test.ts:423, tests/renderer_compile_gate.test.ts:836, tests/farming_windows_body_class.test.ts:69, tests/browser/farm_prewarm_programs.browser.test.ts:83, tests/browser/a11y.browser.test.ts:1358) and two capture rigs lose their optional-chained read (scripts/farm_gpu_tour.mjs, scripts/pr_shot_targets.mjs). It also unwinds D072's premise: the countdown D072 recommends ratifying IS readyAtMs minus farmNowMs, so refusing here reopens D072 as the wire field or nothing. No wiki regen, no i18n fills, no deed, no Reliquary page, no art, no census row, no golden, no monolith ceiling. Reviewers: cross-platform-sync (both worlds and the offline/headless clock), architecture-reviewer (the seam), frontend-seam-reviewer (the two consumers), test-coverage-auditor (pins deleted rather than re-pointed).
  3. [WORK] Keep the member, but refuse acceptance (1): gate the online fraction path at stage3 until status flips
     PRICE: WORK, and the Phase 7 QA already argued against it at farming/state.md:3268-3272. The stage selection in src/render/farm_patches_core.ts would take a host or authority-trust input, which splits the three-host render behavior the pure core exists to keep identical and puts a non-cosmetic branch into a module whose banner (src/render/farm_patches_core.ts:6-11) states it takes no tier, preset or governor input. The cost is paid by everyone: the ready mesh lags for EVERY online player by the throttle at src/render/farm_patches.ts:95 (FARM_SYNC_INTERVAL_S 0.5 s) plus the staggered fplot refresh, to remove a rare NTP-scale skew whose worst outcome today is a not_ready refusal. Pins: the stage-walk arms in tests/farm_patches_core.test.ts re-pinned plus a new online-arm pin, and the farming fairness scan re-read because a growth stage is ACTIONABLE information and this option makes it host-dependent. Nothing moves on the facet, so no parity pin, no count pin, no census row, no golden. No wiki, no i18n, no deed, no Reliquary, no art. Reviewers: cross-platform-sync (the deliberate host divergence), frontend-seam-reviewer, render-performance-reviewer (the read cadence the gate rides on).
RECOMMENDATION: Ratify KEEP. The packet's own records lean hard this way: the CLOCK-BASE CONTRACT at src/world_api/farming.ts:40-46 named this exact member as the surface it was deferring to the first timer consumer, the Phase 7 QA re-argued it and reached KEEP with every alternative priced as worse, the member is minimal (no wire token, no persistence, each world returning its own base fresh per call), and D072 already recommends ratifying the countdown that depends on it, so a refusal here would reopen D072 as well.
FLAGS: needsMaintainerValue=false; r5Surface=false; contingentOn=D072 reads the other direction (its ratify option presumes this member survives), so if this row is refused, D072 must be reopened; ordering is this row first.

### D155  [npc-terrain-pad-rule]
SEED: row 4 (standing deviation (bh), named in seed row 4's list and never expanded by the Phase 18 sweep)
QUESTION: Ratify the NPCs-are-terrain rule, that every NPCS row is a REQUIRED calm-anchor pad at rIn 6 / rOut 14, so seating any NPC anywhere silently reshapes minted ground inside its skirt and re-mints the terrain goldens, and with it Farmer Hollis's Highwatch shelf on the Phase 9 QA eyeball alone (up to 4.3 yd of movement, no re-seat)? And does the ledger's own closing advisory, that future NPC placements probe the pad DELTA rather than only the slope, become a gate or stay a sentence?
ANCHORS: LEDGER. farming/state.md, the (bh) block (read at :3810-3829 as read): 'NPCs ARE TERRAIN: every NPCS row is a calm-anchor pad (src/sim/terrain_calm_anchors.ts, rIn 6 / rOut 14), so seating four farmers reshaped the ground inside their skirts', with the measurement 'the dense atlas moved 60 of 282406 points, 0.02 percent, all inside the four footprints; the largest under Farmer Hollis at Highwatch, up to 4.3 yd where the natural relief diverges most from the legacy field, and about 0.6 yd under bed_thornpeak_5; centimeters at the other three', the deliberate re-mint list ('the atlas fixture, the Eastbrook chunk vertex digest, and the four farming-zone map plates plus the world strip'), and the QA READ (Phase 9 QA, 2026-08-17, three angles at the LOW preset): 'the pad reads as a natural terrace, Hollis stands a step uphill of the beds with no visible mound, crater, or seam, so NO re-seat; the maintainer read on the pad rule itself still stands. Future NPC placements should probe the pad delta, not just slope.' Handoff row '(bh) NPCs-are-terrain pad rule (Highwatch eyeballed, no re-seat) / P9 / maintainer / open ruling-owed' at farming/state.md:478 as read; the reads-owed roll-up at :415 as read; the OPEN-items restatement at :4383-4386 as read; the phase record at farming/phase-09-qa.md:18, farming/progress.md:2193 and :2242. Evidence frames present in this worktree: docs/screenshots/farming-phase-09/qa-highwatch-shelf-low.png and qa-highwatch-shelf-overview.png. LIVE CODE, verified here. The rule itself: src/sim/terrain_calm_anchors.ts:160-162, for (const id in NPCS) { const npc = NPCS[id]; pad('npc', npc.pos.x, npc.pos.z, 6, 14, false); }, the trailing false being optional=false, i.e. required. Its rationale, same file :95-98: 'Required pads (the classic gather node / NPC / camp / tarn set) always keep their ring, because removing an existing ring would itself move minted terrain', beside CALM_PAD_SKIP_DIVERGENCE = 0.75. The failure mode the module exists for, :5-7: a fixed-width skirt makes 'the pad ... a ledge with an unwalkable rim (the Highwatch training dummy report)'. The seat: src/sim/content/zone3.ts:1471-1476, farmer_hollis, pos: { x: -18, z: 695.5 }, title 'Highwatch Terrace Farmer'. The mirrored pin that names this deviation in its own header: tests/farmer_npc_placement.test.ts:23-31, 'a re-seat is a deliberate content edit that also moves the terrain atlas, the Eastbrook chunk digest, the map plates and every golden (deviation (bh)), so it must red HERE first, by literal', with the four seat literals at :38-62 and the seating arms at :150-212. What is gated TODAY: tests/placement_integrity.test.ts:71-91 ('npc' is a required roster category), :187-209 (every surviving pad footprint sits on classic ground, calm factor 0), :236-244 (every roster pad reaches a road under the climb gate); the rim-slope arm exists only as a SPOT-specific pin at :274-286 ('the Highwatch training dummy is reachable, not a ledge', hard-coded to (-40, 648) against PLAYER_MAX_CLIMB_SLOPE) and is never generalized over the roster, and nothing anywhere bounds a pad's height DELTA against the previously minted field (grep for 'pad delta' across src/, tests/ and scripts/ returns exactly one hit, the advisory sentence in farming/state.md itself). PLAYER_MAX_CLIMB_SLOPE = 1.5 at src/sim/pathfind.ts:55. The goldens in the blast radius: the dense atlas binary tests/fixtures/terrain_height_parity.v1.f64le.gz (1052297 bytes, loaded at tests/terrain_height_parity.test.ts:53); the baked plate fingerprint gate tests/map_bg_baked.test.ts:14-45 over public/map_bg/<zone>.webp plus world_strip, re-baked by npm run assets:mapbg (scripts/build_map_backgrounds.mjs); and the Eastbrook chunk digest's own (bh) re-mint paragraph at tests/terrain_chunk_geometry.test.ts:146-158, which records that it moved for Jessica's vale pad, not Hollis's.
OPTIONS:
  1. [no-op] Ratify the rule and the shipped seats as they stand: NPC pads stay required at rIn 6 / rOut 14, Hollis keeps the Highwatch shelf on the eyeball, and the pad-delta advisory stays advisory
     PRICE: A dated RULED line on (bh) and the handoff row at farming/state.md:478 flipped. Nothing in the tree moves: the four seats, the re-minted atlas fixture, the Eastbrook digest and the plates are already shipped and already guarded (seat literals at tests/farmer_npc_placement.test.ts:38-62; classic ground and road reachability over every roster pad at tests/placement_integrity.test.ts:187 and :236). No wiki regen, no M16 fills, no deed record, no Reliquary page, no WebP art, no census row, no parity member, no monolith ceiling movement, no reviewer dispatch.
  2. [WORK] Ratify the rule, but arm the advisory it closes on: a roster-wide gate so a new or re-seated NPC seat proves its ground instead of being eyeballed
     PRICE: Test-only, no src change, no golden movement. Extends tests/placement_integrity.test.ts with a generic arm over openWorldPads, generalizing the spot-specific Highwatch-training-dummy pin at :274-286 into a per-pad rim walk (adjacent-ring height steps under PLAYER_MAX_CLIMB_SLOPE, src/sim/pathfind.ts:55), and optionally a delta ceiling comparing terrainHeight against the pre-pad field over the rIn 6 disc. The rim arm is derivable and needs no new number; the DELTA arm needs one the packet does not hold (see valueNeeded). Cost to price honestly: the arm walks every pad in the roster on the real seed, and that suite already carries a 24000-cell BFS budget, so a careless generic arm lengthens an already heavy suite and must be sized rather than just written. No wiki, i18n, deed, Reliquary, art, census, parity or ceiling obligations. Reviewers: test-coverage-auditor (a new world-walking pin), architecture-reviewer (it reads sim terrain through the pure probe seam).
  3. [WORK] Re-seat Farmer Hollis off the divergent Highwatch shelf so the pad's 4.3 yd delta shrinks
     PRICE: A content edit at src/sim/content/zone3.ts:1475 (the pos literal), which by design reds the mirrored seat literal at tests/farmer_npc_placement.test.ts:52-56 first, then re-derives its bed-distance, facing, in-zone, dry-land, off-road and off-camp arms at :150-212 (all computed, no absolute height literals, so they re-pass or genuinely fail). Re-mints: the dense atlas binary tests/fixtures/terrain_height_parity.v1.f64le.gz, and the baked plates the tests/map_bg_baked.test.ts fingerprint gate reds (thornpeak_heights plus the world strip, on the ledger's own re-mint record) via npm run assets:mapbg, committing the changed public/map_bg WebPs. The Eastbrook chunk digest at tests/terrain_chunk_geometry.test.ts:146-158 does NOT move: Hollis is at z 695.5, outside that rect; it moved for Jessica. Also owes a re-capture of docs/screenshots/farming-phase-09/qa-highwatch-shelf-low.png and -overview.png, since the shipped evidence would otherwise photograph a seat that no longer exists. No new ids, so no wiki regen, no M16 fill, no deed, no Reliquary page, no item art, no census row, no parity member, no ceiling movement. Reviewers: content-obligations-reviewer (a src/sim/content diff) and architecture-reviewer. Stated plainly: this buys nothing the eyeball did not already accept, and re-mints a megabyte-scale binary golden to do it.
  4. [WORK] Change the rule itself: make the NPC roster row optional (pad('npc', ..., 6, 14) with the default optional=true), so a seat whose divergence is beneath CALM_PAD_SKIP_DIVERGENCE 0.75 stops flattening ground at all
     PRICE: One argument at src/sim/terrain_calm_anchors.ts:162, and world-wide consequences. The module argues against it in place at :95-98: required pads 'always keep their ring, because removing an existing ring would itself move minted terrain'. Dropping rings under every low-divergence NPC in the game re-mints the dense atlas fixture, the Eastbrook chunk digest, and every affected zone plate plus the world strip, and puts each newly padless NPC through the dropped-optional arm at tests/placement_integrity.test.ts:211-219 (drift must stay under 1.25) and the road-reachability arm at :236. Whole-world terrain churn far past this packet's scope, and it re-opens the ledge class the module was written for (:5-7). Reviewers: architecture-reviewer plus content-obligations-reviewer, with the whole terrain golden family re-run.
RECOMMENDATION: Ratify as shipped (option 1): the packet's own evidence is a three-angle LOW-preset eyeball that found a natural terrace with no mound, crater or seam, the module states in place that un-padding an NPC would itself move minted terrain, and the only forward-looking thing the ledger asks for is the pad-delta probe, which is option 2 and can ride a later world phase; if the sitting wants one execution rather than none, option 2's number-free rim arm is the cheap half and moves no golden.
FLAGS: needsMaintainerValue=true VALUE_NEEDED: Only if option 2's DELTA arm is taken: the maximum terrain-height delta, in yards, that a new or re-seated NPC pad may introduce against the previously minted field, plus where it is measured (at the seat, across the rIn 6 pad disc, or out to the divergence-sized skirt edge). The packet holds two measurements and no ceiling: 4.3 yd under Hollis and about 0.6 yd under bed_thornpeak_5 (farming/state.md:3812-3816 as read). 4.3 is the observed worst case that was accepted by eyeball, not a sanctioned bound, so it cannot be promoted into a threshold without the maintainer saying so. The rim-slope variant of option 2 needs no value (PLAYER_MAX_CLIMB_SLOPE 1.5, src/sim/pathfind.ts:55).; r5Surface=false; contingentOn=none

### D156  [feast-tuning-constants]
SEED: row 4 (named there among the tuning reads as 'feast tuning'; the sweep expanded that half only into D019, which took the payout, and D107 took the raid-scale charge question, leaving these four constants unpriced)
QUESTION: Ratify or re-set the party feast's four remaining tuning constants: harvest_feast `charges: 10`, `durationTicks: 3600` (180 s), `sellValue: 250`, and recipe_harvest_feast's 4 + 4 + 2 reagent counts. The seeded row's fifth arm (trainer fee 10000) is already dead: Phase 11f moved the row to `acquisition: ['drop']`, so the feast charges no fee in advance or otherwise.
ANCHORS: farming/state.md:480 (handoff row as read: 'Feast tuning: charges 10, 180 s, trainer fee 10000, sellValue 250, reagents x4/x4/x2 / P12 / maintainer / open ruling-owed', still open beside CLOSED siblings, so never later ratified); the same read at farming/state.md:418-421 as read ('The tuning reads: feast charges 10 / durationTicks 3600 (180 s) / trainer fee 10000 / sellValue 250'); the Phase 12 content ledger at farming/state.md:4262-4265 as read, which still records the now-superseded 'rung 50 ... trainer fee 10000'; ruling 11k-D-K5 at farming/state.md:1168-1170 as read ('The shipped harvest_feast values: charges 10, durationTicks 3600', which ADOPTS them for the apex rungs rather than ratifying them here) and 11k-D-K2 at farming/state.md:1149-1155 as read (the apex sellValue derived 'strictly above 250 (harvest_feast) and strictly below 380 (laden_hearth)'). LIVE CODE, re-read in this worktree: src/sim/content/profession_items.ts:1053-1054 ('charges 10 and durationTicks 3600 (180s at 20 Hz) are maintainer-flagged tuning, like every farming constant'), :1060 sellValue 250, :1062-1063 charges 10 / durationTicks 3600, and :840 / :856 / :870 the three apex feasts at sellValue 300 that K2 derived off the 250 floor; src/sim/content/recipes.ts:4095-4096 ('FLAGGED FOR THE MAINTAINER: the reagent counts (4 + 4 + 2) and the 250 output sellValue are proposed tuning'), :4106-4110 the three reagent lines, :4114 acquisition ['drop'], and :3749-3757 the (aj) fee discharge. LIVE PINS: tests/farm_recipes.test.ts:865 (FEAST_EXPECTED_INPUT_VALUE = 336), :919-931 (the 4/4/2 literals), :933-945 (output 250 plus the strict-below-input arm), :947-961 (the (bz) unpriced-produce invariant), :963-1000 (the feast record toEqual {charges: 10, durationTicks: 3600, templateId, dishItemId} plus the closed key shape); tests/feast_tooltip_view.test.ts:62-68 pins the rendered '(10 servings, lasts 3 min)' line, resolved from the record rather than typed copy. LIVE GENERATED: src/guide/content.generated.ts:10943-10966 bakes the 4/4/2 materials and feeCopper 0, and :10980-10983 bakes 'servings': 10, 'minutes': 3. R5: docs/design/power-verification.md:167-170 quotes each apex feast's `charges: 10`, `durationTicks: 3600` and names farming's harvest_feast in the same sentence, over :145-150's 'a feast moves DELIVERY, not the ceiling'.
OPTIONS:
  1. [no-op] Ratify all four as shipped (charges 10, durationTicks 3600, sellValue 250, reagents 4/4/2), and record the trainer-fee arm as retired by Phase 11f rather than ruled
     PRICE: A dated RULED line at farming/state.md:480 flipping the row, and nothing else: no literal moves, no pin flips, no regeneration. The two live FLAGGED-FOR-THE-MAINTAINER comments (profession_items.ts:1053-1054, recipes.ts:4095-4096) become accurate as history and can be reworded at packet teardown under D100/D039 rather than in this change.
  2. [WORK] Re-price the 250 output sellValue
     PRICE: One literal at src/sim/content/profession_items.ts:1060, plus the 250 literal and the strict-below-input assertion at tests/farm_recipes.test.ts:933-945 (FEAST_EXPECTED_INPUT_VALUE 336 stays, so any value at or above 336 also reds the gold-negative arm). The real cost is upstream of the file: 11k-D-K2 derived the three apex feasts' 300 as strictly above THIS 250 and strictly below laden_hearth's 380, so any new value at or above 300 forces profession_items.ts:840/856/870 to move with it and re-opens the K2 derivation. No wiki regen (the generated recipe row carries name/count/quality only, no output sellValue), no WebP art, no M16 fills, no deed or Reliquary row, no census row, no parity golden, no monolith ceiling. Reviewer: content-obligations-reviewer.
  3. [WORK] Re-set the 4 + 4 + 2 reagent counts
     PRICE: src/sim/content/recipes.ts:4106-4110, then three pins: the count literals at tests/farm_recipes.test.ts:919-931, the FEAST_EXPECTED_INPUT_VALUE 336 arithmetic at :865 with its strict-below-input arm at :933-945, and a re-check that the (bz) unpriced-produce invariant at :947-961 still names exactly evergarden_greens and gilded_sunmelon. Wiki regen RIDES: src/guide/content.generated.ts:10950-10965 bakes the counts, so `npm run wiki:content` plus the tests/guide.test.ts freshness gate. No new item id, so no WebP art, no M16 fills, no deed, no Reliquary page, no census row (scripts/merge_audit/symbol_census.mjs sees no new symbol), no world_api parity member, no monolith ceiling. Reviewer: content-obligations-reviewer.
  4. [WORK] Re-tune charges 10 or durationTicks 3600
     PRICE: R5 SURFACE, an escalation rather than an execution. Mechanically small: src/sim/content/profession_items.ts:1062-1063, the exact-shape toEqual at tests/farm_recipes.test.ts:963-1000, and the resolved tooltip literal at tests/feast_tooltip_view.test.ts:62-68 (the numbers resolve from the record, so no new i18n key and no non-Latin fills ride); wiki regen for src/guide/content.generated.ts:10980-10983 ('servings' 10, 'minutes' 3) with the tests/guide.test.ts freshness gate. The price is elsewhere: 11k-D-K5 took the apex feasts' numbers FROM this row on the premise that 'the only differences from the rung below are the tier, the dish and the bill', so a family-consistent retune moves the `charges: 10` / `durationTicks: 3600` literals docs/design/power-verification.md:167-169 quotes, which re-opens a frozen R5 input; and moving the party rung ALONE falsifies K5's own premise instead. A raid-scale charge count specifically is D107's question and is refused there. Reviewers: content-obligations-reviewer, plus the R5 escalation path rather than an in-phase execution.
RECOMMENDATION: Ratify all four: one arm of the seed is already dead by Phase 11f, D019 settles the payout and D107 keeps K5's charges and duration refused-to-move, the 250 is a load-bearing lower bound the apex feasts' 300 was derived off under K2, the 4/4/2 counts are the only ones the shipped 336-in-against-250-out arithmetic has ever been proven gold-negative on, and seventeen phases of economy sweeps recorded no finding against any of the four.
FLAGS: needsMaintainerValue=true VALUE_NEEDED: Only if any option other than ratify is taken. The packet records hold no alternative anywhere: no replacement charge count, no replacement durationTicks, no replacement output sellValue, and no replacement reagent counts. 11k-D-K2 supplies bounds (strictly above 250, strictly below 380, a multiple of 10) for the APEX feast price only, never for this one, and D107 records that a raid-scale charge count and its balance basis 'the record does not hold'. A retune therefore needs the maintainer to name the number outright, plus the basis it is derived from if it is not to be a chosen constant.; r5Surface=true; contingentOn=D107 (the charges and durationTicks half must be asked after D107 settles whether 11k-D-K5's numbers stand; if D107 reopens them, this row's charges/duration arm merges into that escalation and only the sellValue and reagent-count arms remain here)

### D157  [seed-back-rates]
SEED: row 4 (named explicitly in that row's tuning-reads list: 'seed-back rates')
QUESTION: Ratify or re-set the tier 3 and tier 4 seed-back rates (two-seed chance 0.08 / 0.06, pays-anything chance 0.40 / 0.35), now that Phase 11e's GATE 1 stocked all eight upper-tier seeds at the farmer counters and falsified the no-vendor-faucet reason the rates were originally authored on?
ANCHORS: farming/state.md handoff table row (farming/state.md:481 as read): 'Seed-back rates 0.08/0.40 tier 3, 0.06/0.35 tier 4 (economy-sensitive) / P5 / maintainer / open ruling-owed', still open beside siblings in the SAME table that carry dated closures ((aj) trainer fees CLOSED 2026-08-22, the fine-twin buyValue doctrine CLOSED 2026-08-21), so it was never later ratified; re-stated in the maintainer-reads-owed list at farming/state.md:423 as read. LIVE CODE, verified in this worktree: src/sim/professions/farming.ts:271-273 (FARM_SEED_BACK_MIN_TIER = 3; FARM_SEED_BACK_TWO_CHANCE = {3: 0.08, 4: 0.06}; FARM_SEED_BACK_ONE_CHANCE = {3: 0.4, 4: 0.35}); the one-draw two-threshold resolve at src/sim/professions/farming.ts:898-903 and the grant at :933-934; the constants' own comment block at src/sim/professions/farming.ts:246-270, which records that 11e's faucet falsified the block's old reason, re-derives the expectation (0.48 seeds per tier-3 harvest, 0.41 per tier-4), and ends 'Whether the rates should MOVE now that the faucet exists is a live tuning question for the maintainer, recorded rather than answered here.' PINS: tests/professions_farming.test.ts:2588-2601 (the literal map pin, banner 'TUNING, PROVISIONAL, FLAGGED FOR THE MAINTAINER (economy-sensitive)', plus the band-shape arm two < one < 1), the draw-count arms at tests/professions_farming.test.ts:2585-2586 (tier 1/2 draws 2, tier 3/4 draws 3), the band probes at :2616, :2638-2639, :2660, :2680-2682, :2706-2707, :2760, and tests/parity/coverage_c.test.ts:954, which crosses the recorded golden's tier-3 seedBack === 2 against FARM_SEED_BACK_TWO_CHANCE[3] to prove that golden shift was a re-seat and not a retune. Wire field already shipped: src/sim/types.ts:7474 and :7491 (seedBackCount, omit-when-zero).
OPTIONS:
  1. [no-op] Ratify the shipped rates as they stand (0.08/0.40 tier 3, 0.06/0.35 tier 4, min tier 3)
     PRICE: A dated RULED line (qr-19-seed-back-rates) on farming/state.md:481 and on the reads-owed mention at farming/state.md:423. Nothing else moves: no constant, no pin, no golden, no wiki regen, no i18n.
  2. [docs] Ratify AND retire the provisional flagging, so a later reader does not re-open a settled number
     PRICE: The RULED line above, plus comment-only edits: the 'live tuning question for the maintainer' paragraph at src/sim/professions/farming.ts:266-270 and the 'TUNING, PROVISIONAL, FLAGGED FOR THE MAINTAINER (economy-sensitive)' banner at tests/professions_farming.test.ts:2589-2591 become a dated RULED citation. Zero behavior change and zero pin-value change, so no golden re-record, no census row in scripts/merge_audit/symbol_census.mjs (no symbol added or moved), and no monolith ceiling (src/sim/professions/farming.ts carries no row in tests/monolith_budget.test.ts). Reviewer: the comment-citation practice the packet already uses for ratified constants (the D039 teardown-citations family).
  3. [WORK] Re-set either or both tiers' pair (and optionally FARM_SEED_BACK_MIN_TIER) to maintainer-named values
     PRICE: Edit the two maps at src/sim/professions/farming.ts:272-273 and restate the derived expectation sentence at :265-268 (0.48 / 0.41 no longer holds). Re-pin the literal map arm at tests/professions_farming.test.ts:2594-2595 and re-derive every band probe that compares a recorded draw against a threshold (:2616, :2638-2639, :2660, :2680-2682, :2706-2707, :2760). Cross-pin tests/parity/coverage_c.test.ts:954 and its literal seedBack === 2 claim, and RE-RECORD tests/parity/golden/farming_session.json (UPDATE_PARITY, its own reviewed commit) if the new two-chance no longer straddles that golden's recorded tier-3 draw. Moving FARM_SEED_BACK_MIN_TIER is strictly more expensive than moving a rate: it changes the harvest DRAW COUNT, so tests/professions_farming.test.ts:2585-2586 flips and the WHOLE parity directory re-records rather than one scenario. What does NOT ride, stated so the price is honest: no wiki regen and no tests/guide.test.ts freshness gate (no src/guide or generated wiki surface emits the rate, grep finds none); no i18n and no M16 non-Latin fills (the two rendered keys, hudChrome.farming.seedBackLine and seedBackLineQty at src/ui/i18n.catalog/hud_chrome.ts:5175-5176, are number-free and already filled in all five non-Latin locales); no deed record, no Reliquary page, no committed WebP art (no new id); no tests/world_api_parity.test.ts movement (seedBackCount is already on the wire, no facet member changes); no census row (a literal value change mints no symbol); no monolith ceiling. Reviewers per the Review Dispatch Matrix: the sim reviewer (the roll must stay exactly one ctx.rng.next inside the fixed draw block, or determinism and every golden re-seats) and content-obligations-reviewer for the economy arm; add the parity reviewer only if the golden re-records.
RECOMMENDATION: Ratify: the packet's own records lean that way, because the comment block was deliberately RE-ARGUED at the Phase 11e QA after GATE 1 falsified its original premise and still found the rates defensible (the seed-back is now a discount on a counter route that exists rather than the only route to a tier 3/4 seed), and no phase or QA sweep since has raised a finding against the 0.48 / 0.41 per-harvest expectation.
FLAGS: needsMaintainerValue=true VALUE_NEEDED: Only if the third option is taken: the four replacement numbers (two-seed chance and pays-anything chance, for tier 3 and tier 4) and whether FARM_SEED_BACK_MIN_TIER stays 3. The packet holds nothing to derive them from: it records the shipped pair, the DECISION D counter buyValues (32 at tier 3, 64 at tier 4) and the derived 0.48 / 0.41 expected seeds per harvest, but no replant-cost model and no market price series, so any new pair must be the maintainer's own numbers.; r5Surface=false; contingentOn=none

### D158  [growth-tonic-price-and-skillup-faucet]
SEED: row 4 (the farming handoff table's open-family rows, priced 'to be priced, one row each'; this is the row at farming/state.md:483). NOT D032's row: seed row 4's tuning-read list names 'watch and compost fees, tonic', and D032 spent that word on the tonic BONUS 0.5/2 from the SEPARATE handoff row at :482. D137's fifth option explicitly routes this one here: 'the growth tonic's (ai) row still open ruling-owed at farming/state.md:483, which belongs to seeded row 4 rather than here'.
QUESTION: Ratify the growth tonic's economy as shipped, sellValue 6 with NO buyValue (crafted-only, never vendor-stocked), and accept the (ai) QA addendum's finding that the tonic is the cheapest rung-0 alchemy skill-up craft live today with the intent recorded? Or re-set the price, or raise the herb count to the shipped rung-0 floor so the cheaper faucet closes?
ANCHORS: LEDGER. farming/state.md:483 as read, the handoff row '| Growth tonic 6 sell-only plus (ai) cheapest rung-0 alchemy skill-up ruling | P4 / P6 QA | maintainer | open ruling-owed |', still open beside CLOSED siblings at :479 and :484, so never later ratified. Its two halves are listed as SEPARATE tuning reads in the ledger's open-decisions block at farming/state.md:424 ('growth tonic 6 sell-only') and :426 ('(ai) the tonic as the cheapest rung-0 alchemy skill-up'), which is why they are not D032's, whose four constants come from the row above at :482. The Phase 4 value record at farming/state.md:2611-2613 as read: 'growth_tonic (sellValue 6, NO buyValue: never vendor-stocked, alchemy-crafted in Phase 6)', which STATES the 6 without deriving it, unlike compost's sibling sentence in the same breath that names the 4x convention. The (ai) dormancy ruling and its QA addendum at farming/state.md:2859-2882 as read: the ruling rests on 'Crafting it early is a strict gold loss (output 6 under input 20), so no exploit window opens', and the addendum answers 'the gold-loss argument above weighs only copper; the tonic is ALSO the cheapest rung-0 alchemy SKILL-UP craft in the game (silverleaf x2 + vial, under the shipped rung-0 rows at herbs x3-x4 + extras), so alchemy leveling gains a strictly cheaper mat path live today ... Maintainer call: raise the herb count to the shipped floor, or accept the cheap skill-up faucet and record the intent here'. The (bz) tier-1 binder ruling still cites it as 'the unresolved (ai)-addendum hazard' at farming/state.md:4155.

LIVE CODE, re-read in this worktree. src/sim/content/items.ts:1118-1124, growth_tonic (kind junk, quality common, sellValue 6 at :1123, no buyValue field), with the sell-only doctrine written above it at :1105-1109: 'The growth tonic is NEVER vendor-stocked: its one faucet is recipe_growth_tonic, brewed by an ALCHEMIST out of wild Sheenleaf (D7, the cross-profession trade), so it carries sellValue only. A buyValue here would be the dead-row trap's opposite, a price for a faucet that must not exist.' src/sim/content/recipes.ts:4119-4145, recipe_growth_tonic (alchemy, apothecary, acquisition ['trainer'], skillReq 0 at :4141, reagents silverleaf_herb x2 at :4138 and glass_vial x1 at :4139), whose own comment already flags exactly this row: :4128 'Input 20 vs output 6' and :4130-4136 'FLAGGED FOR THE MAINTAINER: the reagent counts (2 herbs, the low end of the shipped 2-to-4 band, plus the usual single vial) and skillReq 0'. THE FIVE rung-0 alchemy rows, re-derived by reading the skillReq of all eighteen professionId 'alchemy' records: recipe_minor_healing_potion (recipes.ts:139, no acquisition and no stationType, so known by default and station-free; linen_scrap 1 + spider_leg 1 + silverleaf_herb 2), recipe_silverleaf_healing_draught (:1548, herbs 4 + vial), recipe_silverleaf_mana_draught (:1563, herbs 3 + vial), recipe_elixir_of_the_boar (:1578, venom_gland 2 + vale_wheat 1 + herbs 2 + vial), and the tonic (:4119). Reagent values: silverleaf_herb sellValue 4 and no buyValue (items.ts:1003-1009), glass_vial sellValue 3 buyValue 12 (profession_items.ts:154-161), linen_scrap 3 (items.ts:2873-2879), spider_leg 4 (items.ts:2859-2865).

PINS. tests/farm_recipes.test.ts:709-838 is the tonic's own describe block: TONIC_EXPECTED_INPUT_VALUE = 20 as a literal at :715 with its derivation comment at :711-714 ('2 Sheenleaf at 4 each (no buyValue, so the sell floor is the basis) plus one glass vial at its 12-copper buyValue'); the herb count 2 at :775 pinned as a literal on purpose ('swap the herb out for a farm-grown input and the trade this recipe exists to create disappears'); skillReq 0 with its stated reason at :751-763; the strict output-below-input arm at :808-820, input asserted equal to the literal at :815; and the sell-only arm at :824-838, where buyValue must be undefined but sellValue is asserted only toBeGreaterThan(0) at :836. SO THE 6 ITSELF IS UNPINNED: nothing in the tree holds it as a literal, and the only live constraint is the strict bound against the 20, which admits any value from 1 to 19. tests/professions_zone_rollout.test.ts:1176 keeps growth_tonic in the NEVER_STOCKED set (size pinned at 16 at :1193, walked across NPC counters, the heroic vendor and the delve shops), and :1320-1323 already carries the (ai) deviation as a written comment: 'DELIBERATE and ledgered as deviation (ai): the tonic IS craftable by a player who gathers the herbs ... so this arm asserts unstocked-ness, never uncraftability.'

WHAT RIDES A CHANGE. Wiki: src/guide/content.generated.ts:9168-9199 carries the recipe with its material counts (silverleaf 2, vial 1), its skillReq, station, fee and output name/count/quality, but NO sellValue, so a bill change owes a regen and a price change does not. Prose: src/ui/i18n.catalog/guide.ts:3070 (farm.bedsBody) names the tonic with no number and no reagent route, deliberately spoiler-safe, so neither option rewords a key or pulls an M16 fill. Parity: tests/parity/scenarios.ts:5989 grants the tonic with sim.addItem rather than crafting it, so tests/parity/golden/farming_session.json does not encode the bill.

R5, THE FROZEN SURFACE. docs/design/power-verification.md:1085 names recipe_growth_tonic as ALCHEMY'S CHEAP-PATH recipe, '(0, 11c)', inside section 12.2's craft-mastery table, and 12.3 measures the 'cheapest path in materials' (the lowest reagent cost per skill point) at '0.66 to 0.88 of the cost' off exactly that figure.

THE BASIS DIVERGENCE, checkable and load-bearing on the (ai) half. power-verification.md:1104-1107 states its basis is sellValue times quantity, on which the tonic reads 11c and IS the cheapest rung-0 alchemy row (mana draught 15c, minor healing potion 15c, healing draught 19c). tests/farm_recipes.test.ts:144-148 uses buyValue where positive else sellValue, on which the tonic reads 20 and recipe_minor_healing_potion reads 15. On the basis a player actually pays (the vial carries a 12-copper buyValue, the herbs carry none) the tonic is NOT the cheapest rung-0 alchemy skill-up: the trainer-free, station-free minor healing potion is, and it has been shipped since long before farming. The addendum's parenthetical 'under the shipped rung-0 rows at herbs x3-x4 + extras' also skips that row, which is herbs x2 plus two mob drops, so the tonic's real edge over the field is 4 copper of sell-value per craft rather than a whole herb.
OPTIONS:
  1. [no-op] Ratify both halves as shipped: sellValue 6 sell-only stands, and the cheapest rung-0 alchemy faucet is accepted with the intent recorded
     PRICE: A dated RULED line at farming/state.md:483 flipping the handoff row, with the same ruling id on the (ai) QA addendum block at farming/state.md:2871-2882 (which asks for exactly this, 'accept the cheap skill-up faucet and record the intent here') and on the two tuning reads at :424 and :426. NOTHING in code or tests moves, because the shipped values are already pinned both ways and the deviation is already written down rather than silent: tests/farm_recipes.test.ts holds the input literal 20 (:715), the herb count 2 (:775), skillReq 0 (:751-763), the strict output-below-input bound (:808-820) and sell-only (:824-838), and tests/professions_zone_rollout.test.ts holds growth_tonic in NEVER_STOCKED (:1176, size 16 at :1193) with the (ai) deviation spelled out at :1320-1323. No wiki regen, no i18n key, no M16 fill, no committed WebP, no deed record, no Reliquary page, no census row in merge-deletion-list.md, no parity golden, no world_api parity pin, no monolith ceiling, and no reviewer beyond qa-checklist.
  2. [WORK] Ratify both halves AND pin the sellValue 6 as a literal, so the number being ratified stops being the only unpinned one in the row
     PRICE: Everything in option 1, plus one line: tests/farm_recipes.test.ts:836 today asserts only sellValue toBeGreaterThan(0), so a later phase could move the 6 anywhere from 1 to 19 and every arm stays green. Tightening it to toBe(6) beside the existing TONIC_EXPECTED_INPUT_VALUE literal at :715 costs one expect in one suite and gives the ratification the same teeth D032's four constants already have. Owes no regen, no fill, no art, no census row, no golden; reviewer qa-checklist only. Cheap insurance, and the only thing separating it from option 1 is one assertion.
  3. [WORK] Raise the herb count to the shipped rung-0 floor (silverleaf_herb 2 to 3), closing the strictly-cheaper faucet
     PRICE: THE R5 SURFACE MOVES, so under this phase's stopping rules this is an ESCALATION rather than an execution: docs/design/power-verification.md:1085 names recipe_growth_tonic as alchemy's cheap-path recipe at '(0, 11c)', and on that table's own sell-value basis (stated at :1104-1107) three herbs plus the vial reads 15c, which TIES recipe_silverleaf_mana_draught (recipes.ts:1563) and recipe_minor_healing_potion (recipes.ts:139), so both the number and WHICH ROW occupies the column move, and 12.3's 'cheapest path in materials ... 0.66 to 0.88 of the cost' band at :1143-1148 is measured off exactly that per-skill-point cost. The Verdict that closed 2026-08-29 re-opens on its own conditions and a harness re-cut rides it. Setting that aside, the code price is small and clean: one literal at src/sim/content/recipes.ts:4138, a reword of the recipe's own FLAGGED comment at :4130-4136 (which says '2 herbs, the low end of the shipped 2-to-4 band') and of :4128 ('Input 20 vs output 6' becomes 24); two literals in tests/farm_recipes.test.ts, the herb count at :775 and TONIC_EXPECTED_INPUT_VALUE at :715 with its derivation comment at :711-714; the strict output-below-input arm at :808-820 stays green and widens (6 under 24). `npm run wiki:content` IS owed because src/guide/content.generated.ts:9176-9180 carries the material count as data, gated by the tests/guide.test.ts freshness arm, but no prose key rewords (src/ui/i18n.catalog/guide.ts:3070 names no reagent route on purpose) so no M16 fills. No new id, so no committed WebP, no ITEM_ART_PENDING re-mint, no deed record, no Reliquary page, no merge-deletion-list census row, no world_api parity pin, no monolith ceiling. No parity golden movement expected (tests/parity/scenarios.ts:5989 grants the tonic with addItem rather than crafting it) but the farming parity directory is re-run to prove it. Reviewers: content-obligations-reviewer plus qa-checklist; architecture-reviewer skipped, pure data and test changes per the Review Dispatch Matrix at docs/prd/masterwrought/implementation-plan.md:152. Side effect worth naming: it raises herb demand, which R18 welcomes, and it makes the tonic's gold loss steeper, which strengthens the (ai) dormancy argument the addendum weakened.
  4. [WORK] Re-set the growth tonic's sellValue off 6
     PRICE: One literal at src/sim/content/items.ts:1123, and the honest warning is that it reds NOTHING as the tree stands: only the strict output-below-input arm at tests/farm_recipes.test.ts:808-820 constrains it, so any value from 1 to 19 passes silently (option 2 exists to fix that). No wiki regen (src/guide/content.generated.ts:9188-9192 carries the output's name, count and quality but not its value), no i18n key, no M16 fill, no art, no deed, no Reliquary page, no census row, no golden, no parity pin. What it does cost is the record: the (ai) dormancy ruling at farming/state.md:2867-2869 rests on 'Crafting it early is a strict gold loss (output 6 under input 20), so no exploit window opens', and lifting the output toward the input erodes the one argument holding the pre-go-live craft harmless, so the (ai) half must then be answered by option 3 rather than by option 1. The Phase 4 record at farming/state.md:2611-2613 and the recipe comment at recipes.ts:4128 both need re-deriving. Reviewers: content-obligations-reviewer plus qa-checklist. The replacement number is a maintainer-named value this row does not guess.
  5. [WORK] Move the tonic off rung 0 (gate it at 25 or 50) so it stops being a rung-0 skill-up row at all
     PRICE: Refused by the row's own pinned record rather than by cost. tests/farm_recipes.test.ts:751-763 pins skillReq 0 with the reason written into the assertion message ('the tonic is a plant-time knob for EVERY farm tier, so it must not be gated behind a mid or late alchemy rung'), and the same reasoning is in the recipe comment at recipes.ts:4132-4136; the SCAFFOLDING_BY_RUNG budget/level pair moves with the rung, so itemLevelBudget and level move too. The harm the record names is real: gating it leaves tier-1 and tier-2 farmers with a yield knob no low alchemist can brew for them, which is the opposite of D7's cross-profession trade. It also does not close the (ai) question, it relocates it: the cheap column at power-verification.md:1085 simply falls to the next-cheapest rung-0 alchemy row, so the R5 escalation of option 3 rides this too. Same content obligations as option 3 (recipes.ts literals, two test suites, `npm run wiki:content` because the generated entry carries skillReq and tier at src/guide/content.generated.ts:9171-9172, content-obligations-reviewer plus qa-checklist), for a worse outcome. Listed so the disposition is on the table with its price rather than assumed away.
RECOMMENDATION: Ratify both halves (option 1), taking option 2's one-line sellValue pin as cheap insurance. The packet's own records lean that way from both ends: the sell-only doctrine is written as design twice (items.ts:1105-1109 and farming/state.md:2611-2613) and a buyValue would price a faucet the record says must not exist; the (ai) deviation is already ledgered in a live test comment rather than hidden (tests/professions_zone_rollout.test.ts:1320-1323); and the hazard is measurably narrower than the addendum recorded, since on the basis a player actually pays (buyValue where it exists) the tonic reads 20 against recipe_minor_healing_potion's 15, so the cheapest rung-0 alchemy skill-up is a station-free, trainer-free row that shipped long before farming. The only option that closes the faucet moves a frozen R5 input, which this phase's stopping rules make an escalation rather than an execution, so the ratification is also the one answer that finishes inside this sitting.
FLAGS: needsMaintainerValue=true VALUE_NEEDED: Only if the maintainer changes rather than ratifies. Option 4 needs a replacement growth_tonic sellValue: an integer strictly below the pinned input value (20 today, 24 if option 3 also lands). The packet records do NOT derive it: farming/state.md:2611-2613 states the 6 with no basis, and the four-times-sell staple convention the sibling constants use prices vendor-STOCKED rows, which this one is deliberately not, so no shipped convention resolves it. Option 3 needs the maintainer to confirm what 'the shipped floor' resolves to: 3 (matching recipe_silverleaf_mana_draught at recipes.ts:1563) is the only reading that removes the strictly-cheaper faucet without overshooting; 4 would match recipe_silverleaf_healing_draught at :1548 and make the tonic the DEAREST rung-0 herb row. Option 5 needs the target rung (25 or 50).; r5Surface=true; contingentOn=none

### D159  [dish-curve-point-assignments]
SEED: row 4 ('dish curve points' is named in the row-4 tuning-reads list; D032's own retune option already forward-refers to 'the dish-assignments row', which the sweep never expanded)
QUESTION: Ratify the eight plain farm dishes' shipped foodHp/sellValue pairs (90/6, 117/12, 243/25, 432/40, 552/60, 552/75, 980/150, 980/150, each a reuse of a point the shipped food curve already carries) and the live dish reagent bills, or re-set any of them? Four of the eight bills are no longer the Phase 6 proposals this row was opened against: Phase 11e widened them under the add-never-substitute rule and re-derived their input figures.
ANCHORS: LEDGER: farming/state.md:486 as read (the still-open handoff row); the Phase 6 sign-off list at farming/state.md:4444-4448 as read ('Phase 6 proposed values awaiting sign-off: the eight dish foodHp/sellValue assignments (each reuses a shipped curve point; flagged at their rows in profession_items.ts), the dish reagent counts'); the Phase 6 record naming all eight pairs at farming/state.md:2568-2578 as read; the row-4 tuning-reads list at phase-19-rulings-gate.md:195 as read ('dish curve points'). LIVE CODE, re-read in this worktree: src/sim/content/profession_items.ts:900 (the maintainer flag) and :903-966 (the eight defs, kind 'food', no buyValue, quality by rung); the sibling buff-dish block :1003-1039 (same pairs plus the 11c-owned wellFed field); src/sim/content/recipes.ts:3738 (the same flag over the bills) and :3772-3956 (FARM_RECIPES, with the 11e-widened bills at :3849 bannock highland_barley x4 + thornpeak_cabbage x2 + fine_thornpeak_cabbage x1 + cooking_salt x2, :3875 soup, :3904 tart, :3933 platter). PINS: tests/farm_recipes.test.ts:104-112 (ALLOWED_FOOD_CURVE_POINTS), :171-194 (EXPECTED_INPUT_VALUE literal input table), :538-555 (the reuse arm with its used.size >= 6 spread and 980/150 ceiling non-vacuity), :558-571 (exact-input arm), :572-601 (the raw-sell no-minting arm, whose non-vacuity rests on the beet braise sitting EXACTLY at break-even), :1126-1141 (every allowed point must be carried by a live NON-dish food). tests/vendor_floor.test.ts:281-283 (the crafted-only foodHp tier list 90/117/243/432/552/980/1392) and :322-357 (the crafted-only id-to-value map naming all eight dishes by literal). WIKI: src/guide/content.generated.ts:10386-10424 carries both the reagent counts and effect.food.amount. R5: docs/design/power-verification.md:164 as read, the food term is the apex plate's wellFed 6 for 900 s, not foodHp or sellValue.
OPTIONS:
  1. [no-op] Ratify the shipped pairs and the live bills as authored
     PRICE: A dated RULED line flipping the handoff row at farming/state.md:486. No code, no pin, no regen: the reuse claim, the per-row input literals and the crafted-only id-to-value map already pin exactly what is being ratified, so the ruling adds authority to arms that are green today.
  2. [docs] Ratify, and retire the two pending-sign-off headers in the same change
     PRICE: Comment-only rewrite of the 'VALUES ARE PROPOSED AND FLAGGED FOR THE MAINTAINER' paragraphs at src/sim/content/profession_items.ts:900 and src/sim/content/recipes.ts:3738 so they cite the ruling instead of an open sign-off, plus the state.md row. No value moves, no pin moves, no artifact regen; only the copy scan (no em/en dashes, no emoji) rides it.
  3. [WORK] Re-set any pair or bill onto OTHER already-shipped curve points and counts
     PRICE: Value edits in src/sim/content/profession_items.ts:903-966 and/or the bills in src/sim/content/recipes.ts:3772-3956, then in the SAME change: EXPECTED_INPUT_VALUE re-derived per row (tests/farm_recipes.test.ts:171-194) AND the matching 'Input N vs output M' authoring comment corrected on each touched recipe row (the 11e QA caught exactly one of those figures deleted rather than corrected); the exact-input arm :558-571; the raw-sell no-minting arm :572-601, whose non-vacuity needs SOME row still exactly at break-even, so touching the beet braise bill means nominating a replacement break-even row or re-shaping that assertion; the reuse arm's spread and ceiling non-vacuity :538-555; the crafted-only id-to-value map tests/vendor_floor.test.ts:322-357, plus the tercile-label arm :293-310 if a value crosses a boundary and the tier list :281-283 if a tier empties; the fine-twin arms if a bill drops a twin slot (a twin left with no consumer anywhere reds the whole-column assertion). Wiki regen (npm run wiki:content) because src/guide/content.generated.ts carries both the counts and the effect amount, freshness-gated by tests/guide.test.ts. Nothing else rides: no new ids, so no committed WebP art, no M16 non-Latin fills, no world_entity_i18n row, no deed record, no Reliquary page, no symbol_census.mjs row, no IWorld facet member or tests/world_api_parity.test.ts movement, no parity golden (no timing or wire field moves), and neither content file sits under a tests/monolith_budget.test.ts ceiling. Reviewers per the Review Dispatch Matrix: content-obligations-reviewer on the content diff, test-coverage-auditor on the re-derived pins, qa-checklist to close. Not an R5 input: R5's food term is the apex plate's wellFed value, and these dishes' wellFed fields are 11c-D-2's and stay untouched.
  4. [WORK] Mint a NEW food-curve point for a farm dish (retire the no-new-rung claim)
     PRICE: Everything in the option above, plus: ALLOWED_FOOD_CURVE_POINTS gains a row (tests/farm_recipes.test.ts:104-112), and the backing arm at :1126-1141 REFUSES a dish-only point by construction (every allowed pair must be carried by a live NON-dish food), so minting one also means re-pricing a shipped non-farm food to own it, which lands inside D071's crafted-point territory, or re-shaping that arm deliberately; the crafted-only tier list at tests/vendor_floor.test.ts:281-283 gains a rung and the 11n vendor-floor margin ladder (vendor food sits its 10/15/20 margin BELOW the crafted points) has to answer it; and the 'the farm line adds no new rung to the food curve' claim is retired in prose at src/sim/content/profession_items.ts:893-897 and src/sim/content/recipes.ts:3738 in the same change. Boundary worth stating: a farm dish authored at or above 1392 would sit on the apex plates' own foodHp band, which is 11c/11k surface and should be escalated rather than executed, even though foodHp itself is not an input to the frozen R5 record.
RECOMMENDATION: Ratify as shipped (option 1, or option 2 if the maintainer wants the pending-sign-off comments retired with the ruling): every pair still reuses a point a live non-dish food carries, every bill is gold-negative at the exact literal pinned with the beet braise deliberately at break-even so cooking converts produce without minting copper, and four of the eight bills were already revised once deliberately (11e's add-never-substitute widening, re-derived at the time), so these numbers have survived a real revision plus the packet's economy sweeps with no finding against them; a re-set needs the maintainer's own numbers, which the packet records do not hold.
FLAGS: needsMaintainerValue=true VALUE_NEEDED: Only under the two re-set options: the replacement foodHp/sellValue pair per dish and the replacement reagent count per bill, named by the maintainer. The packet records hold ONE set of numbers, the shipped one (Phase 6's eight pairs at farming/state.md:2568-2578 and the 11e-widened bills), and no alternative proposal anywhere, so nothing can be derived and nothing may be guessed.; r5Surface=false; contingentOn=D071 (food-per-point-inversions): D071 owns the crafted food points these dishes reuse, and tests/farm_recipes.test.ts:1126-1141 requires each allowed point to be carried by a live NON-dish food, so a smoothing ruled there decides which points survive for this row to ratify.

### D160  [fine-twin-dish-consumers]
SEED: row 4 (the farming handoff-table row at farming/state.md:491, stamped "open ruling-owed. STILL OPEN and unchanged after masterwrought Phase 11j"; the Phase 18 sweep's row-4 re-derive picked up the quality half of the row six lines above it as D031 but never reached this one, and no D-row in the 151 names any of the three twin ids)
QUESTION: Do the hoe-reagent-only farming fine twins gain a dish consumer, or is hoe-reagent-only ratified as shipped design? Live code makes the set THREE, not the two the ledger row names: fine_vale_wheat, fine_marsh_rice and fine_highland_barley each have exactly ONE consumer anywhere on the merged tree and it is their hoe rung, while fine_evergarden_greens (which the candidate framing groups with them) is the only double-booked hoe twin, carrying three consumers already.
ANCHORS: LEDGER: farming/state.md:491 as read, the handoff-table row "fine_marsh_rice / fine_highland_barley dish consumers (hoe-reagent-only today) | P6 | maintainer | open ruling-owed. STILL OPEN and unchanged after masterwrought Phase 11j", whose own closing clause ("These two remain the only twins whose sole consumer is a hoe rung") is FALSIFIED by live code below; the Phase 6 sign-off framing at farming/state.md:4449-4452 as read ("whether fine_marsh_rice / fine_highland_barley should ALSO gain dish consumers: the Phase 5 deferred literal named exactly five twins, so the closure left those two hoe-reagent-only ... adding dishes for them is a content decision, not a gap in the closure pin"); the closure record at farming/state.md:2590-2591 as read ("remain hoe-reagent-only by design of the 5-slot closure"); the 11h supply-line note at phase-11h-supply-line-apex.md:354 as read; the herb-twin sibling finding recorded rather than fixed at state.md:9646-9650 as read, which explicitly defers to THIS row ("farming's own fine-twin question, already on the OPEN list"); and the working-note restatement at farming/progress.md:1058 as read. LIVE CODE, all verified in this worktree at HEAD d3f8bae369 on release/v0.41.0: the two defs and their consumer comments, src/sim/content/items.ts:1217-1221 ("A hoe reagent, and ONLY that: recipe_skysilver_hoe consumes it ... It gained NO dish in the economy-hooks phase, deliberately") above fine_marsh_rice at :1222-1229 (sellValue 16, buyValue 64), and src/sim/content/items.ts:1294-1297 ("A hoe reagent, and ONLY that: recipe_osmium_hoe consumes it") above fine_highland_barley at :1298-1305 (sellValue 30, buyValue 120). THE THIRD TWIN THE LEDGER ROW MISSES: fine_vale_wheat (src/sim/content/items.ts:1074) has exactly one reagent site in the whole of src/sim, recipe_bronze_hoe at src/sim/content/recipes.ts:3530 (grepped every src/sim reference: def, three comments, farm_crops.ts:190, and that one reagent line). THE ONE THAT IS NOT IN THE SET: fine_evergarden_greens has three consumers, the apex hoe at recipes.ts:3594 (count 2), recipe_evergarden_harvest_platter at :3946 (count 1) and recipe_laden_hearth at :3203 (count 1, cooking 125), so the candidate's parenthetical inclusion of it is wrong against the tree. THE OPEN-QUESTION STATEMENT IN SHIPPED SOURCE: recipes.ts:3962-3963, the Phase 11 buff-dish header, "NO fine twins here: the fine_marsh_rice / fine_highland_barley consumer set is an OPEN maintainer question"; the partition framing it sits under at recipes.ts:3727-3730 ("The hoe ladder took three of the eight (fine_vale_wheat, fine_marsh_rice, fine_highland_barley); these dishes take the remaining five"). HOE_RECIPES itself at recipes.ts:3523, with the three exclusive slots at :3530, :3545 and :3560, each count 4. THE HOST DISHES an in-place option would use: recipe_fenbridge_rice_bowl at recipes.ts:3812-3830 (marsh_rice x4 + cooking_salt x1, rung 25, trainer, output 25) and recipe_highwatch_barley_bannock at recipes.ts:3847-3873 (highland_barley x4 + thornpeak_cabbage x2 + fine_thornpeak_cabbage x1 + cooking_salt x2, rung 50, trainer, output 60); note the rice bowl and recipe_vale_hearth_loaf (:3774-3790) are the only two plain dishes carrying no fine twin at all, and both for exactly this reason. THE PINS: tests/farm_recipes.test.ts:125-136 (HOE_REAGENT_TWINS derived from the hoe bills, FINE_TWINS_CLOSED_HERE derived as the column minus it), :171-196 (EXPECTED_INPUT_VALUE, rice bowl 40 at :174, bannock 226 at :186, with the derivation comment at :175-186), :207-208 (FARM_RECIPES 14, dishes 12), :286-314 (the exact band map plus the non-emptiness arm), :558-571 (every dish vendors strictly below its pinned input), :575-602 (the raw-sell-basis arm, whose comment at :583 records "soup is at 2, bannock at 4, braise at 0"), :604-633 (every dish carries a reagent with NO buyValue), :627-687 (the twin-consumer arm: the dish-only loop at :630-636, the exclusivity retirement note at :637-658, the orphan sweep at :659-663, the four-member HOE_REAGENT_TWINS literal at :667-672, FINE_TWINS_CLOSED_HERE length 8 at :673, and the arm this ruling actually moves, "exactly the apex twin is double-booked" at :674-677), :1027-1031 (BUFF_DISH_IDS), :1035-1046 and :1048-1062 (the eight-plain / four-buff key-shape sweeps), :1064-1105 (the 11c well-fed ladder arm). The disjointness prose that would need rewording sits at tests/professions_zone_rollout.test.ts:1042-1064 ("These three gained no dish in Phase 6 and were never part of the deferral", with HOE_TWIN_CONSUMERS at :1047-1051). Wiki exposure: src/guide/content.generated.ts:10469 (rice bowl) and :10547 (bannock), plus the fine-twin material rows at :8299. Not in scope, verified: content data files are EXEMPT from the monolith ceilings (tests/monolith_budget.test.ts:36, "Data-as-code is exempt by design (src/sim/content/ ...)"), and scripts/merge_audit/symbol_census.mjs carries no reagent or itemId term (grepped, zero hits). R5 FENCE, verified: the frozen record's food term is the APEX plate only, docs/design/power-verification.md:164 and :166 as read (aura well_fed at 6 for 900 s, "delivered by an apex Harvest Feast"), and its own change-impact table at :1181 as read names "an apex plate value or duration" as what touches the food term; a plain reagent slot on a mid-rung dish moves neither.
OPTIONS:
  1. [no-op] Ratify hoe-reagent-only as shipped design, for the corrected set of three (fine_vale_wheat, fine_marsh_rice, fine_highland_barley)
     PRICE: A dated RULED line at farming/state.md:491 that also corrects the row's "these two" to the live three. Nothing executable moves: every pin listed above is green today and stays green, the load-bearing invariant (every twin has a consumer by SOME route) already passes over the whole column at tests/farm_recipes.test.ts:659-663, and no def, recipe, item id, icon, i18n key, golden, wiki artifact, deed, Reliquary page, census row or parity surface is touched. The only code-adjacent follow-on is comment-class and optional: three shipped source comments currently advertise the question as open (src/sim/content/recipes.ts:3962-3963 "an OPEN maintainer question", and the "A hoe reagent, and ONLY that ... deliberately" blocks at items.ts:1217-1221 and :1294-1297) and would be restamped RULED with the ruling id; that is a comment edit with no assertion behind it, so it can ride any later commit. No reviewer is required for a docs-only ruling, though the RULED line rides whatever commit closes the phase.
  2. [WORK] Give each hoe twin a count-1 slot on its own crop's existing dish (fine_marsh_rice into recipe_fenbridge_rice_bowl, fine_highland_barley into recipe_highwatch_barley_bannock, and, if the set is ruled as three, fine_vale_wheat into recipe_vale_hearth_loaf)
     PRICE: Two (or three) one-line reagent additions in src/sim/content/recipes.ts at the bills named above, following the shipped base-plus-fine pattern that fine_bog_beet, fine_frost_gourd, fine_thornpeak_cabbage, fine_gilded_sunmelon and fine_evergarden_pumpkin already use at count 1. WHAT FLIPS: EXPECTED_INPUT_VALUE at tests/farm_recipes.test.ts:174 and :186 re-pins (rice bowl 40 to 104 at buyValue 64; bannock 226 to 346 at buyValue 120; the loaf 20 to 20 plus fine_vale_wheat's buyValue, which the maintainer must supply because items.ts:1074-1081 must be read for it at authoring time), and the derivation comment at :175-186 must gain the new arithmetic rather than be left stale, which is the exact rot that block was written to prevent. The strictly-below-input arm at :558-571 stays green by construction (input rises, output is untouched). The raw-sell-basis arm at :575-602 stays green and its recorded margins move, so the "bannock at 4" figure in its comment at :583 is re-derived (bannock raw input rises by 30). The no-buyValue arm at :604-633 stays green because every host bill keeps its unpriced base produce, and the recipe_economy counterfactual vendor-fed set is untouched for the same reason. THE ARM THAT ACTUALLY REDS is "exactly the apex twin is double-booked" at :674-677: it becomes three or four ids and both the assertion and its label re-pin, which is the deliberate visible edit that arm exists to force. HOE_REAGENT_TWINS at :667-672 and FINE_TWINS_CLOSED_HERE's length 8 at :673 are unmoved (both derive from the hoe bills and the crop column, neither of which changes). FARM_RECIPES 14 / dishes 12 (:207-208), the band map (:286-314), the plain/buff key sweeps (:1035-1062) and the 11c ladder arm (:1064-1105) are all unmoved: no recipe row and no item def is added. PROSE THAT MUST MOVE IN THE SAME CHANGE or it becomes a lie: items.ts:1217-1221 and :1294-1297 both say "A hoe reagent, and ONLY that"; recipes.ts:3727-3730's three-took / five-took partition; recipes.ts:3962-3963's "OPEN maintainer question"; and tests/professions_zone_rollout.test.ts:1042-1046's "These three gained no dish in Phase 6" (its executable arms at :1052-1064 stay green, since the twins do not join the historical CLOSED_PHASE5_DEFERRALS map). SAME-CHANGE OBLIGATIONS: npm run wiki:content regenerates src/guide/content.generated.ts (the two craft rows at :10469 and :10547 gain a reagent and the twins' material pages gain a used-in entry), freshness-gated by tests/guide.test.ts. NOT owed, each checked rather than assumed: no new item id, so no committed WebP art and no ITEM_ART_PENDING movement (src/ui/icons.ts:5825), no icons.ts procedural recipe, no tests/shipped_item_ids.golden.json row, no src/ui/i18n.catalog/items.ts key and therefore no M16 non-Latin fills; no deed record (deeds are per conquerable content, not per bill) and no Reliquary page (no unique loot); no census row (scripts/merge_audit/symbol_census.mjs has no reagent or itemId term); no IWorld member, so no tests/world_api_parity.test.ts edit and no parity golden; no monolith ceiling (content is exempt at tests/monolith_budget.test.ts:36). REVIEWERS per the Review Dispatch Matrix (implementation-plan.md:144-154 as read): qa-checklist on the completed set; architecture-reviewer is explicitly skipped for pure data and test changes; plus content-obligations-reviewer under the repo-wide content-diff rule, which is not itself a row of that matrix. THE HONEST DESIGN COST: the twins are a pick-roll drop with one demand sink today, and a dish slot puts cooking in competition with the hoe rung that gates every higher-tier bed, so this is an economy change to the tool gate the farming loop is built on, not a cosmetic one.
  3. [WORK] Mint a new plain dish per twin instead of widening an existing bill
     PRICE: Two new food item defs plus two new cooking rows in src/sim/content, deliberately PLAIN (foodHp and sellValue only, the six-key shape at tests/farm_recipes.test.ts:1035-1046): a wellFed field would put a new rung on the ratified 11c ladder that decision 11c-D-2 fixed at one buff dish per crop tier (tests/farm_recipes.test.ts:1064-1105), and at any value above the apex 6 for 900 s it would move the R5 food term itself (power-verification.md:164, :166 and :1181 as read), so this option is fenced to plain dishes on purpose. Everything option 2 owes, plus: FARM_RECIPES 14 to 16 and dishes 12 to 14 at :207-208; EXPECTED_BAND_COUNTS at :286-297 re-derived for whichever rungs the new rows take; the plain sweep 8 to 10 at :1037; the rung scaffolding, quality-by-rung and foodHp/sellValue curve-point arms at :245-255, :490-535 and :540-556; the trainer-fee and recipeList pins at :1278-1282; two new ids in tests/shipped_item_ids.golden.json; two src/ui/i18n.catalog/items.ts English names (contributor-English-only, with M16 non-Latin fills owed in the same change if either name is wordy); two src/ui/icons.ts procedural recipes that must survive the pairwise-distinctness rule, or two ITEM_ART_PENDING entries at :5825 plus rows in docs/design/farming-asset-manifest.json under the standing art-debt handoff; and the wiki regen carries two whole new craft pages rather than two reagent lines. Still no deed, no Reliquary page, no census row, no parity surface, no monolith ceiling. Same reviewer set as option 2. This is the most expensive disposition and the record contains no argument for it: the shipped pattern for closing a twin has been a slot on an existing bill in every one of the five Phase 5 closures and both 11h capstones.
RECOMMENDATION: Ratify hoe-reagent-only (option 1), correcting the set to three: the record calls the state deliberate twice in shipped source (items.ts:1217-1221 and :1294-1297 as read) and once calls a change to it "a content decision, not a gap in the closure pin" (farming/state.md:4449-4452 as read), the only load-bearing invariant is already green over the whole twin column, and the loaf and the rice bowl carry no twin for exactly the coherent reason that their crop's twin went to the hoe ladder, so nothing here is an inconsistency to fix. The counterweight the maintainer should weigh against that: masterwrought Phase 11j retired the exclusivity rule that made hoe-reagent-only structural (fine_evergarden_greens is now deliberately double-booked at recipes.ts:3594 and :3946), so if route diversity is wanted, option 2 is the shipped-pattern way to buy it, and the ruling should then cover fine_vale_wheat too rather than leave the identical question standing one row down.
FLAGS: needsMaintainerValue=true VALUE_NEEDED: (1) The SCOPE: whether the ruling covers the corrected set of three (fine_vale_wheat included) or only the two the ledger row names, and if only two, why wheat is excluded. (2) Under options 2 or 3, the reagent count per twin: the packet records hold the shipped precedent of 1 across all five farm twin slots but do not choose between it and the accent cap of 2 for these rows. (3) Under option 2, whether the bannock may carry a SECOND fine twin (it already holds fine_thornpeak_cabbage at recipes.ts:3864), or whether fine_highland_barley should instead ride recipe_highwatch_barley_porridge; the records hold no rule either way. (4) Under option 3, the two dish ids, display names, rungs and foodHp/sellValue curve points, none of which the packet holds. No number here may be invented.; r5Surface=false; contingentOn=D004 only if the maintainer wants a reagent count of 2 rather than 1 under option 2 or 3: farming's own five shipped twin slots are all at count 1, which is self-contained, but whether RULE 2's accent cap reaches the farm ladder at all is not stated in the records read here, so a count of 2 wants D004's value-half answered first.

### D161  [toolsnote-stale-nonlatin-fills]
SEED: new (seeded by the farming/state.md handoff row at :493, 'Gathering wiki tools note: five stale non-Latin fills after an English reword', and 11j QA open item 12 at state.md:12842; the HORIZON half is D126's and this row does not duplicate it, the disposition of these five named rows is not ruled anywhere)
QUESTION: The gathering wiki's tools note (`guide.profPages.toolsNoteThreeRods`) renders above the tool table on EVERY gathering page, and its five non-Latin rows still carry three claims the English retired. Does the maintainer re-fill those five at the Phase 20 release fill, re-key the note now the way 11i did so the pending machinery carries it, or accept the stale rows on the record and let D126's horizon dispose of them?
ANCHORS: LEDGER: docs/prd/masterwrought/farming/state.md:493 as read (the handoff row, status cell 'open ruling-owed', 'THE EXPOSURE IS THAT NO GATE CAN SEE IT: a reword leaves a translated row translated ... Either re-fill those five at release or rule that the key is re-keyed'); docs/prd/masterwrought/state.md:12842-12845 as read (11j QA open item 12, same two dispositions); docs/prd/masterwrought/state.md:14136-14148 as read (the 11i mechanism record: seven keys 'RETIRED AND RE-KEYED ... the ONE mechanism the repo has that forces a `pending` row in every locale', pending measured 7677 to 7817, 'what the re-key BUYS is that every locale now renders the CORRECT ENGLISH in the meantime'). LIVE CODE, all re-read in this worktree: the English value src/ui/i18n.catalog/guide.ts:2975-2976; the sole render site src/guide/pages/professions_gathering.ts:89 (guarded at :86 by `if (!g.tools.length) return ''`, so it renders on every shipped gathering page, farming included); the five stale rows src/ui/i18n.locales/ja_JP.ts:14858, ko_KR.ts:14868, ru_RU.ts:15084, zh_CN.ts:14541, zh_TW.ts:14547, each still ending on the retired land-tools claim and ja_JP still reading 'each land trade has two crafted tools' plus 'everyone knows the land recipes from the start'; src/ui/i18n.resolved.generated/pending.ts carries the key exactly 15 times, once per Latin locale (es, es_ES, fr_FR, fr_CA, it_IT, de_DE, pt_BR, cs_CZ, nl_NL, pl_PL, id_ID, tr_TR, sv_SE, vi_VN, da_DK) and never for the five non-Latin, so the release-tier gate is green over them; the only two guards are English-only (tests/guide.test.ts:2822-2845 and :4241-4250, both `setLanguage('en')`); M16 lives at tests/i18n_completeness.test.ts:157 and fires only on a non-Latin value byte-identical to English; the release fill worklist reads the registry's `pending` slice alone (scripts/i18n_fill_worklist.mjs:1-11), so a `translated` row can never reach it; the staleness demotion is unsatisfiable in source (scripts/i18n_scan.mjs:32-37); the retire-and-replace shape is scripts/i18n_retired_keys.mjs:37-41. GIT, verified here: the five non-Latin rows were last touched at ba37787cb6, and the English has moved four times since (dad384b5c0 'the shared tools note was false on the farming wiki page', 5d85a728bb, 76088becfe, 428f67ced7), each touching only the catalog and the Latin-filled resolved slices; the 11i re-key a60aa9a4d7 touched 18 overlay files, dropping 13 Latin rows and re-filling the five non-Latin in the same commit.
OPTIONS:
  1. [WORK] Re-key now: retire `toolsNoteThreeRods`, author a successor key (the 11i mechanism)
     PRICE: NOT a one-liner and not English-only. Retire or delete the old key, add the successor to src/ui/i18n.catalog/guide.ts, re-point the render site src/guide/pages/professions_gathering.ts:89 and the two English pins in tests/guide.test.ts (:2838 and :4241), add a RETIRED_KEYS row with its reason in scripts/i18n_retired_keys.mjs if the old value is kept (the enchantsNote shape at :37-41) or delete it outright the way `profPages.toolsNote` was, then regenerate with `npm run i18n:gen` (translation_keys.generated.ts, the 21 resolved slices, pending.ts) and re-run the i18n freshness gate. THE PRICE THAT MAKES IT WORK: M16 (tests/i18n_completeness.test.ts:157) reds the moment the five non-Latin locales English-fill the new key, so five fresh non-Latin translations of the full four-paragraph note ride the SAME change, which is an src/ui/i18n.locales/ edit of exactly the class D079 asks the maintainer to ratify. Precedent on this very key: a60aa9a4d7 did it, 18 overlay files, five non-Latin re-filled in the commit. No wiki:content regen (this is catalog prose, not generated content), no deed, no Reliquary page, no WebP art, no census row, no parity golden, no world_api_parity pin, no monolith ceiling moves. Reviewers: qa-checklist (i18n arm) plus content-obligations-reviewer on the same-change list.
  2. [docs] Re-fill the five at the Phase 20 release fill, carried by hand
     PRICE: No code change in this packet: a dated RULED line at farming/state.md:493 and state.md:12842, plus an EXPLICIT hand-carried entry on the Phase 20 fill list, because nothing generates one. scripts/i18n_fill_worklist.mjs:1-11 turns only the registry's `pending` slice into a batch, these five read `translated`, and the release-tier gate hard-fails on `pending` rows only, so no tool will ever name this key. The work itself is the same five translations as the re-key option, deferred: the note is the longest prose leaf on the page family (four paragraphs, roughly 700 English words), and it lands in src/ui/i18n.locales/, the maintainer's own release lane. Until the fill ships, the public /wiki keeps telling five languages that each land trade has two crafted tools, that every character knows the land recipes (all four hoe rungs are `acquisition: ['trainer']`), and that the land trades' top tools open no ground (a tier-N bed needs a tier-N hoe). It is also the same hand-carried promise that already failed here once: the 11j QA reworded the English and the five rows did not follow, and four further rewords have landed since.
  3. [no-op] Accept the five stale rows on the record and let D126's horizon dispose of them
     PRICE: No-op beyond a dated RULED line pointing at D126. Honest ONLY under D126's second or third option, both of which eventually demote an unverified row to pending and pull this key onto a fill worklist; under D126's first option (stamp every existing row with its current English hash) these five are frozen as fresh permanently and this exposure becomes permanent with them, which is the same trap D126's own text names for q_prof_attune_outfitter. Under every option the earliest possible correction is a future fill pass, so the wiki renders the retired claims in five languages until then, and the row must be ruled again if D126 lands on option 1.
RECOMMENDATION: Re-key. The packet's own records already answered this question once on this exact key: 11i hit the identical defect, closed it by retiring and re-keying seven keys, and recorded the re-key as 'the ONE mechanism the repo has that forces a pending row in every locale', with the interim benefit that every locale renders the CORRECT ENGLISH rather than a confident false translation; the re-fill option has no automated carrier at all (the worklist reads `pending` only), which is precisely why this row exists.
FLAGS: needsMaintainerValue=false; r5Surface=false; contingentOn=D126 (only for the third option: accepting the stale rows is coherent only if D126 lands on its second or third option, so rule D126's horizon first) AMENDED IN PLACE 2026-09-02 at execution (D161), never rewritten: the addresses (farming/state.md:493, state.md:12842-12845, state.md:14136-14148) have drifted (556, 13348-13351, 14689-14705 at a1b435ff85); the note is three paragraphs, about 725 words; THREE tests in tests/guide.test.ts consume the key and 'publishes the tool-gate thresholds through placeholders in EVERY locale' walks every resolved slice for the first 'toolsNote' row, so the old key is deleted outright (the a60aa9a4d7 shape) and the successor keeps the stem; the five non-Latin rows were written at a60aa9a4d7 and the English moved five times since. Full list in phase-19f-english-i18n-completeness.md.

### D162  [epoch-bump-at-golive]
SEED: row 4 (an open-family farming handoff-table row, farming/state.md:494, that the sweep's row-4 re-derive did not expand: its immediate sibling at :495, the renderer.ts exact-count ceiling re-pin, became D026, and D006 at phase-19-rulings-gate.md:41 expressly routes "the epoch-bump read" here rather than into the state.md thirteen)
QUESTION: At the masterwrought and farming go-live, is the already-shipped combined ONLINE_WORLD_LAYOUT_VERSION 26 ratified as the ONE epoch bump both features ride, or does farming's bed content earn a separate epoch of its own? And is the go-live deploy order (server first, then the OTA bundle) written into the record as a named exemplar, or left to the generic rule in docs/ota-updates.md?
ANCHORS: THE ROW: farming/state.md:494 as read, the handoff-table row "ONLINE_WORLD_LAYOUT_VERSION epoch bump at farming go-live (dated note 2026-08-30: the epoch has since moved repeatedly and reads 26 after the eighth v0.41.0 sync, which renumbered the branch's 12 past the release's raid ladder 13 to 25; the go-live question this row records stands unchanged)", owner maintainer, status "open ruling-owed". THE RAISE: farming/state.md:4411-4416 as read, "ONLINE_WORLD_LAYOUT_VERSION at farming go-live (raised by the nineteenth-absorb cross-platform review): the epoch discriminator in src/world_api.ts scopes itself to 'the authoritative town layout' and went 6 to 7 release-side for an unrelated reason; farm beds are world content with positions, so whether a farming client/server pairing across the go-live deploy needs its own epoch bump is a maintainer call, decided once at go-live"; carried into the OPEN list at farming/state.md:156-157 as read ("the ONLINE_WORLD_LAYOUT_VERSION go-live question joined the OPEN list") and enumerated at farming/state.md:431-432 as read ("the ONLINE_WORLD_LAYOUT_VERSION epoch call at go-live; the renderer.ts exact-count ceiling re-pin prepared at the nineteenth absorb"). SYNC RECORD: state.md:22840-22842 as read, "ONLINE_WORLD_LAYOUT_VERSION lands at 26 (the release ran raid epochs 13 to 25 and reserved 12; the branch's 12 renumbered past the ladder in every mirror and pin)". TABLE CROSS-REFS: phase-19-rulings-gate.md:41 (D006 routes "the epoch-bump read" to seeded row 4, not to itself) and :168 (D133: "the epoch already carries its own open row at farming/state.md:494"). LIVE CODE, all re-read in this worktree: src/world_api.ts:190 `export const ONLINE_WORLD_LAYOUT_VERSION = 26 as const;` and :191 the derived `auth-world-26` discriminator, with the bump's own justification at :185-189 ("26 = Masterwrought and farming ship together: equipped-instance snapshots carry required Perfecting fields ... and the self wire carries the `fplot` farm-plot delta. An epoch-25 client can neither render Perfected copies nor a farm, and an epoch-25 server omits both, stranding Perfecting progress and plots") and the renumber note at :179-184 ("12 is deliberately unassigned ... the v0.41.0 sync renumbered it 26 below so it sits above every raid epoch"); the refusal literal at :195-196. STRICT FAIL-CLOSED HANDSHAKE: server/ws_auth.ts:266 (`if (msg?.t !== ONLINE_WORLD_AUTH_TYPE)`) with the refusal wired at :66, the client frame built by src/net/world_auth_message.ts:13 and :23, the client's own copy of the message at src/net/online.ts:149 and :1248. HAND-MAINTAINED NODE MIRRORS (not generated): scripts/lib/world_auth.mjs:4 `'auth-world-26'`, scripts/lib/world_auth.d.mts:1. PINS THAT READ THE VALUE LITERALLY: tests/bank_wire_epoch.test.ts:102-108 (plus its compile-time epoch fixtures at :13-69 and the per-epoch rationale comment at :93-101), tests/security.test.ts:113-121, tests/world_auth_scripts.test.ts:179-184 (which reads the .d.mts file text back byte-for-byte). WHY FARMING WAS THOUGHT TO NEED ITS OWN: farm bed geography is COMPILED CLIENT CONTENT, src/sim/content/farm_patches.ts:1-49 (the static garden-bed geography, one patch per hub) with FARM_BEDS_BY_ID at :182 and the persisted-save-key rule at :51-56, while the per-player plot state rides the fplot self delta (server/game.ts:9197, src/net/online.ts:3715, src/world_api/farming.ts:36). DEPLOY MECHANICS: docs/ota-updates.md:174-186 as read ("The compatibility rule is the world-layout EPOCH, not the release ... Ordering only becomes load-bearing on a release that bumps the epoch (or the sibling STABLE_TIMER_WIRE_VERSION), where the server must be deployed first", with epoch 11 as the named exemplar), the preflight at scripts/ota/check_server_layout.mjs:34 and :125-134, and the visible update gate at src/net/ota_update_gate.ts:33 and :148 (described at docs/ota-updates.md:59-73). MAINTAINER PRECEDENT AGAINST A GEOMETRY-ONLY BUMP: DEPLOY.md:420-432 as read, where the world grew past the old terrain rectangle and the instance plane rebased and "The release left the fail-closed layout gate at ONLINE_WORLD_LAYOUT_VERSION 3 through both changes, so stale bundles are still admitted at reconnect; bumping it is the one-line mechanical answer if the maintainer resolves the surfaced forced-refresh question toward refusing stale sessions."
OPTIONS:
  1. [no-op] Ratify the shipped combined epoch 26 as the one masterwrought-and-farming go-live bump
     PRICE: A dated RULED line only. The handoff row at farming/state.md:494 flips, the OPEN-list bullet at farming/state.md:4411-4416 and the two enumerations at :156-157 and :431-432 take the ruling id, progress.md takes its Phase 19 row. Nothing in the tree moves: src/world_api.ts:190 already reads 26 and its comment at :185-189 already names farming's fplot delta as half the bump's ground; all three exact-value pins already read 26 (tests/bank_wire_epoch.test.ts:102-108, tests/security.test.ts:113-121, tests/world_auth_scripts.test.ts:179-184) and both Node mirrors already carry 'auth-world-26' (scripts/lib/world_auth.mjs:4, scripts/lib/world_auth.d.mts:1). Zero code, zero tests, zero i18n keys and zero non-Latin fills, no npm run i18n:gen, no npm run wiki:content and so no tests/guide.test.ts freshness cost, no deed record, no Reliquary page, no committed WebP art, no census row (scripts/merge_audit/symbol_census.mjs sees no new export), no parity golden, no tests/world_api_parity.test.ts movement, no tests/monolith_budget.test.ts ceiling. No R5 surface. Reviewer: the docs pass only.
  2. [docs] Ratify 26 AND write the go-live deploy order into the record as a named exemplar
     PRICE: Everything in option 1, plus one exemplar paragraph in docs/ota-updates.md beside the epoch-11 one at :174-186, saying the masterwrought/farming go-live is a bumping release so the server deploys BEFORE the OTA bundle, and that the shells need no store re-ship to survive the window because the visible update gate already turns an ONLINE_WORLD_INCOMPATIBLE_MESSAGE refusal into download-then-apply (src/net/ota_update_gate.ts:33, :148) and scripts/ota/check_server_layout.mjs:125-134 is the preflight that proves the running server speaks the checkout's discriminator. Docs only: ota-updates.md feeds no generated artifact, so nothing regenerates and tests/guide.test.ts is untouched; the refusal string is already shipped and matcher-localized (src/world_api.ts:195-196), so no i18n key and no M16 fills; no census row, no parity golden, no ceiling, no R5 surface. Written under the anchor rule (stable paths and symbols, no line numbers or counts). Reviewers: the docs pass, plus cross-platform-sync if the native-shell half is written down.
  3. [docs] Ratify the bump but make the NUMBER a merge-time re-derive rather than a value ratified today
     PRICE: A standing instruction on farming/state.md:494 and in the sync checklist the packet already runs at each release absorb: the constant is re-derived from the release tip at the FINAL sync, exactly as the eighth sync did (state.md:22840-22842 as read; the renumber itself is recorded in the code comment at src/world_api.ts:179-184). Zero code today. When it next fires the mechanical bill is fixed and small: the constant at src/world_api.ts:190, the numbered comment entry at :185-189, the three exact-value pins (tests/bank_wire_epoch.test.ts:102-108 including its not-toBe ladder, tests/security.test.ts:113-121, tests/world_auth_scripts.test.ts:179-184) and the two hand-maintained Node mirrors (scripts/lib/world_auth.mjs:4 and scripts/lib/world_auth.d.mts:1, the second read back byte-for-byte by world_auth_scripts.test.ts:184). No content, i18n, wiki regen, deed, Reliquary, art, census, parity golden or monolith ceiling either now or when it fires; no R5 surface. Reviewer at the fire: cross-platform-sync.
  4. [WORK] Split farming onto its own epoch number (27) so the two features could be deployed apart
     PRICE: src/world_api.ts:185-190: the single 26 entry splits into two numbered comment entries and the constant goes to 27; all three exact-value pins re-pinned in the same change, including the not-toBe ladders whose comments name the epochs they fence (tests/bank_wire_epoch.test.ts:93-108, tests/security.test.ts:113-121) and tests/world_auth_scripts.test.ts:179-184; both Node mirrors hand-edited (scripts/lib/world_auth.mjs:4, scripts/lib/world_auth.d.mts:1) because :184 reads the .d.mts text literally; the OTA preflight is value-free and needs nothing (scripts/ota/check_server_layout.mjs:34). And it buys nothing observable: the branch's delivery rule is ONE Masterwrought PR (the D22 supersession recorded in the handoff table at farming/state.md:499 as read), so no deploy exists at which a farming-only epoch could ever be seen, and a client without the compiled bed geography (src/sim/content/farm_patches.ts:1-49, FARM_BEDS_BY_ID :182) is precisely the client the Perfecting half of 26 already fences. No content, i18n, wiki regen, deed, Reliquary, art, census row, parity golden or monolith ceiling; no R5 surface. Reviewers: architecture-reviewer, cross-platform-sync, test-coverage-auditor for the re-pinned ladders (docs/qa-gate.md is the canonical roster).
RECOMMENDATION: Ratify the shipped 26 (option 1), with option 2's one-line deploy-order note as the cheap rider. The branch has already taken the bump and its own code comment names farming in it (src/world_api.ts:185-189: "Masterwrought and farming ship together ... the self wire carries the `fplot` farm-plot delta. An epoch-25 client can neither render Perfected copies nor a farm"), Perfecting forces that bump regardless of how farming is answered so "no bump at all" is not on the table, and the maintainer's own recorded precedent runs against splitting on geometry grounds (DEPLOY.md:420-432 as read left the gate at epoch 3 through both a terrain-rectangle growth and an instance-plane rebase); every pin and both Node mirrors in the tree already read 26, so ratifying costs a dated line while splitting costs a five-file re-pin for a deploy that cannot happen. Answer it in the same sitting as D133, whose display-only option defers its own epoch call to this row.
FLAGS: needsMaintainerValue=false; r5Surface=false; contingentOn=none

### D163  [qprofintro-overflow-grant]
SEED: row 4 (a farming handoff-table 'open ruling-owed' row, named in the row-4 enumeration at farming/state.md:433 and routed there explicitly by D006; sibling of D021, which rules the other half of the same (bg) grant fence)
QUESTION: Ratify the quest-template fallback grant deliberately bypassing bag capacity (a full 16/16 backpack still receives the First Furrow hoe and seed as overflow slots, painted 17/16 by the counter's own over state), or capacity-gate the accept and giver-talk grant the way the same file's turn-in reward already is?
ANCHORS: LEDGER: farming/state.md:496 as read, the handoff row 'q_prof_intro requiredItems overflow grant on a full bag (17/16 visible) | P9 QA | maintainer | open ruling-owed'; the residual text at farming/state.md:3975-3980 as read ('the requiredItems grant on accept and re-talk bypasses bag capacity (a full 16/16 bag still receives the hoe and seed as overflow slots, 18/16; the q_prof_intro template's pre-existing behavior, not farming's; the bag header paints "17/16" so it is visible, maintainer read owed on the template)'); the P9 QA SHOULD-FIX line at farming/progress.md:2225-2226 as read; the Phase 17 close list at farming/state.md:4408-4409 as read; the row-4 enumeration at farming/state.md:433 as read. LIVE CODE re-read in this worktree: src/sim/quests/quest_commands.ts:186-188 (finalizeQuestAccept force-adds every questFallbackGrants id through ctx.addItem with NO fit pre-check) and its giver-talk twin regrantMissingQuestItems at :291-310 (same ungated ctx.addItem at :301), against the SAME file's turn-in reward gate at :388-390 (countFit + bagsFullError); src/sim/quest_fallback.ts:20-31 (the pure grant list, capacity-blind by construction); src/sim/sim.ts:8283-8298 (Sim.addItem goes straight to addStacked); src/sim/bags.ts:251-253 ('Applies NO capacity cap (capacity is a pre-check concern); callers on a gated path check canAddItem/fitsAll first') and the doctrine header at bags.ts:17-27, which enumerates the gated boundaries (quest turn-in among them) and the deliberately ungated ones (need/greed, master loot, delve rewards, dev gives) but does NOT name the fallback grant; src/sim/bags.ts:50 (BACKPACK_SLOTS = 16, the 16 in '17/16'); the over-capacity paint at src/ui/bags_window.ts:641 (className `bag-capacity${model.used > model.capacity ? ' over' : ''}`) and :651-653; the shipped same-class precedent at src/sim/professions/fishing.ts:607-612 ('Deliberately NOT capacity-gated ... could soft-lock the quest chain. Force-add (over-capacity tolerated)'); the farming carrier itself at src/sim/content/zone1.ts:1053 (q_farm_intro, giver farmer_jessica) and :1085 (requiredItems ['garden_hoe','vale_wheat_seed']). BLAST RADIUS: tests/professions_starter_tools.test.ts:207-221 sweeps every requiredItems quest and asserts at least five exist; the other carriers are copper_mining_pick (zone1.ts:1031, :1445), gathering_sickle (zone1.ts:1504, :1752), firebottle (zone2.ts:1006), crypt_keystone (zone3.ts:2131), ps_briny_lure (proving_shore.ts:533), ps_passing_stone (proving_shore.ts:655). tests/quest_fallback.test.ts is 92 lines and carries no capacity arm at all.
OPTIONS:
  1. [no-op] Ratify the template bypass as shipped (status quo)
     PRICE: Genuinely no-op: a dated RULED line on the farming/state.md:496 handoff row and the matching Phase 17 close-list entry. No code, no test pins, no wiki regen, no i18n or M16 fills, no deed, Reliquary, art, census or parity movement, no reviewer beyond the qa-checklist floor.
  2. [docs] Ratify, and name the fallback grant in the bags.ts capacity doctrine so its absence stops reading as an oversight
     PRICE: Comment-only: extend the enumerated ungated list in the src/sim/bags.ts:17-27 header (and optionally a pointer line at quest_commands.ts:182-186) to include the accept and giver-talk fallback grant, carrying the same soft-lock reason fishing.ts:609-611 already states. No behavior change, so no pin moves; neither src/sim/bags.ts nor src/sim/quests/quest_commands.ts is ceiling-pinned in tests/monolith_budget.test.ts; no new export, so no scripts/merge_audit/symbol_census.mjs row; no wiki regen, no fills, no goldens. Reviewers: qa-checklist floor only.
  3. [WORK] Capacity-gate the fallback grant (refuse when the grants would not fit)
     PRICE: A QUEST-TEMPLATE rule change, not a farming one. Add a fitsAll(meta.inventory, bagPools(meta.bags), grants) pre-check at quest_commands.ts:186 and again at :300, refusing through the shipped bagsFullError (src/sim/bags.ts:362); no new i18n key and no M16 fills, since 'error.bagsFull' is already in the client matcher (src/ui/sim_i18n.ts:36 plus its locale rows). It reaches EVERY requiredItems quest, including the shipped Proving Shore tutorial (ps_briny_lure, ps_passing_stone) and the two zone-1 gathering intros, content the farming packet does not own. The refusal SHAPE is part of the call: refuse the whole accept (a full-bagged newcomer cannot take the tutorial quest) or accept and withhold (the :291 giver re-grant is then the recovery route, so no tool is ever destroyed). New capacity arms in tests/quest_fallback.test.ts (which has none today) and in the tests/professions_starter_tools.test.ts sweep. Parity: the two accept scenarios use q_wolves and q_boars (tests/parity/scenarios.ts:2004 and :2223), neither carrying requiredItems, so NO golden movement is expected; adding a requiredItems accept scenario would mint one. No IWorld facet member, so tests/world_api_parity.test.ts is untouched; no wiki, deed, Reliquary, art or census obligation. Reviewers per the Review Dispatch Matrix: sim-invariants and the tests reviewer, plus a parity read because both hosts run the same accept path.
RECOMMENDATION: Option 2: the packet's own records size this as pre-existing template behavior that is visible rather than harmful (the counter paints its own `over` state), the repo already carries a shipped force-add doctrine for exactly this class at fishing.ts:609-611, and the only real defect is that the bags.ts doctrine header enumerates the ungated paths without listing this one, which a comment closes at no behavioral cost.
FLAGS: needsMaintainerValue=false; r5Surface=false; contingentOn=none

### D164  [journey-script-gate-entry]
SEED: farming/state.md handoff row at line 497 as read, 'Journey script joins the gate when the merged Masterwrought PR ships' (P9b QA, maintainer, open ruling-owed), with its Phase 9b QA origin at farming/state.md:4022-4024; new to this table.
QUESTION: Does the committed go-live journey (scripts/farming_journey_e2e.mjs) ever join CI, and if so on which lane, or is it ratified permanently as a hand-run acceptance rig with the layer-honesty source pin standing in as the CI guard?
ANCHORS: LEDGER: farming/state.md handoff row at line 497 as read ('Journey script joins the gate when the merged Masterwrought PR ships (reworded at the 11b absorb: D22 is superseded, delivery is the one Masterwrought PR, and the script stays off CI until that ships) | P9b QA | maintainer | open ruling-owed'); the Phase 9b QA residual at farming/state.md:4022-4024 as read ('the journey script stays outside the gate under D22 (the layer-honesty pin stands in), maintainer call at go-public'); the 11b absorb rewrite record at state.md:6156 as read (the journey-script handoff row named among the forward-looking rewrites). LIVE CODE, verified in this worktree: scripts/farming_journey_e2e.mjs (392 lines; :14-18 the prereq 'the vite dev client must already be serving (default http://127.0.0.1:5188/)' that NOTHING in CI provides, :37 enterOfflineGame, :303 the one sanctioned '/dev farmgrow'); the standing source guard tests/farm_verb_reachability.test.ts:175-206 ('the journey script stays layer-honest', negative arms :187-198 over seven forbidden window.__game verbs, positive arms :201-205 over [data-plant], data-quest="q_farm_intro", mobile-interact, KeyF), which rides the always-run Node suite and is the only thing about this script CI sees today; the gate's ONE browser step scripts/lib/gate_steps.mjs:178-185 ('browser regressions', npm run test:browser) and its CI twin .github/workflows/ci.yml:350-370 (job browser-gate, name 'Browser tests', timeout-minutes 10 sized on a 3.73-minute slowest healthy wall for a 2.7x margin) plus :442-443 (run: npm run test:browser); 'Browser tests' is a REQUIRED merge-queue context, docs/merge-queue.md:167, pinned at tests/ci_workflow.test.ts:1271 (job-name form) and :1299 (doc form, required half only); the browser-mode scope note tests/browser/_harness.ts:1-8 ('The 3D world / canvas pixels stay OUT of scope', windows fed a fixture IWorld); vitest.browser.config.ts:16-26 (chromium-only, opt-in, tests/browser/**); the two whole-file job pins any new job trips, tests/ci_workflow.test.ts:1309 (bounds every ci.yml job against the checkout-stall class) and :1926 (EVERY job key in ci.yml AND nightly.yml must be classified pg-wired / guarded DB-less / test-free or the test reds); nightly.yml runs no *_e2e.mjs either (its only script calls are :95 nightly_targets, :201 i18n_coverage_summary, :324 nightly_report; its browser job is :225-291); scripts/browser_path.mjs:8-13 (load-time throw unless a Chromium-family binary resolves) with scripts/browser_path_resolve.mjs:14-45 (playwrightCacheRoots plus playwrightBrowserCandidates, so the browser-gate job's own 'npx playwright install chromium' cache at ~/.cache/ms-playwright IS resolvable by the script's puppeteer-core path); scripts/release_mint.mjs:36-84 (the required-checks ruleset re-point any new required context would ride).
OPTIONS:
  1. [no-op] Ratify it permanently OFF CI: the layer-honesty pin is the standing guard and the journey stays a hand-run acceptance rig
     PRICE: One dated RULED line on farming/state.md:497 plus the same stamp on the Phase 9b QA residual at farming/state.md:4022-4024 (the two places that carry the deferral). No workflow edit, no new pin, no re-derived bound, no required-context change. tests/farm_verb_reachability.test.ts:175-206 keeps guarding the class the script exists to catch (a verb driven through window.__game), and scripts/farming_journey_e2e.mjs stays committed and runnable by hand per its own :14-18 recipe. No wiki regen, no i18n, no M16 fills, no deed, no Reliquary page, no WebP art, no census row, no parity golden, no world_api parity pin, no monolith ceiling. No reviewer.
  2. [no-op] Defer once more: keep it off CI now, re-ask at go-public (the row's own wording)
     PRICE: One dated RULED line recording the deferral and its trigger on farming/state.md:497. Genuinely no-op today, and it keeps the row alive as one of the few CI items outliving the packet; it is also the option that stays contingent on D022, since 'ships' is D022's to date. Nothing in the tree moves; the same nil obligations as option 1.
  3. [WORK] Add an UNREQUIRED nightly lane that serves vite and runs the committed script
     PRICE: One new job (or one new step on the existing nightly browser job, nightly.yml:225-291) that installs Playwright Chromium so scripts/browser_path_resolve.mjs:14-45 resolves the binary scripts/browser_path.mjs:8-13 throws without, serves vite on :5188 (the script's :14-18 prereq) with a port-scoped teardown, and runs both viewports. A new job key forces TWO whole-file pins to be updated in the same change: tests/ci_workflow.test.ts:1309 (a measured timeout-minutes bound, derived the way browser-gate's 10 was derived from its 3.73-minute wall, and the packet holds NO measured wall for this script, only '17/17 PASS desktop and mobile', so the measurement is part of the work) and tests/ci_workflow.test.ts:1926 (classify the job test-free or guarded DB-less, with the token scan cross-checking that the classification is not a lie). Stays off docs/merge-queue.md's required half, so no name pin at tests/ci_workflow.test.ts:1271/:1299 and no scripts/release_mint.mjs ruleset re-point. Keep tests/farm_verb_reachability.test.ts:175-206 as-is (it slices the same .mjs). Honest residual: a wall-clock puppeteer walk with retry loops is a flake surface, and a red nightly is triage, not a merge block. No wiki regen, no i18n, no deed, no Reliquary, no art, no census row, no parity golden, no world_api parity, no monolith ceiling. Reviewers: gate-integrity-reviewer (any change to the gate/CI selection pipeline) plus qa-checklist.
  4. [WORK] Put it on the REQUIRED PR tier, either ported into tests/browser or as its own required job
     PRICE: Two mechanisms, both dear. (a) PORT into tests/browser/farming_journey.browser.test.ts so it rides the existing 'Browser tests' job: this is not a new file in an established family, because tests/browser/_harness.ts:1-8 states the suite renders window PAINTERS against a fixture IWorld with 'The 3D world / canvas pixels stay OUT of scope', so a full offline client boot, a real Sim, and the /dev farmgrow and xp/copper checkpoints have no equivalent there and would be re-authored; the two then diverge unless the layer-honesty pin at tests/farm_verb_reachability.test.ts:175-206 is re-pointed or the .mjs is retired. (b) OWN required job: everything in option 3 plus the required-context surface. Either way it lands inside a required merge-queue check: browser-gate's 10-minute bound carries only a 2.7x margin over a 3.73-minute healthy wall (ci.yml:363-370), so the bound must be re-derived at tests/ci_workflow.test.ts:1309 from a measured journey wall; a new required NAME additionally moves docs/merge-queue.md:167's required half and both pins at tests/ci_workflow.test.ts:1271 and :1299, and needs the branch-ruleset re-point scripts/release_mint.mjs:36-84 owns (per docs/merge-queue.md, 'Minting a release branch') or the queue goes dormant. Every PR in the repo then pays the journey's wall, and a stalled puppeteer run blocks the queue rather than reporting. It would also be the FIRST scripts/*_e2e.mjs ever on CI (grep over .github/workflows/ finds none), so the precedent lands on the whole rig family. No wiki regen, no i18n, no deed, no Reliquary, no art, no census row, no parity golden, no world_api parity, no monolith ceiling. Reviewers: gate-integrity-reviewer, the test-quality reviewer, and qa-checklist.
RECOMMENDATION: Ratify it permanently off CI (option 1): the guard for the exact class the journey exists to catch already runs on every CI leg (tests/farm_verb_reachability.test.ts:175-206, non-vacuous in both directions), no scripts/*_e2e.mjs has ever run on any workflow, and the one browser lane it could join is a required merge-queue check whose bound leaves a margin this script's unmeasured, vite-serving wall-clock walk would eat; if a running proof is wanted, option 3's nightly lane buys it without touching the required list.
FLAGS: needsMaintainerValue=false; r5Surface=false; contingentOn=D022 (ip-17-push-consent): this row's own trigger is the merged Masterwrought PR shipping, so options 3 and 4 can only be EXECUTED after D022 sets when that happens; options 1 and 2 are answerable today and do not wait.

### D165  [crop-glb-preload-lane]
SEED: row 4 (an open-family farming handoff-table row; one of the two '(glance)' rows in that table the sweep left unexpanded)
QUESTION: Do all 12 crop-stage GLBs keep riding the one preload lane that gates the Renderer, so world entry waits on the whole 17-file farm set, or does the packet split them off that lane and let the primitive-box fallback draw until they land?
ANCHORS: farming/state.md:500 (the handoff row, as read: 'CRITICAL preload lane for the 12 crop-stage GLBs (background-lane split invited) | P7 QA | maintainer | open ruling-owed (glance)') and farming/state.md:3336-3339 (as read, the P7 QA note that raised it: 'the 12 crop-stage GLBs ride the CRITICAL preload lane though only bed/bin are needed at scene build (maintainer ruling invited on a background-lane split)'). LIVE CODE, verified in this worktree: src/render/farm_patches.ts:154-162 registers the WHOLE set through registerDeferredPreload, one thunk per url; src/render/farm_patches_core.ts:243 and :299-311 define that set as 17 urls (bed, sprout, compost bin, both feast tables, plus 3 families x 4 stages = the 12); src/render/assets/preload.ts:20-27 and :109-128 are why the lane is still blocking ('assetsReady() still awaits every CRITICAL registered task before the Renderer is constructed', the two lanes differing only in WHEN the fetch starts), opened at src/main.ts:1367; src/render/farm_patches.ts:905-946 builds the program anchors from templateParts over every stage and feast url and src/render/farm_patches.ts:426-432 stages them at FarmPatchVisuals construction, so the anchors read the LOADED scenes; src/render/farm_patches.ts:196-207 is the fallback (glbTemplateParts over an absent scene yields a primitive box, never a wait); src/render/farm_patches.ts:145-151 is the residency policy this row would re-open; src/render/CLAUDE.md:166-186 states the standing contract ('preload.ts is the boot gate, and it has TWO lanes') plus the tier-independent superset rule. THE MECHANISM THE SPLIT WOULD USE IS GONE: state.md:10998-11001 (as read) records the v0.41.0 sync removing DeferredPreloadPriority, beginBackgroundPreloads and the priority parameter with 'ZERO references survive', re-confirmed live by grep, and carried as two written census deletion rows at merge-deletion-list.md:269 and :433 (as read). Measured on disk here: the 12 stage GLBs are 155,356 bytes of the farm set's 208,200 (about 152 KB of about 203 KB), all local bundle files fetched after Play, never on the launcher.
OPTIONS:
  1. [no-op] Ratify the single blocking lane as shipped
     PRICE: One dated RULED line on the farming handoff row. No code, no pins, no regen, no reviewers. The set stays a superset of every tier's placement set the way src/render/CLAUDE.md:182-186 requires, buildFarmProgramAnchors keeps staging real stage geometry rather than fallback boxes, and the cost stays the measured 152 KB of local bundle bytes that already fetch concurrently behind the loading screen after Play.
  2. [WORK] Re-introduce a non-blocking background lane in preload.ts and move the 12 stage GLBs onto it
     PRICE: A third lane in src/render/assets/preload.ts (a register + begin pair plus a task list assetsReady does NOT await) and its opener beside src/main.ts:1367; the split registration in src/render/farm_patches.ts:154-162 keyed off farm_patches_core's url helper. The real price is the prewarm: buildFarmProgramAnchors (farm_patches.ts:905-946) runs at construction and would then stage anchors off the primitive-box fallback for all 12 stages, so the first real crop draw compiles on-frame, exactly the hitch the anchors exist to prevent; keeping the gate honest needs a re-anchor pass when the lane settles, which is a NEW GPU producer under src/render/CLAUDE.md's scheduler rule and must ride the FarmCompileGate seam. Pins that flip: tests/defer_launcher_preloads.test.ts (lane-order arms plus the EAGER_ALLOWED source scan), tests/ios_entry_memory.test.ts (the registry's value-erasure guard must hold on the new list too), tests/farm_patches_adapter.test.ts:975-1032 (the anchors-staged and gate-label arms), and tests/farm_patches_core.test.ts:294-311 plus tests/render_glb_replacement_assets.test.ts:509-520 if the 17-url helper splits in two (both assert 17 and the manifest existence). CENSUS: merge-deletion-list.md:269 and :433 currently swear DeferredPreloadPriority and beginBackgroundPreloads are gone, so a re-introduction under either name reds scripts/merge_audit/symbol_census.mjs unless those two rows are retired (or replaced with explained-extras rows) in the SAME change; a new name instead needs its own explained-extras row. Docs: the 'TWO lanes' paragraph at src/render/CLAUDE.md:166-181 is no longer true and must be rewritten. Reviewers per the dispatch matrix: render-performance-reviewer (a new GPU producer and a compile-gate change) plus qa-checklist. NOTHING ELSE RIDES: no wiki regen (no guide surface moves), no M16 non-Latin fills (no player string), no deed record, no Reliquary page, no committed WebP art, no IWorld member and so no tests/world_api_parity.test.ts edit, and no monolith ceiling (farm_patches.ts is 995 lines and carries no row in tests/monolith_budget.test.ts).
  3. [WORK] Keep the two lanes and demand-load a family's stage GLBs at its first plot
     PRICE: No lane change and no census exposure, but the work lands in the hot path instead: src/render/farm_patches.ts gains an on-demand loadGltf plus a re-template step when it resolves, and the plot sync must not re-issue the fetch per frame. It carries the SAME anchor consequence as option 2 (the anchors stage before any family is known, so they hold fallback programs) and adds a visible pop the first time each family shows a stage, since templateParts (farm_patches.ts:196-207) draws a colored box until the scene arrives. It also contradicts the module's own written residency policy (farm_patches.ts:145-151, the P7 QA deferral that states the 17 scenes are retained deliberately), so that comment is rewritten in the same change. Same pins as option 2 minus the preload-lane ones: tests/farm_patches_adapter.test.ts anchors arms, and the two 17-url arms if the helper splits. Same reviewers.
RECOMMENDATION: Option 1, ratify. This row's own measurement is what settles it: the 12 stage GLBs are about 152 KB of local bundle bytes fetched after Play, so they are nowhere near the launcher-decode class that created the lanes in the first place, while both split options break the program anchors (they would compile against fallback boxes) and option 2 additionally owes a census edit against two written deletion rows for a lane the release itself deleted.
FLAGS: needsMaintainerValue=false; r5Surface=false; contingentOn=none

### D166  [idle-sway-reducedmotion]
SEED: row 4 (the second of the two '(glance)' rows at the foot of the farming handoff table, farming/state.md:501, that row 4's re-derive expanded up to :499 and then stopped; its sibling D033 came from :498)
QUESTION: The farm crop idle sway advances on every frame whatever the viewer's reduced-motion choice: FarmPatchVisuals.update() takes dt alone and neither renderer drive site passes the flag the renderer already computes. Ratify the exemption on the ambient-world-wind reading (the foliage and grass precedent), or gate the sway on reducedMotion() the way Phase 18 gated the legendary-regalia drift?
ANCHORS: LEDGER: farming/state.md:501 as read, 'Idle sway ignores reducedMotion (foliage precedent) / P7 QA / maintainer / open ruling-owed (glance)'; the P7 QA prose that raised it, farming/state.md:3338 as read, 'idle sway ignores reducedMotion (ambient world motion, the foliage precedent; maintainer glance)'. LIVE CODE (verified in this worktree): the sway loop src/render/farm_patches.ts:586-597, per-frame writes keyed only on dt (visual.phase advanced at :592, the lean composed onto the seat quaternion at :595-596), no motion input anywhere in the signature; its amplitude table src/render/farm_patches.ts:365-378 (SWAY_SPEED 1.1; SWAY_AMPLITUDE sprout 0.01, stage2 0.025, stage3 0.04, stage4 0.05, withered 0, so a withered plot already sits still); the seam that would carry the flag, src/render/farm_patches.ts:445 drive(world, dt, worldReady = true); the two call sites that never pass it, src/render/renderer.ts:4959 (the prewarm-guarded call, 89 columns) and src/render/renderer.ts:11822 ('this.farmPatchVisuals?.drive(this.sim, dt);'); the flag itself, src/render/renderer.ts:12197-12199 'private reducedMotion(): boolean { return this.reduceMotionSetting || (this.reduceMotionMql?.matches ?? false); }'. SCOPE: the visuals are per-viewer (src/render/farm_patches.ts:9 header, the myFarmPlots mirror at :477), so the motion is the viewer's own beds, never a crowd. THE PRECEDENT, verified rather than assumed: src/render/foliage.ts:3776 does take reducedMotion but spends it only on updateTreeHides (:3778-3790); the plant wind itself is the shader at src/render/foliage.ts:841 (and the grass card copy at :2517) running off sharedUniforms.uTime, which src/render/renderer.ts:4843 and :9952 advance unconditionally in both the prewarm and live frames, and src/render/blade_grass.ts:169 binds the same clock, so every ambient plant motion in the game is un-gated today. THE COUNTER-PRECEDENT, inside this packet: src/render/legendary_regalia_core.ts:147-155 legendaryRegaliaEmitDt returns 0 under reducedMotion, its header calling it 'an accessibility choice by the viewer, never a graphics shed', doctrine written at docs/design/graphics-settings-fairness.md:82-83 and behaviorally pinned at tests/legendary_regalia.test.ts:227-244. PINS THAT FLIP: tests/farm_patches_adapter.test.ts:919 'drive(): the ready flag gates the read alone, the sway always runs' (its sway arm at :934-939), the source-text literal inside tests/farm_patches_adapter.test.ts:839 asserting the live site is exactly 'this.farmPatchVisuals?.drive(this.sim, dt);' (:914), and describe('idle sway') at tests/farm_patches_adapter.test.ts:462. HEADROOM: src/render/renderer.ts measures 13020 lines against the zero-slack ceiling 13023 at tests/monolith_budget.test.ts:774.
OPTIONS:
  1. [no-op] Ratify the exemption: ambient plant wind is not reduced-motion gated, the sway keeps running
     PRICE: Genuinely no-op. A dated RULED line on the P7 QA note (farming/state.md:3338) and the handoff row (:501). No code, no test edit, no i18n or M16 fills, no wiki regen, no deed, no Reliquary page, no WebP art, no census row, no parity golden, no world_api member, no ceiling movement, no reviewer dispatch. The accepted consequence stated plainly: a viewer who set Reduce motion (or whose OS did) still sees their own crops leaning, up to 0.05 rad at stage 4, on beds they are standing next to.
  2. [docs] Ratify the exemption AND record the class boundary
     PRICE: Docs only. One bullet in docs/design/graphics-settings-fairness.md beside the regalia bullet at :82, naming ambient plant wind (the foliage and grass uTime shader plus the farm crop sway) as the exempt class, so the 'accessibility choice, never a graphics shed' doctrine stops reading as if it covered everything that moves. No test pins doc prose, so nothing reds; the fairness suite is untouched. Same dated RULED lines as option 1. No code, no census, no parity, no i18n, no art. Reviewer: none required beyond the docs read.
  3. [WORK] Gate the sway on reducedMotion()
     PRICE: WORK, and it lands wider than it looks. (a) A fourth parameter on FarmPatchVisuals.drive (src/render/farm_patches.ts:445) threaded to update() (:588), plus the disposition the ruling must also state: freeze the phase where it stands, or re-seat group.quaternion to visual.seatQuat so a stopped crop stands normal to its bed. (b) Both renderer sites re-wired: src/render/renderer.ts:11822 fits on one line, but :4959 is already 89 columns and adding the argument passes biome's lineWidth 100, wrapping the call to about six lines, which puts renderer.ts near 13025 against the zero-slack 13023 ceiling at tests/monolith_budget.test.ts:774; that needs an extraction out of renderer.ts to pay for it, or the ceiling raise that is D026's subject and a maintainer decision either way. (c) ANY renderer.ts byte owes the Eastbrook re-mint: node scripts/assets/eastbrook_grand_armoury/remint_polish_provenance.mjs re-stamps the four polish seals under docs/screenshots/eastbrook-vale-rebuild/polish (the rendererIntegrationSha256 leaf, scripts/assets/eastbrook_grand_armoury/capture_contract.mjs:500). (d) Three pins re-cut: the 'the sway always runs' arm at tests/farm_patches_adapter.test.ts:919 becomes a two-arm behavioral pin (moving and suppressed), the live-site literal at :914 re-cut to the new call shape, and describe('idle sway') at :462 gains the suppressed arm. (e) The packet's own Phase 18 precedent puts the decision in a pure core rather than the painter (legendaryRegaliaEmitDt), so a new farm_patches_core.ts export owes a merge-deletion-list.md exports row and a green tests/merge_audit_symbol_census.test.ts. NOT owed: no i18n or M16 fills (no player text), no wiki regen or tests/guide.test.ts freshness gate, no deed record, no Reliquary page, no committed art, no IWorld member so no tests/world_api_parity.test.ts pin and no parity golden. Reviewers: frontend-seam-reviewer and render-performance-reviewer (a per-frame write path in the entity loop), plus test-coverage-auditor for the re-cut pins.
RECOMMENDATION: Ratify and record it (option 2): the packet's own P7 QA note classified the sway as ambient world motion and asked for a glance rather than a fix, and the code bears that out, since every other plant in the game leans on the same un-gated uTime clock, so gating the crop alone would leave farm beds the one plant on screen that stops; the bullet is worth the ten minutes because the Phase 18 regalia doctrine currently reads broader than it is.
FLAGS: needsMaintainerValue=false; r5Surface=false; contingentOn=D026 (only for the work option: gating the sway grows renderer.ts past its zero-slack ceiling, so the margin ruling has to land first or an extraction has to pay for it)

### D167  [aura-visible-cap-low-fairness]
SEED: new (a handed-to-maintainer row in the same farming/state.md handoff table as D033's row-4 seed, at farming/state.md:504; distinct from D033, which rules the downgrade's LEGIBILITY given the icon is on screen, from D120, which rules the overwrite MECHANIC, and from D109, which ratifies a different fairness classification on a different surface)
QUESTION: Ratify or change the fairness classification that a Well Fed icon shed by the LOW graphics preset's buff cap is cosmetic upkeep rather than actionable information? AURA_VISIBLE_CAP_LOW = 8 now sheds LONG-duration buffs first and Well Fed runs 600 to 900 sec, so it sits in exactly the bucket that sheds first, on the one preset, from the player whose only cue to re-eat is that icon.
ANCHORS: LEDGER: farming/state.md:504 as read, the handed-to-maintainer table, 'AURA_VISIBLE_CAP_LOW = 8 self-buff cap fairness (pre-existing, not farming's) | P11 QA | maintainer | handed-to-maintainer'; the P11 QA prose at farming/state.md:4211-4215 as read, 'recycles auras past the cap on the LOW tier; pre-existing and outside this diff, but the well-fed buff is information a player re-eats on, and no test pins that a self-buff survives the cap. Owner: maintainer fairness read, not farming's.' LIVE CODE re-read in this worktree: src/game/ui_tier_knobs.ts:91 (AURA_VISIBLE_CAP_LOW = 8, authored with no derivation in its own comment) and :96-98 (auraVisibleCap, 'low' only, POSITIVE_INFINITY elsewhere); src/ui/auras_painter.ts:228 (the single cap read) with its FAIRNESS comment block at :211-226; src/ui/aura_overflow_priority.ts:25-33 (ALWAYS_VISIBLE_AURA_IDS, six ids: divine_ascension, shaman_thunder_charges, shaman_warspirit_cadence, moontide, old_blood, verdance; well_fed is NOT among them), :34-36 (isExempt: isDebuff, alwaysRender, or an allowlisted id) and :57-83 (selectShedSlots fills the cap budget with shortDuration buffs FIRST, so a long buff is the first thing shed); src/ui/auras_view.ts:85 (NEVER_SHED_IDS, the SECOND exemption set, carried flag only), :343 (SHORT_BUFF_PRIORITY_SEC = 60) and :348 (isShortDurationBuff, duration <= 60). Well Fed's own numbers put it an order of magnitude past that bucket: src/sim/wellfed.ts:43 (WELL_FED_AURA_ID) minting durations 900 s at the apex rungs (src/sim/content/profession_items.ts:731, :740, :749) and 600 s on the farm dishes (:1010, :1019, :1028, :1037). TWO MITIGATIONS LANDED AFTER THE P11 QA NOTE and the note does not know about them: the honest '+N' overflow badge (src/ui/aura_overflow_badge.ts:23-29, key hudChrome.unitFrame.buffOverflowLabel at src/ui/i18n.catalog/hud_chrome.ts:715, plural tooltip at :2744) and the default-off 'Always Show All Buffs' opt-out (src/ui/hud.ts:4158-4163 and :8831-8833 buffBarFxTier reporting 'ultra' to auraVisibleCap, wired at hud.ts:4997, surfaced at src/ui/options_view.ts:748). Doctrine side: docs/design/graphics-settings-fairness.md:43-46 classes buff-icon overflow cosmetic, :140-181 records the badge and priority passes. THE GAP THE NOTE NAMES STILL HOLDS: tests/professions_graphics_fairness.test.ts is the packet's actionable-vs-cosmetic guard and carries no aura-cap arm at all, and tests/aura_overflow_priority.test.ts:81 reaches the exempt set by index ([...ALWAYS_VISIBLE_AURA_IDS][0]) rather than pinning membership, so nothing anywhere asserts that a self-buff does or does not survive the cap.
OPTIONS:
  1. [no-op] Ratify: a Well Fed icon is cosmetic upkeep; the honest +N badge plus the Always Show All Buffs opt-out are the answer
     PRICE: Genuinely no-op: a dated RULED line only. docs/design/graphics-settings-fairness.md:43-46 already states the classification, the badge and the opt-out already ship, and no file, pin, or artifact moves. Leaves the note's factual complaint standing (still no test pins it either way).
  2. [docs] Ratify AND pin it: keep the shed exactly as it is, and close the 'no test pins that a self-buff survives the cap' gap with a fairness arm asserting the CURRENT contract
     PRICE: Test and docs only. tests/professions_graphics_fairness.test.ts gains an aura-cap arm over the real selectShedSlots (src/ui/aura_overflow_priority.ts) with plain AuraSlotState fixtures, asserting the shipped contract in four directions: a 900 s well_fed slot IS sheddable, a debuff is not, an ALWAYS_VISIBLE_AURA_IDS id is not, and exempt slots do not spend the cap budget; plus one arm that Hud.buffBarFxTier's 'ultra' override (src/ui/hud.ts:8831-8833) makes the cap never bite. One paragraph in docs/design/graphics-settings-fairness.md beside the 2026-08-26 and 2026-08-27 entries recording the ruling and its date. No src change, so: no wiki regen and no tests/guide.test.ts freshness exposure, no new t() key and no M16 non-Latin fills, no deed record, no Reliquary page, no committed WebP art, no census row (scripts/merge_audit/symbol_census.mjs sees no new export), no parity member and no tests/world_api_parity.test.ts touch, no ceiling moved in tests/monolith_budget.test.ts. Reviewers per the Review Dispatch Matrix: test-coverage-auditor, frontend-seam-reviewer.
  3. [WORK] Reverse: well_fed becomes never-shed, joining the affordance exemption
     PRICE: src/ui only, small diff but load-bearing. One id added to ALWAYS_VISIBLE_AURA_IDS (src/ui/aura_overflow_priority.ts:25-33) OR to NEVER_SHED_IDS (src/ui/auras_view.ts:85); the implementer must pick, since the two sets answer the same question and differ only in which layer owns it, and the fairness doc names the priority module. New arms in tests/aura_overflow_priority.test.ts (its :81 read is by index, so no membership pin flips and nothing existing goes red), the fairness arm from the option above inverted, and the doc paragraph. No wiki regen, no t() key or M16 fills, no deed, no Reliquary page, no art, no wire byte, no parity pin, no monolith ceiling (aura_overflow_priority.ts is not a ratchet file); a census explained-extras row in docs/prd/masterwrought/merge-deletion-list.md is needed ONLY if the implementer mints a NEW exported symbol instead of adding to an existing set. Reviewers: frontend-seam-reviewer, test-coverage-auditor. The real price is design, not diff: it spends one of the eight low slots permanently, and sets a precedent every other long-lived stat buff (every flask, every raid buff) can claim, eroding the cap the low preset exists to enforce. Note for the frozen record: the R5 food term is the aura's VALUE and DURATION (6 for 900 s, docs/design/power-verification.md:164 as read); icon visibility is not an R5 input, so no option here moves it.
  4. [WORK] Change the cap number itself: raise AURA_VISIBLE_CAP_LOW, or retire the cap and keep only the opt-out
     PRICE: Needs a maintainer number first (see valueNeeded). Flips the exact-value pin at tests/ui_tier_knobs.test.ts:129 and re-baselines the many AURA_VISIBLE_CAP_LOW-relative arms in tests/auras_painter.test.ts (most are written relative to the constant and follow, but the several that hard-count rendered nodes need re-reading), plus the knob-readout arm at tests/ui_tier_knobs.test.ts:295. Re-opens the per-frame DOM cost the cap exists to bound on the preset low-end and large-raid players pick, which makes it a render-performance question as much as a fairness one. No content, i18n, wiki, deed, Reliquary, art, parity, or census obligation. Reviewers: frontend-seam-reviewer plus render-performance-reviewer.
RECOMMENDATION: Ratify and pin (option 2): the packet's own record files this as 'pre-existing, not farming's' and hands over a fairness READ rather than a defect, and the two mitigations that landed after that note (the honest +N badge and the default-off opt-out) already meet the 're-eats on it' worry without spending one of eight slots permanently, so the only part of the note still simply true is the missing pin.
FLAGS: needsMaintainerValue=true VALUE_NEEDED: Only if option 4 is picked: the new AURA_VISIBLE_CAP_LOW value, or the decision to retire the cap outright. 8 is authored at src/game/ui_tier_knobs.ts:91 with no recorded derivation, and the packet records hold no measurement of how many simultaneous buffs a low-preset player actually carries, so no defensible replacement number can be read out of the ledger. Options 1 to 3 need no value.; r5Surface=false; contingentOn=none

### D168  [ci-shard-weights-go-public-harvest]
SEED: row 4 (the handed-to-maintainer farming handoff row at farming/state.md:506; seeded row 4 covers "every farming handoff-table row still in the open-family and handed-to-maintainer classes", and the Phase 18 sweep did not expand this one)
QUESTION: At go-public, does the wholesale CI shard-weight re-harvest ride the delivery PR (a whole-repo re-baseline of scripts/ci_shard_weights.generated.json taken off the packet's own branch run, which as the tooling stands today also forces a change to the non-vacuity pin the gate reviewer's finding produced), or does the PR ship on the honest local-carry table with the harvest owed to the next upstream full-mode run after the merge?
ANCHORS: LEDGER: farming/state.md:506, the handed-to-maintainer handoff row "CI shard-weights harvest re-run at go-public (scripts/ci_shard_weights_harvest.mjs) | 16th absorb | go-public session | handed-to-maintainer" (as read, no dated close inside the status cell); its origin at farming/progress.md:1600 (the sixteenth absorb, as read: "new farming suites ride the neutral fallback until scripts/ci_shard_weights_harvest.mjs re-runs; unreachable while D22 keeps farming off CI, deferred to go-public"); the standing procedure and its acceptance bar at docs/qa-gate.md:310-349 ("CARRIED to Phase 20: the post-push harvest ... owed at Phase 20, immediately after the first push whose CI run goes green in full mode, and before the packet's PR is called mergeable"), with the documented fallback at docs/qa-gate.md:279-296 (--carry-local-missing --runs 3 as the phase-close step) and again at :351-354. LIVE CODE verified in this worktree (HEAD 6972704db9, feature/masterwrought, tree clean, nothing pushed): scripts/ci_shard_weights.generated.json:2-7 still reads run "32621561241", harvested "2026-08-23", files 3651, harvestedFiles 2939, so 712 rows are carried, counted off the committed blob as 302 local-median (reason "phase 18 local carry pending the post-push harvest") plus 410 prose-backfill; 58 of the packet's own test files are rows and 55 of those 58 are carried, so the LPT balance for this packet's suites is presently planned entirely off local medians. scripts/ci_shard_weights_harvest.mjs:233-235 exits 1 on any run whose shard jobs did not print "changes-job decision: mode=full", and :250-252 writes "harvestedFiles: Object.keys(sorted).length, carried: {}" on a wholesale harvest. tests/ci_shard_partition.test.ts:316 asserts Object.keys(carried).length > 0 (comment at :311-313: "Floors near the committed shape (2939 harvested + 410 carried = 3349 at Phase 18), so a table that emptied one side cannot satisfy the identity trivially"), so a wholesale harvest reds that arm, which contradicts docs/qa-gate.md:344-347 requiring "carried empty" AND "tests/ci_shard_partition.test.ts is fully green" in one breath. The bars a fresh harvest can also move are computed live off the table, not pinned literals: worst-over-median <= 1.15 at tests/ci_shard_partition.test.ts:159 and :275, coverage at :281. One verified doc drift beside the decision, not part of it: the live coverage literal is 0.94 at tests/ci_shard_partition.test.ts:281 while docs/qa-gate.md:250 and :347 both say 95 percent, and the 0.95 literal exists only at scripts/merge_audit/shard_weight_union.mjs:174 (a session can fix that wording). The carry contract that makes the carried rows a stopgap rather than a result is stated at scripts/lib/ci_shard_weight_carry.mjs:30-36.
OPTIONS:
  1. [WORK] Harvest at go-public, riding the delivery PR (what docs/qa-gate.md prescribes today)
     PRICE: After the first push whose CI run goes green in FULL mode, run node scripts/ci_shard_weights_harvest.mjs <run-id> and commit the regenerated scripts/ci_shard_weights.generated.json: all 3651 rows re-valued off a feature-branch run, the 302 local-median and 410 prose-backfill attributions dropped. The SAME change must relax or re-word the non-vacuity arm at tests/ci_shard_partition.test.ts:316, because scripts/ci_shard_weights_harvest.mjs:252 writes carried: {} and 0 > 0 reds it; that arm is the machine half of the gate reviewer's finding, so the relaxation has to keep the fabrication check meaningful (the harvestedFiles + |carried| identity at :318 and the carriedDefects modal check still bite) rather than simply deleting it. Then re-read the live bars: worst-over-median 1.15 at :159 and :275 is scored off the NEW weights, so a fresh harvest can breach it, and the heal is a re-partition, never a bar raise. Also: retire the "CARRIED to Phase 20" block at docs/qa-gate.md:310-354 and reword the 0.94-vs-95-percent drift at :250/:347; biome check --write on the JSON; a dated RULED qr-19 line plus a status flip on farming/state.md:506. Reviewer: gate-integrity-reviewer (root CLAUDE.md routes any gate/CI selection-pipeline change there). Honest cost beyond the mechanics: it re-baselines a repo-wide shared artifact inside a game-content PR, and the harvest commit lands AFTER the run it was taken from, so the PR's CI re-runs on a tree that no longer matches the harvested run.
  2. [docs] Ship the delivery PR on the local-carry table; the wholesale harvest is owed to the next upstream full-mode run AFTER merge
     PRICE: No table rewrite and no pin change. The phase-close step already documented at docs/qa-gate.md:279-296 (node scripts/ci_shard_weights_harvest.mjs --carry-local-missing --runs 3, on the merged tree, as the last integration step) restores the coverage floor with honest per-row provenance, which is exactly the state the committed blob is already in. Edits are docs only: a dated RULED qr-19 line on farming/state.md:506 with the status flipped from handed-to-maintainer, and a reword of docs/qa-gate.md:310-354 so the harvest is owed to the next upstream full-mode run rather than to "Phase 20 ... before the packet's PR is called mergeable". Cost: 712 rows keep standing in for a 2026-08-23 harvest through one more cycle, and 55 of the packet's own 58 suites keep planning off local medians, so the LPT balance claim for exactly the new work is the weakest part of the table. It also leaves the tests/ci_shard_partition.test.ts:316 contradiction latent for whoever runs the eventual harvest.
  3. [WORK] Harvest, but as a standalone follow-up PR off the release branch rather than inside the delivery PR
     PRICE: Same mechanics as option 1 (the harvest, the :316 arm, the 1.15 re-read, the qa-gate block retirement, gate-integrity-reviewer), but landed as its own small PR whose diff is the weight table plus one test arm, which is a diff a gate reviewer can actually read. Delivery PR then merges under option 2's terms. Cost: one extra PR and its CI cycle, a second sequencing hand-back (the follow-up needs a full-mode run on the post-merge branch), and the packet's own suites ride local medians until it lands.
RECOMMENDATION: Option 1, harvest at go-public riding the delivery PR: the packet's own procedure doc states the wholesale re-harvest is owed "immediately after the first push whose CI run goes green in full mode, and before the packet's PR is called mergeable", and the carry contract calls all 712 carried rows a stopgap for exactly that harvest, so the record leans to executing it, with the tests/ci_shard_partition.test.ts:316 contradiction handled in the same change rather than discovered mid-run.
FLAGS: needsMaintainerValue=false; r5Surface=false; contingentOn=D022 (ip-17-push-consent). The harvest needs a completed, all-green, FULL-MODE CI run of a PUSHED branch, and this worktree is deliberately local (HEAD 6972704db9, nothing pushed), so this row can only be answered after the push ruling. AMENDED IN PLACE 2026-09-02 at execution (D168), never rewritten: the committed table at 6377bed8b0 is 3650 rows, harvestedFiles 2938, 712 carried (302 local-median plus 410 prose-backfill), not 3651 and 2939 (one row pruned since pricing); the handoff row's pipe-flattened anchor in the Phase 19G contract matched nothing and is corrected there; the pins the harvest reds are TWO arms of tests/ci_shard_partition.test.ts, 'the identity is non-vacuous: real rows on both sides, and every carried row is a row' and 'an undeclared row, a mis-stated ms, and a fabricated fallback block each RED it' (the recipe's step 3 says why); this row's own SEED and ANCHORS addresses have drifted since the pricing tip 6972704db9: the handoff row sits at farming/state.md:569 at dda61d91be (not :506), and the harvester's mode-check refusal and its harvestedFiles-plus-carried write sit at scripts/ci_shard_weights_harvest.mjs:296-299 and :306-316 (not :233-235 and :250-252); the pricing text keeps its addresses as history. Full list in phase-19g-closeout.md. ESCALATED 2026-09-02 (D168) under qr-19-ci-shard-weights-go-public-harvest (Phase 19G, under qr-19-best-for-project), the maintainer's word in session: 'Escalate whole'. NOT EXECUTED: the precondition (a green FULL-MODE CI run of a PUSHED branch) is unreachable under the standing NO-push rule, and the dependency D009+D170 has been ESCALATED and unexecuted since Phase 19A (the carrier assignment is the maintainer's), so the harvest could not be the LAST code-carrying change even if the run existed. No code or artifact in the harvest set moved (the two farming records moved, by design: they are the record); the coverage-drift rider (docs/qa-gate.md and the union tool say 95 percent where the release commit b38f38ba16 lowered the test literal to 0.94; reconcile toward 0.95, see the recipe) stays bundled with the harvest change by the maintainer's choice; the farming/state.md handoff row stays handed-to-maintainer, unflipped; a dated ESCALATED line sits after the sixteenth-absorb entry in farming/progress.md. THE RECIPE, ready for the push ruling (corrected 2026-09-02 by the D168 gate-integrity review; the full correction list is on the D168 AMENDED bullet of phase-19g-closeout.md): (1) BEFORE the run, read the row-count floor off the table, node -p "Object.keys(require('./scripts/ci_shard_weights.generated.json')).length - 1"; a smaller row count after the harvest means the run was not full mode after all. (2) After the first pushed run whose shard jobs print 'changes-job decision: mode=full' and go green: node scripts/ci_shard_weights_harvest.mjs <run-id> (it also refuses a run with fewer than ten green test jobs, the eight shards plus the two lanes). (3) TWO arms of tests/ci_shard_partition.test.ts red on the wholesale table, and both are repaired without weakening: in 'the identity is non-vacuous: real rows on both sides, and every carried row is a row' the carried-count floor of one admits an empty map while the harvestedFiles-plus-carried identity, the row and harvested floors, and the requireMap arm 'passes carriedDefects with the map REQUIRED, not merely consistent when present' keep biting; in 'an undeclared row, a mis-stated ms, and a fabricated fallback block each RED it' the mis-stated-ms and modal mutations move onto a SYNTHETIC table that carries rows (they read the live carried map today, and an empty map throws on the first and starves the second), never behind a truthiness guard on the live map; tests/ci_shard_weight_carry.test.ts already pins all three defect classes on fixtures; re-cut the identity arm's in-code comment ('2939 harvested + 410 carried = 3349 at Phase 18', stale twice over already) to the harvested table's shape. (4) Run npx vitest run tests/ci_shard_partition.test.ts tests/ci_shard_weight_carry.test.ts and read the live bars off the NEW weights in 'partitions the real tests/ tree into N complete packs (suite completeness)' (the only bar arm that reads the table; worst over median at or under 1.15, coverage at or over the literal), the stale-row arm finding no row whose file the syncs removed, and every literal arm a harvest can move: the named-file weight arms (druid_balance_probe above 120000, mail_expiry below 80000), the variance floor in the plausibility arm (at least five files above 60000), the per-row 20-minute ceiling, the 2400-row vacuity floor, and the identity arm's own row and harvested floors; a COVERAGE breach is healed by --carry-local-missing for the uncovered rows (or a fresh harvest), a RATIO breach by a deliberate shard-count change or by measuring where the distribution moved (the same run re-harvested reproduces the same weights), NEVER by a bar raise (the LPT packer is a deterministic greedy with no tunable to re-partition with). (5) RECONCILE THE THREE COVERAGE SURFACES: the release commit b38f38ba16 lowered the test literal from 0.95 to 0.94 while docs/qa-gate.md (five sites) and scripts/merge_audit/shard_weight_union.mjs (refuses under 0.95) kept 95 percent; the recommendation is to RESTORE 0.95 in the test (live coverage 0.9989 at this tip; MEASURED_FALLBACK_MS is 28 ms, so every uncovered file is planned as nearly free), never to write 0.94 into the prose; the maintainer's call at the harvest. (6) Retire the 'CARRIED to Phase 20' block of docs/qa-gate.md ONLY in the commit that lands a replacement carrying BOTH its own acceptance list (all four in one pass: the pre-run row-count floor cleared, harvestedFiles equal to the row count with carried empty and the prose-backfill block gone, the partition suite fully green with coverage clearing the literal AND the stale-row arm finding nothing, worst over median at or under 1.15) AND the three procedural items this recipe would otherwise lose (tests/ci_shard_weight_carry.test.ts in the vitest command, the --carry-local-missing fallback when no green full-mode run exists, the floor command itself); and drop its 'biome check --write on the JSON' step, which is a RED step, not a no-op (biome.json excludes the table, so biome exits 1 with 'No files were processed in the specified paths'; serializeWeightTable is the table's formatter, and the exclusion is load-bearing, never to be 'fixed' by un-ignoring the file). (7) Flip the handoff row's status cell to the CLOSED text in the Phase 19G contract; gate-integrity-reviewer on the diff. Ordering: last, after D009+D170 is either executed or ruled deferred past the PR. If no green full-mode run exists when Phase 20 closes, the fallback is --carry-local-missing on the merged tree, which restores the floor with honest per-row provenance and leaves the wholesale harvest owed.

### D169  [rarebody-reword-landmine]
SEED: new (a farming/state.md handoff row, 'open, deferred (reword landmine)'; adjacent to but NOT covered by seeded row 3's 'reworded wiki keys' stale fills', which concerns keys already reworded, whereas this key's 21 rows are currently accurate)
QUESTION: The farming page renders the shared 'Rare finds' prose, which names only the three node-trade windfall flavors (pristine vein, ancient heartwood, moonlit bloom) and not farming's golden harvest. Does that omission stand ratified, or does the flavor get named, and if so in which shape: retire-and-rekey the shared key, a farming-only add-on key, or an in-place reword of a key all 21 locales have already translated?
ANCHORS: LEDGER: docs/prd/masterwrought/farming/state.md:524 as read, the whole record ('guide.profPages.rareBody (shared across the four gathering pages, filled translated key) names only the three node-trade windfall flavors; farming's golden-harvest flavor is absent (the farming page's new tableBody covers it, so no falsehood renders); the reword needs the new-key treatment plus fills | P13 lane A | release fill / maintainer | open, deferred (reword landmine)'). It is the ONLY mention of the key under docs/. LIVE CODE, all re-read in this worktree: the English source is src/ui/i18n.catalog/guide.ts:3004-3005, whose sentence names the three flavors and carries {oneIn} and {mult}; it is rendered by rareSection() at src/guide/pages/professions_gathering.ts:177-189, which passes ONLY oneIn and mult and no flavor data; the page assembler runs rareSection() for every non-fishing gathering page at :361 ('${isFishing ? '' : rareSection() + corpseSection()}'), farming included, since isFarming is bound at :347 and gates only farmingSection() at :356, so the farming page really does render it. THE NUMBERS ARE TRUE FOR FARMING and only the enumeration is short: src/sim/professions/farming.ts:911 rolls the SAME event (rollGatherRareEvent(ctx.rng, 'crop')) and :1066 applies the same GATHER_RARE_EVENT_YIELD_MULT to count and fine, against src/sim/professions/gather_events.ts:31 (GATHER_RARE_EVENT_CHANCE = 1/90), :35 (GATHER_RARE_EVENT_YIELD_MULT = 5) and :39-49 (gatherRareEventFlavor, case 'crop' returns 'golden_harvest'); the signed claim holds too (farming.ts:1088-1089, 'A golden windfall lands SIGNED, the node rare-event idiom'), as does the deed claim (guide.ts:3029, gatherDeeds.farmingSown, 'A golden harvest records its own zero-Renown collector's mark'). THE NON-FALSEHOOD COVER the ledger cites is src/ui/i18n.catalog/guide.ts:3087-3088, farm.tableBodyOneMeal ('Every harvest you bring in rolls the same windfall chance the other gathering trades enjoy, and now and then a crop comes up golden ... Golden Harvest is recorded in your Book of Deeds'), rendered on the farming page alone by farmingSection() at professions_gathering.ts:333-343, wired at :356, two sections above the rare block. FILL STATE: the src/ui/i18n.status.json entry 'main:guide.profPages.rareBody' (enHash 55f88e031b9afbba, placeholders oneIn and mult) carries all 21 locales at state 'translated' with srcHash equal to enHash and ZERO pending; 18 overlay files under src/ui/i18n.locales/ carry a row for it. NO LITERAL PIN on the prose: tests/guide.test.ts:4139 asserts only that guide.profPages.rareHeading resolves non-empty, and grep over tests/ and scripts/ for rareBody returns nothing else. THE RETIRE-AND-REKEY MACHINERY is scripts/i18n_retired_keys.mjs, header lines 3-10 as read (RETIRED_KEYS is consumed by tests/guide_key_coverage.test.ts, scripts/i18n_scan.mjs and scripts/i18n_build.mjs, which must always agree), whose 'Reworded successors' block at lines 38-45 as read already carries this page's two precedents, 'guide.profPages.specimenBody' -> specimenBodyFamilies and 'guide.profPages.gatherDeeds.farming' -> farmingSown. THE GENERATED FLAVOR RECORD is three-membered: scripts/wiki/build_content.mjs:1135-1143 emits rareEvent.flavors as { ore, wood, herb } from gatherRareEventFlavor, typed at src/guide/content.generated.ts:265 and pinned by tests/guide.test.ts:3092-3096 (toEqual { ore: 'pristine_vein', wood: 'ancient_heartwood', herb: 'moonlit_bloom' }); no page consumes it (grep over src/guide/ returns only the generated type), so the prose enumeration is hand-authored, not generated. THE LANDMINE ITSELF is this table's own D126: the reword-staleness gate is unsatisfiable at scripts/i18n_scan.mjs's three emit sites, and the release-tier gate hard-fails only on 'pending', so an in-place reword of a fully translated key demotes nothing and reds nothing. R5: docs/design/power-verification.md contains no hit for GATHER_RARE_EVENT, 'windfall' or 'golden', and no option here moves a sim constant.
OPTIONS:
  1. [no-op] (A) Ratify the omission: the shared prose keeps its three node flavors and the farming page's golden story stays where farm.tableBodyOneMeal already tells it
     PRICE: One dated RULED qr-19 line on farming/state.md:524 plus this phase's ledger and progress.md rows. No catalog edit, no new key, no M16 fill, no pending row, no i18n regen, no wiki regen, no retirement entry, no pin moves, no reviewer beyond the phase's own fresh reader and qa-checklist. Genuinely no-op: nothing false renders today (verified above, the numbers and the signed, zone-announce and deed claims are all true of farming, only the flavor list is short), and the same page carries the golden windfall in full two sections up.
  2. [WORK] (B) Retire-and-rekey the shared key, the packet's own named shape: mint a successor naming the fourth flavor, point rareSection at it, retire rareBody
     PRICE: One new wordy English value in src/ui/i18n.catalog/guide.ts beside :3004 carrying the same {oneIn} and {mult} placeholders (suffix naming per the specimenBodyFamilies and harvestBodyFamilies precedents on this very page), one line re-pointed at src/guide/pages/professions_gathering.ts:181, and 'guide.profPages.rareBody' added to the 'Reworded successors' block of scripts/i18n_retired_keys.mjs (lines 38-45 as read) with its reason comment. Rides in the SAME change: the five M16 non-Latin fills (zh_CN, zh_TW, ja_JP, ko_KR, ru_RU), since the value is wordy; i18n regen via npm run i18n:gen, which rewrites src/ui/i18n.catalog/translation_keys.generated.ts, the resolved bundles and src/ui/i18n.status.json, flipping the 21 rareBody rows from 'translated' to 'blocked' with RETIRED_REASON rather than pending; the successor's 16 Latin rows land 'pending' for the Phase 20 fill. tests/guide_key_coverage.test.ts's 'has no live reference' arm then proves the retirement honest. NOT owed: no wiki regen (the page renders t() at request time and no generated field moves, so tests/guide.test.ts:393's freshness arm is untouched), no deed record, no Reliquary page, no committed WebP art, no census row (scripts/merge_audit/symbol_census.mjs), no parity golden, no tests/world_api_parity.test.ts member, no tests/monolith_budget.test.ts ceiling (guide.ts is a data catalog, exempt from module-first). Real cost beyond the fills: it strands 21 already-reviewed translations of the retired value, and because the key is SHARED the new sentence also lands on the mining, logging and herbalism pages, so the maintainer is making one copy call for four pages. Reviewers per the Review Dispatch Matrix: the frontend/i18n reviewer, plus the phase's fresh reader and the qa-checklist gate.
  3. [WORK] (C) Leave the shared key alone and add a farming-only note key rendered under the same heading
     PRICE: rareSection() takes no arguments today (professions_gathering.ts:177, called bare at :361), so it gains the trade id or an isFarming flag, one line at each site; one new farming-scoped wordy English key in src/ui/i18n.catalog/guide.ts (the farm.* block at :3084ff is its natural home) plus its five M16 non-Latin fills in the same change; i18n regen as in (B), with the 16 Latin rows landing pending for Phase 20. Cheaper than (B) in fill terms and strictly safer: nothing retires, no reviewed translation is stranded, the 21 rareBody rows stay accurate, and the other three gathering pages are untouched. Same not-owed list as (B): no wiki regen, no deed, no Reliquary page, no art, no census row, no parity golden, no ceiling. The honest counter-cost is redundancy: it says on the farming page, one heading away, what farm.tableBodyOneMeal (guide.ts:3087-3088) already says at length, which is exactly why the ledger row records that nothing false renders.
  4. [WORK] (D) Data-drive the flavor list: extend the generated rareEvent.flavors record to four members and compose the sentence from it
     PRICE: Everything in (B), since the sentence still changes, PLUS a crop member added at scripts/wiki/build_content.mjs:1135-1143, src/guide/content.generated.ts regenerated via npm run wiki:content (freshness-gated by tests/guide.test.ts:393), and the four-member move of the toEqual pin at tests/guide.test.ts:3092-3096. It buys the prose nothing: the record holds sim IDS ('pristine_vein', 'golden_harvest'), not display phrases, so a data-driven sentence needs four further display-name keys and their fills on top, which is why the enumeration was hand-authored in the first place. Highest cost of the four shapes for the smallest reader-visible difference.
  5. [WORK] (E) Reword guide.profPages.rareBody in place
     PRICE: One string edit at src/ui/i18n.catalog/guide.ts:3005 and nothing else on paper, and that is the trap. All 21 locale rows stay 'translated' with srcHash pinned to the superseded enHash while rendering superseded English, and nothing reds: this table's own D126 records that the reword gate is unsatisfiable at scripts/i18n_scan.mjs's three emit sites, and the release-tier gate hard-fails only on 'pending'. So it manufactures a fresh instance of exactly the invisible-staleness class D126 exists to name, on a 100 percent filled key, immediately before the Phase 20 fill, and it departs both from farming/state.md:524's own reading ('the reword needs the new-key treatment plus fills') and from the RETIRED_KEYS doctrine the catalog already documents twice on this page. Listed for completeness, not as a live candidate.
RECOMMENDATION: (B), the shape the packet's own record already names ('the reword needs the new-key treatment plus fills'), with (A) the honest fallback if the maintainer judges that farm.tableBodyOneMeal two sections up already tells the golden story well enough, and (E) refused outright as the landmine D126 describes; (C) is worth putting to the maintainer because it costs the same one key and five fills while stranding no reviewed translation and touching no other trade's page.
FLAGS: needsMaintainerValue=true VALUE_NEEDED: Only if (B), (C), (D) or (E) is chosen: the exact English phrase for the crop windfall, to sit beside the three hand-authored flavors ('a pristine vein in ore, ancient heartwood in timber, a moonlit bloom among the herbs'). The packet records propose no wording anywhere, and the sim holds only the id 'golden_harvest' (gather_events.ts:48), not player-facing trade dress; under the D138 precedent the replacement sentence is a maintainer-approved wording, and under (B) it is the copy for four pages at once.; r5Surface=false; contingentOn=none AMENDED IN PLACE 2026-09-02 at execution (D169), never rewritten: retiring a fully provided key flips ZERO registry rows (the specimenBody precedent), the successor lands 15 Latin rows pending (en_CA inherits), the five non-Latin rareBody fills were already SHORT of the English (no flavor names, no collector's-mark sentence), the handoff row is farming/state.md:587 and the farming.ts sites are :918, :1073-1074 and :1096 at a1b435ff85. Full list in phase-19f-english-i18n-completeness.md. ANSWERED 2026-09-02 (D169): the maintainer's value is 'a golden harvest from a garden bed', now the fourth flavor of guide.profPages.rareBodyFourFlavors under qr-19-rarebody-reword-landmine.

### D170  [col-junk-drawer-margin]
SEED: row 3 (one of the THIRTEEN standing items, named verbatim in the seed: "col_junk_drawer's three of margin", listed "to be priced, one row each"; D006 is that mandate and D009 is the distinct zone-1 sibling)
QUESTION: col_junk_drawer ("Discover 10 different poor-quality items", renown 5) now asks for ten of the thirteen poor items a character can actually acquire, three of margin, and its meter recounts LIVE quality so a holder of promoted trophies watches an in-progress counter regress. Ratify the amount at 10 and accept the margin, re-tune the amount downward (a rule-9 retro-edit only the maintainer may make), source the three routeless Brightwood Glade wildlife items so the pool rises to sixteen, or write the retroFallbackGrants heal the doctrine names?
ANCHORS: LEDGER: farming/state.md:624 (the handoff row, as read), "has THREE of margin since the 11l QA ... cut the reachable poor pool from 18 to 13 against an amount of 10 ... the candidates are lowering the amount through that heal or sourcing the wildlife pack", status "open ruling-owed (opened 2026-08-24)"; state.md:16341-16359 (the 11l derivation, as read, at the pre-QA count of one of margin); state.md:17291-17295 THE THIRD CENSUS (as read), "sixteen poor items live on the merged catalog, thirteen reachable, the collection still three of margin"; state.md:16374 (the rejection list, as read), "Editing col_junk_drawer's trigger, or sourcing the wildlife pack, inside this phase: both are the maintainer's"; state.md:17861-17864 (unchanged at the latest phase). LIVE CODE, re-read in this worktree: src/sim/content/deeds.ts:1295-1302 (desc 'Discover 10 different poor-quality items.', renown 5, trigger { kind: 'meter', meter: 'poorItemsDiscoveredCount', amount: 10 }); src/sim/deeds.ts:1018-1022 (the meter recounts m.deedStats.itemsDiscovered against the live ITEMS[id]?.quality === 'poor', the regression mechanism); src/sim/content/items.ts:2748-2769 (the '--- Brightwood Glade wildlife pack ---' banner: soft_down, amber_hide, stag_antler, kind junk, quality poor, no loot, vendor, quest or recipe route anywhere); src/sim/content/zone1.ts:178 (brightwood_glade is a POI of eastbrook_vale, zone 1) with the beast templates at :238 forest_wolf and :292 wild_boar; tests/deeds_content.test.ts:2190-2209 (reachable pinned at exactly thirteen), :2211-2216 (unreachable pinned to the wildlife pack), :2218-2241 (trigger.meter plus amount <= reachable.size), :963 FROZEN_CATALOG_SHA256 and :996-1036 the auditable pure-append re-mint proof; tests/deeds.test.ts:1385-1395 (the nine-short / tenth-earns arm); tests/material_taxonomy.test.ts:716-782 (SURVIVING_POOR_JUNK at sixteen plus the live-poor exact-set pin); docs/design/deeds.md:114-116 rule 9 ("Never retro-edit an existing trigger") and :92-98 rule 5 with retroFallbackGrants, implemented at src/sim/deeds.ts:1428-1460 (both stranded arms today key on a permanently-impossible predicate); public/ui/items/amber_hide.webp, soft_down.webp and stag_antler.webp already committed; the number is spelled in all 18 overlays under src/ui/deed_i18n.locales/ (de_DE.ts:551, fr_FR.ts:571, ko_KR.ts:546 as read); src/guide/content.generated.ts:4491-4498 (the generated deed row carries id, name, category, renown, crest, no desc).
OPTIONS:
  1. [no-op] Ratify the amount at 10 and accept the three of margin on the record
     PRICE: A dated RULED line only; no file moves, no regen. The standing guards do the work: tests/deeds_content.test.ts:2218-2241 (amount <= reachable) reds on the fourth promotion from here instead of stranding the deed, and FROZEN_CATALOG_SHA256 (:963) reds on any silent retune. What gets ratified with it: ten of thirteen obtainable poor items required, deepfen_pearl (a dungeon final boss) and soggy_boot (fishing only) among them, and a character holding promoted trophies still seeing an in-progress counter regress (earned deeds are never revoked, so no grant is lost).
  2. [WORK] Re-tune the trigger amount downward (a rule-9 retro-edit, the maintainer's alone)
     PRICE: src/sim/content/deeds.ts:1301 (amount) plus the English desc at :1298, whose number is spelled out, so all 18 overlays under src/ui/deed_i18n.locales/ take a desc re-fill in the same change and the non-Latin ones are M16 fills, not English-only. Re-mints FROZEN_CATALOG_SHA256 (tests/deeds_content.test.ts:963) and collides with the proof beside it: :996-1036 reconstructs the pre-append catalog from the LIVE triggers of non-appended rows, so a retro-edit cannot be re-minted as a pure append and the departure has to be recorded at the pin as a maintainer-authorized rule-9 exception. tests/deeds.test.ts:1385-1395 re-tunes its nine-short / tenth-earns arm. No new item id, no art, no deed record, no Reliquary page, no census row (no new symbol), no world_api parity or golden, no monolith ceiling move. Wiki regen is run-and-verify only (the generated deed row carries no desc, src/guide/content.generated.ts:4491-4498), with the tests/guide.test.ts freshness gate settling it. Reviewers: content-obligations-reviewer, sim-reviewer, plus the i18n arm of the QA gate. Needs a number no packet record holds.
  3. [WORK] Source the Brightwood Glade wildlife pack (amber_hide, soft_down, stag_antler) onto a zone-1 kill table, lifting reachable from 13 to 16 and the margin to six, trigger untouched
     PRICE: Loot rows in src/sim/content/zone1.ts on the beast templates (forest_wolf:238, wild_boar:292), or a new wildlife carrier if the antler and the down want a stag or a bird of their own; the drop chances are the design call inside it. The three ids, their names and their committed WebP art already ship (src/sim/content/items.ts:2748-2769; public/ui/items/*.webp), so no new item id, no art wave, no i18n fill, no census row, no deed or Reliquary obligation, and the trigger never moves, so neither catalog digest budges. Flips tests/deeds_content.test.ts:2190-2209 (reachable to sixteen) and :2211-2216 (unreachable to empty, or the arm retires). tests/material_taxonomy.test.ts:716-782 is a quality pin, not a route pin, and stays at sixteen. Wiki regen run-and-verify (the generated bundle carries no mob drop table today), tests/guide.test.ts freshness settles it. Reviewers: content-obligations-reviewer, sim-reviewer, test-coverage. INTERACTION: Brightwood Glade is a zone-1 POI (zone1.ts:178), so these same rows close D009's affordance gap outright.
  4. [WORK] Write the retroFallbackGrants stranded heal the doctrine names, keeping the amount at 10
     PRICE: A new arm in src/sim/deeds.ts:1428-1460 beside the two live stranded arms, plus its test in tests/deeds.test.ts and the source-scrape arm at tests/deeds_content.test.ts:1112. HONEST CAVEAT: docs/design/deeds.md:92-98 scopes the heal to a deed that has become permanently IMPOSSIBLE for that character, and at thirteen reachable against ten it is merely hard, so there is no honest predicate to key on today (a live-pool-below-amount check would grant on a catalog state that has not happened). This is the option to hold in reserve for the promotion that drops reachable under ten, not a heal that can be written now; choosing it means ruling that the doctrine stretches to a hard-but-possible deed. No i18n, no art, no wiki, no census, no catalog digest move.
RECOMMENDATION: Source the wildlife pack: the packet's own records name it as the candidate that needs no retro-edit at all (state.md:16374 as read), the three ids already ship with committed art and literally no acquisition route anywhere, and it is the only disposition that raises the margin without touching a shipped trigger, 18 locale overlays, or the append-only catalog proof, while closing D009's zone-1 gap with the same rows.
FLAGS: needsMaintainerValue=true VALUE_NEEDED: Only if the re-tune option is taken: the new trigger amount (an integer at or below the reachable thirteen) and its basis. No packet record holds one; the ledgers record only the shipped amount of 10 (src/sim/content/deeds.ts:1301) and the reachable count of 13 (state.md:17291 as read). If the wildlife-pack option is taken instead, the drop chances for the three items are a design call the maintainer may name or delegate.; r5Surface=false; contingentOn=D009 (zone1-no-poor-drop): its "author one new gray item onto the zone-1 and starter-island kill tables" option raises this row's reachable pool by one on its own, and this row's wildlife-pack option closes D009 outright, so D009 must be answered first or the two answered together

### D171  [scroll-elixir-15c-parity]
SEED: row 3 (the 'scroll/elixir 15c parity' member of the thirteen standing state.md items, phase-19-rulings-gate.md:194 as read; the umbrella is D006 at :41, whose own recommendation is 'Expand one-row-each', and no shipped D-row carries this decision, verified by a whole-table scan finding zero occurrences of 'sunpetal' or 'serpent')
QUESTION: The Phase 06 QA ruling priced recipe_sunpetal_scroll at EXACT input parity with recipe_elixir_of_the_serpent because the two grant a byte-identical buff, and Phase 11g's frost_gourd broke it by 15 copper with nothing in the tree reddening. Does the maintainer restore parity (and on which bands, since the rung-25 pair has drifted the same way by 16 copper unrecorded and the rung-0 gap grew from 6 to 10), ratify the split as the new doctrine, or retire the parity rule outright; and in every case, what pin and what player-copy correction ride the ruling?
ANCHORS: THE LEDGER: state.md:10256-10289 as read, 'THE SUNPETAL SCROLL / SERPENT ELIXIR PARITY IS NOW BROKEN, by 15 copper', including the refusal grounds at :10273-10279 ('Dropping the produce off the serpent would leave alchemy with no rung-50 produce consumer and break this phase's core deliverable. Adding produce to the scroll reaches into inscription, which R17 does not name and this phase's scope list excludes') and the pin instruction at :10286-10289 ('no pin was added ... Whoever picks an option should pin it in the same change'). THE RULING BEING DRIFTED: state.md:2166-2175 as read, Phase 06 QA 'RULINGS, all four taken by Fernando in-session ... (1) Scroll cost parity: RE-PRICE', with its scope bound at :2174-2175 ('The rung-0 divergence (26 vs 32) is deliberately outside the ruling's scope'). Still open at state.md:12288, :12829, :13221, :15598 and progress.md:38, all as read. Seeded row 3, phase-19-rulings-gate.md:194 as read (first member); umbrella D006 at :41 as read. LIVE CODE, re-derived in this worktree rather than taken from the ledger. The economy basis is tests/recipe_economy.test.ts:55-69 as read (reagentUnitValue = buyValue when the def carries a finite buyValue > 0, else sellValue; inputValue = sum of count x unit value). RUNG 50: src/sim/content/recipes.ts:2117-2140 recipe_sunpetal_scroll (bill at :2129-2134, sunpetal_herb 1 / arcane_essence 2 / glass_vial 1 / arcane_dust 1) prices at 160 + 36 + 12 + 6 = 214, against recipes.ts:1686-1708 recipe_elixir_of_the_serpent (bill at :1696-1702, frost_gourd count 1 at :1699 under its DECISION-B comment at :1691-1695) at 30 + 12 + 15 + 160 + 12 = 229; unit values from src/sim/content/items.ts:969-976 (sunpetal_herb buyValue 160), :2897-2903 (arcane_essence sellValue 18), :2890-2896 (arcane_dust 6), :1325-1331 (frost_gourd 15), and src/sim/content/profession_items.ts:154-161 (glass_vial sellValue 3, buyValue 12), :32-38 (venom_gland 6), :87-93 (pristine_venom_gland 30). Both resultCount 2, both outputs identical in payload (profession_items.ts:1153-1159 and :1395-1402 as read, aura 'Might of the Serpent', buff_sta 12 / 900, sellValue 20, quality rare; only `kind` differs), and that identity is machine-pinned toStrictEqual at tests/inscription_scroll_exclusivity.test.ts:44-70 as read. THE FINDING IS BIGGER THAN THE 15 COPPER ON RECORD, verified here: the shipped comment at recipes.ts:2126 still asserts 'The rung-25 pair already ships this parity (90 = 90)', and on live data it does not, because 11g put bog_beet 2 on recipe_venomfire_elixir (recipes.ts:1634-1650, bog_beet at :1644; bog_beet sellValue 8 at items.ts:1246-1252) taking that bill to 18 + 16 + 60 + 12 = 106 against recipe_goldleaf_scroll's 60 + 18 + 12 = 90 (recipes.ts:2082-2098, its own comment at :2087 still reading 'Input 90 vs output 15'), a 16-copper break nobody recorded; and the rung-0 divergence the Phase 06 ruling scoped out grew from 26 vs 32 to 26 vs 36, because 11g put vale_wheat 1 (sellValue 4, items.ts:1059-1065) on recipe_elixir_of_the_boar (recipes.ts:1578-1600, vale_wheat at :1593) against recipe_silverleaf_scroll's 26 (recipes.ts:2049-2063). THE RULING'S OTHER STATED GROUND IS ALSO FALSE NOW: recipes.ts:2125 says a cheaper scroll 'would compete with pristine_venom_gland's only crafting sink', and the gland has three more sinks today, one in each apex flask (recipes.ts:2927, :2946, :2965). NOTHING REDS ON ANY OF IT: tests/inscription_catalog.test.ts:87-120 as read pins the bills byte-for-byte (the recipe_sunpetal_scroll literal at :120) but its 22-line parity note at :98-119 is prose with no assertion behind it, and tests/recipe_economy.test.ts:142-151 as read only asserts output < input (40 < 229 and 40 < 214 both hold). PLAYER COPY CARRIES THE BROKEN CLAIM: src/ui/i18n.catalog/guide.ts:2874-2875 as read (guide.profPages.craftProse.inscription.materialsBody) tells players the double scroll batch is 'priced even with the Elixir of the Serpent whose buff it mirrors', which is false on live data and invisible to every gate, because tests/guide.test.ts:2500-2505 as read states in its own words that the farming dormancy arm is 'the ONLY guard over hand-authored guide.* prose (tests regenerate and diff content.generated.ts, which cannot see these strings at all)'; the generated wiki bill itself is fresh and correct at src/guide/content.generated.ts:12963-12990 as read, gated by tests/guide.test.ts:396-419. That key is filled in exactly the five non-Latin overlays (ja_JP.ts:12730, ko_KR.ts:12715, ru_RU.ts:12924, zh_CN.ts:12168, zh_TW.ts:12170, all as read) and pending in the fifteen Latin ones, matching state.md:2172-2174 as read.
OPTIONS:
  1. [WORK] Ratify the split: rule that the inscription route may sit under the alchemy one, keep both bills as shipped, and pin the current numbers so the NEXT drift reds
     PRICE: Not a no-op, and this row has none: the shipped record and the shipped player copy both assert the parity as present fact, so every option carries at least the corrections. A dated RULED line where the finding stands (state.md:10256-10289). Rewrite the standing note at tests/inscription_catalog.test.ts:98-119 from an open question into the ruling, and author the pin the ledger itself asks for (state.md:10286-10289): an arm computing both bills through the shipped reagentUnitValue rule and asserting 214 and 229, so a fourth produce insertion reds instead of drifting. It cannot simply live in inscription_catalog.test.ts, which is an inscription-only file (it already reaches across to ITEMS.elixir_of_the_serpent exactly once, at :406), so either that file's scope widens by one stated exception or the arm lands in tests/recipe_economy.test.ts beside the output-under-input sweep at :142-151. Correct the two false clauses the ruling now rests on in the same change, both in src/sim/content/recipes.ts: the rung-25 parity sentence at :2126 (live 90 vs 106) and the 'only crafting sink' clause at :2125 (three apex flask sinks at :2927, :2946, :2965). Reword the false player-facing claim at src/ui/i18n.catalog/guide.ts:2875, which makes the five non-Latin overlay fills stale (ja_JP.ts:12730, ko_KR.ts:12715, ru_RU.ts:12924, zh_CN.ts:12168, zh_TW.ts:12170); the fifteen Latin rows are already pending so NO new pending row is minted, a wordy replacement English value pulls its M16 non-Latin fills in-change, and otherwise the staleness folds into the Phase 20 fill as D126's class; `npm run i18n:gen` regenerates the resolved bundles. NO `npm run wiki:content` is owed (no bill moves, and src/guide/content.generated.ts:12963-12990 is already correct), so the tests/guide.test.ts:396-419 freshness gate stays green untouched. No id moves: no committed WebP, no ITEM_ART_PENDING or art-audit re-mint, no deed record, no Reliquary page, no shipped-id golden, no world_api parity golden or tests/world_api_parity.test.ts, no census row in scripts/merge_audit/symbol_census.mjs (no new exported name), no monolith ceiling in tests/monolith_budget.test.ts (data, test and catalog edits only). Reviewers: content-obligations-reviewer under the repo-wide content-diff rule plus qa-checklist (implementation-plan.md:154 as read); architecture-reviewer skipped, the matrix excludes pure data and test changes (implementation-plan.md:152 as read). The honest cost: it makes a 7 percent discount on a class-free alternative to the alchemist's own top elixir into shipped doctrine, on a route that needs no profession the buyer does not already have.
  2. [WORK] Restore the rung-50 parity on the scroll: add frost_gourd count 1 to recipe_sunpetal_scroll, taking it to 229 against the elixir's 229
     PRICE: Arithmetically the ONLY exact repair available: under the shipped unit-value rule frost_gourd is worth exactly 15, and inscription's own ink register cannot reach 15 in any combination (arcane_dust 6, glass_vial 12, arcane_essence 18), so an ink-only fix lands at 226 or 232 and leaves a 3-copper residual. One reagent line at src/sim/content/recipes.ts:2129-2134 plus the comment rewrite at :2122-2128; the bill literal at tests/inscription_catalog.test.ts:120 and its note at :98-119; plus the parity pin and the two false-clause corrections from option 1. THE REAL PRICE IS THAT IT PUTS THE FIRST CROP ON A NON-COOKING, NON-ALCHEMY ROW: the packet's accent rule (RULE 2) is scoped to those two crafts at tests/provisioning_supply_line.test.ts:456-464 as read, so the new crop would be UNGOVERNED by the accent sweep unless that filter widens. Re-derived here, the row clears both readings anyway (its largest non-produce count is arcane_essence at 2, so the count reference is 2 x 18 = 36 and the gourd contributes 15, well under the value reference of 160), so widening does not red, but leaving it unwidened silently exempts a shipped crop placement from the rule D004 is separately asking the maintainer to settle. TOUCHED_ROWS and its craft-through-sim arms (tests/provisioning_supply_line.test.ts:395-420 and :1157-1246 as read) would want the row, and the profession assertion at :1084 as read pins every TOUCHED_ROWS member to cooking or alchemy and moves if it joins; the alchemy produce-consumer floor of seven at tests/farm_seed_channels.test.ts:228-239 as read does NOT move. GAMEPLAY CONSEQUENCE the maintainer should price, not the session: the scroll's stated identity is the class-free second door into the band buff (src/ui/i18n.catalog/guide.ts:2729 and :2872 as read), and frost_gourd is tier-3 produce gating at farming 50 (recipes.ts:1691-1693 as read), so the repair puts a second profession between a scribe and their own rung-50 row. `npm run wiki:content` IS owed here (the bill is carried verbatim at src/guide/content.generated.ts:12963-12990), freshness-gated by tests/guide.test.ts:396-419; the guide.ts:2875 reword rides it with the same five non-Latin overlays going stale and the same M16 read. No new id, so no art, deed, Reliquary, census, golden, parity-pin or monolith obligations; same reviewer set as option 1. Does NOT touch the R5 record: section 12.2's cheap-recipe column names recipe_silverleaf_scroll, not this row.
  3. [WORK] Restore the rung-50 parity on the elixir instead: take frost_gourd back off recipe_elixir_of_the_serpent, returning both bills to 214
     PRICE: One line at src/sim/content/recipes.ts:1699, and it reopens DECISION B rather than a price. The serpent elixir IS the alchemy side's rung-50 produce consumer by that decision (recipes.ts:1691-1695 as read); the rung map pins it at 50 (tests/provisioning_supply_line.test.ts:1109 as read), TOUCHED_ROWS carries its produce tuple explicitly (:407-415 as read), the RULE 2 refused-under-count list names 'recipe_elixir_of_the_serpent/frost_gourd' as one of its six entries with a row-count checksum of 4 beside it (:802 and :812 as read), and the alchemy floor at tests/farm_seed_channels.test.ts:234-239 as read loses one of the seven rows it guards. 11g's own ledger already refused this on the merits (state.md:10274-10275 as read: it 'would leave alchemy with no rung-50 produce consumer and break this phase's core deliverable'). Same corrections, pin, reword and reviewer set as option 1, plus a wiki regen, plus a DECISION B amendment naming the replacement rung-50 alchemy produce row.
  4. [WORK] Widen the ruling to the whole ladder: price all three scroll/elixir bands to their twins, not just rung 50
     PRICE: Everything in option 2, plus the rung-25 repair (recipe_goldleaf_scroll at 90 against recipe_venomfire_elixir at 106, recipes.ts:2082-2098 and :1634-1650; a 16-copper gap with no 16-value reagent in inscription's register either, so it needs the same reagent ruling), plus the rung-0 repair (recipe_silverleaf_scroll at 26 against recipe_elixir_of_the_boar at 36, recipes.ts:2049-2063 and :1578-1600). TWO THINGS MAKE THIS AN ESCALATION RATHER THAN AN EXECUTION. First, editing recipe_silverleaf_scroll's bill MOVES AN INPUT TO THE FROZEN R5 RECORD: docs/design/power-verification.md:1089 as read is section 12.2's inscription row, whose cheap-path cell names 'recipe_silverleaf_scroll (0, 17c)', and the 17 is re-derived live on the sellValue basis that table uses (silverleaf_herb 4 x 2 + arcane_dust 6 + glass_vial 3 = 17; items.ts:1003-1009, :2890-2896 and profession_items.ts:154-161), so the literal and 12.2's ratio prose move with any rung-0 bill edit, and no test pins that cell, so it would rot silently rather than red. Second, the Phase 06 ruling put the rung-0 divergence explicitly out of scope (state.md:2174-2175 as read), so widening re-opens a scope bound the maintainer already set. Everything else as option 2, three times over, with one wiki regen covering all three bills.
  5. [WORK] Retire the parity rule: rule that a scroll bill and its band elixir's bill need not match, and pin the doctrine rather than the numbers
     PRICE: Same paperwork as option 1 (dated RULED line, the two false-clause corrections at recipes.ts:2125-2126, the guide.ts:2875 reword with its five stale non-Latin overlays, i18n:gen, no wiki regen, the same absent art/deed/Reliquary/census/golden/parity/monolith obligations, the same content-obligations-reviewer plus qa-checklist), but the pin differs in kind and that is the whole distinction from option 1: option 1 pins the two NUMBERS, so a fourth produce insertion reds and comes back to the maintainer; this option pins only the invariants that must survive (both outputs keep byte-identical payloads, already asserted at tests/inscription_scroll_exclusivity.test.ts:44-70 as read, and each bill keeps output under input, already asserted at tests/recipe_economy.test.ts:142-151), and lets the bills float apart on every future insertion without anyone being told. Cheapest to hold, and the only option that ends the recurrence by removing the rule rather than by defending it; the cost is that the Phase 06 ruling is retired as superseded rather than honoured, and no arm will ever again notice a route being undercut.
RECOMMENDATION: The packet's own records lean toward RESTORING the rung-50 parity on the scroll (option 2): the rule that drifted is a live in-session maintainer ruling (state.md:2166-2170 as read), 11g refused to repair it on SCOPE grounds and never on the merits (state.md:10273-10279 as read), and frost_gourd is the only reagent in the catalog whose unit value lands the repair exactly. THE ROW'S SECOND HALF IS OWED WHICHEVER WAY THE MAINTAINER RULES, and it is why this cannot be a paper decision: the ruling's two stated grounds are both false on live data today (the rung-25 parity claim at recipes.ts:2126, live 90 vs 106, and the 'only crafting sink' clause at :2125, three apex flask sinks), the shipped player copy at guide.ts:2875 tells players the two routes are priced even, and the pin the ledger asked for was never authored, so nothing in the tree reds on any of it. A second read worth stating: the rung-25 pair broke the same way in the same phase and no ledger recorded it, which is the strongest argument for pinning NUMBERS (option 1 or the pin half of option 2) over a bare repair, since a repair with no pin behind it will drift a third time. This is a recommendation, never a ruling.
FLAGS: needsMaintainerValue=true VALUE_NEEDED: If parity is restored: (a) the REAGENT that carries the repair, since frost_gourd at unit value 15 is the only exact-parity item in the catalog and admitting it puts the first crop on an inscription row and a farming-50 gate on a scribe's own rung, while the ink register (dust 6, vial 12, essence 18) cannot reach 15 and lands at 226 or 232; and (b) the BAND SCOPE, rung 50 alone, rung 50 plus the drifted rung-25 pair, or all three bands including the rung-0 pair the Phase 06 ruling put out of scope (the all-bands arm is the R5 escalation). If the split is ratified or the rule retired, no number is needed, but the maintainer must say whether the pin records the NUMBERS (so a future insertion reds) or only the doctrine (so bills may float). The packet records hold none of these.; r5Surface=true; contingentOn=none AMENDED IN PLACE 2026-09-02 at execution (D171), never rewritten: the premise 'the only exact-parity item in the catalog' is false on the live catalog (every tier-3 crop lands unit value 15, and so does chipped_tusk), corrected before the values were taken; the guide prose is at guide.ts:2896-2897 and the five overlay rows four lines lower than cited at 6377bed8b0. Full list in phase-19g-closeout.md. ANSWERED 2026-09-02 (D171) under qr-19-scroll-elixir-15c-parity (Phase 19G, under qr-19-best-for-project): the maintainer's values are (a) frost_gourd count 1 (it mirrors the elixir's own produce line, so the twin bills differ only in gland against ink), (b) rung 50 ALONE (the rung-25 and rung-0 gaps stay recorded, not repaired; the all-bands arm is the R5 escalation and was not taken), and (c) the pin records the NUMBERS and the equality: 'the rung-50 scroll and the serpent elixir bill at exact input parity (229 each, through the shipped unit-value rule)' in tests/recipe_economy.test.ts. The second half rode the same change: the two false comment clauses in src/sim/content/recipes.ts corrected (four pristine_venom_gland sinks; the rung-25 pair 90 against 106), the standing note in tests/inscription_catalog.test.ts rewritten into the ruling with its byte literal re-cut, the player copy RETIRED AND RE-KEYED (guide.profPages.craftProse.inscription.materialsBody to materialsBodyFrostGourd, the reword-is-a-new-key convention, with five fresh non-Latin fills flagged for the maintainer's read in phase-19g-qa.md and fifteen Latin rows pending for Phase 20), the accent sweep in tests/provisioning_supply_line.test.ts made craft-agnostic so the placement is governed, the affinity pin in tests/material_profession_affinity.test.ts moved to three crafts, and the wiki regenerated.

### D172  [stale-client-deploy-window]
SEED: row 3 (duplicates the seeded "the inherited stale-client window" member of the THIRTEEN standing state.md items, phase-19-rulings-gate.md:194 as read; D006 rules only that this member expands into its own row, never the member itself)
QUESTION: Is the inherited stale-client deploy window CLOSED by the packet's own ONLINE_WORLD_LAYOUT_VERSION bump (25 to 26), so the R34 apparatus retires (delete tests/stale_client_rollout.test.ts and its snapshot and rewrite DEPLOY.md's release-specific caveat), or does the window stay open, with the packet's seventeen drop-channel ids becoming a recorded owner admission in DEPLOY.md the way the four reins, the Wildheart six, heroic_duskwhisper and the Crucible appends already are? The premise the window rests on is falsified on this branch, verified live: an epoch-26 server refuses every pre-go-live bundle at the WS first frame, before it can be handed an id it cannot resolve, so the loot-popup throw is unreachable rather than merely rare.
ANCHORS: LEDGER (all as read). state.md:9600-9618, "### The one BLOCKING maintainer decision this phase opens / THE STALE-CLIENT DEPLOY WINDOW versus the farming DUNGEON channel": the deployed bundle predates the unknown-item guards and THROWS in its loot popup on an unresolvable id, DECISION E adds two ids to all five heroic five-man tables, "Both precedents (the four reins, the Wildheart six) were OWNER decisions recorded in DEPLOY.md. This session did not make one." Re-scoped and re-priced at state.md:9619-9632 ("THE EXPOSURE IS SEVEN IDS ACROSS TWO CHANNELS, not two across one"; "NOT AN 11f REGRESSION", Phase 11 already put ten pattern ids on the same raid base table) and state.md:9633-9642 ("'TWO CHEAP RESOLUTIONS' IS ONE": confirming the window has closed and deleting the guard is genuinely cheap; withdrawing the dungeon channel is a design change that reds the one-drop-pillar arm). Handed back unresolved at state.md:9709-9716 (11f QA), and each later phase records only that it ADDS nothing: state.md:10759-10762 (11g), :11499-11501 (11h), :14583-14585 and :14395-14396 (the Phase 15 audit, "closes as far as this phase is concerned", which is a phase scope and not a ruling). Still carried at state.md:17860-17868, the latest standing block. Seeded row 3 at phase-19-rulings-gate.md:194 as read. progress.md:36 and :38. Per-surface analysis: docs/design/professions-tuning-packet-review.md:56 and :178 ("R34. Stale clients get GUARDS, not a version floor."). Runbook: DEPLOY.md:402-437 (deploy order and the two mid-window mixes), :467-518 (the release-specific caveat and the loot-window paragraph naming the reins, the Wildheart six, heroic_duskwhisper and the Crucible appends), :519-526 ("measured against 9d7a1a021, the commit deployed today ... re-run the compatibility diff against the commit actually deployed"), :527-531 (the guard pointer, "delete that pin once clients have rolled"). LIVE CODE, read in this worktree at 6972704db9. The guard: tests/stale_client_rollout.test.ts:1-27 (banner), :42-56 (NEW_RELEASE_ITEM_IDS, the professions packet's THIRTEEN ids and nothing else), :74-95 (the existence arm and the mob/dungeon/heroic sweep, non-vacuity floors at :89-90), :97-160 (the frozen HEROIC_BOSS_LOOT snapshot arm), :126-150 (the still-unresolved MAINTAINER DECISION OWED and RE-SCOPED blocks), :151-159 (the four-reins literal plus the snapshot), with the golden at tests/__snapshots__/stale_client_rollout.test.ts.snap (62 lines). CORRECTION to the guard's own comment at :143 ("which this guard never sweeps"): the sweep DOES walk the raid table, because src/sim/data.ts:394 and :399 merge DUNGEON_MOBS into MOBS and the walk at :82 reads Object.values(MOBS); what it never does is CHECK those rows against anything but the thirteen professions ids, and the frozen snapshot covers only HEROIC_BOSS_LOOT, so the raid channel is walked and never frozen. THE EXPOSURE, counted rather than quoted: exactly SEVENTEEN packet-added drop-channel ids, derived by git diff origin/release/v0.42.0...HEAD over src/sim/content (fifteen single-line loot entries plus the two multi-line heroic rows, no others anywhere in content). Fifteen sit on the Nythraxis base table at src/sim/content/dungeons.ts:1103-1112 (ten, rollGroup 'nythraxis_patterns', Phase 11) and :1129-1133 (five, rollGroup 'nythraxis_farm', 11f: pattern_harvest_feast plus the four tier-4 seeds), each at chance 0.04. Two sit on the heroic channel: src/sim/content/heroic_loot.ts:683 (FARM_PATTERN_HEROIC_CHANCE 0.04), :693 (FARM_HEROIC_PATTERN_GROUP), :694-705 (heroicFarmPatternRows), spread at :718, :730, :742, :754 and :783 onto morthen (:708), vael_the_mistcaller (:720), ysolei (:732), korzul_the_gravewyrm (:744) and wildheart_high_priest (:759); the Nythraxis heroic table at :785 does NOT carry them. Both were confirmed absent at the deployed base (git grep -l over 9d7a1a021 -- src/sim returns nothing for pattern_highwatch_gourd_soup or pattern_duskforged_warblade). THE FACT THAT MOVES THE DECISION, and it is new since the item was written: this branch owns an epoch bump the 11f record could not have known about. src/world_api.ts:186-190 ("26 = Masterwrought and farming ship together ... An epoch-25 client can neither render Perfected copies nor a farm") sets ONLINE_WORLD_LAYOUT_VERSION = 26; origin/release/v0.42.0 and origin/release/v0.41.0 both read 25, and the deployed base 9d7a1a021 (2026-07-29) reads 3 at its src/world_api.ts:113, so the bump is branch-owned (git diff origin/release/v0.42.0...HEAD -- src/world_api.ts shows exactly 25 to 26). src/world_api.ts:191 derives ONLINE_WORLD_AUTH_TYPE = `auth-world-26`, and server/ws_auth.ts:266-277 refuses any first frame whose t is not that exact literal, answering an auth-world-N attempt with WS_AUTH_ERROR.incompatibleWorldLayout (server/ws_auth.ts:66, the literal at src/world_api.ts:194-196) before any credential, character or world work. The epoch is pinned in three places: tests/bank_wire_epoch.test.ts:102-103, tests/security.test.ts:113-114, tests/world_auth_scripts.test.ts:179-180. So every surface the window was ever about (the loot popup, the trade panel, bags and bank) is behind a handshake a pre-go-live bundle cannot pass. Current-tree guards, for contrast with the deployed bundle: src/ui/unknown_item_icon.ts:1-8 and :59-70 (the R34 fallback family), consumed by the loot popup at src/ui/hud/loot/loot_window_controller.ts:11 and :210; git grep -l unknownItemIconHtml at 9d7a1a021 -- src/ui returns NOTHING, confirming the deployed bundle predates the whole family. One non-WS surface checked here rather than assumed: the deployed bundle's market browse already drops unknown listings (9d7a1a021:src/ui/market_view.ts:147-148, "if (!item) continue"). Scale of what the guard's thirteen-id list does not cover: the packet mints roughly 177 new ids (added `id:` lines across items.ts, profession_items.ts, farm_crops.ts, apex_patterns.ts, farm_patterns.ts, delves/items.ts and rift/items.ts on the same diff). The channel registry that any withdrawal would red: tests/apex_pattern_channels.test.ts:86-102 (the sanctioned-host registry naming all three groups) with tests/farm_pattern_items.test.ts:157, tests/nythraxis_raid_unit.test.ts:30 and src/sim/content/heroic_vendor.ts:235-236 (the 12-mark quartermaster route for the same two patterns). Related but SEPARATE and unpriced anywhere in this table: farming/state.md:494 as read, "ONLINE_WORLD_LAYOUT_VERSION epoch bump at farming go-live ... open ruling-owed".
OPTIONS:
  1. [docs] (a) RATIFY the window as still open, and record the packet's admitted ids as an owner decision in DEPLOY.md the way the reins, the Wildheart six, heroic_duskwhisper and the Crucible appends already are
     PRICE: Docs only. One paragraph in DEPLOY.md's loot-window block (:467-518 as read) naming pattern_highwatch_gourd_soup and pattern_highwatch_barley_porridge at 0.04 each on the five heroic five-man finales, plus the fifteen ids on the Nythraxis base table across 'nythraxis_patterns' and 'nythraxis_farm'; tests/stale_client_rollout.test.ts:126-150 rewritten from "MAINTAINER DECISION OWED" into a dated RULED record (comment text only, since both ids are already in the frozen snapshot, so neither the reins literal at :153-158 nor the golden moves); one dated RULED qr-19-stale-client-deploy-window line at state.md:17860-17868 and at the 11f decision block state.md:9600-9642. No source file, no pin flip, no snapshot churn, no i18n, no regen, no wiki regen, no deed row, no Reliquary page, no item id and no committed WebP art, no census row (scripts/merge_audit/symbol_census.mjs untouched), no parity golden and no tests/world_api_parity.test.ts move, no tests/monolith_budget.test.ts ceiling, R5 untouched. Reviewer: privacy-security-review (DEPLOY.md is a deploy file, per the Review Dispatch Matrix at docs/prd/masterwrought/implementation-plan.md:147-148). What ratifying leaves STATED but unclosed, and it should be written down rather than implied: the guard's id list is still the professions packet's thirteen, so the fifteen raid-channel ids are walked and never frozen, and none of the packet's other minted ids are covered at all.
  2. [WORK] (b) RETIRE the R34 window as closed by the packet's own epoch bump: delete the guard and its golden, and rewrite DEPLOY.md's caveat to state the epoch gate instead of a loot-table exclusion
     PRICE: Delete tests/stale_client_rollout.test.ts (179 lines) and tests/__snapshots__/stale_client_rollout.test.ts.snap (62 lines). Rewrite DEPLOY.md:467-531 so the release-specific caveat says an epoch-26 server refuses every pre-go-live bundle at the WS first frame (server/ws_auth.ts:266-277) rather than promising a loot-table exclusion, and repair two sentences the same block now gets wrong: the guard pointer at :527-531 (the file is gone) and :428-432 ("The release left the fail-closed layout gate at ONLINE_WORLD_LAYOUT_VERSION 3 ... so stale bundles are still admitted at reconnect", which no longer describes the tree). Annotate docs/design/professions-tuning-packet-review.md:178 so R34 reads as superseded by circumstance rather than reversed: the guards still ship (src/ui/unknown_item_icon.ts), and a version floor arrived anyway for wire-shape reasons. One dated RULED line at state.md:17860-17868 and :9600-9642. Owed BEFORE the delete, and it is work a session can do rather than a maintainer question: confirm no surface in the DEPLOYED bundle renders an arbitrary item id outside the handshake (the market browse path is already clear at 9d7a1a021:src/ui/market_view.ts:147-148; the mailbox and vendor paths at that commit still owe the same read). scripts/ci_shard_weights.generated.json:8136 keeps a weight row for the deleted file; that is harmless (no suite reds on a stale row) [AMENDED IN PLACE 2026-09-01 at execution: FALSE. tests/ci_shard_partition.test.ts reds on exactly that, walking every MEASURED_WEIGHTS key and failing any that names a file absent from disk, and it pins __provenance.files to the row count beside it. The deletion therefore owed a prune, and because the table is generated and the full harvest needs a green FULL-MODE CI run no local-only wave can produce, D172 added a --prune-missing mode rather than hand-editing it.] and the table is regenerated by node scripts/ci_shard_weights_harvest.mjs <run-id> off a green full CI run, never hand-edited. No i18n, no regen, no wiki regen, no deed, no Reliquary, no art, no census row, no parity golden, no monolith ceiling, R5 untouched. Reviewers: privacy-security-review (DEPLOY.md), plus a fresh reader on the runbook rewrite. The standing risk this option accepts: it makes the deploy contract depend on the epoch bump actually shipping, so it must not land if the bump is later reverted to 25.
  3. [WORK] (c) WITHDRAW the farming DUNGEON channel behind the window (the 11f resolution as first offered)
     PRICE: Delete heroicFarmPatternRows (src/sim/content/heroic_loot.ts:694-705) and its five spreads (:718, :730, :742, :754, :783), then either re-home pattern_highwatch_gourd_soup and pattern_highwatch_barley_porridge onto another pillar or accept them with none, which reds the sanctioned-host registry and the no-fourth-channel sweeps at tests/apex_pattern_channels.test.ts:86-102 and the farm channel map at tests/farm_pattern_items.test.ts:157, plus the append-position pins in tests/dungeons.test.ts and tests/nythraxis_raid_unit.test.ts:30; the frozen golden re-records without the two ids. The quartermaster rows at src/sim/content/heroic_vendor.ts:235-236 stay, so the 12-mark route becomes the sole channel for two rung-75 patterns, which is a design change to a shipped acquisition story and not a window deferral, exactly as state.md:9633-9642 priced it. Rides with it: wiki regen (npm run wiki:content, freshness-gated by tests/guide.test.ts) because two recipes' acquisition prose moves, and the recipe comments at src/sim/content/recipes.ts:3886 and :4035 that name the channel. Reviewers: architecture-reviewer, content-obligations-reviewer, a fresh reader. No i18n, no deed, no Reliquary, no new item id, no art, no census row, no parity golden, R5 untouched. It closes 2 of the 17 ids and leaves the other 15 exactly where they are, so on its own it does NOT close the window.
  4. [WORK] (d) KEEP the guard and WIDEN it: re-scope its id list from the professions thirteen to the packet's own minted ids, and freeze the Nythraxis base table beside HEROIC_BOSS_LOOT
     PRICE: Rewrite tests/stale_client_rollout.test.ts:42-56 to the packet's own minted set (about 177 ids) and add a second frozen golden for the raid table, alongside the existing HEROIC_BOSS_LOOT one. It reds on the first run against all seventeen admitted ids, so each still needs the explicit admission option (a) writes, which makes this option (a) plus a second golden to carry. No shipped behavior changes: no source file outside tests/, no i18n, no regen, no wiki, no deed, no Reliquary, no art, no census row, no parity golden, no monolith ceiling, R5 untouched. Reviewer: a fresh reader, plus privacy-security-review if the DEPLOY.md paragraph is rewritten with it. What it buys: a real gate against a FUTURE id entering a drop channel while the window is open. What it costs: a second golden maintained for a window the epoch bump has already closed, and the pin-trap the file already warns about at :120-124 (the snapshot is `vitest -u`-updatable, so the literal array is the only real teeth).
RECOMMENDATION: Option (b), retire, is what the packet's own records lean toward, offered as a recommendation and not as a ruling: the 11f QA itself named "confirming the window has closed and deleting the guard file" as the one genuinely cheap resolution (state.md:9633-9635 as read), and the condition it was waiting on is now satisfied in the tree rather than argued, since ONLINE_WORLD_LAYOUT_VERSION reads 26 on this branch against 25 on both release branches and 3 at the measured deployed base, and server/ws_auth.ts:266-277 refuses a mismatched first frame before any world work, which makes every surface the window covers unreachable rather than rare. If the maintainer would rather see go-live actually observed before deleting a guard, (a) is the honest fallback and costs one DEPLOY.md paragraph plus a comment rewrite, with the caveat that ratifying leaves the guard checking the professions packet's thirteen ids against a packet that mints about 177. (c) is a design change that closes 2 of 17 and should be refused on that ground alone. Either way two stale sentences in the runbook are worth repairing in the same change: DEPLOY.md:428-432 still says the layout gate is at 3 and stale bundles are admitted at reconnect, and DEPLOY.md:519-526's own instruction to re-measure against the commit actually deployed should be honoured at go-live.
FLAGS: needsMaintainerValue=false; r5Surface=false; contingentOn=The farming epoch-bump-at-go-live ruling (farming/state.md:494 as read, status "open ruling-owed"), which has no D-row in this table. AMENDED IN PLACE 2026-09-01 at execution (D172), never rewritten: this clause is WRONG in both halves. The completeness pass DID add a D-row for it, D162 [epoch-bump-at-golive], and that row is already CLOSED 2026-09-01 by ruling qr-19-epoch-bump-at-golive, executed in Phase 19C. The handoff row it covers has also moved: it reads at farming/state.md:555 today, not :494. So the contingency is DISCHARGED rather than outstanding, and the instruction to ask that ruling first was satisfied one wave earlier. The substance of the caution stands: option (b)'s entire ground is the bump shipping at go-live, and a revert to 25 reopens the window exactly as 11f described it. Ask that one first: option (b)'s entire ground is the bump shipping at go-live, and if it is ever reverted to 25 the window reopens exactly as 11f described it.

### D173  [session-cap-15-to-16]
SEED: row 3
QUESTION: Ratify or revert 11i's forced move of the fishing session cap, FISHING_SESSION_CAP_SEC 15 s to 16 s, which shipped to close a one-tick overrun the tier-6 epic rod opened (bare-pole worst bite 160 ticks + the tier-6 epic window 140 + the miss tick = 301, against the old 300-tick cap). If the maintainer neither ratifies 16 nor accepts one of the two ladder trims a revert forces, they must name the cap in seconds.
ANCHORS: LEDGER: state.md:13659-13681 as read, the heading 'THE FORCED DEVIATION: THE SESSION CAP MOVED, 15 s TO 16 s' and its arithmetic ('tier-6 epic window = 2.5 + 0.75 x 5 + 0.25 x 3 = 7.00 s = 140 ticks; 160 (bite) + 140 (window) + 1 (miss) = 301 ticks, against a 300-tick cap ... RATIFY OR REVERT'), handed back as item (8) at state.md:13687-13688 as read. Carried unchanged in every later roll-up: state.md:12289, :12830 and :13222-13223 as read, the last independently re-deriving the forcing arithmetic ('160 + 140 + 1 = 301 against 300'), and still open in the latest standing list at state.md:17860 as read. progress.md:41 as read records it as the phase's 'ONE DEVIATION handed back as ratify-or-revert'. LIVE CODE, re-read in this worktree: src/sim/types.ts:150 `export const FISHING_SESSION_CAP_SEC = 16` (DT = 1/20 at src/sim/types.ts:19, so the shipped cap is 320 ticks); the whole derivation lives beside the rarity term at src/sim/professions/fishing.ts:130-177 ('THE CAP MOVED AT masterwrought Phase 11i, 15 s to 16 s, and it was FORCED rather than chosen ... WHY THE CAP AND NOT THE ROD'), with FISH_REEL_WINDOW_SEC 2.5 and FISH_REEL_WINDOW_ROD_BONUS_SEC 0.75 at :109-110, FISH_REEL_WINDOW_RARITY_BONUS_SEC 0.25 at :177 and the one window formula fishReelWindowSecFor at :206-217; rarityLadderIndex at src/sim/professions/tools.ts:806-809 (epic is rung 3, rare rung 2). The cap is assigned at src/sim/professions/fishing.ts:579-580 and broadcast as the castStart `time` at :594; the reel deadline is armed from the rod held at the BITE at src/sim/combat/casting_lifecycle.ts:511, and the cap's own defensive-timeout comment ('unreachable in real flow') is at :598-604. The rod that forced it: src/sim/content/items.ts:889-900 (the EPIC-NOT-LEGENDARY rationale, which names FISHING_SESSION_CAP_SEC) and the record itself at :905-912 (quality 'epic' at :909, gatherTool tier 6). PINS THAT HOLD THE NUMBER: tests/fishing_zones.test.ts:995-1052 re-derives the budget through the sim's own function and pins the cap side to a literal at :1003 (`expect(capTicks, 'the session cap is 320 ticks').toBe(320)`), with the live-loop arm at :1054-1105; tests/guide.test.ts:2991 `expect(f.sessionCapSec).toBe(16)`; the published value at src/guide/content.generated.ts:15921 ('sessionCapSec': 16), emitted by scripts/wiki/build_content.mjs:1086 and held fresh by the generator freshness gate at tests/guide.test.ts:396-420. PLAYER SURFACE: the wiki sentence 'A whole session caps at {cap} seconds' interpolates the number (src/ui/i18n.catalog/guide.ts:3037 as read; src/guide/pages/professions_gathering.ts:306 passes formatNumber(f.sessionCapSec)), and the placeholder survives in the overlays (verified in src/ui/i18n.locales/ja_JP.ts and zh_CN.ts), so the value is not spelled in any locale string. NOT AN R5 INPUT: docs/design/power-verification.md has no fishing, angler or reel surface at all (grep clean over the whole file).
OPTIONS:
  1. [no-op] (A) Ratify 16 s as shipped
     PRICE: Genuinely no-op: a dated RULED qr-19 line where the 11i hand-back stands (state.md:13659-13688) and in the standing-items list at :17860. The tree already ships 16 and every pin above is green today, so no source edit, no wiki regen, no non-Latin fill, no deed, Reliquary page, item art or census row, no parity golden or tests/world_api_parity.test.ts member, no monolith ceiling (none of the touched files is pinned in tests/monolith_budget.test.ts). Reviewers: none beyond the record line, with qa-checklist at the phase's own closing step.
  2. [WORK] (B) Revert to 15 s and close the one tick by shipping the clockreel RARE instead
     PRICE: WORK, and it is a live content change rather than a one-number revert. src/sim/types.ts:150 back to 15 AND src/sim/content/items.ts:909 quality 'epic' -> 'rare', which re-prices the apex window to 2.5 + 0.75 x 5 + 0.25 x 2 = 6.75 s = 135 ticks and the worst legal session to 160 + 135 + 1 = 296 inside the 300-tick cap (derived here from fishReelWindowSecFor and rarityLadderIndex, not quoted). Pins that flip: tests/fishing_zones.test.ts:1003 (the 320 literal and its message string), the widest-window arm at :1077-1090 whose comment names the epic reading ('125, not 110: 110 is the tier-5 window at COMMON, and the best shipped rod is epic'), and the rarity-ladder arms at :912-958; tests/guide.test.ts:2991 (16 -> 15); then npm run wiki:content to regenerate src/guide/content.generated.ts:15921 under the tests/guide.test.ts:396-420 freshness gate. The rarity is player-visible in the rod tooltip's rarity-bonus line (src/ui/gather_tool_tooltip.ts:116) and the rod is shipped conquerable content with a Reliquary page (src/sim/content/reliquary.ts:1399) and a deed trigger (src/sim/content/deeds.ts:2991), so the content sweep rides: wiki regen, the reliquary and deeds pins re-run (tests/reliquary_content.test.ts, tests/deeds_content.test.ts), and the apex base-tool family arm at tests/professions_tools.test.ts:738-760. No id moves, so tests/shipped_item_ids.golden.json and the symbol census are untouched; no new WebP (rarity mints no art); no M16 fill (the cap sentence interpolates {cap} and no English string states the rod's rarity). The 11i ledger already refused this arm on design grounds: a RARE tier-6 rod above an EPIC tier-5 one is a rarity inversion every player sees in their bags. Reviewers: content-obligations-reviewer and a sim reviewer on the re-derived budget, then qa-checklist.
  3. [WORK] (C) Revert to 15 s and trim the ladder instead (FISH_REEL_WINDOW_ROD_BONUS_SEC or the rarity term)
     PRICE: WORK, the widest blast radius: one constant at src/sim/professions/fishing.ts:110 (or the rarity term at :177) narrows the reel window of ALL SIX shipped rods to make room for one new one, which is a live gameplay retune of shipped tools rather than a budget tidy. The English wiki prose spells the derived per-rod figures as literals ('the Ironreel pulls the worst wait down to 6.5 seconds with a 3.25 second window, and the Silverstream to 5 with a window past 4', src/ui/i18n.catalog/guide.ts:3037 as read) and tests/guide.test.ts:3005-3007 re-derives exactly those from the published constants, so an English reword is owed in the same change; a reworded wordy English value is the M16 surface, so the non-Latin fills ride with it, and it lands straight on the packet's standing reword-staleness problem (D126, and the five stale guide.profPages.toolsNoteThreeRods fills already recorded). Plus wiki regen under the freshness gate, the tests/fishing_zones.test.ts budget and rarity-ladder arms (:912-958, :995-1052, :1077-1090), the rod tooltip line (src/ui/gather_tool_tooltip.ts:116) and tests/guide.test.ts:2991. Reviewers: content-obligations-reviewer, a sim reviewer, then qa-checklist.
  4. [WORK] (D) Name a third cap value
     PRICE: WORK but small and self-contained: the literal at src/sim/types.ts:150, the 320 pin and its message at tests/fishing_zones.test.ts:1003, the 16 pin at tests/guide.test.ts:2991, and npm run wiki:content to regenerate src/guide/content.generated.ts:15921 under the tests/guide.test.ts:396-420 freshness gate. No content record moves, no deed or Reliquary page, no item art, no census row, no parity golden, no world_api member, no server or headless surface (nothing outside src/sim, src/guide and scripts/wiki reads the constant), no monolith ceiling, and no non-Latin fill (the {cap} placeholder carries the number into every overlay). The value is the maintainer's and it is BOUNDED: the budget asserts strictly greater than, so any cap below 15.10 s (302 ticks) truncates the shipped tier-6 epic reel window, which is the exact defect the move was made to prevent; and since the wiki tells the player the number, a round figure is worth keeping. Reviewers: a sim reviewer on the budget, then qa-checklist.
RECOMMENDATION: Ratify 16 s (option A): the packet's own records show the move was FORCED rather than chosen, the cap is a defensive timeout the shipped code itself calls unreachable in normal play (src/sim/combat/casting_lifecycle.ts:598-604), so a second on it costs a player nothing, while both reverts buy either a rarity inversion on the apex rod or a retune of five already-shipped rods to fit one new one, for no gameplay difference.
FLAGS: needsMaintainerValue=true VALUE_NEEDED: The cap in seconds, if the maintainer takes option D. The packet holds exactly two candidates, 15 (pre-11i) and 16 (shipped), and no third; any named value must exceed 15.10 s (302 ticks) or the shipped tier-6 epic reel window is truncated by the budget. Options A, B and C need no new number.; r5Surface=false; contingentOn=none

### D174  [prog-first-harvest-thirteen-catches]
SEED: row 3 (expands the seeded 'prog_first_harvest's 13 catches' standing item, which D006 rules must expand one row each; the farming four-crop half is D103's, not this row's)
QUESTION: prog_first_harvest ("Harvest your first gathering node", trigger gathering amount 1, renown 5) now takes THIRTEEN landed catches for a fishing-only character, because the amount-1 trigger reads gathering PROFICIENCY and DECISION F set fishing's band-0 gain to 0.08. Ratify the 13-catch timing as shipped, ratify it but reword the deed's English desc so it stops promising the first node, or retune the fishing band-0 gain so the deed fires sooner?
ANCHORS: LEDGER. state.md:14195-14204 as read, the 11i review's hand-back: "ONE MORE FOR THE MAINTAINER, raised by the review rather than by the phase... now takes THIRTEEN catches for a fishing-only character instead of one... it is a live progression change riding a values-only retune. Farming already pushed the same deed to its fourth crop in an earlier phase. The deed's own desc drifts further from what it does either way. RATIFY OR RETUNE." Mechanism and parity consequence at state.md:13605-13614 as read ("at 0.08 a catch the amount-1 gathering trigger now needs 13 catches, so prog_first_harvest no longer fires inside the scenario's 99-tick window and its deedUnlocked emit and its 5 renown go with it"), with the lesson recorded there ("a gain-rate change is a DEED-TRIGGER change wherever a deed counts the quantity that rate feeds"). Carried unclosed in every later roll-up: state.md:12290, :12831, :13225, :15598-15601 and :17856-17868 as read. DECISION F itself is recorded executed at state.md:13366-13430 as read. LIVE CODE, all re-read in this worktree. The record: src/sim/content/deeds.ts:176-183 (desc 'Harvest your first gathering node.', category progression, renown 5, trigger { kind: 'gathering', amount: 1 }). The predicate that makes `amount` a PROFICIENCY and not a node count: src/sim/deeds.ts:1119-1126 (meta.gatheringProficiency[professionId] >= trigger.amount, plus the count-form arm over GATHERING_PROFESSION_IDS). The band-0 gain: src/sim/professions/fishing.ts:282-287, { belowProficiency: 50, gain: 0.08 } at :283, so ceil(1 / 0.08) = 13. The land curve is UNTOUCHED and still fires the deed on node one: src/sim/professions/gathering.ts:317-327 (GATHER_GAIN_TIER_STEP = 25, gatherNodeGainMultiplier) with src/sim/professions/wheel.ts:136-142 (tierProgressMultiplier returns 1 when tiersBelow <= 0), so a proficiency-0 miner, logger or herbalist gains a whole point on their first tier-1 node. The farming half, which is D103's and not this row's: src/sim/professions/farming.ts:323-329 (band-0 gain 0.25, four crops). THE INSTANT DEED THAT ALREADY EXISTS ON THE FISHING SIDE: chr_vale_first_cast, renown 5, src/sim/content/deeds.ts:736-743, marked at src/sim/deeds.ts:2086 (markVisited `fish:${zoneId}` on any ZONE_FISH catch), pinned firing on the FIRST landed fish at tests/professions_fishing.test.ts:1868-1880. Rule 9 (docs/design/deeds.md:114-116) freezes the TRIGGER, not the desc, and the frozen-catalog digest is over (id, trigger, renown) only, tests/deeds_content.test.ts:796-820, whose :818-820 comment records the precedent verbatim ('prog_master_gatherer had only its English desc reworded, which this digest deliberately does not cover'). The drift is already pinned in BOTH directions at tests/professions_fishing.test.ts:1882-1924 ('ACCEPTED DRIFT, NARROWED BY DECISION F'): not earned at zero catches (:1898), not at one (:1908-1909), `expect(needed).toBe(13)` (:1913-1914), earned at thirteen (:1922-1923). The parity golden already encodes the non-firing window: tests/parity/golden/professions_fishing_session.json has ZERO hits for prog_first_harvest and zero for deedUnlocked (grepped in this worktree). Wiki surfaces: src/guide/content.generated.ts:3657-3662 (the deed's generated row and crest path), src/ui/i18n.catalog/guide.ts:3011/3013/3015 ('Your first node of any trade earns Fruits of the Field', already filled in the locale overlays, zh_TW.ts:12031-12035 as read), tests/guide.test.ts:3008-3010 (the wiki is held to the live FISHING_GAIN_SCHEDULE values). Localized descs: all 18 files under src/ui/deed_i18n.locales/ carry a prog_first_harvest row (de_DE.ts:175 as read, desc 'Ernte dein erstes Sammelvorkommen.'). Art already committed: public/ui/deeds/prog_first_harvest.webp. NOT AN R5 SURFACE: docs/design/power-verification.md returns zero matches for fishing, gathering, proficiency or deed (whole-file grep in this worktree).
OPTIONS:
  1. [no-op] Ratify the 13-catch timing as shipped
     PRICE: Genuinely no-op: a dated RULED qr-19 line where the item stands, at the hand-back block (state.md:14195-14204 as read) and the standing roll-ups (state.md:15598-15601 and :17856-17868 as read). Nothing in the tree moves, because the shipped behaviour is already pinned rather than described: tests/professions_fishing.test.ts:1882-1924 asserts both halves (one catch not enough at :1908-1909, thirteen enough at :1913-1923), tests/parity/golden/professions_fishing_session.json already carries the non-firing 99-tick window, and tests/guide.test.ts:3008-3010 already holds the wiki to the live values. No wiki regen, no M16 non-Latin fill, no deed record, no Reliquary page, no committed WebP (the crest exists), no symbol_census row, no world_api parity member and no monolith ceiling. Reviewer: qa-checklist only. Stated rather than smoothed over, because ratifying leaves it standing: the English desc at src/sim/content/deeds.ts:179 stays false for fishing (13) and farming (4), and so does the guide prose at src/ui/i18n.catalog/guide.ts:3011/3013/3015.
  2. [WORK] Ratify the timing, but reword the deed's English desc (and the matching guide prose) so it stops promising the first node
     PRICE: WORK, small at the source and wide in the fills. One string at src/sim/content/deeds.ts:179. Rule 9 permits it (docs/design/deeds.md:114-116 freezes the trigger, not the desc) and FROZEN_CATALOG_SHA256 does NOT re-baseline, since the digest is over (id, trigger, renown) and tests/deeds_content.test.ts:818-820 records the exact precedent (prog_master_gatherer's English desc was reworded and the digest deliberately does not cover it). The real bill is non-English: all 18 files under src/ui/deed_i18n.locales/ carry a translated desc (de_DE.ts:175 as read) and D126's finding is that NOTHING reds them (the reword gate is unsatisfiable at its three write sites in scripts/i18n_scan.mjs), so either the 18 fills ride this change by hand or 18 locales ship a translation of superseded English. Same claim in a second place: src/ui/i18n.catalog/guide.ts:3011/3013/3015, already filled across the locale overlays (zh_TW.ts:12031-12035 as read), so an honest pass touches those rows too. npm run wiki:content runs and its freshness gate (tests/guide.test.ts) must pass; the deed's generated row is src/guide/content.generated.ts:3657-3662. M16 applies only if the replacement English is wordy. The new desc must clear tests/deeds_content.test.ts:1275-1282 (non-empty English, no em/en dash, no emoji). No new id is coined, so no census row, no Reliquary page, no new committed WebP, no parity golden move, no monolith ceiling. Reviewers: content-obligations-reviewer, qa-checklist.
  3. [WORK] Retune FISHING_GAIN_SCHEDULE band 0 so the deed fires sooner
     PRICE: WORK, the heaviest arm by a distance and for the least gain. src/sim/professions/fishing.ts:283 is the only lever, and firing on catch one needs band-0 gain at or above 1, a 12.5x move that collapses DECISION F's settled casts-to-200 span (state.md:13366-13430 as read; band 0 is 1.55 of the 10.94 modelled hours). In one test file it reds the literal pin at tests/professions_fishing.test.ts:694-700, the span-and-one-third-cap arms at :1146-1152, the derived-winner equality at :1203 (the search admits 16 legal 0.01-grid schedules and pins the unique winner, so a hand-picked value is not a re-derivation, it is a refusal of the search), and the per-band-hours ledger pin at :1206-1215; the boundaries at :1216-1222 must NOT move, since fishingTeachingCeilingFor re-gates which water teaches. It also re-mints tests/parity/golden/professions_fishing_session.json (the deed re-enters the 99-tick window, reversing the move recorded at state.md:13605-13614 as read) and needs npm run wiki:content plus the guide freshness gate, because tests/guide.test.ts:3008-3010 holds the wiki to the live schedule values. No deed record, no Reliquary page, no committed WebP, no census row, no M16 fill, no monolith ceiling. Reviewers: architecture-reviewer (a src/sim constant moves), content-obligations-reviewer, test-coverage-auditor, qa-checklist. It re-opens an executed decision to change when one renown-5, no-title deed fires.
RECOMMENDATION: Ratify the 13-catch timing as shipped, with the desc reword available as the cheap rider if the maintainer wants the copy to stop being false: fishing already has its own instant renown-5 deed on the very first catch (chr_vale_first_cast, src/sim/content/deeds.ts:736-743, pinned at tests/professions_fishing.test.ts:1868-1880), which is the same ground D103 recommends the farming half on and is stronger here, so no bootstrap-deed option is priced and the only live gap is which deed fires, not whether one does.
FLAGS: needsMaintainerValue=true VALUE_NEEDED: Only if the desc-reword arm is taken: (a) the replacement English desc for prog_first_harvest, which the packet records do not hold in any candidate form and which has to be true for all four gathering trades at once (one land node for mining, logging and herbalism; four crops farming; thirteen catches fishing); and (b) whether the 18 src/ui/deed_i18n.locales/ fills and the src/ui/i18n.catalog/guide.ts:3011/3013/3015 overlay rows ride the same change or defer to the Phase 20 fill, since nothing reds them either way. The ratify arm needs no value.; r5Surface=false; contingentOn=D103 (the same deed's farming half; ask them in the same sitting so one deed does not end up carrying two different answers). Not blocking: either can be ruled alone. AMENDED IN PLACE 2026-09-02 at execution (D174), never rewritten: D103 is already RULED (qr-19-prog-first-harvest-bootstrap-deed, 2026-09-01, accepted as shipped, reading the desc as truthful because a farm bed is not a GATHER_NODES node), so the sitting cannot be shared and a reword must not contradict it; M16 owes nothing here (the guide rows are already translated, the deed desc is outside it); the state.md addresses have drifted (the hand-back heading at 14746, the post-mortem at 14111-14122, the roll-ups at 12732, 13312, 13731, 16226 and 18867-18878, DECISION F at 13872). Full list in phase-19f-english-i18n-completeness.md.

### D175  [shipped-id-golden-remint-cadence]
SEED: row 3 (the thirteen standing items), member 8, "the shipped-id golden policy"; state.md open item (10), never expanded by the sweep
QUESTION: Which cadence governs the shipped-id golden: release-time re-mint (what tests/shipped_item_ids.test.ts:14-16 and src/sim/content/CLAUDE.md:345-347 say, and what five phase ledgers deviated to) or per-content-change re-mint (what the phase acceptance checklists demand and what the branch's own phase 02 to 09 commits actually did)? And since the branch golden ALREADY pins all 172 of the packet's branch-only item ids, does the maintainer ratify the tree as it stands, making those ids permanent API before the branch merges, or prune the golden back to shipped-only and re-mint at the release?
ANCHORS: LIVE CODE, verified in this worktree. tests/shipped_item_ids.test.ts:1-17 carries the doctrine: shipped ids are permanent API because saves persist raw item ids and load them with no validation (the v0.25.0 incident that broke 18 prod characters is the header's own reason), the golden is APPEND-ONLY, and "After new items ship in a release, re-mint with UPDATE_SHIPPED_ITEMS=1" at :14-16. The assertion itself is at :37-38: a sanity floor expect(shipped.length).toBeGreaterThan(500) and then const missing = shipped.filter((id) => !ITEMS[id]) - a SUBSET filter, which is exactly why an un-re-minted golden never reds and the cadence has no enforcement behind it. MEASURED NOW against the live catalog (esbuild bundle of src/sim/data.ts, run in this worktree): tests/shipped_item_ids.golden.json holds 1220 ids, sorted, zero duplicates; ITEMS holds 1221; exactly ONE id is unpinned, reins_rickshaw_mount, the dev-grant-only mount at src/sim/content/items.ts:417-429 ("no acquisition path, so it stays soulbound", soulbound + noDiscard + sellValue 0). Its two siblings ARE pinned (reins_terrorspark_groundshaker, reins_drakemaw_raptor), and all three are the SOURCE_PENDING_RULING mounts recorded at src/sim/content/reliquary.ts:285-291, so the one live gap is a stale-file artifact of when the last re-mint ran, not a deliberate exclusion. THE PREMISE THE FIVE LEDGERS REST ON IS FALSE ON THE TREE: 418 of the branch golden's ids are absent from origin/release/v0.42.0's own golden (that ref resolves to 22e909839f here, golden length 802), and 172 branch-only ITEMS ids - absent from BOTH that ref's src/sim/content id literals and its golden - are already pinned, all 172 of them, zero unpinned. So "a re-mint here would add 103 ids of which only ONE is this phase's, pinning 102 unshipped branch-only ids as permanent API on a branch that has not merged" (state.md:12271-12277 as read, repeated at 12813-12817 and 15587-15588 as read) describes a line this branch crossed long ago. Git confirms the branch did it, not a merge: masterwrought content commits 25d7154b09 (phase 09 apex gear, +10 golden lines), c10068c79e (tailoring apex bag, +10), 7d0bf1d400 (tool-effect charms, +112) and their siblings re-minted, and the golden was already 1220 at c659387f9a^1, i.e. before the tenth sync. The standing item is state.md:12294-12295 as read, open item (10), "per-phase re-mint or release-time re-mint. Five phases have now assumed release-time"; the acceptance line the deviating phases dropped is phase-11j-gathering-completion.md:508-509 as read. The doctrine is mirrored at src/sim/content/CLAUDE.md:345-347, sitting immediately after the sentence this decision collides with at :343-344 ("Only an item that never left your unmerged feature branch may be deleted outright"), and again at docs/design/professions-tuning-packet.md:393 as read. NOTHING ELSE READS THE GOLDEN: grep for shipped_item_ids across src, tests, scripts, docs and .github returns the test, docs prose, phase files, and one generated shard weight (scripts/ci_shard_weights.generated.json:8057); neither scripts/merge_audit/symbol_census.mjs nor scripts/gate_select.mjs names it.
OPTIONS:
  1. [no-op] Ratify release-time cadence AND the golden as it stands (no prune, no re-mint now)
     PRICE: Genuinely no-op in code: a dated RULED line against state.md open item (10) and this row, nothing else. tests/shipped_item_ids.test.ts and its golden stay byte-untouched and green, because the check at :38 is a subset filter. Rides no same-change obligation at all: no npm run wiki:content and so no tests/guide.test.ts freshness gate, no M16 non-Latin fills, no deed record, no Reliquary page, no committed WebP art, no scripts/merge_audit/symbol_census.mjs row, no parity golden, no tests/world_api_parity.test.ts pin, no tests/monolith_budget.test.ts ceiling. Reviewers: none of the Review Dispatch Matrix agents trigger on a paper ruling (implementation-plan.md:144-153); qa-checklist only at phase close. The ruling MUST also carry the consequence line, because the tree has changed what it means: with all 172 branch-only ids pinned, the escape at src/sim/content/CLAUDE.md:343-344 is closed in practice, so a Phase-20 cut of any of them becomes a RETIREMENT (keep the def, drop the acquisition paths, exemplar RETIRED_HEROIC_ITEMS) plus its merge-deletion-list row, never an outright delete.
  2. [WORK] Ratify release-time cadence but PRUNE the branch delta back out of the golden, re-minting only at the release
     PRICE: WORK, and it is the one diff shape the file's own header reads as a catastrophe: a REMOVAL from tests/shipped_item_ids.golden.json, which tests/shipped_item_ids.test.ts:16-17 defines as the signature of a dead shipped id, so the header gains an explicit branch-prune carve-out in the same change or the next reader mis-diagnoses the diff. The maintainer must also pick WHICH delta, and the two are not the same set: the 172 ids absent from origin/release/v0.42.0's content AND golden (the packet's own), or the full 418 the branch golden holds over that ref's golden, of which 246 are release-content ids the release itself has never pinned and that the branch's re-mints captured for free. The sanity floor at :37 survives either cut (1048 or 802). Everything else stays nil: no wiki regen or guide freshness gate, no i18n or M16 fills, no deed, no Reliquary, no art, no census row, no parity golden, no world_api parity pin, no monolith ceiling; reviewers as in option 1 (qa-checklist at phase close only). Cost is concentrated in the judgment, not the edit.
  3. [docs] Adopt per-content-change re-mint explicitly, re-mint now, and amend the doctrine to match the branch's actual practice
     PRICE: One id moves plus a docs pass. UPDATE_SHIPPED_ITEMS=1 npx vitest run tests/shipped_item_ids.test.ts takes the golden 1220 to 1221, adding exactly reins_rickshaw_mount and nothing else (measured in this worktree, so the additions-only diff review is a one-line read). Then the doctrine edits, or the tree keeps contradicting itself: tests/shipped_item_ids.test.ts:14-16 and src/sim/content/CLAUDE.md:345-347 both say "after new items ship in a release" and would have to say per-change, and the acceptance line the deviating phases dropped (phase-11j-gathering-completion.md:508-509 as read) returns to the remaining phase files. docs/design/professions-tuning-packet.md:393 stays true either way. No wiki regen, no guide freshness gate, no M16 fills, no deed, no Reliquary, no art, no census row, no parity golden, no world_api pin, no monolith ceiling; reviewers as in option 1. Closes the deviation five ledgers carry, and should say in the same edit that the "delete outright" escape at src/sim/content/CLAUDE.md:343-344 is retired for this branch.
  4. [WORK] Rule dev-grant-only ids OUT of the golden (the class the one live gap belongs to)
     PRICE: WORK, and it cuts against the golden's own stated failure mode. Needs a named exclusion the update path implements (a DEV_ONLY_ITEM_IDS set, or a no-acquisition-path predicate, in tests/shipped_item_ids.test.ts) plus REMOVAL of the two dev-grant reins already pinned, reins_terrorspark_groundshaker and reins_drakemaw_raptor, which is again the append-only violation option 2 has to carve out. The header's reason at :1-6 is that a def deletion renders a live save's slot empty, and a /dev-granted mount sits in a real save on any realm booted with ALLOW_DEV_COMMANDS=1, so the exclusion trades a real protection for tidiness. It also collides with D110, which may give both pended mounts a genuine source, at which point the exclusion has to be unwound and the ids re-added. No wiki, i18n, deed, Reliquary, art, census, parity or ceiling obligation; reviewers as in option 1.
RECOMMENDATION: Option 1, ratify release-time cadence and the tree as it stands, with the premise correction stated in the same line: the cadence is cosmetic against a subset check that never reds, the branch has already taken the harm the deviation was avoiding (all 172 of its own branch-only ids are pinned), and pruning would throw away 246 release-content pins the branch made for free - but the ruling must say that a Phase-20 cut of a branch-only id is now a retirement, not a delete.
FLAGS: needsMaintainerValue=false; r5Surface=false; contingentOn=none

### D176  [decision-d-band-scope]
SEED: row 3 (Decision D's band scope, named verbatim among the thirteen standing state.md items; D006 rules only that the thirteen expand one row each and cites this member as an example needing its own answer)
QUESTION: Does masterwrought decision D's self-feeding refusal (a family is not credited for a recipe that produces the family's own gathering tool) stay scoped to the ENDGAME band alone as shipped, extend to EVERY band, or extend to the top levelling band only? The Phase 11j QA deliberately shipped a self-clearing tripwire instead of a rule change because the scope is the maintainer's: nothing in the tree is self-credited-only below the cap today, and logging band 3 is one deleted row from it.
ANCHORS: LEDGER: state.md:12835-12841 as read, open item (11) 'DECISION D'S BAND SCOPE ... Extending the refusal to every band empties no cell (cost table above); leaving it is also defensible ... Ruling wanted, not a fix'; state.md:12822-12824 as read, deviation 3, 'Decision D's endgame-only SCOPE was not changed, only tripwired ... it re-takes a settled decision, which is the maintainer's'; state.md:12480-12490 as read, mutation survivor 2, where the measured extend cost lives (no cell empties: logging b3 2 to 1, mining b3 7 to 6, herbalism b3 6 to 5, fishing b3 2 to 1, farming b1 7 to 6, b2 5 to 4, b3 4 to 3); state.md:17856-17868 as read confirms the thirteen carried in are unchanged, so it is still open. LIVE CODE, verified in this worktree: tests/gathering_supply_coverage.test.ts:232 (isSelfFeedingFor, resolved through the item's own tool record and never a hand-written list), :281 (the scope itself, 'if (band === ENDGAME_BAND && isSelfFeedingFor(recipe.resultItemId, family))'), :209-212 (ENDGAME_BAND and LEVELLING_BANDS derived from the live GATHERING_PROFESSIONS maxSkill), :422 (the empty-levelling-band coverage arm any extension must be re-measured against), :463-521 (the self-clearing tripwire, its instruction 'WHEN THIS REDS, RE-DECIDE DECISION D'S SCOPE, never widen this arm', the measured cost table restated in its comment, and the anti-vacuity pin whose comment reads 'Six tool rungs' above a SEVEN-entry list), :854-861 (the six-row REFUSED_ENDGAME_ROWS outcome pin); src/sim/professions/tools.ts:36 (gatherToolTier, the discriminator the refusal runs on); src/sim/content/recipes.ts:289 and :2324 (recipe_ashwood_axe and recipe_precision_chassis, the two rows that make logging band 3 the one-row near miss); docs/design/professions.md:517-523 (the supply matrix, whose levelling cells name HOE_RECIPES, TOOL_RECIPES and ROD_RECIPES inside their own family's bands), :531-543 (the reading whose whole claim is that a tool ladder appears in its own family's LEVELLING bands and never in its endgame cell, 'two different causes rather than one') and :545 (the R20 statement).
OPTIONS:
  1. [no-op] Ratify the endgame-only scope as shipped, tripwire kept
     PRICE: Genuinely no-op: one dated RULED line at state.md's open item (11) (12835-12841 as read) and at the deviation-3 block (12822-12824). No code, no pin, no doc table moves; the tripwire at tests/gathering_supply_coverage.test.ts:463 already tells whoever reds it to bring the scope back to the maintainer, so it needs no edit for the ratification to hold. No reviewer beyond the standing gate.
  2. [WORK] Extend the refusal to EVERY band
     PRICE: One predicate (drop 'band === ENDGAME_BAND &&' at tests/gathering_supply_coverage.test.ts:281), but three pins and a design table ride it. The refused-set pin at :854 grows from six rows to thirteen (the six endgame rows plus the seven levelling rungs listed at :513) and its 'six rows across all five gathering professions' comment is rewritten. The anti-vacuity pin at :513 must be RE-SOURCED from REFUSED_ENDGAME_ROWS, because the rows it counts leave the matrix; left as written it becomes an assertion over an empty set, exactly the vacuity class this file has already been caught on twice (the 11j fix round's four vacuous arms, then the two survivors). The tripwire at :463 becomes true by construction and must be DELETED, not left green. Before any of it the empty-band arm at :422 must be RE-MEASURED on the merged tree: the only cost on record was taken at the 11j QA, before the tenth release sync, and the guard's own comment already reads 'Six tool rungs' against a seven-entry pin, so the table is at least one row stale. Docs: docs/design/professions.md:517-523 (every levelling cell naming a family's own tool ladder), the reading at :531-543 which the extension outright falsifies, and the R20 statement at :545; state.md carries the ruling id at both blocks. Nothing content-side moves, so no wiki regen (npm run wiki:content plus the tests/guide.test.ts freshness gate), no i18n catalog rows or M16 non-Latin fills, no deed record, no Reliquary page, no committed WebP art, no symbol census row (scripts/merge_audit/symbol_census.mjs), no IWorld member and so no tests/world_api_parity.test.ts change, and no tests/monolith_budget.test.ts ceiling (the guard is a test file, not a ratcheted src monolith). IF the re-measure finds a cell that empties, the option converts to content work (a bill in src/sim/content/recipes.ts) and pulls that entire train plus content-obligations-reviewer. Reviewer either way: test-coverage-auditor, the lane that found both of this file's surviving holes.
  3. [WORK] Extend to the TOP levelling band only (the band below the cap), leaving the low bands exempt
     PRICE: Same edit shape, bounded rather than removed at tests/gathering_supply_coverage.test.ts:281, and it buys four of the seven measured collisions (logging, mining, herbalism and fishing all sit at b3) while leaving farming's bronze and osmium hoe rungs at b1 and b2 credited. Cheaper than the full extension in one specific way: the :513 population drops to the two farming rungs but stays non-empty, so that arm survives as written instead of being re-plumbed, and the tripwire at :463 is re-scoped to the still-exempt bands rather than deleted, keeping its self-clearing property. The refused-set pin at :854 still grows (six to ten) with its comment rewritten, the b3 cells still need the same re-measure on the merged tree, and docs/design/professions.md:517-523 plus the reading at :531-543 are narrowed rather than rewritten wholesale. Same absence of content obligations, and the same conversion to content work if a b3 cell empties. The honest limit: the boundary is free only if it is the band below the cap (derivable from ENDGAME_BAND); any other boundary is a number the packet records do not hold.
RECOMMENDATION: Ratify the endgame-only scope: the 11j QA chose a tripwire over a rule change precisely because the scope is the maintainer's, no band in the merged tree is self-credited-only today (all seven levelling tool rungs sit beside real bills), and the ledger's own design read stands, that below the cap a tool rung is at least a rung a leveller passes through, so extending re-takes a settled decision and moves three pins plus a design-doc table for a change no player can observe.
FLAGS: needsMaintainerValue=false; r5Surface=false; contingentOn=D006 (which settles that the thirteen expand one priced row each rather than under one blanket ruling)

