# Phase 19F: English i18n completeness

Opened 2026-09-01 under ruling qr-19-best-for-project. English keys, matchers and i18n tooling, plus the M16 non-Latin fills a wordy new English value owes in-change. The full 20-locale fill happens later on the release branch per qr-19-english-only.

**10 units, about 33.50 hours estimated.** Every row here was answered in the Phase 19 rulings gate; this phase executes, it does not re-decide. A row whose ruling turns out to rest on a wrong premise is an ESCALATION back to the ledger, never a quiet re-ruling.

## Units

### D070

Rule whether the flask counter-immunity tooltip gains a Spellgnaw devour clause or the judged classic-idiom omission stands, where the recommendation is to let the omission stand and avoid re-opening five already-filled non-Latin rows.

- **Files:** `docs/prd/masterwrought/state.md`, `src/ui/i18n.catalog/items.ts`, `src/ui/i18n.locales/ko_KR.ts`, `src/ui/i18n.locales/zh_CN.ts`, `src/ui/i18n.locales/zh_TW.ts`, `src/ui/i18n.locales/ja_JP.ts`, `src/ui/i18n.locales/ru_RU.ts`, `src/sim/mob/mob_swing.ts`, `src/sim/types.ts`
- **Same-change obligations:** RULING RECORD anchor, verified unique with grep -c = 1 in state.md (the bare phrase without the trailing clause matches 2, the table row too, so use the long form): "Spellgnaw devour exception stays out of the tooltip copy. (3) The" and the RULED line goes AFTER it at column 0. No handoff table row, so no status cell to flip. NO obligations under the recommended arm: no wiki regen, no M16 fills, no deed rows, no Reliquary pages, no art parks, no census rows, no parity goldens, no monolith ceilings. THE KEY, resolved this session so the reword arm is not sized blind: it is itemUi.tooltip.flaskUnremovable, English at src/ui/i18n.catalog/items.ts:118 ("It cannot be dispelled, stolen, or canceled by hand."), and it is ALREADY FILLED in exactly the five non-Latin overlays (ko_KR.ts:2958, zh_TW.ts:2866, zh_CN.ts:2864, ja_JP.ts:2980, ru_RU.ts:3014), which is precisely the reword-staleness exposure the row prices: a reworded English source leaves those five reading green and stale, and no guard sees it (that is D126's whole subject). BEHAVIOR RE-VERIFIED, so the omission is a copy call and not a lie: src/sim/mob/mob_swing.ts:45 isDevourableAura is the devour predicate and :1061 its findIndex call site, and the Aura type comment in src/sim/types.ts already states in full that the devour affix deliberately reads neither the undispellable flag nor the flask marker because the rule scopes to player-driven counters. THE REWORD ARM: one English edit through the write-game-tooltips skill, five hand fills through the i18n-locale-fill skill in the same change (never a pending row left for Phase 20 on a key that is already filled), plus frontend-seam-reviewer because src/ui moves. Ratify arm 1h; reword arm 3h.
- **AMENDED IN PLACE 2026-09-02 at execution, nine corrections; the recommended ratify arm survives all of them and the reword arm was under-priced.** (1) The source row's three state.md addresses (5517-5520, 5103, 5220) were exact at the row's authoring commit f14f015a7b and have drifted +207 at a1b435ff85 (5724-5727, 5310, 5426-5428); the durable anchors are the text: the judged refusal via 'Spellgnaw devour exception stays out of the tooltip copy. (3) The' (grep -cF = 1), the behavior sentence via 'the mob Spellgnaw devour affix reads neither flag and STILL eats a flask', the doc-scope sentence via 'doc scopes "purge" to player-driven counters and names the Spellgnaw'; the state.md Phase 19 table DRAFT copy of the row repeats the stale addresses and, per the 19E precedent, the draft is history and only the gate row is amended. (2) Neither text names the pin the ruling rests on: tests/mob_purge.test.ts, describe 'mob purge affix (Spellgnaw)', it 'still devours a flask aura: the STK-2 ruling covers player counters only (recorded exception)' (with a second devour assertion in tests/lifesap_adversarial.test.ts); the code by symbol is devourBeneficialAura in src/sim/mob/mob_swing.ts (its findIndex(isDevourableAura) is the predicate site), and the Aura.undispellable docblock in src/sim/types.ts scopes the rule to player-driven counters. (3) PLACEMENT: the anchor line ends mid-sentence ('(3) The') and item (3) closes two lines later on '(above).'; the RULED line lands after that closing line, indented to the bullet's continuation column (the qr-19-overcap-legacy-swap-freeze shape), so the sentence is not split and no column-0 line sits inside an indented bullet; the anchor stays the locator. (4) THE REWORD ARM OWES MORE THAN PRICED: the English value is pinned literally in tests/elixir_tooltip_view.test.ts (the FLASK_RULES constant under describe 'flask rules', consumed by 'a flask adds its four rules under the shared Use line' and 'every shipped flask states all four, on its own stat axis'), and the clause has a guide twin, guide.profPages.craftProse.alchemy.ladderBody ('no dispel, steal, or hand cancel takes it off'), authored in the same change with its own five non-Latin rows; a Spellgnaw clause would owe both re-pins, the twin's edit and five more fills. (5) 'never a pending row left for Phase 20 on a key that is already filled' is imprecise: itemUi.tooltip.flaskUnremovable is filled only in the five non-Latin overlays and is pending in the 15 Latin locales today (src/ui/i18n.resolved.generated/pending.ts carries it 15 times), so it rides to the Phase 20 fill regardless; the reword arm's in-change obligation is the five non-Latin rows alone. (6) 'Depends on: D069' is a cross-phase dependency on a 19A unit, satisfied: state.md carries 'RULED (qr-19-flask-dead-timer-pause, 2026-09-01, under qr-19-best-for-project)'. (7) The source row's 'state-important-triggers-and-limits rule' is CLAUDE.md's one-line gloss; the rule is item 7 of the docs/design/tooltip-writing.md checklist ('Every trigger, limit, reset, consumption rule, and failure condition'). (8) Seeded row 3's own wording is "the reworded wiki keys' stale fills (folded into the Phase 20 fill unless ruled re-keyed)"; 'M16-invisible' is the row's gloss. (9) Both texts say 'five non-Latin overlays' and the phase intro says a '20-locale fill'; src/ui/i18n.locales/ holds 21 overlay files and pending.ts lists 21 locales (en_CA included, the English dialect); the sibling units count 20 non-English locales with en_CA excluded, stated where it matters.
- **Reviewers:** qa-checklist, qa-checklist LAST
- **Depends on:** D069
- **Estimate:** 1h

### D074

Rule whether the farming guide prose states the crop growth-timer spread or the spread stays a test pin only, where the recommendation is the status quo or an explicit deferral behind the still-open crop-duration tuning read.

- **Files:** `docs/prd/masterwrought/state.md`, `src/ui/i18n.catalog/guide.ts`, `tests/guide.test.ts`
- **Same-change obligations:** RULING RECORD anchor, verified unique with grep -c = 1 in state.md (the table row carries the same words but closes with an apostrophe before the period, so keep the period form): "reworded value to fill." and the RULED line goes AFTER it at column 0, inside the "### CUT, with the reason, rather than left as future items" block. That block already carries the precedent shape two bullets down ("RE-OPENED (qr-18-REOPEN, 2026-08-31): actioned by Phase 18 as reagent-slice-source-scan-pin." sits mid-bullet at column 0), so match it. No handoff table row, so no status cell to flip. NO obligations under the recommended arm. VERIFIED: no growth-timer claim exists in src/ui/i18n.catalog/guide.ts today, so the status quo is genuinely the shipped state and the ruling is a record, not a revert. THE DEPENDENCY IS THE POINT OF THE ROW: the deferral arm is only honest if D012 (crop-durations-gain-survival, the seeded row 4 tuning read over crop durations, gain schedule, survival endpoints and the pick floor and cap) is answered first, because a prose timer claim authored against durations the maintainer then moves is stale inside the same phase. If the maintainer wants the claim, sequence it AFTER D012 and pay it once. THE ADD ARM: one wordy English guide.* key, its non-Latin M16 fills in the same change (M16 bites because a timer spread sentence is wordy), wiki regen via npm run wiki:content with the tests/guide.test.ts freshness gate, plus frontend-seam-reviewer (src/ui moves) and content-obligations-reviewer (the wiki regen obligation). Status quo or deferral 1h; add-the-claim arm 4h and it should not be paid twice.
- **AMENDED IN PLACE 2026-09-02 at execution, seven corrections; ONE MOVES THE PREMISE and the arm was put to the maintainer in session.** (1) THE DEFERRAL ARM IS DEAD: D012 (crop-durations-gain-survival) is CLOSED. The farming/state.md handoff row 'Crop durations (5-minute sibling gap advisory), gain schedule, survival endpoints, pick floor/cap' reads 'CLOSED 2026-09-01 by ruling qr-19-crop-durations-gain-survival (Phase 19): every constant on this row is RATIFIED AS SHIPPED and none moves ... A re-tune is BARRED', landed in 446564e533, an ancestor of the commit that wrote this unit (62dc63769d). So the deferral has nothing to wait on and the 'copy may go stale within the same phase' argument against the add arm is void: the live choice is binary, the status quo (the test pin alone) or the wordy guide.* claim authored ONCE against the now-final values (4h, 4h10m, 4h30m) with its five non-Latin fills; the maintainer's answer is recorded in the RULED line. (2) The anchor is unique only as a FIXED string: plain grep -c counts 2 because the unescaped '.' matches the state.md draft-table copy ("to fill'."); state it as grep -cF 'reworded value to fill.' = 1. (3) The source row's cites (14547-14551, 14454) have drifted: the block is '### CUT, with the reason, rather than left as future items' under the '## Phase 11h QA ledger' section (the inner heading; the outer '## Phase 11h QA ledger' heading is the section it sits in; both checkable at a1b435ff85); cite by heading text, never by line. (4) The refusal quote spans a line wrap and greps 0 whole in the block; the forms that grep are 'a copy decision beyond a QA fix round' (2 hits, one the draft-table copy) or the tail 'reworded value to fill.' (1). (5) The precedent RE-OPENED line sits in the NEXT bullet (one bullet down, two lines below the anchor), not two bullets down; the column-0 mid-bullet shape claim itself is right. (6) The spread pin is unnamed in both texts: tests/provisioning_supply_line_apex.test.ts, describe 'masterwrought Phase 11h GATE A: the amended uniform-bill rule', it 'and the WALL-CLOCK spread is 12.5 percent, the honest half of the same gate', which resolves the three plate crops through the live bill to FARM_CROPS durationMs and asserts [14_400_000, 15_000_000, 16_200_000]; the live constants in src/sim/content/farm_crops.ts still match. (7) The 'Depends on: nothing' header and the body's 'the deferral arm is only honest if D012 is answered first' were inverted (the ADD arm was the one waiting on D012); moot now that D012 is closed.
- **Reviewers:** qa-checklist, qa-checklist LAST
- **Depends on:** nothing
- **Estimate:** 1h

### D085

Ratify the guide materialsBody prose at its current spoiler altitude (nothing it says is false) rather than naming the on-ramp bill and manufacturing reword-staleness rows on the eve of the Phase 20 release fill.

- **Files:** `docs/prd/masterwrought/state.md`, `src/ui/i18n.catalog/guide.ts`, `tests/guide.test.ts`
- **Same-change obligations:** RULING RECORD anchor, verified unique with grep -c = 1 in state.md: "The materialsBody omission; the trophy-trio band-vs-mover precision" and the RULED line goes AFTER it at column 0, in the "### JUDGED THIS ROUND, do not re-raise" block; that same block already carries the precedent form three lines down ("RE-OPENED (qr-18-REOPEN, 2026-08-31): actioned by Phase 18 as amendment-dating-shortfall; refusal-control-placement."), including the idiom of naming two slugs on one line. A second, weaker anchor sits four lines above in the qa-checklist close ("recorded as judged-no-change: the gate itself graded it a"), and one line is enough. No handoff table row, so no status cell to flip. NO obligations under the ratify arm. ONE FINDING THE ROW DOES NOT CARRY, and the ruling should record it: neither the row nor the ledger names WHICH key is meant. src/ui/i18n.catalog/guide.ts holds nine craftProse blocks (weaponcrafting, armorcrafting, tailoring, leatherworking, cooking, alchemy, engineering, enchanting, jewelcrafting, inscription) and every one has its own materialsBody; the two packet-authored crafts, jewelcrafting and inscription, are the plausible subjects. Ratifying is safe without resolving that, but the reword arm CANNOT be executed until the exact key is re-identified from the Phase 12-era QA nit, so the ruling line should say so rather than leave a future session to guess. THE REWORD ARM: one English catalog edit, its Latin-locale pending rows plus any M16 non-Latin fills in the same change, wiki regen via npm run wiki:content with the tests/guide.test.ts freshness gate, plus frontend-seam-reviewer and content-obligations-reviewer; and it lands days before the Phase 20 fill, which is the timing objection. Ratify arm 1.5h; reword arm 4h.
- **AMENDED IN PLACE 2026-09-02 at execution, eight corrections; the key IS identifiable and the reword arm was mis-priced; the recommended ratify arm survives.** (1) The source row's ledger cites (state.md 19024-19027 and 19030, 'Phase 12-era') point at the Phase 11o QA ledger, not Phase 12: the nit is the qa-checklist LAST close of '## Phase 11o QA ledger (2026-08-26, the twin)' (wrapping 20273-20275 at a1b435ff85) and the JUDGED entry is the first line of that ledger's '### JUDGED THIS ROUND, do not re-raise' block (20277-20278); the unique anchors are '(one applied as commit 6; the materialsBody on-ramp-bill omission' and 'spoiler-altitude judgment call and nothing it says is false).' (each grep -cF = 1) beside the primary anchor this unit already uses; the 'second, weaker anchor' this unit offered ('recorded as judged-no-change: the gate itself graded it a') is NOT unique (2 hits, the draft-table copy). (2) craftProse holds TEN blocks (the parenthetical list names ten) and NINE carry a materialsBody: enchanting has none. (3) THE KEY IS IDENTIFIABLE, so the ruling names it: the ledger context is the Phase 11o engineering round (its six fix commits are all engineering, c9c9f4049a, 795c28c35b, 95d7d81097 among them), the on-ramp is ENGINEERING_ONRAMP_RECIPES in src/sim/content/recipes.ts (the cogwheel and ocular bills), and the prose that never names it is guide.profPages.craftProse.engineering.materialsBody ('Reagents and prior tools'); jewelcrafting and inscription (Phase 05 and 06 rounds) are not the subject, and the row's 'masterwrought materialsBody' is a misnomer (masterwroughtBody and masterwroughtBodyLegendary are different keys). (4) THE REWORD ARM'S PRICE: no wiki regen is owed (src/guide/content.generated.ts and scripts/wiki/build_content.mjs carry no craftProse field, so npm run wiki:content is run-and-confirm-clean and content-obligations-reviewer is not triggered by a wiki obligation; frontend-seam-reviewer still applies); the owed regen is npm run i18n:gen plus the S3 guard; tests/guide.test.ts pins no engineering materialsBody clause (only cooking's and alchemy's), so it is not a same-change file unless a new pin is written. (5) An in-place reword mints NO pending rows: the key is translated in 16 locales (all five non-Latin included) and already pending in 5 Latin ones (it_IT, pl_PL, id_ID, tr_TR, da_DK), a reword leaves the 16 translated-and-stale (D126's class) and M16 does not fire on a reword; the honest reword shape is retire-and-re-key (a RETIRED_KEYS 'Reworded successors' row, a successor key, five non-Latin fills in-change, 15 Latin rows to Phase 20, not the 16 the obligations count: en_CA inherits the English leaf as translated), the same mechanism D161 and D169 use. (6) The reword arm is executable today (the key is named); the timing objection stands as the reason to ratify. (7) 'nothing it says is false' holds under the page's own scoping and the RULED line states it: 'Every land tool recipe' means the pick, axe and sickle lines the paragraph enumerates, since the bronze hoe (recipe_bronze_hoe) consumes Fine Vale Wheat, a farm fine roll and not a vein material, and the hoe ladder is documented on the farming page; the scoping is a recorded read, not a reword. (8) The spoiler policy in src/guide/CLAUDE.md governs generated content, not hand-authored craftProse bills (the key already names full reagent bills); the ratify rationale rests on the gate's own grading and the Phase 20 timing.
- **ADDENDUM 2026-09-02 at the review round.** The ratify arm survived the unit; the unit's own qa-checklist then found the prose counted TWO rod recipes where ROD_RECIPES holds three, a separate factual defect, so the reword arm was taken for that defect alone (the altitude unchanged): guide.profPages.craftProse.engineering.materialsBody retired (RETIRED_KEYS row), the successor materialsBodyThreeRods through the bodyKey override of proseSection in src/guide/pages/professions_craft.ts, five fresh non-Latin fills, the regenerated key union and the twenty-three resolved slices, the merge-deletion-list extras row and the Phase 20 hand-carry. Files touched beyond the list above: src/guide/pages/professions_craft.ts, scripts/i18n_retired_keys.mjs, the five src/ui/i18n.locales/ overlays, src/ui/i18n.catalog/translation_keys.generated.ts, src/ui/i18n.resolved.generated/, docs/prd/masterwrought/merge-deletion-list.md, docs/prd/masterwrought/phase-20-release-fill-and-reclose.md. The fresh read of that round found the record's precedent misattributed (11i, not 11o), the frontend-seam review the amendment's item (4) owed was dispatched at the second round and found the rod pin's reagent arm false-passing on a koi-count swap (rewritten per clause), the successor's five fills unpinned (an every-locale arm added, exact key), two glossary drifts in the zh_CN and ja_JP fills (the established Delve Marks terms restored, Highwatch localized in zh_CN), and the Latin-side render regression unrecorded (now stated on the record). Item (7) above says the hoe ladder is documented on the farming page; the guide has no farming page, it is the gathering pages' tools note, as the RULED line records.
- **Reviewers:** qa-checklist, qa-checklist LAST
- **Depends on:** nothing
- **Estimate:** 1.5h

### D144

Ratify the non-gear closure and the Crucible gear tier's admission to sundering (status quo, which no packet number depends on), and reword the one player-facing 'of the tier' sentence that now reads as a restriction the predicate does not make.

- **Files:** `src/ui/i18n.catalog/guide.ts, endgameMaterialsBody at :2585 to :2586 (the English 'a raid-won piece of epic gear of the tier into exactly one essence')`, `src/sim/professions/sundering.ts, the eligibility docblock's deferral sentence (the row-16 pointer) reworded to the ruling id`, `tests/masterwrought_materials.test.ts:980, the comment reading "the maintainer's (Phase 19 decision table row 16)", reworded to the ruling id`, `docs/prd/masterwrought/state.md, the eighth-sync AMENDED block carrying the sundering record`, `regenerated i18n artifacts from npm run i18n:gen (committed, never hand-edited)`, `READ-ONLY: tests/guide.test.ts, the two live guide pins on 'exactly one essence' and 'one per week per character', which must be PRESERVED or re-pinned deliberately`, `READ-ONLY: src/ui/i18n.locales/{ja_JP,ko_KR,ru_RU,zh_CN,zh_TW}.ts, the five overlays that already carry endgameMaterialsBody`
- **Same-change obligations:** i18n is the load-bearing half: contributors add ENGLISH ONLY to src/ui/i18n.catalog/guide.ts and NEVER edit the src/ui/i18n.locales/ overlays, so this change owes the reworded English value, npm run i18n:gen with the regenerated resolved bundles committed, the S3 guard (tests/localization_fixes.test.ts), and the two tests/guide.test.ts pins re-read. THE TRAP TO RECORD EXPLICITLY: a REWORDED (not new) value leaves all five overlays TRANSLATED rather than pending, so M16 does not catch it and tests/i18n_completeness.test.ts stays green while the five overlays are silently stale; zh_CN renders the clause as a 'this tier' restriction, which is evidence a reader takes it that way. Route those five to the Phase 20 fill under seeded row 3's reworded-keys item (that is D006's routing) rather than editing overlays here, and say so on the record so Phase 20 inherits it. NO wiki-content regen (src/guide/content.generated.ts carries no sundering entry, verified), no deed rows, no Reliquary pages, no art parks, no census rows, no parity goldens (no rng draw site moves), no monolith ceilings, no new item id. Validation: npx tsc --noEmit plus npm run ci:changed on the touched files. RECORDING: dated RULED line qr-19-crucible-gear-sundering-admission. ANCHORS, each grep -c 1: state.md "question carried as row 16; and the release's new encounterOwned aura marker" (the dated line goes AFTER that sentence's clause) and src/sim/professions/sundering.ts 'Phase 19 decision table row 16; until ruled they remain admitted the' (the deferral sentence to rewrite in place with the ruling id). No handoff-table row exists, so no status cell to flip. WHAT THE RULING SHOULD RECORD, unglamorously: the widening moves no number the packet derived (attempts stay ember-gated at 1 per week, and the derivation's 'a second essence would out-pace a weekly raider's spare epics' premise is RELAXED, not broken), and the real second-order effect is a storage read, since the essence is soulbound, noDiscard, sellValue 0, stackSize 20, not a Material, and sits behind a structural-zero market bucket. IF option 2 (exclude by tier) is ruled instead, it becomes a 19A-content change: a maintainer-named cut (source level does NOT order the tiers, so it must be an item-level ceiling or an explicit id list), flipped material pins with a new negative arm, six doc and comment rewrites, the Sunder row disappearing from 186 items' bag menus (which pulls in the PR before/after screenshot rule), and the same i18n chain in the opposite direction. IF option 5 is ruled, it re-prices PERFECTING_ATTEMPT_COST, flips the exact-array pin plus its derived consumers including the parity coverage scenarios, and owes a dated amend-in-place line on the rank derivation.
- **AMENDED IN PLACE 2026-09-02 at execution, eleven corrections; the recommended arm (option 4 with option 1 folded) survives all of them, and ONE changes the mechanism of the reword.** Every address in the source row is correct at the read commit the row itself declares ('all reads at 2ab5c2f7d0'); this block records the a1b435ff85 forms and the durable anchors, it does not fault the row. (1) The test comment is the three-line one ending "the maintainer's (Phase 19 decision table row 16)." inside 'the eighth-sync gear allowlist: raid-sourced epic non-gear never sunders' (tests/masterwrought_materials.test.ts, now :979-981); cite by test name. (2) At a1b435ff85 the row's other addresses read: the eighth-sync AMENDED sentence 'question carried as row 16' at state.md 23162-23167 (the anchor greps 1), the seed row '| 16 |' at phase-19-rulings-gate.md:207, the draft heading '### THE BUCKET-B LIST is the Phase 19 table DRAFT (STEP 4)' at state.md 24836 and D038's seed cell at 24894, the naming registry 'Shared items:' line at 327-328 (line-wrapped: anchor on 'down any raid epic of the tier), Maker'), the sundered_essence ItemDef and its docblock at src/sim/content/items.ts 3037-3051, the noDiscard / noMarketList / soulbound members at src/sim/types.ts 1056-1063, the menu call `if (isSunderable(def)) out.push('sunder')` at src/ui/bag_item_context_menu.ts:94 (import still :22), the rank derivation '### THE RANK-COUNT DERIVATION (qr-12-CADENCE, farming/state.md row 129)' at state.md 20341-20353 with its verbatim premise "2 would out-pace a weekly raider's spare epics" at 20344-20345, the Phase 05 QA widening at 1512-1523 (anchor 'aligning with the settled any-raid-epic-of-the-tier model'), the '## Validation matrix (per change type)' at 548-559 (S3 line 553), the `sunder: 'Sunder'` comment in src/ui/i18n.catalog/hud_chrome.ts (the catalog module, 5925-5926), endgameMaterialsBody at guide.ts 2585-2586 and perfectingBody at 2588-2589, the guide pins 'exactly one essence' and 'one per week per character' at tests/guide.test.ts 3522-3523 with the Perfecting cost pin 'one Sundered Essence, and one Prismglass Setting' at 3529, and docs/design/power-verification.md at 1320 lines with its one 'essence' hit (arcane_essence) at 1250. (3) THE MERGE HAS LANDED: at a1b435ff85 both 22e909839f (the v0.41.0 sync) and c173606bbb are ancestors of HEAD; lastflame_core keeps kind 'junk', now sits in MATERIAL_ITEM_IDS through CRUCIBLE_RECIPE_PENDING_MATERIAL_ITEM_IDS, and isSunderable still refuses it; every figure re-measures identical (217 sunderable, all gear; 186 Crucible = 175/9/2 against 31; item levels 14 at 29, 17 at 33, 186 at 35; source levels 14 at 20, 186 at 26, 17 at 27; 145/145 vendor offers). (4) Two quotes span line wraps: anchor src/sim/professions/sundering.ts on 'any-raid-epic-of-the-tier model, the normal-only boundary' (line 48) and src/sim/professions/perfecting.ts on 'the pacing lever (1/week accrual, R4 untouched); the essence and setting' (line 90). (5) RULED-LINE PLACEMENT: the anchored sentence's final clause ('applies one to players.') is now followed by the existing RULED line for qr-19-encounter-owned-aura-wire (2026-09-01); the D144 line lands AFTER that RULED line so the two rulings on one sentence do not interleave. (6) 27 is the HEROIC Nythraxis source level (NYTHRAXIS_RAID_LOOT_SOURCE_LEVEL); normal Nythraxis' 14 ids sit at 20, so a naive ceiling at 26 keeps 14 + 186 and drops only the 17 heroic ids; the conclusion (source level does not order the tiers) stands. (7) THE MECHANISM OF THE REWORD, per this phase's standing rule (a changed wordy value is a NEW key with a RETIRED_KEYS 'Reworded successors' row, never an in-place reword): option 4 lands as the successor key guide.professions.endgameMaterialsBodyAnyRaid beside endgameMaterialsBody, the old key retired, so M16 forces the five non-Latin fills IN-CHANGE and the 15 Latin rows land pending for Phase 20; the row's 'route those five to the Phase 20 fill under seeded row 3' and the unit's 'a REWORDED (not new) value leaves all five overlays TRANSLATED' describe the in-place shape and do not apply (and would have been invisible anyway: Phase 20 STEP 1 derives the reword-staleness package only from state.md's literal 'RELEASE-FILL OBLIGATIONS' blocks, the farming deferred rows and the named machine-anchored fills, never from seeded row 3 or D006); no overlay of the retired key is edited, the two guide pins are preserved by the successor's text, and the professions page's one render site (src/guide/pages/professions.ts, the prof-endgame section) re-points. (8) content.generated.ts has six 'sunder' substring hits (an ability id, item names, a zone name), none a sundering entry, so 'no wiki regen' holds but a bare grep -c is not its proof. (9) Option 2 rewrites five doc and comment sites plus the hud_chrome catalog comment (six in all, the row's 'hud_chrome.ts' being that catalog module). (10) 'Depends on: nothing' holds for the recommended arm with the frozen-R5 caveat the gate preamble states: options 1 and 4 re-measure nothing, option 3 folds into D140, option 5 re-opens the Verdict. (11) The row was authored mid-merge; at a1b435ff85 there is no MERGE_HEAD and the tenth-sync merge c659387f9a has the exact parents the row names.
- **Reviewers:** cross-platform-sync, frontend-seam-reviewer, content-obligations-reviewer, qa-checklist
- **Depends on:** nothing
- **Estimate:** 4h

### D148

Fix scripts/i18n_scan.mjs so DICT scopes (sim, server) report real per-locale presence instead of the merged English spread, then regenerate the status registry; the honest fix, but I measured the backlog it surfaces and it is roughly 8,000 pending sim rows, which is a Phase 20 scheduling decision, not a Phase 19 one.

- **Files:** `scripts/i18n_scan.mjs`, `src/ui/i18n.status.json`, `src/ui/sim_i18n.ts`, `src/ui/server_i18n.ts`, `tests/i18n_status_registry.test.ts`, `tests/localization_coverage.test.ts`, `docs/prd/masterwrought/phase-19-rulings-gate.md`, `docs/prd/masterwrought/phase-20-release-fill-and-reclose.md`
- **Same-change obligations:** MECHANISM CONFIRMED in source: addDictScope at scripts/i18n_scan.mjs:258-287 marks a locale translated on isPresent(dict[lang][key]), and DICT at src/ui/sim_i18n.ts:10067-10079 builds every locale as { ...baseEnTable, ...BASE_DICT[lang], ...PET_DICT[lang], ...RAID_BOSS_DIALOGUE_DICT[lang], ...IGNIVAR_DICT[lang] }, so a value is always present; the real check must read the per-locale SOURCE table (BASE_DICT[lang] and its siblings), never the assembled DICT. THE BACKLOG, MEASURED IN THIS WORKTREE rather than estimated: baseEnTable carries 762 keys; BASE_DICT fills are 180 each for cs_CZ, nl_NL, pl_PL, id_ID, tr_TR, sv_SE, vi_VN and da_DK, 482 each for es, es_ES, fr_FR, fr_CA, it_IT, de_DE and pt_BR, and 491 each for zh_CN, zh_TW, ko_KR, ja_JP and ru_RU. A real-presence scanner therefore turns about 7,971 sim-scope rows pending (8 x 582 plus 7 x 280 plus 5 x 271) before server scope's 165 keys are counted. That is the whole point of the fix AND the reason it cannot land silently: the release-tier gate (I18N_RELEASE_TIER=1) hard-fails on any pending row, so this change hands Phase 20 a fill package roughly an order of magnitude larger than the one that phase is currently scoped against. Price the option-2 staging step (a copied-English detector for the sim scope, the H3b shape that tests/i18n_admin_catalog.test.ts:10-12 already runs for admin and server) as the executable half if the maintainer does not want the gate red now. REGEN, never hand-edit: npm run i18n:gen (i18n:build, i18n:admin, i18n:scan) rewrites src/ui/i18n.status.json, and the PreToolUse hook blocks direct edits to the resolved-i18n artifact class; npm run i18n:worklist re-derives the fill briefs after. NOT OWED: no wiki regen, no deed rows, no Reliquary pages, no art parks, no census row (scripts/ is a census root but no new export is minted by a function-body change; add a merge-deletion-list row only if a new helper is exported), no parity golden, no monolith ceiling. RULED LINE ANCHOR (grep -c = 1): '  D148   sim-scope-pending-is-unreachable  ' in docs/prd/masterwrought/phase-19-rulings-gate.md; no farming handoff-table row (seed is 'new', raised by the Phase 18 QA i18n-matcher review). Ruling id qr-19-sim-scope-pending-is-unreachable.
- **AMENDED IN PLACE 2026-09-02 at execution, thirteen corrections; THE MEASURED BACKLOG IS 4,569, NOT 7,971, the fix needs one new export, and the fork was put to the maintainer on the measured numbers.** (1) The RULED-line anchor is pipe-flattened and greps 0 as written; the form that greps 1 is grep -cF '| D148 | sim-scope-pending-is-unreachable |'. (2) src/ui/i18n.status.json and src/ui/i18n.status.summary.json are gitignored, untracked artifacts regenerated by pretest and the CI i18n:gen step (determinism pinned by 'regenerates status.json + the summary byte-identically across two perturbed-env runs' in tests/i18n_status_registry.test.ts), so 'regenerate the status registry' commits nothing: the registry-side deliverable is scripts/i18n_scan.mjs, and the PreToolUse hook covers only *.generated.ts and i18n.resolved.generated/, not the registry. (3) DICT in src/ui/sim_i18n.ts spreads FIVE per-locale sources after baseEnTable (BASE_DICT, PET_DICT, the single 'log.arenaQueueAutoLeave1v1' row from ARENA_QUEUE_AUTO_LEAVE_1V1, RAID_BOSS_DIALOGUE_DICT, IGNIVAR_DICT), and the addDictScope closure in scripts/i18n_scan.mjs iterates NON_EN (21 locales, en_CA included) with no DIALECT_BASE branch; a real-presence check must consult all five sources and decide en_CA. (4) THE FIX NEEDS A NEW EXPORT: the five source tables are unexported consts, and the scanner loads sim_i18n through an esbuild stub, so the honest fix exports one helper from src/ui/sim_i18n.ts (simDictProvidedKeys, the per-locale union of keys the five sources carry) for the scanner to read, which mints a symbol-census row (an Explained extras row in docs/prd/masterwrought/merge-deletion-list.md's Phase 19D table, Class exports, no Path since src/ui is in census scope) and owes a green tests/merge_audit_symbol_census.test.ts and a census RESULT PASS; the unit's 'no new export is minted by a function-body change' described the wrong path. (5) THE BACKLOG, RE-MEASURED at a1b435ff85 (scratchpad d148_measure.mjs, an esbuild bundle exposing the source tables): DICT.en holds 762 keys = baseEnTable 721 + PET_DICT 41 (the unit's 762 is the assembled table, not baseEnTable); source presence per locale runs 516 to 556 (cs_CZ 516; the other seven newlocales blocks 521; fr_FR, fr_CA, it_IT, de_DE, pt_BR 524; es, es_ES 554; the five non-Latin 556; BASE_DICT alone reads 474 / 479 / 482 / 491 because each newer block also spreads its BASE_NEW rows, never 180), so the honest fix turns 4,569 sim rows pending across the 20 non-English locales, or 5,086 if en_CA is left without the main scope's English-dialect rule (its block carries copied English only); every missing row is byte-identical English today (4,569 = 4,569), 4,429 of them under H3b's predicate and 4,414 under M16's. (6) THE DIALECT RULE: en_CA folds onto en exactly as the main and admin scopes do (DIALECT_BASE), so it reads translated; es_ES and fr_CA are read from their OWN blocks (the sim DICT carries no dialect inheritance, the Phase 20 starter prompt records it). (7) Server scope is NOT a backlog: 166 keys (not 165), every locale table literal (plus the SERVER_NEW blocks), 15 copied-English rows all blocked cognates; a real-presence scanner surfaces sim rows only, and the server half stays on the presence rule it already has. (8) Phase 20's package is the registry's pending set, already 9,941 rows (main 9,223 + admin 718); +4,569 is about 1.5x, not an order of magnitude. (9) The H3b shape is checkNoCopiedEnglish in tests/localization_fixes.test.ts, pinned by 'H3b: server DICT has no un-allowlisted copied-English' and its admin twin, BOTH release-tier only, so a sim twin (option 2) reds the release tier the moment it lands and keeps only the PR tier green; tests/i18n_admin_catalog.test.ts:10-12 is a pointer comment; a sim arm would also owe a sim scope in scripts/i18n_blocked_seed.mjs and a re-scoped registry pin 'main/sim blocked rows exist ONLY for retired main keys'. (10) tests/localization_coverage.test.ts has no stake in the DICT scopes; the file that matters is tests/localization_fixes.test.ts, whose 'S2: sim_i18n DICT is complete across all locales' must stay green (the DICT stays dense while the registry goes sparse). (11) The registry pins the fix moves: 'the pending set is empty (release tier: no untranslated key may ship)' goes red under I18N_RELEASE_TIER=1 as soon as sim rows read pending (the expected Phase 20 red, the release-i18n job's own contract), and a new arm pins that a sim key no locale source carries reads pending while a carried one reads translated. (12) Provenance: the 52 rift rows entered src/ui/sim_i18n.ts inside the tenth-sync merge c659387f9a as the Phase 18 QA fix-round edit that merge carried; f14f015a7b added the pin. (13) phase-20-release-fill-and-reclose.md carries no D148 line today; the hand-written handoff paragraph this unit adds is spelled out in the unit's commit.
- **Reviewers:** cross-platform-sync, gate-integrity-reviewer, qa-checklist
- **Depends on:** nothing
- **Estimate:** 4h

### D149

Settle whether the 52 rift mechanic display names get real locale fills or join the brand-allow precedent, then carry the chosen shape: under the recommended option 2 the packet lands the BRAND_ALLOW extension for the non-Latin five plus a hand-written Phase 20 worklist entry, and the 15 Latin locales' 780 values are filled on the Phase 20 lane.

- **Files:** `tests/i18n_completeness.test.ts`, `src/ui/sim_i18n.ts`, `tests/sim_i18n_rift_mechanics.test.ts`, `docs/prd/masterwrought/phase-20-release-fill-and-reclose.md`, `docs/prd/masterwrought/phase-19-rulings-gate.md`
- **Same-change obligations:** LIVE SHAPE verified: the 52 English rows sit in baseEnTable at src/ui/sim_i18n.ts:761-810 onward ('aura.rift*' and 'mechanic.rift*'), derived and pinned from RIFT_MOBS by tests/sim_i18n_rift_mechanics.test.ts so a newly authored mechanic reds. THE PRECEDENT IS ASYMMETRIC, as the row says: BRAND_ALLOW at tests/i18n_completeness.test.ts:158-190 already lists the 16 abilityUi.cast.rift_* ids as fantasy proper nouns exempt from the non-Latin leak guard, while those same ids DO carry real Latin fills, so option 3 (English everywhere) would make the shipped 16 the anomaly. WHERE SIM-SCOPE FILLS ACTUALLY LAND: not src/ui/i18n.locales/ but the per-locale BASE_DICT blocks inside src/ui/sim_i18n.ts (block starts verified: es :1296, es_ES :1809, fr_FR :2325, fr_CA :2853, it_IT :3590, de_DE :4107, zh_CN :4629, zh_TW :5123, ko_KR :5617, ja_JP :6124, pt_BR :6641, ru_RU :7153, cs_CZ :7676, nl_NL :7867, pl_PL :8063, id_ID :8258, tr_TR :8453, sv_SE :8644, vi_VN :8837, da_DK :9029); Phase 20's own starter prompt already records that sim DICTs carry NO dialect inheritance, so every base locale is filled explicitly. MUST LAND AFTER D148 or in place of its carrier: until the scanner is fixed these 52 keys report translated with 0 pending in every locale, so the fill needs a HAND-WRITTEN worklist entry in phase-20-release-fill-and-reclose.md, which is a same-change obligation of whichever option is chosen. NOT OWED: no wiki regen, no deed rows, no Reliquary pages, no art parks, no census row, no parity golden, no monolith ceiling; M16 is the substance of the row rather than a rider. RULED LINE ANCHOR (grep -c = 1): '  D149   rift-mechanic-names-translate-or-not  ' in docs/prd/masterwrought/phase-19-rulings-gate.md; no farming handoff-table row. Ruling id qr-19-rift-mechanic-names-translate-or-not.
- **AMENDED IN PLACE 2026-09-02 at execution, ten corrections; TWO FALSIFY THE PREMISE, so the row was ESCALATED to the maintainer in session rather than executed as priced.** (1) The 52 rows are the baseEnTable block under the comment 'Rift mob and boss mechanic names' in src/ui/sim_i18n.ts (29 'aura.rift*' plus 23 'mechanic.rift*', 777-830 at a1b435ff85, not 761-810), pinned by 'every surfaced rift mechanic name resolves through the aura matcher' in tests/sim_i18n_rift_mechanics.test.ts, which derives the set from RIFT_MOBS with vacuity floors and never pins the number 52 (an observed count). (2) BRAND_ALLOW is the Set inside 'non-Latin player surfaces ship no untranslated English' in tests/i18n_completeness.test.ts (158-198, the 16 cast ids its last entries). (3) The RULED-line anchor is pipe-flattened; grep -cF '| D149 | rift-mechanic-names-translate-or-not |' = 1. (4) tr_TR renders 'Buzul Mezarı' (dotless ı; the ASCII spelling greps 0); ead5768827 added the 16 keys English everywhere plus the BRAND_ALLOW entries, and the fills, Latin AND non-Latin, landed in 0ff751e676. (5) THE PREMISE IS FALSE: all 16 abilityUi.cast.rift_* ids carry REAL translations in every locale INCLUDING the five non-Latin (0ff751e676, the v0.32.0 release fill of 2026-07-30: 冰川之墓, 氷河の墓, 빙하의 무덤, Ледяная могила); the BRAND_ALLOW rift entries are a dead exemption (the guard never consults them) and the shipped precedent is 'translated everywhere', option 1, not the asymmetric option 2 the row calls the consistent reading; option 2 would introduce a NEW English-in-CJK precedent that no shipped rift name follows. (6) The option-2 carrier as written is inert: M16 scans only the main catalog tables (TABLES / enFlat), never sim_i18n, so adding sim keys to BRAND_ALLOW exempts nothing, and no sim-scope non-Latin leak guard exists to exempt them from; under the D148 fix the registry itself carries the 52 keys in 20 locales as pending, so the hand-written Phase 20 entry becomes a belt-and-braces note rather than the only carrier. (7) For the eight newer locales (cs_CZ, nl_NL, pl_PL, id_ID, tr_TR, sv_SE, vi_VN, da_DK) the bulk of the existing sim fills live in BASE_NEW (src/ui/sim_i18n.newlocales.ts), spread into each BASE_DICT block and guarded by tests/sim_i18n_base_new_passthrough.test.ts, so a fill for those eight may land in either place (a block row overrides the spread); the Files list names src/ui/sim_i18n.newlocales.ts as the alternative carrier. (8) The 20-locale / 780 / 1,040 arithmetic assumes en_CA excluded as the English dialect (the registry and BASE_DICT carry 21 non-en locales); stated. (9) The source row's seed is the bucket-A closure row for rift-mechanic-name-matcher-gap in state.md's '### THE BUCKET-A LIST (done or carried per item)', which forwards to D148/D149; no document records an 'i18n-matcher review' by that name. (10) Provenance: the 52 baseEnTable rows and their AURA_NAME_KEY rows entered in the tenth-sync merge c659387f9a (the inseparable sync-plus-QA edit); f14f015a7b added only the pin and the goldens.
- **Reviewers:** cross-platform-sync, frontend-seam-reviewer, qa-checklist
- **Depends on:** D148
- **Estimate:** 6h

### D150

THE ROW'S PREMISE IS STALE: the Drowned Temple deploy-window alias is already live in the tree, so the executable work is not adding it but amending the row in place, adding the pin it shipped without, and restamping the arm with the ruling id and its retirement condition.

- **Files:** `tests/localization_fixes.test.ts`, `src/ui/hud.ts`, `docs/prd/masterwrought/phase-19-rulings-gate.md`
- **Same-change obligations:** THE FINDING, verified twice: the alias arm IS at HEAD, src/ui/hud.ts:14563-14582, matching the pre-reword sentence as an escape (the escape sequence, never the raw byte) and returning dungeonText('drowned_temple','enterText'); its own comment already says 'D150 carries the ruling'. It entered on the tenth sync merge c659387f9a (2026-08-31, conflict resolution); the branch parent 2ab5c2f7d0 does NOT contain it, which is why the row was priced against a tree that no longer exists. So AMEND THE ROW IN PLACE with a dated 2026-09-01 line rather than rewriting it, and re-scope the ruling from 'add the alias' to 'ratify the landed alias and close its two gaps'. WHAT IS GENUINELY MISSING: no test anywhere pins it (grep for 'air turns to cold water' and 'u2014 the air' across tests/ and scripts/ returns zero hits), so the arm can be dropped by any future merge in silence; the pin belongs beside the existing precedent at tests/localization_fixes.test.ts:2017-2030 ('the phase 03 wire-carried renames keep legacy aliases for the deploy window'), whose sibling at :2008-2015 shows the retirement shape ('the retired Venomfire Vigor deploy-window alias stays deleted'). THE THREE COPIES, so the pin cites the right one: content src/sim/content/temple.ts:856 (the match key, emitted raw by src/sim/instances/dungeons.ts:717), catalog src/ui/i18n.catalog/merge.ts:4074 (what renders), alias src/ui/hud.ts:14579. MONOLITH CEILING, load-bearing here: tests/monolith_budget.test.ts:431 pins src/ui/hud.ts at 18728 against a live 18708, so the comment restamp has 20 lines of slack and must be line-neutral or near it; the ratchet forbids raising the ceiling. NOT OWED: no wiki regen, no new i18n key and so no M16 fills, no deed rows, no Reliquary pages, no art parks, no census row, no parity golden. RULED LINE ANCHOR (grep -c = 1): '  D150   drowned-temple-entertext-deploy-alias  ' in docs/prd/masterwrought/phase-19-rulings-gate.md; no farming handoff-table row. Ruling id qr-19-drowned-temple-entertext-deploy-alias.
- **AMENDED IN PLACE 2026-09-02 at execution, eleven corrections; one changes the price (zero slack), the re-scoped premise survives.** (1) src/ui/hud.ts sits at ZERO slack: tests/monolith_budget.test.ts (the `file: 'src/ui/hud.ts'` row, 'Exact count, zero slack') pins 18716 against a live 18716, lowered from 18728 at 19D (D129) and again at the 19D review round; the restamp must be strictly line-neutral (words rewritten inside the existing comment lines). (2) The alias arm is at 14569-14588 inside `private localizeSystemText(text: string)` (the comment block 14569-14582 with 'D150 carries the ruling' at 14578, the `if` at 14583-14588, the escape-spelled literal at 14585), not 14563-14582 / 14579; cite by symbol. (3) The RULED-line anchor is pipe-flattened; grep -cF '| D150 | drowned-temple-entertext-deploy-alias |' = 1. (4) Provenance: the arm landed in the tenth-sync merge c659387f9a as a Phase 18 QA fix-round edit the merge could not separate (that commit's own PROVENANCE paragraph names src/ui/hud.ts among thirteen such files), and at c659387f9a it still matched the LIVE content sentence: it became a deploy-window alias only in the next commit, f14f015a7b, which is both the reword (temple.ts and merge.ts moved from the em dash to the colon) and the commit that AUTHORED the D150 row, so the row's premise was stale at its own writing, not made stale by a later merge; the provenance record is state.md's 'THE CATALOG EM DASH FIX: ten rows, and the ELEVENTH WAS WRONG TO LEAVE' bullet, which says the fix landed 'with a deploy-window alias (below)' and no record follows. (5) The one-byte-divergence proof the source row cites is recorded nowhere in the tree; the mechanism is verifiable from the code (src/sim/instances/dungeons.ts emits dungeon.enterText raw; localizeSystemText matches text === dungeon.enterText by exact bytes). (6) The landed arm is six code lines behind a fourteen-line comment, not 'three lines'. (7) The amendment carries its execution date, 2026-09-02. (8) THE PIN SHAPE: the S3 parsers in tests/localization_fixes.test.ts (armExactKeys reads quoted `'...':` keys, armRegexes reads `/.../.exec(text)`) cannot see a template-literal `text ===` arm, so the pin is BEHAVIORAL through the Hud.prototype harness that file already uses for localizeLootText (Object.create(Hud.prototype)): the private method is driven with the pre-reword sentence spelled as an escape and must resolve to dungeonText('drowned_temple', 'enterText'), never the raw legacy bytes, beside 'the phase 03 wire-carried renames keep legacy aliases for the deploy window'. (9) The arm comment says 'the arena queue line's ellipsis twin below', but the twin (the two 'hud.logs.arenaJoin' rows) sits ABOVE, inside the `exact` map; the line-neutral restamp corrects it. (10) THE RETIREMENT HORIZON: the restamp names the release this branch integrates onto (release/v0.42.0, per the sync titles) and does not copy the drifted 'integrates onto release/v0.41.0' horizon that the two phase 03 alias comments in src/ui/sim_i18n.ts and the alias test's comment still carry (origin already holds release/v0.41.0 through v0.41.2); those three drifted comments are a separate rider for the maintainer, recorded in the QA doc. (11) The unit cites the localization_fixes precedents by range (:2008-2015, :2017-2030); by name they are 'the retired Venomfire Vigor deploy-window alias stays deleted' and 'the phase 03 wire-carried renames keep legacy aliases for the deploy window'.
- **Reviewers:** cross-platform-sync, frontend-seam-reviewer, qa-checklist
- **Depends on:** nothing
- **Estimate:** 1.5h

### D161

Retire guide.profPages.toolsNoteThreeRods and ship a successor key so the pending machinery carries the note (the 11i re-key mechanism), which forces five fresh non-Latin translations of the four-paragraph tools note in the same change instead of leaving ja_JP, ko_KR, ru_RU, zh_CN and zh_TW rendering three retired claims on every gathering wiki page.

- **Files:** `src/ui/i18n.catalog/guide.ts`, `src/guide/pages/professions_gathering.ts`, `tests/guide.test.ts`, `scripts/i18n_retired_keys.mjs` (RETRACTED 2026-09-02 at the review round: the delete-outright shape touches it not at all), `src/ui/i18n.locales/ja_JP.ts`, `src/ui/i18n.locales/ko_KR.ts`, `src/ui/i18n.locales/ru_RU.ts`, `src/ui/i18n.locales/zh_CN.ts`, `src/ui/i18n.locales/zh_TW.ts`, `src/ui/i18n.status.json`, `src/ui/i18n.catalog/translation_keys.generated.ts`, `src/ui/i18n.resolved.generated/`, `docs/prd/masterwrought/farming/state.md`, `docs/prd/masterwrought/state.md`, `docs/prd/masterwrought/progress.md` (the phase-close roll-up row, not the unit commit), `docs/prd/masterwrought/merge-deletion-list.md` (ADDED 2026-09-02 at the review round: a key rename owes a rename row and an explained-extras row; the unit's 'no census row' NOT-OWED claim was wrong and the census caught it in 633e6c233a)
- **Same-change obligations:** SAME-CHANGE OWED: five M16 non-Latin fills of the full four-paragraph note (roughly 700 English words each), because M16 (tests/i18n_completeness.test.ts) reds the moment the five non-Latin locales English-fill the successor; a RETIRED_KEYS row with its reason in scripts/i18n_retired_keys.mjs if the old value is kept (the enchantsNote shape) or an outright delete the way profPages.toolsNote was done; both English pins in tests/guide.test.ts re-pointed; regen via npm run i18n:gen (translation_keys.generated.ts, the 21 resolved slices, pending.ts, i18n.status.json) plus the i18n freshness gate; the 15 Latin rows land pending for the Phase 20 fill. NEVER hand-edit the generated artifacts. NOT OWED: no npm run wiki:content and so no tests/guide.test.ts freshness cost (catalog prose, not generated content), no deed row, no Reliquary page, no committed WebP art, no census row, no parity golden, no world_api parity pin, no monolith ceiling. REVIEW SKIPS per the matrix: cross-platform-sync is skipped (pure i18n catalog work), privacy-security-review and migration-safety do not apply. ANCHOR (grep -c = 1) in /Users/fernando/Documents/wocc-masterwrought/docs/prd/masterwrought/state.md: 'still carry the old claims on that key'; the dated RULED line goes AFTER that paragraph's last line (the 11j QA open item 12 block). HANDOFF ROW in /Users/fernando/Documents/wocc-masterwrought/docs/prd/masterwrought/farming/state.md, match the row opening 'Gathering wiki tools note: five stale non-Latin fills after an English reword' (grep -c = 1); its status cell begins 'open ruling-owed.' STATUS CELL REPLACEMENT: replace only the leading token 'open ruling-owed.' with 'CLOSED 2026-09-01 by ruling qr-19-toolsnote-stale-nonlatin-fills (Phase 19, under qr-19-best-for-project): the key is RETIRED AND RE-KEYED, with the five non-Latin fills riding the same change.' and leave every following sentence of that cell standing as the record.
- **AMENDED IN PLACE 2026-09-02 at execution, twelve corrections; ONE DECIDES THE RETIREMENT SHAPE (delete outright), the recommended arm survives.** (1) Files: src/ui/i18n.status.json is gitignored and untracked (regenerated by pretest and the CI i18n:gen step); the committed regen outputs are src/ui/i18n.catalog/translation_keys.generated.ts and src/ui/i18n.resolved.generated/ (23 locale slices, the 22 LOCALES plus en_XA, plus pending.ts, index.ts and loaders.ts), and a key deletion cannot be parked (the stale slices stop compiling, TS2353). (2) The note is THREE paragraphs (two \n\n breaks, about 725 English words), rendered as three <p> by paras() in src/guide/pages/ui.ts; the five existing non-Latin fills are three-paragraph too and the successor's fills mirror that shape. (3) THREE pins consume the key in tests/guide.test.ts, not two: 'the shared tools note is TRUE of farming, not only of the node trades' (the clause pins, English), 'resolves the new professions keys in English' (the prose() resolution pin), and 'publishes the tool-gate thresholds through placeholders in EVERY locale', which is NOT English-only: after its English arms it walks every resolved locale slice under src/ui/i18n.resolved.generated/ and reads the FIRST line containing 'toolsNote', so the successor's name must keep the 'toolsNote' stem AND no older toolsNote row may survive above it in catalog order. (4) THAT DECIDES THE RETIREMENT SHAPE: the old key is DELETED outright (the a60aa9a4d7 shape, no RETIRED_KEYS row) and its five non-Latin overlay rows are replaced by the successor's rows in the same change, so every slice carries exactly one toolsNote row; a kept-and-retired key would have made that pin read the stale row in every locale. The successor is guide.profPages.toolsNoteFiveLadders (the note describes five ladders: the three node trades, farming and fishing). (5) The delete shape still owes run-and-confirm-clean of tests/guide_key_coverage.test.ts ('renders every catalog key that is not explicitly retired or off-sweep') and tests/i18n_status_registry.test.ts. (6) ANCHOR: 'still carry the old claims on that key' sits in the 11j QA ledger's 'AND THE EXPOSURE IS RECORDED because no gate can see it' paragraph under '### WHAT THIS AUDIT FOUND AND FIXED', not in open item (12); open item (12) is '(12) **THE REWORDED WIKI KEY'S FIVE STALE NON-LATIN FILLS.**' under '### OPEN ITEMS, ALL THE MAINTAINER'S', ending 'rule that the key is re-keyed instead.' (grep -cF = 1); the RULED line lands after the exposure paragraph and a dated pointer after open item (12)'s last line, so both records close (item (11) beside it already carries its own RULED line). (7) GIT: the five non-Latin rows were written at a60aa9a4d7 (the 11i re-key; the ja_JP, zh_CN and zh_TW rows were touched again at ba37787cb6, ko_KR and ru_RU were not), and the English has moved FIVE times since a60aa9a4d7 (ba37787cb6 at the 11i QA late round, then dad384b5c0 at the 11j QA, 5d85a728bb, 76088becfe, 428f67ced7), none touching src/ui/i18n.locales/ (each also carried tests or sim content beyond 'only the catalog'); a60aa9a4d7's 18 overlay files were 13 Latin overlays dropping the seven retired keys' rows (91 rows) plus the five non-Latin re-filled. (8) The source row's ledger addresses have drifted: the handoff row is farming/state.md:556, open item (12) at state.md 13348-13351, the 11i mechanism record at 14689-14705 (anchors 'seven STATICALLY consumed keys were RETIRED AND RE-KEYED', 'rose 7677', 'what the re-key BUYS is that every'; its long quotes are line-wrapped). (9) The guards cited by range: 'the shared tools note is TRUE of farming...' spans 2822-2896 and the :4241 pin sits inside 'resolves the new professions keys in English' (from 4116). (10) M16 is 'non-Latin player surfaces ship no untranslated English' (a WORDY leaf byte-identical in zh_CN/zh_TW/ja_JP/ko_KR/ru_RU, BRAND_ALLOW and devCommand.* excluded); the successor is wordy, so the five fills ride in-change as the unit says. (11) The ruling id qr-19-toolsnote-stale-nonlatin-fills is minted by this unit (no gate-table row exists past D151, the D152+ precedent): the dated RULED line and the status-cell flip land in the same commit, dated 2026-09-02, not the pre-written 2026-09-01. (12) The five fills were written with the i18n-locale-fill conventions and the glossary and are FLAGGED for the maintainer's read in phase-19f-qa.md; they are not presented as reviewed translations.
- **ADDENDUM 2026-09-02 at the review round, three more corrections.** (13) The 'no census row' NOT-OWED claim was wrong: a key rename owes a rename row plus an explained-extras row in merge-deletion-list.md (landed in 633e6c233a, then updated for the second re-key). (14) The note's English was itself false in one count (THREE 20-copper land starters where the fenced set is FOUR, the Garden Hoe included: TIER_1_TOOLS in tests/professions_starter_tools.test.ts), misleading in one place (the allotment farmer stands at the Eastbrook allotments, a hub, not at a tool counter) and incomplete in one (the rods' Delve Marks route at the Drowned Litany counter), and the five fresh fills reproduced all three; the successor was re-keyed once more, the same delete-outright shape, to guide.profPages.toolsNoteFourStarters with fresh five non-Latin fills (tool and rod names in Latin to match the tool table beneath the note), a fourth clause arm deriving the fenced-starter count from ITEMS, and the every-locale placeholder pin reading the exact key with exactly one toolsNote row per slice. (15) Carried for the maintainer, not taken: the gathering tool table renders every tool name as untranslated English in all locales (src/guide/pages/professions_gathering.ts, esc(tool.name)) where the crafting page resolves entities.items.<id>.name; the fills keep Latin tool names to match the table until that is fixed. (16) ADDED 2026-09-02 at the third round: the toolsNoteFourStarters fills used a Delve Marks term the overlays do not (ja_JP 探索の印, zh_CN 秘境印记, zh_TW 祕境印記); the established terms (デルヴの刻印, 探秘印记, 秘探印記) were restored in all three.
- **Reviewers:** frontend-seam-reviewer, content-obligations-reviewer, qa-checklist
- **Depends on:** nothing
- **Estimate:** 6h

### D169

Retire the shared guide.profPages.rareBody and mint a successor that names farming's golden harvest beside the three node-trade windfall flavors, the new-key treatment the ledger itself calls for, rather than rewording a key all 21 locales already carry as translated.

- **Files:** `src/ui/i18n.catalog/guide.ts`, `src/guide/pages/professions_gathering.ts`, `scripts/i18n_retired_keys.mjs`, `src/ui/i18n.locales/ja_JP.ts`, `src/ui/i18n.locales/ko_KR.ts`, `src/ui/i18n.locales/ru_RU.ts`, `src/ui/i18n.locales/zh_CN.ts`, `src/ui/i18n.locales/zh_TW.ts`, `src/ui/i18n.status.json`, `src/ui/i18n.catalog/translation_keys.generated.ts`, `src/ui/i18n.resolved.generated/`, `tests/guide_key_coverage.test.ts`, `docs/prd/masterwrought/farming/state.md`
- **Same-change obligations:** MAINTAINER VALUE NEEDED FIRST: the exact English phrase for the crop windfall, to sit beside the three hand-authored flavors. The key is SHARED across mining, logging, herbalism and farming, so it is one copy call for four pages. SAME-CHANGE OWED: the successor key beside the current value carrying the same two placeholders (suffix naming per the specimenBodyFamilies and harvestBodyFamilies precedents on this very page); one line re-pointed in the rare section builder; a Reworded successors entry in scripts/i18n_retired_keys.mjs with its reason (the three consumers of RETIRED_KEYS must always agree); the five M16 non-Latin fills, since the value is wordy; npm run i18n:gen, which flips the 21 rareBody rows from translated to blocked with a retired reason rather than pending and lands the successor's 16 Latin rows pending for Phase 20; the no-live-reference arm in tests/guide_key_coverage.test.ts then proves the retirement honest (verify, no edit expected). REFUSED OUTRIGHT: the in-place reword, which manufactures a fresh instance of exactly the invisible-staleness class D126 names, on a 100 percent filled key, immediately before the Phase 20 fill. NOT OWED: no npm run wiki:content (the page renders t() at request time, no generated field moves), no deed, no Reliquary page, no art, no census row, no parity golden, no world_api member, no monolith ceiling (the catalog is data, exempt from module-first). REGEN ORDERING: this row, D161 and D171's i18n rider all rewrite src/ui/i18n.status.json and the 21 resolved slices, so batch them and regenerate once, or land them in that order. ANCHOR (grep -c = 1) in /Users/fernando/Documents/wocc-masterwrought/docs/prd/masterwrought/farming/state.md: 'the reword needs the new-key treatment plus fills'; this is inside the handoff row itself, and the dated close goes in the status cell. HANDOFF ROW: match the row opening 'guide.profPages.rareBody (shared across the four gathering pages, filled translated key)'; its status cell reads 'open, deferred (reword landmine)'. STATUS CELL REPLACEMENT: 'open, deferred (reword landmine)' becomes 'CLOSED 2026-09-01 by ruling qr-19-rarebody-reword-landmine (Phase 19, under qr-19-best-for-project): retire and re-key with the maintainer's crop-windfall wording, five M16 fills in-change, the 16 Latin rows to the Phase 20 fill.'
- **AMENDED IN PLACE 2026-09-02 at execution, ten corrections; two fix the regen mechanics and one strengthens the recommended arm; the maintainer's wording was asked for in session.** (1) The handoff row is at farming/state.md:587 (the unit's grep anchors hold); farming.ts by symbol: rollGatherRareEvent(ctx.rng, 'crop') in the harvest path (now :918), GATHER_RARE_EVENT_YIELD_MULT applied at :1073-1074, the 'A golden windfall lands SIGNED' comment at :1096; the guide.profPages.farm block opens at guide.ts:3068; the 'Reworded successors' block heads scripts/i18n_retired_keys.mjs at :37-38 with entries :39-45. (2) RETIRING FLIPS ZERO ROWS: every one of rareBody's 21 rows is PROVIDED (18 overlay rows plus the es_ES, fr_CA and en_CA inheritances) and scripts/i18n_scan.mjs keeps a provided row 'translated' whatever RETIRED_KEYS says (the specimenBody precedent: 21 translated, 0 blocked; blocked rows appear only for UNPROVIDED rows of a retired key, the gatherDeeds.farming outlier); the retirement's registry effect is nil, its effect is the guide_key_coverage allowlist and i18n_build's pending exclusion. (3) The successor lands 15 Latin rows pending, not 16 (en_CA inherits the English leaf as translated), matching farmingSown, toolsNoteThreeRods, tableBodyOneMeal and D161; the status cell reads '15 Latin rows'. (4) THE FIVE NON-LATIN rareBody FILLS ARE ALREADY SHORT OF THE ENGLISH (executor's read, not in either text): each renders only the chance, the multiplier, the signature and the zone announcement and omits the three flavor names and the collector's-mark sentence the English carries (authored short in 6adc55b5a5, the same commit as the English, so a translation gap rather than a reword), so 'the 21 rows are currently accurate' holds for the 15 Latin and 3 dialect rows only, the successor's five fills are full fresh translations, and the case for the new-key shape is stronger than priced. (5) Files: src/ui/i18n.status.json is gitignored and untracked; the committed regen outputs are translation_keys.generated.ts and the src/ui/i18n.resolved.generated/ slices (21 locale slices plus en.ts, en_XA.ts and pending.ts); D171 is a 19G unit, so the cross-wave batching note is moot; within 19F the order is D161 then D169, a sequencing choice over shared files (guide.ts, professions_gathering.ts, the five overlays), not a logical dependency (the source row's contingentOn=none). (6) The retirement arms are 'has no retired key that is still referenced in src/' and 'has no retired key the render sweep actually RENDERED' (no test named 'has no live reference'); tests/i18n_status_registry.test.ts is the fourth RETIRED_KEYS consumer ('main/sim blocked rows exist ONLY for retired main keys' and 'no retired key carries a pending row anywhere'), both green here. (7) The ruling id qr-19-rarebody-reword-landmine is minted by this unit (no gate-table row): the dated close in the status cell and the state.md and progress.md ledger rows land in the same commit, dated 2026-09-02, after the maintainer's crop-windfall wording was given in session. (8) D126 is a phase-19-rulings-gate.md row (mirrored in state.md), not a new-rows row; docs/ mentions of the key now include this unit and its source row. (9) grep over src/guide/ for 'flavors' hits the generated type and the emitted data object; no page module consumes the record, which is the substantive point; isFarming also gates the farming-only Cooking related link and never rareSection(). (10) The five fills were written with the i18n-locale-fill conventions and the glossary and are FLAGGED for the maintainer's read in phase-19f-qa.md.
- **Reviewers:** frontend-seam-reviewer, content-obligations-reviewer, qa-checklist
- **Depends on:** D161
- **Estimate:** 3.5h

### D174

Ratify the 13-catch timing as shipped (the mechanic is already pinned in both directions and nothing in the tree moves) and take the copy rider: reword prog_first_harvest's English desc and the three gatherDeeds guide lines so they stop promising the first node of any trade, then hand-fill the 18 deed locales and the 18 guide overlays that nothing reds.

- **Files:** `src/sim/content/deeds.ts (line 179, desc 'Harvest your first gathering node.'; verified live, trigger and renown untouched)`, `src/ui/i18n.catalog/guide.ts (lines 3011, 3013, 3015: the mining, logging and herbalism gatherDeeds lines, each opening 'Your first node of any trade earns Fruits of the Field'; verified. The fishing line at :3017 already avoids the claim and needs no edit)`, `src/ui/deed_i18n.locales/ (all 18 files carry a prog_first_harvest desc: cs_CZ, da_DK, de_DE, es, fr_FR, id_ID, it_IT, ja_JP, ko_KR, nl_NL, pl_PL, pt_BR, ru_RU, sv_SE, tr_TR, vi_VN, zh_CN, zh_TW; verified by grep)`, `src/ui/i18n.locales/ (the same 18 overlays carry gatherDeeds rows; en_CA, es_ES and fr_CA are divergence-only and carry none, verified by grep count)`, `src/guide/content.generated.ts (regenerate only, never hand-edit; see the correction in obligations)`, `docs/prd/masterwrought/state.md (dated RULED line)`, `docs/prd/masterwrought/phase-19-new-rows.md (dated in-place amendment to option 2's wiki claim)`, `docs/prd/masterwrought/progress.md`, `NO TEST EDIT: tests/professions_fishing.test.ts:1882-1924 pins the mechanic, not the desc, and stays green (re-read at HEAD); tests/deeds_content.test.ts:1275-1282 must simply pass on the new English`
- **Same-change obligations:** SAME-CHANGE OBLIGATIONS. i18n regen: npm run i18n:gen must run (the resolved bundles are generated artifacts and the PreToolUse hook blocks hand-edits). WIKI REGEN, and this is a CORRECTION to the row's own price that should be amended in place with a dated line rather than rewritten: option 2 says 'npm run wiki:content runs ... the deed's generated row is src/guide/content.generated.ts:3657-3662', which implies the artifact moves. It does not. The wiki deed emit at scripts/wiki/build_content.mjs:570-580 carries id, name, category, renown, feat and crest and deliberately NO desc (spoiler-safe), verified by reading src/guide/content.generated.ts:3656-3663 which has no desc field. So the obligation is run-and-confirm-clean under the tests/guide.test.ts freshness gate, not a regen with a diff. M16: applies only if the replacement English is wordy, in which case the non-Latin fills ride the same change. FROZEN_CATALOG_SHA256 does NOT re-baseline (the digest is over id, trigger and renown; tests/deeds_content.test.ts:818-820 records the prog_master_gatherer precedent verbatim). NO new deed record, NO Reliquary page, NO committed WebP (public/ui/deeds/prog_first_harvest.webp already ships), NO census row, NO parity golden move (tests/parity/golden/professions_fishing_session.json stays as minted because the mechanic does not move; only option 3 re-mints it), NO tests/monolith_budget.test.ts ceiling, R5 untouched (docs/design/power-verification.md has zero matches for fishing, gathering, proficiency or deed). MAINTAINER VALUE REQUIRED for this arm and it cannot be guessed: (a) the replacement English desc, which must be true for all four gathering trades at once (one land node for mining, logging and herbalism; four crops farming; thirteen catches fishing), and (b) whether the 18 deed fills and the 18 guide overlay fills ride this change or defer to the Phase 20 release fill. Nothing reds either way: D126's finding is that the reword-staleness gate is unsatisfiable at all three write sites in scripts/i18n_scan.mjs, so a deferred fill ships 18 locales translating superseded English silently. LEDGER ANCHOR, verified unique: grep -c -F '### ONE MORE FOR THE MAINTAINER, raised by the review rather than by the phase' docs/prd/masterwrought/state.md returns 1 (line 14478); the dated RULED (qr-19-prog-first-harvest-thirteen-catches, 2026-09-01) line goes AFTER that heading. No handoff-table row is owned by this row (checked farming/state.md). ARM SENSITIVITY, priced honestly: if the maintainer takes the bare ratify (the row's own recommendation), this row costs about 0.5 hours of ledger lines, lands NO code, and the wave slot returns roughly 4.5 hours; the English desc then stays false for fishing (13) and farming (4) and so does the guide prose. Option 3 (retune FISHING_GAIN_SCHEDULE band 0) is a different wave entirely (19A-content or a sim-values row) and reopens an executed decision to move when one renown-5 deed fires: it needs band-0 gain at or above 1, a 12.5x move, and reds the derived-winner equality at tests/professions_fishing.test.ts:1203 plus the per-band-hours ledger pin, so it is not planned here.
- **AMENDED IN PLACE 2026-09-02 at execution, nine corrections; ONE MOVES A PREMISE (D103 is already ruled) and one removes an obligation (M16 owes nothing here); the recommended ratify arm survives.** (1) The heading anchor is unique (count 1) and sits at state.md:14746 at a1b435ff85 (14478 was right one commit before this unit landed); cite by heading. (2) D103 IS ALREADY RULED: state.md carries 'RULED (qr-19-prog-first-harvest-bootstrap-deed, 2026-09-01, under qr-19-best-for-project): accepted as shipped', and its recorded rationale reads the current desc as truthful because 'a farm bed is not a GATHER_NODES node'; 'ask them in the same sitting' cannot happen, a reword arm's desc must not contradict that rationale (a desc that folds farming's four crops in re-opens it), and the unit's 'stays false for fishing (13) and farming (4)' is half wrong on the packet's own record: under D103's reading only fishing's thirteen catches make the desc loose. (3) M16 IMPOSES NO SAME-CHANGE FILL on this unit: the five non-Latin overlays already carry translated gatherDeeds.mining/logging/herbalism rows, so a reword leaves them stale-but-translated (D126's silent case) whatever the new English is, and the deed desc is outside M16 entirely (deed English resolves from the catalog def; the 18 src/ui/deed_i18n.locales/ chunks are their own channel, guarded by the release-tier arms of tests/deed_i18n.test.ts); the 18 + 18 fills are purely value (b). (4) The i18n regen is owed by the guide.ts half only; the 18 deed chunks are hand-edited, not regenerated, and not hook-covered. (5) The source row's state.md addresses have drifted: the hand-back block at 14746-14754, the golden-prediction post-mortem at 14111-14122 (both quotes line-wrapped; the forms that grep are 'at 0.08 a catch the amount-1 gathering trigger now' and 'DEED-TRIGGER change wherever a deed counts the quantity that rate feeds'), the '13 catches' roll-ups at 12732, 13312, 13731 and 16226, the '### OPEN ITEMS: THE THIRTEEN ARE THE MAINTAINER'S, AND THIS PHASE ADDS NONE' section at 18867-18878 (which since 2026-09-01 carries the qr-19-thirteen-maintainer-open-items RULED line naming D174), DECISION F at 13872 with '### THE CASTS-TO-200 MODEL (DECISION F), REPRODUCIBLE FROM THIS DOC ALONE' at 13874-13938; FARMING_GAIN_SCHEDULE opens at src/sim/professions/farming.ts:330 with band 0 at :331; the precedent comment spans tests/deeds_content.test.ts 820-821 and the digest pin is 'every shipped deed keeps its trigger and renown unchanged' (FROZEN_CATALOG_SHA256), the describe 'frozen trigger + renown catalog (design rule 9: never retro-edit a trigger)' being only its header; the zh_TW rows span 12030-12035 (cite the keys). (6) tests/professions_fishing.test.ts quotes the old desc in the comment of 'ACCEPTED DRIFT, NARROWED BY DECISION F: fishing still completes prog_first_harvest, no longer on the first catch' (no assertion reads the desc); a reword owes that comment line or an explicit acceptance. (7) The wiki deed emit also carries an optional rewardTitle or rewardBorder and a conditional crest; for prog_first_harvest the generated row is id, name, category, renown, feat and crest with no desc, so run-and-confirm-clean holds. (8) The RULED line lands after the heading, with dated pointers on the four roll-up lines and inside the OPEN ITEMS section beside the existing RULED line. (9) The ratify arm needs no value and the maintainer chose between it and the desc rider in session (recorded in the RULED line).
- **Reviewers:** content-obligations-reviewer, frontend-seam-reviewer, qa-checklist
- **Depends on:** nothing
- **Estimate:** 5h

## Exit criteria

1. Every unit above executed, or escalated with a dated reason.
2. Each unit's domain reviewers dispatched FRESH (never the implementer), all findings applied: blocking, should-fix and nits alike.
3. The fix round re-read by a fresh reader, because fixes are unreviewed code.
4. Every new pin mutated and watched go red. A pin that survives its own mutation is not a pin.
5. `npx tsc --noEmit` clean, the architecture, monolith and parity guards green, the census RESULT PASS, `npm run ci:changed` after the LAST commit.
6. `node scripts/gate_select.mjs` on the COMMITTED tree, pg-armed, judged by exit code.
7. `docs/prd/masterwrought/phase-19f-qa.md` written and its verdict recorded before the next wave opens.

## Standing rules

NO push, NO PR, NO teardown. Local only. A green gate is not consent.
