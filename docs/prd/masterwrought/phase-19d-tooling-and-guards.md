# Phase 19D: Tooling and guards

Opened 2026-09-01 under ruling qr-19-best-for-project. Scripts, gates and test guards. A selection-semantics mistake here silently skips tests repo-wide, so every change must fail toward MORE tests, never fewer.

**12 units, about 45.25 hours estimated.** Every row here was answered in the Phase 19 rulings gate; this phase executes, it does not re-decide. A row whose ruling turns out to rest on a wrong premise is an ESCALATION back to the ledger, never a quiet re-ruling.

## Units

### D027

Ratify the deliberately self-clearing PENDING_ART_IDS allowlist deviation that lets gather_farming ship without its profession icon, since the only alternative needs the same master-SHA art wave the batch already waits on.

- **Files:** `docs/prd/masterwrought/farming/state.md`, `tests/profession_icons.test.ts`
- **Same-change obligations:** ANCHOR: put the RULED (qr-19-pending-art-allowlist-signoff, 2026-09-01) line AFTER the unique line "- Locked deviations from phase files: Phase 1: (a) PENDING_ART_IDS allowlist in" in farming/state.md (grep -cF = 1), inside the locked-deviations paragraph where (a) is described. HANDOFF ROW to flip, farming/state.md line 471, match on the exact full row "  (a) PENDING_ART allowlist sign-off (gather_farming, self-clearing)   P1   maintainer   open ruling-owed  " (grep -cF = 1); the replacement status cell is "RATIFIED 2026-09-01 by qr-19-pending-art-allowlist-signoff (self-clears when the art wave lands)". Also strike (a) from the maintainer-reads-owed enumeration in the same edit, at farming/state.md:409 where the list opens "(a) the PENDING_ART allowlist sign-off". PREMISE RE-VERIFIED: tests/profession_icons.test.ts:62 still reads const PENDING_ART_IDS = new Set(['gather_farming']), the inverted self-clearing arm is live at :285-292 (it fails with the message that the id must be struck from PENDING_ART_IDS once art backs it), and the sweep at :328 still walks the set, so the guard genuinely retires itself and cannot rot silently. SAME-CHANGE OBLIGATIONS: none. Zero source movement, so no wiki regen, no M16 fills, no deed row, no Reliquary page, no art park change, no census row (scripts/merge_audit/symbol_census.mjs mints nothing for a test-only ratification), no parity golden, no monolith ceiling. WHAT THE MAINTAINER IS SIGNING, worth stating in the ruling: not that the icon is optional, but that the allowlist is the sanctioned park until his wave draws it, and that the guard flips red the moment the art lands so the debt cannot be forgotten.
- **Reviewers:** test-coverage-auditor, qa-checklist
- **Depends on:** D010
- **Estimate:** 0.75h

### D029

Answer the surviving half of farming handoff row (al): whether the stills-bundle digest splits into renderer-code and content-graph halves, or the single bundle hash is ratified now that the bookkeepingOnly tightening already forces a rendered receipt on stills-renderer script edits.

- **Files:** `docs/prd/masterwrought/farming/state.md`, `scripts/build_mob_portrait_source_manifest.mjs`, `scripts/lib/mob_portrait_manifest_diff.mjs`, `scripts/lib/mob_portrait_manifest_diff.d.mts`, `tests/mob_portrait_source_manifest.test.ts`
- **Same-change obligations:** RECOMMENDED ARM (ratify the single hash): record only, no code. ANCHOR 1 (farming/state.md, grep -c = 1): the sentence 'The row's other half, the bundle-hash renderer/content split, is answered under qr-19-portrait-bundle-hash-split (D029) and folds into this cell in the same edit.' inside the (al) handoff cell is a PLACEHOLDER written by the already-landed D153 ruling: REPLACE that sentence in place with the answer, do not append after it. ANCHOR 2 (farming/state.md, unique line, grep -c = 1): 'splitting the bundle hash into renderer-code and content-graph halves is'; the dated RULED line goes AFTER the SHARPENED RESIDUAL sentence it ends (its next line is '  the open call.'), in the house form 'RULED (qr-19-portrait-bundle-hash-split, 2026-09-01, under qr-19-best-for-project): ...'. HANDOFF ROW: (al) is already 'PARTLY CLOSED 2026-09-01'; the status cell text to change is only the placeholder sentence above, and the cell's opening 'PARTLY CLOSED' becomes 'CLOSED' once both halves carry an answer. No wiki regen, no M16 fills, no deed or Reliquary rows, no art park move, no census row, no parity golden, no monolith ceiling in play. VERIFY BEFORE RULING: re-read isBundleOnlyDrift and withoutBundleFingerprint in scripts/build_mob_portrait_source_manifest.mjs and the four bookkeepingOnly arms in tests/mob_portrait_source_manifest.test.ts on the merged tree, since the tightening the ratification rests on was minted before the tenth sync. FLIP ARM PRICE, honestly: the digest split is not additive-cheap. build_mob_portrait_source_manifest.mjs pins schemaVersion 2 with an explicit comment that bumping it 'would make every committed manifest a bootstrap migration and demand the 242-row re-render this field exists to prevent', so the split must be authored as a strictly additive field plus a reworked withoutBundleFingerprint, a reworked describeManifestDrift, both .mjs and .d.mts, new drift arms in the guard test, a re-mint of every committed manifest baseline, and every future absorb re-mints two receipts instead of one. That arm is 12 to 16 hours and owes gate-integrity-reviewer plus test-coverage-auditor.
- **Reviewers:** qa-checklist, gate-integrity-reviewer, test-coverage-auditor
- **Depends on:** nothing
- **Estimate:** 2h

### D129

Replace tests/language_fanout_registry.test.ts's blanket `{ file: 'hud.ts', memos: 'coordinator' }` exemption with a per-signature opt-in list, applying the ANSWERED / NOT_A_LANGUAGE_GATE / LANGUAGE_KEYED contract inside the largest file in the tree and fixing every stale-locale memo the audit turns up.

- **Files:** `tests/language_fanout_registry.test.ts`, `src/ui/hud.ts`
- **Same-change obligations:** HONEST SIZE DRIVER: src/ui/hud.ts holds 60 `private last*` fields, of which the exemption's own text calls about two dozen compared repaint memos. Each needs a REAL analysis (does this signature carry locale-dependent text on the write side, the read side, or neither) and then a row, and the guard enforces the quality: a NOT_A_LANGUAGE_GATE reason under 120 characters and an ANSWERED why under 40 both fail the thin-reason arm, and an ANSWERED row must name a call refreshLocalizedDynamicUi actually makes. THE AUDIT WILL FIND BUGS, and each one is an i18n defect that ships WITH its own test in the same change, on the targetDiscordSig precedent (a paired suite driving two locales over byte-identical identity data, tests/target_flair_line_view.test.ts). Whatever it finds, a fix is either a fan-out arm in refreshLocalizedDynamicUi or a language-keyed signature, so the LANGUAGE_KEYED list and its structural verifier grow too. The two classification arms currently skip the file BY NAME, so removing the blanket row changes what the whole gate covers: expect first-run noise and budget for it. If a fix needs a new player-visible string it owes the English catalog row in src/ui/i18n.catalog and M16 non-Latin fills when the value is wordy, but the expected shape is repaint plumbing rather than new copy. src/ui/hud.ts ceiling 18728 with the file at 18708, so fan-out arms eat the same 20-line headroom D111 does, which is why the two are sequenced. No content, no wire, no parity golden. RECORDING: dated RULED line, id qr-19-hud-coordinator-fanout-exemption. ANCHOR: `memos: 'coordinator',` in tests/language_fanout_registry.test.ts (grep -c == 1); the row's `reason` is REPLACED by the per-signature list and the dated line goes AFTER the deleted row's position, recording that the by-name skip on the biggest file in the tree was closed and why (the shipped per-memo LANGUAGE_KEYED reach stands until then). No handoff-table row (Seed row column reads `new`).
- **Reviewers:** cross-platform-sync, frontend-seam-reviewer, test-coverage-auditor, gate-integrity-reviewer, qa-checklist
- **Depends on:** D111
- **Estimate:** 12h

### D130

Teach the symbol census to filter its extras allowlist through isCensusPath, so a row naming a name under an excluded path prefix is reported as informational instead of as an unsatisfiable entry the tool confidently calls 'GONE from merged'.

- **Files:** `scripts/merge_audit/symbol_census.mjs`, `scripts/merge_audit/symbol_census.d.mts`, `tests/merge_audit_symbol_census.test.ts`, `docs/prd/masterwrought/merge-deletion-list.md`
- **Same-change obligations:** THIS IS A SCHEMA CHANGE TO A GATE, which is why the row is sized as its own task rather than spent by a sweep. The extras rows carry Class, Name, Phase, Ruling, Reason and no path, so the tool cannot tell an excluded-path row from any other: parseExplainedExtras gains an OPTIONAL Path (or Scope) column, the row type carries it, unusedExtras at symbol_census.mjs:2237 filters rows whose path fails isCensusPath, and the report arm near :2432 gets an honest message instead of the GONE line. APPEND-ONLY DISCIPLINE: an absent Path column must keep every one of the existing extras rows parsing byte-unchanged, and the defective-row rule gains one arm (a row claiming an excluded path that is not actually excluded is itself a defect). DECLARATION TWIN moves with the source (.d.mts), which the census itself treats as a definition. DOC HALF: merge-deletion-list.md's header rule is re-worded, and the five literal-only rows (releaseParentRefs, newestReleaseRef, classifyLineage, isAnchorPath, Lineage, all live in scripts/merge_audit/golden_composition.mjs and its .d.mts) either move back into the extras table with a Path column or stay put with the rule re-stated; the ruling says which. VALIDATION: the census must still report RESULT PASS with the exit code captured WITHOUT a pipe (the phase's STEP 4), and the fix is proven by reproducing the old wrong message on a fixture row and showing it become informational. No content, no i18n, no wire, no parity golden, no ceiling. RECORDING: dated RULED line, id qr-19-census-allowlist-path-asymmetry. ANCHOR: `none of these exist on any parent, so scanning them would only mint EXTRA rows for` in scripts/merge_audit/symbol_census.mjs (grep -c == 1, in the EXCLUDED_PATH_PREFIXES doc comment), the RULED line goes AFTER it, recording that the exclusion itself STANDS (it is lossless for MISSING and only suppresses EXTRA) and that what changed is the allowlist side. No handoff-table row (Seed row column reads `new`).
- **Reviewers:** gate-integrity-reviewer, test-coverage-auditor, qa-checklist
- **Depends on:** nothing
- **Estimate:** 5h

### D131

Seed graphicsDefaultApplied beside graphicsPreset in the four genuinely exposed capture and perf rigs, and add a source-scan guard so any future script seeding the preset without the marker reds, with a documented carve-out for the rigs that force the tier by ?gfx= URL.

- **Files:** `scripts/climb_stall_shot.mjs (the seed object at :38, JSON.stringify({ graphicsPreset: 5, ... }))`, `scripts/lib/perf_hitch_browser.mjs (:95 settings.graphicsPreset = value)`, `scripts/load_probe.mjs (:439 current.graphicsPreset = presetValue)`, `scripts/perf_tour.mjs (:497 cur.graphicsPreset = value)`, `NEW guard suite under tests/ (filename is maintainer-named; the precedent that already reads scripts/ source text is /Users/fernando/Documents/wocc-masterwrought/tests/pr_shot_targets.test.ts)`, `READ-ONLY references: scripts/pr_shot_targets.mjs:81 (the correct idiom, already right) and /Users/fernando/Documents/wocc-masterwrought/src/render/gfx.ts:1378 forcedTierFromSearch (the reason 10 of the 25 are immune)`
- **Same-change obligations:** NONE of the content chain: no wiki regen, no M16 fills, no deed rows, no Reliquary pages, no art parks, no census rows, no parity goldens, no i18n (capture rigs are dev-channel, English stays). No monolith ceiling: none of the five files is pinned in tests/monolith_budget.test.ts (verified). What IS owed: the guard must be proven NON-VACUOUS by mutation (strip the marker from one allowlisted script, watch it red) per the packet's mutation-harness rule, and the 11 low-only seeders plus the 10 ?gfx-forced rigs must be re-run green so the carve-out is a per-entry documented list, never a bare allowlist. Validation is npx vitest run on the new guard then npm run ci:changed after the LAST commit (tsc does not read .mjs, so tsc proves nothing here). RECORDING: the row is 'new', so it stands only in the decision table. Dated RULED line qr-19-capture-seed-without-marker goes in the NEW Phase 19 ledger section of /Users/fernando/Documents/wocc-masterwrought/docs/prd/masterwrought/state.md, plus the progress.md Phase 19 row. Table anchors, each grep -c 1 in its own file: '  D131   capture-seed-without-marker  ' in /Users/fernando/Documents/wocc-masterwrought/docs/prd/masterwrought/phase-19-rulings-gate.md and the same string in state.md; the dated line goes in the ledger section, never inline in the table. NO handoff-table row exists for this slug (the farming handoff table, farming/state.md:465 to 627, carries no graphics row), so there is no status cell to flip.
- **Reviewers:** test-coverage-auditor, gate-integrity-reviewer, qa-checklist
- **Depends on:** nothing
- **Estimate:** 3h

### D132

Under the recommended arm, leave the fire short-fight harness modelling the Meteor bank at ignitionPct and write the reason at the code, so the ignite conservation arm passes for a stated, safe-direction reason instead of by accident.

- **Files:** `tests/fire_short_fight_tuning.test.ts (the estimator read at :180 to :181, its comment at :204 'estimate: ignitionPct of fire crit damage and of non-crit Meteor impacts', and the conservation arm at :534 to :545 whose bounds are paid <= banked * 1.02 and paid >= banked * 0.8)`, `READ-ONLY reference: src/sim/content/classes.ts:8080 igniteFrac 0.4, the rate the sim actually banks`
- **Same-change obligations:** None. No sim or server code moves, so no wiki regen, no M16 fills, no deed rows, no Reliquary pages, no art parks, no census rows, no parity goldens, no i18n. Not on the monolith ratchet. Validation: npx vitest run tests/fire_short_fight_tuning.test.ts plus npm run ci:changed. RECORDING: the row is 'new'; dated RULED line qr-19-ignite-bank-estimator-rate in the Phase 19 ledger in state.md plus the progress.md row. Table anchor, grep -c 1 per file: '  D132   ignite-bank-estimator-rate  ' in phase-19-rulings-gate.md and in state.md. No handoff-table row, so no status cell to flip. ESCALATION BRANCH, price it honestly: if the maintainer picks 'correct the estimator to igniteFrac', the arm's bounds must be re-derived rather than kept, which is a balance-adjacent threshold change; under the Phase 19 stopping rule and the CLAUDE.md rule that gameplay math follows real classic-era formulas and is never tuned to make a test read better, that is an ESCALATION back to the maintainer with the new numbers, not an execution in this wave.
- **Reviewers:** test-coverage-auditor, qa-checklist
- **Depends on:** nothing
- **Estimate:** 1h

### D141

Accept the raid tier above the apex tier and amend the placement prose, rescoping R3's sub-cap wording and the ilvl-31 crafted-band pin to the masterwrought family explicitly and re-deriving the two dev-bis premise pins, with the pbe_boost pair and the R5 lead-cap arm left to D140.

- **Files:** `docs/prd/masterwrought/state.md, the Power placement block (the AMENDED staleness stamp and its three relative-position lines, the last of which is at :298) and R3's sub-cap sentence at :95`, `docs/design/power-verification.md, the HISTORY second entry at :108 to :135, which today routes the identical contradiction to rows 12 to 16`, `tests/masterwrought_budget.test.ts, the ilvl-31 crafted-band pin at :1742 to :1775 (GREEN today, and what PR 3704 reds on THREE assertions: the offender filter at :1762 to :1764, the exact atBand literal toBe(17) at :1772, and the apex-array membership sweep at :1773)`, `tests/dev_bis_gear.test.ts, the two it.fails arms at :155 and :225, whose own block at :135 to :154 names THIS ruling and the same-commit flip rule, with the count pin an EXACT toBe(2) at :168`, `tests/server/pbe_boost.test.ts, the it.fails pair at :924 and :947 (their note names rows 12 AND 13, so their flip is JOINT with D140 and waits)`, `READ-ONLY: src/sim/equipment_rules.ts masterwroughtConflictSlot (returns null on any def without the flag, which is why S1 is what the code already does), /Users/fernando/Documents/wocc-masterwrought/src/sim/content/crucible_professions.ts (27 lines, ONE reagent def: the crafted epics and hammer are NOT in the tree yet)`
- **Same-change obligations:** NO content def, recipe level or item id moves, so NO wiki regen, NO M16 fills, NO deed rows, NO Reliquary pages, NO art parks, NO census rows and NO parity goldens. The amendment must stay PROSE-only: the Phase 19 stopping rule makes any ruling that moves a ratified R5 number an escalation rather than an execution, so option 2 (re-tier the apex pieces) is doubly blocked, by the frozen R5 surface and by the fact that the Crucible ilvl numbers are another packet's maintainer-fixed scope. DOCTRINE TENSION to resolve before writing: the row's option-1 text says 'rewrite the three stale relative-position claims', but the packet's own rule is that a wrong row is AMENDED IN PLACE with a dated line and never rewritten; the safe form is a dated RULED line appended under the existing AMENDED staleness stamp naming the ruled placement, leaving the three stale lines and their stamp standing. TWO MAINTAINER-NAMED SHAPES this row cannot guess: the replacement scoping of the band pin (a family-keyed band, or a named Crucible carve-out on the ignivar_loot precedent) and the replacement shape of the dev-bis count pin (pin the new exact count, or re-scope the arm to at most the cap). Note the record measures ZERO flagged dev-bis picks on all nine classes against the :168 pin's exact 2, so 're-derive' means re-authoring exact literals, not flipping it() back and hoping. Spell any packet R-number written into src/, server/, tests/, a CLAUDE.md or docs/design as 'masterwrought R<n>' IN FULL, per the standing ruling-number collision rule. RECORDING: dated RULED lines carrying qr-19-apex-tier-vs-crucible-placement in TWO doc files. ANCHORS, each grep -c 1: state.md 'the raid catalog puts a stronger incumbent in the chest slot (the R5 lead' (the eighth-sync AMENDED block) and state.md 'prospectively (nothing red today) and is folded into row 13; the same span's' (the fast-follow fold), with the dated lines AFTER each; state.md 'Existing legendaries (ilvl 33 to 37, budgets 44 to 49) remain the untouched ceiling.' as the placement block's tail anchor, dated line AFTER; state.md 'Sub-cap: at most ONE' at :95 for the R3 rescope, amended in place with a dated line; power-verification.md 'ruling is Phase 19 decision table rows 12 to 16', dated line AFTER, never a rewrite of the sealed 'NOTHING IN THIS RECORD WAS REWRITTEN' sentence. No handoff-table row exists, so no status cell to flip. SUB-DECISION S1 (the fast-follow stays outside the masterwrought family) is priced inside this row and is what masterwroughtConflictSlot already implements; S2 (the fast-follow inherits the flag) would red three sweeps that walk every flagged def and needs cross-branch coordination with PR 3704, so it is out of this wave.
- **Reviewers:** test-coverage-auditor, architecture-reviewer, content-obligations-reviewer, qa-checklist
- **Depends on:** nothing
- **Estimate:** 8h

### D152

Ratify the structural farming affinity exemption with its stale 27 corrected to the live 39 in both ledger places, or take the option-2 narrowing to the fifteen structurally uncoverable ids, which is a pure test-suite change that buys a tripwire nothing in the tree has today.

- **Files:** `docs/prd/masterwrought/farming/state.md`, `tests/material_profession_affinity.test.ts`, `src/sim/content/farm_crops.ts`, `src/ui/i18n.catalog/hud_chrome.ts`, `src/ui/hud/professions/material_hint_view.ts`, `tests/material_hint_view.test.ts`, `docs/prd/masterwrought/merge-deletion-list.md`
- **Same-change obligations:** HANDOFF ROW to flip, exact text to match (grep -c = 1 in docs/prd/masterwrought/farming/state.md): '  (w) structural affinity exemption, 27 materials (standing since Phase 4)   P4 / P5   maintainer   open ruling-owed  '. REPLACEMENT STATUS CELL: 'CLOSED 2026-09-01 by ruling qr-19-structural-affinity-exemption (Phase 19, under qr-19-best-for-project): the blanket structural exemption is RATIFIED as standing, and the row figure is AMENDED rather than rewritten: 27 is stale, the live derived set is 39 (FARM_MATERIAL_ITEM_IDS, pinned at tests/professions_zone_rollout.test.ts:1008 with its own 27-plus-11e derivation at :1003-1005). The claim survives its stale count because the watch fee still consumes produce, and the Phase 5 QA already re-judged and upheld the auto-exemption with the exact-set pin as its gate.' SECOND EDIT SITE, same ruling id: the maintainer-reads-owed roll-up line reading '   PENDING_ART allowlist sign-off; (w) the structural affinity exemption' (state.md:410 as read), where the same 27-to-39 correction rides. VERIFIED LIVE: FARM_MATERIAL_ITEM_IDS at src/sim/content/farm_crops.ts:355; the exemption is a TEST-ONLY construct (tests/material_profession_affinity.test.ts:16-42 banner and set, census skip at :277-306, honesty arm at :308-320) so it mints no export and owes no census row; the smuggle guard is the 39-id sorted literal at tests/material_taxonomy.test.ts:430-484. WHAT RATIFYING CONCEDES, state it in the line: nothing in the tree can then notice a future crop shipping produce with no dish, because the census arm blanket-skips all 39 and the closure arm at tests/professions_zone_rollout.test.ts:976-1012 counts every seed and produce id live by construction. OPTION COSTS if the maintainer moves off ratify: option 2 test-local narrowing is about +3h and weakens the content-side anti-smuggle anchoring; option 2 content-side adds a derived sibling export beside FARM_MATERIAL_ITEM_IDS and therefore owes an 'Explained extras' row in merge-deletion-list.md (src/ is a census root, symbol_census.mjs:110); option 3 (tooltip purpose lines for compost, the twelve seeds and withered_husks, whose Used-by line can never render, src/ui/hud/professions/material_profession_hint_view.ts:86-99) is about +4h, moves the wave to 19B-ui plus 19F-i18n, adds hudChrome.materialHint keys beside growthTonic at src/ui/i18n.catalog/hud_chrome.ts:4151-4153 with English-only per the contributor rule (M16 non-Latin fills only if a value is wordy), flips the no-more-and-no-less arm at tests/material_hint_view.test.ts:89, and owes a merge-deletion-list row per new key. NOT OWED under any option: wiki regen, deed rows, Reliquary pages, art parks, parity goldens, IWorld parity members, monolith ceilings (the copy lands in the catalog and the view module, never in hud.ts). Re-run tests/material_profession_affinity.test.ts, tests/material_taxonomy.test.ts and tests/professions_zone_rollout.test.ts together; they are deliberately each other's smuggle guard.
- **Reviewers:** qa-checklist, content-obligations-reviewer, frontend-seam-reviewer
- **Depends on:** nothing
- **Estimate:** 2h

### D155

Ratify the NPCs-are-terrain required-pad rule and Farmer Hollis's eyeballed Highwatch shelf, and decide whether the ledger's closing pad-delta advisory becomes an armed roster-wide gate; the cheap executable half is a number-free rim-slope arm generalizing the spot-specific Highwatch training-dummy pin over every roster pad.

- **Files:** `docs/prd/masterwrought/farming/state.md`, `tests/placement_integrity.test.ts`, `src/sim/terrain_calm_anchors.ts`, `src/sim/content/zone3.ts`, `tests/farmer_npc_placement.test.ts`
- **Same-change obligations:** HANDOFF ROW to flip, exact text to match (grep -c = 1 in docs/prd/masterwrought/farming/state.md): '  (bh) NPCs-are-terrain pad rule (Highwatch eyeballed, no re-seat)   P9   maintainer   open ruling-owed  '. REPLACEMENT STATUS CELL: 'CLOSED 2026-09-01 by ruling qr-19-npc-terrain-pad-rule (Phase 19, under qr-19-best-for-project): the rule is RATIFIED as shipped. Every NPCS row stays a REQUIRED calm-anchor pad at rIn 6 / rOut 14 (src/sim/terrain_calm_anchors.ts:160-162), Hollis keeps the Highwatch shelf on the Phase 9 QA three-angle LOW-preset read (a natural terrace, no mound, crater or seam), and no golden re-mints. The module argues the rule in place at :95-98: un-padding an existing NPC would itself move minted terrain, which is the failure this ratification avoids.' SECOND EDIT SITE, same ruling id: the reads-owed roll-up line reading '   trade-pipe leak, and the garden_hoe reagent knock-on; (bh) the' (state.md:415 as read), plus the OPEN-items restatement block at state.md:4383-4386. VERIFIED LIVE: the required pad loop at src/sim/terrain_calm_anchors.ts:160-162 with optional=false; the seat literal farmer_hollis pos { x: -18, z: 695.5 } at src/sim/content/zone3.ts:1471-1476, mirrored by literal at tests/farmer_npc_placement.test.ts:38-62; the spot-specific rim pin at tests/placement_integrity.test.ts:274-286 against PLAYER_MAX_CLIMB_SLOPE (src/sim/pathfind.ts:55, 1.5); and grep confirms 'pad delta' appears nowhere in src/, tests/ or scripts/, so the advisory is currently a sentence only. OPTION 2 PRICE (the recommended cheap half): test-only, no src change, no golden movement, a generic per-pad rim walk over openWorldPads in tests/placement_integrity.test.ts (287 lines today, already carrying a 24000-cell BFS budget), so the arm must be SIZED against that budget rather than just written; the rim variant needs no maintainer number, the DELTA variant does and the packet holds only two measurements (4.3 yd under Hollis, about 0.6 under bed_thornpeak_5) and no sanctioned ceiling, so the delta arm cannot land without the maintainer naming the bound and where it is measured. OPTION 3 (re-seat) is the expensive one and changes the wave to 19A-content: it re-mints tests/fixtures/terrain_height_parity.v1.f64le.gz (about 1.05 MB) and the baked plates via npm run assets:mapbg with the committed public/map_bg WebPs, and owes a re-capture of docs/screenshots/farming-phase-09/qa-highwatch-shelf-low.png and -overview.png; the Eastbrook chunk digest does NOT move (Hollis at z 695.5 is outside that rect). NOT OWED under options 1 or 2: wiki regen, M16 fills, deed rows, Reliquary pages, art parks, census rows, parity goldens, IWorld members, monolith ceilings.
- **Reviewers:** test-coverage-auditor, architecture-reviewer, qa-checklist
- **Depends on:** nothing
- **Estimate:** 4h

### D164

Ratify scripts/farming_journey_e2e.mjs as a permanently hand-run acceptance rig with the layer-honesty source pin standing in as the CI guard, rather than putting the first scripts/*_e2e.mjs in repo history onto a workflow.

- **Files:** `docs/prd/masterwrought/farming/state.md`, `docs/prd/masterwrought/progress.md`, `.github/workflows/nightly.yml`, `tests/ci_workflow.test.ts`, `scripts/farming_journey_e2e.mjs`
- **Same-change obligations:** SCOPE NOTE: under the recommended arm nothing in the tree moves, only two ledger stamps. The workflow and pin files are the bill ONLY if the maintainer picks a lane: an unrequired nightly job must install Playwright Chromium so the browser-path resolver finds a binary, serve vite on the port the script's own prereq names with a port-scoped teardown, and run both viewports, and a new job key forces TWO whole-file pins in the same change (the measured timeout-minutes bound, which the packet holds NO measured wall for, so measuring the journey IS part of the work, and the pg-wired / guarded DB-less / test-free classification whose token scan cross-checks the claim). The required-tier arm additionally moves the required-context name pins, the merge-queue doc's required half, and needs the branch-ruleset re-point that scripts/release_mint.mjs owns or the queue goes dormant. NOT OWED on any arm: no wiki regen, no i18n or M16 fills, no deed, no Reliquary page, no art, no census row, no parity golden, no world_api parity, no monolith ceiling. KEEP: the layer-honesty pin in tests/farm_verb_reachability.test.ts stays as-is, it slices the same .mjs and is non-vacuous in both directions. ANCHOR (grep -c = 1) in /Users/fernando/Documents/wocc-masterwrought/docs/prd/masterwrought/farming/state.md: 'the journey script stays outside the'; the dated RULED line goes AFTER that Phase 9b QA residual's last line. HANDOFF ROW, match exactly: '  Journey script joins the gate when the merged Masterwrought PR ships (reworded at the 11b absorb: D22 is superseded, delivery is the one Masterwrought PR, and the script stays off CI until that ships)   P9b QA   maintainer   open ruling-owed  '. STATUS CELL REPLACEMENT: 'open ruling-owed' becomes 'CLOSED 2026-09-01 by ruling qr-19-journey-script-gate-entry (Phase 19, under qr-19-best-for-project): the journey stays permanently OFF CI as a hand-run acceptance rig, with the layer-honesty source pin as the standing guard.'
- **Reviewers:** gate-integrity-reviewer, qa-checklist
- **Depends on:** nothing
- **Estimate:** 0.5h

### D172

On the recommended arm (option b) this retires the R34 stale-client apparatus as closed by the branch's own epoch-26 handshake: delete tests/stale_client_rollout.test.ts and its snapshot, rewrite DEPLOY.md's release-specific caveat to state the WS epoch gate instead of a loot-table exclusion, and annotate R34 as superseded by circumstance rather than reversed.

- **Files:** `tests/stale_client_rollout.test.ts (DELETE, 179 lines, verified)`, `tests/__snapshots__/stale_client_rollout.test.ts.snap (DELETE, 62 lines, verified)`, `DEPLOY.md (rewrite the release-specific caveat block at 467 to 531; repair the two stale sentences at 428 to 432, 'The release left the fail-closed layout gate at ONLINE_WORLD_LAYOUT_VERSION 3 ... so stale bundles are still admitted at reconnect', and the dead guard pointer at 527 to 531)`, `docs/design/professions-tuning-packet-review.md (annotate R34 at :178 as superseded by circumstance: the guards still ship, a version floor arrived anyway for wire-shape reasons)`, `docs/prd/masterwrought/state.md (two dated RULED lines)`, `docs/prd/masterwrought/phase-19-new-rows.md (dated in-place amendment to D172's FLAGS line)`, `docs/prd/masterwrought/progress.md (Phase 19 row)`, `READ-ONLY inputs, no edit: src/world_api.ts (verified line 190, ONLINE_WORLD_LAYOUT_VERSION = 26), /Users/fernando/Documents/wocc-masterwrought/server/ws_auth.ts (verified line 266, the fail-closed t-literal refusal), /Users/fernando/Documents/wocc-masterwrought/scripts/ci_shard_weights.generated.json (line 8136 keeps a weight row for the deleted file: LEAVE IT, generated, never hand-edited)`
- **Same-change obligations:** SAME-CHANGE OBLIGATIONS: none of the content family. No wiki regen (npm run wiki:content untouched, no tests/guide.test.ts freshness cost), no M16 non-Latin fills, no new i18n key at all, no deed row in src/sim/content/deeds.ts, no Reliquary page, no item id and so no art-park row or committed WebP, no scripts/merge_audit/symbol_census.mjs census row, no parity golden, no tests/world_api_parity.test.ts member move, no tests/monolith_budget.test.ts ceiling, R5 untouched. WORK OWED BEFORE THE DELETE, and it is the real cost of this row: confirm no surface in the DEPLOYED bundle (9d7a1a021, present in this worktree, verified with git cat-file) renders an arbitrary item id OUTSIDE the WS handshake. Partly discharged here already: 9d7a1a021:src/ui/market_view.ts:147-148 drops unknown listings; 9d7a1a021:src/ui/mailbox_window.ts:418 has an else arm that renders the raw id as text and :703 does 'if (!item) continue', so mail degrades rather than throws. What remains is the 22 'ITEMS[' sites in 9d7a1a021:src/ui/hud.ts (vendor and loot paths) plus 9d7a1a021:src/ui/trade_view.ts; git grep -l unknownItemIconHtml at 9d7a1a021 -- src/ui returns nothing, confirmed here, so the deployed bundle predates the whole R34 fallback family. LEDGER ANCHOR, verified unique: grep -c -F 'THE STALE-CLIENT DEPLOY WINDOW versus the farming DUNGEON channel.' docs/prd/masterwrought/state.md returns 1 (line 9738); the dated RULED (qr-19-stale-client-deploy-window, 2026-09-01) line goes AFTER that line, in the same shape the already-landed D006 block uses under '### OPEN ITEMS: THE THIRTEEN ARE THE MAINTAINER'S, AND THIS PHASE ADDS NONE' (state.md:18246, also unique). AMENDMENT OWED, the row is WRONG as written and must be amended in place with a dated line, never rewritten: D172's FLAGS say the epoch ruling 'has no D-row in this table', but the completeness pass added D162 [epoch-bump-at-golive] covering exactly farming/state.md:494. Anchor, verified unique: grep -c -F 'contingentOn=The farming epoch-bump-at-go-live ruling' docs/prd/masterwrought/phase-19-new-rows.md returns 1; the dated correction line goes AFTER that FLAGS line. NO HANDOFF-TABLE ROW IS OWNED BY D172 (checked farming/state.md: the only open ruling-owed row in this neighbourhood is :493, which belongs to D161, and :494, which belongs to D162), so no status cell is replaced here. FRESHNESS NOTE: the row records reading the tree at 6972704db9; HEAD is now 446564e533. All four load-bearing facts re-verified at HEAD. ARM SENSITIVITY: option (a) ratify collapses this to a docs-only subset in the same wave (one DEPLOY.md paragraph plus a comment rewrite, about 1.5 hours) and leaves the guard checking the professions packet's thirteen ids against a packet that mints about 177; option (c) withdraw would move the row to 19A-content (it edits src/sim/content/heroic_loot.ts, reds tests/apex_pattern_channels.test.ts and the farm channel map, and owes a wiki regen), and it closes only 2 of the 17 admitted ids. Do NOT hand-edit the generated shard-weight table; it is regenerated by node scripts/ci_shard_weights_harvest.mjs off a green full CI run. Gate with node scripts/gate_select.mjs before calling it done.
- **Reviewers:** privacy-security-review, qa-checklist, cross-platform-sync
- **Depends on:** D162
- **Estimate:** 5h

### D175

Rule the shipped-id golden cadence and, on the recommended close, ratify the tree as it stands while closing the one live gap and the doctrine contradiction: re-mint the single missing id, amend the three places that still say 'after a release', and write the consequence line that a Phase-20 cut of a branch-only id is now a retirement rather than a delete.

- **Files:** `tests/shipped_item_ids.golden.json (re-mint ONLY via UPDATE_SHIPPED_ITEMS=1 npx vitest run tests/shipped_item_ids.test.ts; verified in this worktree: 1220 ids, sorted, zero duplicates, reins_rickshaw_mount absent (grep -c returns 0) while its two siblings are pinned at lines 825 and 830, so the re-mint is a one-line additions-only diff)`, `tests/shipped_item_ids.test.ts (the header doctrine at lines 14 to 17, 'After new items ship in a release, re-mint ...'; only if the cadence is amended. The assertion itself at :37-38 is a subset filter, which is why an un-re-minted golden never reds and the cadence has no enforcement behind it)`, `src/sim/content/CLAUDE.md (lines 345 to 347 mirror the same doctrine, and line 344's 'Only an item that never left your unmerged feature branch may be deleted outright' is the escape the ruling closes; verified by reading :340-347)`, `docs/prd/masterwrought/merge-deletion-list.md (the retirement-not-delete consequence row)`, `docs/prd/masterwrought/phase-20-release-fill-and-reclose.md (restore the acceptance line the deviating phases dropped, phase-11j-gathering-completion.md:508-509 as its exemplar)`, `docs/prd/masterwrought/state.md (dated RULED line plus the consequence line)`, `docs/prd/masterwrought/progress.md`, `READ-ONLY, verify only: docs/design/professions-tuning-packet.md:393 (true under either cadence, no edit) and /Users/fernando/Documents/wocc-masterwrought/scripts/ci_shard_weights.generated.json:8057 (generated, leave it)`
- **Same-change obligations:** SAME-CHANGE OBLIGATIONS: none of the content family, on every arm. No npm run wiki:content and so no tests/guide.test.ts freshness cost, no i18n key and no M16 non-Latin fills, no deed record, no Reliquary page, no committed WebP art and no art-park row, no scripts/merge_audit/symbol_census.mjs census row, no parity golden, no tests/world_api_parity.test.ts pin, no tests/monolith_budget.test.ts ceiling, no R5 surface. Nothing else in the repo reads the golden: grep across src, tests, scripts, docs and .github returns the test, docs prose, phase files and one generated shard-weight row. HARD CONSTRAINT ON THE DIFF SHAPE: the golden is APPEND-ONLY and a REMOVAL is the file's own stated signature of a dead shipped id (tests/shipped_item_ids.test.ts:16-17), so any prune arm (option 2) or dev-only-exclusion arm (option 4) must add an explicit carve-out to that header in the SAME change or the next reader mis-diagnoses the diff; option 4 additionally removes two ids already pinned (reins_terrorspark_groundshaker, reins_drakemaw_raptor) and collides with D110, which may give both pended mounts a real source and force the exclusion to be unwound. THE RULING MUST CARRY THE CONSEQUENCE LINE, because the tree has changed what the doctrine means: all 172 of the packet's branch-only ids are already pinned, so the 'delete outright' escape at src/sim/content/CLAUDE.md:344 is closed in practice and a Phase-20 cut of any of them becomes a RETIREMENT (keep the def, drop the acquisition paths, exemplar RETIRED_HEROIC_ITEMS in src/sim/content/heroic_loot.ts) plus its merge-deletion-list row. LEDGER ANCHORS, both verified unique: grep -c -F '1. **The shipped-id golden was NOT re-minted**, against the acceptance' docs/prd/masterwrought/state.md returns 1 (line 12484), and the bullet's closing tail grep -c -F 'their ids are in the golden). Ratify or overrule.' also returns 1 (line 12489); put the dated RULED (qr-19-shipped-id-golden-remint-cadence, 2026-09-01) line AFTER that closing tail so the bullet is not split, and mirror it at the two roll-ups that name the item (state.md:13046 and :15886). No handoff-table row is owned by this row (checked farming/state.md). ORDERING, and it is the reason this row is late in the wave rather than early: if the re-mint rider is taken it must run AFTER every Phase 19 row that mints or retires an item id, or the golden is stale again the moment it lands; it is the last tooling row of the wave. Do not hand-edit the golden and do not hand-edit the generated shard-weight table.
- **Reviewers:** qa-checklist, architecture-reviewer
- **Depends on:** nothing
- **Estimate:** 2h

## Exit criteria

1. Every unit above executed, or escalated with a dated reason.
2. Each unit's domain reviewers dispatched FRESH (never the implementer), all findings applied: blocking, should-fix and nits alike.
3. The fix round re-read by a fresh reader, because fixes are unreviewed code.
4. Every new pin mutated and watched go red. A pin that survives its own mutation is not a pin.
5. `npx tsc --noEmit` clean, the architecture, monolith and parity guards green, the census RESULT PASS, `npm run ci:changed` after the LAST commit.
6. `node scripts/gate_select.mjs` on the COMMITTED tree, pg-armed, judged by exit code.
7. `docs/prd/masterwrought/phase-19d-qa.md` written and its verdict recorded before the next wave opens.

## Standing rules

NO push, NO PR, NO teardown. Local only. A green gate is not consent.
