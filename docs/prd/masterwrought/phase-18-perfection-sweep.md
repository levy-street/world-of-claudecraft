# Phase 18: The perfection sweep (every repo-actionable residual, closed)

Appended 2026-08-30 on the bare axis under ruling qr-18-REOPEN (state.md): the maintainer
directs a perfection close, so the judged refusals and the code-shaped recorded-not-acted
reads across ALL packet ledgers RE-OPEN as actionable work. This phase closes every one of
them that needs no maintainer decision. Anything that needs a decision goes to the Phase 19
decision table, never guessed here. Its QA twin is `phase-18-qa.md`.

### Starter Prompt
```
STANDING DELIVERY RULE (maintainer, 2026-08-20, unchanged): THIS PHASE DOES NOT PUSH AND
DOES NOT OPEN A PULL REQUEST. The branch stays LOCAL. A green gate is not consent.
This is Phase 18 of the Masterwrought feature: the perfection sweep. Ruling qr-18-REOPEN
(state.md, 2026-08-30) re-opens every judged refusal and every code-shaped
recorded-not-acted maintainer read across the packet ledgers as actionable work; this
phase closes all of them that need no maintainer decision.

Model: xhigh effort. Harness: Claude Code. Worktree: ~/Documents/wocc-masterwrought
(branch feature/masterwrought). ULTRACODE: yes (the inventory derivation, the review
rounds, and the per-unit fresh readers run as Workflows with per-finding adversarial
verification). LOCAL ONLY.

WORKTREE GUARD (FIRST; the user runs concurrent sessions): run pwd. If not in
/Users/fernando/Documents/wocc-masterwrought, switch NOW with EnterWorktree
(path: /Users/fernando/Documents/wocc-masterwrought). If it refuses, STOP.

STEP 0 - PRE-FLIGHT:
- git status clean at the Phase 17 close (tip bf73bd285d or later). Re-resolve the newest
  origin/release/** by VERSION SORT (git branch -r --list 'origin/release/*' | sort -V,
  last row); if it moved past e19d832b47, merge it and run the release-merge-audit skill,
  then the riders: the Eastbrook provenance re-mint if src/render/renderer.ts moved (four
  literals, one script; memory renderer-edits-owe-eastbrook-remint) and the ARM 3 bypass
  anchor CLEAN RELEASE-TIP capture the Phase 16 QA recorded as owed at the next real sync
  (evidence convention: docs/screenshots/masterwrought-phase-16/perf-tour-ab-2026-08-30.json;
  headed tour TWICE, the first launch is cold). If the release did not move, record the
  no-op the Phase 17 way.
- Memory scan: the packet memory (endgame-crafting-purple-tier-research), the gotcha
  catalog's workflow/subagent cluster (typed reviewers via the Agent tool, never workflow
  agentType+schema: it died 11/11 in Phase 17), background-gate-run-no-tail-pipe,
  biome-format-pass-is-not-a-check-pass (it fired at the Phase 17 gate on db.ts's exact
  ceiling), gate-freshness-requires-committed-tree, renderer-edits-owe-eastbrook-remint,
  worktree-isolated-session-compound-bash-refused (script files in the scratchpad).

STEP 0.5 - RECORD THE RE-OPENING IN PLACE: every ledger row this phase acts on gains a
dated RE-OPENED (qr-18-REOPEN) line where it stands (never renumber, never delete the
original judged reason: the record of why it was refused stays beside the record of why
it is now done).

STEP 1 - DERIVE THE INVENTORY (Explore agents; do not read the ledgers in the main loop):
sweep docs/prd/masterwrought/state.md (every "JUDGED", "JUDGED REFUSALS", "RECORDED, NOT
ACTED", "maintainer reads" section of every phase ledger, Phase 17's included),
docs/prd/masterwrought/farming/state.md (the handoff table's open rows AND the per-phase
deviation ledgers' recorded reads), and docs/design/power-verification.md section 14.
Classify EVERY item into exactly one bucket: (A) actionable-in-repo without a decision
(this phase), (B) needs a maintainer decision (a value, a basis, a design call: carry to
the Phase 19 table with a one-line price), (C) already discharged or a pure record of a
measured fact (cite where). The derived list OUTRANKS the seed list below (standing rule:
the sweep outranks the settled count); report the bucket counts before building anything.

THE SEED LIST (known members of bucket A; the derivation adds to it, never subtracts
without a citation):
- Render: farm_patches becomes a PREPARED producer (the Phase 14 recorded read, scope
  widened at Phase 17): a RETAINED prewarm home for the fourteen farm GLBs' materials
  (family x stage, the interior-encounter "clone materials kept alive, never disposed"
  rule) or a compile gate at plot attach with the static bed as stand-in, PLUS the feast
  shadow-budget depth-twin arm, an ENTITY_GATE_STAND_INS row and its test case if a gate
  is used, and the acceptance bar: zero live-program gpuPrep events on an offline tour
  that plants, stage-advances, wet-band flips, and places a feast. Dispatch
  render-performance-reviewer on the diff. The feast click proxy: measure its compile
  unit via gpuPrepEventsSnapshot on a feast spawn; if it costs one, share a module-level
  proxy material (markSharedMaterial, the sparkleMat precedent) with the disposal story
  adjusted.
- Server hot path: the admin metrics FAMILY (market metrics, overview, activity) moves to
  serialize-once readouts (the realm_readout_memo / maybeRaw idiom) and the metrics route
  takes the oversight read limiter; the mst resolver path gets an inline loop over the
  shared predicate (zero per-tick closure; one definition kept for the craft gate); the
  zone-celebration fan-out: MEASURE the per-recipient serialization cost at the recorded
  rates first and either land the shared-bytes shape or record the measured refusal with
  numbers (a pid-differing event is not trivially shared; do not break the wire for a
  once-per-character event without the measurement).
- Persistence: the two unfenced offline writers (the characters.ts rename signer sweep
  and server/pbe_boost.ts) join the unleased fence with real-Postgres arms; the nonce
  fence's missing expiry term is a DECISION (Phase 19), not this phase's.
- The pbe_boost scorer hands a protection tank a caster belt (spiritweld_girdle): fix the
  scorer, pin it. The trade-layer substitution class (Phase 12 QA read: fix home = the
  trade layer): implement and pin.
- Frontend: the two bare-named modal painters (legendary_naming_dialog.ts,
  input_dialog.ts) join the painter gate (the *_controller suffix the gate already sweeps,
  or an explicit registration) with their cold contract; the commission board's missing
  dialog semantics (markDialogRoot); the Perfecting window's rail tile and keybind inside
  the movable menu frame (HUD_FRAME_SPECS row + keyboard path per src/ui/CLAUDE.md); the
  remaining inline tone hexes in hud.ts move to tokens (count them first; extraction
  lowers the ratchet); the admin market-metrics page gets its screenshot.
- Tests and tooling: the mobile E2E tooling repair (mobile_visual.mjs's retired
  #mobile-more click; the overlap audit's 13 strict violations, judged against the live
  layout rather than tolerated); the druid balance harness re-pin (drifted 12 percent,
  never re-pinned); the vendor_floor tercile arm made honest (a live-derived endpoint or a
  relabel); the census adopts the fanout-helper alternative (announceZoneCelebration
  named in SIM_EVENT_FANOUT_HELPERS, restoring extractor visibility, the three union-only
  pins and the two simevent-emit deletion rows retired in the same change, mutation guard
  re-run); the ci_workflow block extractor's blank-line truncation gets its comment and
  its guard; the deferred blob warn gains a shutdown drain hook at saveAll.
- Content and wiki: the public wiki's craft-gain boundaries above the reachable cap (63
  of 170 rows) reworded from the live constants; the mediawiki/seed regen obligation
  (npm run wiki:seed) discharged; REF_ARMOR's stale calibration comment states its
  provenance; the stale "11e decision 6" label corrected to 11g-D-C everywhere it stands.

STEP 2 - EXECUTE IN UNITS (one commit per unit, explicit paths, bodies, no session
trailers): each unit follows module-first (a sibling module behind an existing seam,
never a coordinator method cluster; extractions LOWER the monolith ceilings in the same
change), carries its tests (sim and server units always), its same-change obligations
(the census row for every new exported name, content id, i18n key, or SimEvent; the
matcher for every new emit; the wiki regen for content), and its own domain reviewer plus
a fresh reader per the Review Dispatch Matrix. Apply ALL findings (blocking, should-fix,
nits); the fix round gets its own fresh review.

STEP 3 - VALIDATION per unit batch: npx tsc --noEmit, the unit's suites, tests/
architecture.test.ts on any src/sim touch, tests/monolith_budget.test.ts on any ratchet
file, node scripts/merge_audit/symbol_census.mjs (RESULT PASS, exit captured without a
pipe), npm run ci:changed AFTER the last commit of the batch, then node
scripts/gate_select.mjs on the committed tree. The freeze is the QA twin's job.

STEP 4 - DOCS: progress.md Phase 18 row; the Phase 18 ledger in state.md (the derived
inventory with its three buckets and counts, every unit's record, the JUDGED list for
anything this phase itself refuses (with the reason and the qr-18-REOPEN citation it is
refusing under), and the Phase 19 decision table DRAFT: every bucket-B item with its
options, a price per option, and a recommendation).

STOPPING RULES: an item that turns out to need a value, a basis, or a design call is
CARRIED to the Phase 19 table, never guessed; the R5 surface stays frozen; a finding that
contradicts a ratified row is an escalation; any red gate step stops; and the hard one:
NO push, NO PR, NO teardown.

REPORT: the derived inventory (bucket counts and the full bucket-A list with a
done/carried status per item), every unit's commit and its review record, the census and
gate results by exit code AND printed markers, the Phase 19 decision table draft, and
NEXT = Phase 18 QA (phase-18-qa.md, FRESH session).
```
