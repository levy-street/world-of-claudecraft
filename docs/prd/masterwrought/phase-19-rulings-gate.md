# Phase 19: The rulings gate (every open maintainer decision, answered and executed)

Appended 2026-08-30 under ruling qr-18-REOPEN. The packet's residual list is two kinds of
item: work a session can do (Phase 18) and decisions only the maintainer can make (this
phase). Phase 19 opens on ONE complete decision table, takes the maintainer's answers
in-session, executes every ruling, and records each where it stands. No decision is
guessed; no row closes without a ruling id.

## The decision table (FINALIZED by the Phase 18 QA, 2026-08-31)

Every row: the question, where it stands with its ledger and live-code anchors, the options with a
price each, a recommendation, and the seeded row it closes. **151 rows.** A recommendation is a
recommendation and never a ruling: no decision here is guessed, and no row closes without the
maintainer's answer and a ruling id.

HOW THIS TABLE WAS BUILT, so a reader can tell which half carries which warrant. Rows D001 to D132
are the Phase 18 sweep's bucket-B draft, derived by workflow from 815 inventoried items across
state.md, farming/state.md and power-verification.md, adversarially re-verified by ninety-one
cluster verifiers, and priced by a twelve-lane pricing pass; they arrive here unchanged from the
ledger. Rows D133 to D151 are the Phase 18 QA's own work: the sweep expanded only four of the
sixteen seeded rows below (rows 3, 4, 8 and 10), so the QA priced the remaining twelve to the same
contract, each one adversarially re-checked against the live tree.

READ BEFORE RULING, three rows that are not what their seed says:
- **D139** (seeded row 11) is only PARTLY answered. Phase 18 recorded a MEASURED REFUSAL of the
  shared-bytes splice and called the row closed, but the refusal does not touch the other half: the
  fan-out still walks every player in the realm calling zoneAt per player, which is unmeasured and
  is now the leading per-celebration cost. The sitting owes two answers, not one.
- **D140, D141, D142, D143 and D144** sit on the FROZEN R5 surface. They quote recorded numbers and
  re-measure nothing. A ruling that moves an R5 input re-opens the Verdict that closed 2026-08-29
  under the four ratified rulings' own conditions, and that re-open is itself part of the price.
- **D126** (the reword-staleness gate) is unsatisfiable as specified in a single sitting: the
  backlog is roughly 1,600 locale overlay rows across 92 strings, and four separate guards were each
  checked and each cannot see it. Rule the horizon, not the backlog.

| D001 | 11h-two-deviations | Ratify or revert the two forced 11h accent-rule deviations: flask grain (highland_barley) authored at 1 instead of 11h-GATE-C's 2, and capstone base crops at 2+1 instead of 11h-GATE-D's 3+1. | state.md, 11h ledger 'FOR THE MAINTAINER... ratify-or-revert' block (lines 11068-11080 as read); re-proven FORCED by the Phase 15 mutation audit (lines 14361-14378: Y8/Y9 both red tests/provisioning_supply_line.test.ts); still open in the Phase 15c ELEVEN roll-up (lines 15595-15603). Literals live: src/sim/content/recipes.ts:2930/2949/2968 (count 1) and :3150 (count 2). | Ratify both deviations as recorded [no-op (a dated RULED qr-19 line in state.md)]; Revert the literals and amend RULE 2's cap/count half [one-number edits on five recipe rows in src/sim/content/recipes.ts plus rewriting the accent-sweep arms in tests/provisioning_supply_line.test.ts; a standing-rule change with its own blast radius, plus wiki regen]; Exempt the apex tier from the accent rule [rewrite the sweep 11g's QA explicitly wrote to prevent exactly this exemption; highest-risk rule surgery] | Ratify: both deviations were proven forced by mutation (restoring either rulings' literal is a red suite today), and the ledger records them as accurate, so reverting buys a rule amendment for no gameplay gain. | row 3 (duplicates the seeded '11h's two deviations (ratify or revert)' standing item) |
| D002 | apex-feast-crop-count | Does the apex feast's crop row rise to the accent cap of 2 (contributing 80 against a count-reading reference of 56) or stay at 1, once RULE 2's value-half reading is ruled? | state.md, Phase 15c apex-feast ledger (lines 15158-15164 as read: 'THE CROP IS AT ONE RATHER THAN THE ACCENT CAP OF TWO, DELIBERATELY'; at 2 all three rows join RULE 2's refused census, six entries to nine; at 1 it clears BOTH readings) and the rejection list (line 15615). | Keep the crop at 1 [no-op (clears both RULE 2 readings; keeps the RULE 2 decision as cheap as Phase 15c left it)]; Raise the crop to 2 [one count edit across the three apex-feast recipe rows plus accent-census and pin updates in the provisioning tests and wiki regen; only coherent if row 3's RULE 2 value-half ruling permits the rows the count reading refuses] | Keep it at 1: the phase chose it deliberately so as not to spend the maintainer's open RULE 2 item, and raising it is only legal under one of the two contested readings, so rule row 3's RULE 2 sub-row first and revisit only if the maintainer wants a richer crop presence. | new (contingent on row 3's 'RULE 2's value-half reading' sub-row; does not duplicate it) |
| D003 | apex-hoe-acquisition-channel | Which record is authoritative for the apex hoe: the executed Decision A (tier 5, trainer at engineering 125, per the phase file) or the 11i handoff (tier 6, drop), with the two costed flips as the menu? | state.md, 11i/11j ledger 'HANDED BACK, NOT CLOSED. The maintainer owns which record is authoritative' block (lines 11669-11675 as read, both flips costed) plus Decision A as executed (lines 11679-11684); still open in the Phase 15c ELEVEN roll-up (lines 15600-15601). | Ratify the executed record (tier 5, trainer) [no-op beyond the RULED line]; Flip the acquisition alone at tier 5 (trainer to drop) [one line plus its pin, per the ledger's own costing]; Flip to the 11i handoff shape (tier 6, drop) [a pattern item (second new id, art park row, M16 check), a HEROIC_VENDOR_STOCK row, pattern-universe and ITEM_ART_PENDING counts re-derived, a wield-table tier-6 row authored first, and Decision B re-opened] | Ratify the executed record: the ledger's structural argument stands (FARM_CROPS' tier type is 1/2/3/4, so a tier-6 hoe has no crop tier to open without 11e-scope crop content), and the tier-5 shape is built, tested, and premise-checked. | row 3 (duplicates the seeded 'apex hoe channel (the Decision-A contradiction)' standing item) |
| D004 | crop-accent-cap-refusal | Rule RULE 2's open value-half (count) reading, which settles whether the apex-feast crop reagent count re-tunes from 1 to the accent cap of 2. | state.md, the apex-feast refused-alternatives census (lines 15611-15616: 'The crop at the accent cap of 2. Legal, and refused because it would have grown an open maintainer item by three rows') plus 11i open item 10 (lines 13690-13694: RULE 2's value-half cost now six entries across four rows). | Rule the standing reading; crop count stays 1 [no-op (RULED line closes the census; the refusal becomes permanent)]; Rule the alternative reading and re-tune the crop to 2 [apex feast def edit in src/sim/content, farm-recipe and apex pins re-derived, the RULE 2 census re-counted (six-across-four grows by three rows), wiki regen] | Rule the standing reading and keep count 1: the packet refused this re-tune twice as not worth growing the open item, so its own records lean status-quo; the value is the ruling itself, which retires the census either way. | row 3 (expands the 'RULE 2's value-half reading' standing item) |
| D005 | flask-capstone-deviations-ratify | Ratify or revert 11h's two forced accent-sweep deviations (flask grain at 1 versus 11h-GATE-C's 2; capstone base crops at 2 plus fine 1 versus 11h-GATE-D's 3), knowing both restored literals red the standing accent sweep unless RULE 2's cap/count half is amended. | state.md:11503-11507 (11h QA hands the two deviations back: 'Ratify or revert; the cost of each is one number per row and a red accent sweep until RULE 2 is amended') and state.md:14361-14378 (the later audit re-ran mutations Y8/Y9 plus per-row variants on a frozen tree: every restore reds, so the deviations are FORCED and remain the maintainer's ratify-or-revert). Duplicates seeded row 3 (the thirteen standing items explicitly include '11h's two deviations (ratify or revert)'). | Ratify both deviations as shipped [no-op; a RULED line at the 11h hand-back block]; Revert to the GATE-C/D literals and amend RULE 2's cap/count half [two recipe rows in src/sim/content/recipes.ts plus the RULE 2 amendment and re-pin of the five accent arms across two files that Y8/Y9 and the per-row variants proved red]; Revert one deviation only [same class of work but the per-row mutation runs show a partial restore still reds (the hearth alone at 3 reds; the runewater flask alone at 2 reds five arms), so it buys nothing without the RULE 2 amendment] | Ratify: the audit independently re-proved (Y8/Y9 re-run, not re-argued) that both deviations are forced by the standing accent sweep, so a revert costs a RULE 2 amendment plus five re-pinned arms for no gameplay difference. | row 3 |
| D006 | thirteen-maintainer-open-items | Confirm that each of the thirteen standing state.md maintainer items (scroll/elixir 15c parity, RULE 2's value-half reading, the stale-client window, 11h's two deviations, 11i's session cap, prog_first_harvest's 13 catches, the apex hoe channel, the shipped-id golden policy, Decision D's band scope, the reworded wiki keys' stale fills, col_junk_drawer's three of margin, zone 1's no-poor-drop, and the eleventh-hour additions the ledgers carry) expands into its own priced value-or-basis row rather than one blanket ruling. | state.md, 'OPEN ITEMS: THE THIRTEEN ARE THE MAINTAINER'S, AND THIS PHASE ADDS NONE' (read at state.md:17856-17868; inventory cite 17753), whose named members (col_junk_drawer margin, zone-1 no-poor-drop) match seeded row 3's enumeration exactly. The farming OPEN-items list the inventory conflated (crop display names, (bg) intro grant fence, the epoch-bump read; farming/state.md:4379ff) is a distinct set that expands under seeded row 4, not here. | Expand one-row-each in the final table [the Phase 18 QA derivation work seeded row 3 already mandates ('to be priced, one row each'); no code]; Collapse into one blanket ruling [no-op on paper, but it violates the no-ruling-inferred-from-silence stopping rule since several members need a distinct value (session cap seconds, margin amount, band scope)] | Expand one-row-each: seeded row 3's own text mandates it, and the members are heterogeneous enough (values, ratifications, and a Phase 20 fold-in) that a blanket answer would leave rows closing without their own ruling id. | row 3 (duplicates the state.md thirteen umbrella; the farming enumeration in the inventory's decisionNeeded belongs to row 4) |
| D007 | tier6-tool-rung | Should a tier-6 land-tool rung exist at all; if yes, its WIELD_REQUIREMENT_BY_TIER row must be authored first because the wield resolver fails open for unknown tiers, and its interaction with the Decision-A contradiction must be settled. | state.md, the rejection-list block 'THE REJECTION LIST, AS RECORDED DESIGN' (lines 12299-12301 as read): 'A tier-6 tool rung for anyone ... resolver fails OPEN, so it would ship ungated; recorded in brainstorm.md as future-tier intent instead.' Code confirms: src/sim/professions/wield_gate.ts, WIELD_REQUIREMENT_BY_TIER covers tiers 1-5 and wieldRequirementForTier returns 0 for a missing tier by documented design. | Keep the rejection standing (future-tier intent stays in brainstorm.md) [no-op]; Author the tier-6 rung now [a TIER6 proficiency constant plus the WIELD_REQUIREMENT_BY_TIER row FIRST, then tool item + recipe content, deeds/reliquary/wiki/art same-change obligations, sim tests, and a settlement of the Decision-A apex-hoe-channel contradiction it collides with (seeded row 3's item)]; Independent hardening: make the resolver fail closed on unknown tiers [changes the documented fail-open-toward-pre-R22 behavior in wield_gate.ts; sim test plus both-hosts parity check; bricks any future unregistered tool until its row lands] | Keep the rejection standing: the ledger records it as deliberate future-tier intent, the fail-open comment is documented design not a bug, and authoring a new tier mid-packet would reopen the Decision-A contradiction the seeded table already prices separately. | new (interacts with row 3's apex-hoe / Decision-A item but is a distinct decision the seed does not carry) |
| D008 | tool-gate-obtainability-ruling | Must obtainability rulings (11h-GATE-B and its 11i/11j/11k heirs) derive through wieldRequirementForTier beside farmCropSkillThreshold, i.e. name the wield rung in the ruling basis, or does the profession threshold alone suffice? | state.md, the 11h-to-11i handoff block, open item 6 (state.md:14932-14939 as read 2026-09-01, address re-measured; the earlier 14751 cite had rotted, and the handoff twin sits at :15095-15097): 'THE TOOL GATE IS REAL AND UNRULED (open item 6). If 11i writes player-facing copy about what a catch or a crop asks of a player, it consults wieldRequirementForTier as well as the profession threshold.' No later RULED line exists. | Rule the basis must name the wield rung [amend the 11h-GATE-B basis text and the heir records in state.md (doc edits); one audit pass that player-facing obtainability copy already consults wieldRequirementForTier, code changes only if that audit finds a wrong line]; Rule the profession threshold alone suffices [no-op beyond a dated RULED line; the derivation stays incomplete on the record and future heirs inherit it] | Rule the basis names the wield rung: the handoff already obliges 11i's player-facing copy to consult wieldRequirementForTier, so the ruling basis should match what the copy rule demands rather than leaving the derivation and the copy on different authorities. | new (not among the thirteen standing items seeded in row 3) |
| D009 | zone1-no-poor-drop | Author a starter-zone poor (gray) mob drop back onto the zone-1 and starter-island kill tables so the guide's Sell Junk loop works before zone 2, or accept the gap on the record. | state.md, 'THE MAINTAINER'S ITEMS, THIRTEEN NOW' block (lines 16944-16948 as read): zone 1's only three grays were promoted (bandana, tallow, scale), 'a starter-zone character has nothing for the Sell Junk button the guide teaches until zone 2; the candidates are a poor drop authored back onto a zone-1 table or the gap accepted on the record'; later records widen it to both starter zones (the island's items are all quest-kind). | Author one new gray item onto the zone-1 and starter-island kill tables [a new item id in src/sim/content plus committed WebP art, loot-table rows, wiki regen, the item-art audit, census row; no deed/reliquary (poor junk is not conquerable content)]; Accept the gap on the record [no-op dated RULED line; the taught Sell Junk loop stays dead until zone 2]; Reword the guide to teach Sell Junk at zone 2 [guide prose key edits plus the reworded-key stale-fill exposure the M16 gate cannot see (the toolsNoteThreeRods precedent)] | Author the gray drop: the guide teaches the loop in the starter zone, so accepting the gap leaves a taught-loop contradiction, and one junk item is cheaper and safer than a guide reword that re-triggers the invisible stale-fill trap. | row 3 (it is the thirteenth standing item, named verbatim in the seed: 'zone 1 carries no poor mob drop') |
| D010 | art-batch-ledger | Schedule the maintainer art pass on his master SHA over the parked art batch: the merged ITEM_ART_PENDING 81 ids, DEED_ART_PENDING 20 rows (CORRECTED 2026-08-31 from 15; 15 was the true Phase 16 count and the live set has held 20 since the release/v0.41.0 merge, re-derived from src/ui/icons.ts), and the judgment sub-items (cross-family icon reads, 32px legibility eyeballs, the map-pin color unification)? | farming/state.md, the ART BATCH HANDOFF row (lines 439-451 read): the deliverable is the ledger, committed art demands the maintainer's master SHA per the profession_icons E2 rule; state.md, Phase 16 ledger art reconciliation (lines 22180-22199 read): the merged ITEM_ART_PENDING holds exactly the packet's 81 parked ids, DEED_ART_PENDING 15 rows AS READ AT PHASE 16, art wave runs on master after the packet. CORRECTED 2026-08-31 by re-deriving the set from src/ui/icons.ts: it holds 20 rows, the five extra being the Crucible raid deeds dgn_ignivar, dgn_ignivar_heroic, dgn_varkhul, dgn_varkhul_heroic and dgn_varkhul_flawless, which the release/v0.41.0 merge appended BEHIND the 15. They are release-owned debt, correctly parked and not packet obligations, but they are five more paintings in the same wave, and this row is what sizes that wave for the maintainer, so an under-count here is the thing worth fixing. | Schedule the art wave post-merge on master [no-op for the packet (the parked allowlists are the deliverable); the maintainer runs the asset-pipeline batch on his SHA and drains ITEM_ART_PENDING/DEED_ART_PENDING then]; Author art in the packet branch now [violates the recorded profession_icons E2 rule (committed art demands the maintainer's master SHA), forks provenance, and re-litigates a settled doctrine paragraph in the Phase 16 ledger] | Schedule post-merge: the ledger is explicit twice over that the batch parks by rule, so the only ruling needed is the maintainer acknowledging the wave (and its judgment sub-items) as his queue item, dated. | row 4 (expands the farming handoff table's ART BATCH HANDOFF handed-to-maintainer row) |
| D011 | crop-display-name-lore-pass | The maintainer lore pass over the eight crop display names: rename any (ids locked by D11, only display names may move) or ratify the shipped names. | farming/state.md, handoff table row 'Crop display names lore pass (D11 ids locked) / packet / maintainer / open ruling-owed' (line 488; echoed at 427-428 and the D11 block); no closure row. | Ratify the shipped names [no-op (flip the handoff row closed with the ruling id)]; Rename some or all display names in-session [display-name edits in the crop item defs, English catalog updates, non-Latin fills for any wordy new name (M16), wiki regen (guide freshness gate), no id or save impact]; Defer past the PR [no-op now; the row stays open carrying the ruling id and reason] | Take the ruling in-session: it is display-name-only under locked ids so either answer is cheap, and if the maintainer has no lore preference, ratify the shipped names to close the row rather than defer. | row 4 (the 'name lore passes' tuning read; the hoe-names row at line 489 is its sibling) |
| D012 | crop-durations-gain-survival | Value read over the core farming pacing constants: crop durations (with the 5-minute sibling-gap advisory), the gain schedule, the survival endpoints, and the pick floor/cap; ratify the shipped values or name new ones. | farming/state.md, handoff table row (line 487): 'Crop durations (5-minute sibling gap advisory), gain schedule, survival endpoints, pick floor/cap / P3 / P5 / maintainer / open ruling-owed'; neighboring rows that closed say so explicitly, this one does not. | Ratify the shipped constants [no-op (flip the handoff row closed)]; Re-tune any constant to a maintainer-named value [farming content constant edits, farming test pins re-derived, parity goldens regenerated if timing is sampled (UPDATE_PARITY, its own reviewed commit), wiki regen; bounded by the 11e-D-A frozen band boundaries] | Ratify the shipped values: they survived the whole packet's QA and there is no play-signal arguing for movement, and a pure value read with no data leans status quo; any re-tune must come as the maintainer's own numbers, never guessed. | row 4 ('crop durations' is named in the row 4 tuning-reads list) |
| D013 | farm-biome-palette-trim | Wire FarmBiomePalette.trim to a draw-time consumer in the planned higher-tier trim pass, or drop the authored-but-unconsumed channel? | farming/state.md handoff table (line 525 as read): 'wire trim in the higher-tier pass or drop the channel / P13 integration / maintainer / art batch / open, handed-to-maintainer'. Code confirms trim still authored-unconsumed: src/render/farm_patches_core.ts:112-120 ('unconsumed at draw time today'). | Keep the channel, wire it in the art-batch higher-tier pass [Defer with the art batch (a ruled deferral, row status flipped); the eventual pass adds a setColorAt consumer in src/render/farm_patches.ts plus a render-performance review]; Wire it now [The same consumer work this session: farm_patches.ts tint application, palette test extension, render-performance reviewer, plus art judgment the batch owns]; Drop the channel [Delete trim from FarmBiomePalette and all FARM_BIOME_PALETTES rows plus the exhaustiveness test, then re-author the per-biome values if the pass ever comes] | Option 1: the row is already handed to the maintainer/art batch, the authored per-biome values are exactly what the higher-tier pass needs, and deleting data a planned pass consumes just to tidy a field is negative work. | row 4 (a handed-to-maintainer farming handoff-table row, expanded one-row-each by row 4) |
| D014 | farmer-titles-greetings | Sign off, or reword, the four farmer NPC titles and greetings as authored in Phase 9 (Allotment Keeper, Fen Paddy Farmer, Highwatch Terrace Farmer, Parterre Gardener). | farming/state.md handoff table (line 502 as read): 'Phase 9 farmer titles and greetings as authored / P9 / maintainer / open ruling-owed'; the four titles named in the Phase 17 close summary (lines 4386-4387). No closing ruling in either state file. | Sign off as authored [no-op (ruling id, row status flipped)]; Reword any of the four [Content edit in the farmer NPC defs plus src/ui/world_entity_i18n.ts names, non-Latin fills for wordy renames (M16), wiki regen] | Sign off (option 1): the titles shipped through Phase 9 review, each anchors to its zone's real geography (Fenbridge paddies, Highwatch terraces, the Evergarden parterre), and nothing since has flagged the copy. | row 4 (an open-family farming handoff-table row, expanded one-row-each by row 4) |
| D015 | farming-100-crest-commission | Commission and approve the final prog_farming_100 Harvestmaster crest (crossed hoe and sickle over a wheat crown per the brief) to replace the committed interim tied-wheat-sheaf medallion, or keep the interim art. | farming/state.md, deviation (bt) (lines 4050-4057 as read: the title shelf forbids fallback art, interim crest COMMITTED at public/ui/deeds/prog_farming_100.webp) plus the art-batch ledger handoff row ('DEED_ART_PENDING 8 plus crest replacement', handed-to-maintainer); docs/achievements/icon-brief.md:167 flags the commissioned replacement. | Keep the interim medallion, replacement stays flagged for the maintainer art pass [no-op (the brief already carries the flag; ruling records the deferral)]; Commission the final crest now [Art generation plus the sanctioned converter run replacing the WebP, CREDITS/mapping row update, reliquary_cell_art pin re-check; art judgment the maintainer pass owns] | Option 1: the interim crest is a deliberate, test-legal stand-in and the replacement is maintainer-commissioned art already routed to his art pass; an in-session generation would preempt the very call this row hands him. | row 4 (the crest replacement is a named element of the handed-to-maintainer art-batch ledger row row 4 expands) |
| D016 | farming-100-dormancy-waiver | Ratify the now-closed prog_farming_100 dormancy-waiver record (self-cleared by Phase 11e's seed stocking, GATE 1 discharged) and decide whether to take the still-untaken D12 golden-harvest cadence read (1/90 per harvest times survival, far rarer in wall clock than per-swing). | farming/state.md handoff table (line 470 as read): '(bs) dormant-deed waiver ... plus the D12 cadence read / P10 / maintainer / open ruling-owed'; the MAINTAINER-READ-OWED text at lines 4045-4049; the D12 framing at lines 416-417. The waiver itself closed (the D11/(bo) row above it reads CLOSED 2026-08-21 by Phase 11e, deeds earnable), so only the ratification and the cadence read remain. | Ratify the record and take the D12 read in-session [A paper derivation off shipped constants (1/90 x survival endpoints to a wall-clock celebration rate), recorded on the row; no code]; Ratify the record, defer the D12 read [Ruling id on the row with the read left named; the row cannot fully close]; Reopen the waiver [Moot: Phase 11e stocked the seeds and inverted the honesty arm, so there is nothing dormant to waive] | Option 1: the waiver half is already discharged in-repo so ratification is free, and the cadence read is arithmetic over constants the ledger already states, closing (bs) completely in one sitting. | row 4 (explicitly named there as the '(bs) waiver cadence' tuning read) |
| D017 | farming-any-profession-deeds | Accept, or carve out, the shipped default that farming automatically counts toward existing any-profession-at-N deeds (including the Master Gatherer trigger) via the data-driven GATHERING_PROFESSION_IDS arm. | farming/state.md handoff table (line 492 as read): 'Farming counting toward any-profession deeds (default yes, automatic) / P1 / maintainer / open ruling-owed'; the accepted-default framing at lines 2311-2313 and the Phase 17 close list at line 4440. No closing ruling exists. | Accept the default [no-op (ruling id, row flipped; the automatic arm is already the shipped, pinned behavior)]; Carve farming out [An exclusion in the deeds trigger derivation (src/sim/content/deeds.ts side), tests/deeds_content.test.ts update, wiki regen, and player-visible regression for anyone already credited] | Accept (option 1): the record calls it the accepted default, the data-driven arm has shipped and been pinned since Phase 1, and a carve-out now would retroactively strip earned deed credit for no stated design gain. | row 4 (an open-family farming handoff-table row, expanded one-row-each by row 4) |
| D018 | farming-gain-schedule-ratification | Ratify or retune the derived FARMING_GAIN_SCHEDULE gain column (0.25/0.125/0.0625/0.03125), which replaced the provisional 1/0.5/0.1/0.02 via 11e Decision A against the settled 70-to-75 calendar-day target, with the boundary column (25/50/75/100) frozen as the teaching-ceiling source. | state.md R19 charter block (lines 164-172, the TUNING PROVISIONAL FLAGGED marker) plus the 11e prediction table (state.md:8297-8313, Decision A: gain column moved, boundary column pinned separately); the open handoff row is farming/state.md:487 (Crop durations, gain schedule, survival endpoints, pick floor/cap: open ruling-owed); live values confirmed at src/sim/professions/farming.ts:324-329. Expands seeded row 4 (the farming handoff-table tuning reads; this is the crop-durations/gain-schedule row). | Ratify the derived schedule [no-op beyond the RULED line on farming/state.md:487 and dropping the PROVISIONAL wording from the R19 charter and the farming.ts comment (comment-only edits)]; Retune the gain column [new gain values in src/sim/professions/farming.ts plus re-pin of the gain-column arm in the farming suites and a re-run of the calendar-days-to-100 model; the boundary column must not move (it re-maps crop gray-out for every live farmer)]; Defer past the PR [no-op; recorded as a ruling with the maintainer's reason per the Phase 19 charter] | Ratify: the packet's own records lean this way, since the derived column is dyadic-exact, lands 74.00 days inside the settled 70-to-75 window, and 11e already pinned the two columns separately so only the ratified half can ever move. | row 4 |
| D019 | harvest-feast-payout-retune | Keep harvest_feast's party payout at the 5-stamina/600-second serving it inherited when 11c-D-2 re-tuned its dish, or deliberately re-tune the feast (for example a dedicated feast dish)? | state.md, the QA INFO record (read at state.md:7072-7080; batch cite 7006 drifted): the re-tune dropped the shared feast from 12/900 to 5/600 as an unnamed CONSEQUENCE of 11c-D-2 ('farming tops out one below the apex'), 'recorded here so a later phase re-tunes it deliberately'. Live: src/sim/content/profession_items.ts:1037 (evergarden_braised_greens wellFed 5/600) and :1064 (harvest_feast dishItemId). The feast tuning reads (charges 10 / 180 s / fee / sellValue) are already row 4 material (farming/state.md:418-421). | Ratify 5/600 (the 11c-D-2 consequence stands as the deliberate value) [no-op beyond the ruling line; the dominance-sweep adjacency arm already pins it]; Mint a dedicated feast dish at a chosen value [A new content def plus feast.dishItemId repoint, sim tests, wiki regen, item art (WebP) and non-Latin name fills, and it reopens the 11c-D-2 well-fed ladder the dominance sweep pins]; Re-point the feast at a higher existing dish [One-line content edit plus re-pinning the dominance/adjacency arms, but it breaks the 'farming tops out one below the apex' premise 11c-D-2 wrote] | Ratify 5/600: the drop is a direct consequence of 11c-D-2 as written, the adjacency is now deliberately pinned, and any richer payout reopens the settled well-fed ladder for a communal flavor item; this row expands row 4's feast-tuning read with the masterwrought-side derivation attached. | row 4 (expands the feast-tuning read) |
| D020 | hoe-display-names-prices | Ratify or rename the hoe ladder's Skysilver/Osmium compressed-coinage display names, and confirm or move their vendor prices? | farming/state.md handoff table row (farming/state.md:489): 'Hoe display names (Skysilver/Osmium compressed coinages) and prices / P5 / maintainer / open ruling-owed', still open where neighboring rows carry CLOSED annotations. This is literally the 'hoe names' entry in seeded row 4's tuning-read list. | Ratify the shipped names and prices [no-op beyond flipping the handoff row with a ruling id]; Rename (a lore pass) and/or re-price [Content edits in the hoe defs plus every pinned literal (professions_starter_tools tool sets, gossip/vendor pins), non-Latin name fills for wordy names, wiki regen, and price ripples into the P5/P7 economy pins] | Ratify as shipped unless the maintainer holds a lore preference: the names and prices have survived every phase since P5 without a gameplay complaint, and a rename buys pin churn and fill cost for pure flavor; this duplicates seeded row 4's hoe-names read. | row 4 (duplicates the hoe-names tuning read) |
| D021 | intro-grant-fence-trade-pipe | Extend the face-to-face trade guard (src/sim/social/trade.ts) to honor noVendorSell/noMarketList, or accept and record the 4-copper-per-cycle trade-pipe leak on quest-granted garden_hoe and vale_wheat_seed (the garden_hoe crafting-reagent liquidity knock-on rides the same call)? | farming/state.md, the (bg) block (read at farming/state.md:3783-3810; the handoff row 'open ruling-owed' at :477 and the handed-to-maintainer deviation list at :414-415): face-to-face trade is the one exchange pipe not reading the two flags, 'a 4-copper-per-cycle balance leak shared with every requiredItems item, not a security hole'; options stated verbatim ('extend the trade guard... or accept and record. Left as-is this phase'); the QA-named knock-on: noMarketList already forces bronze-hoe engineers to Farmer Jessica (unlimited, 20 copper) or a face-to-face trade. This is standing deviation (bg) in seeded row 4. | Accept and record the leak (status quo, ruled) [no-op beyond flipping the (bg) handoff row]; Extend the trade guard to honor noVendorSell/noMarketList [A general trade-rule change in src/sim/social/trade.ts affecting every requiredItems item, sim+parity tests, a denial reason key for the client, and it closes the one remaining hoe-reagent transfer pipe, worsening the recorded liquidity knock-on] | Accept and record: the ledger itself sizes the leak as a 4-copper class-wide balance nit rather than a security hole, and extending the guard is a general trade-rule change whose main measurable effect would be cutting the last cooperative pipe for the garden_hoe reagent; this duplicates seeded row 4's standing deviation (bg). | row 4 (duplicates standing deviation (bg)) |
| D022 | ip-17-push-consent | Does the maintainer give the word, and when, to push the merged masterwrought branch and open the single delivery PR (the one hand-back no delegation can supply)? | farming/state.md handoff table, ip-17-PUSH row 'open ruling-owed' (line 620) and delegated ruling 111 (lines 1403-1409, 'the push and the single PR happen only on the maintainer's own word, at the time'); state.md Phase 17 close block lines 22727-22728 ('NO push, NO PR ... ip-17-PUSH the one structural hand-back'); qr-18-REOPEN sequencing at state.md:22748-22749 ('the PR opens after Phase 20'). | Give consent now, effective at the Phase 20 close [one push (origin only) plus the single PR from the drafted body and screenshot subtrees, then CI babysit; nothing else moves]; Defer past Phase 19 (rule 'after Phase 20', consent given then) [no-op; the row stays the one open hand-back exactly as the Phase 19 starter prompt's STEP 5 expects]; Withhold or rescope delivery [re-opens the one-branch one-PR delivery contract qr-18-REOPEN reaffirmed; every downstream phase premise moves] | Defer to the Phase 20 close: the packet's own records sequence the PR after Phase 20 (qr-18-REOPEN) and the Phase 19 gate is written to leave ip-17-PUSH as the one surviving open row, so today's ruling should be the sequencing, not the push. | row 4 (it is the open-family farming handoff row at line 620, though phase-19-rulings-gate.md STEP 5 already carves it out as the one row allowed to stay open) |
| D023 | linkdead-notice-loss | Ratify the accepted loss that a plot ripening during the linkdead grace window loses its transient ripe notice permanently (every durable surface shows the truth on resume), or fund a sim connectivity concept so the notice can be replayed. | farming/state.md deviation (bb) (lines 3367-3375, 'LINKDEAD ACCEPTANCE, maintainer read owed at feature review', probed: sendRaw drops frames for a non-open socket and the login check stays silent because notified persisted true); the reads-owed list at line 413 and the handoff-table row at line 475 ('open ruling-owed'). (The inventory's state.md:413/475 cites were the farming ledger's lines, not masterwrought state.md.) | Ratify the acceptance [no-op; qr-19 ruling id flips the (bb) handoff row]; Fund a sim connectivity/delivery concept with notice replay [a new sim concept the core deliberately lacks, replay-on-resume across all three hosts, parity goldens and determinism exposure; the ledger's own probe says every honest fix needs this]; Narrow login re-emit hack [refuted by the probe: notified persists true so the login check cannot distinguish delivered from dropped without the same connectivity concept] | Ratify: the acceptance's own reasoning stands (journal, map pins, and fplot status all show the truth on resume) and both alternatives collapse into building a connectivity concept the deterministic sim deliberately lacks, a price far above a lost transient toast. | row 4 (standing deviation (bb), named explicitly in the seeded row's deviation list) |
| D024 | map-doc-farmer-flag | Is the world editor meant to author farmer NPCs? If yes, sanitizeNpc in src/sim/map_doc.ts gains one farmer whitelist line plus a round-trip pin; if no, the silent drop is ratified as editor-curation scope. | farming/state.md handoff-table row line 519 ('handed-to-maintainer', class 'editor curation') and the Phase 9 residual record lines 3865-3868 ('as it drops cardMaster and warfareVendor ... add the line if the editor is meant to author farmers'). Code verified live: sanitizeNpc whitelists only vendorItems/market/banker/dynamic (src/sim/map_doc.ts:317-320), no farmer line, so the drop still reproduces. | Ratify the drop (editor does not author farmers) [no-op; matches the cardMaster/warfareVendor precedent the sanitizer already sets]; Whitelist the farmer flag [one line in src/sim/map_doc.ts plus a map_doc round-trip pin test; opens the question of the sibling dropped flags (cardMaster, warfareVendor) for consistency] | Ratify the drop: farmers ship as authored content, the sanitizer drops its sibling role flags identically, and no editor-authoring need has surfaced in nine phases; reopen alongside the sibling flags only if editor curation ever wants role NPCs. | row 4 (a handed-to-maintainer farming handoff-table row, line 519) |
| D025 | map-pin-color-unification | Commission the painted farm map-pin (a MapMarkerArtId needing the maintainer's master-SHA art) and pick one cross-surface pin color (zone-map oak green vs minimap station orange), or keep the two argued, pinned procedural fills. | farming/state.md maintainer-reads art-batch entry lines 448-449; deviation (bd) plus its Phase 8 QA amendment lines 3383-3403 ('Left as a per-surface choice for the Phase 13 painted-art batch to unify ... maintainer glance invited, no code change in QA'); the art-batch ledger handoff row line 505 ('handed-to-maintainer', 'maintainer art pass'). Code verified: no farm id in src/ui/map_marker_icon_art.ts; the zone map still fills --color-map-oak. | Keep both procedural fills (status quo) [no-op; both painters argue their own choice and both painter tests pin the literal token]; Commission the painted MapMarkerArtId and unify the color [maintainer master-SHA art per the profession_icons E2 rule, the full provenance chain (WebP, mapping.json, CREDITS reword), both painter tests re-pointed; rides the whole handed-off art-batch pass] | Keep the procedural fills and leave the painted pin inside the maintainer's art-batch pass where it is already recorded as asset debt: the per-surface split is argued, honest, and pinned, and the unifying art cannot land without the maintainer's own master SHA anyway. | row 4 (the art-batch ledger handoff row at line 505, handed-to-maintainer class) |
| D026 | monolith-ceiling-repin | At feature review, are the hud.ts and renderer.ts monolith ceilings re-pinned with authoring margin, or kept as zero-margin exact-count pins? | farming/state.md, handoff-table open row "renderer.ts exact-count ceiling re-pin (13774) prepared for feature review / 19th absorb / maintainer / open ruling-owed" at farming/state.md:495, plus the STANDING WARNING coordinator-headroom block at farming/state.md:3095-3105; root CLAUDE.md keeps ceiling raises a maintainer decision; the eighth sync re-measured all 19 rows to exact counts without deciding margin. | Keep zero-margin exact-count pins [no-op (each release sync re-measures; any growth stays loud)]; Re-pin both with a small authoring margin [Edit the two ceilings in tests/monolith_budget.test.ts (one commit); risk: growth inside the margin lands silently, which the ratchet exists to forbid]; Extract first, then re-pin lower [Extraction behind each coordinator's seam plus lowered ceilings and moved tests; the largest work item, out of scope for a ruling row] | Keep the zero-margin exact counts: exactness is the loud-change property the packet's syncs rely on (release growth alone breaking a ceiling is the signal working as designed), and margin trades that signal for quiet growth; the record leans keep. | row 4 (expands the open ruling-owed handoff-table row "renderer.ts exact-count ceiling re-pin prepared for feature review", widened to cover hud.ts) |
| D027 | pending-art-allowlist-signoff | Ratify (or refuse) the PENDING_ART_IDS allowlist deviation that lets gather_farming ship without its profession icon, self-clearing when the maintainer's art lands? | farming/state.md, handoff table row (a) (line 471: 'open ruling-owed'), the maintainer-reads list (lines 409-411), and the art-batch handoff (lines 439-443: committed art demands the maintainer's master SHA); allowlist live and self-clearing in tests/profession_icons.test.ts. | Ratify the allowlist deviation [no-op (flip the handoff row's status with the ruling id; the pin self-clears when art lands)]; Refuse and demand the icon now [Blocked in-packet: committed art requires the maintainer's master SHA (profession_icons E2 rule), so this collapses into scheduling the art wave (see the art-wave row)] | Ratify: the deviation is deliberately self-clearing and the alternative is not executable without the same master-SHA basis the art wave already waits on; this expands seeded row 4's standing deviation (a). | row 4 |
| D028 | pendingsend-dropped-command-stranding | Accept the family-consistent online pendingSend stranding when a command is silently dropped (spectate/reconnect), or order a family-wide re-arm fix? | farming/state.md, handoff table row (bq) (line 499: 'residual: online pendingSend stranding on a silently dropped command (spectate/reconnect), family-consistent / P9b QA / maintainer / open ruling-owed (optional)'). | Accept as family-consistent [no-op (flip the row with the ruling id)]; Order a family-wide re-arm fix [pendingSend re-arm on reconnect/spectate across the whole command family in src/net/online.ts, online tests for every armed command, and a double-send risk to design out] | Accept: the ledger marks the row optional and the stranding matches the whole command family's existing behavior, so a fix is a family-wide net change out of proportion to the residual; this expands seeded row 4's open-family class. | row 4 |
| D029 | portrait-bundle-hash-split | Split the stills-bundle digest into renderer-code and content-graph halves so a pixel-affecting src/render edit is distinguishable from content churn, or keep the single bundle hash? | farming/state.md, handoff table row (al) (line 473: 'art-suite guard extensions plus the bundle-hash renderer/content split / maintainer / open ruling-owed') and the sharpened residual (lines 3000-3004: 'splitting the bundle hash ... is the open call'). | Keep the single hash and ratify (al) as-is [no-op (flip the row; the receipt-free-refresh tightening already forces a rendered receipt on stills-renderer script edits)]; Split the digest into two halves [Rework the digest computation in the art-suite guard, re-mint every bundle baseline, and every future absorb re-mints two receipts instead of one] | Keep the single hash: the guard's later bookkeepingOnly tightening (farming/state.md lines 2996-3000) already closed the worst blindness (script edits demand a receipt again), and the split's per-absorb re-mint cost buys diagnosability no defect has yet needed; this expands seeded row 4's standing deviation (al). | row 4 |
| D030 | simplified-gathering-rows | Ratify the shipped simplified-mode worked-gathering rows (deviation (be): the simplified professions body also paints every gathering row the player has worked, plus the Farming row while any bed is planted), narrow them to the Farming row alone, or revert to the strict CTA-only doctrine? | farming/state.md, Phase 08 QA ledger, the '(be) SIMPLIFIED-MODE GATHERING ROWS, maintainer read owed at feature review' deviation (read at farming/state.md:3406-3427; inventory cite 3384); confirmed still open by the later pre-flight (state.md:20710). Seeded row 4 lists (be) among the standing deviations explicitly. | Ratify the shipped worked-rows shape [no-op]; Narrow to the Farming row alone [one predicate in the buildProfessionsView pure core + professions_view test updates]; Revert to strict CTA-only [same one-predicate change, plus it re-opens the (az) journal-reachability hole for pre-attunement farmers the QA live-drive found (journal reachable only via Shift+K)] | Ratify: the deviation closed a real reachability defect, skill-0 rows stay hidden so a fresh character's window is unchanged, and both modes already paint through the one gatheringSectionHtml builder. | row 4 (expands the standing-deviation (be) entry named in that seeded row) |
| D031 | tier4-dish-quality-rare | Accept or fix the quality-label mismatch where rung-100 farm dishes read a lower quality than the apex consumables on the same rung, without moving the frozen 11c power ladder R5 is measured against. | farming/state.md, rulings table, the tier-3-domination / tier-4-quality row (line 485 as read): PARTLY CLOSED 2026-08-22 by 11f-GATE-A (domination half gone); the tier-4 QUALITY half 'stays open and deliberately untouched ... recorded rather than fixed' because 11c owns the power ladder. | Accept the mismatch on the record [no-op (a dated RULED line on the farming rulings-table row)]; Raise the rung-100 farm dishes' quality label to match the same-rung apex consumables [quality-field edits in the farming dish defs (src/sim/content), foodHp/duration untouched so 11c and R5 never move; re-pin any farm_recipes quality arms, wiki regen, sell-value/sort-rank knock-ons to check]; Lower the apex consumables' quality label instead [touches the 11c-owned apex surface R5 is measured against; per the Phase 19 stopping rules a ruling contradicting a ratified R5 row is an escalation, not an execution] | Accept on the record: the ledger itself already chose recorded-rather-than-fixed for exactly this reason (11c owns the power ladder and R5 is measured against it), and the label is cosmetic while the frozen surface is not. | row 4 (the open quality half of the PARTLY CLOSED farming handoff-table row at farming/state.md:485; the row-4 re-derive from the table's text must carry partly-closed rows' open halves, not just rows stamped open) |
| D032 | watch-fees-compost-husks-tonic | Ratify or re-set the four farming economy constants: watch fees 2/3/4/6, compost sell/buy 2/8, FARM_HUSKS_PER_COMPOST 2, and the growth tonic bonus 0.5/2. | farming/state.md, rulings table (line 482 as read): '/ Watch fees 2/3/4/6, compost 2/8, FARM_HUSKS_PER_COMPOST 2, tonic bonus 0.5/2 / P4 / maintainer / open ruling-owed /', still open beside CLOSED siblings, so never later ratified. | Ratify the shipped values [no-op (a dated RULED line flipping the table row)]; Retune any of the four [constant edits in the farming content/economy modules, re-derived economy pins in the farm suites, wiki regen where the values surface, and a check that no downstream curve-point reuse (the dish-assignments row) shifts] | Ratify the shipped values: they have survived seventeen phases of QA and economy sweeps without a finding against them, and this row is a pure value pick with no recorded pressure to move. | row 4 (explicitly named there among the tuning reads: 'watch and compost fees, tonic') |
| D033 | wellfed-downgrade-legibility | Should the silent tier-4-to-tier-1 well-fed downgrade get a surface beyond the buff hover (for example a combat-log line), or stay invisible by design? | farming/state.md, handoff table (line 498 as read): 'Tier-4-to-tier-1 wellfed downgrade legibility (buff hover is the one surface) / P11 QA / maintainer / open ruling-owed (optional)'; the P11 QA prose record (lines 4206-4210 as read): invisible in the combat log by design, same-name displacement emits a refresh, 'Owner: maintainer UX read, only if the drop should be legible.' | Stay invisible by design [no-op (a dated RULED line; the row is already marked optional)]; Add a combat-log line on a downgrade [an event on the well-fed displacement path in both hosts, a t() key for the line, sim/server test plus parity, FCT/log-noise review]; Strengthen the buff hover only (name the displaced tier) [tooltip copy per docs/design/tooltip-writing.md via the write-game-tooltips skill, t() key, no sim change] | Stay invisible: the record marks the row optional and the mechanism (same-name refresh) is classic-shaped; if the paired last-eaten-wins ruling keeps the mechanic, the buff hover already reveals the state identically in both hosts. | row 4 (an open-family farming handoff-table row, farming/state.md:498) |
| D034 | lucent-weapon-rung-r21-demand-risk | How to answer the recorded demand risk that the apex Lucent weapon enchant clears Greater by only 1 while billing lucent_reagent + arcane_shard + 2 arcane_essence (the shape enchants.ts's own beat-by-3 law exists to prevent), given R5 forbids raising the magnitude: accept, cut the bill, or schedule a demand-side pass. | power-verification.md section 14 'Recorded, not acted on', the +1-over-Greater bullet (lines 1220-1230, 'recorded as a judged risk to the lucent_reagent sink for a future demand pass, not tuned here'); state.md judged-risk block lines 21520-21528 ('the envelope forced it ... raising it is exactly the move R5 forbids'). No Phase 18 actioned marker on either. | Accept the risk on the record, keeping the future-demand-pass note [no-op; qr-19 ruling id on both records]; Cut the bill (drop the arcane_shard or lucent count) [src/sim/content/enchants.ts def edit, recipe-economy and R21 ratio pins, wiki regen; weakens the very shard/reagent sink the risk is about]; Schedule an R21 demand-side pass as real follow-up work [a docs/issue row only; no code this phase]; Raise the magnitude to +3 over Greater [FORBIDDEN: an R5-surface edit (the term lands twice on a dual-wielder, +2 becomes +4 on the binding lane); a Phase 19 stopping-rule escalation, not an execution] | Accept on the record (with the demand-pass note standing): the packet's own two records already grade it 'recorded rather than tuned' because the envelope forced the +1, and every tuning arm either violates frozen R5 or damages the sink it would protect. | row 8 (duplicates the seeded row verbatim: 'the apex weapon rung sits +1 over Greater ... tune, or accept', power-verification.md section 14) |
| D035 | farm-fine-produce-kind-label | Should the twelve farm fine produce ids (FARM_MATERIAL_ITEM_IDS) read 'Fine Material' on their kind line to match the nine ore/log/herb fine grades, or keep the current 'Material'? | state.md, Phase 16 kind-line consistency read (lines 16953-16958 as read): 'a farming-era label split the maintainer may want closed in src/ui/item_kind_label.ts'; seeded as the first half of Phase 19 row 10. Code confirms the split: item_kind_label.ts:55-63 grants fineMaterial only via baseMaterialFor, which excludes the farm ids. | Close the split: farm fine produce reads 'Fine Material' [One arm in src/ui/item_kind_label.ts covering FARM_MATERIAL_ITEM_IDS plus its label test; itemUi.kind.fineMaterial already exists, so no new i18n key or fills]; Keep 'Material', record accepted [no-op (ruling id on the read)] | Close it (option 1): the record leans that way ('may want closed'), 'Fine Vale Wheat: Material' beside 'Fine Iron Ore: Fine Material' is an unmotivated inconsistency, and the fix is one guarded arm with zero i18n cost. | row 10 (duplicates the seeded row's first consistency read) |
| D036 | type-scale-11px-divergence | The Perfecting/naming CSS (and the plant sheet) uses 11px for several metadata roles where DESIGN.md section 5.3 has no 11px row and body text 'never goes below 12px': add the row to the scale, retune the roles up to 12px, or defer to the foundation phase as the record frames it. | state.md, Phase 17 QA DESIGN.md-compliance read (lines 21045-21050 as read): 'the new Perfecting/naming CSS uses 11px for several metadata roles (and the plant sheet likewise) where section 5.3 has no 11px row; pre-foundation vocabulary reused on purpose, so "type on the section 5 scale" is aspirational until the foundation phase retunes it.' DESIGN.md 5.3 Scale (line 361 as read): 'never goes below 12px; compress spacing before shrinking type.' The 11px is live (e.g. src/styles/components.css, the Q28 .prof-craft-row anatomy). | Defer to the foundation-phase retune, recorded [no-op (a dated RULED line; the record already scopes the reconciliation there)]; Add an 11px metadata row (and the 10px chip row) to DESIGN.md 5.3 [DESIGN.md edit; blesses sub-12px text against the section's own floor, a precedent every future window inherits]; Retune the 11px roles up to 12px now [CSS edits across the Perfecting/naming and plant-sheet blocks in src/styles/components.css, overflow risk in dense chip rows, before/after screenshots owed for a visual change] | Defer to the foundation phase: the ledger deliberately reused pre-foundation vocabulary and scopes the retune there, so a one-off fix now would churn the same surfaces twice; verified live against DESIGN.md 5.3 and the shipped CSS (the batch had left this row unverified). | new (row 10's 'two consistency reads' are different items: the fine-produce grade label and the nameplate_canvas ratchet) |
| D037 | apex-armor-silhouette-split | Re-split the five apex armor pieces that are numerically identical to their reference drops (rating aside) into distinct stat silhouettes, or keep the shipped complement-only differentiation? | state.md, Phase 08 RULINGS TAKEN IN-PHASE, fresh-review S3 block (lines 2800-2808 as read: 'Re-split if the maintainer wants distinct silhouettes'); the complement is mechanical (the sweep asserts the reference drop lacks the apex piece's rating field). Distinct from seeded row 14, which rules only the two crit-40 twins against the retuned references. | Keep complement-only differentiation [no-op]; Re-split the five pieces into distinct silhouettes [five R5-surface def edits (stat re-derivation under the band-shape rules), masterwrought_budget and sweep pin updates, wiki regen; must be sequenced with row 14's twin-complement ruling and row 12's R5 re-measure-or-ratify] | Keep the shipped shape unless row 12's re-measure is taken anyway: the identity is the audit's deliberate outcome (band shapes canonical, the Wrath crafted-complementarity lever the research memo endorses), and a re-split is an R5-surface edit the packet froze. | new (coordinate with rows 12 and 14; does not duplicate row 14's twin question) |
| D038 | sunder-lock-exemption-ratification | Ratify the lock-exempt sunder shape (a player-locked raid epic can be deliberately sundered, consistent with disenchant, as the twin pins record), or flip the two pins and add the lock deny to sunderAdmitted plus the id-only victim ladder? | state.md, the 'RULING WANTED (maintainer ratification): sunder lock-exemption was taken by merge fiat, not a recorded ruling' block with the flip recipe spelled out (read at state.md:3060-3068; inventory cite 3016), carried unresolved at the Phase 09 QA tail (state.md:3634-3637). Twin pins live in tests/bag_item_context_menu.test.ts and tests/masterwrought_materials.test.ts, each naming the other. AMENDED 2026-09-01 (addresses only, the question is unchanged): all three state.md cites in this cell have moved. The RULING WANTED block now reads at state.md:3143 to 3150; the inventory cite 3016 no longer lands on this record at all (that line now sits in the phase 09 reviewed-fixes paragraph); and the Phase 09 QA tail carrying it now reads at state.md:3758 to 3762, re-measured against the committed tree after this ruling's own 22-line insert. | Ratify lock-exempt (disenchant-consistent) [no-op (a dated ruling line converts the merge fiat into a ruling)]; Flip to lock-deny [flip both twin pins + lock deny in sunderAdmitted + a lock arm in consumePreferredDisenchantVictim (the id-only ladder has none today) + context-menu gating + sim tests] | Ratify lock-exempt: it keeps the lock meaning identical across both irreversible destruction verbs (disenchant already ignores it), and the mutually-naming twin pins already guarantee the pair can only flip together if the maintainer later reverses. | new (seeded row 16 is the distinct Crucible gear-tier sundering admission) |
| D039 | teardown-comment-citations | Whether and when to take the post-merge packet teardown (ip-17-TEARDOWN): delete docs/prd/masterwrought/ and its farming/ subtree as ONE decision, re-homing the eleven screenshot cone rows, the roughly fourteen permanent-test comment citations, and the two test-read files (merge-deletion-list.md, farming/qa-checklist.md) in the same change. | implementation-plan.md, Phase 17 deliverables, the ip-17-TEARDOWN cut block (read at implementation-plan.md:996-1007); farming/state.md handoff row 110 (shape fixed, closed-by-the-2026-08-20-delegation, read at farming/state.md:1394-1402); decisions-index.md TEARDOWN BLAST-RADIUS ADDENDUM (read at 101-113; the N13 doc promotions are already EXECUTED per 115-123); Phase 18 close reaffirms NO teardown (state.md:22725-22727). Note: the inventory's state.md:527-528 cite was wrong; these are the real homes. | Ratify the recorded deferral: a post-merge chore in its fixed one-decision shape [no-op now; the blast radius (eleven cone rows, ~14 comment citations re-derived by grep at execution time, two disk-read files) is paid post-merge]; Take the teardown in Phase 19 [the whole blast radius early, it violates the phase's standing NO-teardown delivery rule, and it deletes the reviewer's map before the seventeen-phase PR is reviewed]; Retire the chore and keep the packet docs forever [no-op now; permanent stale-PRD debt in docs/prd/ against docs/CLAUDE.md's living-vs-historical rule] | Ratify the deferral: the shape is already fixed by delegation, Phase 19's own standing rule forbids executing it, and the packet docs are the PR's review evidence, so the only live ruling is timing and the record already answers it (post-merge). | new (handoff row 110 is closed-by-delegation, so it sits outside row 4's open classes; no seeded row carries the teardown timing) |
| D040 | ach-first-legendary-crafted-path | Is the crafted earn path for the first-quality collection deeds, including storefront ACH_FIRST_LEGENDARY minted by the legendary promotion's quality:legendary discovery mark, intended, or should the quality discovery marks be gated to drop-sourced items? | state.md, Phase 13 legendary-promotion deed block, AMENDED note (lines 19728-19739 as read: 'ACH_FIRST_LEGENDARY gained a crafted earn path (a maintainer read, recorded in the QA ledger)'), plus the sibling per-material item 7b (lines 1447-1454: 'decide per material whether... is intended'). Mapping live at server/steam/achievement_map.ts:65 and server/epic/achievement_map.ts:70. | Accept the crafted earn path as intended [no-op (record the ruling; closes item 7b's per-material question the same way)]; Gate the quality discovery marks to drop-sourced items [sim change in the collection-deed discovery-mark grant path plus promotion and material tests, re-characterizing the parity golden that already minted col_first_epic for all ten raiders, wiki regen] | Accept: the ledger already carries it as a recorded maintainer read, the deeds are cosmetic-only (renown 0), and gating would re-open shipped goldens for no power consequence. | new |
| D041 | apex-armor-no-requiredclass | Keep apex crafted armor equip eligibility armor-proficiency-wide (no requiredClass on the nine apex defs), or class-lock them to mirror their band-reference drops? | state.md, Phase 08 apex-armor QA read (lines 3233-3238 as read: 'Judged deliberate under R2 tradability and the classic crafted-gear norm... the standing shape for phases 09/10'), a session judgment with no qr-* ruling; the hands family deliberately does NOT inherit the shape (lines 3369-3373: there the requiredClass list IS the proficiency encoding). | Keep proficiency-wide eligibility [no-op]; Add requiredClass to the nine apex defs [nine def edits in the apex-armor content module, rating-dominance re-check, per-piece sweep pin updates, wiki regen; cuts against the R2 tradability rationale the shape rests on] | Keep it: the recorded judgment rests on R2 tradability and the classic crafted-gear norm, no rating dominance was found (off-archetype wearers get off-stat primaries), and phases 09/10 built on the shape. | new |
| D042 | apex-feast-design-cuts | Which, if any, of the cut apex-feast features does the maintainer fund: a role-pick interaction at the table, raid-scale charges, a second oncePerDay row, or differentiated feast bills (the distinct prop was already actioned by Phase 18; deed power stays closed by the cosmetic-only invariant)? | state.md, Phase 15c REJECTION LIST (lines 15617-15620 as read, with the qr-18-REOPEN annotation showing only the distinct-prop half actioned as apex-feast-distinct-prop); all cuts inherited from the phase file's own list and none re-proposed. | Fund none; the shipped feast stands [no-op]; Fund a chosen subset [per feature: sim feast-module work behind the SimContext seam, IWorld facet and parity updates where UI-visible, sim and server tests, wiki regen; each is its own scoped content/mechanic unit, not a Phase 19 execution] | Fund none now: every cut was a scope call the phase file itself made and nothing re-proposed them; any funded feature is a design phase of its own, so record the ruling and route funded items to a future phase rather than executing in the rulings gate. | new |
| D043 | apex-hoe-deed-decline | Does the apex hoe (or the completed tier-5 tool family) get a Book of Deeds record, or does the asymmetry against the rod's col_deepest_cast deed stand? | state.md, 11j QA verdicts block (lines 12103-12113 as read: the deed decline was correct and simply unwritten, now recorded with the distinction that the rod's deed celebrates reaching a catch band nothing else opens while the hoe opens no crop tier at all); QA re-verified no sibling tier-5 tool carries a deed. | Accept the asymmetry [no-op]; Add a deed for the hoe or the completed tier-5 family [src/sim/content/deeds.ts record(s) per docs/design/deeds.md, tests/deeds_content.test.ts pins, a crest entry in the icon brief, wiki regen; cosmetic-only by invariant] | Accept it: the recorded distinction is principled (the rod's deed marks access nothing else grants; the hoe grants none), it was QA re-verified, and a family deed would celebrate a milestone the content itself does not mark. | new |
| D044 | apex-hoe-reliquary-decline | Do crafted land tools (the hoe, or the whole pick/axe/sickle/hoe family) get Reliquary pages matching the three crafted rods on the shelf, or does the land-tool no-row precedent stand? | state.md, 11j QA 'right for the wrong reason' block (lines 12103-12109 as read, address re-measured 2026-09-01 and now at 12304-12310, an exact +201 offset with the block text unchanged: the shelf catalogues three crafted rods, so the original ground was false; the decline now rests on the land-tool symmetry, and the Reliquary is 'the one surface where the completed tier-5 family reads asymmetric'). | Keep the land-tool no-row precedent [no-op (the asymmetry stays a recorded cosmetic residual)]; Author Reliquary pages for the hoe or the land-tool family [pages per docs/design/reliquary.md with their prose keys, tests/reliquary_content.test.ts pins, wiki regen; one to four pages of bounded content work] | Keep the precedent: the corrected ground is symmetric across pick, axe, and sickle and the residual is one recorded surface, though this is the one decline where both precedents are demonstrably live, so it is a cheap add if the maintainer prefers rod symmetry. | new |
| D045 | apex-rod-delve-marks-route | Do the tier-6 apex crafted tools (the apex rod, and osmium_hoe as the first recorded exception) get a delve Marks route, and if so at what delve tier and Marks price? | state.md, apex-rod ledger (lines 13700-13705 as read: the rod is the SECOND deliberate exception to the 'every crafted tool above tier 3 has a delve Marks route' rule, and 'putting a tier-6 apex tool on a delve counter is a delve pricing and tier decision this phase has no ruling for'), with a later warning not to copy the absence as precedent. | No delve route; both recorded exceptions stand [no-op]; Add Marks rows for the tier-6 tools [new DELVE_SHOPS counter rows requiring a first-ever tier-6 price rung and gate (the counter tops at 56/heroicClear), delve-shop tests and pins, wiki regen; also forces settling the recorded stale rod-price premise on the same counter] | Keep the exceptions: each has a distinct recorded argument (the rod's chain is already luck-free via its deterministic Marks schematic and stays market-listable; the hoe opens no content), and a route would mint the new tier-6 price rung and gate the phase deliberately avoided. | new |
| D046 | apex-vendor-value-asymmetry | Keep the accepted apex vendor-value asymmetry (apex epics vendor at 1.5 to 3 percent of their same-slot drops), or raise apex sellValues and accept the ripple? | state.md, Phase 08 ledger (apex armor catalogs), the vendor-asymmetry acceptance bullet (lines 2825-2828 read): the economy invariant binds sellValue strictly below the 265c-491c bills, and raising the intermediates' sellValues would ripple the phase 07 pins for no gameplay gain. No later ruling amends it. | Ratify the asymmetry as shipped [no-op (record the ruling id on the Phase 08 acceptance bullet)]; Raise apex sellValues toward same-slot drop parity [the maintainer picks new values under the strictly-below-bill invariant; edit src/sim/content items, re-pin the phase 07 economy pins, wiki regen, and re-check the vendor-loop margin the invariant exists to close] | Ratify as shipped: the packet's own record already accepted it with a stated economic reason (vendor value is not power, and the invariant caps any raise below the bills anyway), so reopening buys pin churn for a number nobody needs. | new |
| D047 | arcane-shard-greater-rung-sink | Commission the arcane_shard supply/demand measurement (the deferred future demand pass), or rule definitively that no shard consumer ever lands below the apex band? | state.md, Phase 11m ledger, THE SINKS section (lines 17497-17523 read): 12 consumers, 21 units, scarcest supply in its family, presence holds under 11m-FLOOR, and "adding a consumer below the apex band would spend a scarce supply to fix a ratio nobody measured"; docs/design/power-verification.md section 14 (lines 1220-1230 read) records the +1-over-Greater rung as a judged demand risk to the lucent_reagent/shard sink, left for a future demand pass, not tuned. | Commission the demand measurement [a scoped measurement task (shard inflow from epic/legendary disenchant rates vs the 21-unit consumption) whose result feeds a later tuning ruling; no content edit until it reports]; Rule apex-only forever, no measurement [record the ruling on the 11m sinks section and the four NEVER-arcane_shard reservations in recipes.ts become permanent doctrine; the power-verification.md judged-risk row gets a closing note]; Add a sub-apex shard consumer now [contradicts the recorded refusal with no basis; recipes.ts/enchants.ts edit, census re-run, R21 arms, wiki regen, and it spends the scarcest supply on an unmeasured ratio] | Commission the measurement: both ledgers explicitly park this on a future demand pass rather than a standing verdict, and only a measurement can settle whether the +1 rung and the sub-apex ban starve the sink. Answer it alongside seeded row 8, which prices the same demand risk from the enchant-rung side. | new |
| D048 | autoequip-cap-upgrade-decline | Should auto-equip swap an in-slot Masterwrought upgrade at the family cap (and permit within-family swaps for over-cap legacy loadouts), or stay the silent decline as shipped? | state.md, Phase 01 QA ledger, the design-notes block (lines 642-651 read): 'behavior as shipped, not defects', with the over-cap legacy freeze explicitly flagged 'flag for a ruling if legacy telemetry says otherwise'; live code src/sim/sim.ts (the masterwroughtConflictSlot call, empty ignoreSlots, comment at lines 9183-9186) still declines. No later ruling ratifies it. AMENDED 2026-09-01 (qr-19-autoequip-cap-upgrade-decline): the address moved, the behavior did not. The gate was extracted in commit a7d0225f48 (recorded in the Phase 18 bucket-A closure row maybeautoequip-def-only-peek), so src/sim/sim.ts holds no masterwroughtConflictSlot call at all now: Sim.maybeAutoEquip calls autoEquipFamilyConflict at src/sim/sim.ts:9184, and the empty ignoreSlots on BOTH worn-family rules, plus the design reason for it (auto-equip has chosen no target slot yet, so it declines instead of picking a worn piece to displace), lives in the autoEquipFamilyConflict docblock in src/sim/auto_equip_gate.ts, the two empty arrays at :56 and :68. The cited sim.ts range 9183-9186 is stale; the decline itself is unchanged and still pinned. The cell's last sentence is half overtaken too: qr-19-overcap-legacy-swap-freeze (2026-09-01) ratifies the over-cap legacy freeze, so 'No later ruling ratifies it' now holds only for the in-slot upgrade half, which is the half this row answers. | Ratify the silent decline [no-op (record the ruling on the Phase 01 note; a player report of 'my upgrade stopped auto-equipping' stays designed behavior)]; Teach auto-equip the swap [sim change: reason about which worn piece a swap frees (populate ignoreSlots like the explicit equip path), tests in tests/ for the cap and legacy over-cap arms, parity green in both worlds, and it abandons the unique-equipped-precedent symmetry the comment pins]; Ratify the decline but unfreeze over-cap legacy swaps only [narrower sim change on the legacy arm plus its test; still a behavior change to a shipped path] | Ratify the silent decline for v1: the record shipped it deliberately mirroring the unique-equipped rule, and the ledger's own condition for reopening (legacy telemetry saying otherwise) has not fired. | new |
| D049 | band-numeric-floor | Adopt a numeric floor on bills per band (and pick its value), or keep decision E's presence-only, count-free coverage reading? | state.md, Phase 11j ledger, THE REJECTION LIST AS RECORDED DESIGN (line 12302 read): 'A numeric floor on bills per band, on either arm', refused as recorded design; the guard's coverage philosophy in the same ledger pins presence-only. No ruling adopts a floor anywhere. | Keep presence-only coverage [no-op (the ruling id stamps the rejection-list row as ratified)]; Adopt a numeric floor [the maintainer supplies the number per arm; amend the coverage guard test from presence to count, author any bills a band then lacks (content + wiki regen + census), and every future band change inherits the count obligation] | Keep presence-only: the refusal is recorded design amending decision E, no gameplay complaint motivates a floor, and a count pin is exactly the literal-count doc rot the repo's anchor rule warns against. | new |
| D050 | bandana-tailoring-rung | Keep the bandana's rung-0 tailoring row, or promote it to rung 25 on the strength of its two level-20 galecrest sources? | state.md, Phase 11l BUILT ledger, THE REJECTION LIST AS RECORDED DESIGN (lines 16368-16371 read): the zone1 band carries two of four sources (vale_bandit, gorrak), a counted tie, broken by 'galecrest is a level-20 zone where a rung-0 tailoring row is the honest rung for a 6-copper drop'. No RE-OPENED annotation on this row; no later ruling. | Keep rung 0 (ratify the tie-break) [no-op]; Promote to rung 25 [recipes.ts rung edit, the 11l coverage/census arms re-run, wiki regen; overrides a recorded tie-break for a 6-copper drop with no demand signal] | Keep rung 0: the QA counted the source tie and the honest-rung argument stands unrebutted; nothing has changed the drop's value or sources since. | new |
| D051 | banlist-async-refresh | Adopt the async off-loop banlist refresh through the cached_read seam (a redesign reshaping the synchronous banlist suites), or keep the synchronous stat-held read on the realm loop? | state.md, Phase 13 QA ledger, RECORDED NOT ACTED block (lines 20245-20251 read): the read stays synchronous, held to one stat per second, which bounds frequency but never duration (a hanging mount still blocks the name screen for the mount's timeout); the structural close is 'a redesign the banlist suites' synchronous shape resists, left as a maintainer decision'. Unlike its neighbors in that block, this row carries no qr-18-REOPEN action. | Keep the synchronous stat-held read [no-op (the hang exposure stays stated at the constant, in main.ts, and in DEPLOY.md)]; Adopt the async cached_read redesign [server change through the cached_read seam per server/CLAUDE.md hot-path rules, rewrite the synchronous banlist suites, server-hot-path-reviewer dispatch; closes the duration-unbounded hang window]; Defer past the PR with the ruling recorded [no-op now; the row keeps a ruling id naming the deferral] | Keep the synchronous read (or defer with a recorded ruling): the exposure is a rare operator-side mount hang already mitigated by the keep-the-file-on-local-disk guidance, and the redesign's price is a suite rewrite the packet has no need to carry. | new |
| D052 | bogiron-hauberk-bill | Stretch the trophy-bill bar to admit bogiron_hauberk (300) as a rung-25 armorcrafting trophy row, or keep bogiron_nugget output-excluded as poor trash per the 11l domination standard? | state.md, Phase 11l BUILT ledger, THE REJECTION LIST AS RECORDED DESIGN (lines 16363-16365 read): the hauberk needs ten iron ore or five essence on top of six nuggets from a single 0.3 source; 'hobnail_boots at 90 is the honest bill'. Live tree: src/sim/content/recipes.ts trophy exclusion ledger (lines 4298-4306 read) records the nugget as the ninth output-exclusion, with hobnail_boots itself strictly dominated by the trainer's own rung-0 sabatons, so no hobnail row survives and the nugget is poor trash. | Keep the nugget output-excluded [no-op]; Admit the hauberk at rung 25 [overrides BOTH the stretched-bill bar and the 11l domination standard in one move: a recipes.ts row, the exclusion-ledger comment rewritten, coverage/census arms, wiki regen, and a precedent that any single-0.3-source material can buy a stretched bill] | Keep the exclusion: with the hobnail row itself dead by domination, the nugget has no honest bill at all, and admitting the hauberk would spend a doctrine (the stretched-bill bar) to rescue trash. | new |
| D053 | capacity-pregate-candidate-refusals | Relax the pinned at-most-one-specimen-less-family-per-corpse premise to admit dune_troll (tusk) and frostmane_yeti (horn) as harvest-spread members, or ratify the shipped substitutes (Sundered Horror carries tusk; horn's floor holds on six) and keep the premise? | state.md, Phase 11m ledger, DEVIATIONS deviation (c) (lines 17060-17062 as read at the Phase 18 sweep; AMENDED 2026-09-01, the block now sits further down after the Phase 19 ruling appends): two settled spread candidates refused by the capacity pre-gate premise, both fang carriers; the premise is pinned at tests/corpse_harvest_sim.test.ts ('no corpse tags two specimen-less harvest families together', line 938 read). No later ruling. | Ratify the substitutes, keep the premise [no-op (ruling id on deviation (c))]; Relax the premise, admit both candidates [amend the fitsAll pre-gate and its pinned test, re-tag the two templates, re-run the three harvest censuses and the spread's corpus arms; the pre-gate's reserve-room guarantee weakens for every multi-family corpse] | Ratify the substitutes: the premise is a capacity-safety invariant pinned in the suite, both refusals already have working replacements (tusk carried, horn's floor met), so relaxing it buys nothing the spread still needs. | new |
| D054 | capstone-input-jump | Ratify the fine-twin-driven input cost rise on two already-shipped capstone recipes (recipe_grand_cauldron 1010 to 1410, +39.6 percent; recipe_laden_hearth 606 to 1006, +66 percent, driven by the tier-4 FINE twin at 320 apiece), or direct the cheaper base-2 no-twin bill shape (80 instead of 400)? | state.md, Phase 11h BUILT ledger, the two-hands-back section (lines 11508-11517 read): recorded because no design doc covers it and the phase was not authorised to rule; both rows stay gold-negative by a wide margin, R5 untouched, and the twin is the deliverable (it gives both tier-4 twins a consumer at 125, the R20 shape). The 11h QA (line 14386 read) reproduced both numbers independently. Carried unruled since. | Ratify the jump as authored [no-op (ruling id on the 11h hand-back; the twin consumer stands)]; Direct the base-2 no-twin shape [recipes.ts reagent edit on two shipped rows, BUT it orphans both tier-4 FINE twins' only consumer at 125 (undoing the R20 shape the twin was authored for), re-runs the 11h bill table and census, wiki regen]; Keep the twin, trim its count [a middle number the maintainer picks; same regen/census obligations, partial consumer retained] | Ratify the jump: the packet's own record says the twin is the deliverable and both recipes remain gold-negative by a wide margin, so the flagged number is a cost-of-doing-R20 rather than a defect; this row sits beside (but is not one of) row 3's '11h two deviations' and should be answered in the same sitting. | new |
| D055 | consumable-ladder-design-doc | Does the consumable ladder get a docs/design home, or is the PRD plus def-comment plus pin derivation ratified as its permanent record? | state.md, the carried maintainer-call list in the Phase 11c QA audit record's RECORDED, NO CHANGE block (lines 7415-7417 as read 2026-09-01; AMENDED in place 2026-09-01 from 'the Phase 12 close (lines 7289-7291)', which named the wrong section and pointed at reviewer prose): 'the consumable ladder has no docs/design home, its derivation lives in this PRD, the def comments and the pins (maintainer call)'. No docs/design consumable doc exists; no later ruling. | Ratify the PRD + def comments + pins as the permanent record [no-op (a dated RULED line in state.md)]; Author docs/design/consumable-ladder.md distilling the derivation [one new living doc under the anchor rule (stable paths, no rotting counts); it duplicates content the pins already enforce and can drift, per docs/CLAUDE.md's code-over-doc warning] | Ratify the status quo: the derivation already lives in enforced pins and def comments, and docs/CLAUDE.md itself warns design docs drift from code, so a new doc adds rot risk without adding authority. | new |
| D056 | count1-hollow-floor-members | Are the three count-1 corpse-harvest floor members (Palecoil and Aurelhorn for horn, the Sundered Horror for tusk, all rare or elite) acceptable floor quality, or should a future spread back horn and tusk with ungated density? | state.md, the 11m 'Maintainer reads this phase records, not numbered' block (lines 17861-17870): the two count-1 horn members and the count-1 tusk member sit inside otherwise-met floors with their density recorded; unnumbered, no ruling since. | Accept count-1 members as sufficient (floors are met) [no-op (RULED line; the reads stay recorded)]; Direct a future content spread adding ungated horn/tusk carriers at stated counts [new mob/carrier content records in src/sim/content, corpse-harvest census and hollowness pins re-derived in tests/corpse_harvest_sim.test.ts, wiki regen, plus full content obligations (deeds/art) for any new mob] | Accept: the ledger records these as observations inside otherwise-met floors (density recorded), not defects, and a spread is future content the packet never scoped; the record leans status quo. | new |
| D057 | crafted-drink-line-gap | Does a crafted drinkMana line get added (which profession, tiers, placement), or does the mana-drink economy stay vendor-only? | state.md, qr-11n-WIDE exclusion list 1 (lines 18198-18204): zero crafted drinkMana items exist, pinned with a crafted-side positive control; the R21-shaped gap 'stands (future content, never an 11n nerf)'. Minting one reds the five-drink premise arm (state.md line 18483, red-matrix row F). | Stay vendor-only; record the gap as reserved future content [no-op (the pinned crafted-side control keeps guarding it)]; Add a crafted drinkMana line now [new recipe/item defs plus a profession and tier placement decision, the pinned five-drink premise arm and exclusion-list pin re-cut, R21 demand re-read, wiki regen, item art, non-Latin fills, deed/reliquary check] | Stay vendor-only for this packet: the record explicitly reserves the gap as future content and never an 11n change, and minting the line now reds a pinned premise arm for no in-packet need. | new |
| D058 | crop-adjacency-buffs | Do the approved-and-held D24 crop-adjacency buffs un-park into scope now (with a spec), or stay in the Wave 2 parking lot? | farming/state.md, decisions ledger D24 (lines 2167-2170): 'Wave 2 parking lot (explicitly OUT of this packet): crop-adjacency buffs (approved, held)'; no later line un-parks it. | Keep parked for Wave 2 [no-op (RULED line confirming the park)]; Un-park with a spec now [a full sim feature: a spec first, then an adjacency module behind the SimContext seam, plot-neighbor math, sim tests, tooltip text per tooltip-writing.md, wiki regen, parity-golden impact review] | Keep parked: D24 says explicitly OUT of this packet, and un-parking a whole approved-in-concept feature inside the rulings gate expands scope against qr-18-REOPEN's close-the-packet intent. | new |
| D059 | deepest-cast-deed-trigger | Ratify col_deepest_cast's shipped collectItems OBTAIN trigger (which fires on any acquisition, market purchase included), or fund a per-item craft mark so the deed fires only on crafting the apex rod. | state.md, the 11i deed-trigger deviation block and open item 9 (lines 13646-13657, 13689): 'A per-item craft mark was the alternative and was declined rather than silently taken... THE MAINTAINER'S CALL.' Live code: src/sim/content/deeds.ts:2985-2992 ships collectItems with the 'Obtain' desc. | Ratify the collectItems OBTAIN trigger [no-op (RULED line; desc already says Obtain honestly)]; Fund a per-item craft mark [DeedTrigger union extension, mark namespace registration, a save/load round-trip pin in the same change, migration-safety triggered, deed desc reworded to Craft, deed tests and wiki regen] | Ratify: the ledger calls collectItems the honest shipped mechanism (matching col_glimmerfin and col_full_creel), and the craft mark's namespace-plus-migration cost buys only the exclusion of market purchases from a renown-10, no-title deed. | new |
| D060 | delve-rod-marks-prices | Ratify or re-derive the two delve-shop rod prices (stormreel 24 Marks, tidewrought 56 Marks), which were set on the comfort ladder but are now access prices since the rods gate catch bands 3 and 4. | state.md, the 11i findings block (lines 14308-14317): 'both are ACCESS items priced as comfort items... Re-deriving two Marks prices is a delve-economy decision with no ruling behind it, so 11i recorded the invalidation and changed no number.' Live code: content/delves/shop.ts:174 and :179, with the RE-CHECK TRIGGER comment at :137-150 recording the access status. | Ratify 24/56 as the access prices [one comment touch-up in content/delves/shop.ts retiring the stale premise, plus the RULED line]; Re-derive both on an access basis to maintainer-named values [two number edits in content/delves/shop.ts, delve-shop pins re-derived, wiki regen, an economy read against the three unaffected land rows per rung] | Ratify unless the maintainer names new values: raising Marks prices post-ship nerfs access to already-shipped catch bands, and 11i's own restraint (record the invalidation, change no number) shows the only missing piece is the ruling, not the derivation. | new |
| D061 | design-foundation-token-literals | Land the DESIGN.md foundation-phase token set (wash, scrim, bevel) so the recorded literals (the crafting-tab underline #cc9a3c, the wash/scrim literals, the 0.28 bevel) migrate onto named tokens, or explicitly defer them to that program. | state.md, the UI-phase recorded-literal lines (20582-20584: the underline and wash/scrim literals 'await the DESIGN.md foundation-phase tokens'; 21051-21052: the .prof-track-step.filled 0.28 bevel 'joins the recorded literal set'); DESIGN.md's token rules (sections 4-5) exist but the foundation phase has not landed. | Defer to the DESIGN.md foundation-phase program [no-op (RULED line; the literals stay enumerated and non-silent)]; Land the foundation token set now [semantic token additions in src/styles/tokens.css (wash/scrim/bevel), migration of every recorded literal across the styles layer, DESIGN.md rollout-phase execution with its themeCssVars derivation rules, frontend review and PR screenshots] | Defer: this is DESIGN.md rollout-program work the packet only recorded against (deliberately not silent), and landing a repo-wide token phase inside the masterwrought rulings gate is exactly the scope expansion the gate exists to avoid. | new |
| D062 | disenchant-ratio-outlier | Add a shard-rung disenchant sink or rebalance the essence/shard ratio (shard rung: 12 consumers / 21 units vs essence's 40 / 84, shard the family's scarcest supply), or keep the recorded outlier as-is? | state.md, Phase 15 premise-measurement RECORDED/NOT-CHANGED block (lines 17902-17910 as read): presence holds, ratio recorded as an outlier, a new shard sink refused because it would spend the scarce supply on an unmeasured ratio; reopened under qr-18-REOPEN. | Keep the outlier as recorded (status quo) [no-op]; Take a ratio measurement first, decide after [One measurement session over both reagent files (enchants.ts consumers vs sundering supply), recorded as a read; no code until it lands]; Add a shard-rung sink now [New consumer def(s) in src/sim/content plus masterwrought_materials/sundering tests, wiki regen, against the ledger's own refusal and on an unmeasured ratio] | Keep the outlier (option 1): the packet's own record refuses a sink precisely because shard supply is the family's scarcest and the ratio is unmeasured; nothing has been measured since. AMENDED IN PLACE 2026-09-02 at 19E execution: option 2's "sundering supply" is false (sundering mints sundered_essence only and competes for the same raid epics), the "Phase 15 premise-measurement RECORDED/NOT-CHANGED block (lines 17902-17910)" is the 11m ledger's player-report bullet (state.md:18970 to :18977 at the merge tip 2ebe95e731), essence reads 41 / 86, and the tool-effect recharge is a third shard sink outside the reagent rows; the measurement was taken first and the read is recorded under qr-19-disenchant-ratio-outlier in THE SINKS section, answering D047 and D034 with the same figure (the outlier stands, supply-limited, not starved). | new |
| D063 | ember-accrual-uncapped-veto | Confirm or veto R4's uncapped total banked Maker's Ember accrual for a returning character, given the per-completion payout is already capped at EMBER_ACCRUAL_GRANT_CAP = 20 with the anchor advancing only as far as the grant paid. | state.md, Maker's Ember decisions block (lines 1065-1067 as read: 'UNCAPPED per R4 (a vetoable ruling...)') plus the migration-safety record (lines 1468-1472: payout capped at 20 per completion, R4's total stays uncapped, backlog stays banked); constant at src/sim/professions/masterwrought_materials.ts:88,204. No ratifying ruling anywhere in state.md. | Confirm R4: total stays uncapped, backlog banked [no-op (record the ruling id on the R4 line)]; Cap the banked total at a value the maintainer names [Edit the accrual in src/sim/professions/masterwrought_materials.ts, extend its tests (returning-character arm), record the value]; Veto banking entirely (grant only the current week) [Same file plus test rewrite of the banked-week arms; punishes absence, contradicting the recorded mercy rationale] | Confirm (option 1): the later 20-per-completion payout cap already throttles any single grant, and the backlog only pays out through real completions, so the returning-character windfall R4 flagged is bounded in practice. | new |
| D064 | encounter-owned-aura-wire | Serialize the encounterOwned aura marker to clients now (one new wire byte with parity implications), or keep it server-internal until an encounter actually applies one to players? | state.md, the eighth-v0.41.0-sync AMENDED block, RECORDED-NOT-ACTED maintainer reads (lines 21706-21709 and 22847-22850 as read): 'never rides the wire... a one-byte design call if an encounter ever applies one to players'; not folded into rows 12-16. Code confirms sim-only: src/sim/types.ts:669, zero hits in server/, src/net/, src/world_api/. | Keep it server-internal, trigger recorded [no-op (the record already names the trigger: first encounter that applies one to a player)]; Serialize it now [Aura snapshot encode/decode in server plus src/net ClientWorld mirror, parity pin update, protocol/version touch, for a marker no player-visible aura carries today] | Keep it internal (option 1): no encounter applies the marker to players, so the byte buys nothing yet, and the ledger already frames it as a call deferred to that trigger. | new |
| D065 | fang-band1-gap | Should a band-1 mob template gain the fang componentTag (fang currently spans bands 0, 2, 3 with no band-1 source), and if so which template? | state.md, Phase 15 RECORDED-NOT-ACTED block (lines 18086-18087 as read): 'pre-existing, outside the phase's claim set, floor green. An observation for a future spread.' Fang componentTags live across the zone content tables (src/sim/content/amberfall.ts, frostveil.ts, etc.); no band-1 source has been added since. | Accept the gap, leave it to a future spread [no-op (record the ruling; the observation stays on the ledger)]; Tag a band-1 template the maintainer names [One componentTags edit in the matching src/sim/content zone file plus the content same-change obligations (wiki regen, census/spread assertions), a content-obligations review] | Accept for this packet (option 1): the record itself scopes it as a future-spread observation with the floor green, and picking the template is a content call with no measured pressure behind it. | new |
| D066 | feast-role-pick-interaction | Should the placed feast table gain a choose-your-plate role-pick interaction, replacing the authored dishItemId role model, or does the recorded 11k rejection stand? | state.md:15609-15620, the 11k REJECTION LIST AS RECORDED DESIGN (the bullet listing 'a role-pick interaction at the table ... inherited from the phase file's own list and none re-proposed'; the qr-18-REOPEN annotation on that bullet actioned only the distinct-prop half as apex-feast-distinct-prop, the role-pick remains unjudged-forward); rejection rationale in phase-11k-provisioning-capstone.md (the dishItemId indirection exists precisely so the role is authored, not chosen). Not covered by any seeded row (row 4's feast tuning is charges/fees, not this). | Refusal stands: roles stay authored via dishItemId [no-op; a RULED line on the rejection-list bullet]; Build the role-pick interaction [a new interaction surface plus a wire arm implemented in both Sim and ClientWorld with the IWorld parity pin, a refusal family, sim/server tests, and i18n; it deliberately reverses the dishItemId authored-role design] | Let the refusal stand: the packet's design rationale is load-bearing (the dishItemId indirection exists so the role is authored, not chosen), nothing since has re-proposed the mechanic, and the build price is a whole new interaction surface for an unrequested feature. | new |
| D067 | fenshadow-maul-tuning-read | Is the fenshadow maul's +158 copper per-craft surplus at its specialization-only bill (222 in against 420 out after the 40-copper gold sink, the largest of the nine floors) acceptable under the list-count-only economy doctrine, or should its bill or output be re-tuned? | state.md:16325-16336 (the tuning read flagged for the maintainer's eyes without a ruling row, doctrine list-count-only by design) and state.md:16595-16600 ('Whether the maul's surplus is wanted is the maintainer's read, recorded, not decided'); in-code at the FLAGGED FOR MAINTAINER EYES comment, src/sim/content/recipes.ts:4200-4212. No seeded row covers it (row 3's thirteen items and row 4's farming rows exclude it). | Accept the surplus [no-op; a RULED line at the recorded rows]; Re-tune the bill or output [recipes.ts row edit plus re-pin of the per-row floor in tests/recipe_economy.test.ts (pinned loud, not guarded), wiki regen, and the R21-on-output standard re-judged for the level-13 rung-50 output]; Fold into one surplus ruling with the gathered-cost row [no-op mechanically; one combined RULED line covering the maul and quiver reads] | Rule it as one combined row with the gathered-cost surplus item (same doctrine, same pinned test surface) and lean accept: the economy is list-count-only by design, the floor is pinned so any change is loud, and the surplus is bounded by one tusk per craft from a single named elite. | new |
| D068 | fishing-telemetry-preseed | Accept the fishing metrics family's full 14-zone by 6-band pre-seed (504 series, about 84 percent permanent zeros, only three zones owning their own catch table) or bound the pre-seeded zone vocabulary to zones with catch tables? | state.md:13856-13863 (the 11i-era review: 'a doubling of an existing deliberate pattern rather than a new defect, but nobody had stated the cost'); the pattern is stated as a deliberate bounded-by-construction contract in server/fishing_telemetry.ts:45-59 (HARVEST_BANDS 14 zones x 6 bands so a never-firing pair reads as a real zero, cross product derived by tests/server/http/game_metrics.test.ts). Appears in no seeded row. | Accept the full cross-product pre-seed [no-op; a RULED line at the record]; Bound the vocabulary to zones with their own catch tables [exporter edit in server/fishing_telemetry.ts plus re-derive of the game_metrics cross-product test; it undoes the deliberate zero-vs-gap contract (a never-firing pair would read as a missing series again) and every non-table zone still fishes via the eastbrook_vale fallback, so real catches in bounded-out zones would go uncounted]; Defer past the PR [no-op; recorded as a ruling] | Accept: the pre-seed is the same bounded-cardinality contract server/http/game_signals.ts uses, 504 static series is trivial exporter load, and bounding it would drop real catches from fallback-water zones; the ledger itself classifies it as a stated cost, not a defect. | new |
| D069 | flask-dead-timer-pause | Should flask timers keep ticking while the player is dead (classic fidelity, requiring the flask to be exempted from the shared dead-entity aura guard the resurrection sicknesses rely on), or does the shipped pause-while-dead behavior stand? | state.md:4395-4402 (ADDRESS AMENDED IN PLACE 2026-09-01 under qr-19-flask-dead-timer-pause: the row was written against 4292-4299, which a later release sync moved onto unrelated Eastbrook provenance prose) (the timer PAUSES while dead, updateAuras early-returns for a dead entity; 'classic flasks kept ticking; a fidelity nuance recorded, not changed: the shared dead guard is what the sicknesses rely on'; recorded in the resurrection.ts comment). Code confirmed: the top e.dead guard at src/sim/combat/auras.ts:270, with the header comment declaring both dead guards load-bearing. Appears in no seeded row. | Keep pause-while-dead as shipped [no-op; a RULED line beside the recorded nuance]; Classic tick-through-death for flasks [split the shared load-bearing dead guard in src/sim/combat/auras.ts so flask auras tick while sickness handling still halts, plus sim tests, three-host determinism/parity coverage, and a tooltip/wiki wording check; the guard is explicitly documented as one the sicknesses rely on] | Keep the shipped pause: the deviation strictly favors the player (death extends the flask), the guard it would split is documented load-bearing for the sicknesses, and the ledger deliberately recorded it as a fidelity nuance rather than a defect. | new |
| D070 | flask-tooltip-spellgnaw-exception | Does the flask tooltip's counter-immunity line gain a clause for the pinned Spellgnaw devour exception (per tooltip-writing.md's state-important-triggers-and-limits rule), or does the judged classic-idiom omission (consumable tooltips do not enumerate mob-affix interactions) stand? | state.md:5517-5520 (the judged refusal: dispelled/stolen/canceled are player-counter verbs, 'classic-idiom consumable tooltips do not enumerate mob-affix interactions, so the pinned Spellgnaw devour exception stays out of the tooltip copy'); the interaction itself is real and pinned (state.md:5103 the devour affix eats a flask reading neither flag; state.md:5220 the doc scopes purge to player counters and names Spellgnaw). Appears in no seeded row. | Omission stands [no-op; a RULED line on the judged refusal]; Add the exception clause [reword the filled itemUi flask tooltip key in the English catalog via the write-game-tooltips pass, plus the five non-Latin release refills a reword of an already-filled key silently owes (the M16-invisible reword-staleness trap row 3 already carries for the wiki keys)] | Let the omission stand: the refusal was already judged on the classic-idiom rule, the Spellgnaw behavior is test-pinned and doc-scoped where it matters, and rewording a filled tooltip key re-opens the invisible stale-fill exposure for five locales. | new |
| D071 | food-per-point-inversions | Smooth the two pre-existing per-point food price inversions (boar vs venison, now narrowed 2.24 to 2.18; hardtack vs eel, cross-zone) with maintainer-picked values, or ratify them as classic-shaped pricing? | state.md:18347-18349, in that QA's recorded-not-acted reads block ('Two pre-existing per-point price inversions in the food line did not worsen'); the adjacent read at state.md:18343-18346 notes tests/farm_recipes.test.ts ALLOWED_FOOD_CURVE_POINTS hardcodes the crafted points, so a crafted food retune breaks it. Appears in no seeded row. | Ratify as classic-shaped pricing [no-op; a RULED line at the recorded read]; Smooth the inversions [new price/sellValue numbers the maintainer must pick in src/sim/content/items.ts, re-pin of the food curve arms (the hardcoded 117/243/432/552 crafted points if a crafted row moves), wiki regen, and the economy sweeps re-run] | Ratify: both inversions predate the packet, the packet's own work narrowed rather than worsened them, and smoothing requires value picks the maintainer never made against a deliberately hardcoded curve-point table. | new |
| D072 | fplot-msremaining-wire-field | Add a per-tick msRemaining wire field to the fplot key (accepting that it defeats the heavy-self diff gating and re-serializes every planted farmer at 20 Hz), or ratify ruling (ay)'s client-derived countdown (readyAtMs minus farmNowMs) as the permanent design? | farming/state.md:3345-3355, ruling (ay): the field is DECLINED because a per-tick-varying field on the fplot key would defeat the heavy-self diff gating (the key's bytes are identical between real transitions by design) and re-serialize every planted farmer at 20 Hz for a cosmetic read; the banner in farm_projection.ts records the ruling. The earlier deferral template is farming/state.md:3516-3518. Reopened by qr-18-REOPEN as a judged refusal; not in row 4's deviation list ((ay) is not among (a)/(w)/(al)/(ap)/(bb)/(be)/(bg)/(bh)), so unseeded. | Ratify the decline: client-derived countdown is permanent [no-op; a RULED line on (ay), the farm_projection.ts banner already records the contract]; Add the wire field [fplot wire key change implemented in both Sim and ClientWorld with the parity pin and wire tests, plus accepting the 20 Hz re-serialization of every planted farmer and the loss of the byte-identical diff-gating property] | Ratify the decline: the journal already derives the countdown correctly from readyAtMs, the field buys only a cosmetic read at a real 20 Hz serialization cost, and the inherited (ap) skew acceptance it rides on is separately seeded under row 4. | new |
| D073 | gathered-cost-surplus-list | Are the quiver's +164 and the maul's +278 per-craft surpluses under gathered-cost accounting (crafter gathers the signable lines and pays sellValue, both figures pinned) accepted as shipped, or are the bills/outputs re-tuned, and to what values? | state.md:16944-16951 (that QA's maintainer-items block: 'the quiver is a second faucet under gathered cost (+164 after the sink against the maul's +278, both pinned)') and state.md:16595-16600 (the maul's surplus is the maintainer's read, recorded, not decided); the gathered-column accounting and the FLAGGED FOR MAINTAINER EYES note live at src/sim/content/recipes.ts:4200-4212 with the per-row pins in tests/recipe_economy.test.ts. Appears in no seeded row (row 3's thirteen exclude it; row 4 is farming-only). | Accept both surpluses as shipped [no-op; a RULED line at the recorded rows]; Re-tune the bills or outputs [recipes.ts edits with maintainer-picked values, re-pin of both the vendor-purchase and GATHERED per-row columns in tests/recipe_economy.test.ts, wiki regen, and the R21-on-output standard re-applied]; Fold into one surplus ruling with the fenshadow maul row [no-op mechanically; one combined RULED line covers both reads] | Fold this and the fenshadow-maul item into ONE Phase 19 surplus row and lean accept: gathered-cost surplus prices the crafter's own gathering time (the vendor-purchase floors are the doctrine's guard against buy-to-sell loops), both figures are pinned loud so any drift reds, and the packet consistently recorded rather than fixed them. | new |
| D074 | growth-timer-prose-clause | Should the player-facing farming guide prose state the crop growth-timer spread (a new player-visible timer claim, a wordy key carrying non-Latin fills), or does the spread stay test-pinned only? | state.md, the 11h QA 'CUT, with the reason' block (read at state.md:14547-14551; the batch cite 14454 has drifted): the spread is pinned in test per 11h-GATE-A, and the prose clause was refused as 'a copy decision beyond a QA fix round, and it would be a third reworded value to fill'. No growth-timer claim exists in src/ui/i18n.catalog/guide.ts today. The adjacent CUT item in that block was re-opened by qr-18-REOPEN; this one was not. | No prose claim (status quo): the spread stays a test pin only [no-op]; Add the claim: a guide.* key stating the spread [One wordy English key in src/ui/i18n.catalog/guide.ts plus its non-Latin M16 fills, wiki regen (npm run wiki:content, tests/guide.test.ts freshness), and the copy goes stale if the open crop-duration tuning read (row 4) later moves the values]; Defer the claim behind the crop-duration ruling: write the prose only after row 4's durations settle [A ruling line now; the same key+fills+regen cost later, paid once against final values] | Take the status quo or the deferral: crop durations are themselves an open row-4 tuning read, so authoring a player-visible timer claim before they settle buys fill cost and staleness risk for copy that may be wrong within the same phase, and the packet's own record already leaned that the test pin was the asked-for deliverable. | new |
| D075 | gyrelens-use-effect-gap | Amend ruling R14 to permit minting a cosmetic use family for the gyrelens_array held offhand, or ratify its no-use gap as permanent? | state.md, the frozen apex catalog row (state.md:3360-3361: 'NO use: no cosmetic use family exists' and R14 forbids inventing one) against R14 itself (state.md:120-122: v1 apex items carry pure stats and bounded utility only, no new proc effects anywhere in this packet); the precedent re-read at state.md:18590-18591 explicitly corrects an inverted 'gyrelens precedent is legal' claim, confirming the comment records NO use. Not a seeded Phase 19 row. | Ratify the gap permanent (record a ruling id on the catalog row) [no-op beyond the ruling line]; Amend R14 and mint a cosmetic use family for gyrelens_array [A new tool-effect/use family in sim content plus its resolver, tests, tooltip per docs/design/tooltip-writing.md, i18n fills, and it breaches the R14 'no new effects' fence the whole v1 catalog was balanced under] | Ratify the gap: R14 is a deliberate maintainer fence, the ledger has twice re-confirmed the no-use reading as the precedent, and a cosmetic family for one item is exactly the effect-surface growth R14 exists to refuse in v1. | new |
| D076 | harvest-confirm-channel | Do prompt-mode farming tool effects ever gain a harvest confirm channel (confirm UI plus the promptSlotRefused predicate and its resolver arm edited together), or is the refusal ruled permanent? | farming/state.md handoff row (farming/state.md:576): 'accepted-no-action (re-deferred with the paired-edit rule)', with (ba) re-confirmed journal-informational at farming/state.md:3342. Live code still refuses: src/sim/professions/tools.ts:358-360 promptSlotRefused returns true for 'farming', banner at :350-357 requires the predicate and the resolver arm move together, enforced at :468. Thrice deferred (P5, P8 QA, later re-read); not in row 4's open/handed-to-maintainer classes because its row class is accepted-no-action. | Rule the refusal permanent: flip the handoff row from re-deferred to closed with a ruling id [no-op beyond the doc flip (the paired-edit banner stays as the guard)]; Land the channel: confirm UI plus the paired predicate/resolver edit [A farming-UI confirm flow in src/ui, the tools.ts predicate and resolver arm edited together, sim+parity tests, i18n for the prompt copy; a real sub-feature the ledger sized as the Phase 7/8 decision nobody took]; Keep it re-deferred (status quo, no ruling) [no-op, but the row survives into every future sweep] | Rule it permanent: three phases have independently accepted no-action, the journal is informational by design, and closing the row costs one doc flip while the paired-edit banner already protects anyone who later changes their mind. | new |
| D077 | held-offhand-model-park | Do gyrelens_array and copperlens_ocular leave the conscious held-offhand no-model park and get 3D models, now that the voidbound tome GLB half of the park is already actioned by Phase 18 under qr-18-REOPEN? | state.md, the conscious no-model pin (read at state.md:3503-3506; batch cite 18588 is the later copperlens join at state.md:18696): gyrelens_array and voidbound_grimoire joined the pin, with the voidbound tome GLB carrying a fresh 'RE-OPENED (qr-18-REOPEN, 2026-08-31): actioned by Phase 18 as voidbound-grimoire-tome-glb' annotation. Live: src/render/characters/manifest.ts:987-991 still lists only the three inscription tomes as held_offhand models; no voidbound/gyrelens/copperlens GLB exists under public/models yet. AMENDED 2026-09-01 (qr-19-held-offhand-model-park; the addresses and the Live clause moved, the question did not): every cite in this cell is stale and the Live clause above is now FALSE. Phase 18 executed the tome half, so ITEM_OFFHAND_MODELS carries FOUR tome rows including voidbound_grimoire mapped to tome_voidbound, public/models/weapons/tome_voidbound.glb ships beside the other three, and tests/held_weapon_models.test.ts asserts the retirement positively; the old manifest.ts:987-991 cite now lands on an unrelated clip map. The conscious no-model pin reads in the Phase 09 close-out block of state.md with the RE-OPENED annotation directly above it, and the copperlens join in the Phase 11o ripples block; the cites 3503-3506, 18588 and 18696 all land on unrelated records today. Still true and load-bearing for the question actually asked: no gyrelens or copperlens GLB exists anywhere under public/models, and the exact-set pin still holds both lens ids among its thirteen. | Keep the park for the lenses (voidbound proceeds via Phase 18; gyrelens_array and copperlens_ocular stay consciously model-less) [no-op beyond the ruling line]; Commission lens GLBs too [One image-to-glb pipeline run per item (exporter, optimizer, fingerprint pins, manifest rows, render-performance review), a new lens model family with no existing pattern to reuse]; Fold the lenses into a named future beauty pass [A ruling line now plus a recorded follow-up row that survives into later sweeps] | Keep the park for the lenses: the tome follow-up was the only half with an existing procedural pattern and it is already re-opened into Phase 18, while the lenses would need a new model family for two items whose absence is a conscious, pinned, cosmetic-only gap. | new |
| D078 | horn-gills-specimen-rows | Should horn and gills gain HARVEST_COMPONENT_SPECIMENS rows despite the written decision that a signed pristine jackpot on a bare-hands-floor (tier 1) component would invert the premium ladder the tier table states? | state.md, the 11m-ORPHAN execution block (read at state.md:17398-17408; batch cite 17299 drifted): both components mapped at MONSTER_MATERIAL_TIERS 1 with 'NO HARVEST_COMPONENT_SPECIMENS row for either as a decision written into the table's comment', the one-id exception deliberately not taken, proven by the 1006-ids snapshot diff. No re-open annotation on this block. | Keep the refusal (the table comment stands as the rule) [no-op beyond the ruling line]; Add specimen rows for both [Two HARVEST_COMPONENT_SPECIMENS rows plus their specimen item defs, art, deed/reliquary sweep re-checks, and it overrides the recorded premium-ladder rationale, giving tier-1 bare-hands components a jackpot richer than tooled tiers] | Keep the refusal: the rationale is written into the table comment as a design rule, the tier table's premium ladder is the balancing premise, and nothing since has produced a reason to invert it. | new |
| D079 | i18n-overlay-edit-departure | Acknowledge or reverse the deliberate 18-file src/ui/i18n.locales overlay edit (five non-Latin M16 re-fills plus 13 Latin overlays' stale WoW-scheme rows stripped to pending) that the QA gate flagged for the maintainer's eye? | state.md, the Phase 16 QA departure list item 7 (read at state.md:22142-22147; batch cite 21979 drifted): the phase edits 18 overlay files against the contributor letter-of-the-rule, 'judged correct because leaving them would keep rendering the exact trade dress the re-cut removes', and 'Flagged for the maintainer's eye by the QA gate, seconded here.' No ratifying ruling found afterwards. | Acknowledge (ratify the departure with a ruling id; the 13 stripped-to-pending rows fold into the Phase 20 release fill) [no-op beyond the ruling line]; Reverse: restore the pre-edit overlay rows [Reverting 18 overlay files, which re-renders the removed WoW trade dress in five non-Latin locales and resurrects 13 stale Latin rows the re-cut invalidated; also re-breaks the naming-registry record] | Acknowledge: the edit was the only way to stop live locales rendering removed trade dress, the QA gate itself judged it correct and asked only for the maintainer's eye, and reversal would reintroduce exactly the strings the re-cut exists to remove. | new |
| D080 | intermediate-masterwork-tier-ceiling | Ratify tier 2 as the deliberate masterwork ceiling for all ten intermediates with wyrmfall_core deliberately untiered, or pick new tier values (masterwork.ts is R1-locked, so any change is a maintainer call)? | state.md, the intermediates record (read at state.md:2829-2836; batch cite 2785 drifted): 'Tier 2 for all ten intermediates (the arcanite_bar refined-reagent precedent; tier 2 is the deliberate ceiling, masterwork.ts constants locked)', catalyst side effect pinned EFFECT-DEAD, 'wyrmfall_core stays deliberately untiered (availability premium, not refinement)'; the R1 whole-file lock on masterwork.ts is recorded in the same ledger ('R1 locks the whole file, the reviewer confirmed the alternative seam is forbidden'). | Ratify tier 2 and the untiered wyrmfall_core as designed [no-op beyond the ruling line]; Re-tier the intermediates (values the maintainer must supply) [An R1-lock exception on masterwork.ts, re-derived chance inputs across the nine intermediate recipes, the EFFECT-DEAD pin and phase 07 economy pins re-proven, and no recorded gameplay problem motivating it] | Ratify: the ceiling follows the arcanite_bar precedent, the side effects are already pinned effect-dead, every apex bill maxes through its intermediate, and reopening an R1-locked file needs values nobody has asked for. | new |
| D081 | legendary-name-soft-masking | Should player-given legendary item names get client-side display-time soft-masking, or is the character-name precedent (server offensiveName screening plus kick-then-clear, no client masking) ratified? | state.md Phase 13 'Judged this phase, do not re-raise' list, the 'no client-side soft-masking of the name (the character-name precedent)' entry (lines 19825-19827, carrying NO Phase 18 actioned marker, so still live under qr-18-REOPEN); the D13-5 moderation decision block (lines 19677-19712) decides server screening, sim shape, and kick-then-clear but never display-time masking. | Ratify the precedent: no client masking [no-op; a qr-19 ruling id on the judged line]; Fund a client masking layer [a masking matcher in every name render sink of both hosts (tooltips, inspect, market, peer cards), shipping a client-visible word list where the banlist is deliberately server-side operator-tunable (USERNAME_BANLIST), plus tests; diverges from character names which deliberately lack it] | Ratify the precedent: D13-5's server screening plus the decided kick-then-clear flow already owns moderation, character names set the shape, and a client-shipped mask list would expose the operator banlist, so the refusal the packet already judged is the right standing answer. | new |
| D082 | legendmaker-renown-doubling | Ratify renown 50 for the prog_legendmaker deed (doubling the prog_masterwright 25 precedent) or set a different band-legal value. | state.md Phase 13 QA 'RECORDED, NOT ACTED (maintainer reads)' first bullet (lines 19905-19906, 'band-legal ... a balance number', no Phase 18 actioned marker); D13-7 decision text lines 19724-19728 ('the 50-vs-masterwright-25 doubling recorded as a maintainer read'); the QA grade 'renown 50 in the prestige band, the rule-2 reading defensible' at line 20011. | Ratify 50 [no-op; qr-19 ruling id on the recorded read]; Lower to 25 (match prog_masterwright) [src/sim/content/deeds.ts row edit; the renown-sum pin (3345 at the Phase 13 stamp) and deeds_view counts move, FROZEN_CATALOG_SHA256 re-minted the auditable way, wiki regen]; Another band-legal value [same file, pin, catalog-hash, and wiki costs as lowering to 25, plus a fresh band-legality check] | Ratify 50: the packet's own records lean this way (top-band prestige for the capstone chain, effort-paced never luck-gated, and the QA graded the placement defensible), and nothing in any ledger argues for a different number. | new |
| D083 | lesser-healing-potion-tallow-sink | Keep the lesser healing potion vendor row at its 109 floor-plus-sink price as a deliberate tallow sink even though Hale and the zone-3 counter sell the same potion at 85, re-price it against the counter, or drop the row. | state.md Phase 11l review round, the content reader's finding (lines 16828-16830, 'the row is a tallow sink held under the list-count-only clause, a tuning read beside the boots'); repeated among the tuning reads beside the thirteen maintainer items (lines 16948-16951, 'the lesser healing potion is a tallow sink the counter undercuts'). Not in seeded row 3's thirteen-item enumeration. | Keep the row as recorded (deliberate sink under the list-count-only clause) [no-op; qr-19 ruling id on the tuning read]; Re-price at or below the 85 counter [one vendor-listing edit in src/sim/content plus the vendor economy pins and wiki regen; kills the tallow sink the row exists to hold]; Drop the row [same content edit plus the list-count pin moves; loses both the sink and the convenience listing] | Keep the row: both ledger records already hold it deliberately under the list-count-only clause, the undercut only costs buyers who skip the counter a few copper, and re-pricing defeats the tallow sink that is the row's whole purpose. | new |
| D084 | mastery-model-nongathered-pricing | Should the crafts-to-mastery cost model price non-gathered (mob-drop) reagents, which currently cost zero and therefore let trophy rows win its tie-breaks? | state.md Phase 11l rejection list, lines 16376-16378 ('the trophy rows win its tie-breaks because a mob drop costs it nothing: a model question for the maintainer, recorded with the moved literal'); no Phase 18 actioned marker. The model's current winner is pinned (the rung-25 literal, 'winner pinned at exactly 25 crafts', line 16244). | Ratify zero-cost mob drops [no-op; the pinned tie-break winners stand]; Price mob drops into the model [a basis decision first (sellValue vs buyValue vs derived), then the mastery-model edit, every re-derived winner, the moved rung-25 literal and its pins, and R-row re-checks] | Ratify zero-cost: the model is a tie-break heuristic whose current winners are pinned and defended in the ledger (the galecrest tie-break is argued honest at lines 16370-16371), and pricing mob drops demands a second basis ruling with no evidence any current winner is wrong. | new |
| D085 | materialsbody-onramp-bill-omission | Should the guide's masterwrought materialsBody prose name the on-ramp bill, or stay at its current spoiler altitude (which says nothing false)? | state.md Phase 12-era QA ledger, lines 19024-19027 ('the materialsBody on-ramp-bill omission recorded as judged-no-change: the gate itself graded it a spoiler-altitude judgment call and nothing it says is false') and the JUDGED list at line 19030; neither line carries a Phase 18 actioned marker, so the refusal is live under qr-18-REOPEN but needs the maintainer's altitude call. | Ratify the current altitude [no-op; qr-19 ruling id on the judged line]; Name the bill in the prose [English catalog reword in the guide craftProse key plus wiki regen; the reword-staleness trap (translated rows stay green while stale) plus new pending Latin rows and possible M16 fills landing right before the Phase 20 fill] | Ratify the current altitude: the gate's own grading stands (nothing the prose says is false), the guide's spoiler-safe contract leans away from naming bills, and a reword now manufactures reword-staleness rows on the eve of the Phase 20 release fill. | new |
| D086 | maul-oiled-boots-tuning-read | Do the two near-dominated vendor-trophy crafted rows, the smithing maul (+158 gold per craft at specialization alone, dominated by the rung-25 ironshod_maul on every axis but agility 2) and the oiled boots (dominated by the rung-0 fenbridge boots on every axis but agility 1 and quality), stay adopted as-is, get retuned, or join the exclusion set? | state.md, Phase 11l QA ledger tuning-read bullets: state.md:16595-16601 ("Whether the maul's surplus is wanted is the maintainer's read, recorded, not decided") and state.md:16714-16726 ("recorded at their rows as tuning reads (the maul is the maintainer's already flagged surplus row), not exclusions"). | Ratify both rows as adopted (tuning reads close as accepted) [no-op]; Retune stats or price on one or both defs [Edit the two item defs, re-derive the pinned economy floors and exact-rung maps (a skillReq move on the oiled boots reds fifteen suites per state.md:16783), wiki regen, confirm neither row is on the frozen R5 surface]; Move both to the exclusion set [Exclusion-set edit plus re-derived list-count pins and adopted-row counts, wiki regen, art/reliquary rows re-checked] | Ratify: the ledger deliberately classed both as tuning reads rather than exclusions, the floors are pinned-loud not guarded, and the maul surplus was already the maintainer's own flagged row, so the record leans accept. | new |
| D087 | mobile-chip-legibility | Accept the 10 to 11px mobile chip and metadata type as-is, retune it onto the DESIGN.md section 5 type scale (which has no 11px row) now, or leave the revisit standing until the design-foundation retune? | farming/state.md, maintainer art-batch handoff list item (bf)(4) at farming/state.md:462-463 and handoff row (bf) at farming/state.md:549 (accepted-no-action, chip revisit at the art pass); state.md, Phase 14 QA DESIGN.md-compliance bullet at state.md:21046-21051 (11px roles are pre-foundation vocabulary, aspirational until the foundation retune). | Accept as-is (ratify the legibility judgement, retire the revisit) [no-op]; Retune onto the section 5 scale now [CSS edits across the farming chips plus the Perfecting/naming and plant-sheet 11px metadata roles, re-verify 390x844 no-overflow live, mobile screenshots]; Defer to the DESIGN.md foundation/parchment retune (revisit stands) [no-op beyond recording the ruling] | Defer to the foundation retune: the Phase 14 QA already ruled the 11px roles pre-foundation vocabulary aspirational until the section 5 retune, and handoff row (bf) is already accepted-no-action with the revisit parked at the art pass, so a standalone retune now would be re-done there. | new |
| D088 | nonapex-load-keeps-perfecting-fields | Should the item-instance load sanitizer become def-aware and strip Perfecting fields from a non-apex id, or is the per-field drop-only load doctrine (the revert-safety contract) ratified so the keep stands? | state.md, Phase 15 judged-do-not-re-raise list at state.md:19255-19256 ("the load bound keeps the fields on a non-apex id (per-field drop-only doctrine; the sanitizer never sees the def)"), doctrine statements at state.md:19322 ("drop-only and NOT a whitelist") and state.md:19553-19555 ("cross-field arms would break the drop-only doctrine"); re-opened into this table by qr-18-REOPEN. src/sim/item_instance_load.ts has no apex/def awareness. | Ratify drop-only; the keep stands [no-op (record the ruling id on the judged line)]; Add a def-aware cross-field strip [New sanitize arm in src/sim/item_instance_load.ts consulting the item def, sim tests for every field pair, breaks the drop-only doctrine on which revert safety and forward-compat of shipped saves depend, and a def rename or removal would silently strip legal shipped copies] | Ratify drop-only: the packet's own record says cross-field arms would break the doctrine, no legal writer mints Perfecting fields on a non-apex id, and revert safety depends on the load path never consulting the def; the ledger leans keep on three separate lines. | new |
| D089 | obtainability-ruling-wield-rung | Should the crop obtainability acceptance wording (11h-GATE-B, inherited by 11i/11j/11k) name the hoe wield rung, since the real plant floor is max(farmCropSkillThreshold(tier), wieldRequirementForTier(tier)), tier 3 wielding at 70 and tier 4 at 85, or keep the wording on farmCropSkillThreshold alone? | farming/state.md, 11h audit "THE PLANT PATH RUNS THREE GATES, NOT TWO" block at farming/state.md:4590-4600 (marked FOR THE MAINTAINER, surfaced not decided, packet open item 6); state.md confirms the tool gate real and unruled. Behavior verified in src/sim/professions/farming.ts step 12 via bestWieldableGatherToolTierOrNone and wield_gate.ts. | Amend the acceptance wording to name the composed floor [Ledger wording edit in farming/state.md plus any test comment restating GATE-B; no behavior change, no pins move]; Keep the wording as-is [no-op (the gate stays true-but-partial; the three-gate note remains the only record of the real floor)] | Amend the wording: it costs a ledger edit only, the tool gate is real and unruled, and the record exists precisely so the next phase does not write crop copy against the wrong floor. | new |
| D090 | offline-shape-only-name-screen | Should the offline host screen legendary-name CONTENT (shipping the banlist and matcher client-side), or stay shape-only per the character-name precedent, given an offline name never reaches another player? | state.md, Phase 13 judged-this-phase list at state.md:19827-19829 ("the offline host running shape-only (an offline name never reaches another player)") and the D13-2 naming audit at state.md:19999 ("the offline host runs shape-only by the judged ruling"); re-opened for a design call by qr-18-REOPEN. Live design confirmed in src/sim/professions/legendary_name.ts:1-9 ("SHAPE ONLY, deliberately"; content screening is server-side offensiveName). | Ratify shape-only [no-op (record the ruling id; the code and comment already state the design)]; Ship content screening into the offline client [Move or duplicate the banlist/matcher into src/sim or the client bundle, new sim tests, discloses the banlist itself in the shipped bundle, and contradicts the character-name precedent for zero exposure gain] | Ratify shape-only: an offline name reaches no other player, the character-name precedent already settled the same split, and client-side screening would ship the slur list itself; every record leans ratify. | new |
| D091 | oiled-boots-signed-scale-margin | Is the oiled boots trophy row's intended reachable margin the post-gills-signing 83-vs-80 (ratify), or should it be re-tuned toward the prior 88-vs-80, a formula-owner value call the gold-negative invariant cannot answer? | state.md, 11m QA one-design-read bullet at state.md:17870-17876 ("the oiled boots trophy row's reachable margin moved 88-vs-80 to 83-vs-80 because a gills harvest signs the scale... whether the intended margin should move is a formula-owner's call no test knows"); restated untouched at the 11m close and the later reachable-floor note (state.md:17428, 17624). | Ratify 83-vs-80 as the intended margin [no-op (the gold-negative invariant is machine-checked and green)]; Re-tune toward 88-vs-80 [Adjust the boots bill or the mudfin_scale/gills pricing, re-derive the reachable-floor and exact-rung pins (the fifteen-suite sensitivity), wiki regen] | Ratify 83-vs-80: the move came from a real mechanic (a gills harvest signing the scale), not an error, the invariant is green either way, and three restatements later no record leans toward re-tuning. | new |
| D092 | over-cap-load-bench | Close the over-cap keep-everything loophole with a load-time bench (flipping the pinned equip-transition-only two-piece rule, and deciding what a benched legacy third piece becomes), or ratify the transition-only bound? | docs/design/power-verification.md:1249-1253 ("A save already over the cap keeps everything... deliberately and pinned. No shipped player path reaches three... Closing it needs a load-time bench, which is sim logic and out of scope") and the matching state.md Phase 15 ledger bullet at state.md:21329-21331; no ruling exists on either. | Ratify the equip-transition-only bound [no-op (record the ruling; the bound is already pinned)]; Add a load-time bench [New sim load logic plus a which-piece-benches rule and a full-bag destination answer, sim and server tests, save-migration risk on legacy characters, and it flips a deliberately pinned rule] | Ratify transition-only: no shipped player path reaches three pieces (only a direct meta.equipment write does), so a bench buys protection against a state the game cannot produce at real save-handling risk; both records call the current bound deliberate. | new |
| D093 | overcap-legacy-swap-freeze | May an over-cap legacy character swap WITHIN the masterwrought family (widening ignoreSlots to exempt same-family swaps), or is the v1 freeze, under which a three-piece legacy loadout is locked until unequipped down to the cap, ratified? | state.md, QA design-notes block at state.md:646-650 ("An OVER-CAP legacy character cannot swap WITHIN the family at all... acceptable for v1, flag for a ruling if legacy telemetry says otherwise"); sibling arm of the over-cap load-bench decision and can share its answer. | Ratify the v1 freeze [no-op (record the ruling)]; Widen ignoreSlots for same-family swaps [Edit the family-cap check on the equip path, sim tests for the swap arms; risk: a sanctioned swap path lets an over-cap set persist indefinitely instead of decaying to the cap]; Defer pending legacy telemetry [no-op now; re-opens only if telemetry shows real over-cap saves] | Defer pending telemetry (ratifying the freeze for v1): the ledger's own condition is telemetry-gated, no shipped path creates over-cap saves so the affected population is approximately empty, and the answer should ride whatever the load-bench row (its sibling) decides. | new |
| D094 | panel-title-parchment-retune | Which .panel-title styling authority governs, D14-6's grouped base.css rule or the Fancy Gold theme's tree-wide rule from release PR #3284 (folding in the .prof-empty h3 second gold title), or is the reconciliation deferred to the DESIGN.md phase-2 parchment retune? | state.md, Phase 14 QA bullets at state.md:21067-21070 ("a second tree-wide .panel-title styling authority on top of D14-6's grouped rule, to reconcile at the parchment retune"), state.md:21050-21051 (.prof-empty h3 second gold title) and state.md:20611-20612 (parchment titles are the program-wide phase 2 retune); both authorities live (src/styles/base.css:486 grouped rule plus the theme rules). | Defer the reconciliation to the parchment retune [no-op (record the ruling; both records already park it there)]; Reconcile now onto D14-6's grouped rule [CSS edits in src/styles retiring the theme's tree-wide rule, before/after screenshots, and merge-churn risk against the release-owned Fancy Gold theme (PR #3284)]; Adopt the theme rule as the authority and retire the grouped rule [Same cost class: base.css edit, screenshots, and D14-6's grouped-rule record amended] | Defer: both ledger records already point the reconciliation at the parchment retune, and the second authority is release-owned, so a packet-side reconcile now invites merge churn against work the retune will redo anyway. | new |
| D095 | parked-item-art-wave | When, and on what master-SHA basis, does the maintainer run the item-art wave that lands committed WebP art plus mapping.json ownership for the 81 packet ids parked on ITEM_ART_PENDING? | state.md, state-GATE-5/ip-16-ICON PARK block (lines 5741-5750, including the Phase 16 corrected rule: a PARKED id carries NO mapping.json owner) plus the Phase 17 art-census record (lines 22186-22192: the merged ITEM_ART_PENDING holds exactly the packet's 81, the wave runs on the maintainer's master SHA after the packet); set live at src/ui/icons.ts (ITEM_ART_PENDING export near line 5811); farming art-batch handoff at farming/state.md lines 439-443. | Ratify the park and record the wave's schedule/basis as a dated ledger line (wave stays post-merge on the maintainer's master SHA) [no-op]; Run the art wave now, in-packet [Maintainer supplies the master SHA; asset-pipeline pass over 81 ids, committed WebP plus one mapping.json owner each per the landed-art rule, re-pins in tests/item_art_consistency.test.ts and tests/profession_icons.test.ts; large out-of-scope batch against the standing no-push rule]; Defer past the PR with an explicit qr-19 defer ruling [no-op beyond the recorded ruling line] | Ratify the park (option 1): the ledger already records twice that the wave runs on the maintainer's own schedule after the packet, so Phase 19 only needs the basis recorded, not the art executed. | new |
| D096 | pattern-discard-confirm-escalation | Does discarding an unlearned pattern escalate past the generic discard confirm, or does the recorded classic-correct basis (patterns are ordinary tradable items; the escalated confirm keys on instance payloads) stand? | state.md, Phase 11-era settled-rulings block (lines 775-780: 'discard of an unlearned pattern stays the generic confirm ... classic-correct'), re-opened by qr-18-REOPEN as a judged refusal routed to this table. | Ratify the generic confirm [no-op]; Build an escalated confirm for unlearned patterns [Widen the escalated-confirm keying beyond instance payloads in the discard-confirm module, new t() key plus HUD test; overturns the recorded classic-correct ordinary-tradable-item basis] | Ratify the generic confirm: the ruling is settled with its basis recorded, and nothing since Phase 11 has produced a loss case that argues against it. | new |
| D097 | patterns-hotbar-placement | Should one-shot unlock items (patterns) become hotbar-placeable, or does the isHotbarItemId exclusion stand on its dead-button-after-first-press rationale? | state.md, Phase 11-era settled-rulings block (lines 775-777: 'patterns are NOT hotbar-placeable', comment at isHotbarItemId beside the reins rationale, elixir precedent), re-opened by qr-18-REOPEN. | Ratify the exclusion [no-op]; Make patterns hotbar-placeable [Edit isHotbarItemId plus its comment, wire hotbar press-to-learn, hotbar view tests; contradicts the elixir precedent and ships a button that dies after one press] | Ratify the exclusion: the dead-button rationale is recorded at the code site and no player-facing demand for the change has been logged since. | new |
| D098 | peer-card-unique-tag | Widen the peer inspect wire with perfected (or a derived Unique-Equipped bit) so a peer's card can tag an instance-legendary copy, or ratify D13-3's exclusion (peer card deliberately shows no tag) as final? | state.md, Phase 13 ledger D13-3 (lines 19658-19670: perfected stays OFF the peer wire per the Phase 12 judged decision; the tagless peer card is 'the recorded consequence') and the conscious-residue list (line 19842). | Ratify D13-3's exclusion [no-op]; Add a derived isUniqueEquipped boolean to the peer view [New computed member on the eqi peer projection in both hosts, exclusion-pin and parity updates (tests/world_api_parity.test.ts plus the wire pins), tooltip consumption; smallest surface that shows the tag]; Put perfected itself on the peer wire [eqi allowlist join plus exclusion-pin rewrites, parity tests both hosts, and it overturns the Phase 12 judged wire-privacy decision wholesale] | Ratify D13-3 (option 1): the exclusion is a twice-affirmed judged decision and the residue is cosmetic display on an inspect card, not actionable information. | new |
| D099 | perfecting-perfected-coexistence-load | Add a cross-field load-bound arm that rejects perfecting+perfected coexistence at item-instance load (breaking the per-field drop-only doctrine), or keep the coexistence tolerated since no legal writer produces it? | state.md, Phase 13 QA JUDGED list (lines 19553-19556: coexistence survives the per-field load bound; 'no legal writer, no corrupting consumer, cross-field arms would break the drop-only doctrine'), re-opened by qr-18-REOPEN. | Keep coexistence tolerated [no-op]; Add the cross-field rejection arm [New arm in item_instance_load.ts plus load-bound tests; sets the first cross-field precedent against the drop-only doctrine for a state no writer can produce] | Keep tolerated: with no legal writer and no corrupting consumer the arm defends against nothing, and the doctrine it breaks is load-bearing for every other field. | new |
| D100 | planning-docs-teardown-decision | Tear down or keep the two packets' planning-doc subtrees (docs/prd/masterwrought/ and its farming/ subtree) at packet close, including re-homing the seven screenshot cone subtrees and rewording the four out-of-packet comment citations? | state.md, packet ground rules (lines 78-79: ONE teardown decision covers both packets, taken with the maintainer) and the Phase 17 close (lines 22722-22730: teardown offer prepared, waits on the maintainer's explicit word); preconditions at farming/state.md lines 527-528 (cone set-equality in tests/ci_workflow.test.ts; four comment citations in monolith_budget/item_art_consistency/mob_portrait_source_manifest tests). | Defer the teardown past the PR (record the maintainer's word; execute after merge) [no-op now; the prepared offer executes later]; Rule teardown now and execute in-packet [Delete both subtrees, re-home or retire the seven screenshot cone subtrees with their SPARSE_CONE rows, reword the four comment citations, all one change; collides with Phase 19's standing NO-teardown delivery rule]; Rule keep: planning docs stay as the historical record [no-op; the four comment citations are accepted as-is and the cone rows stay] | Defer past the PR (option 1): the standing Phase 19 rule is NO teardown and both preconditions block teardown, not the merge, so the only thing this phase can and should take is the maintainer's word, recorded. | new |
| D101 | produce-vendor-row | Does any farm produce beyond brook_carrot get a vendor buyValue/vendor row (restoring vendor-stock completability for the two rung-0 bills the vale_wheat swap broke), or does the farm-and-market-only route stand as recorded design? | state.md, Phase 11h rejection-list record (lines 10250-10254: 'Not re-proposed: a produce buyValue or a vendor row of any kind', re-affirmed at line 11436) and the underlying tradeoff recorded as 'worth the maintainer's eye rather than a defect' (lines 10212-10231). | Ratify the farm-and-market-only route [no-op]; Vendor-stock the produce the two rung-0 bills need [buyValue plus vendor rows in src/sim/content/farm_crops.ts, faucet test extension, wiki regen, and an economy read against R17/R18 (a new produce faucet competes with the farming loop the packet tuned)] | Ratify the recorded design: the rejection is written as design twice, the bills stay completable through farming and the market, and a vendor faucet undercuts the farming loop R19's calendar model was tuned on. | new |
| D102 | prog-enchanting-rare-deed | Does enchanting get a rare-tier output (which puts it on rareTierCrafts and owes a prog_enchanting_rare deed), or does its lane stay deliberately empty as shipped? | state.md, Phase 15 rejection list plus the lane-empty derivation (state.md:16379-16380 refusal; state.md:16190-16200 shows every candidate above 28 barred by shipped rulings, so 'Enchanting's lane goes empty') | Keep the lane empty as shipped (ratify the refusal) [no-op]; Author a rare enchanting output plus the prog_enchanting_rare deed (trigger, renown, name all unpicked) [New item/recipe in src/sim/content, deed row in deeds.ts (tests/deeds_content.test.ts pin), WebP art, wiki regen, i18n names; reverses the recorded lane-empty outcome and picks values the record does not hold] | Keep the lane empty: the record refused this precisely because it is 'a deed minted to make a mapping true', and every rare-eligible candidate is barred by a shipped ruling, so reversal buys flavor at the cost of contradicting settled reservations. | new |
| D103 | prog-first-harvest-bootstrap-deed | Is prog_first_harvest taking a farming-first character four crops instead of one accepted as shipped, or does farming get a new bootstrap deed (Rule 9 forbids editing the shipped trigger, so a new deed is the only lever)? | state.md, Phase 11e 'Two maintainer decisions that stay OPEN' block (state.md:9044-9048 as read; consequence recorded at state.md:8377-8379). Distinct from row 3's thirteen, which carry only the fishing-side 13-catch item. | Accept four-crop timing as shipped [no-op (a RULED line in the ledger)]; Commission a bootstrap deed firing on the first crop [New record in src/sim/content/deeds.ts plus deeds_content.test.ts pin, wiki regen, i18n name/desc, renown value the maintainer must pick] | Accept as shipped: farming's own chr_vale_first_harvest still fires immediately on the first crop (state.md:8373-8376), so a first-time farmer already gets an instant deed and the gap is which deed fires, not whether one does. | new |
| D104 | proving-shore-farming-intro | Now that newborn characters spawn on the Proving Shore, how does the Eastbrook farming intro (q_farm_intro, still hung off the Eastbrook farmer) re-surface in the new-player flow? | state.md, sixteenth-sync 'recorded, not acted on' premise-shift lines (state.md:17124-17128 and 17178-17181 as read); src/sim/content/proving_shore.ts carries no farming content, q_farm_intro unchanged in zone1.ts; absent from farming/state.md handoff rows | Accept: players reach Eastbrook naturally and meet the intro there [no-op (ratify; the Shore is revisitable by ferry bell and Eastbrook remains the early hub)]; Add a Proving Shore breadcrumb quest pointing at the Eastbrook farmer [One quest record plus text keys, wiki regen, quest test; smallest re-surfacing change]; Move or duplicate the farming intro onto the Proving Shore [Farming-packet-owned content move: new NPC/quest content, tutorial-flow rework, deed/wiki obligations, drift risk against the shipped Shore tutorial] | Add the breadcrumb quest: it discharges the recorded premise shift at one quest's cost without moving farming content the farming packet owns; the ledger records the shift but leans neither way, so this is a genuine design pick. | new |
| D105 | quartermaster-core-backstop-price | Confirm or re-tune the Heroic Quartermaster wyrmfall_core catch-up price (12 marks, the ring price point) as a deliberate bad-luck backstop, given the revisit the ledger scheduled at phase 15 power verification never happened. | state.md, Phase 06 Quartermaster decisions block (state.md:1056-1059 as read: 'deliberately a bad-luck backstop rather than an alternative farm; revisit at phase 15 power verification'); phase-15-power-verification.md and power-verification.md carry no quartermaster/wyrmfall_core price read (grep confirms), so the scheduled revisit is undischarged | Ratify 12 marks as the backstop price [no-op (a RULED line discharging the phase-15 revisit)]; Re-tune the marks value [Edit the catch-up row plus its test pin and wiki regen; an economy value only the maintainer can pick, adjacent to but not on the frozen R5 surface] | Ratify: the price was set intentionally as a backstop rather than a farm, and nothing in the phase-15 record or since produced evidence it is mis-priced; the row exists to close the dangling revisit, not to re-open tuning. | new |
| D106 | quest-gated-silk-floor-ruling | Do quest-gated camped templates (spider_egg, the Broodmother clutch, the only such template in the game) stay legal harvest-floor members, given a refusal leaves silk at 5 of 6 with no flavor-true candidate on the shipped roster? | state.md, Phase 15 'MAINTAINER READ, carrying a ruling' block (state.md:17616-17620 as read); confirmed still standing at state.md:18099 and 18350; the quest-gated census is pinned as literals so a second such member can only land consciously | Yes: quest-gated camped templates are legal floor members [no-op (ratify the phase's by-the-letter reading; census literals already guard the class)]; No: refuse them [Silk's floor becomes unmeetable on the shipped roster: needs a new spider template (mob content plus camps, deeds, wiki, art obligations) or a flavor-false tag, both outside settled decision 12, plus a requiresQuestId predicate arm and census re-pin] | Rule yes: the settled admission (qr-11m-SPREAD) says nothing about quest gates, the clutch satisfies the predicate by the letter, the pinned census makes the class a conscious edit forever, and a no forces content work the packet has no mandate for. | new |
| D107 | raid-scale-feast-charges | Does the apex feast gain raid-scale charges (a charge count and balance basis the record does not hold), or do the settled K5 numbers (charges 10, durationTicks 3600) stand? | state.md, Phase 15 rejection list (state.md:15617-15620 as read, 'inherited from the phase file's own list and none re-proposed'); phase-11k-provisioning-capstone.md:128-135, Decision K5 SETTLED with raid-scale charges refused, not deferred (drags a perf read into a content phase and moves the R5 feast-uptime input) | Keep refused; K5 stands [no-op]; Reopen with a maintainer-supplied charge count [Edit harvest_feast charges plus feast tests and wiki regen, a raid-scale perf read, and an amendment to the R5 feast-uptime input, which is an escalation while R5 is frozen] | Keep refused: K5 settled the numbers twice, no charge count or balance basis exists anywhere in the record, and reopening moves a frozen-R5 input, which the Phase 19 stopping rules treat as an escalation, not an execution. | new |
| D108 | rank0-bind-shape | Close the rank-0 bind hole (a failed first Perfecting attempt leaves a bound copy, with no marker, that the Maker's Bond unbind clears for its fee) with a first-attempt marker, or ratify the live unbind behavior as designed? | state.md, Phase 16 'RECORDED, NOT ACTED (maintainer reads)' block, THE RANK-0 BIND SHAPE (state.md:21018-21027 as read); live behavior pinned in tests/professions_commissions.test.ts; the phase-12 spec intended permanence | Ratify the live rank-0 unbind behavior [no-op (the guide-copy reword already rides the release fill's reword lane)]; Add a first-attempt marker [A save-shape change (new persisted field), perfecting.ts logic, flips the live pin, plus persistence/migration and parity work] | Ratify: the ledger itself records no power or economy leak (rank 0, no R5 bonus, the pre-attempt copy was tradable per R2, the fee is a pure sink), and a save-shape migration is a heavy price to restore a spec nicety nobody is exploiting. | new |
| D109 | regalia-prestige-shed-presumption | Ratify or reverse the fairness classification that worn-gear legendary-regalia prestige display is cosmetic (the glow is preset-shed below the medium effects tier) rather than actionable information? | state.md, Phase 17 judged/notes block (state.md:22126-22130 as read: 'the presumption that worn-gear prestige display is cosmetic, not actionable, is load-bearing and recorded'; the fairness suite pins the classification) | Ratify: prestige display is sheddable cosmetic [no-op (the fairness suite already pins it)]; Reverse: the glow becomes non-sheddable [Remove the preset shed in the effects tier, update the fairness-suite pin, and eat the glow's render cost on low tiers, against the CLAUDE.md fairness doctrine's own rule of thumb] | Ratify: the glow conveys no information a player acts on (who is wearing prestige gear is not a combat input), which is exactly the cosmetic-richness side of the recorded fairness doctrine, and the suite already pins the classification. | new |
| D110 | reliquary-pended-mounts-decision | Rule acquisition sources for the two pended Horizons mounts (drakemaw_raptor has no path at all, terrorspark_groundshaker is dev-grant only) so the Reliquary capstone becomes earnable. | state.md, Phase 16 'RECORDED, NOT ACTED (maintainer reads)' block (state.md:18808-18810 as read: 'ONE owner decision from earnable'); src/sim/content/reliquary.ts:285-291 confirms both remain the catalog's two SOURCE_PENDING_RULING mounts | Rule a source for each (drop table, vendor, deed, or event) [Awarding-table content plus reliquary.ts un-pend, tests/reliquary_content.test.ts pin, wiki regen, and any deed obligations; the specific source picks are maintainer values (goes back in-session if unstated)]; Keep both pended [no-op; the capstone stays unearnable (feat:true prevents a dead-end, visibility read only)] | Rule the sources: the packet's own record leans this way ('ONE owner decision from earnable', the two mounts 'all that remain'), and the execution cost is one bounded content change per mount. | new |
| D111 | report-window-focus-trap-carveout | Should report_window gain a managed teardown and focus trap (closing its pinned NO_MANAGED_TEARDOWN carve-out), or keep the accepted bare-hide close behavior? | state.md, Phase 11 farming-audit notes block (state.md:6670-6673 as read: 'cheap to close at a maintainer's option'); the carve-out is live at tests/managed_window_close_registry.test.ts:71-77 ('report-window' in NO_MANAGED_TEARDOWN) | Close the carve-out with a managed teardown [Small src/ui edit plus removing the NO_MANAGED_TEARDOWN row and updating the registry test; the ledger prices it as cheap]; Keep the bare-hide close [no-op (the pin documents the accepted carve-out)] | Close it: the ledger already judged the fix cheap and only parked it on the maintainer's option, and removing an a11y carve-out for one small diff is the better trade than carrying the exception forever. | new |
| D112 | rl-env-virtual-clock | Is farming re-admitted to the headless RL action space via the virtual-clock design (a seed-stable lockoutNowMs injection plus an episode-time compression policy, growing the append-only ACTIONS list by farming's five commands and the obs vector), or does the recorded Phase 16 cut stand? | state.md, Phase 18 ledger, 'THE RL SCOPE CUT' block (read at state.md:22209-22215; inventory cite 22046) plus headless/CLAUDE.md:18-40 ('Scope: what the env covers, and the recorded farming CUT', commit 278d603f81): the cut is recorded, and re-admission is named as its own design change in src/sim/obs.ts terms, never a quiet flag. | Keep the recorded cut (status quo) [no-op]; Defer with a filed issue naming the virtual-clock condition [one GitHub issue plus a dated ruling line in state.md; no code]; Commission the re-admission as its own post-packet design change [src/sim/obs.ts virtual-clock injection design + episode-time compression policy + append-only ACTIONS growth (five commands; every trained policy's action head is positional) + new obs terms + python/gym binding updates + determinism tests] | Keep the cut: the ledger itself rules re-admission a standalone src/sim/obs.ts design change with an unspecced compression story, no reward term reads farming state, and the one-sim-three-hosts claim is intact without it. | new |
| D113 | rl-host-perfecting-unreachable | Should the headless RL host gain a resetDay/ember-faucet day cycle so the Perfecting deed is earnable there, or is the unearnable deed ratified as correct because deeds are not an RL surface? | state.md, Phase 16 ledger recorded-reads list ('The RL/headless host cannot complete Perfecting (the ember faucet needs resetDay)... a pre-existing property; deeds are not an RL surface', read at state.md:19910-19912; inventory cite 19785); a live grep confirms zero resetDay wiring anywhere under headless/. | Ratify the unearnable deed as correct [no-op (optionally one dated ruling line beside the record)]; Wire a deterministic day cycle into the headless env [headless/env_server.ts resetDay injection + a seed-stable calendar story + env tests, and it drags the virtual-clock re-admission question (previous row) with it] | Ratify: the record already calls it a pre-existing property of a host where deeds are not a surface, and a day cycle buys nothing any reward term reads. | new |
| D114 | rung0-vendor-route-loss | Accept that the two rung-0 rows (recipe_hunters_game_skewer, recipe_elixir_of_the_boar) lost vendor-stock-plus-a-kill completability to the vale_wheat swap (now needing a farm detour or a World Market buy), or direct an alternative vendor-stocked reagent that still satisfies RULE 2's body constraint? | state.md, the vale_wheat swap record ('What is gone is the vendor route specifically, and that is a tradeoff worth the maintainer's eye rather than a defect', read at state.md:10300-10317; inventory cite 10230; swap rationale at state.md:9882-9889). No ruling on it anywhere; not among row 3's thirteen (its RULE 2 value-half item is the adjacent interpretation question, not this tradeoff). | Accept the farm-detour tradeoff [no-op]; Swap in a different vendor-stocked reagent [two bill edits in src/sim/content cooking/alchemy tables + RULE 2 value-half re-derivation + wiki regen + reachability/anti-chore pin updates, with the exact crop-as-body defect the record says RULE 2 exists to prevent]; Have a vendor stock vale_wheat [one vendor row + it breaches farming's no-counter-sells-produce doctrine and undercuts the farm faucet] | Accept: the ledger already argues the swap is the rule doing its job ('THE SWAP IS STILL RIGHT'), R18 is not breached, and the World Market route remains open. | new |
| D115 | scriptorium-station-seating | Seat a real scriptorium station in a later world phase (repointing the six inscription recipes' stationType off the apothecary), or ratify the apothecary foreign binding as permanent? | state.md, the inscription foreign-binding rationale with its reversibility note ('the decorative inscription_lectern prop already stands in Eastbrook's artisan row... a later world phase can seat a real scriptorium station and repoint each recipe's stationType', read at state.md:1848-1861; inventory cite 1817). Live code agrees: stationTypeForCraft('inscription') is undefined by design, pinned green, with the six ids on the foreign-bound allowlist in tests/professions_crafting_hub.test.ts. | Ratify the binding for this packet, record the seat as a later-world-phase option [no-op now (one dated ruling line); the reversibility is already built in]; Ratify the foreign binding as permanent [no-op, but it retires the recorded reversibility note and the lectern prop stays decorative forever]; Seat the station in Phase 19 [station def + eastbrook_layout seat + six stationType repoints + shrink the foreign-bound allowlist pin + faq.a8 station-count reword + station i18n and props + screenshots; a world-content change out of proportion for a rulings gate] | Ratify for this packet and keep the later-phase seat open: the record deliberately engineered the reversibility (prop placed, pins scoped), so deferring costs nothing and seating now is disproportionate. | new |
| D116 | seed-vendor-proficiency-advisory | Should seed vendor rows carry a proficiency advisory when the buyer's farming skill cannot yet plant the seed, or does the shipped no-advisory vendor convention stand? | state.md, Phase 11e QA judged-refusals list ('Left as is: vendor_row_gates.ts is scoped by its own banner to land TOOLS, and tier 1 and 2 seeds have always shipped this way, so an advisory here would be a new convention rather than consistency', read at state.md:8628-8631; inventory cite 8547). Reopened only by qr-18-REOPEN's letter (state.md:22732-22748), which routes maintainer design calls to this table. | Keep the no-advisory convention [no-op]; Add a proficiency advisory to seed vendor rows [a new vendor-row convention spanning the shipped tier 1/2 seed rows (vendor_row_gates.ts widening or a new tooltip advisory path) + i18n keys across all locale blocks + vendor UI tests + tooltip-doctrine review] | Keep the convention: the refusal's consistency ground still holds (all seed tiers have always shipped this way), so an advisory would mint a one-off convention no other vendor row follows. | new |
| D117 | stripped-legendary-rename-affordance | Should a re-name affordance exist for a legendary whose player-given name moderation stripped, or does the permanent-nameless outcome (arm 1 refusing re-promotion) stand? | state.md, the moderation-strip consequence record ('a stripped copy is a PERMANENTLY nameless legendary (arm 1 refuses a re-promotion; whether a re-name affordance should exist is a maintainer read, see the Phase 13 QA ledger)', read at state.md:19702-19712; inventory cite 19589). Sole mention, no later ruling; distinct from seeded row 2's instance-keyed Reliquary-page question. | Ratify permanent-nameless [no-op (one dated ruling line)]; Add a moderation-cleared re-name path [relax the arm-1 re-promotion refusal behind a moderation-cleared flag in the server naming flow + an abuse story for the strip/re-name loop + banlist re-check + sim and server tests + i18n] | Ratify permanent-nameless: the refusal arm is load-bearing against re-promotion abuse, losing the name is a coherent deterrent for a name that earned a strip, and no record shows demand for the affordance. | new |
| D118 | tusk-rung50-ceiling | Raise the 460 crafted-output value ceiling (and accept a stretched bill) so a rung-50 ogre-tusk output can exist, or keep the recorded rejection standing. | state.md, Phase 16 'THE REJECTION LIST, AS RECORDED DESIGN' (lines 16366-16367 as read): 'A rung-50 output above the 460 ceiling for the ogre tusk (every zone3-band uncrafted weapon sells 700 or more): a new ceiling and a stretched bill.' | Keep the rejection standing [no-op]; Set a new crafted-output ceiling and author the rung-50 tusk output [a maintainer ceiling value, one recipe row with a stretched bill in src/sim/content, re-derived output-doctrine and economy pins, wiki regen plus content same-change obligations, and a check that the R5 surface stays untouched] | Keep the rejection: the arithmetic bars every candidate (zone3-band uncrafted weapons all sell 700+), the 460 ceiling is load-bearing across the whole nine-output doctrine, and nothing since Phase 16 has produced a demand for the output. | new |
| D119 | two-markitemdiscovered-routes | Ratify the established two-route discovery idiom for promoted legendaries (the same-tick stamp-site mark plus the login retro sweep) or pick which single route survives a consolidation. | state.md, the Phase 17 judged list, reopened by qr-18-REOPEN (lines 19837-19838 as read): 'the two markItemDiscovered routes (the established idiom)'. Code: the stamp-site mark at src/sim/professions/perfecting.ts:698 (documented at 598-601 as landing same-tick, never at the next login's retro pass) and the retro sweep at src/sim/deeds.ts:1363-1375. | Ratify the two-route idiom [no-op plus a dated RULED line closing the reopened judged-refusal row]; Consolidate to the stamp-site route only [drops the retro sweep's legacy-save backfill for promoted legendaries; deeds sweep edit plus re-pinned deeds tests]; Consolidate to the retro route only [loses the documented same-tick discovery landing (discovery would wait for the next login retro pass); perfecting edit, test churn in both the perfecting and deeds suites] | Ratify the two routes: each consolidation loses a documented behavior (same-tick landing or legacy-save backfill), the ledger already names it 'the established idiom', and the original judged refusal found consolidation is not the mechanical refactor it looks like. | new |
| D120 | well-fed-last-eaten-wins | Keep the classic last-eaten-wins well-fed overwrite (eating a tier-1 dish after an apex plate downgrades the buff) or add a downgrade guard. | state.md, the 11c review record (lines 7032-7034 as read): 'Note RECORDED, not changed: eating a tier-1 dish after an apex plate downgrades the buff, the deliberate classic last-eaten-wins rule', a recorded-not-acted note qr-18-REOPEN reopens; the adjacent ruling 11c-D-2 (farming/state.md:479 as read) ratified the single well_fed id but never ruled on guarding the downgrade. | Keep last-eaten-wins, ratified [no-op (a dated RULED line); the legibility question stays with its own row]; Add a downgrade guard (refuse or confirm when the incoming buff is weaker) [wellfed.ts logic plus a sim test, identical behavior in both hosts, new confirm/refusal player copy with t() keys, and a deliberate departure from classic-era behavior] | Keep last-eaten-wins: classic fidelity is a project pillar, the ledger records the rule as deliberate, and the softer remedy (making the downgrade legible) is already priced separately as the row-4 legibility item rather than a mechanic change. | new (distinct from the row-4 legibility row: this is the mechanic policy, that is the UX surface) |
| D121 | admin-preauth-ip-meter | Should /admin/api mount an IP-keyed rate meter ABOVE the require-admin frame, so the two awaited per-request auth reads (accountAndScopeForToken, adminRolesForAccount) are bounded per address, or stay on the deliberate direct-reads posture with the in-handler buckets only? | The Phase 18 hot-path review of 8cc3248db6 (this ledger): the analytics bucket sits below the auth frame, so a 429'd request has already paid both reads; server/CLAUDE.md rules the admin gate stays on direct reads (the auth-guard cache is marketplace-only); the sizing comment now names the pair as the deliberately unmetered real cost. | Stay on direct reads, comment-only (shipped) [no-op; the Phase 18 fix round already lands the honest sentence]; Mount a pre-auth IP meter for /admin/api [a middleware-onion change touching every admin route, its own limiter family and tests, and a review of the operator lockout failure mode] | Stay: the reads are two primary-key lookups on an operator-only surface, the buckets bound the handlers, and a pre-auth meter's lockout failure mode (an office NAT bricking the dashboard including moderation) costs more than the reads it saves. | new |
| D122 | character-blob-warn-threshold | CHARACTER_BLOB_WARN_BYTES (131,072) now sits BELOW the legitimate worst case: the maximal legal character blob measures 151,495 bytes through the real serializer, so a character at every container ceiling prints the oversized-save warn on every autosave. Raise the threshold above the measured worst case, keep it as a deliberate early signal, or re-scope what the gauge watches? | The Phase 18 measurement unit (this ledger and tests/professions_blob_growth.test.ts): measured 151,495 (band 151,115 to 151,496), predicted 156,205 independently with the -4,710 drift accounted term by term; the standing carry said roughly 41.4 KB at about 3.2x headroom, so the recorded relation was inverted, not merely stale. The Phase 18 sweep recorded it and refused to tune it (a value decision). | Raise to a measured-worst-case-plus-headroom value [one constant plus its comment, the gauge and heartbeat pins re-read together; loses the early signal for the fleet-creep case the gauge exists to catch]; Keep 131,072 and accept the warn on maxed characters [no-op; the warn becomes routine for the very characters most worth watching, which is how a signal dies]; Re-scope the gauge to a growth RATE or a percentile band rather than an absolute ceiling [a small windowed core beside the p99 gauge already added this phase, plus its scrape and heartbeat rows] | Re-scope, or raise with the p99 gauge as the real watch: the absolute ceiling was calibrated against a blob a third the true size, and this phase already shipped the p99 windowed gauge that answers the fleet-creep question the warn was standing in for. | new |
| D123 | jewelcrafting-exclusion-stale | The 11l rung exclusion record excludes gleamstag_charm, deepfen_pearl, pale_pearl and ogre_toe_ring from jewelcrafting on the ground that 'the uncrafted neck and ring pool is 25 items: 9 honor pieces at sellValue 0 and 16 above 600, nothing in (25, 460]'. The live tree answers 34 items, 9 at zero, 24 above 600, and mother_of_pearl (ring, uncommon, sellValue 50, src/sim/content/proving_shore.ts:756) sitting INSIDE the band the record calls empty. Is the record wrong, or is the content? | The Phase 18 tests unit (tests/trophy_domination_claims.test.ts): the live census is pinned with the contradiction named in place, and the header was deliberately NOT re-worded, because which side is wrong is a design call. The weapon band the same header flags as unpinned holds exactly, so this is a single stale clause, not a stale record. | Re-word the record at its live figures, keeping all four exclusions on the narrower ground that the one in-band row is a Proving Shore starter ring rather than a jewelcrafting-register output [a content edit to a balance record, plus an argument the record does not currently make; cheapest, leaves the exclusions standing]; Re-open the lane and author a jewelcrafting trophy row against one of the four [a new recipe plus its economy row, the reagent/output check in tests/recipe_economy.test.ts, and a re-derivation of whether a 50-value output clears each trophy's sellValue (pale_pearl 30 and ogre_toe_ring 25 would, deepfen_pearl 600 and gleamstag_charm 2500 would not); reopens a lane 11l QA closed twice]; Exclude the row by register, ruling a starter-zone item outside the crafted-output register [restores 'nothing in band' as a DERIVED fact rather than a re-worded claim, but needs a rule that holds for every future starter-zone item, and the derived test then pins the filter rather than the census] | Re-word at the live figures: the exclusions are sound on their merits, one clause of the census went stale, and inventing a register rule to make a sentence true again is a larger commitment than the sentence is worth. | new |
| D124 | tooltip-line-doctrine | src/ui/tooltip_line.ts's header says 'Prefer this over new innerHTML builders'. The tree does the opposite: that module has ONE importer, while the HTML-string line family this phase collapsed is four modules and growing, and every one is a new innerHTML builder in that header's sense. Reaffirm the doctrine or soften the header to describe what the module actually is? | The Phase 18 sim-misc unit (this ledger, commits d2d678a4c7 and 97656679fe), which collapsed the four string builders and left the header's claim untouched because changing it either way IS the ruling. The two mechanisms are genuinely distinct and stay two: createTooltipLine returns an HTMLDivElement, the builders return strings Hud.itemTooltip composes into one innerHTML assignment, and neither can serve the other without serializing a node to re-parse it or assigning innerHTML. | Reaffirm the doctrine: new tooltip lines take the node path and the string builders become legacy to migrate [a migration of four modules and 27 call sites, plus a composition change in Hud.itemTooltip, which today builds one string]; Soften the header to describe it as the node-returning option for callers that need a node [a comment edit; the honest description of what shipped]; Leave the header as-is [no-op, but the tree keeps contradicting a stated preference, which is how a doctrine stops being read] | Soften the header: the preference lost on the merits over four modules and years, and a stated rule the tree ignores is worse than no rule. Worth noting for whoever rules: the string path is NOT the unsafe one, every line goes through esc(), so the header's implied safety argument does not by itself decide it. | new |
| D125 | mobile-pet-frame-combo-overlap | On mobile, an energy user's pet frame overlaps the player frame: the frame renders 55.06px with the combo row visible against --mobile-pet-frame-drop: calc(53px * scale + 4px) (src/styles/hud.mobile.css:2843), so the 53px estimate is calibrated for the non-combo case. Re-derive the drop from the painted height, add a combo-aware term, or accept the overlap? | The Phase 18 tests unit, measured directly with a scratch probe rather than inferred: pre-heraldry 45.30px, heraldry-era 45.30px, heraldry-era WITH the combo row 55.06px. The combo row is display:flex only for an energy user and none otherwise (src/ui/hud.ts:9072). The structural heraldry modernization is height-neutral for a warrior, so this is shipped-CSS drift, not a regression this phase introduced, and it moves no existing assertion. | Re-derive the constant from the painted worst case [one CSS value plus the browser assertions that pin the pair's gap; costs vertical space for every class, including the majority who never show a combo row]; Add a combo-aware term so the drop follows the painted state [a class-conditional custom property and its mobile pins; correct in both states, one more conditional in the mobile layer]; Accept and document the overlap [no-op; an energy user on mobile keeps a pet frame that collides with the player frame] | Add the combo-aware term: the overlap is real and only for energy users, so paying it globally taxes every other class for a state they never enter. | new |
| D126 | reword-staleness-backfill-horizon | Arming the reword-staleness gate needs a persisted translation-time srcHash, and no store exists. What is the back-fill horizon for the roughly 290,000 rows already marked `translated` with no recorded provenance? | The Phase 18 i18n unit, item 10, stopped rather than guessed. scripts/i18n_scan.mjs writes `{ state: 'translated', srcHash: enHash, by: 'human' }` at all three emit sites (242, 272, 322), assigning srcHash FROM enHash in the same expression, and enHash is recomputed from the current English every run, so `srcHash !== enHash` is unsatisfiable: flipping the comparison today demotes ZERO rows while making the gate look armed, which is strictly worse than the honest dormancy comment already at lines 32-37. No existing store can hold the hash either: i18n.status.json is gitignored and regenerated each run under an explicit no-self-read invariant, the overlays are flat key-to-string maps with no metadata slot, and there is no sidecar under src/ui/, docs/i18n-scaling/ or scripts/. So arming needs a new committed artifact plus a registry schema bump off version 1, and then this decision. WORKED EXAMPLE, ADDED 2026-08-31 BY THE GATE FIX ROUND, which turns this row from a hypothetical into a measured one. This phase's own em dash pass reworded 92 player-visible strings across 67 records (59 quests and 8 NPC greetings: zone1 12+2, zone2 18+3, zone3 23+2, temple 6+1). The ENGLISH side self-heals for quests and greetings, because they resolve from the live content tables through world_entity_i18n.ts, verified in the resolved bundle. The NON-ENGLISH side does not: 18 of the 21 locale overlays carry per-key rows for these quests, on the order of 1,600 rows, each a translation of superseded English. NOTHING MARKS THEM AND NOTHING REDS. All four candidate guards were checked and none can see it: the S3 guard parses sim EMIT sites and this prose is entity content resolved through tEntity, so it is out of scope by construction; the i18n freshness gate compares committed artifacts against a regeneration, which catches a stale artifact and not a translation that no longer matches its English; the release-tier gate hard-fails on `pending` rows and these are `translated`; and the reword gate itself is unsatisfiable in source, confirmed at the three write sites. So the demotion this row exists to enable has never fired and cannot, and the backlog it would have to classify now has a name and a size rather than an estimate. | Stamp every existing row with its current English hash [the gate arms cleanly and catches every FUTURE reword; every already-stale row is frozen as fresh permanently. Not hypothetical: src/sim/content/zone1.ts:1448-1456 carries a STALE-OVERLAY NOTE recording q_prof_attune_outfitter as reworded (webwood spider to Sableweb Lurker) with no matching overlay re-fill, so translated locales still render the old mob name, and this option guarantees they always will]; Leave existing rows unstamped and treat unstamped as stale [honest, and catches the backlog; up to 289,837 rows demote to pending in one step, hard-failing the release-tier gate at a scale no fill pass can absorb and burying the genuine 20,117-row backlog inside a 290k-row wall]; Stamp provisionally, re-verified per locale at the next full fill [arms the gate for all new work immediately and retires the unverifiable back-fill incrementally instead of in one irreversible step; needs a fourth registry state beside translated/pending/blocked and a fill-workflow change so a pass clears provisional rows as it goes] | Stamp provisionally: the first option permanently blinds the gate to a backlog we can already name an instance of, the second detonates the release gate, and only the third lets the gate start earning its keep on new work while the old rows retire at the pace a human can actually verify them. | new |
| D127 | wyrmfall-core-nodiscard | deed_of_making now carries noDiscard and wyrmfall_core, its named arm, does not. Flag both, flag only the deed, or close the row as correctly judged in Phase 13? | Phase 13 recorded deed_of_making as having no noDiscard, judged as matching its named arm; qr-18-REOPEN reopens it. The doctrine at items.ts binds noDiscard to the SOULBOUND token shape ('Token shape follows heroic_mark: tool + explicit stackSize + soulbound + noDiscard'), and the deed's own comment contrasts the TRADABLE arm (the wyrmfall_core shape) against the soulbound token arm. A catalog sweep by the Phase 18 content-parity unit: all 29 noDiscard items today are soulbound or noVendorSell, and NOT ONE is a freely vendorable, tradable, market-listable item, so the doctrine does not settle the tradable arm either way. | Flag the deed only, arms diverge (what shipped) [breaks the matches-its-named-arm symmetry and makes deed_of_making the catalog's first vendorable-and-tradable noDiscard item, a new precedent; protects the piece where the asymmetry is real, a single-use capstone whose only replacement is a skill-125 craft on a 553-basis bill, and leaves the repeatable faucet drop alone; the rationale is written at the def and pinned in BOTH directions in tests/orange_promotion.test.ts so the divergence cannot rot silently]; Flag both [restores symmetry and satisfies the plain reading that a chase material is never lost to a stray discard; changes destroy-prompt behavior on a shipped, market-traded material nobody flagged, and widens the new precedent to a second id]; Flag neither and close as correctly-judged-in-13 [leaves the reopened row undischarged] | Keep the shipped divergence: replacement cost is the real asymmetry, and symmetry across a doctrine boundary is not a reason on its own. Cost to switch is one line plus one pin flip either way, and nothing persisted depends on it. | new |
| D128 | single-slot-writer-slot-key | The four single-slot painter writers share ONE cached (kind, value) entry per element, so two different single-slot writers on one element make that equality false every call and both writes bypass elision forever. Seven live sites were doing it, one a hot per-frame painter. Give the four writers a real slot key so the collision cannot be expressed, keep the AST scan that detects it, or leave the scan as the only defence? | The Phase 18 frontend-HUD unit (this ledger). All seven sites are fixed and an AST guard names each one on a revert, including two LOOP-VARIABLE receivers that the original four-site list and any regex missed. The perf gate's ARM 2 exemption rationale was ALSO wrong and is corrected: elision is guaranteed per (element, KIND), not per element, which is exactly the case the exemption waved through, and extending ARM 2 to the pooled painters would not have caught it either, since the defect is invisible to a per-file scan and to any driver that does not compare counts across two identical frames. | Option A, give the four single-slot writers a slot key so SingleSlotEntry stops being {kind, value} and shouldWriteSingleSlot compares the slot for the requested kind [removes the class rather than reporting it, and also covers what the scan admits it cannot see: one element reached through two different expressions, or written from two modules. COSTS: memory on the hottest cache in the HUD, a WeakMap over every element the HUD routes a write through (party and raid rows, aura nodes, the FCT pool, every action-bar slot), where almost all hold ONE kind today and a four-slot record makes every one pay for four, against a 2-field shape chosen so the elided path allocates nothing after first write; shouldWriteSingleSlot is shared VERBATIM by Hud's private writers and by makeWriterFacet over the SAME cache, so both move together or the invariant goes; painter_host's allocation-free-skip pins and decision table get rewritten, not adjusted; and it must be MEASURED against hud_perf_budget.baseline.md, since the payoff is real writes removed and the price is per-element memory]; Option B, widen the AST scan cross-module [cheaper and purely detection, but it needs an element identity the scan does not have: source text is what makes it cheap, and cross-module means a naming convention nobody has agreed or a real TS program instead of createSourceFile, turning a 40ms scan into a full-program typecheck inside a Vitest. It can never cover a receiver crossing a call boundary, so it buys reach and never completeness]; Leave the shipped scan as the only defence [no-op; the seven sites are fixed and a regression is named on sight, but the collision stays expressible] | Option A, as a MAINTAINER-OWNED change with a tour capture attached, not folded into this packet: it rewrites the hottest cache in the HUD and its payoff has to be measured rather than argued. Option B is not worth its price, since the shape it would add is not the shape that shipped. Either way the guard stays keyed on the SHAPE and stays an AST walk, for the reason the mutation demonstrated: the original list named four sites and the AST scan found seven. AMENDED IN PLACE 2026-09-02 at 19E execution: measured first (an Option A prototype through the whole painter battery and two headless tours each way): zero live writes removed today, +16 bytes per routed element; hud.ts sits at its ceiling (18716 / 18716) so the implementation half owes extraction; the ARM 2 rationale rider had already landed on 2026-08-31. Ruled ROUTE-OUT on the number under qr-19-single-slot-writer-slot-key at the guard test, with the follow-up row in phase-19-routed-followups.md. | new |
| D129 | hud-coordinator-fanout-exemption | tests/language_fanout_registry.test.ts carries a blanket `{ file: 'hud.ts', memos: 'coordinator' }` row in NOT_A_LANGUAGE_GATE that BOTH classification arms skip BY NAME. Replace it with a per-signature opt-in list, narrow it to the coordinator's own frame state, or leave it blanket with the new per-memo reach standing? | The Phase 18 frontend-HUD unit (this ledger). The exemption's stated reason is real: hud.ts's own fan-out arms are individually pinned by half 1, so listing its two dozen unrelated memos would mean every hud.ts edit is re-approved in two places. But it is a hole the size of the LARGEST FILE IN THE TREE, and this phase produced the proof: targetDiscordSig was discovered, the file was classified, and no arm asked the question, while the gated rebuild emitted FOUR localized faces (the role chip, the Discord rank chip, the dev chip, and the AI mark with its aria-label) that all sat in the previous locale after a language switch. | Per-signature opt-in list with written reasons, the ANSWERED/NOT_A_LANGUAGE_GATE contract applied INSIDE hud.ts [strongest and the honest one; roughly two dozen rows to author once, each needing a real reason, and thereafter every new hud.ts memo costs a row, which is the cost the rest of src/ui already pays]; Narrow the exemption to the coordinator's own frame state [needs a rule for 'the coordinator's own frame state' that a scan can apply, and the executing unit does not believe one exists that is not itself a name convention; it also would NOT have caught this bug, since the offending method is per-frame paint]; Leave the blanket row and let LANGUAGE_KEYED reach into the file per memo (what shipped) [cheapest useful thing: holds one memo inside the coordinator at the cost of one row, checked structurally rather than asserted in prose; but it is OPT-IN, so it catches nothing nobody has thought about, and closes THIS hole and no other] | The per-signature opt-in list, SIZED AS ITS OWN TASK, with the shipped per-memo reach standing until then. A by-name skip on the biggest file in the tree cannot be argued away by the fact that its other arms are pinned elsewhere; this bug is what that argument looks like when it fails. | new |
| D130 | census-allowlist-path-asymmetry | The census filters its MERGED SCAN through isCensusPath but does NOT filter the extras ALLOWLIST, so a row naming a name under an excluded path prefix becomes an entry nothing can ever satisfy, and unusedExtras reports it as 'GONE from merged', which is the exact opposite of the truth. Teach the tool to recognise an excluded-path row, or keep the convention that such names are filed as literal-only records? | The Phase 18 census close-out (this ledger). Five names (releaseParentRefs, newestReleaseRef, classifyLineage, isAnchorPath, Lineage) were reported GONE while all five are defined, exported, called and asserted in scripts/merge_audit/golden_composition.mjs and its .d.mts. Cause established by controlled experiment, not by reading: the identical bytes at both extensions were copied into a scanned directory, where the tool's own readMergedTree ACCEPTED them, and refused them under scripts/merge_audit/. Extension is not the cause (.mjs and .mts are both in SOURCE_EXTENSIONS) and declaration twins DO count (five .d.mts-only names from a neighbouring module already resolve today). The exclusion itself is INTENTIONAL and documented twice in the tool, and it is LOSSLESS for the direction that matters: no parent ref carries scripts/merge_audit/ at all, so nothing there can ever be MISSING, and the only verdict suppressed is EXTRA, which is what it was written to suppress. The tool is right to fail loudly on an unsatisfiable row and wrong about the reason it gives. | Keep the convention: file such names as literal-only records, which is what the doc's own header rule already prescribes (a name the census cannot match does not need an extras row) [what shipped; zero tool change, record preserved, one doc edit; but it is a convention a future author must know, and getting it wrong produces a confidently WRONG failure message rather than a helpful one]; Filter the allowlist through isCensusPath so an excluded-path row is informational rather than unsatisfiable [durable and self-policing, but the rows carry no path column, only prose, so the tool cannot tell an excluded-path row from any other today: it needs a path column or an explicit marker, a parser change and a pin update, which is a SCHEMA change to the census]; Drop the exclusion and explain the auditors [73 rows now and a row for every future auditor export forever, for tools that exist on no parent by construction, which is the cost the exclusion was created to avoid]; Narrow the exclusion to symbol_census.mjs alone [pays most of the previous option's cost and fixes none of the asymmetry] | Filter the allowlist, sized as its own task. The shipped convention is correct and cost nothing, but it fails in the worst direction: an author who files honestly into the wrong table gets a message asserting their live export was lost, which is a confidently wrong claim rather than a missing one. A schema change to the gate is not something a sweep packet should spend, which is why it is tabled rather than taken. | new |
| D131 | capture-seed-without-marker | Twenty-five capture scripts seed woc_settings graphicsPreset WITHOUT graphicsDefaultApplied, so src/main.ts runs firstRunGraphicsPreset BEFORE the renderer reads the preset and the device probe is free to PERSIST its own tier over the seed. Fix the exposed scripts, add a guard, or accept it? | The Phase 18 E2E unit, measured across five arms rather than read: the marker came back TRUE from a seed that never set it, which is the proof the probe ran. The committed frames were at the lowest preset anyway, but by ACCIDENT, because headless is software GL and resolveDefaultGraphicsPreset lows software GPUs; the seed was advisory and the harness agreed with it by luck. The unit then NARROWED the blast radius honestly instead of reporting all 25: render/gfx.ts tierFromHints consults forcedTierFromSearch FIRST and returns on a hit, so a ?gfx= URL outranks the stored preset outright. 14 of the 25 seed a tier ABOVE low (which headless would replace) and 10 of those force the tier in their URL and are immune. FOUR are genuinely exposed: scripts/climb_stall_shot.mjs, scripts/lib/perf_hitch_browser.mjs, scripts/load_probe.mjs, scripts/perf_tour.mjs. The remaining 11 seed low only, so they are unaffected headless and merely fragile headed. scripts/pr_shot_targets.mjs, the canonical PR screenshot path, is already correct. | Fix the four exposed scripts to seed the marker beside the preset [three of the four are PERF INSTRUMENTS, which is the consequential kind: a tour that believes it is benching a high tier may be benching whatever the machine resolved, and its numbers would be wrong in a way nothing reports; four one-line edits on another unit's surface]; Add a guard that any script seeding graphicsPreset also seeds the marker [durable and catches the next one, but it must not fire on the 10 that force the tier by URL, so it needs to understand forcedTierFromSearch or accept a documented allowlist]; Fix the four AND add the guard [the complete answer, and the guard is what stops the pattern recurring in a 25-script family]; Accept [no-op; every future capture and perf run keeps depending on the machine agreeing with the seed by luck, and the failure is silent] | Fix the four and add the guard, sized as one task on the perf surface. The Phase 18 unit fixed only the two rigs it owned and REPORTED the rest rather than reaching into another unit's files, which is the right boundary; the remainder is a real defect with a silent failure mode and should not be left as a report. | new |
| D132 | ignite-bank-estimator-rate | The fire short-fight harness models Meteor's Ignite bank at the fire mastery's ignitionPct (0.3), while the sim actually banks igniteFrac (0.4, src/sim/content/classes.ts:8080) on those ground impacts. The estimator therefore UNDER-banks and the conservation ratio reads high. Correct the estimator to 0.4, leave it deliberately conservative, or something else? | The Phase 18 gate fix round. The arm failed at ratio 1.029 for an unrelated reason (a display-name rename dropped every ground impact out of the banked total), and fixing that landed it at 0.994. The rate asymmetry was found while diagnosing it and was deliberately NOT touched, because correcting it would LOOSEN a conservation arm, and loosening a conservation assertion is never a fix-round decision. The arm's bounds are unchanged and byte-identical to HEAD: paid <= banked * 1.02 and paid >= banked * 0.8. | Correct the estimator to igniteFrac so it models what the sim actually banks [the harness then describes the real mechanic rather than an approximation of it, and the ratio's meaning tightens; but the arm currently passes BECAUSE the estimate is conservative, so correcting it moves the reading and the bounds have to be re-derived rather than kept, which is a balance-adjacent change this packet has no mandate for]; Leave it conservative and say so at the code [no-op plus a comment; the arm keeps passing for a reason that is now written down instead of accidental, and an under-banking estimator fails SAFE, since it can only make a real double-dip easier to see, never harder]; Re-derive the bounds against the corrected estimator as one task [the complete answer, and the only one that leaves both the model and the thresholds meaning what they say] | Leave it conservative and write the reason at the code, then re-derive as its own task if anyone wants the tighter model. An estimator that under-banks fails in the safe direction for a conservation arm, and the alternative is re-deriving balance thresholds, which is not something a sweep packet should do. Gameplay math follows real classic-era formulas and is never tuned to make a test read better. | new |
| D133 | commission-minimum-fee-floor | Does the commission minimum-fee floor ship inside this packet at all, and if it does, (a) which mechanics carry the fee (escrow at open, transfer at delivery, or a display-only stated offer) and (b) which basis sets the floor (the UNBIND_FEE_BY_QUALITY_TIER ladder, a maintainer-named fraction of the output's buyValue, or a maintainer-named flat number)? Kept as ONE row rather than two: the cheapest live answer closes both halves in one sentence, and the mechanics choice constrains which bases are computable at all (escrow needs an amount known at open, a stated offer does not), so the basis becomes a separate question only if the ruling is to build. | state.md:20356-20370 as read, the Phase 14 ledger block "### THE STOP RULE FIRED: the commission minimum-fee floor is PARKED": "no record anywhere gives the floor's value or basis, and a fee's transfer mechanics collide with the sim's own recorded no-escrow position", then "OWED A MAINTAINER RULING: (a) the fee's mechanics ... (b) the floor's basis". Re-affirmed at state.md:20539-20540 ("The fee floor's park is the stopping rule executing, not scope shed"), state.md:20741-20743 ("THE PARK audited complete ... no fee-floor code, test, or copy anywhere"), state.md:21029 ("The commission fee-floor ruling (the Phase 14 fired stop rule) stands.") and phase-14-qa.md:72-76 (the deliverable "HALF-MET BY CONSTRUCTION"; the negative-case audit "N/A for the same reason"). Handoff row 128 is farming/state.md:626 as read, the 7th and last row of the second handoff table (:618-626) after the 121 rows of the first (:469-589), status "open ruling-owed, PARKED (stays parked per the Phase 17 corrected premises)"; counted into the maintainer-gated class at decisions-index.md:80-85 ("54 in-table rows (38 open-family plus the fee-floor park appended in this same change per state-COLLECT ...)"). Unticked deliverable: phase-14-crafting-ux.md:98 as read; source promise: brainstorm.md:46-47. LIVE code at 2ab5c2f7d0: the no-escrow doctrine is prose in three places (src/sim/professions/commission_order.ts:15-17, :144-148 "No escrow: nothing is charged or reserved", src/sim/professions/CLAUDE.md:172-174) and pinned once (tests/professions_commission_order.test.ts:120-126, the copper-unchanged assertion); no fee field exists on either shape (CommissionOrder :43-68, CommissionOrderRow :127-142); openCommissionOrder :150-193 with OpenOrderDenyReason :80-85, deliverCommissionOrder :284-333 with DeliverOrderDenyReason :95-101, cancelCommissionOrder :196, the retention sweep updateCommissionOrders :375-389, and the board is "in-memory Sim state only ... not persisted across a server restart" (:18-19). Candidate basis: src/sim/professions/commission.ts:63 UNBIND_FEE_BY_QUALITY_TIER = [2500, 10000, 40000] copper with unbindFeeFor :69-74. Seam and wire: src/world_api/professions.ts:197-219 (CommissionOrderView), :424 and :427-430 (the four action members); src/sim/sim.ts:8927-8934 and :8963; src/net/online.ts:1872, :3674, :4321; the deny-reason union src/sim/types.ts:6969-6984 and its client ladder src/ui/hud.ts:12113-12143; server/game.ts:658-666 (CORDER_WIRE_HZ 4 plus the four prompt commands), :667-673 (the named residual: corder has no proximity gate, so one board mutation rebuilds for every session), the ws arm :6865-6871, the snapshot gate :9039-9055; parity pins tests/world_api_parity.test.ts:393-397. The shipped player promise a floor would falsify is src/ui/i18n.catalog/guide.ts:3134-3135, "Nothing is held back when you post: an order reserves no coin and no materials, so the price and who supplies the reagents stay between the two of you", rendered on the public wiki by src/guide/pages/professions_economy.ts:130 and already translated in all 21 overlays (src/ui/i18n.locales/, e.g. de_DE.ts:1082). | Ratify the park and ship the packet with no fee floor, recording any build as post-packet work [docs only: a dated RULED qr-19 line at state.md:20356-20370, the handoff row flipped at farming/state.md:626, the maintainer-gated count re-derived at decisions-index.md:80-85, the progress.md Phase 19 row, and one honesty amendment so brainstorm.md:46-47's "adds both" and the unticked phase-14-crafting-ux.md:98 deliverable read as half-delivered by decision, optionally one filed issue; zero code, zero tests, zero i18n, zero regen, no deed, Reliquary, item-art or R5-surface obligation, no reviewer beyond the docs pass]; Display-only stated offer with an enforced floor, no coin ever moves [an optional offer field on CommissionOrder (:43-68), CommissionOrderRow (:127-142) and CommissionOrderView (world_api/professions.ts:197-219); a below-the-floor refusal that must land in THREE places, the sim enum OpenOrderDenyReason (:80-85), the event union src/sim/types.ts:6969-6984, and the client deny ladder src/ui/hud.ts:12113-12143, and that last arm GROWS src/ui/hud.ts, pinned at an exact count with zero slack (tests/monolith_budget.test.ts:379-388), so it must be paid by an extraction on the craft_denial_line_view / tool_effect_result_view precedent with the ceiling re-lowered in the same change; the openCommissionOrder signature grown in the facet (world_api/professions.ts:427) and in BOTH worlds (sim.ts:8927-8934, net/online.ts:4321) with the ws arm (server/game.ts:6865-6871) and tests/world_api_parity.test.ts:393-397 re-run; the row model and painter plus a formatMoney line (src/ui/hud/professions/commission_order_view.ts:20-39, commission_order_window.ts) with new hudChrome.commissionBoard.* keys in src/ui/i18n.catalog/hud_chrome.ts, their five M16 non-Latin fills (zh_CN, zh_TW, ja_JP, ko_KR, ru_RU) and npm run i18n:gen; the guide promise at guide.ts:3134-3135 reworded, which makes all 21 shipped overlay translations stale for the Phase 20 fill (a prose value does not dirty src/guide/content.generated.ts, so the freshness arm at tests/guide.test.ts:400-415 is not the cost, the overlay staleness is); four suites extended (professions_commission_order, commission_order_view, commission_order_window, commission_wire_cadence) with a negative below-the-floor case; census rows for every new export (scripts/merge_audit/symbol_census.mjs, RESULT PASS); before/after desktop and mobile screenshots committed under docs/screenshots per the PR template; and an epoch call this row cannot make for the maintainer: ONLINE_WORLD_LAYOUT_VERSION reads 26 (src/world_api.ts:185-190) with three pins (tests/bank_wire_epoch.test.ts:102, tests/security.test.ts:113, tests/world_auth_scripts.test.ts:179), tolerated absence on the Phase 14 crafterMasterworks precedent (world_api/professions.ts:206-213) versus a bump, and the epoch already carries its own open row at farming/state.md:494. Reviewers: architecture-reviewer, cross-platform-sync, server-hot-path-reviewer (the corder payload widens on a broadcast with no proximity gate, game.ts:667-673), frontend-seam-reviewer, test-coverage-auditor (docs/qa-gate.md:608-623 is the canonical roster). STILL needs a maintainer-named floor value or fraction, and whether an offer nobody is bound to honor counts as the "fee floor" brainstorm.md:46-47 names is itself a maintainer call]; Transfer at delivery, an atomic copper move when the piece changes hands [everything in the display-only option, plus the money move inside deliverCommissionOrder (:284-333) on the trade.ts precedent (affordability re-verified at the move, src/sim/social/trade.ts:640-644, then the atomic swap :758-759, never at accept), a new unaffordable arm on DeliverOrderDenyReason (:95-101) carried through the same three places, and the honest limit the in-memory board forces (commission_order.ts:18-19: a restart drops accepted orders, so a promised payment can evaporate) either recorded as a stated limit or paid off by persisting the board (a new table, a migration and a retention story per server/CLAUDE.md "Hot paths"); the purse itself needs no new column (it rides the serialized character state), but whether the move owes an audit trail on the bank_ledger precedent (server/db.ts:1161 copper_delta, :1190 counterparty_copper_delta) is a maintainer call, and it is the first thing a reviewer asks; adds privacy-security-review and database-performance-reviewer, plus migration-safety if the board is persisted. A player-to-player transfer creates and destroys no coin, and R4/R9 govern the MATERIAL economy (tests/masterwrought_materials.test.ts:545 and :573), so neither is in scope either way; the floor value is still maintainer-named]; Escrow at open, the requester's coin held when the order is posted [everything in the transfer option, plus reversing a doctrine stated in three places (commission_order.ts:15-17, :144-148, src/sim/professions/CLAUDE.md:172-174) and flipping the live pin (tests/professions_commission_order.test.ts:120-126), refunds wired on cancel (:196) and on the expiry arm of updateCommissionOrders (:378-383) with the terminal prune (:384-387) audited so no order is ever dropped still holding coin, which makes board persistence mandatory rather than optional because in-memory escrow destroys real player coin on a restart, so this option carries the new table, migration, retention sweep, migration-safety, database-performance-reviewer, privacy-security-review and an audit trail, and turns the guide reword into a full rewrite of orderBoardBody (21 overlays plus the M16 five in the same change); the heaviest option on the table, proposed at the packet's closing gate] | Ratify the park (option 1). Four records already lean there in their own words: the Phase 14 judged list calls the park the stopping rule executing rather than scope shed (state.md:20539-20540), the Phase 14 QA audited the park AS a park and confirmed no fee-floor code, test or copy exists (state.md:20741-20743, phase-14-qa.md:72-76), Phase 16 recorded that the ruling "stands" (state.md:21029), and Phase 17 wrote "stays parked" into the handoff row when it appended it (farming/state.md:626). The quality-signal half of brainstorm.md:46-47 shipped and tested, so the research line is half-answered already; both remaining halves still turn on a value no record holds, which is exactly what this phase's own stopping rule sends back to the maintainer rather than guesses (phase-19-rulings-gate.md:81-83); and every mechanic above display-only either reverses a doctrine three files state in prose or forces the in-memory board into persistence in the packet's last phase. If the maintainer would rather ship something now, the display-only option is the only one that touches neither coin nor persistence, and it still owes a named value plus the hud.ts extraction its refusal arm implies. | row 1 (the commission minimum-fee floor, closed as ONE row carrying both half-decisions, (a) mechanics and (b) basis: the recommended answer closes both together and the mechanics choice constrains the basis set, so the basis is recorded as sub-ruling D133b in the same sitting only if the ruling is to build) |
| D134 | named-legendary-instance-reliquary-page | Does a player-named legendary INSTANCE (a Perfected apex copy raised by the promotion, stamped `rolled.quality: 'legendary'` plus a player-chosen `payload.name`) earn a Reliquary rung of its own, and in which shape (a bounded instance-CLASS mark slot on the professions shelf, or a genuinely instance-keyed rung that catalogues the named copy), or is the Book of Deeds credit the complete cosmetic record? | farming/state.md ruling 135, p13-OPEN-RELIQUARY (lines 1845 to 1859 as read), the only record and explicitly undecided: "The open question for the maintainer: should a FUTURE reliquary rung exist for promoted instances (an instance-class page, not a def page), or is the Book of Deeds credit (prog_legendmaker) the complete cosmetic record? Owner: maintainer; until ruled, no phase may author an instance-keyed reliquary page." Minted at phase-13-orange-promotion.md lines 178 to 183 as read ("Open item to RECORD, not decide"), carried as the Phase 18 union matrix's single OPEN-MAINTAINER row (state.md line 22574 as read, "honored as recorded"); no later RULED line exists (grep over docs/prd/masterwrought as read 2026-08-31), the farming handoff table (lines 465 to 608, its absorb appendix to 627, as read) carries no row for it, and decisions-index.md holds no 135 and no Reliquary symbol, so ruling 135 is the one place a ruling lands. CODE ANCHORS, every line read at 2ab5c2f7d0 because the tree is mid-merge (drafted row D119 cites this same file at an older base, so the commit is stamped deliberately): `src/sim/professions/perfecting.ts:645` `promotePerfectedCopy`, the stamp site (`markItemDiscovered(ctx, meta, itemId, 'legendary')` at :735, `ctx.bumpDeedStat(meta, 'legendariesForged', 1)` at :736), reached only through `resolveLegendaryPromotion` at :575, with `src/sim/professions/legendary_name.ts` `normalizeLegendaryName` the one shape authority on both hosts and `server/perfect_item_ref.ts` `parsePerfectItemRef` (:23, its naming-authority note at :46 to :56) the server screen; `src/sim/content/reliquary.ts:127` `ReliquaryRelicDef` (FIVE kinds, item / mark / mount / weapon_skin / title, none instance-keyed), `:456` `RELIQUARY_PROFESSION_MARKS`, `:1296` the `professions_masterwork` page, `:1783` the derived `RELIQUARY_MARK_IDS` allowlist; `src/sim/reliquary.ts:613` `noteReliquaryMark` (allowlist-gated) and `:148` `serializeReliquaryState` (def-keyed and mark-keyed only); the deed side already fires TWICE, `src/sim/content/deeds.ts:1066` `col_first_legendary` on the `quality:legendary` visit mark (renown 0) and `:3225` `prog_legendmaker` (renown 50), both proven to land same-tick by `tests/orange_promotion.test.ts:303` to `:320`, with the authoring comment at `:3220` stating the design premise, "NO title and no border, deliberately: R3 gives the prestige to the ITEM"; the binding rules an instance rung would cross are `docs/design/reliquary.md:65` (rule 3, bounded by construction), `:73` (rule 5, item-global ownership) and `:93` (rule 9, the performance contract), with `:514` the append-only page-id migration contract. | (A) Ratify the deeds credit as the complete cosmetic record and close 135 with no rung, now or later [one dated RULED line carrying a `qr-19-<slug>` id at farming/state.md ruling 135, the maintainer's answer quoted in substance and the executing commit named, the recording shape this phase's own contract demands (phase-19-rulings-gate.md STEP 2, lines 57 to 64 as read), plus the Phase 19 ledger row in state.md and the progress.md row (STEP 5, line 76 as read); one bullet in the "Locked decisions" block of docs/design/reliquary.md (:474 to :507), the shape the permanent recipe-pattern exclusion already uses inside the Source-hint vocabulary bullet at :498 to :502, so the refusal is written where a future page author looks; decisions-index.md needs no row (it holds no 135 and no Reliquary symbol as read); zero code, zero pins, zero regen, zero fills, no new item id so no census row and no committed WebP, no deed record moves (both credits already ship), the R5 surface untouched; qa-checklist is the only reviewer]. (B) Author a bounded instance-CLASS MARK slot (one new mark id proving the first promotion, seated on `professions_masterwork` or on its own mark page), the Phase 18 `gather_event:golden_harvest` recipe repeated [the mark id string, its namespace, and its page seat are maintainer-named values; `src/sim/content/reliquary.ts` `RELIQUARY_PROFESSION_MARKS` plus the page relic list plus a source hint, one `noteReliquaryMark` call at the `promotePerfectedCopy` stamp site, a sim test on the write with a negative arm for the pre-promotion state; `tests/reliquary_content.test.ts` moves the slot total (`:489`, 427 as read), the mark total (`:506`, 32 as read), the overview pair and the character completion pair (`:451`, owned 363 total 363 as read), plus the page-count pins at `:367` to `:371` if it takes its own page, and TWO source pins ride with them that a count-only reading misses, the own-hint rule for multi-source pages (`:3877` to `:3893`) and the per-page `EXPECTED_DISTINCT_SOURCES` equality (`:3896` to `:3900`); `server/character_sheet.ts:209` `RELIQUARY_MARK_ENGLISH` MUST gain its row in the SAME change (the (bv) QA addendum, farming/state.md lines 4075 to 4081 as read, names the failure mode: `sheetRelicRecentText` at `:255` returns null for an unknown id and the /c/ SSR page silently drops the entry) and its English must be byte-identical to the client leaf, because `tests/character_sheet.test.ts:515` to `:527` sweeps EVERY mark id asserting server equals client, with the set equality at `:542`; `scripts/wiki/build_content.mjs:587` `RELIQUARY_MARK_GUIDE_NAMES` takes the same string or the wiki emit THROWS at `:639` ("reliquary wiki emit: unknown mark"), then `npm run wiki:content` regen, with `tests/guide.test.ts:1010` the derived cross-check; one English `hudChrome.reliquary.markFind.<leaf>` in `src/ui/i18n.catalog/hud_chrome.ts:6478`, forced by the DERIVED pin `tests/reliquary_window.test.ts:1066` ("authors a markFind leaf for every catalogued Reliquary mark id") and NOT by the Phase 21 locale arm at `:782` to `:822`, which derives over `slain:` leaves only, so the five non-Latin fills are an M16 obligation only if the value is wordy and otherwise land at the Phase 20 release fill, though the golden-harvest precedent shipped all five in-change (`src/ui/i18n.locales/ja_JP.ts:11843` as read, one row in each of ko_KR, ru_RU, zh_CN, zh_TW); `tests/profile_page.test.ts` sweeps the new English through its copy scan at `:351` to `:357`, while its `:364` and `:365` are floors at 29 that do NOT move; committed cell art or an authored glyph, because `src/ui/reliquary_cell_art.ts` answers only the perfect-specimen, golden-harvest, `masterwork:`, field-note and `slain:` families and a new family falls through to null at `:323`; the cell QUALITY rung is a SECOND resolver, `src/ui/reliquary_window.ts:1631` `cellQuality` knows only the `masterwork:` and `gather_event:` prefixes and paints everything else 'common' at `:1656` (the live recorded complaint at `docs/design/reliquary.md:662` to `:666`), with the painter's prefix arms pinned as source text at `tests/reliquary_window.test.ts:137` to `:143`, so the mark either reuses one of those two namespaces or both resolvers gain an arm; a page-id append if it takes its own page, which is the append-only migration contract, not an edit; no new item id, so no census row and no committed item WebP, and the R5 surface stays untouched; reviewers content-obligations-reviewer plus frontend-seam-reviewer for the two resolver arms]. (C) Author a genuinely instance-keyed rung that catalogues the named copy [a SIXTH `ReliquaryRelicDef` kind plus per-instance persisted ownership state that does not exist today (state is def-keyed and mark-keyed, `serializeReliquaryState`), paid against rule 9's performance contract on the character blob and the heavy-self wire; it contradicts rules 3 and 5 as written, so `docs/design/reliquary.md` is AMENDED rather than extended, a maintainer design call; the single-kind page pin (`tests/reliquary_content.test.ts:509`) and the `emitReliquaryUnlock` ownership path constrain where it may sit; anything of it that render or ui reads moves a member on the matching `src/world_api/` reliquary facet, both worlds, and the pinned list in `tests/world_api_parity.test.ts`; surfacing a player-chosen name re-opens the moderation-strip flow (state.md lines 19702 to 19712 as read: a stripped copy is permanently nameless and the published Discord card is out of reach of the strip), so privacy-security-review and a takedown story ride it, with database-performance-reviewer on the persisted state; plus every obligation in option B. Multi-session, and larger than this packet]. | Option A, ratifying the deeds credit, with the ground stated carefully and the ruling's own wording widened as it is written. The packet's record leans that way in four places: ruling 135's own sentence already names the deciding fact (the reliquary model is keyed on ITEM DEFS and the promotion mints no def, farming/state.md lines 1852 to 1853 as read); the promotion deed's authoring comment states the prestige premise outright (deeds.ts:3220, R3 gives it to the ITEM, the named legendary IS the trophy); the packet has written a NIL Reliquary verdict repeatedly and never once written a page (state.md:2818 the crafted-gear no-page precedent with its Phase 11 pattern carve-out, state.md:4692 ruling 10 keeping the four crafted epic tools out, farming rows 31, 55 and 81 at lines 867, 1026 and 1186 as read, and the neighbouring drafted row D044 declining the apex hoe on this same shelf); and the promotion in fact already lands TWO Book of Deeds credits, `col_first_legendary` at the stamp and `prog_legendmaker` at renown 50, which is more cosmetic record than row 135's text (it names only `prog_legendmaker`) credits it with, so the ruling is worth widening to name both. One caution the packet has already paid for: 11j found an earlier Reliquary decline "right for the wrong reason" (state.md lines 12103 to 12109 as read: the shelf catalogues three crafted rods, so "not conquerable unique loot" was false on its face), so a ratification should rest on the def-keyed model and rules 3 and 5, never on a conquerability claim. If the maintainer wants a Reliquary presence regardless, option B is the in-model, already-executed shape (the Phase 18 golden-harvest cell is the priced precedent), and option C is the one to keep out of this packet. | row 2 (Ruling 135 p13-OPEN-RELIQUARY: does a player-named legendary INSTANCE warrant an instance-keyed Reliquary page) |
| D135 | feast-per-area-placement-cap | Does the placed-feast family gain a per-AREA placement cap on top of the shipped one-active-feast-per-placer bound, and if so at what COUNT and on which SCOPE (a spatial-grid cell, a radius in yards, or a whole zone), or does the Phase 17 refusal stand as the answer of record? Both halves are maintainer-named and no ledger in the packet holds either one: not the count, not the scope. | state.md:22629-22631 as read, Phase 17 JUDGED REFUSALS item 3: "Hot-path F3 (feast per-area cap): the per-placer bound is the recorded anti-abuse answer; the interest-cost exposure is the same class every placed ground object carries (pre-existing shape, recorded as a read below)". Items 3 and 6 are the TWO entries in that six-item list carrying no qr-18-REOPEN annotation; the other four were re-opened and actioned by Phase 18 (annotations at 22620, 22625, 22633, 22636 as read), so Phase 18 left F3 standing rather than re-opening it. The "recorded as a read below" pointer does not resolve: no item in the RECORDED, NOT ACTED list (22645-22674 as read) records an interest-cost read, so that cross-reference is stale. Adjacent records: farming/state.md:565 as read (Phase 12 QA accepted set naming the "O(live feasts) scan", owner "none (perf trigger recorded)", status accepted-no-action) and farming/state.md:4349-4352 as read ("The one-active scan is O(live feasts) per placement ... Owner: a server perf round if packed-hub numbers ever flag it (an ownerKey index makes it O(1))"). The per-placer rule itself is settled content: state.md:15113-15115 as read pins K4 in both directions, and phase-11k-provisioning-capstone.md:120-127 as read rejects the per-tier variant. LIVE code at 2ab5c2f7d0: src/sim/professions/feast.ts:228-259 placeFeastAction (dead, busy, swimming, then the ownerKey walk over ctx.feasts at 253-259 emitting farmDenied 'feast_active'), the rule stated at feast.ts:38-43, the roster teardown arms at feast.ts:353-362, the 1 Hz sweep at feast.ts:472 updateFarmFeasts; the whole placement body was read and carries no area, cell or density gate of any kind. What a cap could enforce through: the placement path ALREADY walks every live feast (feast.ts:254), so a count inside a radius can ride that same bounded walk with one distance compare against ctx.entities.get(feast.entityId).pos and need no spatial query at all; otherwise src/sim/sim_context.ts:152 ctx.grid over src/sim/spatial.ts:11 SpatialGrid (every placed feast is in it via src/sim/entity_roster.ts:154) exposes spatial.ts:95 forEachInRadius and spatial.ts:121 someInRadius (shipped precedent src/sim/professions/farmer_npcs.ts:46), both RADIUS-shaped, while SpatialGrid.cells and keyAt are private (spatial.ts:12, :17), so a true per-CELL scope has no public accessor today; a zone scope would key off src/sim/data.ts:846 zoneAt. CORRECTION to the render-side reasoning: src/render/farm_patches.ts:328-336 (FEAST_SHADOW_CAP = 8) reads "Presence is deliberately never capped: a feast is actionable (you can eat it) ... the natural bound is one feast per online placer crossed with the ~120 yd interest scope", but a placed feast is kind 'object' (src/sim/entity.ts:853-855 createGroundObject), and server/interest_policy.ts:50-55 interestLimitSq gives the non-npc arm INTEREST_RADIUS (= PLAYER_INTEREST_RADIUS 90, src/sim/types.ts:43) on entry and INTEREST_DROP_RADIUS (100, src/sim/types.ts:48) to persist, so the live server bound is TIGHTER than that comment's ~120 yd (which is NPC_INTEREST_RADIUS at server/interest_policy.ts:24 and does not apply to an object). Sibling placeable: src/sim/professions/mobile_station.ts:97-118 keeps one station per player in PlayerMeta.mobileStation and carries no per-area cap either. | (a) RATIFY the Phase 17 refusal, no per-area cap: the per-placer bound crossed with the object interest scope stays the bound, and FEAST_SHADOW_CAP keeps shedding the expensive cosmetic half [docs only: one dated RULED qr-19-feast-per-area-placement-cap line at state.md:22629-22631, repairing the stale "recorded as a read below" pointer in the same line; farming/state.md:565 already reads accepted-no-action so no status flips, and its trigger at farming/state.md:4349-4352 already names the owner if packed-hub numbers ever flag it; no code, no test, no wire arm, no i18n, no regen, no wiki, no deed, no reliquary, no art, no R5 touch, no decisions-index.md edit (no namespace moves), no reviewer beyond the docs read]; (b) PER-RADIUS cap at a maintainer-named count and a maintainer-named radius (the record holds neither; constants the code already names are INTERACT_RANGE 5 at src/sim/types.ts:35, STATION_RADIUS 20 at src/sim/content/professions.ts:522, and the 90 yd object interest radius, but the choice is the maintainer's) [cheapest shape rides the existing walk: extend feast.ts:253-259 to count live feasts within the radius off ctx.entities positions, no new index and no new spatial primitive; if the maintainer prefers the grid, spatial.ts:95 forEachInRadius counts any N with a closure and spatial.ts:121 someInRadius covers exactly N=1, so no counting sibling in spatial.ts is required either way; then a NEW arm in the farmDenied reason union at src/sim/types.ts:7533-7538, which is a WIRE union widening (the event crosses at server/farming_commands.ts:89 and :103) and compile-breaks the exhaustive Record<FarmDeniedReason, true> at tests/farm_event_feedback.test.ts:266-290 until its row lands; one English catalog leaf beside src/ui/i18n.catalog/hud_chrome.ts:5177 feast_active (the HUD key is a template literal at src/ui/hud/professions/farming_view.ts:34-36, so the leaf is hand-added and no HUD map moves) plus, if the English value is wordy enough to trip M16, the five non-Latin fills the sibling carries (ja_JP, ko_KR, ru_RU, zh_CN, zh_TW are the only five of the twenty-one overlays holding feast_active), with the other sixteen inherited by the Phase 20 release fill lane that the release-tier gate hard-fails on, exactly the footprint the reserved feast_range precedent accounts for at feast.ts:56-64; the i18n regen; new arms in tests/professions_feast.test.ts and tests/feast_online.test.ts (sim and server both, the packet's standing rule); reviewers server-hot-path-reviewer, architecture-reviewer, cross-platform-sync (a new wire arm an older client cannot name, so the packet's own stale-client-window standing item under seeded row 3 is the inherited exposure) and a fresh reader; draw-free so determinism is untouched, and no IWorld facet member or tests/world_api_parity.test.ts pin moves because no new client-visible STATE is added; no wiki regen, no deed row, no Reliquary page, no item id and no committed WebP art, because nothing conquerable or nameable is minted]; (c) PER-CELL cap at a maintainer-named count and a maintainer-named cell size (the SpatialGrid default of 32 at spatial.ts:15 is an implementation default, never a design ruling) [the (b) price plus NEW public surface on SpatialGrid, since cells and keyAt are private (spatial.ts:12, :17): a cell-keyed count accessor as its own pure unit with its own test; and the cell scope makes the refusal boundary invisible to the player (one step across a cell edge flips the answer), a design cost (b) does not carry]; (d) PER-ZONE cap at a maintainer-named count [the (b) code, wire, test, i18n and reviewer price, plus a zone tally the module does not keep: a zoneAt-keyed count rebuilt per placement walks every live feast again (the very scan the perf half wanted bounded), or it needs a maintained index with teardown arms at feast.ts:353-362 and feast.ts:472; it also refuses placements zone-wide, a shared-world budget design call rather than an anti-abuse bound]; (e) SKIP the cap and take the recorded trigger instead: the ownerKey index beside ctx.feasts that makes the one-active scan O(1), which farming/state.md:4349-4352 already names as the owner-triggered fix [a small sim-side index plus teardown arms wherever feast state is dropped (feast.ts:353-362, feast.ts:472) and one test; no wire arm, no i18n, no regen, reviewers architecture-reviewer plus a fresh reader; it closes the perf half of F3 and nothing about area density, so it answers only if the maintainer's concern is the scan rather than the crowd]. | Option (a), ratify, is what the packet's own records lean toward, offered as a recommendation and not as a ruling. Phase 18's re-open sweep annotated four of the six judged refusals and left F3 standing, and shipped code states the same answer in its own voice at src/render/farm_patches.ts:328-336, which sheds shadows rather than presence because a feast is actionable. The correction above strengthens rather than weakens that case: the live object interest bound is 90 yd on entry, not the comment's ~120, so real density is tighter than the refusal assumed. Every enforcing option needs a count and a scope no ledger holds, and each mints a refusal a player can hit at a raid door, which is a gameplay change carrying a performance justification, while the measured half already has its own cheaper named fix at farming/state.md:4349-4352. If the maintainer wants density bounded regardless, (b) is the shape to fund: it is the scope the shipped primitives express, and on the existing walk it costs no new index, though it would still need a maintainer-named radius and a maintainer-named count. Either way, the stale "recorded as a read below" pointer at state.md:22631 is worth repairing in the same RULED line. | row 5 (the feast per-area placement cap, a value and a scope, per cell or per zone) |
| D136 | nonce-fence-expiry-term | Decision: does the live-session `nonce` fence gain the expiry qualifier its own sibling arm already carries (`AND expires_at > now()`), so a lapsed but unreclaimed lease row stops admitting a resumed session's autosave over a landed offline strip, or does the packet ratify suspend-first and propagate the honest limit the code already states at ONE site to the two that overpromise? A ruling must settle three residues the arms carry: whether `heartbeatCharacterLeases` takes the same qualifier in the SAME commit (unqualified, a recovered process re-extends its own lapsed rows and reopens the window inside one tick; qualified, a lapsed row can never be healed, so one stall past the 90 s TTL makes every session that process holds permanently unsaveable), whether the term applies to all four fenced save paths or only the autosave (they share one fence shape, so narrowing it means a FIFTH shape and its own pin, not a smaller diff), and what client-visible reason a session fenced out by EXPIRY is kicked with (the live path reuses the takeover literal). | docs/prd/masterwrought/state.md:20238 to 20241 (as read 2026-08-31), inside the "### RECORDED, NOT ACTED" block opening at :20237, in "## Phase 13 QA ledger (2026-08-27, the twin)" at :19943: "The nonce fence carries no expiry term (inherited): an expired-but-unreclaimed lease plus a same-realm double boot can autosave over a landed strip; the operator answer stays suspend-first (DEPLOY.md)." Unlike its neighbours at 20242 to 20244 and below it carries NO qr-18-REOPEN line, and docs/prd/masterwrought/phase-18-perfection-sweep.md:82 to 83 routes it here in as many words ("the nonce fence's missing expiry term is a DECISION (Phase 19), not this phase's"); the related SETTLED record is state.md:20120 to 20136 (THE RECONNECT WINDOW, the one-predicate fence); no farming handoff row exists (a `nonce` grep over docs/prd/masterwrought/ returns only state.md:20238, phase-18-perfection-sweep.md:82, phase-19-rulings-gate.md:22). LIVE code read at committed 2ab5c2f7d0 because the working tree is mid-merge, all line numbers as read: server/character_save_statement.ts:characterUpdateStatement, the `nonce` arm 107 to 116 (`EXISTS (SELECT 1 FROM character_leases WHERE character_id = $1 AND holder = $4 AND nonce = $5)`, no expiry term) against the `unleased` arm 117 to 126 (`NOT EXISTS (... WHERE character_id = $1 AND expires_at > now())`); the shape is picked for ALL FOUR fenced writers by server/db.ts:liveSaveFence 3426 to 3430 (saveCharacterState 3432, saveCharacterAndMarketState 3494, saveCharacterAndGuildBankState 3590, saveCharacterStateOnClient 3747); server/db.ts:LEASE_TTL_SECONDS 4556 (90 s, three missed 30 s heartbeats), :PROCESS_LEASE_HOLDER 4563 (realm plus a per-boot UUID, so a peer process is never the same holder), :acquireCharacterLease 4578 to 4604 (reclaim predicate 4600 `expires_at < now()`, and every acquire ROTATES the nonce, which is why a real reclaim closes the hole), :releaseCharacterLease 4614 to 4630, :heartbeatCharacterLeases 4635 to 4643 (`WHERE holder = $1`, unqualified), :releaseAllCharacterLeases 4647 to 4649, and a `character_leases` grep over server/ at that commit shows the only DELETEs are 4620, 4627 and 4648, so a lapsed row survives until a reclaim, a clean leave, or a shutdown. The window needs a session THIS process cannot see, which is what the record's double-boot wording is for: the strip endpoint already refuses an online character on its own map (server/clear_item_name.ts:294 plus the post-read re-check 306 to 316). The limit is ALREADY stated in code at one site, server/clear_item_name.ts:56 to 63: "the live save's own nonce fence carries no expiry term, so a session whose lease lapsed (ninety seconds of missed heartbeats) and was never reclaimed can autosave over a landed strip. An operator handling a contested name suspends the account first (DEPLOY.md)." The two sites that do NOT say it: the statement builder's `unleased` header at server/character_save_statement.ts:18 to 27 ("a 0-row result tells the offline writer to refuse rather than land a write the session's next autosave would clobber", which implies a LANDED strip is final) and the operator flow at DEPLOY.md:535 to 541; server/CLAUDE.md:100 to 111 is accurate as written (its promise at 108 to 110 is scoped to a displaced zombie overwriting LIVE state, which is true) and only omits the offline writer's side. What a fence miss costs today: server/game.ts:4484 to 4515 (revert this session's guild-book ops, `guildBankIncident('save_fenced_out')` when the save carried books, kick "character taken over"), the escrow-side twin server/game.ts:4663 to 4671 (escrowSessionLost, kind 'fenced'), and server/paid_guild_creation.ts:269 (`lease_lost` kicks after a charge compensation); server/game.ts:flushPeriodicSaves 2698 to 2709 fires the save and the heartbeat unawaited in one tick body, with no ordering guarantee; the strip's writer is server/offline_character_save_db.ts:runOfflineCharacterSave 70 to 92. | (A) RATIFY suspend-first and pay only the honesty cost: no SQL, and the paragraph to propagate already exists verbatim at server/clear_item_name.ts:56 to 63, so the work is the `unleased` header at server/character_save_statement.ts:18 to 27 (a landed strip is not final while a lapsed unreclaimed row can still admit its own session's autosave), one added sentence in server/CLAUDE.md's persistence bullet 100 to 111 covering the OFFLINE writer's side (leave the displaced-zombie sentence at 108 to 110 alone, it is true as written), and one operator line beside DEPLOY.md:535 to 541 saying suspend before stripping because a landed strip can still be reverted. No pin moves (tests/server/character_save_statement.test.ts asserts SQL fragments and CHARACTER_SAVE_LEASED_LINE, never comments), no regen, no i18n, no content obligations (no wiki regen, no deed or reliquary row, no item art, no M16 fill, no census row, no parity golden, R5 untouched), no monolith exposure (neither file sits on tests/monolith_budget.test.ts). Reviewer: privacy-security-review (DEPLOY.md is a deploy file, the trigger column at docs/prd/masterwrought/implementation-plan.md:148). The exposure the ruling then owns, stated: a strip reports success and can be reverted by the lapsed session's next autosave, and the same window covers every writer behind the unleased arm (the rename and reclaim signer sweeps, the PBE roster save, server/db.ts:3485). No auth or replay exposure: the nonce is server-stamped per join and never client-supplied, so this is write ordering, not authentication [prose only: 3 files, 0 pins, 0 regen, 1 reviewer]. (B) ADD THE TERM: `AND expires_at > now()` appended inside the EXISTS subquery at server/character_save_statement.ts:107 to 116, with (B1) the same qualifier on server/db.ts:heartbeatCharacterLeases 4635 to 4643 a SEPARATE call the ruling must make (the two-sided risk is in the Decision cell) and (B2) whether the term binds all four fenced paths or only the autosave, since a narrower scope needs a new fence kind plus its own unit arm rather than less work. The pin trap: both existing assertions are `toContain` (tests/server/character_save_statement.test.ts:42 and tests/character_lease.test.ts:130) and stay GREEN over an appended term while proving nothing, so the change owes POSITIVE assertions in both files plus one real-Postgres arm in the nonce describe at tests/character_save_statement_pg_integration.test.ts:441 to 460 (its grantLease 99 to 107 already takes a secondsFromNow, so an expired-lease refusal is a three-liner); nothing existing reds on SQL text, because both pg fixtures grant an hour (that file's 444, 449, 456 and tests/guild_bank_pg_integration.test.ts:143 to 150) and the nonce arm's `not.toContain('NOT EXISTS')` at :43 is unaffected. One repo obligation the SQL edit must respect: server/db.ts is on the monolith ratchet at tests/monolith_budget.test.ts:1537, ceiling 4937, the row's own words "Exact count, zero slack", so B1's qualifier must ride the existing `WHERE holder = $1` line with no added comment line, or the same commit owes an extraction to pay for the growth. The behavior price is real: a fence miss is not a quiet skip, so a lapse now reaches the revert-count-kick arm at server/game.ts:4484 to 4515, the escrow twin at 4663 to 4671, and the paid-guild path at server/paid_guild_creation.ts:269. Keeping the present reason is matcher-covered already (src/ui/api_error_i18n.ts:367, the leave map src/ui/server_i18n.ts:2040 to 2047); a DISTINCT maintainer-named reason owes an English `errors.api.*` row in src/ui/i18n.catalog plus the api_error_i18n arm, and if the leave reason also differs the LEAVE_REASON_KEY row at server_i18n.ts:2046 with its two fallback rows at :133 and :287, all in the same change under the S3 guard (tests/localization_fixes.test.ts), locale fills riding Phase 20, plus a stable code row at server/http/error_codes.ts:112 to 113 only if that reason ever reaches a REST answer. Reviewers: privacy-security-review, migration-safety (server/db.ts, implementation-plan.md:149), database-performance-reviewer (the qualifier rides the existing character_id primary-key probe, no index work), and cross-platform-sync if a new reason string touches the matchers (implementation-plan.md:151). No wiki regen, no deed or reliquary pin, no item art, no census row, no parity golden: IWorld is untouched [2 source files, 2 pin files given positive assertions, 1 new pg arm, 1 ratchet constraint, an optional i18n set on a maintainer-named string, 3 to 4 reviewers]. (C) CLOSE IT AT THE OFFLINE WRITER and leave the live fence alone: server/offline_character_save_db.ts:runOfflineCharacterSave 70 to 92 issues `DELETE FROM character_leases WHERE character_id = $1 AND expires_at <= now()` inside the transaction it already bounds (OFFLINE_CHARACTER_SAVE_LOCK_TIMEOUT_MS at :43), before the fenced UPDATE, so the strip reclaims the corpse it is already permitted to write past and a resumed session's nonce then matches nothing. Price: one statement, and the existing sequence pin REDS rather than passing, so this is a repair and not just an addition: tests/server/save_offline_character_state.test.ts:99 to 109 pins statements[0] BEGIN, [1] statement_timeout, [2] lock_timeout, [3] idle_in_transaction and then `expect(update).toBe(4)`, so an inserted DELETE moves the UPDATE to index 5 and fails that arm until it is re-pinned with the DELETE asserted explicitly by text and position; plus one real-Postgres arm on the pattern already at tests/character_save_statement_pg_integration.test.ts:164 to 175. Two second-order costs to price honestly: this does NOT spare live sessions, because a session whose row was deleted takes the same 4484 to 4515 revert-count-kick path on its next save, so C narrows the kick to characters an offline write actually touched instead of removing it; and it changes the offline writers' contract from "refuse while any unexpired lease stands" to "reclaim the lapsed row first", a design call the ruling must make rather than a mechanical substitution. It also leaves the nonce arm admitting a lapsed row for every non-offline write, so option A's honesty prose is still owed on top. Reviewers: privacy-security-review, migration-safety, database-performance-reviewer. No i18n, no content obligations, no ratchet exposure (server/offline_character_save_db.ts is not on the monolith list) [1 source file, 1 pin repaired plus 1 pg arm added, 3 reviewers, no i18n, no content obligations]. | Recommend (B) with B1 answered separately, and (A) as the arm to take if any new kick path is unacceptable. The ledgers lean both ways and the split is the substance: state.md:20238 to 20241 records the operator answer as suspend-first, while state.md:20120 to 20136 settled this whole family on ONE in-statement predicate over a check-then-write, and that is the record that put `expires_at > now()` into the unleased arm in the first place. As it stands the two halves of one fence disagree about what a live lease IS, and both disagree with the reclaim predicate acquireCharacterLease already uses at server/db.ts:4600; B is the only arm that makes all three agree, and the write it newly refuses is made in a state where a peer is already entitled to steal the character, so it was never safe to land. B1 is where the caution belongs and is not this row's to settle: unqualified, the heartbeat heals a lapsed row and reopens the window inside one tick (server/game.ts:2698 to 2709 fires both unawaited); qualified, one stall past the 90 s TTL makes every session that process holds permanently unsaveable and kicks them all at the next autosave, a larger blast than the hole being closed. C is the smallest closure of the case the record actually names and is the fallback if B is refused, but it is a narrowing of the kick and not an exemption from it. A is cheaper than it first looks, because server/clear_item_name.ts:56 to 63 already carries the honest paragraph and A only propagates it. This row names no value: the reason string, the TTL, whether B1 rides B's commit, and B2's scope are all maintainer-named. | Seed row: row 6 (the nonce fence's missing expiry term, "keep suspend-first, or add the term", "Phase 13 QA recorded read", docs/prd/masterwrought/phase-19-rulings-gate.md:22) |
| D137 | alchemy-band3-repeatable | Alchemy is the only one of the ten crafts with no repeatable band-3 (skillReq 75) recipe: its band-3 seat is `recipe_quickening_catalyst`, which is `oncePerDay`, so the 75 to 100 leg has no full-gain row at all. Does the maintainer fund an authored repeatable band-3 alchemy recipe (and if so, what output, at what potency or value, on what bill), or ratify the gap as the deliberate consequence of putting the packet's one pacing gate on alchemy's own intermediate? | state.md:21337-21340 as read, the alchemy bullet verbatim ("Alchemy has no repeatable band-3 recipe (its only tier-3 row is oncePerDay), so an alchemist at 75 without a flask pattern has one catalyst a day or a hundred grey-rate crafts. Cannot be closed by tuning down; new content is out of scope"), inside the Phase 15 ledger's "RECORDED, NOT ACTED (maintainer reads on top of the standing sets)" block opening at state.md:21312. FOUR of that block's eight bullets carry a qr-18-REOPEN "actioned by Phase 18" annotation (21314, 21317, 21333, 21342) and four do NOT (21324 REF_ARMOR, 21329 the two-piece bound, 21337 alchemy, 21347 qr-GRAY); the four unannotated ones are exactly the maintainer-decision reads the ruling routes to this table (decisions-index.md:18 as read, "routes every open maintainer DECISION into Phase 19's table"), and they are already claimed one apiece by seeded row 15, by D092 (state.md:23143), by this row, and by seeded row 9. Same read at docs/design/power-verification.md:1161-1164 (section 12.4's first bullet, heading at :1159), priced into 12.2 at :1085 (the alchemy row 150 / 375 / 2.50) and :1096-1098 ("its repeatable skillReq set is 0, 25, 50, 100, 125, so the 75 to 100 leg runs at the 0.5 multiplier and costs 50 crafts rather than 25"), into 12.3 at :1136-1138, and recorded again as one of the nine factual corrections at state.md:21431-21434. LIVE CODE, re-derived at 2ab5c2f7d0 by parsing every record in recipes.ts with comments stripped (the trap a naive lookahead falls into: recipes.ts:504 carries the literal `professionId: 'enchanting'` inside a COMMENT and mis-attributes recipe_clockreel_fishing_rod; the corrected parse reads 170 records, matching the wiki record's 170 at state.md:21343-21345, and enchanting reads 3 rows, matching power-verification.md:1149-1152): src/sim/content/recipes.ts:2211 `recipe_quickening_catalyst` (alchemy, skillReq 75, oncePerDay true, acquisition ['trainer'], apothecary, inside `INTERMEDIATE_RECIPES` at recipes.ts:2207); alchemy's eighteen rows sit at 0, 25 and 50 (thirteen rows, twelve trainer-taught plus the acquisition-less `recipe_minor_healing_potion` at recipes.ts:139), 75 (the gated catalyst), 100 (the three drop-taught flasks at recipes.ts:2921, 2940, 2959) and 125 (`recipe_grand_cauldron`, drop-taught, recipes.ts:3143), so alchemy is the only craft of the ten whose repeatable skillReq set omits 75 AND the only one with no repeatable TRAINER row at 75 or above, while the other nine R13 intermediates (state.md:2272-2281) all sit repeatable at 75 (pinned by tests/recipe_economy.test.ts:951-963). The bands are src/sim/professions/wheel.ts:104 `TIER_SKILL_STEP` = 25, :108-111 `tierForSkill`, :136-142 `tierProgressMultiplier` (1 / 0.5 / 0.25 / 0, the orange/yellow/green/gray reading spelled in its own doc comment at :126-135) against `CRAFT_SKILL_GAIN` = 1 at src/sim/professions/crafting.ts:146. Nothing pins the gap itself: no arm asserts alchemy's repeatable skillReq set or the absence of a 75 rung, and the nearest arms cover rungs 0, 25 and 50 only (tests/recipe_economy.test.ts:1305-1313, tests/provisioning_supply_line.test.ts:147) plus the 125 ceiling (tests/provisioning_supply_line_apex.test.ts:1018-1021). Scope bound worth stating: the gap bites only an alchemist whose craft can climb past 75 at all (active archetype, paired major, or hobby; anything else hard-stalls at 75, power-verification.md:1153-1155 as read). | Ratify the gap and pin it [a dated RULED line where the read stands (state.md:21337-21340) plus an AMENDED line on power-verification.md's 12.4 first bullet (:1161-1164), and one new tripwire arm asserting alchemy's live repeatable skillReq set (0, 25, 50, 100, 125) beside the existing rung arms in tests/recipe_economy.test.ts, so a later authored row reds the record rather than rotting it silently; no item id, no art, no i18n key, no census row, no wiki regen and no guide reword owed (the shipped routeBody at src/ui/i18n.catalog/guide.ts:2803 already calls the 100 to 125 stretch a trickle), no reviewer beyond qa-checklist; carries the wording rider below at no extra cost]; Author a repeatable band-3 row with a NON-power output, a second alchemy material or reagent on the shipped 75-band scaffolding (skillReq 75, itemLevelBudget 20, level 20, acquisition ['trainer'], apothecary station, stated at recipes.ts:2187-2197; junk-kind common-quality material per the profession_items.ts doctrine recorded at state.md:2280-2281) [a new item def in src/sim/content/profession_items.ts beside the catalyst at :1460, plus a new recipe record in a NEW exported array wired into `ALL_RECIPES` (recipes.ts:4633): never a `LADDER_RECIPES` growth (pinned at 54 rows, six crafts, three rungs, tests/recipe_economy.test.ts:638 and :1275-1286, with the doctrine at recipes.ts:2184-2186 and :1711-1719) and never an `INTERMEDIATE_RECIPES` seat (length-pinned at 10 at tests/recipe_economy.test.ts:644, and its per-row shape arm at :951-965 requires every non-catalyst row there to consume exactly one catalyst); a new trainer-taught array also joins the trainer-acquisition equality at tests/recipe_economy.test.ts:606-619 and owes the sibling count literal every other term there carries; an R15 IP naming verdict, web-verified and recorded in the packet's naming registry (R15 at state.md:123, the Phase 07 exemplar at state.md:2282-2291); English name and description in src/ui/i18n.catalog/items.ts (the id list around :2544-2553 and the name map around :3113-3122), English-appended per that file's own stated convention for this family, with the non-Latin fills routed to the Phase 20 release fill unless the maintainer wants them in-change under M16; a committed WebP under public/ui/items or an ITEM_ART_PENDING exemption at src/ui/icons.ts:5824, either way re-minting the art audit verdict, which is not two literals but a sealed blob: the byte length and sha256 at tests/item_art_consistency.test.ts:485-488 as well as itemArtFilesReviewed 1124 and liveItemDefinitions 1139 at :507-508; an "Explained extras" row in docs/prd/masterwrought/merge-deletion-list.md for EVERY new name (the export, the recipe id, the item id, each i18n key), which is what scripts/merge_audit/symbol_census.mjs reads; `npm run wiki:content` plus a reword of the alchemy craftProse against the freshness gate, touching ladderBody at src/ui/i18n.catalog/guide.ts:2799-2800 and, because a new taught rung changes what they say, routeBody at :2803 and identityBody at :2794 (which today names the catalyst as the only 75-rung teach), every reword keeping the live content arms at tests/guide.test.ts:3930-3959 and :3961 onward green, and a wordy English value pulls its own M16 fills; a NEW result id, because tests/professions_crafts_to_mastery.test.ts:458-459 pins `resultItemId` one-to-one catalog-wide; new economy arms in tests/recipe_economy.test.ts, plus the per-material craft-list arms in tests/material_profession_affinity.test.ts (the shape at :209) and, if the bill names a crop or produce, tests/farm_seed_channels.test.ts:234-240 (the at-least-seven alchemy floor) and tests/provisioning_supply_line.test.ts:147; a re-derivation of power-verification.md 12.2, 12.3 and 12.4 (alchemy's intended path drops 150 to 125 and its ratio 2.50 to 3.00, which also moves 12.3's "2.5 times alchemy's longer intended 150" clause at :1136-1138 and the counts seeded row 9 rests on); a second 4 gold tier-3 training fee on the same rung, derived rather than chosen (src/sim/professions/training.ts:53, TRAINING_FEE_BY_TIER[3] = 40000, kept exception-free by R8 at training.ts:41-52); content-obligations-reviewer plus qa-checklist, with architecture-reviewer skipped because the Review Dispatch Matrix (docs/prd/masterwrought/implementation-plan.md:152) excludes pure data and test changes. No deed row is owed (deeds are per-craft skill milestones at 50 and 125, tests/deeds_content.test.ts:567-600) and no Reliquary page (the output is not conquerable unique loot). The output's identity, name, bill and sellValue are a maintainer-named design call this row does not guess]; Author a repeatable band-3 CONSUMABLE, a rung-75 draught or elixir filling the step between the rung-50 rare draughts and the drop-taught apex flasks [everything in the previous option PLUS the power surface: consumables are 2.4 to 3.5 points of the R5 envelope (state.md:21284-21285), and the ladder-order constraint the trophy row already litigated at recipes.ts:4453-4485 ("R21 in reverse: the shipped rows go dead for anyone holding tallow", :4466-4467) together with R23's vendor-is-a-floor rule mean the potency is a maintainer-named value, never a derived one; arms in tests/flask_consumables.test.ts and the Well Fed band and dominance arms in tests/masterwrought_budget.test.ts; an R5 record amendment and a harness re-cut if the piece enters any measured kit, which while R5 is frozen is an escalation rather than an execution; architecture-reviewer joins only if the output needs new sim effect logic instead of reusing the shipped potion, elixir and flask fields (implementation-plan.md:152). This is squarely the "new content" the ledger's own sentence puts out of scope]; Un-gate the catalyst, or add a second tier-3 alchemy row derived from it [refused by the packet's own design rather than by cost: the catalyst is the ONE pacing gate and the whole apex demand math rests on it (state.md:2301-2313, one craft per day per character, three catalyst-days per apex piece, six for a capped kit), and its `oncePerDay` is pinned in three places, tests/quickening_catalyst_gate.test.ts:98-109 (the whole-row shape, a suite dedicated to this gate), tests/recipe_economy.test.ts:951-965, and transitively at tests/professions_crafts_to_mastery.test.ts:399-411 with the exact excluded set at :445-450, while tests/masterwrought_budget.test.ts:752, :778 and :932 pin its ABSENCE on every apex row, so this re-opens R13 and re-prices every apex piece rather than a rung]; Move an existing repeatable alchemy row up to skillReq 75 [cheapest in bytes, likeliest to reopen a settled row: the three flasks are pinned at rung 100 and the cauldron at 125 by the apex consumable sweep (tests/masterwrought_budget.test.ts:760-779) and by the top-rung arm at tests/provisioning_supply_line_apex.test.ts:1018-1021, the 0 / 25 / 50 ladder rows are pinned three-per-rung (tests/recipe_economy.test.ts:1275-1286) with a consumable owed at each (:1305-1313), so the only movable rows are the trophy and farm rows, and each carries its own settled placement record (recipes.ts:4453-4485 for the lesser potion, and the growth tonic's (ai) row still "open ruling-owed" at farming/state.md:483, which belongs to seeded row 4 rather than here)] | Ratify the gap and pin it. Both ledgers already lean that way in their own words ("Cannot be closed by tuning down; new content is out of scope", state.md:21339-21340, repeated at power-verification.md:1163-1164); the qr-18-REOPEN sweep actioned this block's four code-shaped bullets and left the four maintainer-decision ones, this among them, for exactly this table (decisions-index.md:18 as read); and the packet's own precedent for a content feature surfacing at the rulings gate is D042 (state.md:23093, "any funded feature is a design phase of its own, so record the ruling and route funded items to a future phase rather than executing in the rulings gate"), with D092 (state.md:23143) the shape a ratification takes on a sibling bullet of this very block. The gap is also narrower than the recorded sentence implies, which is the second half of the recommendation. THE WORDING RIDER, worth taking whichever way the maintainer rules: state.md:21339 and power-verification.md:1163 both say "a hundred grey-rate crafts" at skill 75, and neither the number nor the word survives the live curve. At capability tier 3 the rung-50 rows are exactly one tier below, so they pay 0.5 and the 75 to 100 leg is 50 crafts, which is what power-verification.md:1096-1098 itself derives; the hundred-craft figure belongs to the 100 to 125 leg, and at that capability the rung-50 rows are two tiers below, which is GREEN at 0.25, not gray (gray is three or more tiers below and pays zero, wheel.ts:136-142), so no quantity of gray crafts advances anything. The shipped player copy already reads the corrected way ("The last stretch from 100 to 125 is a deliberate trickle", src/ui/i18n.catalog/guide.ts:2803), so the rider is a docs-only correction with no wiki regen behind it. If the maintainer does fund an authored row, the non-power material option is the one that leaves the R5 surface untouched while R5 is frozen, and its output, name, bill and value remain a maintainer-named design call. | row 7 (Alchemy has no repeatable band-3 recipe: author one, or accept the gap; phase-19-rulings-gate.md:23 as read, "Phase 15 recorded read") |
| D138 | qr-gray-row-wording-false | Ruling qr-GRAY's row states a premise the packet's own measurement falsifies: does the maintainer amend row 128 in place against the measured read, rewrite the sentence outright, or rule the measurement wrong and keep the wording as it stands, and does whichever form is chosen also carry to the doc mirrors that repeat the premise (brainstorm.md's future-tier bullet, which is the line a future-tier session actually loads; the two historical phase specs; and power-verification.md's own section 12.2, which still names the floor path "cheap" while its section 12.3 measures that path as dearer)? | The ruling row is docs/prd/masterwrought/farming/state.md lines 1768 to 1777 as read (row 128, inside "THE QUALITY-REVIEW ADOPTION PASS, 2026-08-20", heading at line 1606 as read). Its falsified sentence, lines 1770 to 1771 as read: "the cheapest path to any craft skill number is always bulk-spamming low recipes rather than crafting the band's real content". Falsified in materials by docs/design/power-verification.md section 12.3, lines 1132 to 1148 as read: "the tier-0 spam path is DEARER than the intended path for 8 of the 10 crafts (only leatherworking and inscription come out cheaper)". It is also false on time, but the two eights are DIFFERENT sets and must not be conflated: section 12.2's table rows at lines 1084 to 1093 as read put the floor path longer than intended on all TEN crafts (375 against 125 on the eight rows that read 125 / 375 / 3.00, 375 against alchemy's 150, 225 against enchanting's 150), while the dearer eight excludes leatherworking and inscription and includes alchemy and enchanting, so what the measurement supports is longer on ten, dearer on eight, and both at once on those eight. "always" fails twice more: enchanting has no tier-0 row at all, so both its columns start at skill 25 (lines 1090 and 1139 to 1141 as read), and the genuinely cheapest path in materials is the lowest reagent cost per skill point, which sits one or two tiers UNDER the band rather than at the floor and beats the intended path for 9 of 10 crafts at 0.66 to 0.88 of cost (lines 1142 to 1148 as read). Recorded open at docs/prd/masterwrought/state.md lines 21347 to 21350 as read, under "RECORDED, NOT ACTED (maintainer reads on top of the standing sets)" at line 21312 as read inside the Phase 15 ledger (heading at line 21129 as read): "qr-GRAY's own row wording is measurably false as written ... The record states both; the row itself is left for the maintainer." Four of its neighbours in that block carry dated RE-OPENED lines from qr-18-REOPEN (lines 21314, 21317, 21333 and 21342 as read) and this one does not, so Phase 18 left it here deliberately. The counter-measurement is not one reader's: state.md lines 21431 to 21437 as read, "THE NINE FACTUAL CORRECTIONS, each re-derived", already re-derived alchemy's row to 150 / 375 / 2.50, enchanting's to 150 / 225 / 1.50, and the cheapest-per-point read to 9 of 10 at 0.66 to 0.88. The row's OBSERVATION half (below-band crafts gain at half and quarter rate) survives every option, and its live code anchor is src/sim/professions/wheel.ts:tierProgressMultiplier (the 1, 0.5, 0.25, 0 ladder at line 136) with src/sim/professions/archetype.ts:craftSkillGainMultiplier (line 524), both read at 2ab5c2f7d0; so does the DECIDED half, whose stated WHY is the 11e, 11f and 11i pacing models being derived against the shipped multiplier (lines 1771 to 1777 as read), which the false premise does not carry. Doc mirrors of the same premise: brainstorm.md lines 75 to 78 as read, implementation-plan.md lines 878 to 881 as read and phase-15-power-verification.md lines 138 to 142 as read (the last two via the phrase "the cheapest below-band spam path"), plus power-verification.md's own section 12.2 at lines 1080, 1082 and 1101 as read, which defines and labels the floor path as the "cheap path" that 12.3 then measures as dearer; implementation-plan.md line 232 as read only points at the row and carries no premise. NO non-doc mirror exists: git grep at 2ab5c2f7d0 over src/, server/, tests/, scripts/ and public/ for qr-GRAY, gray-grind, "cheapest path", "bulk spam" and "below-band" returns nothing but one unrelated hit at tests/owned_class_balance_role_bands.test.ts:32, and the five test citations of farming/state.md and brainstorm.md are comments, not file reads, so no pin, no wiki row, no player-facing string carries the wording. | (A) Amend in place, the doctrine form: one dated AMENDED line under farming/state.md row 128 replacing the false premise with the measured read (floor spam is longer on all ten crafts and dearer on eight, and the real arbitrage is the lowest reagent cost per skill point one or two tiers under the band), the exact replacement sentence a maintainer-approved wording, the DECIDED half left standing, the same dated amendment on brainstorm.md lines 75 to 78 so the future-tier revisit is aimed at the arbitrage rather than the floor, and a dated RULED qr-19 line on the state.md recorded read at 21347 to 21350 so it stops standing open [three doc edits plus this phase's own Phase 19 ledger row and progress.md row; no code, no test, no wiki regen, no deed or reliquary pin, no item art, no M16 non-Latin fill, no census row, no parity golden, no R5 surface touch; no domain reviewer, since only docs move, with the phase's fresh reader and the phase-level qa-checklist still running]; (B) A plus re-pointing the two historical phase specs (implementation-plan.md 878 to 881, phase-15-power-verification.md 138 to 142) that repeat the premise as "the cheapest below-band spam path" [two further doc edits, and precedent runs BOTH ways so the maintainer picks: phase-15-power-verification.md lines 131 to 132 as read already carry an in-place parenthetical correction inside a phase body ("this line said 'three' and the tree and commit 08ba6cf61f both say two"), while decisions-index.md lines 33 to 34 as read say farming phase-file bodies "are history"; the risk of skipping it is a reader later finding some mirrors moved and some not]; (C) A plus renaming section 12.2's own "cheap path" column and its two prose uses (power-verification.md lines 1080, 1082 and 1101 as read) to the floor-spam name section 12.3 already uses [one design-doc edit; it closes a contradiction inside the authority document itself, since 12.2 labels as "cheap" exactly the path 12.3 measures as dearer, and 12.2 is what a future-tier session reads for the numbers; no test pins power-verification.md's text, so the price is the edit and the re-read alone]; (D) Literal REWRITE of row 128's sentence instead of a dated amendment [same one-line edit cost, but it spends an explicit exception to farming/state.md lines 677 to 678 as read, "an existing row that was wrong is AMENDED IN PLACE with a dated line rather than rewritten", and it destroys the record that the row was ever wrong]; (E) Rule the MEASUREMENT rather than the row, keeping the wording and recording that the cost half is not authority [one doc line, no code, but it needs the maintainer to name WHICH half is wrong, and on the document's own terms only the reagent-value half is disputable: power-verification.md lines 1104 to 1109 as read say the craft-count columns are worked by hand and reproduce from the document while the reagent-value column does not; it also has to overrule a correction a fresh reader already re-derived once (state.md 21431 to 21437 as read), and it leaves state.md 21347 to 21350 standing as an unresolved contradiction against section 12.3]; (F) Promote the counter-measurement to a pinned artifact BEFORE amending: commit the three-path walk (intended, floor spam, cheapest-per-point) as a script on the scripts/r5_envelope_probe.ts precedent with a test on the tests/r5_envelope_probe.test.ts precedent (both present at 2ab5c2f7d0, and state.md lines 21405 to 21411 as read record that exact move being made once already, for the same reason: a gate resting on numbers nothing in the repo could re-derive) [one new scripts/ module and one new test file, tsc and changed-file biome, the tests reviewer plus the fresh reader and qa-checklist, and no sim or content def is touched so the R5 surface stays untouched; it also forces the re-read against the merged catalog that section 12.2 has not had, since 12.2's table and 12.3's 8-of-10 split carry no sync amendment while section 12.4's wiki bullet was AMENDED 2026-08-29 for the seventh v0.41.0 sync that added four tailoring bag recipes (recipe_duskweave_bag and recipe_resonant_weave_bag both read skillReq 50 at 2ab5c2f7d0, src/sim/content/recipes.ts:1743 and :1763), so the amended numbers would be re-derived rather than copied, at whatever count that read returns] AMENDED IN PLACE 2026-09-02 at 19E execution: option C is four sites and five tokens in section 12.2 (the definition line, the header row's two column labels, and two prose uses), not a header plus two prose uses; every section-12 address is +17 and the farming/state.md row is at :1834 to :1843 at the merge tip; the DEARER sentence wraps :1151 to :1152; the test-citation count reads ten across eight files. Executed as A with C folded, rider F offered and not taken, recorded under qr-19-qr-gray-row-wording-false.; (G) Defer past the PR [no cost now, and it carries a known-false authority row into the merge, which is the thing seeded row 9 exists to stop] | Option A with C folded in, and F named as an optional rider rather than an alternative. The packet's record already leans this way and only the row lags: state.md 21350 as read says in the packet's own voice that "The record states both; the row itself is left for the maintainer", so the corrected read is written down and what is missing is the disposition of the row, not the finding. Amending rather than re-litigating is safe here because the DECIDED half of qr-GRAY never rested on the false premise (its WHY is the shipped-multiplier derivation of the 11e, 11f and 11i pacing models, farming/state.md 1771 to 1777 as read), so no gain-curve question re-opens and tierProgressMultiplier is not touched. The dated-amendment form is the packet's own doctrine and not a style preference (farming/state.md 677 to 678 as read, decisions-index.md "The rule" at lines 24 to 29 as read), which is why A is preferred over D. brainstorm.md is folded into A because it is the mirror with forward consequence: left as written it aims a future-tier revisit at the floor when the measurement says the lever is the per-point arbitrage. C is folded in because it is the same false claim standing in the authority document, one word in a column header plus two prose uses, and leaving it means the ledger row is corrected while the table it cites still calls the dearer path cheap. F is worth offering because the amended premise would otherwise rest on a maintainer RECORD rather than a reproducible artifact (power-verification.md lines 1104 to 1109 as read state the reagent-value column does not reproduce from the doc, and no committed script produces it), and it is the only option that re-verifies the 8-of-10 split against the merged catalog. B and E each need a maintainer call this row cannot make on their behalf, and D and G both leave a recorded contradiction inside the authority set. | row 9 (qr-GRAY's row wording is measurably false as written, reword the row or rule the measurement) |
| D139 | zone-celebration-fanout-shape | Seed row 11 is only PARTLY answered, so the sitting owes two answers, not one. FIRST, ratify or reject Phase 18's MEASURED REFUSAL of the shared-bytes splice, which the ledger already calls answered. SECOND, rule the half that refusal does not touch: emitToZonePlayers walks EVERY player in the realm and calls zoneAt on each one, once per celebration, a cost the same review recorded as unmeasured and identical under both arms of the refusal. Accept the scan on record, measure it before ruling its shape, or index the walk away? | state.md:22655-22658 as read (the reopened Hot-path F4 note, "recorded so the NEXT tenant is a conscious decision; the seam if the list grows is the realm-readout memo plus sendRaw"); state.md:23290-23297 as read ("zone-celebration-fanout-tenants is a MEASURED REFUSAL (117 ns and 192 bytes per recipient today vs 8 to 11 ns shared; 0.4 us per second at design cadence, 0.14 percent of a tick at the absolute ceiling) ... premises pinned in tests/zone_celebration_fanout_shape.test.ts (so Phase 19 seed row 11 is answered: no shape change owed)"); and state.md:23355-23363 as read, which both AMENDS that record (the hot-path review found routeEvents' O(recipients x sessions) selection pass dominating the stringify term by three to four orders of magnitude, since closed by server/event_pid_index.ts) and records the residue verbatim: "emitToZonePlayers scans every player with a zoneAt call per celebration (O(all players), unmeasured, identical under both arms of the refusal)". LIVE anchors at 2ab5c2f7d0: src/sim/professions/gather_events.ts:71-85 emitToZonePlayers (a walk over ctx.players.values() at :76 with the DUNGEON_X_THRESHOLD and zoneAt(e.pos.x, e.pos.z).id !== zoneId filter at :82) and :129-138 announceZoneCelebration; src/sim/data.ts:846-858 zoneAt, which walks the ACTIVE content's zone list per call (ZONES holds 15 at src/sim/data.ts:721-737). The measurement's premises really are pinned: tests/zone_celebration_fanout_shape.test.ts (225 lines at 2ab5c2f7d0) holds the small-flat-payload cap (CELEBRATION_COPY_MAX_BYTES 256 at :57), the pid-only-varying shape (shapes.size exactly 1, :124-125), one event per recipient at widths 1, 12 and 64 (:197-205), the instanced-celebrant zero arm (:166-173), and the ceiling read as source text out of DEFAULT_MAX_PLAYERS_PER_REALM = 5000 (server/http/config.ts:216, test :208-224); the routing amendment is in the same header at :23-35 with its counting arms live in tests/event_pid_index.test.ts. The reason this row exists: no arm in that suite counts zoneAt calls or the non-recipient walk, so the scan carries no premise pin at all. Two facts neither record carries: the fanout has a FOURTH tenant beside the three celebrations the record names, announceGatherRareEvent (gather_events.ts:91-107, the second DIRECT caller of emitToZonePlayers; the other three ride announceZoneCelebration at :137), and the record's realm-rate line borrows the rare-event cadence itself as the celebration rate (suite header :13-14 as read, "1 per zone per 20 min, 20 zones, 200 in-zone", the knob at gather_events.ts:15-31, GATHER_RARE_EVENT_CHANCE 1/90), so the shared scan runs at roughly double the rate that line assumes (the exact multiplier is a maintainer read, not a number this row names); and that same line assumes 20 zones where ZONES holds 15, which errs in the conservative direction. | Option 1, ratify the splice refusal AND accept the scan on record [a dated RULED line where both records stand (state.md:22655 and :23290), one honest sentence at gather_events.ts:71 saying the walk is O(all players) and never measured, one line in the suite header putting the scan explicitly outside the pinned premises, and biome on the two touched files; no test, no regen, no fills, no census row, no reviewer beyond a doc read. The cost is that the packet ships a hot path whose only cost statement is "unmeasured", which is exactly the posture the routing term hid behind for a phase]; Option 2, ratify the splice refusal and MEASURE the scan before ruling its shape [one bench over the fanout suite's own fixture, which already builds N in-zone players plus the far-zone and instanced non-recipients (tests/zone_celebration_fanout_shape.test.ts:71-107 as read), so the wasted walk is already expressible there; the figure recorded in the suite header and the ledger the way the two existing measurements are, and the shape then ruled on the number. No production code, no regen, no goldens, no fills, no census row, no reviewer dispatch. The suite pins premises and never timings today (no timing assertion in its 225 lines as read), so whether the bench lives in a Vitest arm or in a scratch harness with only the premises pinned is a maintainer-named call. It may well end at Option 1 anyway, which is the point: it makes THAT ratification a measured one]; Option 3, index the walk away [the real blast radius. Either new SimContext state (a per-zone player bucket or a cached per-player zoneId, of which there is none today: the tow path re-resolves zoneAt twice inline at src/sim/sim.ts:6673-6685 as read, and the shape to copy is the mastery-reset counter at src/sim/sim_context.ts:127-135, "one integer read per tick instead of an O(players) walk"), kept roster-exact on every roster and position mutation the ONE sim performs (add, remove, teleport, tow, every zone crossing), the discipline src/sim/spatial.ts:2-4 already states for the grids; or a rect query on the existing playerGrid (sim_context.ts:153), which SpatialGrid does not have today (forEachInRadius, someInRadius and hasInRadius only, src/sim/spatial.ts:95-181) and which would visit roughly 12 by 12 cells for the 360 by 360 eastbrook_vale rect (zone1.ts:157-158, WORLD_SIZE 360 at data.ts:739, cellSize 32 at spatial.ts:15), making it SLOWER than the walk on a small realm. Four obligations beyond the code, all verified at 2ab5c2f7d0: the SimEvent emit census names its fanout helpers BY HAND (SIM_EVENT_FANOUT_HELPERS holds exactly emitToZonePlayers and announceZoneCelebration, scripts/merge_audit/symbol_census.mjs:130, resolver at :1505-1513), so a rename or a second helper must join that set plus its pin arms at tests/merge_audit_symbol_census.test.ts:158-161 and :188-196 or those event kinds silently leave the emits class; three suites hand-build a minimal SimContext slice (tests/zone_celebration_fanout_shape.test.ts:98-105, tests/professions_attunement_events.test.ts:20-28, tests/gather_rare_events.test.ts fakeCtx at :199-232) and each fake must build the new field and keep it exact; roster maintenance added on Sim reds the src/sim/sim.ts ceiling in tests/monolith_budget.test.ts (:797 onward, the packet's zero-slack discipline, so the answer there is extraction, never a raise); and ORDER is the trap, the fan-out emits in ctx.players roster order and tests/masterwork_zone_broadcast.test.ts:140-142 pins "in roster order" verbatim, so a bucket either re-sorts (handing back part of the saving) or moves that pin. No IWorld facet member and no tests/world_api_parity.test.ts duty (the fanout is sim-internal), and no parity golden re-record is owed today: no file under tests/parity/golden/ carries masterworkZone, attunedZone or legendaryForgedZone at 2ab5c2f7d0, though a scenario added later would. Dispatches server-hot-path-reviewer and architecture-reviewer (docs/qa-gate.md:612 as read routes simulation architecture there; there is no separate sim reviewer agent) plus test-coverage-auditor if pins move, and the determinism scan in tests/architecture.test.ts applies to whatever index is added]; Option 4, shrink the per-player constant without new state, testing each player against the celebration zone's own rect instead of re-resolving zoneAt per player [confined to emitToZonePlayers plus a sim test; membership and roster order unchanged, so no goldens, no census row, no regen, no fills. A strict rect helper already exists, zoneContaining at src/sim/data.ts:869-877, and its two DOCUMENTED differences from zoneAt are exactly the correctness risk to name rather than assume away: it returns null outside every authored rect where zoneAt clamps through a southmost-band fallback (src/sim/data.ts:849-857 as read), and it reads the static ZONES where zoneAt resolves the ACTIVE world content (src/sim/data.ts:866-868), so an editor play-test map would resolve differently. Buys a constant factor, never the O(all players) shape; dispatches architecture-reviewer] | Option 2, then Option 1 or Option 4 on the number. The splice half is genuinely answered and ratifying it costs nothing: the ledger says so in its own words and the premises are pinned exactly as it claims. The scan half is the one worth not ratifying blind, because this phase already made that mistake once and caught it: the first record priced the stringify term at 117 ns per recipient, the hot-path review then found the routing term three to four orders of magnitude larger, and that is why the record is amended in place rather than trusted. A second term carried as "identical under both arms, unmeasured" is the same shape of hole, the fourth tenant pushes more traffic through it than the record's rate line assumes, and the fixture that would close it already sits in the suite. Option 3 reads as premature before there is a number: its cheap form is slower on a small realm and its correct form moves a pinned event order and the hand-maintained census set. AMENDED IN PLACE 2026-09-02 at 19E execution: the suite record ALREADY carries the fourth tenant and derives its realm rate from it (f14f015a7b); only the state.md records lacked it, and the unstated term is the other tenants' cadence summed on top. Measured first on the suite's fixture shape (about 100 ns per non-home player: 84 us per celebration at 1,000 players, 471 us at the 5,000 cap, 1 percent of one tick), then ruled OPTION 1 on the number under qr-19-zone-celebration-fanout-shape, with a scratch harness and a premise arm pinning the walk shape; every cited state.md, suite, census and monolith address had moved (recorded in phase-19e-measurement.md). | row 11 (the zone-celebration fan-out shape, IF Phase 18's measurement recorded a refusal): the measurement DID record a refusal, so the splice half of the row is a ratification, and the O(all players) zoneAt scan the same review recorded is the half the seed row leaves open |
| D140 | r5-remeasure-on-merged-world | Is the R5 record RE-MEASURED on the catalog current when the re-measure runs, or RATIFIED as the measurement of the pre-raid catalog it was taken on, with the nine expected-fail pins retired? A re-measure re-opens a Verdict that closed 2026-08-29, so it needs the maintainer to say three things this row cannot: that the four ratified rulings' conditions still govern (1a the bestEpicGearFor pool, 2b the modelled "+2 lead-stat points" term as the R5 quantity, 3a the four measured lanes, 4-amend the apex-bag tie), that the closure's R5-surface stop rules are relaxed past "only to this exact extent", and that qr-18-REOPEN's carve-out leaving the R5 rulings untouched is lifted. It must ALSO settle the target set: keep the two protected assets (heroic Nythraxis and S-rift) or add an Ignivar Crucible profile as a third. | Seeded row 12, docs/prd/masterwrought/phase-19-rulings-gate.md:28 as read; the target-set half has its own home at state.md:22848 as read ("R5's protected-asset set does not name the new raid (folded into row 12's re-measure option)"). The authority is the eighth-sync AMENDED block in the Phase 15 ledger, state.md:21639-21718 as read ("THE R5 FIXTURE SURFACE MOVED, the first sync where it did" at :21640-21641; the item-by-item re-derivation on both parents at :21641-21673; the chest lead -1, the zero-flagged dev-bis loadouts and the ledger's "flagged family is still 17" at :21672-21675; the nine-pin TREATMENT at :21675-21686; "what moved is the CATALOG under the measurement, not any packet def or record row" at :21717-21718), sitting under the suspension at :21529-21638 and its RESOLVED line at :21633-21637; the four ratified rulings verbatim at :21971-22003; the closure's stop-rule sentence "The R5-surface stop rules relax only to this exact extent" at :22012-22014; the closure record at :22016-22067, whose own verdict sentence reads "SETTLED BY RATIFICATION, not by a demonstrated bound" (:22060-22061), with the 9.4 coupling row and the 300-seed precision pair inventoried at :22031-22040; the Phase 17 summary THE R5 ESCALATION at :22799-22808; the fresh reader's byte-identity check on every it.fails body at :22913-22914; the ninth sync's R5-untouched re-check at :22943-22972 (the R5 sentence at :22958-22961). The locked ruling itself is state.md:99-102 and amends IN PLACE per decisions-index.md:44-45; decisions-index.md:18 as read records that qr-18-REOPEN "leaves the R5 rulings and the accepted-by-design rows untouched". In the promoted record: docs/design/power-verification.md:13-17 (locked ruling 5 and its protected asset), :26-59 (the CLOSED BY RULING Verdict and the ratified lane table), :108-135 (HISTORY second entry, "NOTHING IN THIS RECORD WAS REWRITTEN: its figures are the measurement of the pre-raid catalog they were taken on" at :125-127), :553-583 (the committed fixture and its two documented invocations, the commands themselves at :559-561), :841 (the 9.6 reproduce invocation), :1178-1190 (section 13's tripwire table, three of whose rows ARE pins below), :1205-1257 (section 14). LIVE CODE, all read at 2ab5c2f7d0: scripts/r5_envelope_probe.ts:HEROIC_TARGET (:137), :SRIFT_TARGET (:142), :ROGUE_BIS (:378), :WAR_BIS (:511), :WAR_EQUIPPED_ITEMS (:566), :WAR_EQUIPPED_DELTA (:570), :furyBody (:650), :CASTER_BIS (:711). The NINE pins, each byte-identical to the pre-merge branch and each carrying a MERGE-INHERITED escalation note plus the flip-back-in-the-same-commit rule: tests/r5_envelope_probe.test.ts:105 (the section 9.6 dead-hit and live-crit mechanism arm, note at :85-104) and :158 (the section 9.5 tank effective-health table, baseline hp 3332 asserted at :162 against the merged 3532); tests/masterwrought_budget.test.ts:941 (an it.fails LOOP over MERGE_INHERITED_TWIN_DUPLICATES at :844, so it is TWO pins, forgefold_legguards and spiritweld_girdle) and :1587 (the R5 Perfected-lead cap arm, note at :1575-1586, MAX_LEAD_BY_SLOT chest 2 at :1609-1610 against the merged measured -1); tests/dev_bis_gear.test.ts:155 (every class picks exactly the cap of flagged pieces, note at :135-154 whose consumer list is at :150-154) and :225 (the flagged picks pinned BY ID per class); tests/server/pbe_boost.test.ts:924 (every role kit at most the cap, kitsAtCap 10 at :942) and :947 (the demotion fallback). A repo-wide git grep of it.fails at 2ab5c2f7d0 returns twelve sites, ten in those four files of which two are comment lines, so the count reconciles as 2 plus 3 (:941 expands to the two twin ids) plus 2 plus 2 = nine, alongside tests/terrain_window_seams.test.ts:56 and :71. Gate arithmetic today: state.md:22907-22910 as read, "11 expected fail ... exactly the nine escalation pins plus the two pre-existing terrain-seam xfails", inside a run the same ledger records as PASS. | (A1) RE-MEASURE on the catalog current when it runs, target set unchanged: re-derive the section 3 baselines item by item at that tip (the eighth sync did exactly this once, on both parents, state.md:21641-21673 as read), re-run both documented invocations of scripts/r5_envelope_probe.ts (power-verification.md:559-561 as read: the default run for rogue, caster and tank, then WOC_R5_SEEDS=60 WOC_R5_SECONDS=600 for the fury binding row), plus the 9.6 equipped arm (WOC_R5_ARMS=FULL,FULL+equipped, :841), the 9.4 coupling row at WOC_R5_SECONDS=300 and the 300-seed precision pair at 600 s (both inventoried at state.md:22031-22040); rewrite the Verdict and sections 3, 8.1, 9.1 to 9.6 and 13 of power-verification.md plus a third HISTORY entry, and amend the locked R5 row in place at state.md:99-102; re-cut and flip back all nine pins in the same commit each note demands, re-deriving WAR_EQUIPPED_DELTA and WAR_EQUIPPED_ITEMS against whatever row 14 rules for the twins; carry the live-derived consumers a re-cut moves (the practice dummy vitals in src/sim/mob/practice_dummies.ts, the server PBE boost kit, scripts/druid_balance_probe.ts, tests/rogue_dps_balance.test.ts, the list dev_bis_gear.test.ts:150-154 keeps); and if the re-measured envelope moves, re-size one or more of the four downward content tunes, which turns this into a content change carrying npm run wiki:content, npm run i18n:gen for any reworded English, the already-flagged tooltip re-reads (power-verification.md:1211-1219), non-Latin M16 fills for any reworded value, the symbol census (a census row owed only if a new exported name or content id appears), and content-obligations-reviewer plus the sim, tests and parity reviewers and qa-checklist over a gate_select and a full gate. No new item ids, so no committed art, no deed row, no Reliquary page; no IWorld facet member moves, so tests/world_api_parity.test.ts and the parity goldens stay untouched; no renderer bytes, so no Eastbrook re-mint. It needs the maintainer's own words on the three re-openings named in the Decision cell, because the phase's own stopping rule (phase-19-rulings-gate.md:81-82) makes a contradiction of a ratified R5 row an escalation rather than an execution. It also answers rows 13 and 16, which name it in their seeded text. [the largest row in the table: a measurement phase plus its execution, the shape Phase 15 and its QA twin took, state.md:21129-22068] (A2) RE-MEASURE and ADD an Ignivar Crucible profile as a third protected asset: A1's price plus a third target derivation in section 5, a third target constant in the harness beside HEROIC_TARGET and SRIFT_TARGET, a third column on every lane table, new rows in tests/r5_envelope_probe.test.ts's target-derivation arm, and every lane re-run at that target. The profile inputs already exist, so no tuning is authored: HEROIC_DUNGEON_TUNING.ignivar_inner_crucible (src/sim/content/dungeon_difficulty.ts:462-491 at 2ab5c2f7d0, difficulty heroic, level 22, armorMultiplier 1.2, finalBossId varkhul_forgefather_of_the_last_flame) and that boss's armorPerLevel 46 (src/sim/content/dungeons.ts:89-105). It adds an armour point rather than a new hit ramp: level 22 is the same level the existing HEROIC_TARGET derives from (the nythraxis_boss_arena heroic row) at the same armorMultiplier 1.2, and only the per-level armour differs (46 against nythraxis_scourge_of_thornpeak's 42 at src/sim/content/dungeons.ts:1043), so the level-driven miss ramp is untouched. Whether the raid belongs in the protected set is a maintainer design call: locked ruling 5 names "heroic raid and S-rift clear difficulty" without naming which raid, and the Ignivar tier post-dates it. [A1 plus roughly a third again on the measurement half] (B) RATIFY the record as the pre-raid measurement and retire the nine pins: a dated scope line on the Verdict plus a third HISTORY entry in power-verification.md, an amendment to section 13's tripwire table where three of the retired pins ARE its named guards (the Perfected-lead row at :1183, the pre-packet-epic baseline row at :1186, the live-derived-fixture row at :1188), and the RULED line in every home the escalation was written into, which is more than three: the eighth-sync AMENDED block inside the Phase 15 ledger (state.md:21639-21718), the Phase 17 ledger's THE R5 ESCALATION (state.md:22799-22808), the locked R5 row amended in place (state.md:99-102), progress.md (the Phase 15 row, the Phase 18 row and the R5 closure entry), and the seeded row in phase-19-rulings-gate.md. Per pin: the two r5_envelope_probe arms and the masterwrought_budget lead-cap arm are pure reproductions of R5 figures and can only retire by deletion, since re-cutting them IS measuring; the two twin rows retire into whatever row 14 rules; the two dev_bis_gear arms and the two pbe_boost arms are NOT R5 numbers and should be RE-CUT rather than deleted, because pbe_boost.test.ts:908-913 as read records that buildBoostedCharacterState hard-throws when a third flagged equip is refused (deleting the cap sweep drops a boot-time crash guard) and dev_bis_gear.test.ts:225 is the only guard that names the CAUSE when a live-derived fixture silently re-gears (its note at :150-154 lists the consumers, and state.md:22902-22903 as read already re-pinned the dummy body 1712 hp / 2941 armor to 1232 / 3265 at this merge). Every retirement rewrites the pin's escalation NOTE as well as its it.fails, because each note ends in a flip-back instruction that a ratification makes false. Two of the four re-cuts need maintainer-named replacement expectations: dev_bis_gear's exact-count arm measures zero on the merged catalog (all nine classes measure [] per the note at dev_bis_gear.test.ts:139-141), so its by-id sibling becomes an empty-set pin unless the maintainer names a re-scope; and pbe_boost's kitsAtCap 10 is recorded only as having "moved the same way the dev-bis pins did" (pbe_boost.test.ts:914-923 as read), never as a number, so its replacement count is re-derived at execution or named by the maintainer, never guessed here. The next frozen stamp then records 2 expected fails rather than 11 (the existing stamps are historical records of runs and are not edited). No probe run, no def edit, no wiki regen, no i18n fill, no art, no census row, no parity golden; the tests reviewer plus qa-checklist over one gate_select. [one commit cluster of doc edits across five homes plus nine pin edits, plus the two maintainer-named replacement expectations] (C) RATIFY NOW and DEFER the re-measure past the PR, which the phase explicitly allows as a ruling in its own right (phase-19-rulings-gate.md:54-55): option B's doc work without the pin retirement, the nine notes re-pointed at a filed follow-up issue, and the packet ships carrying nine standing expected-fail pins plus the two terrain xfails. Those nine are not red today (state.md:22907-22910 records the 11 inside a PASS run), so the cost is not a red gate now: it is that an it.fails arm which starts PASSING on a later catalog reds the suite, and these nine sit on exactly the surface the release keeps moving. Rows 13 and 16 keep pointing at an unclosed row 12, and draft row D037 (state.md:23088) keeps its stated sequencing dependency on this row. [near zero now, option A's full price later, plus the follow-up issue] | Option B, ratifying the record as the pre-raid measurement with the four non-R5 pins RE-CUT rather than deleted, is what the ledgers already lean toward: the record itself is written that way ("its figures are the measurement of the pre-raid catalog they were taken on", power-verification.md:126-127 as read), the closure a re-measure would re-open is explicitly settled by ratification rather than by a demonstrated bound (state.md:22060-22061), that closure relaxed the R5-surface stop rules "only to this exact extent" (state.md:22012-22014), and the ruling that re-opened this packet at all left the R5 rulings untouched by name (decisions-index.md:18). Ruling 2b already accepted on the record that an at-most bound cannot be verified from floors, so a fresh measurement buys a fresh floor rather than the bound R5 asks for. The two contradictions that actually fired both read as the packet sitting BELOW the merged incumbents (the chest lead measures -1 against the ratified cap of 2, and all nine classes' dev-bis loadouts carry zero flagged pieces against a flagged family the ledger still counts at 17, state.md:21672-21675 as read), which is the direction that cannot breach an at-most envelope; the lowered hit ramp on the equipped fury arm is the one thing genuinely unresolved (165 hit reads 35 OVER the merged heroic need and 25 UNDER the merged S-rift need, state.md:21649-21655 as read), and it is a floor-against-floor question 2b already ruled on. One further consideration prices option A alone, and it is why the recommendation avoids A: the catalog has moved AGAIN since the escalation, so a re-measure must run on whatever catalog is current WHEN IT RUNS and must be sequenced after the packet's final release sync or it is stale before it starts. As read 2026-08-31 from this worktree's refs: origin/release/v0.41.0 has moved past the ninth sync's e6b8edb375 to abf8fe802a, origin/release/v0.41.1 stands at 7ea2d5cfd6, and origin/release/v0.42.0 now exists at 22e909839f, whose span already touches src/sim/content/items.ts and src/sim/content/heroic_loot.ts (65 files under src/sim/ between 3e801dc925 and 22e909839f). Whichever option lands, it does not by itself answer rows 13 and 16 (A folds them; B and C leave each to be answered on its own); row 14's twins name this row as their alternative only in the pin comment (tests/masterwrought_budget.test.ts:841-842) and not in row 14's seeded text; row 15 is independent of this row (REF_ARMOR appears nowhere in scripts/r5_envelope_probe.ts or tests/r5_envelope_probe.test.ts at 2ab5c2f7d0, so no option here moves it); and the rogue-balance identity re-anchor in tests/rogue_dps_balance.test.ts stays JUDGED PENDING this same authority's confirmation (state.md:21687-21692 as read). This is a recommendation, not a ruling: if A or A2 is chosen, the target set, the three re-openings named in the Decision cell, and any re-sized tune are further maintainer-named calls, and this row records no number the maintainer has not given. | row 12 (R5 on the merged world: re-measure the record on the merged catalog, the protected-asset target set included, or ratify it as the pre-raid measurement and retire the nine expected-fail pins) |
| D141 | apex-tier-vs-crucible-placement | Where does the Masterwrought apex tier sit against the release's Crucible raid tier now that the merged catalog puts the raid above it: re-tier the apex pieces above the Crucible tier, accept the raid tier above apex and amend the packet's Power placement PROSE (rescoping the ilvl-31 crafted-band pin and R3's sub-cap wording to the masterwrought family explicitly), or fold the placement half into row 12's R5 re-measure; and, as the sub-decision, does the staged Crucible crafted fast-follow (PR 3704) inherit the masterwrought flag and the two-piece cap? | state.md, the Power placement block (lines 252-274 as read; the AMENDED stamp at 254-263 marks three lines STALE and says "The live placement is the maintainer's Phase 19 ruling (rows 12 to 16...)", with 266-267 base apex ilvl 31, 268-269 Perfected ilvl 34 "one to two points over the raid chest per slot", and 270 "Existing legendaries (ilvl 33 to 37, budgets 44 to 49) remain the untouched ceiling") and the eighth-sync AMENDED block (lines 21672-21675 as read: "the raid catalog puts a stronger incumbent in the chest slot (the R5 lead tables measure -1 against the ratified cap of 2) while out-scoring every apex piece on bestEpicGearFor's raw-stat score (all nine classes' dev-bis loadouts carry ZERO flagged pieces; the flagged family is still 17)"; lines 21696-21701 as read fold the fast-follow into this row, "nothing red today"; lines 22845-22847 as read carry the same as a RECORDED, NOT ACTED maintainer read). The R5 record carries the SAME contradiction and the same routing: docs/design/power-verification.md:108-135 as read (HISTORY second entry, the section 8 chest lead at -1 at :122-124, "NOTHING IN THIS RECORD WAS REWRITTEN" at :124-126, "the maintainer's ruling is Phase 19 decision table rows 12 to 16" at :129-131), so this row's amendment lands in TWO doc files, not one. VERIFIED AT 2ab5c2f7d0 and it narrows the row: src/sim/content/crucible_professions.ts EXISTS but ships ONLY the reagent lastflame_core (the whole file is 27 lines, kind junk, quality epic, sellValue 5000; its own header says the recipes, crafted epics and hammer "land with that PR"), wired at src/sim/data.ts:313 and :388 and dropped at src/sim/content/dungeons.ts:154-155 and :324-325, so the crafted epics, the skill-100 floors and the crafted legendary are NOT in the tree at the anchor. The ilvl arithmetic, all read at the anchor (epic bonus 6, src/sim/item_budget.ts:18-25 QUALITY_ILVL_BONUS): apex base 31 (recipe level 25 plus 6), Perfected 34 (src/sim/professions/perfecting.ts:87 PERFECTED_SOURCE_LEVEL = 28, comment "one tier over the raid chest's 27"), Crucible DROPPED epics 35 (src/sim/content/ignivar_loot.ts:38-41, IGNIVAR_RAID_LOOT_SOURCE_LEVEL 26 plus epic 6 plus raid 3), staged CRAFTED epics 37 and the hammer 39 (docs/prd/ignivar-raid-professions.md:170-178 as read, recipe levels 31 and 29, the recipe arm carrying no raid bonus). Live code anchors: src/sim/equipment_rules.ts:283 masterwroughtConflictSlot, whose line 291 returns null on any def without the flag, with MASTERWROUGHT_EQUIP_CAP = 2 and MASTERWROUGHT_LEGENDARY_CAP = 1 at :245-246; src/sim/dev/bis_gear.ts:44 bestEpicGearFor. Pins: tests/dev_bis_gear.test.ts:155 and :225 are it.fails, and the block at :135-154 names THIS ruling and the same-commit flip rule ("re-tier the apex pieces above the Crucible tier, or amend the power placement and re-derive these pins; Phase 19 decision table"), with the count pin an EXACT toBe(2) at :168; tests/server/pbe_boost.test.ts:924 and :947 are it.fails on the same cause, but their note at :921 names "Phase 19 decision table rows 12 and 13", so that pair's flip is JOINT with row 12; tests/masterwrought_budget.test.ts:1587 is the R5 lead-cap arm, it.fails, and its own note at :1575-1586 escalates it as the re-measure-or-ratify question, which is row 12's, not this row's; the ilvl-31 crafted-band pin at tests/masterwrought_budget.test.ts:1742-1775 is GREEN today and is what PR 3704 reds, on THREE assertions, not two (the filter at :1763-1764 admits any recipe output at ilvl 31 or above that is not flagged and pushes offenders at :1765; the non-vacuity literal at :1772 pins atBand.length exactly 17; the membership sweep at :1773 requires every at-band recipe output to be an apex-array output; the offenders assertion is at :1774). R3's wording is state.md:94-96 as read, "Sub-cap: at most ONE legendary-quality crafted piece equipped, inside the global cap", which reads on ANY crafted legendary while the code reads only the flag. The rogue identity re-anchor (tests/rogue_dps_balance.test.ts:75-76, pinned to heartspring_amulet and circle_of_cinders) is recorded JUDGED PENDING this same authority (state.md:21686-21691 as read). | Option 1, ACCEPT the raid tier above apex and amend the placement PROSE, paired with sub-decision S1: rewrite the three stale relative-position claims in the Power placement block (state.md:266-270 as read) as the ruled placement instead of a staleness pointer, leaving every def value untouched so the frozen R5 surface is not re-cut; stamp the same ruling on the R5 record's HISTORY entry (docs/design/power-verification.md:108-135 as read), which today routes the identical contradiction to rows 12 to 16, spelling any packet R-number written there as "masterwrought R" plus the number IN FULL per the naming rule at state.md:245-250 as read; rescope R3's sub-cap sentence (state.md:94-96) to say masterwrought-flagged explicitly so it stops reading on the fast-follow's crafted legendary; rescope the ilvl-31 crafted-band pin (tests/masterwrought_budget.test.ts:1742-1775) so ALL THREE of its assertions key on the masterwrought family rather than on every recipe at ilvl 31 or above (the offender filter at :1763-1764, the exact atBand literal 17 at :1772, and the apex-array membership sweep at :1773), and the replacement scoping is a maintainer-named shape (family-keyed band, or a named Crucible carve-out on the ignivar_loot precedent); re-derive and flip back to it() the four premise pins whose own comments name this ruling (tests/dev_bis_gear.test.ts:155 and :225, tests/server/pbe_boost.test.ts:924 and :947), where "re-derive" means re-authoring exact literals against the merged measurement (the record measures ZERO flagged picks on all nine classes against the :168 pin's exact 2), so the replacement pin SHAPE is a maintainer-named call (pin the new exact count, or re-scope the arm to at most the cap), and the pbe_boost pair waits on row 12 because its own note names both rows; leave tests/masterwrought_budget.test.ts:1587 expected-fail because its note assigns the re-cut to the re-measure question; and record the ruling where the row stands per the Phase 19 STEP 2 rule (a dated RULED line with its qr-19 ruling id at state.md:21696-21701 and :22845-22847 as read, plus the Phase 19 ledger) [about a day: TWO doc files (state.md in two blocks, docs/design/power-verification.md's HISTORY entry) and four test files; NO content def, recipe level or item id moves, so NO wiki regen, NO deed or Reliquary rows, NO committed WebP art, NO M16 non-Latin fill, NO census row and NO parity golden; reviewers per the Review Dispatch Matrix (implementation-plan.md:144-155 as read) are test-coverage-auditor for the four re-derived pins and qa-checklist last, and architecture-reviewer is NOT triggered, the matrix skipping it for pure data and test changes; the doc edits carry the anchor rule, and the amendment must stay PROSE-only, since the Phase 19 stopping rule makes any ruling that moves a ratified R5 number an escalation rather than an execution]. Option 2, RE-TIER the apex pieces above the Crucible tier: needs a maintainer-named source level, because clearing the DROPPED Crucible epics at ilvl 35 needs PERFECTED_SOURCE_LEVEL (src/sim/professions/perfecting.ts:87) at 30 or higher (30 plus epic 6 is 36) and clearing the STAGED crafted epics at 37 needs 32 or higher (38); whether the base apex recipe level 25 moves with it is a SECOND maintainer call, and tests/perfecting.test.ts:600 as read pins recipe.level toBe(25) so moving it reds there; at 32 the Perfected pieces land at ilvl 38, ABOVE state.md:270's "existing legendaries (ilvl 33 to 37) remain the untouched ceiling" line, so that premise needs its own maintainer amendment [multi-day AND blocked: this is a re-cut of the frozen R5 surface, which the packet rule forbids until row 12 rules a re-measure, so it is reachable only after row 12; then the 17-def flagged family re-authors across src/sim/content/profession_items.ts and src/sim/content/items.ts (the roster is enumerated by the equality at tests/perfecting.test.ts:586 as read), the armorRowSweep rows (which pin recipe.level and itemLevelBudget at 25, tests/masterwrought_budget.test.ts:924-925 as read) and both per-slot lead tables in that file re-pin, the per-slot delta literals at tests/perfecting.test.ts:561-575 re-derive off the new constant at :606-608, power-verification.md sections 8, 9 and 13 re-measure through the committed scripts/r5_envelope_probe.ts harness with the 5 percent envelope re-proven; npm run wiki:content runs and its freshness gate (tests/guide.test.ts) must pass, though src/guide/content.generated.ts carries no itemLevel key at the anchor (0 hits as read; its recipe rows carry name, skillReq, tier, station and materials), so a stat-only re-tier may regen to an empty diff and the obligation is the RUN, not an assumed content change; no new id is coined, so no deed row, no Reliquary page, no committed WebP art and no M16 fill, but the deed and Reliquary pins still re-verify; the tooltip-screenshot question recurs on the changed stat numbers, against the section 14 precedent at docs/design/power-verification.md:1211-1219 as read (a changed stat NUMBER inside an already-screenshotted layout was judged below the bar and recorded so a reviewer can overrule it); reviewers architecture-reviewer (a src/sim/ constant moves), content-obligations-reviewer, test-coverage-auditor, qa-checklist; and it deliberately outbids the release's own maintainer-fixed Crucible scope, docs/prd/ignivar-raid-professions.md:14-31 as read fixing crafted ilvl 37 as "two above the dropped tier", so it re-opens another packet's settled scope from inside this one]. Option 3, FOLD the placement half into row 12's re-measure [zero work here, but it answers only half: the fast-follow flag-and-cap question is a family-doctrine call on src/sim/equipment_rules.ts:291 and not a measurement, so folding leaves it unruled, and it leaves the ilvl-31 pin scheduled to red at PR 3704 with no recorded owner]. Sub-decision S1, the fast-follow stays OUTSIDE the masterwrought family (no flag) and R3's wording is rescoped to match [near zero, and it is what the code already does: masterwroughtConflictSlot returns null on an unflagged def at src/sim/equipment_rules.ts:291, so only the English at state.md:94-96 is loose; priced inside option 1]. Sub-decision S2, the fast-follow INHERITS the flag and both caps [at most FOUR wearable defs on PR 3704's branch could carry it, not five: the staged outputs are a mail helm, leather legs and a cloth robe at ilvl 37, a proc weapon ENCHANT, and the legendary hammer (docs/prd/ignivar-raid-professions.md:151-157 as read), and an enchant has no equip slot for masterwroughtConflictSlot to count; flagging any def outside the packet's own apex arrays reds three sweeps that walk EVERY flagged def in ITEMS, the family census equality at tests/masterwrought_budget.test.ts:782-794 as read (flagged ids compared against FLAGGED_TABLE_IDS, built from the packet's APEX_ tables at :602-607), the perfecting roster equality at tests/perfecting.test.ts:586 as read with its craft and apex-recipe resolution at :591-594 and its recipe.level toBe(25) at :600, and the at-band literal at tests/masterwrought_budget.test.ts:1772; the crafted Crucible legendary would land inside R3's one-legendary sub-cap so it cannot be worn beside a promoted apex piece, the "Unique-Equipped: Masterwrought (2)" tag (state.md:277 as read) starts appearing on another packet's items, the tier's whole wearability story re-prices, and it needs cross-branch coordination with PR 3704 plus a design call this packet would be making on another packet's content]. Sub-decision S3, record a binding instruction and defer the flag call to PR 3704 [near zero here, two doc lines (the Phase 19 ledger and docs/prd/ignivar-raid-professions.md's open-questions list at :234-242 as read, which today carries no flag question), but it names an owner without doing the rescope, so PR 3704 inherits a red pin]. | Option 1 with S1, deferring the R5 lead-cap arm and, with it, the pbe_boost pair's flip to row 12. The ledgers already lean this way: the eighth sync stamped the three placement lines STALE rather than re-tuning anything (state.md:254-263 as read), its closing sentence is "what moved is the CATALOG under the measurement, not any packet def or record row" (state.md:21717-21718 as read), and the R5 record says the same of itself, "NOTHING IN THIS RECORD WAS REWRITTEN" (docs/design/power-verification.md:124-126 as read). Option 2 is doubly blocked, by the frozen R5 surface and by the fact that the Crucible numbers are the maintainer's own fixed scope decisions (docs/prd/ignivar-raid-professions.md:14-31 as read), so the packet would be re-opening another packet's settled design to win a comparison. S1 is the reading the code already implements, so option 1 makes the packet's English agree with masterwroughtConflictSlot instead of changing behavior, and it discharges the one thing that will actually break: the ilvl-31 pin, which is green today only because PR 3704 has not landed. This is a recommendation, not a ruling; the -1 chest lead, the zero flagged dev-bis picks and the family count of 17 are quoted from the frozen record and were not re-measured, and both replacement pin shapes (the band pin's scoping and the dev-bis count) stay maintainer-named. AMENDED IN PLACE 2026-09-01 AT EXECUTION (Phase 19D, ruling qr-19-apex-tier-vs-crucible-placement), addresses only, no premise re-ruled: src/sim/content/crucible_professions.ts is 32 lines and ships a second export (CRUCIBLE_RECIPE_PENDING_MATERIAL_ITEM_IDS) beside lastflame_core, not 27 lines with one def; the Power placement block reads at state.md:280-302 with its AMENDED stamp at :282-291 and the legendaries line at :298, not 252-274 / 254-263 / 270; the eighth-sync AMENDED block reads at :22905 and the fast-follow fold at :22945, not 21672-21675 and 21696-21701, and both are mid-sentence continuation lines, so the dated lines land at the block's real end (:22965); the three assertions PR 3704 reds are tests/masterwrought_budget.test.ts:1772, :1773 and :1774 (the filter at :1762-1764 feeds :1774 rather than being an assertion); and docs/design/power-verification.md's sealed sentence cannot be grepped whole (NOTHING closes :124), the matchable form being 'IN THIS RECORD WAS REWRITTEN' at :125. BOTH maintainer-named shapes were answered in session 2026-09-01: the band pin takes a NAMED Crucible carve-out (not a family re-key, which would remove the unflagged-band-reacher hole the arm exists to close), and the dev-bis count becomes the invariant (at most the cap) plus a dated ZERO observation. Measured at execution: all nine classes carry zero flagged picks, the flagged family is 17, and all 17 at-band crafted outputs are flagged. | row 13 (the whole seeded row: the apex-versus-Crucible placement and its crafted fast-follow flag-and-cap sub-decision; the R5 lead-cap re-cut it shares with row 12 stays with row 12, as does the pbe_boost pair whose note names both rows, and the rogue identity re-anchor at tests/rogue_dps_balance.test.ts:75-76 rides this row's authority) |
| D142 | twin-complement-vs-retuned-references | Does the apex complement rule still bind against a reference drop the release retuned underneath it? Rule one of: re-cut forgefold_legguards and spiritweld_girdle off crit 40 so they complement bloodmane_war_legguards and gravescale_girdle again (which also requires a maintainer-named replacement rating field per piece), amend the rule so it binds at AUTHORING time against the then-current reference with these two carried as a dated merge-inherited exemption, or hold the whole question inside row 12's re-measure-or-ratify. | phase-19-rulings-gate.md:30 (seeded row 14, as read). The finding is state.md:21669-21671 as read, "the retuned references make the apex twins forgefold_legguards and spiritweld_girdle DUPLICATE their reference drop's rating (crit 40 both sides) instead of complementing it", with its treatment at state.md:21675-21686 as read (nine pins kept byte-identical under it.fails "per the packet rule that a finding contradicting a ratified row is an escalation and never a re-tune", each carrying the flip-back rule). LIVE CODE at 2ab5c2f7d0: tests/masterwrought_budget.test.ts:MERGE_INHERITED_TWIN_DUPLICATES (:844 as read) under its escalation comment (:830-843 as read: "re-cutting either rating is a re-tune of the R5 surface, which the packet may not do ... the question goes to the maintainer"), the complement arm inside armorRowSweep (:885-890 as read, asserting the reference drop does not carry the apex piece's rating field), the two expected-fail rows plus the set-liveness pin (:934-945 as read), the APEX_ARMOR rating tuples for both twins (:87 and :98 as read, both critRating 40), and a SECOND, still-GREEN arm the Phase 18 treatment note never names, "the rating relationship to each reference is STATED, duplicate or complement" (:1509-1538 as read), which carries the four deliberate cross-family duplicates as literals (:1526-1535) precisely so that, in its own words (:1510-1518 as read), a reader can tell "an intentional duplicate from a missing arm"; the defs src/sim/content/profession_items.ts:spiritweld_girdle (:1535-1547 as read) and :forgefold_legguards (:1548-1560 as read), both critRating 40, under the rule text in their block comment (:1519-1534 as read, "each rating chosen to COMPLEMENT the same-slot drop rather than duplicate it"), restated in the ledger at state.md:2806-2808 as read ("The complement is now MECHANICAL (the sweep asserts the reference drop does not carry the apex piece's rating field)"); the neighbouring state.md:2701-2706 as read states only the one-rating-at-40 band law and NOT the complement, so it is not a second home for the rule; the references src/sim/content/heroic_loot.ts:gravescale_girdle (:359-371 as read) and :bloodmane_war_legguards (:522-534 as read), both now critRating: ARMOR_RATING (40, heroic_loot.ts:45 as read), which leaves hit and haste as the only free fields on either twin. VERIFIED asymmetry between the two twins: forgefold is stat-and-armour identical to bloodmane (armor 315, str 11, sta 9), so on the merged catalog it duplicates its reference on EVERY combat field, while spiritweld (int 9, spi 6) differs from gravescale (str 9, sta 6) and duplicates only the rating. Relationship to D037 (state.md:23088 as read): D037 asks the primary-stat silhouette question over the five pieces numerically identical to their references, this row asks the rating question over the two whose references were retuned. They intersect on exactly one piece, forgefold_legguards, where on the merged catalog the two findings compose into full numeric identity with bloodmane_war_legguards (only access, class-freedom, tradability under R2, sellValue and the masterwrought flag differ); spiritweld_girdle is in this row's set only. Neither row subsumes the other and neither answer implies the other, which is what D037's "coordinate with rows 12 and 14, does not duplicate row 14's twin question" means in practice. | Amend the complement rule to bind at authoring time and ratify crit 40 on both twins as a dated merge-inherited exemption [rewrite the complement arm at tests/masterwrought_budget.test.ts:885-890 into an exemption-aware arm keyed by id and dated, return both rows to it() and retire MERGE_INHERITED_TWIN_DUPLICATES, keeping the equivalent of its set-liveness pin at :934-945 so the exemption cannot outlive its ids; ALSO state the two twins as deliberate duplicates in the still-green :1509-1538 arm beside its four existing literals, which that arm's own stated reason demands and which is easy to miss because it is not red today; amend the rule text in src/sim/content/profession_items.ts:1519-1534 and the ledger's mechanical-complement sentence at state.md:2806-2808, plus the dated RULED line; NO def edit, so the R5 surface, section 9.6's table and the committed probe stay untouched and none of the other seven expected-fail pins move; wiki regen runs and is a no-op for this field (2ab5c2f7d0:src/guide/content.generated.ts carries zero critRating and zero hasteRating keys, counted); no id moves, so no committed WebP art, no M16 non-Latin fill, no deed or Reliquary row, no shipped-id golden change (tests/shipped_item_ids.golden.json:346 and :981 as read), no parity golden, and no census row (scripts/catalog_program_census.mjs carries no rating term, checked); reviewers: qa-checklist per the packet's Review Dispatch Matrix (implementation-plan.md:154 as read) plus content-obligations-reviewer under the repo-wide content-diff rule (.claude/agents/content-obligations-reviewer.md), which is NOT itself a row of that matrix (implementation-plan.md:146-154 as read, whose rows also skip architecture-reviewer for pure data and test changes); the honest cost is a weaker live arm, since an exemption set only holds while the discipline that keeps it closed holds]; Re-complement BOTH twins onto a maintainer-named free rating field [two one-field def edits at src/sim/content/profession_items.ts:1544 and :1557 plus their APEX_ARMOR tuples at tests/masterwrought_budget.test.ts:87 and :98 and the it.fails flip-back at :934-945; only hit and haste are free, and hit is effectively unavailable unless the maintainer also reverses Phase 15's zero-Hit-on-armour law, because the live green spread arm at :1204-1229 as read pins armour at hit 0 crit 5 haste 4 and the whole family union at hit 4 crit 6 haste 7, so BOTH literals re-cut on any pick (both twins to haste reads armour crit 3 haste 6 and union crit 4 haste 9) and a hit pick additionally contradicts docs/design/power-verification.md section 10.2 (:888 as read, sunspun_vestments Hit 40 to Haste 40, on the ground that hit converts at twice the rate of crit and haste) and the never-Hit clause at state.md:2690-2700 as read; on a haste pick spiritweld lands on the same int/spi plus haste shape as wardspeaker_sabatons one slot away, which no arm forbids (the twin-shape arm at :1539-1548 as read is scoped to ilvl-31 jewelry) but which is a differentiation call only the maintainer can make; and the re-cut IS the R5 surface: power-verification.md:816-823 as read prints forgefold as the crit twin, :835-842 as read measures +10.42 and +10.21 on exactly that swap, tests/r5_envelope_probe.test.ts:105-147 as read pins BOTH sides of the pair (bloodmane hit 40 at :143, forgefold crit 40 at :145), so a re-cut breaks a second assertion in an arm the release already broke rather than greening it, and the harness prose at scripts/r5_envelope_probe.ts:539-565 and :802-818 as read (already stale since the retune, since it still calls the baseline legs the hit side) goes stale a second way, so this option owes a section 9.6 amendment and a re-measure that only row 12 can authorise; server side, a haste pick is a wash (server/pbe_boost.ts:roleItemScore:318 as read adds critRating and hasteRating at the same RATING_WEIGHT and ignores hitRating) while a hit pick both re-derives the 30.67 and 32.47 figures recorded in the green tank tripwire's comment at tests/server/pbe_boost.test.ts:1075-1090 as read (its arms compare scores, so they stay green either way) AND moves the score-ordered premise of the EXPECTED-FAIL demotion-fallback arm at :947-981 as read, whose kept-pair literals at :965 name spiritweld_girdle and are re-derived at :970-978, so that pin's owed flip-back changes shape too; the tooltip rating line changes, below the screenshot bar on the packet's own precedent at power-verification.md:1211-1219 as read; wiki regen no-op and the same id-level obligations as option 1, all absent; same reviewer set as option 1, and the R5 record amendment rides the same commit]; Split the ruling, holding forgefold_legguards under row 12 and re-complementing spiritweld_girdle alone [one def edit at profession_items.ts:1544, its tuple at masterwrought_budget.test.ts:87, one it.fails flip-back, and on a haste pick the spread literals re-cut to armour crit 4 haste 5 and union crit 5 haste 8; cheapest of the re-cuts and it leaves the measured piece alone, but it asks the maintainer to rule that spiritweld is NOT R5 surface, which the record has not said (power-verification.md:104-106 and :829-832 as read name it as the not-taken item-swap arm's unmeasured class-free alternate, :760-766 as read names it inside the section 9.5 tank-kit reasoning, and section 14 at :1235-1240 as read is a standing recorded read that ends "spiritweld_girdle is correct on its own budget and must not be nerfed for it"), and it leaves one expected-fail row plus the set-liveness pin standing, so the escalation stays half open until row 12 answers]; Fold the row entirely into row 12's re-measure-or-ratify [no-op now: the two expected-fail rows, the escalation comment and the flip-back rule stay exactly as authored, and row 12's ruling then carries the rating choice as a sub-decision; the cost is that the packet's PR ships with two knowingly red-marked apex rows and the rule question unanswered in its own right] | Fold into row 12 for the mechanical answer and, if row 12 ratifies the record as the pre-raid measurement, take the authoring-time rule amendment in that same commit: the packet's own standing rule is that a finding contradicting a ratified row is an escalation and never a re-tune (state.md:21675-21677 as read), the pin's own comment already reads that re-cutting either rating is a re-tune of the R5 surface the packet may not do (tests/masterwrought_budget.test.ts:835-838 as read), and a re-complement does not green what it appears to fix, because the section 9.6 arm pins the reference's hit line as well as the twin's crit line and would break one more assertion after the edit. If row 12 instead orders the re-measure, the twin ratings should ride that same re-measure so both fields are chosen against the merged catalog and measured once, with the maintainer naming the field per piece there and the zero-Hit-on-armour law ruled explicitly rather than moved by side effect. | row 14 (the twin complement rule against the retuned references, phase-19-rulings-gate.md:30; overlaps D037 on forgefold_legguards only and only on the rating axis, and does not re-ask D037's five-piece silhouette question) |
| D143 | ref-armor-calibration-constant | Does the difficulty model keep `REF_ARMOR` pinned at 2861 now that the committed max-armour prot kit pins at 4085 (a gap the merge record calls "over a thousand points", where the pre-raid gap was about a hundred: 2969 minus 2861 on the re-pin note's own literals), recorded as the model's stated conservatism, or does it re-base onto a maintainer-named reference tank, and if it re-bases, does the re-derivation raise the multiplier ladders or lower the floor literals? A second question rides inside it that no record settles: whether 2861 was ever the RAW kit armour or the mastery-folded figure the four suite headers describe, because the two readings price the same gap differently. | docs/design/power-verification.md:1241-1248 as read (section 14, "Recorded, not acted on"): "`REF_ARMOR = 2861` is a pinned calibration constant, not a live property of the catalog ... Raising it would move every difficulty floor, so this phase pinned the claim that protects the model instead", with that bullet's own "several hundred points above it" (line 1245 as read) now stale against the merged catalog. tests/heroic_difficulty_floors.test.ts:329-336 as read, the re-pin note: "Re-pinned 2969 -> 4085 at the merge of release/v0.41.0 (tip 3e801dc925, 2026-08-30) ... the calibration gap between REF_ARMOR (2861) and the live kit therefore widened from about a hundred points to over a thousand: a maintainer decision on the constant (the packet's Phase 19 table), never a re-tune here", with the live literals at :337 (`expect(a.stats.armor).toBe(4085)`) and :342 (`expect(a.maxHp).toBe(1582)`). Same read at docs/prd/masterwrought/state.md:21324-21328 and :21691-21695 as read, the second adding that the widened gap "sharpens the section 14 REF_ARMOR read". THE BASIS IS NOT SETTLED: the suite headers call 2861 the max-armor kit with "prot mastery" folded in (tests/heroic_difficulty_floors.test.ts:10-14, tests/rift_difficulty_floors.test.ts:6-11, src/sim/content/dungeon_difficulty.ts:64-67 as read), while the pin comment says the 4085 and 1582 literals are "the raw kit numbers, without the prot mastery the header's derivation folds in" (tests/heroic_difficulty_floors.test.ts:323-328 as read) and they come from `characterDerivedStats('warrior', 20, withFlagged)` at :289, which takes no spec mods; prot mastery RAISES armour (src/sim/content/talents_warrior.ts:25 Recompense, `armorPct: 0.10` plus `armorFromStrPct: 0.70`, applied at src/sim/entity.ts:514-517 as read), and R5's own prot-spec tank baseline already reads 3369 armour on a non-max-armour kit (docs/design/power-verification.md:773 as read, in section 9.5, the probe calling `setSpec('prot')` at scripts/r5_envelope_probe.ts:877), so 2861 cannot be a mastery-folded reading of a catalog whose raw max-armour kit measured 2969. LIVE code anchors at 2ab5c2f7d0: src/sim/types.ts:8540 `armorReduction` (min(0.75, a/(a + 85*level + 400))); the literal is TEST-side in four suites (tests/heroic_difficulty_floors.test.ts:33, tests/rift_difficulty_floors.test.ts:48, tests/gravewyrm_normal_tuning.test.ts:37, tests/wildheart_normal_tuning.test.ts:38) and is quoted as live fact in ELEVEN further places: src/sim/content/dungeon_difficulty.ts:66, :121 and :211, src/sim/rift/ranks.ts:101 (which also states the derived "~39.8%"), src/sim/entity.ts:502 (the bear-parity rationale, "~1700-2100 armor vs the warrior's 2861"), scripts/healing_montecarlo.ts:223, docs/healing-monte-carlo-analysis.md:264, :341 and :425, docs/rift-rank-monte-carlo-analysis.md:12, and docs/prd/wildheart-basin-open-field-dungeon.md:275 (all as read). Arithmetic from the committed formula, not an R5 re-measure: the armour step passes 44.24 percent at 2861 and 35.72 percent at 4085 against the level-22 heroic pin, so post-armour MELEE falls about 19 percent (19.8 at level 20, 19.0 at the S-rank level 23) and holding a melee floor needs about 24 percent more mob melee, while the "~39.8%" the comments quote would read about 32.1 percent. Mechanic and ranged damage do NOT move with the constant: mechanic damage "lands RAW (no armor step)" (src/sim/content/dungeon_difficulty.ts:76-77 as read) and a petSpell caster's rolled damage "is unmitigated by armor, so its floor is measured on the raw hit" (:107-108 as read, wildheart's RANGED_FLOOR 100 at tests/wildheart_normal_tuning.test.ts:45-47), but mechanicDamageMult DEFAULTS to the mob's own melee factor (:87-91 and :97-100 as read), so raising melee drags avoidable mechanics with it unless a per-mob override is added. Both ladders solved their multipliers AT the floor (src/sim/content/dungeon_difficulty.ts:68-70 "Solving the 500 floor at each dungeon's WEAKEST spawn-list mob"; src/sim/rift/ranks.ts:102-104 "solved at the WEAKEST template of each class"), so margins are minimal by construction and the recorded normal-mode trash margin is 3 points on a 100 floor (dungeon_difficulty.ts:122 "at least 100 (trash, lands 103+)"). Phase 18 scoped the comment half (docs/prd/masterwrought/phase-18-perfection-sweep.md:109, "REF_ARMOR's stale calibration comment states its provenance"), but its U-DOCS commit 5e1670e829 touched none of those files (its 32-file stat read) and no Phase 18 ledger line records it landing, so that debt is owed whichever way this rules. | (A) Keep 2861, record the widened gap as the model's stated conservatism, and pay only the owed provenance debt [a dated RULED line where the read stands, plus a dated AMENDED line APPENDED under the section 14 bullet in that section's own house style (the ACTIONED and RE-OPENED precedents at docs/design/power-verification.md:1212 and :1233 as read), never a rewrite of the sealed "several hundred points" sentence, and the same dated line at state.md:21324; the provenance sentence then added to the fifteen quoting sites cell 4 lists (four suite constants and headers, three dungeon_difficulty comments, rift/ranks, entity.ts, healing_montecarlo.ts, and the four docs). Comment and doc only: tsc, tests/architecture.test.ts, biome on the touched files through npm run ci:changed; no suite re-records, no content record moves, no wiki regen (verified: scripts/wiki/build_content.mjs emits no difficulty or mob-damage surface, no src/guide module imports either tuning table, and the guide's rift entries are qualitative at src/ui/i18n.catalog/guide.ts:1067-1076 as read), no deed or reliquary pin, no item art, no M16 non-Latin fill, no census row, no server or headless surface (neither imports either table as read), no monolith ceiling (none of the touched files is pinned in tests/monolith_budget.test.ts as read). Reviewers: a fresh reader on the wording plus qa-checklist, per this phase's own STEP 3. The drift detector the model needs is already committed as a literal with a named cause at tests/heroic_difficulty_floors.test.ts:337, not re-run here]. (B) Raise the constant to the live 4085 and HOLD the floors, re-solving the melee ladders upward [a live gameplay tune, not a test edit: the four test literals, then HEROIC_DUNGEON_TUNING (damageMultiplier, addDamageMultiplier, damageMultiplierByMob) and NORMAL_DUNGEON_TUNING's damageMultiplierByMob in src/sim/content/dungeon_difficulty.ts plus RIFT_NORMAL_TUNING and RIFT_HEROIC_TUNING in src/sim/rift/ranks.ts, re-solved about 24 percent up at the weakest template of every band; the mechanic and ranged maps do not re-solve on armour, but every mob with no mechanicDamageMultiplierByMob override inherits the same 24 percent on its aoePulse, stomp and channel damage, so the maintainer must name whether avoidable mechanics ride up or are held by new overrides; the four floor suites re-record every literal and the neighbours reading the same tables come with them (22 test files import src/sim/content/dungeon_difficulty and 10 import src/sim/rift/ranks as read, among them tests/dungeon_difficulty.test.ts, tests/dungeons.test.ts, tests/wildheart.test.ts, tests/boss_add_leash.test.ts, tests/rift_rank_tuning.test.ts, tests/rift_portals.test.ts, tests/rift_race.test.ts, the release's tests/ignivar_raid_trash_tuning.test.ts and its ignivar siblings, and tests/parity/coverage_c.test.ts with tests/parity/scenarios.ts, whose recorded scenarios kill heroic and raid bosses so their goldens move when a tuned swing does); the exact red set cannot be enumerated without a run, and budgeting that run is part of the price; all fifteen prose sites re-true including every "~39.8%", and the two Monte Carlo analysis docs are benches measured on the old reference, so they either re-run or gain a dated stale line; every real tank eats the raise, including the audience the normal tuning names on the record (freshly capped groups at 1439 to 2361 armour, tests/gravewyrm_normal_tuning.test.ts:39-55 as read); and since heroic Nythraxis and S rift are R5's two protected assets with clear difficulty AS the protected asset (docs/design/power-verification.md:16 and :406-410 as read) and the frozen harness itself imports both tuning tables (tests/r5_envelope_probe.test.ts:50 and :55, scripts/r5_envelope_probe.ts:51 and :56 as read), this is an R5 escalation under the packet's own stopping rule and can only ride seeded row 12's re-measure, never execute beside it; reviewers architecture-reviewer, test-coverage-auditor, content-obligations-reviewer, then qa-checklist]. (C) Raise the constant and LOWER the armour-mitigated floor literals instead, leaving every live multiplier alone [cheapest arithmetic, no mob gets stronger, and the armour-independent targets stay exactly where they are (wildheart's RANGED_FLOOR and every mechanic floor), but it restates the model's contract rather than its numbers, so the maintainer must name the new intent sentence for what "at least 500 on the maximum-mitigation reference" promises, and must name the reference before the arithmetic has an input. On the 4085 reading the melee floors scale by about 0.81 (500 to about 404, the 150 summoned-add floor to about 121, the 250 raid add floor to about 202, normal 100/200/50 to about 80/161/40, wildheart's 150 miniboss band to about 121, the rift C to S melee targets in proportion, for example S trash 665 to about 539 at the level-23 ratio); those are DERIVED here, not proposed values. Same four suites re-record, same fifteen prose sites re-true, and the same R5 read applies, because restating the contract on the two protected assets touches the protected surface even when nothing in the world moves]. (D) Stop pinning a literal and derive the reference from the catalog at a maintainer-named tier [replaces the four literals with one shared derivation behind a small test-support module with its own unit test (module-first, and it can never go stale again), but the maintainer must name BOTH the tier and the basis question cell 4 raises (raw kit or mastery-folded), and the packet already carries four different tanks: 2861 as pinned, the pre-raid raw ceiling 2969 (about a 2 percent cut at the level-22 pin), R5's prot-spec tank baseline 3369 (about a 9 percent cut, and itself inside a merge-inherited expected-fail pin at tests/r5_envelope_probe.test.ts:158-163 as read, so it is not usable until seeded row 12 settles), and the raid raw ceiling 4085 (about a 19 percent cut). The value is a maintainer-named value, and the re-solve cost is option B's or option C's at whatever magnitude the named tier resolves to]. | Option A, keep the constant and record the conservatism, with the owed provenance debt executed here as appended dated lines: section 14 already chose to pin the claim that protects the model "instead" of raising the constant, both ladders were solved AT their floors so any raise is a live difficulty change to two R5 protected assets rather than a calibration tidy, and the packet's own stopping rule sends that to seeded row 12 rather than to this gate. The counterweight belongs on the record beside it: the merge note routes the constant here and says the widened gap "sharpens the section 14 REF_ARMOR read" (state.md:21694-21695 as read), which leans toward acting rather than keeping, and the basis finding above means the recorded gap may not even be measured on one basis. So if the maintainer wants the gap closed, the packet's voice says fold it into row 12's re-measure, where the reference tank and the protected assets are settled by one authority in one pass, and answer the basis question and option D's tier question first, since 2861, 2969, 3369 and 4085 are four different tanks on at least two different bases. | row 15 (REF_ARMOR (2861) against the raid-era live max-armour kit; the seed's two named arms are options A and B, with C and D added because the seed's "raise the calibration constant and re-derive every difficulty floor" arm splits into raising the multipliers or lowering the floors, and because the constant's replacement value and its basis are themselves maintainer-named) |
| D144 | crucible-gear-sundering-admission | Should the 186 Crucible (Ignivar) GEAR epics keep feeding Sundered Essence the way every raid gear epic does (the tier scope the seed calls R1's "of the tier"), should the feedstock be cut by tier, should the widened feedstock instead be priced at the Perfecting bench, or does the question fold into row 12's re-measure? The seed's NON-gear half (the fifteen sigils, the lastflame core) is closed in code and pinned, and measurement confirms it, so that half wants ratification, not a decision. | state.md:21701-21706 (as read), the eighth-sync AMENDED block: "the same span's raid-sourced epic NON-gear (the fifteen soulbound sigils, kind tool, and the lastflame core, kind junk) silently widened the sundering feedstock, closed in-repo under the code's own gear-only doctrine (isSunderable is now an explicit gear allowlist, pinned) with the Crucible GEAR tier's essence question carried as row 16"; the seed itself at phase-19-rulings-gate.md:32 (as read), the draft heading at state.md:23040, and D038's seed cell at state.md:23089 reserving this question ("new (seeded row 16 is the distinct Crucible gear-tier sundering admission)"). The "of the tier" phrase lives in the naming registry at state.md:279 ("Sundered Essence (bound, from breaking down any raid epic of the tier)"), the essence def comment at src/sim/content/items.ts:3003-3004, and the sundering docblock at sundering.ts:47-48 ("the settled any-raid-epic-of-the-tier model"); R1 itself (state.md:86-88) names the ingredient and is the one attempt ingredient it leaves without a count. LIVE CODE (all reads at 2ab5c2f7d0, the working tree being mid-merge): src/sim/professions/sundering.ts:71-78 isSunderable (quality epic AND SUNDERABLE_GEAR_KINDS weapon/armor/held_offhand at :66-70 AND itemFromRaid or itemFromHeroicRaid), whose docblock at :62-65 defers this question to row 16 verbatim; src/sim/item_level.ts:240 registers every IGNIVAR_LOOT_ITEM_IDS id at IGNIVAR_RAID_LOOT_SOURCE_LEVEL 26 with raid true, and :236-238 records that the sigil-redeemed set pieces "never appear on a mob loot table at all"; the only other call site is the client menu at src/ui/bag_item_context_menu.ts:91 (imported at :22), so sim and UI share one predicate. PINS: tests/masterwrought_materials.test.ts:964-999, with :971-977 refusing sigil_anvil_helmet and lastflame_core on pinned epic-and-raid-sourced premises, :978-982 admitting grovespring_helmet and naming this row, :985-988 the whole-catalog gear sweep plus its greater-than-20 non-vacuity floor; the suite's own RAID_EPIC fixture (:44) is crownforged_dreadhelm, a pre-Crucible id. MEASURED, run at 2ab5c2f7d0 rather than read (isSunderable over ITEMS, plus itemLevel and itemSourceLevel): 217 sunderable ids, ALL gear, zero tool and zero junk, all 15 sigils and lastflame_core refused; 186 are Crucible (175 armor, 9 weapon, 2 held_offhand) against 31 pre-Crucible; the item-level split is exact (14 at 29, 17 at 33, 186 at 35) and the source-level split is 14 at 20, 17 at 27, 186 at 26; 145 of 145 CRUCIBLE_VENDOR_STOCK offers (ignivar_loot.ts:3312 onward) are sunderable, so a sigil that cannot itself be sundered buys a set piece that can. THE RELEASE DOES NOT MOVE THIS: c173606bbb keeps lastflame_core kind 'junk' (crucible_professions.ts:25-31 there) and only routes the id into MATERIAL_ITEM_IDS through CRUCIBLE_RECIPE_PENDING_MATERIAL_ITEM_IDS (:15), so the allowlist refusal and the kind premise at tests/masterwrought_materials.test.ts:977 both survive, and it strengthens the doctrine (sundering never takes materials); c173606bbb is NOT an ancestor of HEAD 2ab5c2f7d0 but IS an ancestor of the in-flight MERGE_HEAD 22e909839f, where sundering.ts does not exist (packet-owned), the ignivar kind census is identical (175/9/2/15), crucible_professions.ts still holds one def, and the span's ONE raid-table addition is varkhul_emberward on Heroic Varkhul's shield partition at 3 percent, quality 'legendary' (ignivar_drops.ts at 22e909839f), which the epic clause refuses: the incoming merge adds no sunderable id. CONTEXT THE DECISION TURNS ON: perfecting.ts:89-96 states the cost model outright ("The Maker's Ember is the pacing lever (1/week accrual, R4 untouched); the essence and setting have faster faucets"); the rank derivation at state.md:19093-19105 fixes attempts per week at 1 from the ember and prices the essence at 1 because "2 would out-pace a weekly raider's spare epics" (:19096-19097), the one premise a wider feedstock actually relaxes; the essence is kind 'tool', soulbound, noDiscard, sellValue 0, stackSize 20 (items.ts:3008-3017, semantics at src/sim/types.ts:1053-1059), is NOT in MATERIAL_ITEM_IDS (measured), so it sits in bag or bank slots, and server/admin_market_metrics.ts:37-44 calls its market bucket "a structural-zero tripwire"; precedent: the Phase 05 QA ruling at state.md:1412-1421 already widened this same faucet once (heroic-raid epics admitted, "aligning with the settled any-raid-epic-of-the-tier model") and paired the widening with the Phase 12 pricing look. | (1) Ratify the non-gear closure and ADMIT the Crucible gear tier (status quo) [comment-only in code: a dated RULED line on state.md:21701-21706, the deferral sentence at sundering.ts:62-65 and the comment at tests/masterwrought_materials.test.ts:978-980 reworded from "the maintainer's call" to the ruling id; npx tsc --noEmit plus npm run ci:changed on the two touched files per the packet's own validation matrix (state.md:499-509); no pin flips, no regen, no fills, no reviewer beyond a doc read, and decisions-index.md carries no qr-* namespace row to move]; (2) Ratify the non-gear closure and EXCLUDE the Crucible tier [a tier clause in isSunderable at sundering.ts:71-78 cut on a MAINTAINER-NAMED value that cannot be guessed here: source level does NOT order the tiers (Ignivar is 26, Nythraxis 27, measured 186 ids at 26 against 17 at 27, so a naive source-level ceiling drops the older raid and keeps the Crucible, exactly backwards), so the cut has to be an item-level ceiling (33 reproduces the pre-Crucible 31 exactly, measured) or an explicit id-list exclusion, and which one is the maintainer's call; flips tests/masterwrought_materials.test.ts:978-988 (grovespring_helmet admitted to refused, the sweep 217 to 31, the greater-than-20 floor still met at 31) and adds a negative arm with its own premise pins, while the suite's other sunder arms hold because RAID_EPIC is pre-Crucible; rewrites the eligibility docblock at sundering.ts:43-65, the essence def comment at items.ts:3003-3004, the naming registry line at state.md:279 (amended in place per decisions-index.md's never-renumber rule), docs/design/professions.md:165-177 and src/sim/professions/CLAUDE.md:207-214; removes the Sunder row from 186 items' bag menus through bag_item_context_menu.ts:91, so tests/bag_item_action_menu_paint.test.ts:389 and the hud_chrome.ts:5880-5881 comment both want re-reading and a disappearing menu row may pull in the PR's before/after screenshot rule; rewords guide.ts endgameMaterialsBody (:2583-2584), whose "of the tier" then reads false in the other direction, carrying npm run i18n:gen with the regenerated resolved bundles committed, the S3 guard run per state.md:503, the two live guide pins at tests/guide.test.ts:3285-3286 ("exactly one essence", "one per week per character") preserved or re-pinned (:3292 pins the Perfecting cost sentence and does not move), and the five non-Latin overlays that already carry that key (ja_JP, ko_KR, ru_RU, zh_CN, zh_TW; zh_CN renders the clause as 本层, "this tier") left STALE rather than pending, which no gate reds, so they are either refilled in-change or routed to the Phase 20 fill under seeded row 3's reworded-keys item; sim reviewer plus content-obligations-reviewer plus a frontend read for the menu, then the /qa gate; no deed, reliquary, item-art, census-row or IWorld-parity obligation, no wiki-content regen (src/guide/content.generated.ts carries no sundering entry, checked), and no rng draw site moves so no parity golden]; (3) Fold into row 12's re-measure [zero work now, and priced honestly it is the weakest arm: essence supply sits on no R5 axis (docs/design/power-verification.md has zero "sunder" hits across its 1257 lines and exactly one "essence" hit, arcane_essence at :1226, an enchanting reagent; section 14's recorded-not-acted-on list carries no material-faucet row), and attempts stay ember-gated, so a power re-measure cannot answer a faucet-scope question; the real cost is a Phase 19 row closing with no ruling and re-opening in Phase 20]; (4) Option 1 plus reword the player-facing "of the tier" to what the predicate actually does [option 1's edits plus one English value at guide.ts:2583-2584, npm run i18n:gen with the regenerated bundles committed, the S3 guard run, the two guide pins at tests/guide.test.ts:3285-3286 preserved, and the same five non-Latin overlays to refill now or route to the Phase 20 fill: note M16 does NOT catch this, since a reworded (not new) value leaves the overlays translated rather than pending, so tests/i18n_completeness.test.ts stays green and the staleness is silent; an i18n or frontend read; no pin flips and no sim change]; (5) Option 1 or 4 plus RE-PRICE the attempt against the wider feedstock [a MAINTAINER-NAMED essence count in PERFECTING_ATTEMPT_COST (perfecting.ts:92-96), the one attempt ingredient R1 leaves uncounted (state.md:86-88), and the ledger's own reason for 1 ("2 would out-pace a weekly raider's spare epics", state.md:19096-19097) is precisely the premise the widening relaxes; flips the exact-array pin at tests/perfecting.test.ts:137, rides the derived consumers that read the constant (perfecting_wire, orange_promotion, lucent_infusion_guard, deeds_sites_pin, tests/parity/coverage_c and tests/parity/scenarios), flips the guide prose at guide.ts:2587 with its pin at tests/guide.test.ts:3292 plus that key's own five overlays, and owes a dated amend-in-place line on the rank derivation at state.md:19093-19105; sim reviewer then the /qa gate; note the cadence itself does not move, attempts per week stay 1 from the ember] | Option 4, with option 1 as the trim if the maintainer would rather not touch player text this phase: every ledger the packet owns leans admit. The cost model the packet derived its cadence from names the ember the pacing lever and the essence a faster faucet it deliberately does not gate on (perfecting.ts:89-96; the derivation at state.md:19093-19105 sets attempts per week at 1 from the ember alone), so widening the feedstock from 31 ids to 217 moves no number the packet derived; the one premise it does touch, "2 would out-pace a weekly raider's spare epics", is relaxed rather than broken, which is an argument for admitting and recording, and option 5 exists for a maintainer who reads that relaxation as owing a price instead. The Phase 05 QA already widened this faucet once on the same reasoning (state.md:1412-1421), and the essence is soulbound with sellValue 0 behind a structural-zero market bucket, so none of it reaches the economy R21 governs. Excluding by tier would also read backwards to a player: the current raid would become the only raid whose epics are worthless to sunder while the previous tier's 31 keep paying, and 145 of the 186 are sigil-redeemed set pieces, the same shape the pre-Crucible tier pays on. What the wider feedstock genuinely changes is unglamorous and belongs on the record rather than in a tune: the essence is noDiscard, soulbound, sellValue 0, stackSize 20 and not a Material, so a raider who sunders freely banks stacks that cannot be discarded, sold, mailed or listed against a sink of one per ember-gated attempt, which is a storage read, not a balance one. The measurably loose thing is the wording: with two raid tiers live, "of the tier" no longer describes a predicate that admits both, and the zh_CN overlay having translated it as 本层 ("this tier") is evidence a reader takes it as a restriction, which is why option 4 pairs the ratification with that one sentence. | row 16 (the Crucible GEAR tier's sundering admission; the seed's NON-gear half is verified closed in-repo, measured at 2ab5c2f7d0, and is ratified here rather than re-decided, with the release's lastflame_core reclassification verified not to move it) |
| D145 | live-nonce-fence-write-loss | The LIVE session fence (`kind: 'nonce'`) carries the identical uncorrelated-subquery write loss the Phase 18 QA just closed on the offline arm, on the hottest write path in the system: does the live autosave take the characters row lock BEFORE its fence is evaluated, or does the packet accept the residual and record it? A ruling is owed because the two arms differ in COST, not in correctness: the offline writers run units per day, and the live save runs about 33 times a second at 1,000 online. | The shape: `characterUpdateStatement`'s `nonce` arm (server/character_save_statement.ts:117-126) fences with `AND EXISTS (SELECT 1 FROM character_leases WHERE character_id = $1 AND holder = $4 AND nonce = $5)`, an UNCORRELATED subquery Postgres evaluates as an InitPlan with a One-Time Filter, i.e. BEFORE the row lock is taken. `liveSaveFence` (server/db.ts:3471) builds it for all four live callers (server/db.ts:3492, :3596, :3699, :3844), the busiest being `saveCharacterState` (:3477), the 30 s autosave path. REPRODUCED against real Postgres by the Phase 18 QA's server-save reviewer with a probe inserted into tests/character_save_statement_pg_integration.test.ts and then removed: grant lease nonce-A, have a contender hold the characters row FOR NO KEY UPDATE, park session A's fenced save, displace the lease to nonce-B mid-wait (production acquires with `INSERT ... ON CONFLICT (character_id) DO UPDATE` at server/db.ts:4776, which takes no parent lock and so nothing stops the displacement), release the contender: A's write LANDS over B's session (`PROBE RESULT landed=true marker=displaced-must-not-land`). The offline twin was fixed in this same round by taking `SELECT 1 FROM characters WHERE id = $1 AND realm = $2 FOR UPDATE` first (server/offline_character_save_db.ts), pinned by a red-proven pg arm; the live arm was deliberately NOT changed, because its cost profile is a maintainer call. | Take the row lock first on the live arm too, matching the offline fix [one extra statement per save inside the existing transaction, so on the order of 33 additional round trips per second at 1,000 online on a box where Postgres shares four cores with the 20 Hz loop; needs its own real-Postgres arm on the displacement race plus a re-read of the autosave latency budget, and the lock is taken on EVERY save rather than only contended ones]; Re-read the lease inside the same transaction AFTER the write and roll back on a displacement [no lock on the uncontended path, so the common case pays one cheap indexed SELECT rather than a lock wait, but a lost write becomes a ROLLBACK plus a retry decision the caller does not have today, and the autosave has no retry path]; Accept the residual and record it [no-op beyond a dated RULED line plus an honest comment at the nonce arm and a row in the ledger; the window needs a save parked on a contended row long enough for a full displacement, which is rare, and the loser is one autosave cycle of state rather than a durable operator action, unlike the offline arm where a lost rename sweep leaves crafted copies signed with the old name and nothing re-triggers it] | Take the row lock first, matching the offline arm, UNLESS the maintainer has a latency budget this breaks. The correctness argument is identical on both arms and the asymmetry is now recorded in the code, which is the worse of the two states to leave: a reader of the offline fix will reasonably assume the live path is fenced the same way. If the cost is unacceptable, the third option is honest as long as the nonce arm carries a comment saying so, because the current silence reads as a guarantee the statement does not make. | new (raised by the Phase 18 QA server-save review; not in the sweep inventory, and no seeded row covers it) |
| D146 | periodic-flush-atomicity | Should the 30 s autosave's character / market / mail trio commit ATOMICALLY, the way the leave and shutdown flush already does, or does the packet ratify three independent fire-and-forget writes and record the tear window? This row exists because the Phase 18 QA's server-game lane DEVIATED from bucket-A item autosave-transactional-writes on exactly this question and its code comment claims the deferral was tabled here, which it was not until now. | The item (state.md:1475) reads: "the 30s autosave saves characters, market, and mail as three independent writes, so a crash between them can duplicate or drop a mailed-cores letter relative to the gate (same shape as mailHeroicMarks, accepted; the leave/shutdown flush is transactional)". The lane did not deliver atomicity: it extracted the body to server/periodic_save_flush.ts, restored a profiler sample the v0.42.0 merge had dropped, and pinned exactly-once. Its own header says the atomicity call "belongs to the maintainer and is recorded as a Phase 19 row"; the QA's fresh reader checked and found no such row, which is why this one is written. The structural obstacle is real: saveCharacterAndMarketState carries ONE characterId, so a faithful extension is a per-character whole-realm market plus mail transaction, and the cost hazard is documented at socketClosed's withMarket parameter. | Ratify three independent writes and record the tear window [no-op beyond a dated RULED line plus an honest sentence at the flush naming what a crash between them can do; the item text itself already says "accepted", and the leave and shutdown paths, which are the ones that matter for a clean exit, are transactional today]; Make the periodic flush transactional per online character [one whole-realm market plus mail transaction per online character per 30 s, which is the shape saveCharacterAndMarketState forces; needs a measurement first, because at any real population this multiplies the market blob write by the online count, and the socketClosed withMarket comment already names that hazard]; Mint a new multi-character transaction shape [one transaction per flush rather than per character, which is the only form that is both atomic and O(1) in population, but it is a new persistence shape with its own lock-ordering and timeout story and a migration-safety review, and nothing in the tree has that shape today] | Ratify and record. The tear this closes is one autosave cycle of mail-versus-gate skew on an unclean crash, the item text already accepted it, and both alternatives buy that against a write-volume or a new-shape risk that no measurement currently supports. If the maintainer wants atomicity, the third option is the only one that does not scale with population, and it should be sized before it is chosen. | new (raised by the Phase 18 QA server-game review, which found the lane citing a Phase 19 row that did not exist) |
| D147 | sold-volume-four-seam-wiring | Should the market sold-volume cluster be WIRED, turning on a new database write per market sale, or does it stay deliberately dark with the guard that now pins its absence? All four seams are absent today: the DDL is not in ensureSchema, marketSoldVolumeRetentionTable is not registered with the nightly sweep, buyWithSoldVolume has no caller, and neither configure function is called at boot. | Three independent Phase 18 QA reviewers found the same thing, and the store is well built where it exists: bounded by realms times 90 retained days times TRACKED ids (so players cannot grow it by trading harder), the day stamped from the DATABASE clock so overlapping deploys cannot split a day, house rows refused outright, retention typed against the sweep contract, and the DDL proven idempotent twice over a populated table against real Postgres. What is missing is only the wiring. The failure ORDERING is what makes a half-landing dangerous: the writer without the DDL turns every tracked sale into a 42P01 at sale rate, and the writer with the DDL but no sweep registration is an unbounded table. The Phase 18 QA fix round therefore did NOT wire it (turning on a per-sale write is a production behavior call), corrected the header prose that claimed the sweep registration already existed, and landed a guard that pins all four absences with the deferral named, so the day one seam lands the pin reds and forces the rest. | Wire all four seams together [the DDL into ensureSchema, marketSoldVolumeRetentionTable into the retention tables array in server/main.ts, both configure calls at boot, and buyWithSoldVolume at the market_buy dispatch; owes a real-Postgres arm driving a sale through game.ts and asserting the row, plus an ensureSchema-applies-it arm, plus the FIFO admission cap and the lock and statement bounds the same review asked for, plus a database-performance and a migration-safety review; also note buyWithSoldVolume adds two linear finds over the listing book per buy on the WS command path, so it wants a Map or an id index first]; Keep it dark and ratify the guard [no-op; the panel keeps saying the volume half is unavailable, which is the honest shape, and the guard makes a half-landing impossible]; Delete the cluster [removes about 470 lines of tested code plus its pg suite; only correct if the feature is not wanted, and the packet has no record saying that] | Wire it, but as its own change with its own reviewers, not folded into a QA fix round. The store is finished and tested and the dashboard has a permanently dark half without it. The reason it is a ruling rather than a fix is only that it turns on a write per sale in production; once the maintainer says yes, the four seams and the FIFO bound land together and the guard flips from pinning absence to pinning presence. | new (the ledger recorded the four call sites as "routed to those holders", and they never landed; raised again by three Phase 18 QA reviewers). RULED (qr-19-sold-volume-four-seam-wiring, 2026-09-01, under qr-19-best-for-project): WIRED. All four seams landed together (MARKET_SOLD_VOLUME_SCHEMA in ensureSchema, marketSoldVolumeRetentionTable in the nightly sweep, both boot configure calls, buyWithSoldVolume at the market_buy dispatch); buyWithSoldVolume was reduced to one pre-buy read (the hot-path note); the UNWIRED_SEAMS guard flipped to presence at the wiring SITES; a real-Postgres observer-end-to-end arm and an ensureSchema-boot arm prove it. The ruling's open record is the market_sold_volume_db.ts header WIRED block. |
| D148 | sim-scope-pending-is-unreachable | Sim-scope and server-scope matcher keys can never appear on the release-tier fill worklist, because the scanner marks them translated in every locale by construction. Does the packet fix the scanner, add a copied-English detector for those scopes, or accept sim and server matcher text as permanently English on the record? | Verified in the scanner source by the Phase 18 QA i18n review: addDictScope in scripts/i18n_scan.mjs marks a locale translated whenever isPresent(dict[lang][key]), and DICT in src/ui/sim_i18n.ts spreads ...baseEnTable FIRST for every language, so a value is always present. The regenerated registry agrees: all 762 sim-scope keys read translated in every locale with 0 pending, and server scope likewise 165 translated with 0 pending. So the release-tier gate's "the pending set is empty" passes over them by construction. This bit immediately: the 52 rift mechanic names the same QA round added are English in all 20 non-English locales and have no automated carrier to the Phase 20 fill. Distinct from D126, which is a FILLED row going stale; this is a NEVER-filled row reporting itself filled. | Make the scanner check real overlay presence for DICT scopes [a scanner change plus a re-derived registry; the honest fix, and it will surface a backlog of sim and server keys that have never been filled, which is the point]; Add a copied-English detector for the sim scope [the H3b shape that already exists for the server and admin scopes; narrower than the above and does not disturb the state model, but it catches only rows whose value is byte-identical to English]; Accept sim and server matcher text as permanently English and record it [no-op beyond a dated RULED line and a note at the DICT spread; the release-tier gate then honestly says nothing about these scopes rather than appearing to cover them] | Fix the scanner. The current state is the worst of the three because it reports coverage it does not have, and every future sim-side player string inherits the blind spot silently. If the backlog it surfaces is too large to fill now, the second option plus a dated record is an acceptable staging step, but the third should not be chosen while the gate still prints these scopes as fully translated. | new (raised by the Phase 18 QA i18n-matcher review; not covered by D126) |
| D149 | rift-mechanic-names-translate-or-not | Do the 52 rift mechanic display names get real locale fills, or do they join the standing precedent that rift proper nouns stay English in non-Latin locales? Today they are English everywhere with no carrier either way, so the question is open by omission rather than by decision. | The Phase 18 QA closed bucket-A item rift-mechanic-name-matcher-gap by adding 52 English rows to baseEnTable plus their 52 AURA_NAME_KEY rows, derived from RIFT_MOBS and pinned so a newly authored mechanic reds. That is the sanctioned English-only contribution. But per D148 the rows can never reach the fill worklist, and the cited precedent is ASYMMETRIC: the 16 abilityUi.cast.rift_* rows in the M16 BRAND_ALLOW list (commit ead5768827) do have real Latin-locale fills (tr_TR "Buzul Mezari", es "Tumba glacial", pt_BR "Sepultura Glacial") and are exempted only from the non-Latin leak guard. So "rift names stay English" is not actually the shipped precedent. | Fill all 52 in every locale [a maintainer fill pass across 20 locales, 1,040 rows, on the Phase 20 fill lane; needs the D148 carrier or a hand-maintained worklist entry, because nothing will remind anyone]; Fill the Latin locales and add the 52 to BRAND_ALLOW for the non-Latin five [matches what the 16 cast ids actually do today, so it is the consistent reading of the existing precedent; still needs a hand-carried worklist entry]; Rule all rift proper nouns English everywhere and extend BRAND_ALLOW to cover both sets [cheapest, and it makes the existing 16 rows the anomaly rather than the precedent, so those would want their Latin fills reverted or the ruling scoped to new names only] | Option 2, the consistent reading. It is what the shipped rift cast ids already do, so it needs no reversal, and it keeps the non-Latin leak guard meaningful. Whichever is chosen, the 52 keys need a hand-written entry on the Phase 20 fill worklist until D148 is settled, because the registry will keep reporting them translated. | new (raised by the Phase 18 QA i18n-matcher review) |
| D150 | drowned-temple-entertext-deploy-alias | The Drowned Temple's enterText reword is WIRE-CARRIED, and it shipped without the deploy-window alias this branch's own pinned precedent requires. Ratify the alias practice for this reword, or accept a transient window where the line renders as raw English with the em dash the reword existed to remove. | The mechanism, proved by the Phase 18 QA i18n review by diverging the two copies one byte: src/sim/instances/dungeons.ts emits dungeon.enterText as RAW ENGLISH, and the client matches it by exact bytes in localizeSystemText (if (text === dungeon.enterText) return dungeonText(...)), so the CONTENT copy is the match key and the CATALOG copy is what renders. A server not yet restarted therefore emits the old em-dashed sentence, which no longer matches the new client, and falls through to raw English with the dash on screen. tests/localization_fixes.test.ts pins exactly this practice for this branch ("the phase 03 wire-carried renames keep legacy aliases for the deploy window", noting they ride the masterwrought branch onto the release), and localizeSystemText already carries an ellipsis twin for the arena queue line on the same reasoning. | Add the legacy alias for the deploy window [a second exact-match arm in localizeSystemText keyed on the pre-reword sentence, plus its pin beside the existing phase 03 aliases; the shape already exists in that function, so it is a small, precedented edit]; Accept the transient render and record it [no-op beyond a dated RULED line; the window is one deploy long and the worst outcome is one dungeon entry line showing an em dash to an English player, which is the exact defect the reword closed]; Stop matching enterText by raw bytes and carry a key on the wire instead [the durable fix for the whole class, since every wire-carried English string has this shape, but it is a protocol change with its own epoch question and is far beyond this reword] | Add the alias. It is the practice this branch already pins for wire-carried renames, the edit is three lines in a function that already has a twin of the same shape, and without it the reword is briefly self-defeating. Option 3 is the right long-term answer and deserves its own row if the maintainer wants the class closed. | new (raised by the Phase 18 QA i18n-matcher review; the reword itself is the eleventh em dash the QA fixed) |
| D151 | r21-staged-reagent-state | Does masterwrought R21 ("every material has a consumer at the rung that produces it") admit a STAGED REAGENT state, a material that is classified and shipped before the recipes that spend it? The release introduced exactly that category under the packet rule and the packet had no answer for it, so the QA honored the tree's own release-sanctioned exception to unblock the gate and hands the scope call here. | The trigger: release commit c173606bbb ("fix(items): classify last flame core as material") added a derivation source that moved MATERIAL_ITEM_IDS from 116 to 117, pulling lastflame_core under the packet's consumer floor in tests/gathering_supply_coverage.test.ts. Verified by ancestry rather than by reading: c173606bbb is an ancestor of theirs (22e909839f) and of neither ours (2ab5c2f7d0) nor the base, and the merge resolution is clean (the packet guard came across byte-identical; both sides composed in material_ids.ts). The "spent by an unwalked mechanism" reading is REFUTED, not assumed: every non-i18n reference to the id in the tree is its def, the pending list, four loot rows in dungeons.ts, and one comment in sundering.ts recording it as refused feedstock. There is no spend site to cite, and a live probe returns no recipe, enchant or quest naming any pending id. The release change is not a defect either: the pending list carries its own contract ("materials staged before their consuming recipes ship; remove an id once a live recipe names it") and the consumers are authored on PR 3704, which spends it 3, 6 and 15 per craft. WHAT THE QA DID: honored the same exception the release's own sibling suite already honors (tests/material_profession_affinity.test.ts skips exactly CRUCIBLE_RECIPE_PENDING_MATERIAL_ITEM_IDS and pays for the skip with a positive arm), and paid for it the same way plus a SELF-RETIRING arm that reds the day a live consumer appears, so the exemption cannot outlive its reason. Mutation-proven in six directions, including that the presence floor still reds for a non-exempt material. | Ratify the staged-reagent state as part of R21, with the pending list as its carrier [no-op beyond a dated RULED line amending R21's text to say that a material may ship classified-but-unconsumed while it sits on a named, self-retiring pending list; this is what the tree already does on both sides, so nothing moves in code]; Refuse the state and require a consumer at classification time [the release's classification would have to be reverted on this branch or a consumer authored here, which means inventing a balance number for another packet's content (PR 3704 already fixes it at 3, 6 and 15 per craft), and it would put this branch at odds with the release on a content call the release owns]; Narrow R21 to packet-authored materials only [the smallest rule change: the floor stops applying to materials the release classifies, which removes the collision permanently but also removes the guard from exactly the content most likely to arrive unreviewed] | Ratify the staged-reagent state. The tree already implements it on both sides, the exception is named and dated rather than silent, and the arm the QA added retires the exemption automatically the moment PR 3704 lands a consumer, so the rule keeps its teeth. Option 3 is the one to avoid: it would blind the floor to release-authored materials, which is the case that just proved it can catch something. | new (raised by the Phase 18 QA gate-red round; the release moved a material under a packet rule that had no state for it) |

### The sixteen seeded rows this table replaces (kept because the Seed row column cites them)

| # | Decision | Where it stands today | Options (priced by the 18 QA) |
|---|---|---|---|
| 1 | The commission minimum-fee floor: (a) mechanics (escrow at open / transfer at delivery / display-only), (b) basis (UNBIND_FEE ladder / buyValue fraction / flat) | PARKED, Phase 14 ledger + handoff row 128 | to be priced |
| 2 | Ruling 135 p13-OPEN-RELIQUARY: does a player-named legendary INSTANCE warrant an instance-keyed Reliquary page | RECORDED undecided, farming/state.md rulings block | to be priced |
| 3 | The THIRTEEN standing items (state.md): scroll/elixir 15c parity; RULE 2's value-half reading; the inherited stale-client window; 11h's two deviations (ratify or revert); 11i's session cap 15 s to 16 s; prog_first_harvest's 13 catches; the apex hoe channel (the Decision-A contradiction); the shipped-id golden policy; Decision D's band scope; the reworded wiki keys' stale fills (folded into the Phase 20 fill unless ruled re-keyed); col_junk_drawer's three of margin; zone 1 carries no poor mob drop | OPEN, each with its ledger derivation | to be priced, one row each |
| 4 | Every farming handoff-table row still in the open-family and handed-to-maintainer classes (re-derive the live set from the table's text; 44 non-fill rows at the Phase 17 close), including the tuning reads ((bs) waiver cadence, feast tuning, seed-back rates, watch and compost fees, tonic, dish curve points, crop durations, name lore passes, hoe names) and the standing deviations ((a), (w), (al), (ap), (bb), (be), (bg), (bh)) | OPEN in farming/state.md | to be priced, one row each |
| 5 | The feast per-area placement cap (a value and a scope: per cell or per zone) | Phase 17 hot-path read | to be priced |
| 6 | The nonce fence's missing expiry term (keep suspend-first, or add the term) | Phase 13 QA recorded read | to be priced |
| 7 | Alchemy has no repeatable band-3 recipe (author one, or accept the gap) | Phase 15 recorded read | to be priced |
| 8 | The apex weapon rung sits +1 over Greater (a demand risk under R21: tune, or accept) | power-verification.md section 14 | to be priced |
| 9 | qr-GRAY's row wording is measurably false as written (reword the row, or rule the measurement) | Phase 15 recorded read | to be priced |
| 10 | The two consistency reads: fine farm produce reads Material while ore/log/herb fine grades read Fine Material; the nameplate_canvas ratchet row on an upstream-owned line | recorded, unnumbered | to be priced |
| 11 | The zone-celebration fan-out shape, IF Phase 18's measurement recorded a refusal | Phase 18 ledger | to be priced |
| 12 | R5 on the merged world (the eighth v0.41.0 sync moved the fixture surface AND the hit table: all four baseline kits re-rated under the incumbent set-stack retune d404eab938, WAR_BIS 355 to 165 hit, ROGUE_BIS 190 to 120, CASTER_BIS 160 to 50, with the crownforged 2pc bonus replaced; the ramp lowered by the Crucible rebalance c920f39c85, ABOVE_LEVEL_MISS_PCT [0,2.5,14,21] to [0,2.5,8,14], so the merged needs read 130/190; the tank baseline 3332 to 3532 health, +60 the legendary retune 4ed7a279b4's heart_of_the_rift, about +100 the replaced 2pc, the rest the lineage lines): re-measure the record on the merged catalog (re-opens R5 under the four rulings' own conditions, and the re-measure must ALSO settle the target set: the existing two protected assets, heroic Nythraxis and S-rift, or an added Ignivar Crucible profile), or ratify the record as a measurement of the pre-raid catalog and retire the nine expected-fail pins | state.md, the eighth-sync AMENDED block in the Phase 15 ledger; the nine it.fails pins in r5_envelope_probe / masterwrought_budget / dev_bis_gear / server pbe_boost | to be priced |
| 13 | The apex tier's placement against the Crucible raid tier (the raid catalog out-scores every apex piece on bestEpicGearFor and the chest lead measures -1 against the ratified cap of 2), INCLUDING the staged Crucible crafted fast-follow (docs/prd/ignivar-raid-professions.md + src/sim/content/crucible_professions.ts, PR 3704: crafted epics at ilvl 37 with skill-100 floors and a crafted non-masterwrought legendary, prospectively contradicting the packet's crafted-ceiling premises): re-tier the apex pieces above the Crucible tier, accept the raid tier above apex and amend the power placement (then rescope the ilvl-31 pin and the R3 sub-cap wording to the masterwrought family explicitly), or fold into row 12's re-measure; and rule whether the fast-follow inherits the masterwrought flag and cap | same block; tests/dev_bis_gear.test.ts and the masterwrought_budget lead-cap arm; the rogue identity re-anchor awaits the same authority's confirmation | to be priced |
| 14 | The twin complement rule against the retuned references (forgefold_legguards and spiritweld_girdle now duplicate their reference drop's crit 40): re-complement the twins against the retuned references (an R5-surface def edit), or amend the complement rule | same block; the two MERGE_INHERITED_TWIN_DUPLICATES rows in tests/masterwrought_budget.test.ts | to be priced |
| 15 | REF_ARMOR (2861) against the raid-era live max-armour kit (4085 after the Crucible plate; the pre-raid gap was about a hundred points): raise the calibration constant and re-derive every difficulty floor, or keep it and record the widened gap as the model's stated conservatism | power-verification.md section 14 (the REF_ARMOR read); tests/heroic_difficulty_floors.test.ts re-pin note | to be priced |
| 16 | The Crucible GEAR tier's sundering admission (the tier scope of R1's "of the tier"): the raid-sourced Crucible gear epics currently feed sundered essence the way every raid gear epic does, which widens the R4/R9 material economy the packet balanced against the pre-raid raid tier; admit them (status quo), exclude them by tier, or fold into row 12's re-measure. The NON-gear widening (sigils, the lastflame core) is already closed in-repo by the gear allowlist, pinned | state.md, the eighth-sync AMENDED block (the sundering record); src/sim/professions/sundering.ts isSunderable; tests/masterwrought_materials.test.ts, the eighth-sync gear-allowlist arm | to be priced |

### Starter Prompt
```
STANDING DELIVERY RULE (unchanged): NO push, NO PR, NO teardown. LOCAL ONLY.
This is Phase 19 of the Masterwrought feature: the rulings gate. Model: xhigh effort.
ULTRACODE: yes for the execution and review halves; the decision half is a
conversation with the maintainer and runs in the main loop.

WORKTREE GUARD (FIRST): run pwd. If not in /Users/fernando/Documents/wocc-masterwrought,
switch NOW with EnterWorktree (path: /Users/fernando/Documents/wocc-masterwrought). If it
refuses, STOP.

STEP 0 - PRE-FLIGHT: git status clean at the Phase 18 QA close; the newest
origin/release/** re-resolved by version sort and merged with the release-merge-audit if
it moved (riders as in Phase 18). Memory scan as in Phase 18.

STEP 1 - THE TABLE: load the FINALIZED decision table from this file (the Phase 18 QA
wrote it). Verify it is complete against the live ledgers with one Explore sweep (any
open decision missing from the table is added before STEP 2, priced the same way). Then
STOP and present the whole table to the maintainer in ONE message, every row with its
options, prices, and the recommendation. Do not proceed until every row has an answer
in the maintainer's own words (a row may be answered "defer past the PR": that is a
ruling too, recorded as such with the maintainer's reason).

STEP 2 - EXECUTE EVERY RULING, one commit per ruling cluster: content tunes carry their
same-change obligations (wiki regen, deed and reliquary pins, art parks, non-Latin fills,
census rows, the R5 surface untouched unless the ruling names it, in which case the
harness re-cut and the record amendment ride the same commit); mechanics rulings carry
their tests (sim and server always) and their reviewers per the Review Dispatch Matrix;
every ruling is RECORDED where the open row stands (a dated RULED line with the ruling
id qr-19-<slug>, the maintainer's answer quoted in substance, the executing commit), the
handoff table row's status flipped, the decisions index updated if a namespace moved.

STEP 3 - REVIEW: every execution unit gets its domain reviewer and a fresh reader; all
findings applied; the fix round re-read fresh; qa-checklist LAST. The QA twin is folded
INTO this phase by decision (the 11b-qa-GATE-9 precedent): the executions are small,
each is reviewed twice here, and Phase 20's re-close is the independent backstop.

STEP 4 - VALIDATION: tsc, the affected suites, architecture and monolith guards, the
census (RESULT PASS, exit captured without a pipe), ci:changed after the last commit,
then node scripts/gate_select.mjs on the committed tree, then the full npm run gate in
the background (pg armed) judged by exit code AND markers.

STEP 5 - DOCS: progress.md Phase 19 row; the Phase 19 ledger in state.md (the table as
answered, every ruling id, every executing commit, the JUDGED list); the handoff table's
open classes should now hold ONLY the fill rows and ip-17-PUSH; if anything else remains
open, it carries a ruling id that says why.

STOPPING RULES: no ruling is inferred from silence; a ruling that contradicts a ratified
R5 row is an escalation, not an execution; a ruling that needs a value the maintainer did
not give goes back to the maintainer in the same session; any red gate step; NO push, NO
PR, NO teardown.

REPORT: the answered table, every ruling's id and commit, the review record, the gate
results, and NEXT = Phase 20 (phase-20-release-fill-and-reclose.md, FRESH session).
```
