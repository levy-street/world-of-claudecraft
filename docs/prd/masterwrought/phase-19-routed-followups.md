# Phase 19 routed follow-ups (out of this feature)

Recorded 2026-09-01 under qr-19-best-for-project. These rows were classified OUT of the masterwrought and farming feature on FILE EVIDENCE (which files each option would change, and whether this branch already touches them), not on topical judgement, and every OUT call was adversarially challenged.

The maintainer ruled these are RECORDED LOCALLY ONLY: no GitHub issues are filed. **Consequence, stated so it cannot be lost:** this file lives under `docs/prd/masterwrought/`, which rulings qr-19-teardown-comment-citations and qr-19-planning-docs-teardown-decision schedule for deletion after merge. The teardown chore therefore carries an obligation to RE-HOME this list before it deletes the directory. That obligation is part of the teardown ruling, not a note in passing.

| Row | Work | Estimate |
|---|---|---|
| D015 | Ratify the interim tied-wheat-sheaf crest for prog_farming_100 and leave the commissioned Harvestmaster crest in the maintainer's art pass, since committed art needs his master SHA. | 0.75h |
| D025 | The colour half of this row is already done: the Phase 18 sweep unified the farm pin onto one token family on both surfaces, so only the painted MapMarkerArtId remains and it routes to the maintainer's art pass. | 1.5h |
| D051 | The synchronous stat-held username banlist read on the realm loop: the async cached_read redesign is a server moderation rework with no Masterwrought surface, so the ruling records the routing out of this feature and no code lands here. | 1.5h |
| D095 | Ratify the item-art park and record the wave's schedule and master-SHA basis as a dated ledger line, routing the 81 parked ids out of this branch to the maintainer's post-packet art wave where the profession_icons E2 rule already puts them. | 1.5h |
| D098 | Ratify D13-3's exclusion so `perfected` (and any derived isUniqueEquipped bit) stays off the peer inspect wire and the tagless peer card stands as the recorded consequence, landing no code. | 0.5h |
| D116 | Ratify the shipped no-advisory vendor convention for seed rows, keeping vendor_row_gates.ts scoped to tools rather than minting a one-off advisory convention no other vendor row follows. | 0.5h |
| D121 | Ratify the direct-reads posture for /admin/api: no pre-auth IP meter above the require-admin frame, because the operator lockout failure mode costs more than the two primary-key auth reads it would save. | 0.5h |
| D128 | Option A for the single-slot writer slot key (SingleSlotEntry as four independent slots, shouldWriteSingleSlot comparing the requested kind's slot; the prototype is committed as docs/screenshots/masterwrought-phase-19e/d128-option-a-prototype.patch): the three pins it reds rewritten in tests/painter_host.test.ts (the DEFEAT arm, the entry-shape pin, the cross-kind clobber), tests/painter_slot_collision.test.ts's head comment and the prose surfaces that state the two-field shape (painter_host.ts's guarantee block and entry doc, hud.ts's writer comment, hud_perf_budget.test.ts's ARM 2 rationale, the guard's head and seven-site note) re-read as executed, the guard's collision assertion re-classified from a correctness gate to a style rule, a HEADED PERF_GPU=1 two-run hudHotDomWrites A/B on the capture machine for the golden, and hud.ts extraction to stay under its zero-slack ceiling. MEASURED 2026-09-02 (Phase 19E, qr-19-single-slot-writer-slot-key): zero live writes removed today (the seven shipped sites are fixed and the AST scan holds the line), about +8 bytes per routed element in the browser (+16 in Node); the payoff is deleting the class for the shapes the scan cannot see, which is why it stays maintainer-owned. | 7h |
