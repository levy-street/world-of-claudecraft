# Phase 19B: UI, styles and mobile

Opened 2026-09-01 under ruling qr-19-best-for-project. Presentation-seam work under src/ui, src/styles and src/render. Pure-core plus thin-painter recipe, the per-frame budget, and graphics-settings fairness all apply.

**11 units, about 19.75 hours estimated.** Every row here was answered in the Phase 19 rulings gate; this phase executes, it does not re-decide. A row whose ruling turns out to rest on a wrong premise is an ESCALATION back to the ledger, never a quiet re-ruling.

## Units

### D013

Rule the fate of FarmBiomePalette.trim, the authored per-biome colour channel with no draw-time consumer: keep it parked for the higher-tier trim pass the art batch owns, or delete the channel.

- **Files:** `docs/prd/masterwrought/farming/state.md`, `src/render/farm_patches_core.ts`, `src/render/farm_patches.ts`, `tests/farm_patches_core.test.ts`
- **Same-change obligations:** ANCHOR and HANDOFF ROW are the same line: farming/state.md line 525, match on the unique prefix "  FarmBiomePalette.trim has no draw-time consumer" (grep -cF = 1); its status cell reads "open, handed-to-maintainer" and, on the recommended answer, the replacement cell is "DEFERRED 2026-09-01 by qr-19-farm-biome-palette-trim: the channel is kept and wired in the maintainer's higher-tier art pass; stays handed-to-maintainer". Put the dated RULED line immediately AFTER that table row. PREMISE RE-VERIFIED IN THE LIVE TREE, still true: src/render/farm_patches_core.ts:120 still reads "Authored for the higher-tier trim pass; unconsumed at draw time today", the five FARM_BIOME_PALETTES rows plus the fallback still carry a trim value (:127 to :140), and src/render/farm_patches.ts consumes only palette.soil (:290) and palette.wood (:308) through setColorAt, so no third consumer has appeared. SAME-CHANGE OBLIGATIONS ON THE DEFERRAL: none. No wiki regen, no M16 fills, no deed row, no Reliquary page, no art park movement, no census row, no parity golden, no world_api parity member, no monolith ceiling. WHY WIRING IT NOW IS NOT ACTUALLY CHEAP: there is no trim mesh in the shipped bed and bin set, so a draw-time consumer means choosing which surface the trim tints, which is the art judgment the batch owns, and it mints a new per-instance GPU write that the render-performance reviewer must see. THE DROP ARM IS THE ONLY CLEANLY EXECUTABLE ONE IN BRANCH, about 1 hour: delete trim from the FarmBiomePalette interface, the five palette rows and the fallback, and re-derive the signature line at tests/farm_patches_core.test.ts:167 which concatenates soil, wood and trim to prove the four biomes are distinct (dropping trim narrows what that arm proves, so the assertion needs re-stating, not just shortening). It also deletes authored per-biome values the planned pass would have to re-invent, which is why it reads as negative work. AMENDED IN PLACE 2026-09-01 at execution, five corrections measured against the tree, none of them re-ruling anything. (1) The quoted anchor "  FarmBiomePalette.trim has no draw-time consumer" greps 0, not 1: it flattens the markdown pipes to spaces, and the needle that greps 1 is the pipe form "| FarmBiomePalette.trim has no draw-time consumer". (2) The address 525 is stale; the row reads at farming/state.md:586. (3) "Put the dated RULED line immediately AFTER that table row" is UNEXECUTABLE and is SUPERSEDED by the in-cell replacement this same bullet already prescribes, which is the only edit the unit makes: :586 sits mid-table, the handoff table runs unbroken from its header at :528 through :650, so a standalone line after it would terminate the table and orphan the 64 rows below; farming/state.md already rules under qr-18-REOPEN that "a markdown table row cannot carry an inserted annotation line", and every Phase 19 flip on that table carries its dated text inside the status cell. (4) "the five FARM_BIOME_PALETTES rows" is wrong: there are FOUR (eastbrook_vale, mirefen_marsh, thornpeak_heights, evergarden) plus the fallback, and this bullet contradicts itself nine lines later with "the four biomes are distinct"; tests/farm_patches_core.test.ts pins four. (5) The DROP arm's scope and its one-hour price are understated: they omit the two soil/wood/trim channel tuples and the channelUse containment pin in tests/farming_asset_manifest.test.ts, and docs/design/farming-asset-manifest.json, which carries five trim keys plus that channelUse sentence and is hand-maintained with no generator. That widens the refused arm; it does not move the recommendation. CONFIRMED AS WRITTEN: the status cell reads "open, handed-to-maintainer"; the core comment, the :127 to :140 trim span, the two setColorAt consumers and the concatenated signature arm all measure exactly as the bullet says.
- **Reviewers:** frontend-seam-reviewer, test-coverage-auditor, render-performance-reviewer, qa-checklist
- **Depends on:** D010
- **Estimate:** 1.5h

### D028

Accept the family-consistent stranding of the plant sheet's in-flight send belief when the online pipe silently drops a command under spectate or a reconnect window, rather than ordering a family-wide re-arm.

- **Files:** `docs/prd/masterwrought/farming/state.md`, `src/ui/hud/professions/farming_plant_sheet_window.ts`, `src/ui/hud/professions/perfecting_window.ts`, `src/ui/hud/professions/CLAUDE.md`
- **Same-change obligations:** THE ROW NAMES THE WRONG FILE AND MUST BE AMENDED IN PLACE with a dated 2026-09-01 line, never rewritten. Its price says the fix would be "pendingSend re-arm on reconnect/spectate across the whole command family in src/net/online.ts", but pendingSend does not exist anywhere in src/net. Grepped here: the flag is HUD window state, at src/ui/hud/professions/farming_plant_sheet_window.ts:89 with its error-toast forward documented at :141 and consumed at :279, and at src/ui/hud/professions/perfecting_window.ts:126 with the in-flight belief contract stated at :21-25; the family rule lives at src/ui/hud/professions/CLAUDE.md:45 (in-flight sends mirror a pendingSend flag onto the root's aria-busy). So the wave is the UI lane and any fix is a shared clear-on-spectate-or-reconnect arm across the HUD window family, not a transport change. ANCHOR: put the RULED (qr-19-pendingsend-dropped-command-stranding, 2026-09-01) line AFTER the unique line "  (bq) THE ERROR-TOAST RE-ARM (a refinement of the husk-trade feedback" in farming/state.md (grep -cF = 1); the residual it ratifies is the sentence later in that same block beginning "RESIDUAL, ledgered: a command the online pipe silently drops". HANDOFF ROW to flip, farming/state.md line 499, match on the exact full row "  (bq) residual: online pendingSend stranding on a silently dropped command (spectate/reconnect), family-consistent   P9b QA   maintainer   open ruling-owed (optional)  " (grep -cF = 1); the replacement status cell is "ACCEPTED 2026-09-01 by qr-19-pendingsend-dropped-command-stranding". SAME-CHANGE OBLIGATIONS ON THE ACCEPTANCE: none. No wiki regen, no M16 fills, no deed row, no Reliquary page, no art park, no census row, no parity golden, no world_api parity member, no monolith ceiling. THE FIX ARM, if ordered: a shared re-arm hook on the window family (both windows today, and every future window that carries the flag) fired on spectate entry and on reconnect, plus a design for the double-send risk the ledger already flags (the sim re-validates every gate, so a re-click is safe, but a re-arm that races an in-flight answer is the case to pin), plus new arms in the two window suites and the family pin in src/ui/hud/professions/CLAUDE.md restated. Call that six to eight hours, against a control that recovers today on close and reopen. AMENDED IN PLACE 2026-09-01 at execution, four addressing corrections; the bullet's substance stands and every one of its live-code addresses re-measured CORRECT. (1) The handoff row is at farming/state.md:560, not 499. (2) That row is quoted with its markdown pipes flattened to spaces, so grep -cF on the quoted form returns 0, not 1; the real line is "| (bq) residual: online pendingSend stranding on a silently dropped command (spectate/reconnect), family-consistent | P9b QA | maintainer | open ruling-owed (optional) |" and only the fourth cell is replaced. (3) The residual quote "RESIDUAL, ledgered: a command the online pipe silently drops" also greps 0, because the file hard-wraps it across two lines; the residual it names is real and runs to the end of the (bq) block. (4) The RULED placement is wrong. The anchor "  (bq) THE ERROR-TOAST RE-ARM (a refinement of the husk-trade feedback" IS unique, but it is the FIRST line of the (bq) block and wraps into "contract):" on the next line, so inserting after it splits a sentence; the house form in this same file (the qr-19-qprofintro-overflow-grant, qr-19-growth-tonic and qr-19-fplot-msremaining lines) appends the RULED line at the END of the block it ratifies, so it goes after the block's last line instead. RE-MEASURED AND CONFIRMED: pendingSend is absent from all of src/net; the flag sits at farming_plant_sheet_window.ts:89 with its forward documented at :138-144 and its guard at :279, and at perfecting_window.ts:126 with the contract at :21-25, under src/ui/hud/professions/CLAUDE.md:45; neither window carries a spectate or reconnect arm, so the residual is real and unfixed; and both windows clear the flag in open() and close(), so the stated control holds.
- **Reviewers:** frontend-seam-reviewer, qa-checklist
- **Depends on:** nothing
- **Estimate:** 1.25h

### D033

Rule whether the silent tier-4-to-tier-1 well-fed downgrade gets a surface beyond the buff hover (a combat-log line, or a hover that names the displaced tier), or stays invisible by design as the P11 QA recorded it.

- **Files:** `docs/prd/masterwrought/farming/state.md`, `src/ui/hud/professions/wellfed_tooltip_view.ts`, `src/sim/wellfed.ts`, `src/sim/combat/aura_stacking.ts`, `tests/wellfed_tooltip_view.test.ts`
- **Same-change obligations:** RECOMMENDED ARM (stay invisible): record only, no code. ANCHOR (farming/state.md, grep -c = 1 on the substring 'drop should be legible.'): the P11 QA prose bullet ending in the four-space-indented line '    drop should be legible.'; the dated RULED line goes AFTER that line, house form 'RULED (qr-19-wellfed-downgrade-legibility, 2026-09-01, under qr-19-best-for-project): ...'. HANDOFF TABLE ROW to match verbatim (farming/state.md:498, grep -c = 1): '  Tier-4-to-tier-1 wellfed downgrade legibility (buff hover is the one surface)   P11 QA   maintainer   open ruling-owed (optional)  '; REPLACEMENT STATUS CELL: 'CLOSED 2026-09-01 by ruling qr-19-wellfed-downgrade-legibility (Phase 19, under qr-19-best-for-project): the downgrade stays invisible in the combat log by design and the buff hover remains the one surface, identically in both hosts. Verified live rather than taken from the record: every well-fed food shares WELL_FED_AURA_ID and the buff is self-sourced, so same-name displacement emits a refresh and never a fade, and the hover already renders the ACTIVE payload the meal carried, so the state a player acts on is legible the moment they look. A log line would owe a new SimEvent on the displacement path in both hosts for a self-inflicted, reversible outcome.' VERIFY BEFORE RULING: read src/sim/wellfed.ts (the ONE AURA ID block) and the replacement path in src/sim/combat/aura_stacking.ts to confirm the refresh-not-fade claim still holds after the tenth sync. No wiki regen, no M16 fills, no deed or Reliquary rows, no art park, no census row, no parity golden, no monolith ceiling under the recommended arm. FLIP ARM PRICES: the hover arm (name the displaced tier) is roughly 5 hours, pure ui, and must be authored through the write-game-tooltips skill against docs/design/tooltip-writing.md, one English t() key in src/ui/i18n.catalog/items.ts plus an M16 check if the value is wordy, a wellfed_tooltip_view test arm, and a census row for the new key. The combat-log arm is roughly 10 hours: a new SimEvent on the displacement path, mirrored in ClientWorld, a parity pin, a census row for the SimEvent, and a HARD CONSTRAINT that the log sink must be a sibling module, since the already-landed qr-19-monolith-ceiling-repin left src/ui/hud.ts with 20 lines of slack against its pinned ceiling. That arm dispatches architecture-reviewer, cross-platform-sync, frontend-seam-reviewer and test-coverage-auditor. AMENDED IN PLACE 2026-09-01 at execution, five corrections, none of which reopens the recommended arm. (1) The handoff-row address 498 is stale; the row reads at farming/state.md:559. (2) That row is quoted with its markdown pipes flattened to spaces, so the quoted string greps 0; the real line is "| Tier-4-to-tier-1 wellfed downgrade legibility (buff hover is the one surface) | P11 QA | maintainer | open ruling-owed (optional) |". (3) The ANCHOR is correct: "drop should be legible." greps 1 inside the Phase 11 QA ledger block, and the RULED line takes that bullet's FOUR-space continuation indent wrapped at 72 columns, matching the block it joins rather than the two-space RULED lines elsewhere in the file. (4) THE SURFACE IS MISNAMED in the Files list and in the flip-arm price: src/ui/hud/professions/wellfed_tooltip_view.ts takes an ItemDef and is the ITEM tooltip, so it structurally cannot see a live aura; the buff hover this row ratifies is src/ui/hud.ts auraTooltipBodyHtml falling through to auraEffectTooltipHtml and auraEffectDescriptor over the LIVE aura. (5) The refresh-not-fade premise HOLDS after the tenth sync, but its stated cause is incomplete: the shared id plus self-sourcing only make the newer meal a replacement conflict, while the SILENCE comes from all seven wellFed payloads carrying the identical name 'Well Fed', Sim.applyAura emitting a fade whenever the displaced name differs. FLIP-ARM PRICE CORRECTED, and it is a falsified premise on an arm the ruling refuses rather than takes: the hover arm is NOT "pure ui, one t() key". The Aura record carries no field holding the displaced value and Sim.applyAura splices the displaced aura away without preserving it, so naming the displaced tier on the buff hover needs new sim state or a new event; the charitable item-tooltip reading is ui-only but still needs wellFedTooltipLines widened past its ItemDef-only signature plus a live-aura read at the Hud.itemTooltip call site. The seeded 5 hours understates it either way. CONFIRMED AS WRITTEN: the 20 lines of hud.ts slack, and all four named reviewers plus the write-game-tooltips skill.
- **Reviewers:** qa-checklist, frontend-seam-reviewer, architecture-reviewer, cross-platform-sync
- **Depends on:** nothing
- **Estimate:** 2h

### D035

Close the kind-line label split so the twelve farm fine produce ids read 'Fine Material' like the nine ore, log and herb fine grades, instead of the current plain 'Material'.

- **Files:** `src/ui/item_kind_label.ts`, `src/sim/content/farm_crops.ts`, `tests/item_kind_line.test.ts`, `docs/prd/masterwrought/state.md`
- **Same-change obligations:** THIS IS THE ONE ROW OF THE TEN THAT LANDS CODE UNDER ITS RECOMMENDATION. Confirmed live: item_kind_label.ts grants 'Fine Material' only through baseMaterialFor, and MATERIAL_GRADES is exactly the nine node yields, so the twelve fineProduceItemId values in FARM_CROPS fall through to MATERIAL_ITEM_IDS and read 'Material'. All twelve defs are kind 'junk' (checked fine_vale_wheat), so the branch is reachable. THE TRAP, and it is load-bearing: do NOT close this by adding the farm twins to MATERIAL_GRADES or by widening baseMaterialFor. items.ts states beside fine_vale_wheat that farming fine twins get NO downward grade substitution because materialGradeIds walks MATERIAL_GRADES only; widening it would silently let a fine twin satisfy a recipe asking for base produce, a gameplay change nobody asked for. The label fix must be presentation-only. SHAPE: export a derived FARM_FINE_PRODUCE_ITEM_IDS from src/sim/content/farm_crops.ts (one line off Object.values(FARM_CROPS).map(c => c.fineProduceItemId); data-as-code, exempt from module-first), then one arm in itemKindLabel BEFORE the MATERIAL_ITEM_IDS arm. NO NEW i18n KEY and NO FILLS: itemUi.kind.fineMaterial already ships in every locale for the node fine grades, so zero 19F cost and no M16 exposure. NO WIKI REGEN: itemKindLabel has exactly one consumer, src/ui/hud.ts:6404 (the item tooltip); no guide or generated surface reads it. NO MONOLITH MOVE: the hud.ts call site does not change. CENSUS ROW OWED for the new exported name, then node scripts/merge_audit/symbol_census.mjs (RESULT PASS, exit captured without a pipe). TESTS: tests/item_kind_line.test.ts carries no pin asserting farm produce reads 'Material' today, so nothing flips; add arms that all twelve fine twins read 'Fine Material', that the twelve base produce ids and the twelve seeds still read 'Material', and a negative arm proving materialGradeIds is unchanged so no substitution leaked. ANCHOR (state.md, grep -c = 1): 'skip); the row says to keep ours at the next sync.', the last line of the Phase 16 paragraph carrying BOTH halves of seeded row 10; the dated RULED line goes AFTER it, house form 'RULED (qr-19-farm-fine-produce-kind-label, 2026-09-01, under qr-19-best-for-project): ...', and it must say plainly that it closes only the first half, the nameplate_canvas ratchet half staying with its own row. No handoff-table row for this one (the read is unnumbered prose). Biome: npx @biomejs/biome check --write on the three changed source files only. AMENDED IN PLACE 2026-09-01 at execution, four corrections, none of which moves the ruling. (1) THE ANCHOR IS NOT IN A PHASE 16 PARAGRAPH. "skip); the row says to keep ours at the next sync." greps 1 at docs/prd/masterwrought/state.md:17671, inside the Phase 11l QA ledger and its "THE MAINTAINER'S ITEMS, THIRTEEN NOW" subsection; the Phase 16 ledger does not begin for another five and a half thousand lines. The gate row's own cite, "lines 16953-16958 as read", is stale by about 709 lines and lands in an unrelated chase-rare block; the real span is state.md:17662-17671. The anchor STRING is correct and unique, so the insertion point does not move. (2) THE FILES LIST IS INCOMPLETE: the CENSUS ROW OWED clause requires docs/prd/masterwrought/merge-deletion-list.md, which D166 names correctly in its own Files list and this row omits; following the list literally would land a red census. (3) The census prints "RESULT: PASS", with a colon. (4) TWO COUNTS: this phase has ELEVEN units, not ten, as its own header says; and "checked fine_vale_wheat" understates the check, since all twelve fine defs, all twelve base produce ids and all twelve seeds are kind 'junk', so every ordered test arm is true today. VERIFIED AND UNCHANGED: itemKindLabel's only production consumer is the item tooltip in src/ui/hud.ts (three test files import it, none pins a farm id); itemUi.kind.fineMaterial resolves in every locale bundle and is absent from the pending list, so the zero-fill claim is exact; no handoff table anywhere carries this read; and item_kind_label.ts is a registered UI_PURE_CORE whose forbidden-import guard bans only three, render, game, net and painters, so the new '../sim/content/farm_crops' import is allowed.
- **Reviewers:** frontend-seam-reviewer, test-coverage-auditor, qa-checklist
- **Depends on:** nothing
- **Estimate:** 3h

### D076

Rule the harvest confirm channel for prompt-mode farming tool effects permanently refused (the recommendation, one handoff status flip and a dated line) rather than leaving it re-deferred into every future sweep.

- **Files:** `docs/prd/masterwrought/farming/state.md`, `src/sim/professions/tools.ts`
- **Same-change obligations:** HANDOFF TABLE ROW, exact text to match at farming/state.md:576, verified unique with grep -c = 1 on "Harvest confirm channel for prompt-mode tool effects": "  Harvest confirm channel for prompt-mode tool effects ((af)/(ah)/(ba): mints refused, toggle suppressed, journal informational)   P5 to P8   whoever lands a confirm channel   accepted-no-action (re-deferred with the paired-edit rule)  ". REPLACEMENT STATUS CELL: "closed-by-Phase-19 (qr-19-harvest-confirm-channel, 2026-09-01: refusal ruled permanent; the paired-edit banner in tools.ts stays as the guard for anyone who reverses it)". Note for the parent: this row's status is accepted-no-action, not one of the two named statuses (open ruling-owed, handed-to-maintainer), which is exactly why three sweeps re-deferred it instead of closing it. SECOND RECORD anchor in the (ba) deviation, verified unique with grep -c = 1: "so promptSlotRefused and the resolver arm are untouched." and the RULED line goes AFTER it at column 0. NO content obligations under the recommended arm. LIVE CODE RE-VERIFIED and one correction the row does not carry: the paired edit is now a TRIPLE, not a pair. src/sim/professions/tools.ts holds the policy authority promptSlotRefused (returns true for 'farming') behind a banner that names the pairing, the resolver arm that refuses the mint, AND a Phase 18 LOAD-side twin in normalizeToolEffectSlots, whose own comment says to lift the refusal in the same change; so the land-the-channel arm edits three sites, not two, and the ruling text should say so. THE LAND ARM: a farming confirm flow as its own src/ui module on the existing HUD seams (never a new section in hud.ts, which sits at a zero-headroom monolith ceiling), the three-site sim edit, sim and parity tests, i18n keys for the prompt copy, and then frontend-seam-reviewer plus architecture-reviewer plus cross-platform-sync. Rule-permanent arm 1.5h; land-the-channel arm 16h and it is a sub-feature, not a fix round. AMENDED IN PLACE 2026-09-01 at execution, six corrections, none of them re-ruling anything. (1) The handoff row is quoted with its markdown pipes FLATTENED TO SPACES, so grep -cF on that form returns 0; the real line is the table row "| Harvest confirm channel for prompt-mode tool effects ((af)/(ah)/(ba): mints refused, toggle suppressed, journal informational) | P5 to P8 | whoever lands a confirm channel | accepted-no-action (re-deferred with the paired-edit rule) |". (2) farming/state.md:576 is stale by 61 lines; the row is at :637. Both SUBSTRING anchors do still measure grep -cF = 1. (3) The (ba) anchor sits at farming/state.md:3491, not the :3342 the gate row cites, and the RULED line does NOT go at column 0: the whole Phase 8 deviations block is a two-space-indented lazy continuation, and the sibling qr-19-fplot-msremaining-wire-field RULED line in that same block sits at two spaces. That is the shape copied. (4) The prescribed status-cell form "closed-by-Phase-19 (...)" is kept as prescribed, but it is not the form the fifteen other 2026-09-01 flips on this table use, which read "CLOSED 2026-09-01 by ruling qr-19-<key> (Phase 19, under qr-19-best-for-project): ..."; noted so a later sweep reads the difference as deliberate. (5) That same prescribed cell says "the paired-edit banner", repeating the pair framing this unit's own live-code correction refutes; the cell and the banner both say TRIPLE. (6) "hud.ts, which sits at a zero-headroom monolith ceiling" is FALSE at the tip: 18708 lines against a pinned ceiling of 18728, twenty lines of slack, exactly as D033 above already records after qr-19-monolith-ceiling-repin. That claim sits inside the description of the arm the ruling REFUSES, so it falsifies no premise of the recommendation, but the 16h land-arm price rests on a stale constraint and its shape must be re-derived if that arm is ever ordered. LIVE CODE otherwise CONFIRMED, with one attribution slip corrected: it is the RESOLVER arm's comment, not the LOAD twin's, that says to lift the refusal in the same change together with its twin; the twin defers to the resolver arm for the reasoning.
- **Reviewers:** qa-checklist, qa-checklist LAST
- **Depends on:** nothing
- **Estimate:** 1.5h

### D077

Rule whether gyrelens_array and copperlens_ocular leave the conscious held-offhand no-model park now that the voidbound tome half has landed, where the recommendation is to keep the lenses parked and amend the row's now-stale evidence in place.

- **Files:** `docs/prd/masterwrought/state.md`, `docs/prd/masterwrought/phase-19-rulings-gate.md`, `tests/held_weapon_models.test.ts`, `src/render/characters/manifest.ts`
- **Same-change obligations:** THIS ROW IS PARTLY WRONG AND MUST BE AMENDED IN PLACE, NEVER REWRITTEN. Its Live clause says "src/render/characters/manifest.ts:987-991 still lists only the three inscription tomes as held_offhand models; no voidbound/gyrelens/copperlens GLB exists under public/models yet". That is false at the tip: Phase 18 executed the tome half, so ITEM_OFFHAND_MODELS now carries voidbound_grimoire mapped to tome_voidbound (src/render/characters/manifest.ts, the ITEM_OFFHAND_MODELS block, four tome rows), public/models/weapons/tome_voidbound.glb exists beside the other three, and tests/held_weapon_models.test.ts asserts the retirement positively (expect(unmapped).not.toContain('voidbound_grimoire')). Add a dated AMENDED line to the D077 row in phase-19-rulings-gate.md recording that, then rule. RULING RECORD, two homes in state.md, both verified unique with grep -c = 1. PRIMARY: "no-model pin (a voidbound tome GLB on the phase 06 procedural-tome", RULED line AFTER it at column 0, directly under the RE-OPENED line Phase 18 already put there. SECOND (the copperlens join): "held-offhand no-model park beside gyrelens, the crafted-junk signing", RULED line AFTER it. No handoff table row, so no status cell to flip. ART PARK OBLIGATION, and it is the real deliverable of the ratify arm: the park is enforced by an exact-set pin in tests/held_weapon_models.test.ts listing 13 ids including copperlens_ocular and gyrelens_array, each with a comment saying why; the ruling id belongs in those two comments so the next sweep reads a ruled park rather than an open one. No wiki regen, no M16 fills, no deed rows, no Reliquary pages, no census rows, no parity goldens, no monolith ceilings under this arm. THE COMMISSION ARM: two image-to-glb pipeline runs for a lens family with no existing pattern to reuse (exporter, optimizer, lockfile fingerprint pins, manifest rows, CREDITS and media manifest), and it produces new GPU work, so it owes render-performance-reviewer and frontend-seam-reviewer on top of content-obligations-reviewer. Keep-the-park arm 2h including the amendment; commission arm 12h per item and it belongs in the maintainer art wave, not here.
- **Reviewers:** qa-checklist, qa-checklist LAST
- **Depends on:** D010
- **Estimate:** 2h

### D081

Ratify the character-name precedent for player-given legendary item names (server offensiveName screening plus kick-then-clear, no client-side display masking), which is a dated line on a judged entry that already sits one bullet away from a Phase 19 ruling of the same shape.

- **Files:** `docs/prd/masterwrought/state.md`, `src/sim/professions/legendary_name.ts`
- **Same-change obligations:** RULING RECORD anchor, verified unique with grep -c = 1 in state.md: "precedent); the masterwork seal on the legendary lines (phase 16" and the RULED line goes BEFORE it (that is, immediately after the clause it answers, "no client-side soft-masking of the name (the character-name precedent);", which wraps across two lines and ends at the start of the anchor line). The exact house form is already visible eight lines below in this same bullet, where an earlier Phase 19 session wrote "RULED (qr-19-offline-shape-only-name-screen, 2026-09-01, under qr-19-best-for-project):" at column 0 mid-bullet followed by wrapped reasoning; match it byte for byte in shape. No handoff table row, so no status cell to flip. NO obligations under the ratify arm: no wiki regen, no M16 fills, no deed rows, no Reliquary pages, no art parks, no census rows, no parity goldens, no monolith ceilings. USEFUL ADJACENCY the parent should exploit: qr-19-offline-shape-only-name-screen already ruled the sibling half of this same question in this same bullet (content screening is the server pass, shape-only on the offline host), so D081 is the display-time half of a doctrine that is already half ruled, and the two lines should read as one settled position. LIVE CODE RE-VERIFIED: src/sim/professions/legendary_name.ts is shape-only by its own header and names offensiveName as the server pass; USERNAME_BANLIST is server-side and operator-tunable, living in server/main.ts, server/auth.ts and server/http/game_metrics.ts with pins in tests/security.test.ts, which is exactly why a client mask list is the wrong shape. THE MASKING ARM: a matcher in every name render sink of BOTH hosts (tooltips, inspect, market, peer cards), which means src/ui plus src/net plus the offline Sim path, and it ships the operator banlist into the client bundle; it would owe frontend-seam-reviewer, cross-platform-sync and privacy-security-review, and it diverges from character names, which deliberately lack it. Ratify arm 1h; masking arm 20h and it is a doctrine reversal, not a feature.
- **Reviewers:** qa-checklist, qa-checklist LAST
- **Depends on:** nothing
- **Estimate:** 1h

### D111

Close the report-window NO_MANAGED_TEARDOWN carve-out: give report_window a real focus trap through the shared makeWindowFocus bridge and a managed teardown arm in Hud.closeManagedWindow, then delete its registry exemption row.

- **Files:** `src/ui/report_window.ts`, `src/ui/hud.ts`, `tests/managed_window_close_registry.test.ts`, `tests/report_window.test.ts`
- **Same-change obligations:** CONCRETE SHAPE: report_window.ts gains capture/restore through an injected WindowFocusBridge (src/ui/window_focus.ts makeWindowFocus, the same glue hud.ts wires at hud.ts:5600 and the E2E imports) plus an exported close() so all three current bare hides route through one path (the two `[data-close]` handlers and the submit-success `el.style.display = 'none'`); hud.ts gains one deps line and a `case 'report-window':` arm beside the painter arms in closeManagedWindow (src/ui/hud.ts:3497). MONOLITH CEILING is the binding constraint: tests/monolith_budget.test.ts pins src/ui/hud.ts at 18728 (`toBeLessThanOrEqual`) and the file measures 18708 today, so there are 20 lines of headroom, but CLAUDE.md's ratchet says never GROW a sanctioned coordinator; keep the coordinator delta to the case arm plus the one deps line, or pair a small extraction and LOWER the ceiling in the same commit. TEST OBLIGATIONS: delete the `'report-window':` row from NO_MANAGED_TEARDOWN (tests/managed_window_close_registry.test.ts:77) AND re-word the `default:`-arm justification comment that says `Both NO_MANAGED_TEARDOWN rows are justified by ...` (line 387 to 391), which becomes one row; add the new case arm's expected statement body to the switch pins; extend tests/report_window.test.ts with trap-opened-on-open, trap-released-and-focus-returned on X, on Cancel, and on submit success. NO i18n obligation (the X already carries t('hud.report.cancel') for aria-label and title, no new key). No wire, no facet, no parity, no content, no wiki, no deed, no Reliquary, no art. DECIDE IN THE SAME CHANGE the pin's own parenthetical residue: the in-flight submit closure captures the persistent panel, so a stale resolve can touch a reopened window; a managed close makes reopening more likely, so either guard it with a per-open epoch or re-record it explicitly. RECORDING: dated RULED line, id qr-19-report-window-focus-trap-carveout. ANCHOR, state.md Phase 11 farming-audit notes block: `the report_window focus-trap carve-out (already` (grep -c == 1), line AFTER (note the cited 6670-6673 range is stale, the text now sits near 6773 and the quoted phrase is line-wrapped as `cheap to close at` / `a maintainer's option`). No handoff-table row (Seed row column reads `new`), so no status cell to flip.
- **Reviewers:** frontend-seam-reviewer, test-coverage-auditor, qa-checklist
- **Depends on:** nothing
- **Estimate:** 3h

### D124

Soften src/ui/tooltip_line.ts's header from the doctrine 'Prefer this over new innerHTML builders' to an honest description of the module as the node-returning path, since the string-builder family is four modules with 27 call sites and this module has exactly one importer.

- **Files:** `src/ui/tooltip_line.ts`
- **Same-change obligations:** NOTHING pins the sentence: `grep -rn "Prefer this over"` over src, tests and docs hits only this header and the unrelated item_instance_tooltip.ts line, so no test, no source-scan guard and no doc rots. Verified live that the row's premise holds: tooltip_line.ts has exactly ONE real importer (src/ui/hud.ts:838), while tooltip_line_core.ts has four (gather_tool_tooltip.ts, tool_effect_tooltip.ts, hud/professions/mobile_station_tooltip.ts, hud/professions/recipe_pattern_tooltip_view.ts). SAY THE RIGHT THING IN THE NEW WORDING: the string path is NOT the unsafe one (every line goes through esc()), so the new header must not read as retiring src/ui/CLAUDE.md's standing no-raw-innerHTML rule, which is untouched; and it must keep the two mechanisms distinct, since createTooltipLine returns an HTMLDivElement while the builders return strings Hud.itemTooltip composes into one innerHTML assignment, and neither can serve the other. Consider one reciprocal sentence in tooltip_line_core.ts saying which mechanism a new caller picks, which is the half that makes the softening useful rather than merely honest. No i18n, no content, no test, no wire, no ceiling (a comment edit does not move src/ui/hud.ts). Owes `npx @biomejs/biome check --write` on the touched file plus npm run ci:changed after the last commit. RECORDING: dated RULED line, id qr-19-tooltip-line-doctrine. ANCHOR: `Prefer this over new innerHTML builders.` in src/ui/tooltip_line.ts (grep -c == 1); the ruling REPLACES that sentence and the dated line goes AFTER the replacement, naming the ruling id so the next sweep sees the preference was decided rather than dropped. No handoff-table row (Seed row column reads `new`).
- **Reviewers:** frontend-seam-reviewer, qa-checklist
- **Depends on:** nothing
- **Estimate:** 0.5h

### D166

Ratify the farm crop idle sway as ambient world motion exempt from reduced motion (every other plant in the game leans on the same ungated shader clock) and record that class boundary in docs/design/graphics-settings-fairness.md so the Phase 18 regalia doctrine stops reading broader than it is.

- **Files:** `docs/design/graphics-settings-fairness.md`, `docs/prd/masterwrought/farming/state.md`, `src/render/farm_patches.ts`, `src/render/renderer.ts`, `tests/farm_patches_adapter.test.ts`, `docs/prd/masterwrought/merge-deletion-list.md`
- **Same-change obligations:** SCOPE NOTE: under the recommended arm the diff is ONE doc bullet plus the ledger stamps; no test pins doc prose, so nothing reds. The render and test files are the bill ONLY if the maintainer picks the gate arm, and that arm is wider than it looks: a fourth parameter on FarmPatchVisuals.drive threaded to update(), a stated disposition for a stopped crop (freeze the phase, or re-seat the group quaternion to the seat quaternion), BOTH renderer drive sites re-wired where one is already 89 columns so the call wraps past biome's lineWidth 100, which pushes renderer.ts (13020 lines today) past the 13023 ceiling in tests/monolith_budget.test.ts and needs either an extraction to pay for it or the ceiling raise that is D026's subject; ANY renderer.ts byte owes the Eastbrook re-mint (node scripts/assets/eastbrook_grand_armoury/remint_polish_provenance.mjs re-stamps the four polish seals under docs/screenshots/eastbrook-vale-rebuild/polish); three pins re-cut in tests/farm_patches_adapter.test.ts (the 'sway always runs' arm becomes two-arm, the source-text literal of the live call site, and the idle-sway describe block); and the Phase 18 precedent puts the decision in a pure core, so a new farm_patches_core export owes a merge-deletion-list.md exports row and a green symbol census. NOT OWED on any arm: no i18n or M16 fills (no player text), no wiki regen, no deed, no Reliquary page, no art, no world_api member and so no parity golden. ANCHOR (grep -c = 1) in /Users/fernando/Documents/wocc-masterwrought/docs/prd/masterwrought/farming/state.md: 'idle sway ignores reducedMotion'; the dated RULED line goes AFTER that P7 QA line. HANDOFF ROW, match exactly: '  Idle sway ignores reducedMotion (foliage precedent)   P7 QA   maintainer   open ruling-owed (glance)  '. STATUS CELL REPLACEMENT: 'open ruling-owed (glance)' becomes 'CLOSED 2026-09-01 by ruling qr-19-idle-sway-reducedmotion (Phase 19, under qr-19-best-for-project): the ambient plant wind exemption is RATIFIED and the class boundary is recorded in docs/design/graphics-settings-fairness.md.'
- **Reviewers:** frontend-seam-reviewer, render-performance-reviewer, test-coverage-auditor, qa-checklist
- **Depends on:** nothing
- **Estimate:** 1h

### D167

Ratify that a Well Fed icon shed by the LOW preset's eight-slot buff cap is cosmetic upkeep (the honest plus-N overflow badge and the Always Show All Buffs opt-out already answer the re-eat worry) and close the real gap the QA note named by pinning the current shed contract in the packet's fairness suite.

- **Files:** `tests/professions_graphics_fairness.test.ts`, `docs/design/graphics-settings-fairness.md`, `docs/prd/masterwrought/farming/state.md`, `src/ui/aura_overflow_priority.ts`, `src/ui/auras_view.ts`, `tests/aura_overflow_priority.test.ts`, `src/game/ui_tier_knobs.ts`, `tests/ui_tier_knobs.test.ts`
- **Same-change obligations:** SCOPE NOTE: the recommended arm is TEST AND DOCS ONLY, no src change. The new fairness arm runs the real selectShedSlots over plain slot fixtures and asserts the shipped contract in four directions (a 900 second well_fed slot IS sheddable, a debuff is not, an always-visible id is not, exempt slots do not spend the cap budget) plus one arm that the Hud buff-bar tier override reporting 'ultra' makes the cap never bite; one dated paragraph joins the existing entries in docs/design/graphics-settings-fairness.md. The src/ui and knob files are the bill ONLY on the reverse arm (well_fed joins an exemption set, and the implementer must pick WHICH of the two sets that answer the same question owns it, with a census explained-extras row needed only if a NEW exported symbol is minted) or the cap-number arm (which needs a maintainer VALUE first, flips the exact-value pin in tests/ui_tier_knobs.test.ts and re-baselines the hard-counting arms in tests/auras_painter.test.ts, and re-opens the per-frame DOM cost the cap exists to bound). NOT OWED on any arm: no wiki regen, no t() key and no M16 fills, no deed, no Reliquary page, no art, no census row, no parity member or golden, no monolith ceiling. R5 NOTE: the frozen R5 food term is the aura's VALUE and DURATION, icon visibility is not an R5 input, so no arm here touches the frozen record. ANCHOR (grep -c = 1) in /Users/fernando/Documents/wocc-masterwrought/docs/prd/masterwrought/farming/state.md: 'AURA_VISIBLE_CAP_LOW = 8 (src/game/ui_tier_knobs.ts) recycles auras'; the dated RULED line goes AFTER that P11 QA bullet's last line. HANDOFF ROW, match exactly: '  AURA_VISIBLE_CAP_LOW = 8 self-buff cap fairness (pre-existing, not farming's)   P11 QA   maintainer   handed-to-maintainer  '. STATUS CELL REPLACEMENT: 'handed-to-maintainer' becomes 'CLOSED 2026-09-01 by ruling qr-19-aura-visible-cap-low-fairness (Phase 19, under qr-19-best-for-project): the cosmetic classification is RATIFIED and the shed contract is now pinned in tests/professions_graphics_fairness.test.ts.'
- **Reviewers:** test-coverage-auditor, frontend-seam-reviewer, qa-checklist
- **Depends on:** nothing
- **Estimate:** 3h

## Exit criteria

1. Every unit above executed, or escalated with a dated reason.
2. Each unit's domain reviewers dispatched FRESH (never the implementer), all findings applied: blocking, should-fix and nits alike.
3. The fix round re-read by a fresh reader, because fixes are unreviewed code.
4. Every new pin mutated and watched go red. A pin that survives its own mutation is not a pin.
5. `npx tsc --noEmit` clean, the architecture, monolith and parity guards green, the census RESULT PASS, `npm run ci:changed` after the LAST commit.
6. `node scripts/gate_select.mjs` on the COMMITTED tree, pg-armed, judged by exit code.
7. `docs/prd/masterwrought/phase-19b-qa.md` written and its verdict recorded before the next wave opens.

## Standing rules

NO push, NO PR, NO teardown. Local only. A green gate is not consent.
