# Phase 19C: Server, persistence and stability

Opened 2026-09-01 under ruling qr-19-best-for-project. The heaviest risk in the packet by hour: two write-ordering fixes on the hottest save path, and one cluster that turns on a new production write per market sale.

**7 units, about 41.00 hours estimated.** Every row here was answered in the Phase 19 rulings gate; this phase executes, it does not re-decide. A row whose ruling turns out to rest on a wrong premise is an ESCALATION back to the ledger, never a quiet re-ruling.

## Units

### D023

Ratify the recorded acceptance that a plot ripening during the linkdead grace window loses its transient ripe notice permanently, since every durable surface shows the truth on resume and both alternatives collapse into building a connectivity concept the deterministic sim deliberately lacks.

- **Files:** `docs/prd/masterwrought/farming/state.md`, `src/sim/professions/farm_ready.ts`, `src/sim/professions/farm_persist.ts`, `src/sim/professions/farm_projection.ts`
- **Same-change obligations:** ANCHOR: put the RULED (qr-19-linkdead-notice-loss, 2026-09-01) line AFTER the unique line "  (bb) LINKDEAD ACCEPTANCE, maintainer read owed at feature review: a plot" in farming/state.md (grep -cF = 1). HANDOFF ROW to flip, farming/state.md line 475, match on the exact full row "  (bb) linkdead transient-notice loss acceptance   P8   maintainer   open ruling-owed  " (grep -cF = 1); the replacement status cell is "RATIFIED 2026-09-01 by qr-19-linkdead-notice-loss". Also strike (bb) from the maintainer-reads-owed enumeration in the same edit: that list is the wrapped sentence at farming/state.md:412 reading "(ap) the farmNowMs facet member; (bb) the linkdead notice loss;". PREMISE RE-VERIFIED: the once-only mechanism is still the persisted notified flag (src/sim/professions/farm_ready.ts:20-42 states the contract in terms, :96 and :117 are the read and the flip), it is still persisted (farm_persist.ts:152 and :304) and still projected (farm_projection.ts:44 and :194), so a notice dropped in flight is indeed unrecoverable by any login re-check. SAME-CHANGE OBLIGATIONS ON THE RATIFICATION: none. No wiki regen, no M16 fills, no deed row, no Reliquary page, no art park, no census row, no parity golden, no world_api parity member, no monolith ceiling, R5 untouched. THE FUND ARM, priced honestly so the ratification is a choice and not a shrug: a delivery or connectivity concept is new sim state that must behave identically in the offline Sim, the online server and the RL host, it must be persisted and replayed on resume, it re-seats every parity golden that touches farming, and it puts a per-player delivery record on a 20 Hz tick, which is the shape the server hot-path rule bars. Call it forty hours and a re-opened determinism surface, against a lost transient toast whose information the journal, the map pins and the fplot status all still carry. The smaller sibling the block names (a disconnect inside the one tick between addPlayer and its event drain) has the identical cause and rides the same ruling.
- **AMENDED IN PLACE 2026-09-01 at execution, four corrections measured against the tree, none re-ruling anything.** (1) The handoff-row anchor is quoted with its markdown pipes flattened to spaces, so `grep -cF` on the quoted flat form returns 0; the row that greps 1 is the pipe form `| (bb) linkdead transient-notice loss acceptance | P8 | maintainer | open ruling-owed |`. (2) The handoff-row address 475 is stale; the row reads at farming/state.md:536. (3) The reads-owed enum quote `(ap) the farmNowMs facet member; (bb) the linkdead notice loss;` greps 0 because the file hard-wraps it: `(ap)` closes line 412 and the `(bb) the linkdead notice loss;` clause sits on line 413. (4) That address is :413, not :412. PREMISE re-verified CORRECT: farm_ready.ts states the once-only `notified` contract (:20-42), reads it (:96) and flips it (:117); it persists (farm_persist.ts:152, :304) and projects (farm_projection.ts:44, :194), so a dropped notice is unrecoverable. The ratification stands as recommended.
- **Reviewers:** qa-checklist
- **Depends on:** nothing
- **Estimate:** 1h

### D064

Rule the sim-only encounterOwned aura marker server-internal until an encounter applies one to a player (the recommendation, a dated ledger line and one trigger comment), where the alternative is a genuine wire change in the aura snapshot encode/decode pair.

- **Files:** `docs/prd/masterwrought/state.md`, `src/sim/types.ts`, `server/snapshot_timer_wire.ts`, `src/net/aura_wire_decode.ts`, `server/game.ts`, `tests/snapshots.test.ts`, `tests/parity/golden/ignivar_raid_tuning.json`
- **Same-change obligations:** RULING RECORD, two homes, both verified unique with grep -c = 1 in state.md. PRIMARY anchor (the RECORDED-NOT-ACTED list Phase 18 already annotated in this exact shape): "wire (a one-byte design call if an encounter ever applies one to players);" and the RULED line goes AFTER it, at column 0, in the established form "RULED (qr-19-encounter-owned-aura-wire, 2026-09-01, under qr-19-best-for-project):" plus the reasoning wrapped near 72 columns. SIBLING anchor in the eighth-sync AMENDED block: "implies), recorded as a maintainer read, a one-byte design call if an", line AFTER it. No handoff table row exists for this row (it is a sweep-raised prose read, not a handoff class), so no status cell to flip. NO content obligations under the recommended arm: no wiki regen, no M16 fills, no deed rows, no Reliquary pages, no art parks, no census rows, no parity goldens, no monolith ceilings. Optional and cheap: one sentence at the Aura.encounterOwned field comment (src/sim/types.ts:669) naming the ruling id and the trigger, which is where a future reader will actually look. LIVE CODE RE-VERIFIED THIS SESSION so the row's evidence half still holds: encounterOwned appears only in src/sim (types.ts:669, aura_classify.ts:87 and :90, ignivar_forge_chains.ts, encounters/varkhul.ts, encounters/ignivar.ts) and in tests; zero hits in server/, src/net/, src/world_api/. IF THE MAINTAINER SERIALIZES INSTEAD, the price is bigger than the row states and the shape is already sitting there to copy: the sibling flag undispellable rides as presence-only through server/snapshot_timer_wire.ts at the record type (:32), the equality check (:121), the record build (:147) and TWO encode sites (:176 and :567), decoded at src/net/aura_wire_decode.ts:124 and :154, so encounterOwned is a five-site server edit plus a two-site client edit, not one byte in one place. That arm also owes a re-record of every parity golden carrying an aura block (tests/parity/golden/ignivar_raid_tuning.json holds three encounterOwned rows today), an arm in tests/snapshots.test.ts beside the existing Shared Pyre reconnect test at :7560, and the deploy-window question (an un-restarted server emits no flag, so the client must treat absence as false, which the presence-only idiom already does). No IWorld member moves, so tests/world_api_parity.test.ts is untouched either way.
- **AMENDED IN PLACE 2026-09-01 at execution:** both anchors verified CORRECT (the primary at state.md:24130, the eighth-sync-block sibling at :22938, each `grep -cF` = 1) and the premise re-verified: encounterOwned appears only under src/sim and tests (types.ts:669, aura_classify.ts, the three encounter modules, ignivar_forge_chains.ts) with ZERO hits in server/, src/net/ or src/world_api/. The recommended keep-internal arm lands one comment at src/sim/types.ts:669 and the two state.md ledger lines; none of the serialize-arm files is touched. One observation, not a defect: the Reviewers line names qa-checklist twice; dispatched once.
- **Reviewers:** qa-checklist, qa-checklist LAST
- **Depends on:** nothing
- **Estimate:** 1.5h

### D122

Re-mint CHARACTER_BLOB_WARN_BYTES above the measured legal worst case (the constant sits at 131,072 against a measured 151,525, so a maxed character prints the oversized-save warn on every autosave) and name the shipped p99 windowed gauge as the real fleet-creep watch.

- **Files:** `server/character_blob_size.ts`, `tests/character_blob_size.test.ts`, `tests/professions_blob_growth.test.ts`, `tests/server/character_save_statement.test.ts`
- **Same-change obligations:** STOPPING-RULE RISK, flag before executing: the raise needs a NUMBER the maintainer has not given. Either he names it, or the ruling adopts a stated derivation (for example the next power-of-two step above the measured band, 163,840, which still sits one step below the 262,144 guild-bank scale the existing rationale leans on). Do not guess it. RE-MINT DOCTRINE is a same-change obligation, written into the constant's own comment: re-mint WITH a fresh measurement and re-read tests/professions_blob_growth.test.ts's whole-character arm in the SAME change, because that arm asserts the measured relation and reds on a re-mint BY DESIGN. Current live figures to re-derive rather than paste: measured 151,525 bytes, band 151,145 to 151,526 (the test's own re-based paragraph), threshold 131,072 at about 0.87x. PINS THAT MOVE: tests/character_blob_size.test.ts:110 (the literal), :137 (the `< WARN/4` relation), :201, :326-327 (OVER/UNDER); tests/professions_blob_growth.test.ts:1834 plus its 15-line rationale comment; tests/server/character_save_statement.test.ts:91. PINS THAT DO NOT MOVE: the scrape names woc_character_state_bytes_max and woc_character_state_bytes_p99 and their game_metrics tests, since the gauge stays as shipped. No DDL and no characters.state serialize/deserialize change, so migration-safety is optional rather than required. Dev-channel English only, so no t() key and no i18n obligation. No content family obligations, no monolith ceiling. RECORDING: dated RULED line, id qr-19-character-blob-warn-threshold. ANCHOR: `row D122, and it is a real decision rather than a clerical one` in server/character_blob_size.ts (grep -c == 1); the RULED line goes AFTER the paragraph it opens (the block ends `What changed here is only the description, which was false.`), and that whole WHAT THIS COMMENT DELIBERATELY DOES NOT DO paragraph is REWRITTEN by this ruling rather than amended, since it exists only to say the decision is pending. No handoff-table row (Seed row column reads `new`).
- **AMENDED IN PLACE 2026-09-01 at execution, two corrections.** (1) The example derivation "163,840 ... the next power-of-two step above the measured band" is arithmetically wrong: 163,840 is NOT a power of two (the ladder is 2^17 = 131,072 and 2^18 = 262,144, nothing between). The value adopted is 163,840 = 160 KiB under a CORRECTED stated derivation: the smallest 32-KiB-aligned step strictly above the measured legal worst case, which keeps a full 32-KiB step below the 262,144-byte (256 KiB) guild-bank scale the existing rationale treats as the largest plausible single row. This is a WARN threshold that never blocks a write, so a stated-derivation value is ADOPTED rather than escalated, and the maintainer may override the digits. (2) The cited "measured 151,525 bytes, band 151,145 to 151,526" is stale: the QA twin RE-BASED at the phase close to 151,584 bytes, band 151,204..151,584 (tests/professions_blob_growth.test.ts, toBeGreaterThan(151203) / toBeLessThan(151585)); the rulings-gate row's 151,495 is staler still. Per the row's own "re-mint WITH a fresh measurement" doctrine, the re-mint is measured against the live figure. Pin addresses verified CORRECT (character_blob_size.test.ts:110/:137/:201/:326-327, professions_blob_growth.test.ts:1834, character_save_statement.test.ts:91).
- **Reviewers:** privacy-security-review, database-performance-reviewer, test-coverage-auditor, qa-checklist
- **Depends on:** nothing
- **Estimate:** 3h

### D136

Rule whether the live nonce fence gains the expiry qualifier its own sibling arm already carries, and settle the three residues (whether heartbeatCharacterLeases takes the same qualifier in the same commit, whether the term binds all four fenced paths, and what reason an expiry-fenced session is kicked with) or ratify suspend-first and propagate the honest limit to the two sites that overpromise.

- **Files:** `server/character_save_statement.ts (the nonce arm at :117 to :126, no expiry term, against the unleased arm below it which has one; the header comment block at :14 to :30 whose unleased paragraph is what option A rewrites)`, `server/db.ts (liveSaveFence at :3471 and its four callers at :3492, :3596, :3699, :3844; heartbeatCharacterLeases, the B1 sub-decision; acquireCharacterLease's reclaim predicate expires_at < now(), the third disagreeing definition)`, `tests/server/character_save_statement.test.ts (the nonce arms at :38 to :43 are ALL toContain, so they stay GREEN over an appended term while proving nothing: this change owes POSITIVE assertions)`, `tests/character_lease.test.ts (same toContain trap)`, `tests/character_save_statement_pg_integration.test.ts (the nonce describe, now at :626; grantLease at :100 already takes secondsFromNow, so an expired-lease refusal arm is a three-liner)`, `OPTION A ONLY: server/CLAUDE.md persistence bullet, /Users/fernando/Documents/wocc-masterwrought/DEPLOY.md operator flow, with the honest paragraph already written verbatim at /Users/fernando/Documents/wocc-masterwrought/server/clear_item_name.ts:56 to :63 to copy from`
- **Same-change obligations:** MONOLITH: server/db.ts is on the ratchet with ceiling 5123 and the row's own words 'Exact count, zero slack', and wc -l measures EXACTLY 5123 today, so the B1 qualifier must ride the existing WHERE holder = $1 line with no added comment line, or the same commit owes an extraction. No wiki regen, no M16 fills, no deed rows, no Reliquary pages, no art parks, no census rows, no parity goldens (IWorld is untouched). i18n is owed ONLY if the maintainer names a DISTINCT kick reason rather than reusing the takeover literal: then an English errors.api row in src/ui/i18n.catalog plus the src/ui/api_error_i18n.ts arm, the LEAVE_REASON_KEY row in src/ui/server_i18n.ts with its two fallback rows, all in the SAME change under the S3 guard (tests/localization_fixes.test.ts), locale fills riding Phase 20, plus a server/http/error_codes.ts row only if that reason ever reaches a REST answer. Behavior price to state on the record: a fence miss is not a quiet skip, it reaches the revert-count-kick arm in server/game.ts, the escrow twin, and the paid-guild path. RECORDING: dated RULED line qr-19-nonce-fence-expiry-term at the Phase 13 QA RECORDED-NOT-ACTED bullet in /Users/fernando/Documents/wocc-masterwrought/docs/prd/masterwrought/state.md. ANCHOR (grep -c 1, line 20780): 'the operator answer stays suspend-first'; the dated line goes AFTER that bullet's closing '(DEPLOY.md).' line, in the same RECORDED-NOT-ACTED style its four neighbours use for their qr-18-REOPEN lines. NO handoff-table row exists (a nonce grep over docs/prd/masterwrought returns only the state.md bullet, phase-18-perfection-sweep.md:82 and the phase-19 seed list), so no status cell to flip. VALUES THIS ROW CANNOT GUESS: the reason string, whether B1 rides B's commit, and B2's scope are all maintainer-named, and a ruling that needs an ungiven value goes back to the maintainer in-session.
- **AMENDED IN PLACE 2026-09-01 at execution, one address correction plus the residue dispositions.** (1) The anchor "the operator answer stays suspend-first" is at state.md:21410, not :20780; it greps 1 and its closing "(DEPLOY.md)." line is :21411, where the dated RULED line goes. The three residues, settled: B2 (scope) is DERIVED, not guessed: liveSaveFence builds one shared nonce shape for all four callers, so the term binds all four by construction and narrowing to autosave-only would cost a fifth shape; bind all four. The kick REASON is DERIVED: reuse the shipped takeover literal (matcher-covered at api_error_i18n.ts and server_i18n.ts), so no new i18n and no maintainer-named string. B1 (whether heartbeatCharacterLeases takes the same qualifier) is CARRIED to the maintainer, NOT taken: unqualified reopens the window within one heartbeat tick, qualified can make every session a stalled process holds permanently unsaveable after one lapse past the 90 s TTL, a blast the row reserves for the maintainer. The executed arm (B) adds `AND expires_at > now()` to the nonce EXISTS in server/character_save_statement.ts (NOT on the monolith ratchet), leaves the heartbeat unqualified (B1's safe default), and makes the nonce, unleased and reclaim definitions of a live lease agree; the residual (an unqualified heartbeat re-arms a lapsed lease before the next save) is recorded honestly.
- **Reviewers:** privacy-security-review, migration-safety, database-performance-reviewer, cross-platform-sync, qa-checklist
- **Depends on:** nothing
- **Estimate:** 10h

### D145

Rule whether the live autosave takes the characters row lock before its nonce fence is evaluated, matching the offline fix the Phase 18 QA already landed and red-proved, or the packet accepts the residual write loss and says so at the nonce arm instead of leaving a silence that reads as a guarantee.

- **Files:** `server/character_save_statement.ts, the nonce arm at :117 to :126 (the uncorrelated EXISTS Postgres hoists into an InitPlan, evaluated BEFORE the row lock)`, `server/db.ts, liveSaveFence at :3471 and its four callers at :3492 (saveCharacterState, the 30 s autosave), :3596, :3699 and :3844`, `server/offline_character_save_db.ts, the shape to match (the 'SELECT 1 FROM characters WHERE id = $1 AND realm = $2 FOR UPDATE' statement at :100 with its mode rationale at :57 to :60)`, `tests/character_save_statement_pg_integration.test.ts, the nonce describe at :626 onward, where the displacement race arm belongs (grantLease at :100 already takes secondsFromNow)`, `tests/server/save_offline_character_state.test.ts, the statement-sequence precedent an inserted lock statement must be pinned against by text AND position`, `tests/monolith_budget.test.ts, the server/db.ts row`
- **Same-change obligations:** MONOLITH, the hard one: server/db.ts is pinned at ceiling 5123 with 'Exact count, zero slack' and wc -l measures EXACTLY 5123 today, so ANY added statement in the four live save paths reds the ratchet. The fix is extraction behind the file's seam (the offline arm's own precedent moved whole to server/offline_character_save_db.ts and LOWERED the ceiling), never a raise, and after extracting the ceiling is lowered. Budget that extraction as part of this row, not as a surprise. Also owed: a real-Postgres arm reproducing the displacement race (grant nonce-A, contender holds the row FOR NO KEY UPDATE, park the fenced save, displace the lease to nonce-B mid-wait, release; the write must NOT land), red-proven by removing the lock statement; and a re-read of the autosave latency budget, because the lock is taken on EVERY save, not only contended ones, at roughly 33 extra round trips a second at 1,000 online on a box where Postgres shares four cores with the 20 Hz loop. NO wiki regen, no M16 fills, no deed rows, no Reliquary pages, no art parks, no census rows, no parity goldens (IWorld untouched), no i18n unless a new kick reason string is named (then the same S3-guarded matcher chain D136 prices). RECORDING: dated RULED line qr-19-live-nonce-fence-write-loss at BOTH places the read stands. ANCHORS, each grep -c 1 in /Users/fernando/Documents/wocc-masterwrought/docs/prd/masterwrought/state.md: 'Phase 19 row D145 with the reproduction and the price.' (the Phase 18 RECORDED-NOT-ACTED bullet; dated line AFTER it) and 'a second: Phase 19 row D145.' (the QA twin's WHAT THE TWIN FOUND bullet; dated line AFTER it). No handoff-table row exists, so no status cell to flip. IF THE MAINTAINER ACCEPTS THE RESIDUAL, the no-op arm is still not free: the nonce arm owes an honest comment saying the fence is evaluated before the lock, because the current silence beside a documented offline fix reads as a guarantee the statement does not make.
- **AMENDED IN PLACE 2026-09-01 at execution:** both anchors verified CORRECT (state.md:25572 the Phase 18 RECORDED-NOT-ACTED bullet, :25734 the QA-twin bullet, each `grep -cF` = 1) and every cited code address re-measured correct (character_save_statement.ts nonce arm :117-126; liveSaveFence callers db.ts :3492/:3596/:3699/:3844; offline_character_save_db.ts row lock at :100 with rationale :57-60; the pg nonce describe :626). Executed: the THREE DIRECT live save paths take `SELECT 1 FROM characters WHERE id = $1 AND realm = $2 FOR NO KEY UPDATE` before the fenced UPDATE (the review round changed the mode from FOR UPDATE to FOR NO KEY UPDATE, since the ordering is the fix and the offline arm's stronger mode would stall FK-child inserts and buy nothing for the takeover case). PREMISE CORRECTED at the gate: the row's "four live save paths" is falsified by the escrow occupancy invariant. The fourth path, saveCharacterStateOnClient (the marketplace escrow / directed / delivered / paid-guild caller), is EXCLUDED, because the lock's extra workload statement pushes the escrow base occupancy (ESCROW_STATEMENT_TIMEOUT_MS x 6 + lock + connect = 31,000) past the 30,000 ms autosave-period ceiling the tunables ladder pins (baseWorkloadCeilingMs < autosaveMs, tests/server/tunables.test.ts). It keeps its pre-existing InitPlan behaviour (no regression) and its race is CARRIED to the maintainer (re-tune ESCROW_STATEMENT_TIMEOUT_MS or accept the residual). MONOLITH paid: the lock-then-update concern moves to a sibling module and server/db.ts is unchanged at its 5123 ceiling. The displacement-race pg arm reds without the lock and passes with it.
- **Reviewers:** privacy-security-review, migration-safety, database-performance-reviewer, server-hot-path-reviewer, test-coverage-auditor, qa-checklist
- **Depends on:** D136
- **Estimate:** 12h

### D147

Wire the four absent market sold-volume seams together (schema into ensureSchema, marketSoldVolumeRetentionTable into the nightly sweep array, both configure calls at boot, buyWithSoldVolume at the market_buy dispatch), flip the four-absence source guard to a presence guard, and add the real-Postgres end-to-end arm, so the admin dashboard's permanently dark volume half turns on.

- **Files:** `server/db.ts`, `server/main.ts`, `server/game.ts`, `server/market_sold_volume.ts`, `server/market_sold_volume_db.ts`, `server/admin_market_metrics.ts`, `tests/server/market_sold_volume.test.ts`, `tests/market_sold_volume_pg_integration.test.ts`, `tests/server/admin_market_metrics.test.ts`, `docs/prd/masterwrought/phase-19-rulings-gate.md`
- **Same-change obligations:** SEAM COORDINATES verified live: MARKET_SOLD_VOLUME_SCHEMA into the ensureSchema ladder (server/db.ts, ensureSchema at :1286); marketSoldVolumeRetentionTable(pool) into the tables array createRetentionSweep is given (server/main.ts:3884, sweep started at :4088); configureMarketSoldVolume (server/market_sold_volume.ts:151) and configureAdminMarketSoldVolume (server/admin_market_metrics.ts:276) called at boot; the market_buy dispatch arm at server/game.ts:7822 calls buyWithSoldVolume (server/market_sold_volume.ts:244) instead of sim.marketBuy. THE GUARD THAT MUST FLIP, not be deleted: UNWIRED_SEAMS at tests/server/market_sold_volume.test.ts:276-336, four it.each source-text arms asserting the absent symbol is NOT in each file; wiring reds all four and they re-pin as presence arms. PERF SEAM: buyWithSoldVolume runs findListing twice over sim.marketListings per buy on the WS command path; hold one pre-buy read or add an id index before landing (server-hot-path-reviewer's rule). ALREADY PRESENT, do not re-invent: the FIFO admission cap (SOLD_VOLUME_TAIL_MAX_DEPTH 2000, market_sold_volume.ts:115) and the tail-drop counters. PROSE THAT BECOMES A LIE IN THE SAME CHANGE: server/market_sold_volume_db.ts:27-61 (the RETENTION, AND WHAT IS STILL OWED and WIRED TO NOTHING blocks name all four absences by number). NOT OWED, each checked rather than assumed: no wiki regen; no i18n key and no M16 fills (src/admin/pages/MarketMetrics.svelte:57 and :74 already carry BOTH the unavailable and available branches, pinned at tests/admin/market_metrics_page.test.ts:250-252); no deed record; no Reliquary page; no committed WebP art; no census row; no parity golden; no IWorld member; no monolith ceiling (none of these files carries a row). RULED LINE ANCHOR (grep -c = 1): '  D147   sold-volume-four-seam-wiring  ' in docs/prd/masterwrought/phase-19-rulings-gate.md; the dated line goes INSIDE that row's Ruling cell, and the row has NO farming handoff-table row (its seed is 'new', the ledger recorded the four call sites as routed to holders that never landed). Ruling id qr-19-sold-volume-four-seam-wiring.
- **AMENDED IN PLACE 2026-09-01 at execution, two corrections.** (1) The gate-anchor `D147   sold-volume-four-seam-wiring` is quoted with its pipes flattened, so `grep -cF` returns 0; the row is the pipe form at phase-19-rulings-gate.md:182 and the dated RULED line goes in its Ruling cell. (2) The MONOLITH claim "none of these files carries a row" is FALSE: server/db.ts (ceiling 5123, zero slack) and server/game.ts (ceiling 10350, zero slack) are BOTH on the ratchet, and wiring grows both (db.ts gains the MARKET_SOLD_VOLUME_SCHEMA import and its ensureSchema application; game.ts gains the buyWithSoldVolume import). server/main.ts is correctly NOT on the ratchet. Both growths are PAID by extraction with the ceilings LOWERED to the new exact counts, per the ratchet rule. The four seam coordinates verified live (ensureSchema at db.ts:1286, the retention tables array at main.ts:3884, configureMarketSoldVolume at market_sold_volume.ts:151 / configureAdminMarketSoldVolume at admin_market_metrics.ts:276, the market_buy dispatch at game.ts calling buyWithSoldVolume:244); the UNWIRED_SEAMS guard flips from four absence arms to presence.
- **Reviewers:** privacy-security-review, migration-safety, database-performance-reviewer, server-hot-path-reviewer, cross-platform-sync, qa-checklist
- **Depends on:** nothing
- **Estimate:** 12h

### D162

Ratify the already shipped combined ONLINE_WORLD_LAYOUT_VERSION 26 as the one masterwrought and farming go-live epoch and take the cheap rider, one exemplar paragraph in docs/ota-updates.md saying this is a bumping release so the server deploys before the OTA bundle.

- **Files:** `docs/ota-updates.md`, `docs/prd/masterwrought/farming/state.md`, `docs/prd/masterwrought/progress.md`, `src/world_api.ts`, `tests/bank_wire_epoch.test.ts`, `tests/security.test.ts`, `tests/world_auth_scripts.test.ts`, `scripts/lib/world_auth.mjs`, `scripts/lib/world_auth.d.mts`
- **Same-change obligations:** SCOPE NOTE: under the recommended arm (ratify plus the docs rider) NO code lands. The last six files listed are the bill ONLY if the maintainer picks the split-to-27 arm or the re-derive-at-final-sync arm: the constant and its numbered comment in src/world_api.ts, the three exact-value pins (tests/bank_wire_epoch.test.ts including its not-toBe ladder, tests/security.test.ts, tests/world_auth_scripts.test.ts) and the two hand-maintained Node mirrors (scripts/lib/world_auth.mjs and world_auth.d.mts, the second read back byte-for-byte). SAME-CHANGE OWED under the recommendation: the docs/ota-updates.md paragraph written under the anchor rule (stable paths and symbols, no line numbers or counts), beside the existing epoch-11 exemplar, plus the ledger stamps. NOT OWED: no wiki regen, no t() key and no M16 fills (the refusal string is already shipped and matcher-localized), no deed, no Reliquary page, no art, no census row, no parity golden, no world_api parity pin, no monolith ceiling. SEQUENCING: answer in the same sitting as D133, whose display-only option defers its own epoch call to this row, and rule this one first. ANCHOR (grep -c = 1) in /Users/fernando/Documents/wocc-masterwrought/docs/prd/masterwrought/farming/state.md: 'ONLINE_WORLD_LAYOUT_VERSION at farming go-live'; the dated RULED line goes AFTER that bullet's closing line in the OPEN list. HANDOFF ROW, match exactly: '  ONLINE_WORLD_LAYOUT_VERSION epoch bump at farming go-live (dated note 2026-08-30: the epoch has since moved repeatedly and reads 26 after the eighth v0.41.0 sync, which renumbered the branch's 12 past the release's raid ladder 13 to 25; the go-live question this row records stands unchanged)   19th absorb   maintainer   open ruling-owed  '. STATUS CELL REPLACEMENT: 'open ruling-owed' becomes 'CLOSED 2026-09-01 by ruling qr-19-epoch-bump-at-golive (Phase 19, under qr-19-best-for-project): the shipped combined epoch 26 is RATIFIED as the one masterwrought and farming go-live bump, and the server-first deploy order is written into docs/ota-updates.md as a named exemplar.'
- **AMENDED IN PLACE 2026-09-01 at execution, one correction.** (1) The handoff-row anchor is quoted with its pipes flattened, so `grep -cF` returns 0; the row that greps 1 is the pipe form `| ONLINE_WORLD_LAYOUT_VERSION epoch bump at farming go-live (...) | 19th absorb | maintainer | open ruling-owed |` at farming/state.md:555. The OPEN-list anchor "ONLINE_WORLD_LAYOUT_VERSION at farming go-live" greps 1 at farming/state.md:4680 and the dated RULED line goes after that bullet's closing line (:4685). Verified: src/world_api.ts:190 already reads 26 and all three exact-value pins already read 26, so the ratification lands NO code; the docs rider is written under the anchor rule beside the epoch-11 exemplar in docs/ota-updates.md. Sequencing note: D133 is not a 19C unit, so its display-only option's deferral to this row is satisfied whenever D133 is ruled.
- **Reviewers:** cross-platform-sync, qa-checklist
- **Depends on:** nothing
- **Estimate:** 1.5h

## Exit criteria

1. Every unit above executed, or escalated with a dated reason.
2. Each unit's domain reviewers dispatched FRESH (never the implementer), all findings applied: blocking, should-fix and nits alike.
3. The fix round re-read by a fresh reader, because fixes are unreviewed code.
4. Every new pin mutated and watched go red. A pin that survives its own mutation is not a pin.
5. `npx tsc --noEmit` clean, the architecture, monolith and parity guards green, the census RESULT PASS, `npm run ci:changed` after the LAST commit.
6. `node scripts/gate_select.mjs` on the COMMITTED tree, pg-armed, judged by exit code.
7. `docs/prd/masterwrought/phase-19c-qa.md` written and its verdict recorded before the next wave opens.

## Standing rules

NO push, NO PR, NO teardown. Local only. A green gate is not consent.
