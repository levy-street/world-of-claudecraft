// Narrow public barrel for procedural equipment content. The implementation is
// split into the sibling directory so persisted content IDs remain reviewable
// without growing the existing monolithic item table.
export * from './procedural_loot/index';
export * from './procedural_loot/item_defs';
