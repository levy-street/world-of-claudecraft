// Neutral scene wire bounds shared by the authoritative sim, browser mirror,
// and headless observation surface. Keep runtime protocol constants here so
// hosts cannot silently drift by repeating literals.

export const MAX_SCENE_CHOICE_OPTIONS = 4;
